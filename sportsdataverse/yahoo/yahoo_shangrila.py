# GENERATED by tools/codegen/generate.py -- DO NOT EDIT.
# Source: tools/codegen/endpoints/yahoo_shangrila.yaml
"""yahoo_shangrila wrappers (host https://graphite-secure.sports.yahoo.com/v1/query/shangrila). Generated."""

from __future__ import annotations

from typing import TYPE_CHECKING, Dict, List, Optional, Union  # noqa: F401

from sportsdataverse.yahoo.yahoo_shangrila_runtime import _get
from sportsdataverse.yahoo.yahoo_shangrila_parsers import (
    parse_yahoo_editorial,
    parse_yahoo_shangrila,
    parse_yahoo_shangrila_tables,
)

if TYPE_CHECKING:  # pragma: no cover -- annotation-only imports (PEP 563 defers eval)
    import pandas as pd
    import polars as pl

__all__ = [
    "yahoo_oly_medal_count",
    "yahoo_oly_seasons",
    "yahoo_alias",
    "yahoo_article_list_card_players",
    "yahoo_article_list_card_teams",
    "yahoo_basic_players",
    "yahoo_betting_disclaimer",
    "yahoo_combat_event_fights",
    "yahoo_combat_schedule",
    "yahoo_common_pills",
    "yahoo_consensus_rankings_php",
    "yahoo_draft",
    "yahoo_draft_prospects",
    "yahoo_driver_results",
    "yahoo_driver_splits",
    "yahoo_featured_game_ids",
    "yahoo_game_prop_bets",
    "yahoo_game_stats_leaders",
    "yahoo_gametime_game",
    "yahoo_gametime_team",
    "yahoo_golf_tournament_seasons",
    "yahoo_golf_tournaments",
    "yahoo_golf_tournaments_basic",
    "yahoo_league_conferences",
    "yahoo_league_filters_data",
    "yahoo_league_future_odds",
    "yahoo_league_game_ids",
    "yahoo_league_game_ids_by_date",
    "yahoo_league_games_by_round",
    "yahoo_league_info",
    "yahoo_league_injuries",
    "yahoo_league_names",
    "yahoo_league_prop_odds",
    "yahoo_league_standings",
    "yahoo_league_stats_by_team",
    "yahoo_league_stats_individual",
    "yahoo_league_stats_overview",
    "yahoo_league_stats_weekly",
    "yahoo_league_team_ids",
    "yahoo_league_teams",
    "yahoo_leagues_season_states",
    "yahoo_module_game",
    "yahoo_motorsport_standings",
    "yahoo_nascar_drivers",
    "yahoo_nav_dropdown_tray",
    "yahoo_pick_distribution",
    "yahoo_playbook_boxscore",
    "yahoo_playbook_boxscore_poll",
    "yahoo_playbook_boxscore_social_share",
    "yahoo_playbook_combat_match",
    "yahoo_playbook_game",
    "yahoo_playbook_game_odds_poll",
    "yahoo_playbook_golf_tournament",
    "yahoo_playbook_league_odds",
    "yahoo_playbook_player",
    "yahoo_playbook_player_social_share",
    "yahoo_playbook_race",
    "yahoo_playbook_team",
    "yahoo_playbook_team_basic",
    "yahoo_playbook_team_social_share",
    "yahoo_playbook_tennis_match",
    "yahoo_player_basic",
    "yahoo_player_career_stats",
    "yahoo_player_game_log",
    "yahoo_player_props",
    "yahoo_player_search",
    "yahoo_player_season_stats",
    "yahoo_playoff_bracket",
    "yahoo_playoff_series_game",
    "yahoo_polymarket_game",
    "yahoo_racing_schedule",
    "yahoo_scoreboard_game",
    "yahoo_season_stats_football_defense_ncaaf",
    "yahoo_season_stats_football_kicking_ncaaf",
    "yahoo_season_stats_football_passing_ncaaf",
    "yahoo_season_stats_football_punting_ncaaf",
    "yahoo_season_stats_football_receiving_ncaaf",
    "yahoo_season_stats_football_returns_ncaaf",
    "yahoo_season_stats_football_rushing_ncaaf",
    "yahoo_season_team_stats_football_defense",
    "yahoo_season_team_stats_football_kicking",
    "yahoo_season_team_stats_football_kickoffs",
    "yahoo_season_team_stats_football_offense",
    "yahoo_season_team_stats_football_passing",
    "yahoo_season_team_stats_football_passing_defense",
    "yahoo_season_team_stats_football_punting",
    "yahoo_season_team_stats_football_receiving",
    "yahoo_season_team_stats_football_receiving_defense",
    "yahoo_season_team_stats_football_returns",
    "yahoo_season_team_stats_football_rushing",
    "yahoo_season_team_stats_football_rushing_defense",
    "yahoo_team_injuries",
    "yahoo_team_playoff_series",
    "yahoo_team_roster",
    "yahoo_team_schedule_by_season",
    "yahoo_team_search",
    "yahoo_team_stats_leaders_v2",
    "yahoo_team_transactions",
    "yahoo_teams_basic",
    "yahoo_tennis_matches_by_date",
    "yahoo_tennis_tournament",
    "yahoo_tennis_tournaments",
    "yahoo_tennis_tournaments_by_date",
    "yahoo_trending_event_ids",
    "yahoo_trending_game_ids",
    "yahoo_editorial_boxscore",
    "yahoo_editorial_scoreboard",
]


def yahoo_oly_medal_count(
    season: Optional[str] = None,
    sort_method: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Yahoo shangrila persisted query `OlyMedalCount` -> one row per `olympics` entry

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/OlyMedalCount``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/OlyMedalCount

    Args:
        season: season query parameter.
        sort_method: sortMethod query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.yahoo.yahoo_shangrila import yahoo_oly_medal_count
            yahoo_oly_medal_count()

        See Also:
            * `cfbfastR`_ - R sister package for college football
            * `Yahoo Sports`_ - data origin (unofficial, undocumented endpoints)

        .. _cfbfastR: https://cfbfastR.sportsdataverse.org
        .. _Yahoo Sports: https://sports.yahoo.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "season": season,
        "sortMethod": sort_method,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/OlyMedalCount",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_oly_seasons(
    seasons: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Yahoo shangrila persisted query `OlySeasons` -> one row per `olympics` entry

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/OlySeasons``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/OlySeasons

    Args:
        seasons: seasons query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.yahoo.yahoo_shangrila import yahoo_oly_seasons
            yahoo_oly_seasons()

        See Also:
            * `cfbfastR`_ - R sister package for college football
            * `Yahoo Sports`_ - data origin (unofficial, undocumented endpoints)

        .. _cfbfastR: https://cfbfastR.sportsdataverse.org
        .. _Yahoo Sports: https://sports.yahoo.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "seasons": seasons,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/OlySeasons",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_alias(
    alias: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Yahoo shangrila persisted query `alias` -> one row per `pageMetaData` entry

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/alias``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/alias

    Args:
        alias: alias query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.yahoo.yahoo_shangrila import yahoo_alias
            yahoo_alias()

        See Also:
            * `cfbfastR`_ - R sister package for college football
            * `Yahoo Sports`_ - data origin (unofficial, undocumented endpoints)

        .. _cfbfastR: https://cfbfastR.sportsdataverse.org
        .. _Yahoo Sports: https://sports.yahoo.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "alias": alias,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/alias",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_article_list_card_players(
    player_ids: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Yahoo shangrila persisted query `articleListCardPlayers` -> one row per `players` entry

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/articleListCardPlayers``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/articleListCardPlayers

    Args:
        player_ids: playerIds query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.yahoo.yahoo_shangrila import yahoo_article_list_card_players
            yahoo_article_list_card_players()

        See Also:
            * `cfbfastR`_ - R sister package for college football
            * `Yahoo Sports`_ - data origin (unofficial, undocumented endpoints)

        .. _cfbfastR: https://cfbfastR.sportsdataverse.org
        .. _Yahoo Sports: https://sports.yahoo.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "playerIds": player_ids,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/articleListCardPlayers",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_article_list_card_teams(
    team_ids: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Yahoo shangrila persisted query `articleListCardTeams` -> one row per `teams` entry

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/articleListCardTeams``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/articleListCardTeams

    Args:
        team_ids: teamIds query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.yahoo.yahoo_shangrila import yahoo_article_list_card_teams
            yahoo_article_list_card_teams()

        See Also:
            * `cfbfastR`_ - R sister package for college football
            * `Yahoo Sports`_ - data origin (unofficial, undocumented endpoints)

        .. _cfbfastR: https://cfbfastR.sportsdataverse.org
        .. _Yahoo Sports: https://sports.yahoo.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "teamIds": team_ids,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/articleListCardTeams",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_basic_players(
    players: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Yahoo shangrila persisted query `basicPlayers` -> one row per `players` entry

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/basicPlayers``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/basicPlayers

    Args:
        players: players query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.yahoo.yahoo_shangrila import yahoo_basic_players
            yahoo_basic_players()

        See Also:
            * `cfbfastR`_ - R sister package for college football
            * `Yahoo Sports`_ - data origin (unofficial, undocumented endpoints)

        .. _cfbfastR: https://cfbfastR.sportsdataverse.org
        .. _Yahoo Sports: https://sports.yahoo.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "players": players,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/basicPlayers",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_betting_disclaimer(
    betting_disclaimer_id: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Yahoo shangrila persisted query `bettingDisclaimer` -> one row per `bettingDisclaimers` entry

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/bettingDisclaimer``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/bettingDisclaimer

    Args:
        betting_disclaimer_id: bettingDisclaimerId query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.yahoo.yahoo_shangrila import yahoo_betting_disclaimer
            yahoo_betting_disclaimer()

        See Also:
            * `cfbfastR`_ - R sister package for college football
            * `Yahoo Sports`_ - data origin (unofficial, undocumented endpoints)

        .. _cfbfastR: https://cfbfastR.sportsdataverse.org
        .. _Yahoo Sports: https://sports.yahoo.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "bettingDisclaimerId": betting_disclaimer_id,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/bettingDisclaimer",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_combat_event_fights(
    event_group_id: Optional[str] = None,
    season: Optional[str] = None,
    league: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Yahoo shangrila persisted query `combatEventFights` -> one row per `leagues` entry

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/combatEventFights``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/combatEventFights

    Args:
        event_group_id: eventGroupId query parameter.
        season: season query parameter.
        league: league query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.yahoo.yahoo_shangrila import yahoo_combat_event_fights
            yahoo_combat_event_fights()

        See Also:
            * `cfbfastR`_ - R sister package for college football
            * `Yahoo Sports`_ - data origin (unofficial, undocumented endpoints)

        .. _cfbfastR: https://cfbfastR.sportsdataverse.org
        .. _Yahoo Sports: https://sports.yahoo.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "eventGroupId": event_group_id,
        "season": season,
        "league": league,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/combatEventFights",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_combat_schedule(
    season: Optional[str] = None,
    league: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Yahoo shangrila persisted query `combatSchedule` -> one row per `leagues` entry

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/combatSchedule``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/combatSchedule

    Args:
        season: season query parameter.
        league: league query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.yahoo.yahoo_shangrila import yahoo_combat_schedule
            yahoo_combat_schedule()

        See Also:
            * `cfbfastR`_ - R sister package for college football
            * `Yahoo Sports`_ - data origin (unofficial, undocumented endpoints)

        .. _cfbfastR: https://cfbfastR.sportsdataverse.org
        .. _Yahoo Sports: https://sports.yahoo.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "season": season,
        "league": league,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/combatSchedule",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_common_pills(
    add_team_logos: Optional[str] = None,
    date: Optional[str] = None,
    team_ids: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Yahoo shangrila persisted query `common/pills` (response body not captured; shape unknown)

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/common/pills``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/common/pills

    Args:
        add_team_logos: addTeamLogos query parameter.
        date: date query parameter.
        team_ids: teamIds query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.yahoo.yahoo_shangrila import yahoo_common_pills
            yahoo_common_pills()

        See Also:
            * `cfbfastR`_ - R sister package for college football
            * `Yahoo Sports`_ - data origin (unofficial, undocumented endpoints)

        .. _cfbfastR: https://cfbfastR.sportsdataverse.org
        .. _Yahoo Sports: https://sports.yahoo.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "addTeamLogos": add_team_logos,
        "date": date,
        "teamIds": team_ids,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/common/pills",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_consensus_rankings_php(
    sport: Optional[str] = None,
    position: Optional[str] = None,
    filters: Optional[str] = None,
    experts: Optional[str] = None,
    scoring: Optional[str] = None,
    type: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Yahoo shangrila persisted query `consensus-rankings.php` (response body not captured; shape unknown)

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/consensus-rankings.php``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/consensus-rankings.php

    Args:
        sport: sport query parameter.
        position: position query parameter.
        filters: filters query parameter.
        experts: experts query parameter.
        scoring: scoring query parameter.
        type: type query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.yahoo.yahoo_shangrila import yahoo_consensus_rankings_php
            yahoo_consensus_rankings_php()

        See Also:
            * `cfbfastR`_ - R sister package for college football
            * `Yahoo Sports`_ - data origin (unofficial, undocumented endpoints)

        .. _cfbfastR: https://cfbfastR.sportsdataverse.org
        .. _Yahoo Sports: https://sports.yahoo.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "sport": sport,
        "position": position,
        "filters": filters,
        "experts": experts,
        "scoring": scoring,
        "type": type,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/consensus-rankings.php",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_draft(
    league: Optional[str] = None,
    season: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Yahoo shangrila persisted query `draft` -> one row per `leagues` entry

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/draft``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/draft

    Args:
        league: league query parameter.
        season: season query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.yahoo.yahoo_shangrila import yahoo_draft
            yahoo_draft()

        See Also:
            * `cfbfastR`_ - R sister package for college football
            * `Yahoo Sports`_ - data origin (unofficial, undocumented endpoints)

        .. _cfbfastR: https://cfbfastR.sportsdataverse.org
        .. _Yahoo Sports: https://sports.yahoo.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "league": league,
        "season": season,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/draft",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_draft_prospects(
    league: Optional[str] = None,
    season: Optional[str] = None,
    image_height: Optional[str] = None,
    image_width: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Yahoo shangrila persisted query `draftProspects` -> one row per `leagues` entry

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/draftProspects``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/draftProspects

    Args:
        league: league query parameter.
        season: season query parameter.
        image_height: imageHeight query parameter.
        image_width: imageWidth query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.yahoo.yahoo_shangrila import yahoo_draft_prospects
            yahoo_draft_prospects()

        See Also:
            * `cfbfastR`_ - R sister package for college football
            * `Yahoo Sports`_ - data origin (unofficial, undocumented endpoints)

        .. _cfbfastR: https://cfbfastR.sportsdataverse.org
        .. _Yahoo Sports: https://sports.yahoo.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "league": league,
        "season": season,
        "imageHeight": image_height,
        "imageWidth": image_width,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/draftProspects",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_driver_results(
    player_id: Optional[str] = None,
    season: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Yahoo shangrila persisted query `driverResults` -> one row per `players` entry

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/driverResults``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/driverResults

    Args:
        player_id: playerId query parameter.
        season: season query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.yahoo.yahoo_shangrila import yahoo_driver_results
            yahoo_driver_results()

        See Also:
            * `cfbfastR`_ - R sister package for college football
            * `Yahoo Sports`_ - data origin (unofficial, undocumented endpoints)

        .. _cfbfastR: https://cfbfastR.sportsdataverse.org
        .. _Yahoo Sports: https://sports.yahoo.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "playerId": player_id,
        "season": season,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/driverResults",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_driver_splits(
    player_id: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Yahoo shangrila persisted query `driverSplits` -> one row per `players` entry

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/driverSplits``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/driverSplits

    Args:
        player_id: playerId query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.yahoo.yahoo_shangrila import yahoo_driver_splits
            yahoo_driver_splits()

        See Also:
            * `cfbfastR`_ - R sister package for college football
            * `Yahoo Sports`_ - data origin (unofficial, undocumented endpoints)

        .. _cfbfastR: https://cfbfastR.sportsdataverse.org
        .. _Yahoo Sports: https://sports.yahoo.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "playerId": player_id,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/driverSplits",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_featured_game_ids(
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Yahoo shangrila persisted query `featuredGameIds` -> one row per `featuredGames` entry

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/featuredGameIds``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/featuredGameIds

    Args:
        return_parsed: parse the payload through parse_yahoo_shangrila -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.yahoo.yahoo_shangrila import yahoo_featured_game_ids
            yahoo_featured_game_ids()

        See Also:
            * `cfbfastR`_ - R sister package for college football
            * `Yahoo Sports`_ - data origin (unofficial, undocumented endpoints)

        .. _cfbfastR: https://cfbfastR.sportsdataverse.org
        .. _Yahoo Sports: https://sports.yahoo.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {}
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/featuredGameIds",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_game_prop_bets(
    game_id: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Yahoo shangrila persisted query `gamePropBets` -> one row per `games` entry

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/gamePropBets``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/gamePropBets

    Args:
        game_id: gameId query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.yahoo.yahoo_shangrila import yahoo_game_prop_bets
            yahoo_game_prop_bets()

        See Also:
            * `cfbfastR`_ - R sister package for college football
            * `Yahoo Sports`_ - data origin (unofficial, undocumented endpoints)

        .. _cfbfastR: https://cfbfastR.sportsdataverse.org
        .. _Yahoo Sports: https://sports.yahoo.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "gameId": game_id,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/gamePropBets",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_game_stats_leaders(
    game_id: Optional[str] = None,
    season: Optional[str] = None,
    season_phases: Optional[str] = None,
    qualified: Optional[str] = None,
    count: Optional[str] = None,
    is_pregame: Optional[str] = None,
    team_image_height: Optional[str] = None,
    team_image_width: Optional[str] = None,
    player_image_height: Optional[str] = None,
    player_image_width: Optional[str] = None,
    baseball_leader_sort_stat0: Optional[str] = None,
    baseball_leader_sort_stat1: Optional[str] = None,
    baseball_leader_sort_stat2: Optional[str] = None,
    baseball_leader_sort_stat3: Optional[str] = None,
    baseball_leader_sort_stat4: Optional[str] = None,
    baseball_leader_stat_ids0: Optional[str] = None,
    baseball_leader_stat_ids1: Optional[str] = None,
    baseball_leader_stat_ids2: Optional[str] = None,
    baseball_leader_stat_ids3: Optional[str] = None,
    baseball_leader_stat_ids4: Optional[str] = None,
    baseball_player_stat_ids0: Optional[str] = None,
    baseball_player_stat_ids1: Optional[str] = None,
    baseball_team_sort_stat0: Optional[str] = None,
    baseball_team_sort_stat1: Optional[str] = None,
    baseball_team_sort_stat2: Optional[str] = None,
    baseball_team_sort_stat3: Optional[str] = None,
    baseball_team_sort_stat4: Optional[str] = None,
    baseball_team_sort_stat5: Optional[str] = None,
    baseball_team_sort_stat6: Optional[str] = None,
    baseball_team_sort_stat7: Optional[str] = None,
    baseball_team_sort_stat8: Optional[str] = None,
    baseball_team_sort_stat9: Optional[str] = None,
    baseball_team_sort_stat10: Optional[str] = None,
    baseball_team_sort_stat11: Optional[str] = None,
    baseball_team_stat_ids0: Optional[str] = None,
    baseball_team_stat_ids1: Optional[str] = None,
    baseball_team_stat_ids2: Optional[str] = None,
    baseball_team_stat_ids3: Optional[str] = None,
    baseball_team_stat_ids4: Optional[str] = None,
    baseball_team_stat_ids5: Optional[str] = None,
    baseball_team_stat_ids6: Optional[str] = None,
    baseball_team_stat_ids7: Optional[str] = None,
    baseball_team_stat_ids8: Optional[str] = None,
    baseball_team_stat_ids9: Optional[str] = None,
    baseball_team_stat_ids10: Optional[str] = None,
    baseball_team_stat_ids11: Optional[str] = None,
    basketball_leader_sort_stat0: Optional[str] = None,
    basketball_leader_sort_stat1: Optional[str] = None,
    basketball_leader_sort_stat2: Optional[str] = None,
    basketball_leader_sort_stat3: Optional[str] = None,
    basketball_leader_sort_stat4: Optional[str] = None,
    basketball_leader_stat_ids0: Optional[str] = None,
    basketball_leader_stat_ids1: Optional[str] = None,
    basketball_leader_stat_ids2: Optional[str] = None,
    basketball_leader_stat_ids3: Optional[str] = None,
    basketball_leader_stat_ids4: Optional[str] = None,
    basketball_player_stat_ids0: Optional[str] = None,
    basketball_team_sort_stat0: Optional[str] = None,
    basketball_team_sort_stat1: Optional[str] = None,
    basketball_team_sort_stat2: Optional[str] = None,
    basketball_team_sort_stat3: Optional[str] = None,
    basketball_team_sort_stat4: Optional[str] = None,
    basketball_team_sort_stat5: Optional[str] = None,
    basketball_team_sort_stat6: Optional[str] = None,
    basketball_team_sort_stat7: Optional[str] = None,
    basketball_team_sort_stat8: Optional[str] = None,
    basketball_team_sort_stat9: Optional[str] = None,
    basketball_team_stat_ids0: Optional[str] = None,
    basketball_team_stat_ids1: Optional[str] = None,
    basketball_team_stat_ids2: Optional[str] = None,
    basketball_team_stat_ids3: Optional[str] = None,
    basketball_team_stat_ids4: Optional[str] = None,
    basketball_team_stat_ids5: Optional[str] = None,
    basketball_team_stat_ids6: Optional[str] = None,
    basketball_team_stat_ids7: Optional[str] = None,
    basketball_team_stat_ids8: Optional[str] = None,
    basketball_team_stat_ids9: Optional[str] = None,
    football_leader_sort_stat0: Optional[str] = None,
    football_leader_sort_stat1: Optional[str] = None,
    football_leader_sort_stat2: Optional[str] = None,
    football_leader_sort_stat3: Optional[str] = None,
    football_leader_stat_ids0: Optional[str] = None,
    football_leader_stat_ids1: Optional[str] = None,
    football_leader_stat_ids2: Optional[str] = None,
    football_leader_stat_ids3: Optional[str] = None,
    football_player_stat_ids0: Optional[str] = None,
    football_player_stat_ids1: Optional[str] = None,
    football_player_stat_ids2: Optional[str] = None,
    football_player_stat_ids3: Optional[str] = None,
    football_player_stat_ids4: Optional[str] = None,
    football_player_stat_ids5: Optional[str] = None,
    football_player_stat_ids6: Optional[str] = None,
    football_player_stat_ids7: Optional[str] = None,
    football_team_sort_stat0: Optional[str] = None,
    football_team_sort_stat1: Optional[str] = None,
    football_team_sort_stat2: Optional[str] = None,
    football_team_sort_stat3: Optional[str] = None,
    football_team_sort_stat4: Optional[str] = None,
    football_team_sort_stat5: Optional[str] = None,
    football_team_sort_stat6: Optional[str] = None,
    football_team_sort_stat7: Optional[str] = None,
    football_team_sort_stat8: Optional[str] = None,
    football_team_sort_stat9: Optional[str] = None,
    football_team_sort_stat10: Optional[str] = None,
    football_team_sort_stat11: Optional[str] = None,
    football_team_stat_ids0: Optional[str] = None,
    football_team_stat_ids1: Optional[str] = None,
    football_team_stat_ids2: Optional[str] = None,
    football_team_stat_ids3: Optional[str] = None,
    football_team_stat_ids4: Optional[str] = None,
    football_team_stat_ids5: Optional[str] = None,
    football_team_stat_ids6: Optional[str] = None,
    football_team_stat_ids7: Optional[str] = None,
    football_team_stat_ids8: Optional[str] = None,
    football_team_stat_ids9: Optional[str] = None,
    football_team_stat_ids10: Optional[str] = None,
    football_team_stat_ids11: Optional[str] = None,
    hockey_leader_sort_stat0: Optional[str] = None,
    hockey_leader_sort_stat1: Optional[str] = None,
    hockey_leader_sort_stat2: Optional[str] = None,
    hockey_leader_sort_stat3: Optional[str] = None,
    hockey_leader_stat_ids0: Optional[str] = None,
    hockey_leader_stat_ids1: Optional[str] = None,
    hockey_leader_stat_ids2: Optional[str] = None,
    hockey_leader_stat_ids3: Optional[str] = None,
    hockey_player_stat_ids0: Optional[str] = None,
    hockey_player_stat_ids1: Optional[str] = None,
    hockey_player_stat_ids2: Optional[str] = None,
    hockey_team_sort_stat0: Optional[str] = None,
    hockey_team_sort_stat1: Optional[str] = None,
    hockey_team_sort_stat2: Optional[str] = None,
    hockey_team_sort_stat3: Optional[str] = None,
    hockey_team_sort_stat4: Optional[str] = None,
    hockey_team_sort_stat5: Optional[str] = None,
    hockey_team_sort_stat6: Optional[str] = None,
    hockey_team_stat_ids0: Optional[str] = None,
    hockey_team_stat_ids1: Optional[str] = None,
    hockey_team_stat_ids2: Optional[str] = None,
    hockey_team_stat_ids3: Optional[str] = None,
    hockey_team_stat_ids4: Optional[str] = None,
    hockey_team_stat_ids5: Optional[str] = None,
    hockey_team_stat_ids6: Optional[str] = None,
    soccer_player_stat_ids0: Optional[str] = None,
    soccer_player_stat_ids1: Optional[str] = None,
    soccer_player_stat_ids2: Optional[str] = None,
    soccer_player_stat_ids3: Optional[str] = None,
    soccer_player_stat_ids4: Optional[str] = None,
    soccer_team_sort_stat0: Optional[str] = None,
    soccer_team_sort_stat1: Optional[str] = None,
    soccer_team_sort_stat2: Optional[str] = None,
    soccer_team_sort_stat3: Optional[str] = None,
    soccer_team_sort_stat4: Optional[str] = None,
    soccer_team_sort_stat5: Optional[str] = None,
    soccer_team_stat_ids0: Optional[str] = None,
    soccer_team_stat_ids1: Optional[str] = None,
    soccer_team_stat_ids2: Optional[str] = None,
    soccer_team_stat_ids3: Optional[str] = None,
    soccer_team_stat_ids4: Optional[str] = None,
    soccer_team_stat_ids5: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Yahoo shangrila persisted query `gameStatsLeaders` -> one row per `games` entry

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/gameStatsLeaders``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/gameStatsLeaders

    Args:
        game_id: gameId query parameter.
        season: season query parameter.
        season_phases: seasonPhases query parameter.
        qualified: qualified query parameter.
        count: count query parameter.
        is_pregame: isPregame query parameter.
        team_image_height: teamImageHeight query parameter.
        team_image_width: teamImageWidth query parameter.
        player_image_height: playerImageHeight query parameter.
        player_image_width: playerImageWidth query parameter.
        baseball_leader_sort_stat0: baseballLeaderSortStat0 query parameter.
        baseball_leader_sort_stat1: baseballLeaderSortStat1 query parameter.
        baseball_leader_sort_stat2: baseballLeaderSortStat2 query parameter.
        baseball_leader_sort_stat3: baseballLeaderSortStat3 query parameter.
        baseball_leader_sort_stat4: baseballLeaderSortStat4 query parameter.
        baseball_leader_stat_ids0: baseballLeaderStatIds0 query parameter.
        baseball_leader_stat_ids1: baseballLeaderStatIds1 query parameter.
        baseball_leader_stat_ids2: baseballLeaderStatIds2 query parameter.
        baseball_leader_stat_ids3: baseballLeaderStatIds3 query parameter.
        baseball_leader_stat_ids4: baseballLeaderStatIds4 query parameter.
        baseball_player_stat_ids0: baseballPlayerStatIds0 query parameter.
        baseball_player_stat_ids1: baseballPlayerStatIds1 query parameter.
        baseball_team_sort_stat0: baseballTeamSortStat0 query parameter.
        baseball_team_sort_stat1: baseballTeamSortStat1 query parameter.
        baseball_team_sort_stat2: baseballTeamSortStat2 query parameter.
        baseball_team_sort_stat3: baseballTeamSortStat3 query parameter.
        baseball_team_sort_stat4: baseballTeamSortStat4 query parameter.
        baseball_team_sort_stat5: baseballTeamSortStat5 query parameter.
        baseball_team_sort_stat6: baseballTeamSortStat6 query parameter.
        baseball_team_sort_stat7: baseballTeamSortStat7 query parameter.
        baseball_team_sort_stat8: baseballTeamSortStat8 query parameter.
        baseball_team_sort_stat9: baseballTeamSortStat9 query parameter.
        baseball_team_sort_stat10: baseballTeamSortStat10 query parameter.
        baseball_team_sort_stat11: baseballTeamSortStat11 query parameter.
        baseball_team_stat_ids0: baseballTeamStatIds0 query parameter.
        baseball_team_stat_ids1: baseballTeamStatIds1 query parameter.
        baseball_team_stat_ids2: baseballTeamStatIds2 query parameter.
        baseball_team_stat_ids3: baseballTeamStatIds3 query parameter.
        baseball_team_stat_ids4: baseballTeamStatIds4 query parameter.
        baseball_team_stat_ids5: baseballTeamStatIds5 query parameter.
        baseball_team_stat_ids6: baseballTeamStatIds6 query parameter.
        baseball_team_stat_ids7: baseballTeamStatIds7 query parameter.
        baseball_team_stat_ids8: baseballTeamStatIds8 query parameter.
        baseball_team_stat_ids9: baseballTeamStatIds9 query parameter.
        baseball_team_stat_ids10: baseballTeamStatIds10 query parameter.
        baseball_team_stat_ids11: baseballTeamStatIds11 query parameter.
        basketball_leader_sort_stat0: basketballLeaderSortStat0 query parameter.
        basketball_leader_sort_stat1: basketballLeaderSortStat1 query parameter.
        basketball_leader_sort_stat2: basketballLeaderSortStat2 query parameter.
        basketball_leader_sort_stat3: basketballLeaderSortStat3 query parameter.
        basketball_leader_sort_stat4: basketballLeaderSortStat4 query parameter.
        basketball_leader_stat_ids0: basketballLeaderStatIds0 query parameter.
        basketball_leader_stat_ids1: basketballLeaderStatIds1 query parameter.
        basketball_leader_stat_ids2: basketballLeaderStatIds2 query parameter.
        basketball_leader_stat_ids3: basketballLeaderStatIds3 query parameter.
        basketball_leader_stat_ids4: basketballLeaderStatIds4 query parameter.
        basketball_player_stat_ids0: basketballPlayerStatIds0 query parameter.
        basketball_team_sort_stat0: basketballTeamSortStat0 query parameter.
        basketball_team_sort_stat1: basketballTeamSortStat1 query parameter.
        basketball_team_sort_stat2: basketballTeamSortStat2 query parameter.
        basketball_team_sort_stat3: basketballTeamSortStat3 query parameter.
        basketball_team_sort_stat4: basketballTeamSortStat4 query parameter.
        basketball_team_sort_stat5: basketballTeamSortStat5 query parameter.
        basketball_team_sort_stat6: basketballTeamSortStat6 query parameter.
        basketball_team_sort_stat7: basketballTeamSortStat7 query parameter.
        basketball_team_sort_stat8: basketballTeamSortStat8 query parameter.
        basketball_team_sort_stat9: basketballTeamSortStat9 query parameter.
        basketball_team_stat_ids0: basketballTeamStatIds0 query parameter.
        basketball_team_stat_ids1: basketballTeamStatIds1 query parameter.
        basketball_team_stat_ids2: basketballTeamStatIds2 query parameter.
        basketball_team_stat_ids3: basketballTeamStatIds3 query parameter.
        basketball_team_stat_ids4: basketballTeamStatIds4 query parameter.
        basketball_team_stat_ids5: basketballTeamStatIds5 query parameter.
        basketball_team_stat_ids6: basketballTeamStatIds6 query parameter.
        basketball_team_stat_ids7: basketballTeamStatIds7 query parameter.
        basketball_team_stat_ids8: basketballTeamStatIds8 query parameter.
        basketball_team_stat_ids9: basketballTeamStatIds9 query parameter.
        football_leader_sort_stat0: footballLeaderSortStat0 query parameter.
        football_leader_sort_stat1: footballLeaderSortStat1 query parameter.
        football_leader_sort_stat2: footballLeaderSortStat2 query parameter.
        football_leader_sort_stat3: footballLeaderSortStat3 query parameter.
        football_leader_stat_ids0: footballLeaderStatIds0 query parameter.
        football_leader_stat_ids1: footballLeaderStatIds1 query parameter.
        football_leader_stat_ids2: footballLeaderStatIds2 query parameter.
        football_leader_stat_ids3: footballLeaderStatIds3 query parameter.
        football_player_stat_ids0: footballPlayerStatIds0 query parameter.
        football_player_stat_ids1: footballPlayerStatIds1 query parameter.
        football_player_stat_ids2: footballPlayerStatIds2 query parameter.
        football_player_stat_ids3: footballPlayerStatIds3 query parameter.
        football_player_stat_ids4: footballPlayerStatIds4 query parameter.
        football_player_stat_ids5: footballPlayerStatIds5 query parameter.
        football_player_stat_ids6: footballPlayerStatIds6 query parameter.
        football_player_stat_ids7: footballPlayerStatIds7 query parameter.
        football_team_sort_stat0: footballTeamSortStat0 query parameter.
        football_team_sort_stat1: footballTeamSortStat1 query parameter.
        football_team_sort_stat2: footballTeamSortStat2 query parameter.
        football_team_sort_stat3: footballTeamSortStat3 query parameter.
        football_team_sort_stat4: footballTeamSortStat4 query parameter.
        football_team_sort_stat5: footballTeamSortStat5 query parameter.
        football_team_sort_stat6: footballTeamSortStat6 query parameter.
        football_team_sort_stat7: footballTeamSortStat7 query parameter.
        football_team_sort_stat8: footballTeamSortStat8 query parameter.
        football_team_sort_stat9: footballTeamSortStat9 query parameter.
        football_team_sort_stat10: footballTeamSortStat10 query parameter.
        football_team_sort_stat11: footballTeamSortStat11 query parameter.
        football_team_stat_ids0: footballTeamStatIds0 query parameter.
        football_team_stat_ids1: footballTeamStatIds1 query parameter.
        football_team_stat_ids2: footballTeamStatIds2 query parameter.
        football_team_stat_ids3: footballTeamStatIds3 query parameter.
        football_team_stat_ids4: footballTeamStatIds4 query parameter.
        football_team_stat_ids5: footballTeamStatIds5 query parameter.
        football_team_stat_ids6: footballTeamStatIds6 query parameter.
        football_team_stat_ids7: footballTeamStatIds7 query parameter.
        football_team_stat_ids8: footballTeamStatIds8 query parameter.
        football_team_stat_ids9: footballTeamStatIds9 query parameter.
        football_team_stat_ids10: footballTeamStatIds10 query parameter.
        football_team_stat_ids11: footballTeamStatIds11 query parameter.
        hockey_leader_sort_stat0: hockeyLeaderSortStat0 query parameter.
        hockey_leader_sort_stat1: hockeyLeaderSortStat1 query parameter.
        hockey_leader_sort_stat2: hockeyLeaderSortStat2 query parameter.
        hockey_leader_sort_stat3: hockeyLeaderSortStat3 query parameter.
        hockey_leader_stat_ids0: hockeyLeaderStatIds0 query parameter.
        hockey_leader_stat_ids1: hockeyLeaderStatIds1 query parameter.
        hockey_leader_stat_ids2: hockeyLeaderStatIds2 query parameter.
        hockey_leader_stat_ids3: hockeyLeaderStatIds3 query parameter.
        hockey_player_stat_ids0: hockeyPlayerStatIds0 query parameter.
        hockey_player_stat_ids1: hockeyPlayerStatIds1 query parameter.
        hockey_player_stat_ids2: hockeyPlayerStatIds2 query parameter.
        hockey_team_sort_stat0: hockeyTeamSortStat0 query parameter.
        hockey_team_sort_stat1: hockeyTeamSortStat1 query parameter.
        hockey_team_sort_stat2: hockeyTeamSortStat2 query parameter.
        hockey_team_sort_stat3: hockeyTeamSortStat3 query parameter.
        hockey_team_sort_stat4: hockeyTeamSortStat4 query parameter.
        hockey_team_sort_stat5: hockeyTeamSortStat5 query parameter.
        hockey_team_sort_stat6: hockeyTeamSortStat6 query parameter.
        hockey_team_stat_ids0: hockeyTeamStatIds0 query parameter.
        hockey_team_stat_ids1: hockeyTeamStatIds1 query parameter.
        hockey_team_stat_ids2: hockeyTeamStatIds2 query parameter.
        hockey_team_stat_ids3: hockeyTeamStatIds3 query parameter.
        hockey_team_stat_ids4: hockeyTeamStatIds4 query parameter.
        hockey_team_stat_ids5: hockeyTeamStatIds5 query parameter.
        hockey_team_stat_ids6: hockeyTeamStatIds6 query parameter.
        soccer_player_stat_ids0: soccerPlayerStatIds0 query parameter.
        soccer_player_stat_ids1: soccerPlayerStatIds1 query parameter.
        soccer_player_stat_ids2: soccerPlayerStatIds2 query parameter.
        soccer_player_stat_ids3: soccerPlayerStatIds3 query parameter.
        soccer_player_stat_ids4: soccerPlayerStatIds4 query parameter.
        soccer_team_sort_stat0: soccerTeamSortStat0 query parameter.
        soccer_team_sort_stat1: soccerTeamSortStat1 query parameter.
        soccer_team_sort_stat2: soccerTeamSortStat2 query parameter.
        soccer_team_sort_stat3: soccerTeamSortStat3 query parameter.
        soccer_team_sort_stat4: soccerTeamSortStat4 query parameter.
        soccer_team_sort_stat5: soccerTeamSortStat5 query parameter.
        soccer_team_stat_ids0: soccerTeamStatIds0 query parameter.
        soccer_team_stat_ids1: soccerTeamStatIds1 query parameter.
        soccer_team_stat_ids2: soccerTeamStatIds2 query parameter.
        soccer_team_stat_ids3: soccerTeamStatIds3 query parameter.
        soccer_team_stat_ids4: soccerTeamStatIds4 query parameter.
        soccer_team_stat_ids5: soccerTeamStatIds5 query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.yahoo.yahoo_shangrila import yahoo_game_stats_leaders
            yahoo_game_stats_leaders()

        See Also:
            * `cfbfastR`_ - R sister package for college football
            * `Yahoo Sports`_ - data origin (unofficial, undocumented endpoints)

        .. _cfbfastR: https://cfbfastR.sportsdataverse.org
        .. _Yahoo Sports: https://sports.yahoo.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "gameId": game_id,
        "season": season,
        "seasonPhases": season_phases,
        "qualified": qualified,
        "count": count,
        "isPregame": is_pregame,
        "teamImageHeight": team_image_height,
        "teamImageWidth": team_image_width,
        "playerImageHeight": player_image_height,
        "playerImageWidth": player_image_width,
        "baseballLeaderSortStat0": baseball_leader_sort_stat0,
        "baseballLeaderSortStat1": baseball_leader_sort_stat1,
        "baseballLeaderSortStat2": baseball_leader_sort_stat2,
        "baseballLeaderSortStat3": baseball_leader_sort_stat3,
        "baseballLeaderSortStat4": baseball_leader_sort_stat4,
        "baseballLeaderStatIds0": baseball_leader_stat_ids0,
        "baseballLeaderStatIds1": baseball_leader_stat_ids1,
        "baseballLeaderStatIds2": baseball_leader_stat_ids2,
        "baseballLeaderStatIds3": baseball_leader_stat_ids3,
        "baseballLeaderStatIds4": baseball_leader_stat_ids4,
        "baseballPlayerStatIds0": baseball_player_stat_ids0,
        "baseballPlayerStatIds1": baseball_player_stat_ids1,
        "baseballTeamSortStat0": baseball_team_sort_stat0,
        "baseballTeamSortStat1": baseball_team_sort_stat1,
        "baseballTeamSortStat2": baseball_team_sort_stat2,
        "baseballTeamSortStat3": baseball_team_sort_stat3,
        "baseballTeamSortStat4": baseball_team_sort_stat4,
        "baseballTeamSortStat5": baseball_team_sort_stat5,
        "baseballTeamSortStat6": baseball_team_sort_stat6,
        "baseballTeamSortStat7": baseball_team_sort_stat7,
        "baseballTeamSortStat8": baseball_team_sort_stat8,
        "baseballTeamSortStat9": baseball_team_sort_stat9,
        "baseballTeamSortStat10": baseball_team_sort_stat10,
        "baseballTeamSortStat11": baseball_team_sort_stat11,
        "baseballTeamStatIds0": baseball_team_stat_ids0,
        "baseballTeamStatIds1": baseball_team_stat_ids1,
        "baseballTeamStatIds2": baseball_team_stat_ids2,
        "baseballTeamStatIds3": baseball_team_stat_ids3,
        "baseballTeamStatIds4": baseball_team_stat_ids4,
        "baseballTeamStatIds5": baseball_team_stat_ids5,
        "baseballTeamStatIds6": baseball_team_stat_ids6,
        "baseballTeamStatIds7": baseball_team_stat_ids7,
        "baseballTeamStatIds8": baseball_team_stat_ids8,
        "baseballTeamStatIds9": baseball_team_stat_ids9,
        "baseballTeamStatIds10": baseball_team_stat_ids10,
        "baseballTeamStatIds11": baseball_team_stat_ids11,
        "basketballLeaderSortStat0": basketball_leader_sort_stat0,
        "basketballLeaderSortStat1": basketball_leader_sort_stat1,
        "basketballLeaderSortStat2": basketball_leader_sort_stat2,
        "basketballLeaderSortStat3": basketball_leader_sort_stat3,
        "basketballLeaderSortStat4": basketball_leader_sort_stat4,
        "basketballLeaderStatIds0": basketball_leader_stat_ids0,
        "basketballLeaderStatIds1": basketball_leader_stat_ids1,
        "basketballLeaderStatIds2": basketball_leader_stat_ids2,
        "basketballLeaderStatIds3": basketball_leader_stat_ids3,
        "basketballLeaderStatIds4": basketball_leader_stat_ids4,
        "basketballPlayerStatIds0": basketball_player_stat_ids0,
        "basketballTeamSortStat0": basketball_team_sort_stat0,
        "basketballTeamSortStat1": basketball_team_sort_stat1,
        "basketballTeamSortStat2": basketball_team_sort_stat2,
        "basketballTeamSortStat3": basketball_team_sort_stat3,
        "basketballTeamSortStat4": basketball_team_sort_stat4,
        "basketballTeamSortStat5": basketball_team_sort_stat5,
        "basketballTeamSortStat6": basketball_team_sort_stat6,
        "basketballTeamSortStat7": basketball_team_sort_stat7,
        "basketballTeamSortStat8": basketball_team_sort_stat8,
        "basketballTeamSortStat9": basketball_team_sort_stat9,
        "basketballTeamStatIds0": basketball_team_stat_ids0,
        "basketballTeamStatIds1": basketball_team_stat_ids1,
        "basketballTeamStatIds2": basketball_team_stat_ids2,
        "basketballTeamStatIds3": basketball_team_stat_ids3,
        "basketballTeamStatIds4": basketball_team_stat_ids4,
        "basketballTeamStatIds5": basketball_team_stat_ids5,
        "basketballTeamStatIds6": basketball_team_stat_ids6,
        "basketballTeamStatIds7": basketball_team_stat_ids7,
        "basketballTeamStatIds8": basketball_team_stat_ids8,
        "basketballTeamStatIds9": basketball_team_stat_ids9,
        "footballLeaderSortStat0": football_leader_sort_stat0,
        "footballLeaderSortStat1": football_leader_sort_stat1,
        "footballLeaderSortStat2": football_leader_sort_stat2,
        "footballLeaderSortStat3": football_leader_sort_stat3,
        "footballLeaderStatIds0": football_leader_stat_ids0,
        "footballLeaderStatIds1": football_leader_stat_ids1,
        "footballLeaderStatIds2": football_leader_stat_ids2,
        "footballLeaderStatIds3": football_leader_stat_ids3,
        "footballPlayerStatIds0": football_player_stat_ids0,
        "footballPlayerStatIds1": football_player_stat_ids1,
        "footballPlayerStatIds2": football_player_stat_ids2,
        "footballPlayerStatIds3": football_player_stat_ids3,
        "footballPlayerStatIds4": football_player_stat_ids4,
        "footballPlayerStatIds5": football_player_stat_ids5,
        "footballPlayerStatIds6": football_player_stat_ids6,
        "footballPlayerStatIds7": football_player_stat_ids7,
        "footballTeamSortStat0": football_team_sort_stat0,
        "footballTeamSortStat1": football_team_sort_stat1,
        "footballTeamSortStat2": football_team_sort_stat2,
        "footballTeamSortStat3": football_team_sort_stat3,
        "footballTeamSortStat4": football_team_sort_stat4,
        "footballTeamSortStat5": football_team_sort_stat5,
        "footballTeamSortStat6": football_team_sort_stat6,
        "footballTeamSortStat7": football_team_sort_stat7,
        "footballTeamSortStat8": football_team_sort_stat8,
        "footballTeamSortStat9": football_team_sort_stat9,
        "footballTeamSortStat10": football_team_sort_stat10,
        "footballTeamSortStat11": football_team_sort_stat11,
        "footballTeamStatIds0": football_team_stat_ids0,
        "footballTeamStatIds1": football_team_stat_ids1,
        "footballTeamStatIds2": football_team_stat_ids2,
        "footballTeamStatIds3": football_team_stat_ids3,
        "footballTeamStatIds4": football_team_stat_ids4,
        "footballTeamStatIds5": football_team_stat_ids5,
        "footballTeamStatIds6": football_team_stat_ids6,
        "footballTeamStatIds7": football_team_stat_ids7,
        "footballTeamStatIds8": football_team_stat_ids8,
        "footballTeamStatIds9": football_team_stat_ids9,
        "footballTeamStatIds10": football_team_stat_ids10,
        "footballTeamStatIds11": football_team_stat_ids11,
        "hockeyLeaderSortStat0": hockey_leader_sort_stat0,
        "hockeyLeaderSortStat1": hockey_leader_sort_stat1,
        "hockeyLeaderSortStat2": hockey_leader_sort_stat2,
        "hockeyLeaderSortStat3": hockey_leader_sort_stat3,
        "hockeyLeaderStatIds0": hockey_leader_stat_ids0,
        "hockeyLeaderStatIds1": hockey_leader_stat_ids1,
        "hockeyLeaderStatIds2": hockey_leader_stat_ids2,
        "hockeyLeaderStatIds3": hockey_leader_stat_ids3,
        "hockeyPlayerStatIds0": hockey_player_stat_ids0,
        "hockeyPlayerStatIds1": hockey_player_stat_ids1,
        "hockeyPlayerStatIds2": hockey_player_stat_ids2,
        "hockeyTeamSortStat0": hockey_team_sort_stat0,
        "hockeyTeamSortStat1": hockey_team_sort_stat1,
        "hockeyTeamSortStat2": hockey_team_sort_stat2,
        "hockeyTeamSortStat3": hockey_team_sort_stat3,
        "hockeyTeamSortStat4": hockey_team_sort_stat4,
        "hockeyTeamSortStat5": hockey_team_sort_stat5,
        "hockeyTeamSortStat6": hockey_team_sort_stat6,
        "hockeyTeamStatIds0": hockey_team_stat_ids0,
        "hockeyTeamStatIds1": hockey_team_stat_ids1,
        "hockeyTeamStatIds2": hockey_team_stat_ids2,
        "hockeyTeamStatIds3": hockey_team_stat_ids3,
        "hockeyTeamStatIds4": hockey_team_stat_ids4,
        "hockeyTeamStatIds5": hockey_team_stat_ids5,
        "hockeyTeamStatIds6": hockey_team_stat_ids6,
        "soccerPlayerStatIds0": soccer_player_stat_ids0,
        "soccerPlayerStatIds1": soccer_player_stat_ids1,
        "soccerPlayerStatIds2": soccer_player_stat_ids2,
        "soccerPlayerStatIds3": soccer_player_stat_ids3,
        "soccerPlayerStatIds4": soccer_player_stat_ids4,
        "soccerTeamSortStat0": soccer_team_sort_stat0,
        "soccerTeamSortStat1": soccer_team_sort_stat1,
        "soccerTeamSortStat2": soccer_team_sort_stat2,
        "soccerTeamSortStat3": soccer_team_sort_stat3,
        "soccerTeamSortStat4": soccer_team_sort_stat4,
        "soccerTeamSortStat5": soccer_team_sort_stat5,
        "soccerTeamStatIds0": soccer_team_stat_ids0,
        "soccerTeamStatIds1": soccer_team_stat_ids1,
        "soccerTeamStatIds2": soccer_team_stat_ids2,
        "soccerTeamStatIds3": soccer_team_stat_ids3,
        "soccerTeamStatIds4": soccer_team_stat_ids4,
        "soccerTeamStatIds5": soccer_team_stat_ids5,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/gameStatsLeaders",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_gametime_game(
    game_id: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Yahoo shangrila persisted query `gametimeGame` -> one row per `games` entry

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/gametimeGame``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/gametimeGame

    Args:
        game_id: gameId query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.yahoo.yahoo_shangrila import yahoo_gametime_game
            yahoo_gametime_game()

        See Also:
            * `cfbfastR`_ - R sister package for college football
            * `Yahoo Sports`_ - data origin (unofficial, undocumented endpoints)

        .. _cfbfastR: https://cfbfastR.sportsdataverse.org
        .. _Yahoo Sports: https://sports.yahoo.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "gameId": game_id,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/gametimeGame",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_gametime_team(
    team_id: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Yahoo shangrila persisted query `gametimeTeam` -> one row per `teams` entry

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/gametimeTeam``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/gametimeTeam

    Args:
        team_id: teamId query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.yahoo.yahoo_shangrila import yahoo_gametime_team
            yahoo_gametime_team()

        See Also:
            * `cfbfastR`_ - R sister package for college football
            * `Yahoo Sports`_ - data origin (unofficial, undocumented endpoints)

        .. _cfbfastR: https://cfbfastR.sportsdataverse.org
        .. _Yahoo Sports: https://sports.yahoo.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "teamId": team_id,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/gametimeTeam",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_golf_tournament_seasons(
    event_group_id: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Yahoo shangrila persisted query `golfTournamentSeasons` (response body not captured; shape unknown)

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/golfTournamentSeasons``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/golfTournamentSeasons

    Args:
        event_group_id: eventGroupId query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.yahoo.yahoo_shangrila import yahoo_golf_tournament_seasons
            yahoo_golf_tournament_seasons()

        See Also:
            * `cfbfastR`_ - R sister package for college football
            * `Yahoo Sports`_ - data origin (unofficial, undocumented endpoints)

        .. _cfbfastR: https://cfbfastR.sportsdataverse.org
        .. _Yahoo Sports: https://sports.yahoo.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "eventGroupId": event_group_id,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/golfTournamentSeasons",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_golf_tournaments(
    association: Optional[str] = None,
    season: Optional[str] = None,
    show_defending_champs: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Yahoo shangrila persisted query `golfTournaments` -> one row per `golfTournaments` entry

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/golfTournaments``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/golfTournaments

    Args:
        association: association query parameter.
        season: season query parameter.
        show_defending_champs: showDefendingChamps query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.yahoo.yahoo_shangrila import yahoo_golf_tournaments
            yahoo_golf_tournaments()

        See Also:
            * `cfbfastR`_ - R sister package for college football
            * `Yahoo Sports`_ - data origin (unofficial, undocumented endpoints)

        .. _cfbfastR: https://cfbfastR.sportsdataverse.org
        .. _Yahoo Sports: https://sports.yahoo.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "association": association,
        "season": season,
        "showDefendingChamps": show_defending_champs,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/golfTournaments",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_golf_tournaments_basic(
    event_group_id: Optional[str] = None,
    association: Optional[str] = None,
    season: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Yahoo shangrila persisted query `golfTournamentsBasic` -> one row per `golfTournaments` entry

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/golfTournamentsBasic``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/golfTournamentsBasic

    Args:
        event_group_id: eventGroupId query parameter.
        association: association query parameter.
        season: season query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.yahoo.yahoo_shangrila import yahoo_golf_tournaments_basic
            yahoo_golf_tournaments_basic()

        See Also:
            * `cfbfastR`_ - R sister package for college football
            * `Yahoo Sports`_ - data origin (unofficial, undocumented endpoints)

        .. _cfbfastR: https://cfbfastR.sportsdataverse.org
        .. _Yahoo Sports: https://sports.yahoo.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "eventGroupId": event_group_id,
        "association": association,
        "season": season,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/golfTournamentsBasic",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_league_conferences(
    league: Optional[str] = None,
    division_ids: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Yahoo shangrila persisted query `leagueConferences` -> one row per `leagues` entry

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/leagueConferences``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/leagueConferences

    Args:
        league: league query parameter.
        division_ids: divisionIds query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.yahoo.yahoo_shangrila import yahoo_league_conferences
            yahoo_league_conferences()

        See Also:
            * `cfbfastR`_ - R sister package for college football
            * `Yahoo Sports`_ - data origin (unofficial, undocumented endpoints)

        .. _cfbfastR: https://cfbfastR.sportsdataverse.org
        .. _Yahoo Sports: https://sports.yahoo.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "league": league,
        "divisionIds": division_ids,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/leagueConferences",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_league_filters_data(
    league: Optional[str] = None,
    season: Optional[str] = None,
    view_type: Optional[str] = None,
    include_pos_and_splits_data: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Yahoo shangrila persisted query `leagueFiltersData` -> one row per `leagues` entry

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/leagueFiltersData``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/leagueFiltersData

    Args:
        league: league query parameter.
        season: season query parameter.
        view_type: viewType query parameter.
        include_pos_and_splits_data: includePosAndSplitsData query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.yahoo.yahoo_shangrila import yahoo_league_filters_data
            yahoo_league_filters_data()

        See Also:
            * `cfbfastR`_ - R sister package for college football
            * `Yahoo Sports`_ - data origin (unofficial, undocumented endpoints)

        .. _cfbfastR: https://cfbfastR.sportsdataverse.org
        .. _Yahoo Sports: https://sports.yahoo.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "league": league,
        "season": season,
        "viewType": view_type,
        "includePosAndSplitsData": include_pos_and_splits_data,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/leagueFiltersData",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_league_future_odds(
    league: Optional[str] = None,
    bet_categories: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Yahoo shangrila persisted query `leagueFutureOdds` -> one row per `leagues` entry

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/leagueFutureOdds``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/leagueFutureOdds

    Args:
        league: league query parameter.
        bet_categories: betCategories query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.yahoo.yahoo_shangrila import yahoo_league_future_odds
            yahoo_league_future_odds()

        See Also:
            * `cfbfastR`_ - R sister package for college football
            * `Yahoo Sports`_ - data origin (unofficial, undocumented endpoints)

        .. _cfbfastR: https://cfbfastR.sportsdataverse.org
        .. _Yahoo Sports: https://sports.yahoo.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "league": league,
        "betCategories": bet_categories,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/leagueFutureOdds",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_league_game_ids(
    count: Optional[str] = None,
    league: Optional[str] = None,
    week: Optional[str] = None,
    date: Optional[str] = None,
    season: Optional[str] = None,
    game_status_order: Optional[str] = None,
    start_time_order: Optional[str] = None,
    date_flip_offset: Optional[str] = None,
    season_phase: Optional[str] = None,
    conference_ids: Optional[str] = None,
    top25: Optional[str] = None,
    game_day_query_type: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Yahoo shangrila persisted query `leagueGameIds` -> one row per `leagues` entry

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/leagueGameIds``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/leagueGameIds

    Args:
        count: count query parameter.
        league: league query parameter.
        week: week query parameter.
        date: date query parameter.
        season: season query parameter.
        game_status_order: gameStatusOrder query parameter.
        start_time_order: startTimeOrder query parameter.
        date_flip_offset: dateFlipOffset query parameter.
        season_phase: seasonPhase query parameter.
        conference_ids: conferenceIds query parameter.
        top25: top25 query parameter.
        game_day_query_type: gameDayQueryType query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.yahoo.yahoo_shangrila import yahoo_league_game_ids
            yahoo_league_game_ids()

        See Also:
            * `cfbfastR`_ - R sister package for college football
            * `Yahoo Sports`_ - data origin (unofficial, undocumented endpoints)

        .. _cfbfastR: https://cfbfastR.sportsdataverse.org
        .. _Yahoo Sports: https://sports.yahoo.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "count": count,
        "league": league,
        "week": week,
        "date": date,
        "season": season,
        "gameStatusOrder": game_status_order,
        "startTimeOrder": start_time_order,
        "dateFlipOffset": date_flip_offset,
        "seasonPhase": season_phase,
        "conferenceIds": conference_ids,
        "top25": top25,
        "gameDayQueryType": game_day_query_type,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/leagueGameIds",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_league_game_ids_by_date(
    leagues: Optional[str] = None,
    week: Optional[str] = None,
    dates: Optional[str] = None,
    start_range: Optional[str] = None,
    end_range: Optional[str] = None,
    season: Optional[str] = None,
    season_phases: Optional[str] = None,
    conference_ids: Optional[str] = None,
    division_ids: Optional[str] = None,
    top25: Optional[str] = None,
    tournament_ids: Optional[str] = None,
    is_tennis: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Yahoo shangrila persisted query `leagueGameIdsByDate` -> one row per `leagues` entry

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/leagueGameIdsByDate``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/leagueGameIdsByDate

    Args:
        leagues: leagues query parameter.
        week: week query parameter.
        dates: dates query parameter.
        start_range: startRange query parameter.
        end_range: endRange query parameter.
        season: season query parameter.
        season_phases: seasonPhases query parameter.
        conference_ids: conferenceIds query parameter.
        division_ids: divisionIds query parameter.
        top25: top25 query parameter.
        tournament_ids: tournamentIds query parameter.
        is_tennis: isTennis query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.yahoo.yahoo_shangrila import yahoo_league_game_ids_by_date
            yahoo_league_game_ids_by_date()

        See Also:
            * `cfbfastR`_ - R sister package for college football
            * `Yahoo Sports`_ - data origin (unofficial, undocumented endpoints)

        .. _cfbfastR: https://cfbfastR.sportsdataverse.org
        .. _Yahoo Sports: https://sports.yahoo.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "leagues": leagues,
        "week": week,
        "dates": dates,
        "startRange": start_range,
        "endRange": end_range,
        "season": season,
        "seasonPhases": season_phases,
        "conferenceIds": conference_ids,
        "divisionIds": division_ids,
        "top25": top25,
        "tournamentIds": tournament_ids,
        "isTennis": is_tennis,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/leagueGameIdsByDate",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_league_games_by_round(
    league: Optional[str] = None,
    tournament_round_ids: Optional[str] = None,
    season: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Yahoo shangrila persisted query `leagueGamesByRound` -> one row per `leagues` entry

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/leagueGamesByRound``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/leagueGamesByRound

    Args:
        league: league query parameter.
        tournament_round_ids: tournamentRoundIds query parameter.
        season: season query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.yahoo.yahoo_shangrila import yahoo_league_games_by_round
            yahoo_league_games_by_round()

        See Also:
            * `cfbfastR`_ - R sister package for college football
            * `Yahoo Sports`_ - data origin (unofficial, undocumented endpoints)

        .. _cfbfastR: https://cfbfastR.sportsdataverse.org
        .. _Yahoo Sports: https://sports.yahoo.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "league": league,
        "tournamentRoundIds": tournament_round_ids,
        "season": season,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/leagueGamesByRound",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_league_info(
    league: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Yahoo shangrila persisted query `leagueInfo` -> one row per `leagues` entry

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/leagueInfo``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/leagueInfo

    Args:
        league: league query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.yahoo.yahoo_shangrila import yahoo_league_info
            yahoo_league_info()

        See Also:
            * `cfbfastR`_ - R sister package for college football
            * `Yahoo Sports`_ - data origin (unofficial, undocumented endpoints)

        .. _cfbfastR: https://cfbfastR.sportsdataverse.org
        .. _Yahoo Sports: https://sports.yahoo.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "league": league,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/leagueInfo",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_league_injuries(
    league_id: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Yahoo shangrila persisted query `leagueInjuries` -> one row per `leagues.teams` entry

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/leagueInjuries``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/leagueInjuries

    Args:
        league_id: leagueId query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.yahoo.yahoo_shangrila import yahoo_league_injuries
            yahoo_league_injuries()

        See Also:
            * `cfbfastR`_ - R sister package for college football
            * `Yahoo Sports`_ - data origin (unofficial, undocumented endpoints)

        .. _cfbfastR: https://cfbfastR.sportsdataverse.org
        .. _Yahoo Sports: https://sports.yahoo.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "leagueId": league_id,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/leagueInjuries",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_league_names(
    leagues: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Yahoo shangrila persisted query `leagueNames` -> one row per `leagues` entry

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/leagueNames``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/leagueNames

    Args:
        leagues: leagues query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.yahoo.yahoo_shangrila import yahoo_league_names
            yahoo_league_names()

        See Also:
            * `cfbfastR`_ - R sister package for college football
            * `Yahoo Sports`_ - data origin (unofficial, undocumented endpoints)

        .. _cfbfastR: https://cfbfastR.sportsdataverse.org
        .. _Yahoo Sports: https://sports.yahoo.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "leagues": leagues,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/leagueNames",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_league_prop_odds(
    count: Optional[str] = None,
    league: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Yahoo shangrila persisted query `leaguePropOdds` -> one row per `leagues` entry

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/leaguePropOdds``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/leaguePropOdds

    Args:
        count: count query parameter.
        league: league query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.yahoo.yahoo_shangrila import yahoo_league_prop_odds
            yahoo_league_prop_odds()

        See Also:
            * `cfbfastR`_ - R sister package for college football
            * `Yahoo Sports`_ - data origin (unofficial, undocumented endpoints)

        .. _cfbfastR: https://cfbfastR.sportsdataverse.org
        .. _Yahoo Sports: https://sports.yahoo.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "count": count,
        "league": league,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/leaguePropOdds",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_league_standings(
    league: Optional[str] = None,
    season: Optional[str] = None,
    season_phase: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Yahoo shangrila persisted query `leagueStandings` -> one row per `leagues` entry

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/leagueStandings``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/leagueStandings

    Args:
        league: league query parameter.
        season: season query parameter.
        season_phase: seasonPhase query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.yahoo.yahoo_shangrila import yahoo_league_standings
            yahoo_league_standings()

        See Also:
            * `cfbfastR`_ - R sister package for college football
            * `Yahoo Sports`_ - data origin (unofficial, undocumented endpoints)

        .. _cfbfastR: https://cfbfastR.sportsdataverse.org
        .. _Yahoo Sports: https://sports.yahoo.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "league": league,
        "season": season,
        "seasonPhase": season_phase,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/leagueStandings",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_league_stats_by_team(
    leagues: Optional[str] = None,
    count: Optional[str] = None,
    season: Optional[str] = None,
    league_structure_id: Optional[str] = None,
    baseball_cut_type: Optional[str] = None,
    basketball_cut_type: Optional[str] = None,
    football_cut_type: Optional[str] = None,
    hockey_cut_type: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Yahoo shangrila persisted query `leagueStatsByTeam` -> one row per `leagues` entry

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/leagueStatsByTeam``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/leagueStatsByTeam

    Args:
        leagues: leagues query parameter.
        count: count query parameter.
        season: season query parameter.
        league_structure_id: leagueStructureId query parameter.
        baseball_cut_type: baseballCutType query parameter.
        basketball_cut_type: basketballCutType query parameter.
        football_cut_type: footballCutType query parameter.
        hockey_cut_type: hockeyCutType query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.yahoo.yahoo_shangrila import yahoo_league_stats_by_team
            yahoo_league_stats_by_team()

        See Also:
            * `cfbfastR`_ - R sister package for college football
            * `Yahoo Sports`_ - data origin (unofficial, undocumented endpoints)

        .. _cfbfastR: https://cfbfastR.sportsdataverse.org
        .. _Yahoo Sports: https://sports.yahoo.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "leagues": leagues,
        "count": count,
        "season": season,
        "leagueStructureId": league_structure_id,
        "baseballCutType": baseball_cut_type,
        "basketballCutType": basketball_cut_type,
        "footballCutType": football_cut_type,
        "hockeyCutType": hockey_cut_type,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/leagueStatsByTeam",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_league_stats_individual(
    leagues: Optional[str] = None,
    count: Optional[str] = None,
    season: Optional[str] = None,
    qualified: Optional[str] = None,
    league_structure_id: Optional[str] = None,
    baseball_cut_type: Optional[str] = None,
    baseball_position: Optional[str] = None,
    basketball_cut_type: Optional[str] = None,
    basketball_position: Optional[str] = None,
    football_cut_type: Optional[str] = None,
    hockey_cut_type: Optional[str] = None,
    hockey_position: Optional[str] = None,
    golf_sort_stat: Optional[str] = None,
    golf_stat_ids: Optional[str] = None,
    motorsports_sort_stat: Optional[str] = None,
    motorsports_stat_ids: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Yahoo shangrila persisted query `leagueStatsIndividual` -> one row per `leagues` entry

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/leagueStatsIndividual``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/leagueStatsIndividual

    Args:
        leagues: leagues query parameter.
        count: count query parameter.
        season: season query parameter.
        qualified: qualified query parameter.
        league_structure_id: leagueStructureId query parameter.
        baseball_cut_type: baseballCutType query parameter.
        baseball_position: baseballPosition query parameter.
        basketball_cut_type: basketballCutType query parameter.
        basketball_position: basketballPosition query parameter.
        football_cut_type: footballCutType query parameter.
        hockey_cut_type: hockeyCutType query parameter.
        hockey_position: hockeyPosition query parameter.
        golf_sort_stat: golfSortStat query parameter.
        golf_stat_ids: golfStatIds query parameter.
        motorsports_sort_stat: motorsportsSortStat query parameter.
        motorsports_stat_ids: motorsportsStatIds query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.yahoo.yahoo_shangrila import yahoo_league_stats_individual
            yahoo_league_stats_individual()

        See Also:
            * `cfbfastR`_ - R sister package for college football
            * `Yahoo Sports`_ - data origin (unofficial, undocumented endpoints)

        .. _cfbfastR: https://cfbfastR.sportsdataverse.org
        .. _Yahoo Sports: https://sports.yahoo.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "leagues": leagues,
        "count": count,
        "season": season,
        "qualified": qualified,
        "leagueStructureId": league_structure_id,
        "baseballCutType": baseball_cut_type,
        "baseballPosition": baseball_position,
        "basketballCutType": basketball_cut_type,
        "basketballPosition": basketball_position,
        "footballCutType": football_cut_type,
        "hockeyCutType": hockey_cut_type,
        "hockeyPosition": hockey_position,
        "golfSortStat": golf_sort_stat,
        "golfStatIds": golf_stat_ids,
        "motorsportsSortStat": motorsports_sort_stat,
        "motorsportsStatIds": motorsports_stat_ids,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/leagueStatsIndividual",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_league_stats_overview(
    leagues: Optional[str] = None,
    count: Optional[str] = None,
    week: Optional[str] = None,
    week_season_phase: Optional[str] = None,
    season_phase: Optional[str] = None,
    league_structure_id: Optional[str] = None,
    golf_sort_stat: Optional[str] = None,
    golf_stat_ids: Optional[str] = None,
    motorsports_sort_stat: Optional[str] = None,
    motorsports_stat_ids: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Yahoo shangrila persisted query `leagueStatsOverview` (response body not captured; shape unknown)

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/leagueStatsOverview``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/leagueStatsOverview

    Args:
        leagues: leagues query parameter.
        count: count query parameter.
        week: week query parameter.
        week_season_phase: weekSeasonPhase query parameter.
        season_phase: seasonPhase query parameter.
        league_structure_id: leagueStructureId query parameter.
        golf_sort_stat: golfSortStat query parameter.
        golf_stat_ids: golfStatIds query parameter.
        motorsports_sort_stat: motorsportsSortStat query parameter.
        motorsports_stat_ids: motorsportsStatIds query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.yahoo.yahoo_shangrila import yahoo_league_stats_overview
            yahoo_league_stats_overview()

        See Also:
            * `cfbfastR`_ - R sister package for college football
            * `Yahoo Sports`_ - data origin (unofficial, undocumented endpoints)

        .. _cfbfastR: https://cfbfastR.sportsdataverse.org
        .. _Yahoo Sports: https://sports.yahoo.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "leagues": leagues,
        "count": count,
        "week": week,
        "weekSeasonPhase": week_season_phase,
        "seasonPhase": season_phase,
        "leagueStructureId": league_structure_id,
        "golfSortStat": golf_sort_stat,
        "golfStatIds": golf_stat_ids,
        "motorsportsSortStat": motorsports_sort_stat,
        "motorsportsStatIds": motorsports_stat_ids,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/leagueStatsOverview",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_league_stats_weekly(
    leagues: Optional[str] = None,
    count: Optional[str] = None,
    week: Optional[str] = None,
    season: Optional[str] = None,
    season_phase: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Yahoo shangrila persisted query `leagueStatsWeekly` -> one row per `leagues` entry

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/leagueStatsWeekly``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/leagueStatsWeekly

    Args:
        leagues: leagues query parameter.
        count: count query parameter.
        week: week query parameter.
        season: season query parameter.
        season_phase: seasonPhase query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.yahoo.yahoo_shangrila import yahoo_league_stats_weekly
            yahoo_league_stats_weekly()

        See Also:
            * `cfbfastR`_ - R sister package for college football
            * `Yahoo Sports`_ - data origin (unofficial, undocumented endpoints)

        .. _cfbfastR: https://cfbfastR.sportsdataverse.org
        .. _Yahoo Sports: https://sports.yahoo.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "leagues": leagues,
        "count": count,
        "week": week,
        "season": season,
        "seasonPhase": season_phase,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/leagueStatsWeekly",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_league_team_ids(
    league: Optional[str] = None,
    division_ids: Optional[str] = None,
    get_teams_by_division: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Yahoo shangrila persisted query `leagueTeamIds` -> one row per `leagues` entry

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/leagueTeamIds``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/leagueTeamIds

    Args:
        league: league query parameter.
        division_ids: divisionIds query parameter.
        get_teams_by_division: getTeamsByDivision query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.yahoo.yahoo_shangrila import yahoo_league_team_ids
            yahoo_league_team_ids()

        See Also:
            * `cfbfastR`_ - R sister package for college football
            * `Yahoo Sports`_ - data origin (unofficial, undocumented endpoints)

        .. _cfbfastR: https://cfbfastR.sportsdataverse.org
        .. _Yahoo Sports: https://sports.yahoo.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "league": league,
        "divisionIds": division_ids,
        "getTeamsByDivision": get_teams_by_division,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/leagueTeamIds",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_league_teams(
    league: Optional[str] = None,
    season: Optional[str] = None,
    division_ids: Optional[str] = None,
    get_teams_by_division: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Yahoo shangrila persisted query `leagueTeams` -> one row per `leagues` entry

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/leagueTeams``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/leagueTeams

    Args:
        league: league query parameter.
        season: season query parameter.
        division_ids: divisionIds query parameter.
        get_teams_by_division: getTeamsByDivision query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.yahoo.yahoo_shangrila import yahoo_league_teams
            yahoo_league_teams()

        See Also:
            * `cfbfastR`_ - R sister package for college football
            * `Yahoo Sports`_ - data origin (unofficial, undocumented endpoints)

        .. _cfbfastR: https://cfbfastR.sportsdataverse.org
        .. _Yahoo Sports: https://sports.yahoo.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "league": league,
        "season": season,
        "divisionIds": division_ids,
        "getTeamsByDivision": get_teams_by_division,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/leagueTeams",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_leagues_season_states(
    leagues: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Yahoo shangrila persisted query `leaguesSeasonStates` -> one row per `leagues` entry

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/leaguesSeasonStates``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/leaguesSeasonStates

    Args:
        leagues: leagues query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.yahoo.yahoo_shangrila import yahoo_leagues_season_states
            yahoo_leagues_season_states()

        See Also:
            * `cfbfastR`_ - R sister package for college football
            * `Yahoo Sports`_ - data origin (unofficial, undocumented endpoints)

        .. _cfbfastR: https://cfbfastR.sportsdataverse.org
        .. _Yahoo Sports: https://sports.yahoo.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "leagues": leagues,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/leaguesSeasonStates",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_module_game(
    game_id: Optional[str] = None,
    image_height: Optional[str] = None,
    image_width: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Yahoo shangrila persisted query `moduleGame` -> one row per `games` entry

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/moduleGame``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/moduleGame

    Args:
        game_id: gameId query parameter.
        image_height: imageHeight query parameter.
        image_width: imageWidth query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.yahoo.yahoo_shangrila import yahoo_module_game
            yahoo_module_game()

        See Also:
            * `cfbfastR`_ - R sister package for college football
            * `Yahoo Sports`_ - data origin (unofficial, undocumented endpoints)

        .. _cfbfastR: https://cfbfastR.sportsdataverse.org
        .. _Yahoo Sports: https://sports.yahoo.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "gameId": game_id,
        "imageHeight": image_height,
        "imageWidth": image_width,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/moduleGame",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_motorsport_standings(
    league: Optional[str] = None,
    season: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Yahoo shangrila persisted query `motorsportStandings` -> one row per `leagues` entry

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/motorsportStandings``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/motorsportStandings

    Args:
        league: league query parameter.
        season: season query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.yahoo.yahoo_shangrila import yahoo_motorsport_standings
            yahoo_motorsport_standings()

        See Also:
            * `cfbfastR`_ - R sister package for college football
            * `Yahoo Sports`_ - data origin (unofficial, undocumented endpoints)

        .. _cfbfastR: https://cfbfastR.sportsdataverse.org
        .. _Yahoo Sports: https://sports.yahoo.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "league": league,
        "season": season,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/motorsportStandings",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_nascar_drivers(
    league: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Yahoo shangrila persisted query `nascarDrivers` -> one row per `leagues` entry

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/nascarDrivers``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/nascarDrivers

    Args:
        league: league query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.yahoo.yahoo_shangrila import yahoo_nascar_drivers
            yahoo_nascar_drivers()

        See Also:
            * `cfbfastR`_ - R sister package for college football
            * `Yahoo Sports`_ - data origin (unofficial, undocumented endpoints)

        .. _cfbfastR: https://cfbfastR.sportsdataverse.org
        .. _Yahoo Sports: https://sports.yahoo.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "league": league,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/nascarDrivers",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_nav_dropdown_tray(
    get_soccer_data: Optional[str] = None,
    soccer_league_ids: Optional[str] = None,
    soccer_team_ids: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Yahoo shangrila persisted query `navDropdownTray` -> tables: nfl, nhl, nba, mlb, wnba, ncaab, ncaaf, ncaaw, sportsbook_legal_states

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/navDropdownTray``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/navDropdownTray

    Args:
        get_soccer_data: getSoccerData query parameter.
        soccer_league_ids: soccerLeagueIds query parameter.
        soccer_team_ids: soccerTeamIds query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila_tables -> dict of polars DataFrames (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a dict of pandas DataFrames (same keys) instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A dict of polars/pandas DataFrames keyed by the payload's ``data`` collections by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            yahoo_nav_dropdown_tray()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "getSoccerData": get_soccer_data,
        "soccerLeagueIds": soccer_league_ids,
        "soccerTeamIds": soccer_team_ids,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/navDropdownTray",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila_tables(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_pick_distribution(
    league: Optional[str] = None,
    dates: Optional[str] = None,
    count: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Yahoo shangrila persisted query `pickDistribution` -> one row per `leagues` entry

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/pickDistribution``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/pickDistribution

    Args:
        league: league query parameter.
        dates: dates query parameter.
        count: count query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.yahoo.yahoo_shangrila import yahoo_pick_distribution
            yahoo_pick_distribution()

        See Also:
            * `cfbfastR`_ - R sister package for college football
            * `Yahoo Sports`_ - data origin (unofficial, undocumented endpoints)

        .. _cfbfastR: https://cfbfastR.sportsdataverse.org
        .. _Yahoo Sports: https://sports.yahoo.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "league": league,
        "dates": dates,
        "count": count,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/pickDistribution",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_playbook_boxscore(
    game_id: Optional[str] = None,
    standings_season_phases: Optional[str] = None,
    image_height: Optional[str] = None,
    image_width: Optional[str] = None,
    is_baseball: Optional[str] = None,
    is_football: Optional[str] = None,
    is_pro_basketball: Optional[str] = None,
    is_college_basketball: Optional[str] = None,
    is_hockey: Optional[str] = None,
    is_soccer: Optional[str] = None,
    event_state: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Yahoo shangrila persisted query `playbookBoxscore` -> tables: football_positions, football_stat_types, games

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/playbookBoxscore``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/playbookBoxscore

    Args:
        game_id: gameId query parameter.
        standings_season_phases: standingsSeasonPhases query parameter.
        image_height: imageHeight query parameter.
        image_width: imageWidth query parameter.
        is_baseball: isBaseball query parameter.
        is_football: isFootball query parameter.
        is_pro_basketball: isProBasketball query parameter.
        is_college_basketball: isCollegeBasketball query parameter.
        is_hockey: isHockey query parameter.
        is_soccer: isSoccer query parameter.
        event_state: eventState query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila_tables -> dict of polars DataFrames (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a dict of pandas DataFrames (same keys) instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A dict of polars/pandas DataFrames keyed by the payload's ``data`` collections by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            yahoo_playbook_boxscore()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "gameId": game_id,
        "standingsSeasonPhases": standings_season_phases,
        "imageHeight": image_height,
        "imageWidth": image_width,
        "isBaseball": is_baseball,
        "isFootball": is_football,
        "isProBasketball": is_pro_basketball,
        "isCollegeBasketball": is_college_basketball,
        "isHockey": is_hockey,
        "isSoccer": is_soccer,
        "eventState": event_state,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/playbookBoxscore",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila_tables(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_playbook_boxscore_poll(
    game_id: Optional[str] = None,
    standings_season_phases: Optional[str] = None,
    is_baseball: Optional[str] = None,
    is_football: Optional[str] = None,
    is_pro_basketball: Optional[str] = None,
    is_college_basketball: Optional[str] = None,
    is_hockey: Optional[str] = None,
    is_soccer: Optional[str] = None,
    event_state: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Yahoo shangrila persisted query `playbookBoxscorePoll` -> tables: football_positions, football_stat_types, games

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/playbookBoxscorePoll``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/playbookBoxscorePoll

    Args:
        game_id: gameId query parameter.
        standings_season_phases: standingsSeasonPhases query parameter.
        is_baseball: isBaseball query parameter.
        is_football: isFootball query parameter.
        is_pro_basketball: isProBasketball query parameter.
        is_college_basketball: isCollegeBasketball query parameter.
        is_hockey: isHockey query parameter.
        is_soccer: isSoccer query parameter.
        event_state: eventState query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila_tables -> dict of polars DataFrames (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a dict of pandas DataFrames (same keys) instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A dict of polars/pandas DataFrames keyed by the payload's ``data`` collections by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            yahoo_playbook_boxscore_poll()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "gameId": game_id,
        "standingsSeasonPhases": standings_season_phases,
        "isBaseball": is_baseball,
        "isFootball": is_football,
        "isProBasketball": is_pro_basketball,
        "isCollegeBasketball": is_college_basketball,
        "isHockey": is_hockey,
        "isSoccer": is_soccer,
        "eventState": event_state,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/playbookBoxscorePoll",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila_tables(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_playbook_boxscore_social_share(
    game_id: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Yahoo shangrila persisted query `playbookBoxscoreSocialShare` -> one row per `games` entry

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/playbookBoxscoreSocialShare``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/playbookBoxscoreSocialShare

    Args:
        game_id: gameId query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.yahoo.yahoo_shangrila import yahoo_playbook_boxscore_social_share
            yahoo_playbook_boxscore_social_share()

        See Also:
            * `cfbfastR`_ - R sister package for college football
            * `Yahoo Sports`_ - data origin (unofficial, undocumented endpoints)

        .. _cfbfastR: https://cfbfastR.sportsdataverse.org
        .. _Yahoo Sports: https://sports.yahoo.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "gameId": game_id,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/playbookBoxscoreSocialShare",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_playbook_combat_match(
    game_id: Optional[str] = None,
    image_height: Optional[str] = None,
    image_width: Optional[str] = None,
    headshot_height: Optional[str] = None,
    headshot_width: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Yahoo shangrila persisted query `playbookCombatMatch` -> one row per `games` entry

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/playbookCombatMatch``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/playbookCombatMatch

    Args:
        game_id: gameId query parameter.
        image_height: imageHeight query parameter.
        image_width: imageWidth query parameter.
        headshot_height: headshotHeight query parameter.
        headshot_width: headshotWidth query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.yahoo.yahoo_shangrila import yahoo_playbook_combat_match
            yahoo_playbook_combat_match()

        See Also:
            * `cfbfastR`_ - R sister package for college football
            * `Yahoo Sports`_ - data origin (unofficial, undocumented endpoints)

        .. _cfbfastR: https://cfbfastR.sportsdataverse.org
        .. _Yahoo Sports: https://sports.yahoo.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "gameId": game_id,
        "imageHeight": image_height,
        "imageWidth": image_width,
        "headshotHeight": headshot_height,
        "headshotWidth": headshot_width,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/playbookCombatMatch",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_playbook_game(
    game_id: Optional[str] = None,
    image_height: Optional[str] = None,
    image_width: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Yahoo shangrila persisted query `playbookGame` -> one row per `games` entry

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/playbookGame``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/playbookGame

    Args:
        game_id: gameId query parameter.
        image_height: imageHeight query parameter.
        image_width: imageWidth query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.yahoo.yahoo_shangrila import yahoo_playbook_game
            yahoo_playbook_game()

        See Also:
            * `cfbfastR`_ - R sister package for college football
            * `Yahoo Sports`_ - data origin (unofficial, undocumented endpoints)

        .. _cfbfastR: https://cfbfastR.sportsdataverse.org
        .. _Yahoo Sports: https://sports.yahoo.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "gameId": game_id,
        "imageHeight": image_height,
        "imageWidth": image_width,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/playbookGame",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_playbook_game_odds_poll(
    game_id: Optional[str] = None,
    event_state: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Yahoo shangrila persisted query `playbookGameOddsPoll` -> one row per `games` entry

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/playbookGameOddsPoll``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/playbookGameOddsPoll

    Args:
        game_id: gameId query parameter.
        event_state: eventState query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.yahoo.yahoo_shangrila import yahoo_playbook_game_odds_poll
            yahoo_playbook_game_odds_poll()

        See Also:
            * `cfbfastR`_ - R sister package for college football
            * `Yahoo Sports`_ - data origin (unofficial, undocumented endpoints)

        .. _cfbfastR: https://cfbfastR.sportsdataverse.org
        .. _Yahoo Sports: https://sports.yahoo.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "gameId": game_id,
        "eventState": event_state,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/playbookGameOddsPoll",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_playbook_golf_tournament(
    game_id: Optional[str] = None,
    season: Optional[str] = None,
    count: Optional[str] = None,
    stat_ids: Optional[str] = None,
    show_hole_results: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Yahoo shangrila persisted query `playbookGolfTournament` (response body not captured; shape unknown)

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/playbookGolfTournament``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/playbookGolfTournament

    Args:
        game_id: gameId query parameter.
        season: season query parameter.
        count: count query parameter.
        stat_ids: statIds query parameter.
        show_hole_results: showHoleResults query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.yahoo.yahoo_shangrila import yahoo_playbook_golf_tournament
            yahoo_playbook_golf_tournament()

        See Also:
            * `cfbfastR`_ - R sister package for college football
            * `Yahoo Sports`_ - data origin (unofficial, undocumented endpoints)

        .. _cfbfastR: https://cfbfastR.sportsdataverse.org
        .. _Yahoo Sports: https://sports.yahoo.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "gameId": game_id,
        "season": season,
        "count": count,
        "statIds": stat_ids,
        "showHoleResults": show_hole_results,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/playbookGolfTournament",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_playbook_league_odds(
    league: Optional[str] = None,
    dates: Optional[str] = None,
    count: Optional[str] = None,
    start_time_filter: Optional[str] = None,
    range_start_date: Optional[str] = None,
    range_end_date: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Yahoo shangrila persisted query `playbookLeagueOdds` -> one row per `leagues` entry

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/playbookLeagueOdds``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/playbookLeagueOdds

    Args:
        league: league query parameter.
        dates: dates query parameter.
        count: count query parameter.
        start_time_filter: startTimeFilter query parameter.
        range_start_date: rangeStartDate query parameter.
        range_end_date: rangeEndDate query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.yahoo.yahoo_shangrila import yahoo_playbook_league_odds
            yahoo_playbook_league_odds()

        See Also:
            * `cfbfastR`_ - R sister package for college football
            * `Yahoo Sports`_ - data origin (unofficial, undocumented endpoints)

        .. _cfbfastR: https://cfbfastR.sportsdataverse.org
        .. _Yahoo Sports: https://sports.yahoo.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "league": league,
        "dates": dates,
        "count": count,
        "startTimeFilter": start_time_filter,
        "rangeStartDate": range_start_date,
        "rangeEndDate": range_end_date,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/playbookLeagueOdds",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_playbook_player(
    player_id: Optional[str] = None,
    season_phases: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Yahoo shangrila persisted query `playbookPlayer` -> one row per `players` entry

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/playbookPlayer``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/playbookPlayer

    Args:
        player_id: playerId query parameter.
        season_phases: seasonPhases query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.yahoo.yahoo_shangrila import yahoo_playbook_player
            yahoo_playbook_player()

        See Also:
            * `cfbfastR`_ - R sister package for college football
            * `Yahoo Sports`_ - data origin (unofficial, undocumented endpoints)

        .. _cfbfastR: https://cfbfastR.sportsdataverse.org
        .. _Yahoo Sports: https://sports.yahoo.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "playerId": player_id,
        "seasonPhases": season_phases,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/playbookPlayer",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_playbook_player_social_share(
    player_id: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Yahoo shangrila persisted query `playbookPlayerSocialShare` -> one row per `players` entry

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/playbookPlayerSocialShare``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/playbookPlayerSocialShare

    Args:
        player_id: playerId query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.yahoo.yahoo_shangrila import yahoo_playbook_player_social_share
            yahoo_playbook_player_social_share()

        See Also:
            * `cfbfastR`_ - R sister package for college football
            * `Yahoo Sports`_ - data origin (unofficial, undocumented endpoints)

        .. _cfbfastR: https://cfbfastR.sportsdataverse.org
        .. _Yahoo Sports: https://sports.yahoo.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "playerId": player_id,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/playbookPlayerSocialShare",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_playbook_race(
    game_id: Optional[str] = None,
    player_image_height: Optional[str] = None,
    player_image_width: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Yahoo shangrila persisted query `playbookRace` -> one row per `games` entry

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/playbookRace``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/playbookRace

    Args:
        game_id: gameId query parameter.
        player_image_height: playerImageHeight query parameter.
        player_image_width: playerImageWidth query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.yahoo.yahoo_shangrila import yahoo_playbook_race
            yahoo_playbook_race()

        See Also:
            * `cfbfastR`_ - R sister package for college football
            * `Yahoo Sports`_ - data origin (unofficial, undocumented endpoints)

        .. _cfbfastR: https://cfbfastR.sportsdataverse.org
        .. _Yahoo Sports: https://sports.yahoo.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "gameId": game_id,
        "playerImageHeight": player_image_height,
        "playerImageWidth": player_image_width,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/playbookRace",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_playbook_team(
    team_id: Optional[str] = None,
    image_height: Optional[str] = None,
    image_width: Optional[str] = None,
    league_short_name: Optional[str] = None,
    disable_conference: Optional[str] = None,
    disable_division: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Yahoo shangrila persisted query `playbookTeam` -> tables: teams, leagues

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/playbookTeam``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/playbookTeam

    Args:
        team_id: teamId query parameter.
        image_height: imageHeight query parameter.
        image_width: imageWidth query parameter.
        league_short_name: leagueShortName query parameter.
        disable_conference: disableConference query parameter.
        disable_division: disableDivision query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila_tables -> dict of polars DataFrames (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a dict of pandas DataFrames (same keys) instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A dict of polars/pandas DataFrames keyed by the payload's ``data`` collections by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            yahoo_playbook_team()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "teamId": team_id,
        "imageHeight": image_height,
        "imageWidth": image_width,
        "leagueShortName": league_short_name,
        "disableConference": disable_conference,
        "disableDivision": disable_division,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/playbookTeam",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila_tables(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_playbook_team_basic(
    team_id: Optional[str] = None,
    image_height: Optional[str] = None,
    image_width: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Yahoo shangrila persisted query `playbookTeamBasic` -> one row per `teams` entry

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/playbookTeamBasic``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/playbookTeamBasic

    Args:
        team_id: teamId query parameter.
        image_height: imageHeight query parameter.
        image_width: imageWidth query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.yahoo.yahoo_shangrila import yahoo_playbook_team_basic
            yahoo_playbook_team_basic()

        See Also:
            * `cfbfastR`_ - R sister package for college football
            * `Yahoo Sports`_ - data origin (unofficial, undocumented endpoints)

        .. _cfbfastR: https://cfbfastR.sportsdataverse.org
        .. _Yahoo Sports: https://sports.yahoo.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "teamId": team_id,
        "imageHeight": image_height,
        "imageWidth": image_width,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/playbookTeamBasic",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_playbook_team_social_share(
    team_id: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Yahoo shangrila persisted query `playbookTeamSocialShare` -> one row per `teams` entry

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/playbookTeamSocialShare``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/playbookTeamSocialShare

    Args:
        team_id: teamId query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.yahoo.yahoo_shangrila import yahoo_playbook_team_social_share
            yahoo_playbook_team_social_share()

        See Also:
            * `cfbfastR`_ - R sister package for college football
            * `Yahoo Sports`_ - data origin (unofficial, undocumented endpoints)

        .. _cfbfastR: https://cfbfastR.sportsdataverse.org
        .. _Yahoo Sports: https://sports.yahoo.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "teamId": team_id,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/playbookTeamSocialShare",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_playbook_tennis_match(
    game_id: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Yahoo shangrila persisted query `playbookTennisMatch` -> one row per `events` entry

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/playbookTennisMatch``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/playbookTennisMatch

    Args:
        game_id: gameId query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.yahoo.yahoo_shangrila import yahoo_playbook_tennis_match
            yahoo_playbook_tennis_match()

        See Also:
            * `cfbfastR`_ - R sister package for college football
            * `Yahoo Sports`_ - data origin (unofficial, undocumented endpoints)

        .. _cfbfastR: https://cfbfastR.sportsdataverse.org
        .. _Yahoo Sports: https://sports.yahoo.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "gameId": game_id,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/playbookTennisMatch",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_player_basic(
    league: Optional[str] = None,
    player_id: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Yahoo shangrila persisted query `playerBasic` -> tables: players, leagues

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/playerBasic``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/playerBasic

    Args:
        league: league query parameter.
        player_id: playerId query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila_tables -> dict of polars DataFrames (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a dict of pandas DataFrames (same keys) instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A dict of polars/pandas DataFrames keyed by the payload's ``data`` collections by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            yahoo_player_basic()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "league": league,
        "playerId": player_id,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/playerBasic",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila_tables(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_player_career_stats(
    player_id: Optional[str] = None,
    season_phases: Optional[str] = None,
    football_stat_ids: Optional[str] = None,
    basketball_stat_ids: Optional[str] = None,
    baseball_stat_ids: Optional[str] = None,
    hockey_stat_ids: Optional[str] = None,
    soccer_stat_ids: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Yahoo shangrila persisted query `playerCareerStats` -> one row per `players` entry

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/playerCareerStats``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/playerCareerStats

    Args:
        player_id: playerId query parameter.
        season_phases: seasonPhases query parameter.
        football_stat_ids: footballStatIds query parameter.
        basketball_stat_ids: basketballStatIds query parameter.
        baseball_stat_ids: baseballStatIds query parameter.
        hockey_stat_ids: hockeyStatIds query parameter.
        soccer_stat_ids: soccerStatIds query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.yahoo.yahoo_shangrila import yahoo_player_career_stats
            yahoo_player_career_stats()

        See Also:
            * `cfbfastR`_ - R sister package for college football
            * `Yahoo Sports`_ - data origin (unofficial, undocumented endpoints)

        .. _cfbfastR: https://cfbfastR.sportsdataverse.org
        .. _Yahoo Sports: https://sports.yahoo.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "playerId": player_id,
        "seasonPhases": season_phases,
        "footballStatIds": football_stat_ids,
        "basketballStatIds": basketball_stat_ids,
        "baseballStatIds": baseball_stat_ids,
        "hockeyStatIds": hockey_stat_ids,
        "soccerStatIds": soccer_stat_ids,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/playerCareerStats",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_player_game_log(
    player_id: Optional[str] = None,
    count: Optional[str] = None,
    seasons: Optional[str] = None,
    season_phases: Optional[str] = None,
    football_stat_ids: Optional[str] = None,
    basketball_stat_ids: Optional[str] = None,
    baseball_stat_ids: Optional[str] = None,
    hockey_stat_ids: Optional[str] = None,
    soccer_stat_ids: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Yahoo shangrila persisted query `playerGameLog` -> one row per `players` entry

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/playerGameLog``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/playerGameLog

    Args:
        player_id: playerId query parameter.
        count: count query parameter.
        seasons: seasons query parameter.
        season_phases: seasonPhases query parameter.
        football_stat_ids: footballStatIds query parameter.
        basketball_stat_ids: basketballStatIds query parameter.
        baseball_stat_ids: baseballStatIds query parameter.
        hockey_stat_ids: hockeyStatIds query parameter.
        soccer_stat_ids: soccerStatIds query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.yahoo.yahoo_shangrila import yahoo_player_game_log
            yahoo_player_game_log()

        See Also:
            * `cfbfastR`_ - R sister package for college football
            * `Yahoo Sports`_ - data origin (unofficial, undocumented endpoints)

        .. _cfbfastR: https://cfbfastR.sportsdataverse.org
        .. _Yahoo Sports: https://sports.yahoo.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "playerId": player_id,
        "count": count,
        "seasons": seasons,
        "seasonPhases": season_phases,
        "footballStatIds": football_stat_ids,
        "basketballStatIds": basketball_stat_ids,
        "baseballStatIds": baseball_stat_ids,
        "hockeyStatIds": hockey_stat_ids,
        "soccerStatIds": soccer_stat_ids,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/playerGameLog",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_player_props(
    player_id: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Yahoo shangrila persisted query `playerProps` -> one row per `players` entry

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/playerProps``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/playerProps

    Args:
        player_id: playerId query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.yahoo.yahoo_shangrila import yahoo_player_props
            yahoo_player_props()

        See Also:
            * `cfbfastR`_ - R sister package for college football
            * `Yahoo Sports`_ - data origin (unofficial, undocumented endpoints)

        .. _cfbfastR: https://cfbfastR.sportsdataverse.org
        .. _Yahoo Sports: https://sports.yahoo.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "playerId": player_id,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/playerProps",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_player_search(
    league: Optional[str] = None,
    name: Optional[str] = None,
    on_active_roster_only: Optional[str] = None,
    nfl_position_id: Optional[str] = None,
    nba_position_id: Optional[str] = None,
    mlb_position_id: Optional[str] = None,
    nhl_position_id: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Yahoo shangrila persisted query `playerSearch` -> one row per `leagues.players` entry

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/playerSearch``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/playerSearch

    Args:
        league: league query parameter.
        name: name query parameter.
        on_active_roster_only: onActiveRosterOnly query parameter.
        nfl_position_id: nflPositionId query parameter.
        nba_position_id: nbaPositionId query parameter.
        mlb_position_id: mlbPositionId query parameter.
        nhl_position_id: nhlPositionId query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.yahoo.yahoo_shangrila import yahoo_player_search
            yahoo_player_search()

        See Also:
            * `cfbfastR`_ - R sister package for college football
            * `Yahoo Sports`_ - data origin (unofficial, undocumented endpoints)

        .. _cfbfastR: https://cfbfastR.sportsdataverse.org
        .. _Yahoo Sports: https://sports.yahoo.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "league": league,
        "name": name,
        "onActiveRosterOnly": on_active_roster_only,
        "nflPositionId": nfl_position_id,
        "nbaPositionId": nba_position_id,
        "mlbPositionId": mlb_position_id,
        "nhlPositionId": nhl_position_id,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/playerSearch",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_player_season_stats(
    player_id: Optional[str] = None,
    seasons: Optional[str] = None,
    season_phases: Optional[str] = None,
    football_stat_ids: Optional[str] = None,
    football_cut_type_groups: Optional[str] = None,
    basketball_stat_ids: Optional[str] = None,
    basketball_cut_type_groups: Optional[str] = None,
    baseball_stat_ids: Optional[str] = None,
    baseball_cut_type_groups: Optional[str] = None,
    hockey_stat_ids: Optional[str] = None,
    hockey_cut_type_groups: Optional[str] = None,
    group_by_season_phase: Optional[str] = None,
    use_player_unique_id: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Yahoo shangrila persisted query `playerSeasonStats` -> one row per `players` entry

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/playerSeasonStats``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/playerSeasonStats

    Args:
        player_id: playerId query parameter.
        seasons: seasons query parameter.
        season_phases: seasonPhases query parameter.
        football_stat_ids: footballStatIds query parameter.
        football_cut_type_groups: footballCutTypeGroups query parameter.
        basketball_stat_ids: basketballStatIds query parameter.
        basketball_cut_type_groups: basketballCutTypeGroups query parameter.
        baseball_stat_ids: baseballStatIds query parameter.
        baseball_cut_type_groups: baseballCutTypeGroups query parameter.
        hockey_stat_ids: hockeyStatIds query parameter.
        hockey_cut_type_groups: hockeyCutTypeGroups query parameter.
        group_by_season_phase: groupBySeasonPhase query parameter.
        use_player_unique_id: usePlayerUniqueId query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.yahoo.yahoo_shangrila import yahoo_player_season_stats
            yahoo_player_season_stats()

        See Also:
            * `cfbfastR`_ - R sister package for college football
            * `Yahoo Sports`_ - data origin (unofficial, undocumented endpoints)

        .. _cfbfastR: https://cfbfastR.sportsdataverse.org
        .. _Yahoo Sports: https://sports.yahoo.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "playerId": player_id,
        "seasons": seasons,
        "seasonPhases": season_phases,
        "footballStatIds": football_stat_ids,
        "footballCutTypeGroups": football_cut_type_groups,
        "basketballStatIds": basketball_stat_ids,
        "basketballCutTypeGroups": basketball_cut_type_groups,
        "baseballStatIds": baseball_stat_ids,
        "baseballCutTypeGroups": baseball_cut_type_groups,
        "hockeyStatIds": hockey_stat_ids,
        "hockeyCutTypeGroups": hockey_cut_type_groups,
        "groupBySeasonPhase": group_by_season_phase,
        "usePlayerUniqueId": use_player_unique_id,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/playerSeasonStats",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_playoff_bracket(
    league: Optional[str] = None,
    season: Optional[str] = None,
    tournament: Optional[str] = None,
    type: Optional[str] = None,
    playoff_rounds: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Yahoo shangrila persisted query `playoffBracket` -> one row per `leagues.bracketSlots` entry

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/playoffBracket``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/playoffBracket

    Args:
        league: league query parameter.
        season: season query parameter.
        tournament: tournament query parameter.
        type: type query parameter.
        playoff_rounds: playoffRounds query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.yahoo.yahoo_shangrila import yahoo_playoff_bracket
            yahoo_playoff_bracket()

        See Also:
            * `cfbfastR`_ - R sister package for college football
            * `Yahoo Sports`_ - data origin (unofficial, undocumented endpoints)

        .. _cfbfastR: https://cfbfastR.sportsdataverse.org
        .. _Yahoo Sports: https://sports.yahoo.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "league": league,
        "season": season,
        "tournament": tournament,
        "type": type,
        "playoffRounds": playoff_rounds,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/playoffBracket",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_playoff_series_game(
    game_id: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Yahoo shangrila persisted query `playoffSeriesGame` -> one row per `games` entry

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/playoffSeriesGame``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/playoffSeriesGame

    Args:
        game_id: gameId query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.yahoo.yahoo_shangrila import yahoo_playoff_series_game
            yahoo_playoff_series_game()

        See Also:
            * `cfbfastR`_ - R sister package for college football
            * `Yahoo Sports`_ - data origin (unofficial, undocumented endpoints)

        .. _cfbfastR: https://cfbfastR.sportsdataverse.org
        .. _Yahoo Sports: https://sports.yahoo.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "gameId": game_id,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/playoffSeriesGame",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_polymarket_game(
    game_id: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Yahoo shangrila persisted query `polymarketGame` -> one row per `games` entry

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/polymarketGame``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/polymarketGame

    Args:
        game_id: gameId query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.yahoo.yahoo_shangrila import yahoo_polymarket_game
            yahoo_polymarket_game()

        See Also:
            * `cfbfastR`_ - R sister package for college football
            * `Yahoo Sports`_ - data origin (unofficial, undocumented endpoints)

        .. _cfbfastR: https://cfbfastR.sportsdataverse.org
        .. _Yahoo Sports: https://sports.yahoo.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "gameId": game_id,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/polymarketGame",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_racing_schedule(
    league: Optional[str] = None,
    season: Optional[str] = None,
    today: Optional[str] = None,
    has_series: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Yahoo shangrila persisted query `racingSchedule` -> one row per `leagues` entry

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/racingSchedule``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/racingSchedule

    Args:
        league: league query parameter.
        season: season query parameter.
        today: today query parameter.
        has_series: hasSeries query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.yahoo.yahoo_shangrila import yahoo_racing_schedule
            yahoo_racing_schedule()

        See Also:
            * `cfbfastR`_ - R sister package for college football
            * `Yahoo Sports`_ - data origin (unofficial, undocumented endpoints)

        .. _cfbfastR: https://cfbfastR.sportsdataverse.org
        .. _Yahoo Sports: https://sports.yahoo.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "league": league,
        "season": season,
        "today": today,
        "hasSeries": has_series,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/racingSchedule",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_scoreboard_game(
    game_id: Optional[str] = None,
    season: Optional[str] = None,
    season_phase: Optional[str] = None,
    stat_leader_count: Optional[str] = None,
    single_stat_leader: Optional[str] = None,
    bet_event_state: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Yahoo shangrila persisted query `scoreboardGame` -> one row per `games` entry

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/scoreboardGame``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/scoreboardGame

    Args:
        game_id: gameId query parameter.
        season: season query parameter.
        season_phase: seasonPhase query parameter.
        stat_leader_count: statLeaderCount query parameter.
        single_stat_leader: singleStatLeader query parameter.
        bet_event_state: betEventState query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.yahoo.yahoo_shangrila import yahoo_scoreboard_game
            yahoo_scoreboard_game()

        See Also:
            * `cfbfastR`_ - R sister package for college football
            * `Yahoo Sports`_ - data origin (unofficial, undocumented endpoints)

        .. _cfbfastR: https://cfbfastR.sportsdataverse.org
        .. _Yahoo Sports: https://sports.yahoo.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "gameId": game_id,
        "season": season,
        "seasonPhase": season_phase,
        "statLeaderCount": stat_leader_count,
        "singleStatLeader": single_stat_leader,
        "betEventState": bet_event_state,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/scoreboardGame",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_season_stats_football_defense_ncaaf(
    season: Optional[int] = None,
    league: Optional[str] = None,
    league_structure: Optional[str] = None,
    count: Optional[int] = None,
    sort_stat_id: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Legacy player season Defense leaders (NCAAF)

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/seasonStatsFootballDefenseNcaaf``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/seasonStatsFootballDefenseNcaaf

    Args:
        season: season query parameter.
        league: league query parameter.
        league_structure: leagueStructure query parameter.
        count: count query parameter.
        sort_stat_id: sortStatId query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila_tables -> dict of polars DataFrames (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a dict of pandas DataFrames (same keys) instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A dict of polars/pandas DataFrames keyed by the payload's ``data`` collections by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            yahoo_season_stats_football_defense_ncaaf()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "season": season,
        "league": league,
        "leagueStructure": league_structure,
        "count": count,
        "sortStatId": sort_stat_id,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/seasonStatsFootballDefenseNcaaf",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila_tables(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_season_stats_football_kicking_ncaaf(
    season: Optional[int] = None,
    league: Optional[str] = None,
    league_structure: Optional[str] = None,
    count: Optional[int] = None,
    sort_stat_id: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Legacy player season Kicking leaders (NCAAF)

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/seasonStatsFootballKickingNcaaf``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/seasonStatsFootballKickingNcaaf

    Args:
        season: season query parameter.
        league: league query parameter.
        league_structure: leagueStructure query parameter.
        count: count query parameter.
        sort_stat_id: sortStatId query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila_tables -> dict of polars DataFrames (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a dict of pandas DataFrames (same keys) instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A dict of polars/pandas DataFrames keyed by the payload's ``data`` collections by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            yahoo_season_stats_football_kicking_ncaaf()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "season": season,
        "league": league,
        "leagueStructure": league_structure,
        "count": count,
        "sortStatId": sort_stat_id,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/seasonStatsFootballKickingNcaaf",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila_tables(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_season_stats_football_passing_ncaaf(
    season: Optional[int] = None,
    league: Optional[str] = None,
    league_structure: Optional[str] = None,
    count: Optional[int] = None,
    sort_stat_id: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Legacy player season Passing leaders (NCAAF)

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/seasonStatsFootballPassingNcaaf``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/seasonStatsFootballPassingNcaaf

    Args:
        season: season query parameter.
        league: league query parameter.
        league_structure: leagueStructure query parameter.
        count: count query parameter.
        sort_stat_id: sortStatId query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila_tables -> dict of polars DataFrames (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a dict of pandas DataFrames (same keys) instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A dict of polars/pandas DataFrames keyed by the payload's ``data`` collections by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            yahoo_season_stats_football_passing_ncaaf()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "season": season,
        "league": league,
        "leagueStructure": league_structure,
        "count": count,
        "sortStatId": sort_stat_id,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/seasonStatsFootballPassingNcaaf",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila_tables(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_season_stats_football_punting_ncaaf(
    season: Optional[int] = None,
    league: Optional[str] = None,
    league_structure: Optional[str] = None,
    count: Optional[int] = None,
    sort_stat_id: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Legacy player season Punting leaders (NCAAF)

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/seasonStatsFootballPuntingNcaaf``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/seasonStatsFootballPuntingNcaaf

    Args:
        season: season query parameter.
        league: league query parameter.
        league_structure: leagueStructure query parameter.
        count: count query parameter.
        sort_stat_id: sortStatId query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila_tables -> dict of polars DataFrames (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a dict of pandas DataFrames (same keys) instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A dict of polars/pandas DataFrames keyed by the payload's ``data`` collections by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            yahoo_season_stats_football_punting_ncaaf()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "season": season,
        "league": league,
        "leagueStructure": league_structure,
        "count": count,
        "sortStatId": sort_stat_id,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/seasonStatsFootballPuntingNcaaf",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila_tables(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_season_stats_football_receiving_ncaaf(
    season: Optional[int] = None,
    league: Optional[str] = None,
    league_structure: Optional[str] = None,
    count: Optional[int] = None,
    sort_stat_id: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Legacy player season Receiving leaders (NCAAF)

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/seasonStatsFootballReceivingNcaaf``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/seasonStatsFootballReceivingNcaaf

    Args:
        season: season query parameter.
        league: league query parameter.
        league_structure: leagueStructure query parameter.
        count: count query parameter.
        sort_stat_id: sortStatId query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila_tables -> dict of polars DataFrames (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a dict of pandas DataFrames (same keys) instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A dict of polars/pandas DataFrames keyed by the payload's ``data`` collections by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            yahoo_season_stats_football_receiving_ncaaf()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "season": season,
        "league": league,
        "leagueStructure": league_structure,
        "count": count,
        "sortStatId": sort_stat_id,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/seasonStatsFootballReceivingNcaaf",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila_tables(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_season_stats_football_returns_ncaaf(
    season: Optional[int] = None,
    league: Optional[str] = None,
    league_structure: Optional[str] = None,
    count: Optional[int] = None,
    sort_stat_id: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Legacy player season Returns leaders (NCAAF)

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/seasonStatsFootballReturnsNcaaf``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/seasonStatsFootballReturnsNcaaf

    Args:
        season: season query parameter.
        league: league query parameter.
        league_structure: leagueStructure query parameter.
        count: count query parameter.
        sort_stat_id: sortStatId query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila_tables -> dict of polars DataFrames (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a dict of pandas DataFrames (same keys) instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A dict of polars/pandas DataFrames keyed by the payload's ``data`` collections by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            yahoo_season_stats_football_returns_ncaaf()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "season": season,
        "league": league,
        "leagueStructure": league_structure,
        "count": count,
        "sortStatId": sort_stat_id,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/seasonStatsFootballReturnsNcaaf",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila_tables(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_season_stats_football_rushing_ncaaf(
    season: Optional[int] = None,
    league: Optional[str] = None,
    league_structure: Optional[str] = None,
    count: Optional[int] = None,
    sort_stat_id: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Legacy player season Rushing leaders (NCAAF)

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/seasonStatsFootballRushingNcaaf``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/seasonStatsFootballRushingNcaaf

    Args:
        season: season query parameter.
        league: league query parameter.
        league_structure: leagueStructure query parameter.
        count: count query parameter.
        sort_stat_id: sortStatId query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila_tables -> dict of polars DataFrames (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a dict of pandas DataFrames (same keys) instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A dict of polars/pandas DataFrames keyed by the payload's ``data`` collections by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            yahoo_season_stats_football_rushing_ncaaf()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "season": season,
        "league": league,
        "leagueStructure": league_structure,
        "count": count,
        "sortStatId": sort_stat_id,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/seasonStatsFootballRushingNcaaf",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila_tables(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_season_team_stats_football_defense(
    season: Optional[int] = None,
    league: Optional[str] = None,
    league_structure: Optional[str] = None,
    count: Optional[int] = None,
    sort_stat_id: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Legacy team season Defense (NCAAF)

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/seasonTeamStatsFootballDefense``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/seasonTeamStatsFootballDefense

    Args:
        season: season query parameter.
        league: league query parameter.
        league_structure: leagueStructure query parameter.
        count: count query parameter.
        sort_stat_id: sortStatId query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila_tables -> dict of polars DataFrames (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a dict of pandas DataFrames (same keys) instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A dict of polars/pandas DataFrames keyed by the payload's ``data`` collections by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            yahoo_season_team_stats_football_defense()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "season": season,
        "league": league,
        "leagueStructure": league_structure,
        "count": count,
        "sortStatId": sort_stat_id,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/seasonTeamStatsFootballDefense",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila_tables(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_season_team_stats_football_kicking(
    season: Optional[int] = None,
    league: Optional[str] = None,
    league_structure: Optional[str] = None,
    count: Optional[int] = None,
    sort_stat_id: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Legacy team season Kicking (NCAAF)

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/seasonTeamStatsFootballKicking``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/seasonTeamStatsFootballKicking

    Args:
        season: season query parameter.
        league: league query parameter.
        league_structure: leagueStructure query parameter.
        count: count query parameter.
        sort_stat_id: sortStatId query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila_tables -> dict of polars DataFrames (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a dict of pandas DataFrames (same keys) instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A dict of polars/pandas DataFrames keyed by the payload's ``data`` collections by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            yahoo_season_team_stats_football_kicking()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "season": season,
        "league": league,
        "leagueStructure": league_structure,
        "count": count,
        "sortStatId": sort_stat_id,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/seasonTeamStatsFootballKicking",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila_tables(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_season_team_stats_football_kickoffs(
    season: Optional[int] = None,
    league: Optional[str] = None,
    league_structure: Optional[str] = None,
    count: Optional[int] = None,
    sort_stat_id: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Legacy team season Kickoffs (NCAAF)

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/seasonTeamStatsFootballKickoffs``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/seasonTeamStatsFootballKickoffs

    Args:
        season: season query parameter.
        league: league query parameter.
        league_structure: leagueStructure query parameter.
        count: count query parameter.
        sort_stat_id: sortStatId query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila_tables -> dict of polars DataFrames (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a dict of pandas DataFrames (same keys) instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A dict of polars/pandas DataFrames keyed by the payload's ``data`` collections by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            yahoo_season_team_stats_football_kickoffs()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "season": season,
        "league": league,
        "leagueStructure": league_structure,
        "count": count,
        "sortStatId": sort_stat_id,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/seasonTeamStatsFootballKickoffs",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila_tables(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_season_team_stats_football_offense(
    season: Optional[int] = None,
    league: Optional[str] = None,
    league_structure: Optional[str] = None,
    count: Optional[int] = None,
    sort_stat_id: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Legacy team season Offense (NCAAF)

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/seasonTeamStatsFootballOffense``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/seasonTeamStatsFootballOffense

    Args:
        season: season query parameter.
        league: league query parameter.
        league_structure: leagueStructure query parameter.
        count: count query parameter.
        sort_stat_id: sortStatId query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila_tables -> dict of polars DataFrames (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a dict of pandas DataFrames (same keys) instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A dict of polars/pandas DataFrames keyed by the payload's ``data`` collections by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            yahoo_season_team_stats_football_offense()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "season": season,
        "league": league,
        "leagueStructure": league_structure,
        "count": count,
        "sortStatId": sort_stat_id,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/seasonTeamStatsFootballOffense",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila_tables(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_season_team_stats_football_passing(
    season: Optional[int] = None,
    league: Optional[str] = None,
    league_structure: Optional[str] = None,
    count: Optional[int] = None,
    sort_stat_id: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Legacy team season Passing (NCAAF)

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/seasonTeamStatsFootballPassing``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/seasonTeamStatsFootballPassing

    Args:
        season: season query parameter.
        league: league query parameter.
        league_structure: leagueStructure query parameter.
        count: count query parameter.
        sort_stat_id: sortStatId query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila_tables -> dict of polars DataFrames (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a dict of pandas DataFrames (same keys) instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A dict of polars/pandas DataFrames keyed by the payload's ``data`` collections by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            yahoo_season_team_stats_football_passing()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "season": season,
        "league": league,
        "leagueStructure": league_structure,
        "count": count,
        "sortStatId": sort_stat_id,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/seasonTeamStatsFootballPassing",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila_tables(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_season_team_stats_football_passing_defense(
    season: Optional[int] = None,
    league: Optional[str] = None,
    league_structure: Optional[str] = None,
    count: Optional[int] = None,
    sort_stat_id: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Legacy team Passing defense allowed (NCAAF)

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/seasonTeamStatsFootballPassingDefense``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/seasonTeamStatsFootballPassingDefense

    Args:
        season: season query parameter.
        league: league query parameter.
        league_structure: leagueStructure query parameter.
        count: count query parameter.
        sort_stat_id: sortStatId query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila_tables -> dict of polars DataFrames (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a dict of pandas DataFrames (same keys) instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A dict of polars/pandas DataFrames keyed by the payload's ``data`` collections by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            yahoo_season_team_stats_football_passing_defense()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "season": season,
        "league": league,
        "leagueStructure": league_structure,
        "count": count,
        "sortStatId": sort_stat_id,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/seasonTeamStatsFootballPassingDefense",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila_tables(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_season_team_stats_football_punting(
    season: Optional[int] = None,
    league: Optional[str] = None,
    league_structure: Optional[str] = None,
    count: Optional[int] = None,
    sort_stat_id: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Legacy team season Punting (NCAAF)

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/seasonTeamStatsFootballPunting``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/seasonTeamStatsFootballPunting

    Args:
        season: season query parameter.
        league: league query parameter.
        league_structure: leagueStructure query parameter.
        count: count query parameter.
        sort_stat_id: sortStatId query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila_tables -> dict of polars DataFrames (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a dict of pandas DataFrames (same keys) instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A dict of polars/pandas DataFrames keyed by the payload's ``data`` collections by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            yahoo_season_team_stats_football_punting()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "season": season,
        "league": league,
        "leagueStructure": league_structure,
        "count": count,
        "sortStatId": sort_stat_id,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/seasonTeamStatsFootballPunting",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila_tables(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_season_team_stats_football_receiving(
    season: Optional[int] = None,
    league: Optional[str] = None,
    league_structure: Optional[str] = None,
    count: Optional[int] = None,
    sort_stat_id: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Legacy team season Receiving (NCAAF)

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/seasonTeamStatsFootballReceiving``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/seasonTeamStatsFootballReceiving

    Args:
        season: season query parameter.
        league: league query parameter.
        league_structure: leagueStructure query parameter.
        count: count query parameter.
        sort_stat_id: sortStatId query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila_tables -> dict of polars DataFrames (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a dict of pandas DataFrames (same keys) instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A dict of polars/pandas DataFrames keyed by the payload's ``data`` collections by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            yahoo_season_team_stats_football_receiving()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "season": season,
        "league": league,
        "leagueStructure": league_structure,
        "count": count,
        "sortStatId": sort_stat_id,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/seasonTeamStatsFootballReceiving",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila_tables(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_season_team_stats_football_receiving_defense(
    season: Optional[int] = None,
    league: Optional[str] = None,
    league_structure: Optional[str] = None,
    count: Optional[int] = None,
    sort_stat_id: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Legacy team Receiving defense allowed (NCAAF)

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/seasonTeamStatsFootballReceivingDefense``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/seasonTeamStatsFootballReceivingDefense

    Args:
        season: season query parameter.
        league: league query parameter.
        league_structure: leagueStructure query parameter.
        count: count query parameter.
        sort_stat_id: sortStatId query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila_tables -> dict of polars DataFrames (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a dict of pandas DataFrames (same keys) instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A dict of polars/pandas DataFrames keyed by the payload's ``data`` collections by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            yahoo_season_team_stats_football_receiving_defense()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "season": season,
        "league": league,
        "leagueStructure": league_structure,
        "count": count,
        "sortStatId": sort_stat_id,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/seasonTeamStatsFootballReceivingDefense",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila_tables(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_season_team_stats_football_returns(
    season: Optional[int] = None,
    league: Optional[str] = None,
    league_structure: Optional[str] = None,
    count: Optional[int] = None,
    sort_stat_id: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Legacy team season Returns (NCAAF)

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/seasonTeamStatsFootballReturns``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/seasonTeamStatsFootballReturns

    Args:
        season: season query parameter.
        league: league query parameter.
        league_structure: leagueStructure query parameter.
        count: count query parameter.
        sort_stat_id: sortStatId query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila_tables -> dict of polars DataFrames (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a dict of pandas DataFrames (same keys) instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A dict of polars/pandas DataFrames keyed by the payload's ``data`` collections by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            yahoo_season_team_stats_football_returns()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "season": season,
        "league": league,
        "leagueStructure": league_structure,
        "count": count,
        "sortStatId": sort_stat_id,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/seasonTeamStatsFootballReturns",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila_tables(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_season_team_stats_football_rushing(
    season: Optional[int] = None,
    league: Optional[str] = None,
    league_structure: Optional[str] = None,
    count: Optional[int] = None,
    sort_stat_id: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Legacy team season Rushing (NCAAF)

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/seasonTeamStatsFootballRushing``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/seasonTeamStatsFootballRushing

    Args:
        season: season query parameter.
        league: league query parameter.
        league_structure: leagueStructure query parameter.
        count: count query parameter.
        sort_stat_id: sortStatId query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila_tables -> dict of polars DataFrames (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a dict of pandas DataFrames (same keys) instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A dict of polars/pandas DataFrames keyed by the payload's ``data`` collections by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            yahoo_season_team_stats_football_rushing()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "season": season,
        "league": league,
        "leagueStructure": league_structure,
        "count": count,
        "sortStatId": sort_stat_id,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/seasonTeamStatsFootballRushing",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila_tables(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_season_team_stats_football_rushing_defense(
    season: Optional[int] = None,
    league: Optional[str] = None,
    league_structure: Optional[str] = None,
    count: Optional[int] = None,
    sort_stat_id: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Legacy team Rushing defense allowed (NCAAF)

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/seasonTeamStatsFootballRushingDefense``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/seasonTeamStatsFootballRushingDefense

    Args:
        season: season query parameter.
        league: league query parameter.
        league_structure: leagueStructure query parameter.
        count: count query parameter.
        sort_stat_id: sortStatId query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila_tables -> dict of polars DataFrames (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a dict of pandas DataFrames (same keys) instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A dict of polars/pandas DataFrames keyed by the payload's ``data`` collections by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            yahoo_season_team_stats_football_rushing_defense()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "season": season,
        "league": league,
        "leagueStructure": league_structure,
        "count": count,
        "sortStatId": sort_stat_id,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/seasonTeamStatsFootballRushingDefense",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila_tables(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_team_injuries(
    team_id: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Yahoo shangrila persisted query `teamInjuries` -> one row per `teams` entry

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/teamInjuries``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/teamInjuries

    Args:
        team_id: teamId query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.yahoo.yahoo_shangrila import yahoo_team_injuries
            yahoo_team_injuries()

        See Also:
            * `cfbfastR`_ - R sister package for college football
            * `Yahoo Sports`_ - data origin (unofficial, undocumented endpoints)

        .. _cfbfastR: https://cfbfastR.sportsdataverse.org
        .. _Yahoo Sports: https://sports.yahoo.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "teamId": team_id,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/teamInjuries",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_team_playoff_series(
    team_id: Optional[str] = None,
    season: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Yahoo shangrila persisted query `teamPlayoffSeries` -> one row per `teams.playoffSeries` entry

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/teamPlayoffSeries``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/teamPlayoffSeries

    Args:
        team_id: teamId query parameter.
        season: season query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.yahoo.yahoo_shangrila import yahoo_team_playoff_series
            yahoo_team_playoff_series()

        See Also:
            * `cfbfastR`_ - R sister package for college football
            * `Yahoo Sports`_ - data origin (unofficial, undocumented endpoints)

        .. _cfbfastR: https://cfbfastR.sportsdataverse.org
        .. _Yahoo Sports: https://sports.yahoo.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "teamId": team_id,
        "season": season,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/teamPlayoffSeries",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_team_roster(
    team_id: Optional[str] = None,
    player_image_height: Optional[str] = None,
    player_image_width: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Yahoo shangrila persisted query `teamRoster` -> one row per `teams` entry

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/teamRoster``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/teamRoster

    Args:
        team_id: teamId query parameter.
        player_image_height: playerImageHeight query parameter.
        player_image_width: playerImageWidth query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.yahoo.yahoo_shangrila import yahoo_team_roster
            yahoo_team_roster()

        See Also:
            * `cfbfastR`_ - R sister package for college football
            * `Yahoo Sports`_ - data origin (unofficial, undocumented endpoints)

        .. _cfbfastR: https://cfbfastR.sportsdataverse.org
        .. _Yahoo Sports: https://sports.yahoo.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "teamId": team_id,
        "playerImageHeight": player_image_height,
        "playerImageWidth": player_image_width,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/teamRoster",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_team_schedule_by_season(
    season: Optional[str] = None,
    team_id: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Yahoo shangrila persisted query `teamScheduleBySeason` -> one row per `teams` entry

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/teamScheduleBySeason``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/teamScheduleBySeason

    Args:
        season: season query parameter.
        team_id: teamId query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.yahoo.yahoo_shangrila import yahoo_team_schedule_by_season
            yahoo_team_schedule_by_season()

        See Also:
            * `cfbfastR`_ - R sister package for college football
            * `Yahoo Sports`_ - data origin (unofficial, undocumented endpoints)

        .. _cfbfastR: https://cfbfastR.sportsdataverse.org
        .. _Yahoo Sports: https://sports.yahoo.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "season": season,
        "teamId": team_id,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/teamScheduleBySeason",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_team_search(
    name: Optional[str] = None,
    image_height: Optional[str] = None,
    image_width: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Yahoo shangrila persisted query `teamSearch` -> one row per `teams` entry

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/teamSearch``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/teamSearch

    Args:
        name: name query parameter.
        image_height: imageHeight query parameter.
        image_width: imageWidth query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.yahoo.yahoo_shangrila import yahoo_team_search
            yahoo_team_search()

        See Also:
            * `cfbfastR`_ - R sister package for college football
            * `Yahoo Sports`_ - data origin (unofficial, undocumented endpoints)

        .. _cfbfastR: https://cfbfastR.sportsdataverse.org
        .. _Yahoo Sports: https://sports.yahoo.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "name": name,
        "imageHeight": image_height,
        "imageWidth": image_width,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/teamSearch",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_team_stats_leaders_v2(
    league: Optional[str] = None,
    team_id: Optional[str] = None,
    count: Optional[str] = None,
    season: Optional[str] = None,
    baseball_cut_type: Optional[str] = None,
    qualified: Optional[str] = None,
    include_team_stats: Optional[str] = None,
    include_player_stats: Optional[str] = None,
    is_baseball: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Yahoo shangrila persisted query `teamStatsLeadersV2` -> tables: leagues, teams

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/teamStatsLeadersV2``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/teamStatsLeadersV2

    Args:
        league: league query parameter.
        team_id: teamId query parameter.
        count: count query parameter.
        season: season query parameter.
        baseball_cut_type: baseballCutType query parameter.
        qualified: qualified query parameter.
        include_team_stats: includeTeamStats query parameter.
        include_player_stats: includePlayerStats query parameter.
        is_baseball: isBaseball query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila_tables -> dict of polars DataFrames (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a dict of pandas DataFrames (same keys) instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A dict of polars/pandas DataFrames keyed by the payload's ``data`` collections by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            yahoo_team_stats_leaders_v2()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "league": league,
        "teamId": team_id,
        "count": count,
        "season": season,
        "baseballCutType": baseball_cut_type,
        "qualified": qualified,
        "includeTeamStats": include_team_stats,
        "includePlayerStats": include_player_stats,
        "isBaseball": is_baseball,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/teamStatsLeadersV2",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila_tables(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_team_transactions(
    team_id: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Yahoo shangrila persisted query `teamTransactions` -> one row per `teams` entry

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/teamTransactions``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/teamTransactions

    Args:
        team_id: teamId query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.yahoo.yahoo_shangrila import yahoo_team_transactions
            yahoo_team_transactions()

        See Also:
            * `cfbfastR`_ - R sister package for college football
            * `Yahoo Sports`_ - data origin (unofficial, undocumented endpoints)

        .. _cfbfastR: https://cfbfastR.sportsdataverse.org
        .. _Yahoo Sports: https://sports.yahoo.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "teamId": team_id,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/teamTransactions",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_teams_basic(
    team_ids: Optional[str] = None,
    image_height: Optional[str] = None,
    image_width: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Yahoo shangrila persisted query `teamsBasic` -> one row per `teams` entry

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/teamsBasic``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/teamsBasic

    Args:
        team_ids: teamIds query parameter.
        image_height: imageHeight query parameter.
        image_width: imageWidth query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.yahoo.yahoo_shangrila import yahoo_teams_basic
            yahoo_teams_basic()

        See Also:
            * `cfbfastR`_ - R sister package for college football
            * `Yahoo Sports`_ - data origin (unofficial, undocumented endpoints)

        .. _cfbfastR: https://cfbfastR.sportsdataverse.org
        .. _Yahoo Sports: https://sports.yahoo.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "teamIds": team_ids,
        "imageHeight": image_height,
        "imageWidth": image_width,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/teamsBasic",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_tennis_matches_by_date(
    tournament_id: Optional[str] = None,
    season: Optional[str] = None,
    date: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Yahoo shangrila persisted query `tennisMatchesByDate` -> one row per `tennisTournaments` entry

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/tennisMatchesByDate``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/tennisMatchesByDate

    Args:
        tournament_id: tournamentId query parameter.
        season: season query parameter.
        date: date query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.yahoo.yahoo_shangrila import yahoo_tennis_matches_by_date
            yahoo_tennis_matches_by_date()

        See Also:
            * `cfbfastR`_ - R sister package for college football
            * `Yahoo Sports`_ - data origin (unofficial, undocumented endpoints)

        .. _cfbfastR: https://cfbfastR.sportsdataverse.org
        .. _Yahoo Sports: https://sports.yahoo.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "tournamentId": tournament_id,
        "season": season,
        "date": date,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/tennisMatchesByDate",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_tennis_tournament(
    tournament_id: Optional[str] = None,
    season: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Yahoo shangrila persisted query `tennisTournament` -> one row per `tennisTournaments` entry

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/tennisTournament``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/tennisTournament

    Args:
        tournament_id: tournamentId query parameter.
        season: season query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.yahoo.yahoo_shangrila import yahoo_tennis_tournament
            yahoo_tennis_tournament()

        See Also:
            * `cfbfastR`_ - R sister package for college football
            * `Yahoo Sports`_ - data origin (unofficial, undocumented endpoints)

        .. _cfbfastR: https://cfbfastR.sportsdataverse.org
        .. _Yahoo Sports: https://sports.yahoo.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "tournamentId": tournament_id,
        "season": season,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/tennisTournament",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_tennis_tournaments(
    league_id: Optional[str] = None,
    match_type: Optional[str] = None,
    season: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Yahoo shangrila persisted query `tennisTournaments` -> one row per `tennisTournaments` entry

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/tennisTournaments``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/tennisTournaments

    Args:
        league_id: leagueId query parameter.
        match_type: matchType query parameter.
        season: season query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.yahoo.yahoo_shangrila import yahoo_tennis_tournaments
            yahoo_tennis_tournaments()

        See Also:
            * `cfbfastR`_ - R sister package for college football
            * `Yahoo Sports`_ - data origin (unofficial, undocumented endpoints)

        .. _cfbfastR: https://cfbfastR.sportsdataverse.org
        .. _Yahoo Sports: https://sports.yahoo.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "leagueId": league_id,
        "matchType": match_type,
        "season": season,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/tennisTournaments",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_tennis_tournaments_by_date(
    season: Optional[str] = None,
    date: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Yahoo shangrila persisted query `tennisTournamentsByDate` -> one row per `tennisTournaments` entry

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/tennisTournamentsByDate``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/tennisTournamentsByDate

    Args:
        season: season query parameter.
        date: date query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.yahoo.yahoo_shangrila import yahoo_tennis_tournaments_by_date
            yahoo_tennis_tournaments_by_date()

        See Also:
            * `cfbfastR`_ - R sister package for college football
            * `Yahoo Sports`_ - data origin (unofficial, undocumented endpoints)

        .. _cfbfastR: https://cfbfastR.sportsdataverse.org
        .. _Yahoo Sports: https://sports.yahoo.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "season": season,
        "date": date,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/tennisTournamentsByDate",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_trending_event_ids(
    count: Optional[str] = None,
    league: Optional[str] = None,
    date_flip_offset: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Yahoo shangrila persisted query `trendingEventIds` -> one row per `trendingEvents` entry

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/trendingEventIds``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/trendingEventIds

    Args:
        count: count query parameter.
        league: league query parameter.
        date_flip_offset: dateFlipOffset query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.yahoo.yahoo_shangrila import yahoo_trending_event_ids
            yahoo_trending_event_ids()

        See Also:
            * `cfbfastR`_ - R sister package for college football
            * `Yahoo Sports`_ - data origin (unofficial, undocumented endpoints)

        .. _cfbfastR: https://cfbfastR.sportsdataverse.org
        .. _Yahoo Sports: https://sports.yahoo.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "count": count,
        "league": league,
        "dateFlipOffset": date_flip_offset,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/trendingEventIds",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_trending_game_ids(
    count: Optional[str] = None,
    league: Optional[str] = None,
    date_flip_offset: Optional[str] = None,
    dates: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Yahoo shangrila persisted query `trendingGameIds` -> one row per `trendingGames` entry

    Endpoint: ``GET https://graphite-secure.sports.yahoo.com/v1/query/shangrila/trendingGameIds``
    Example URL: https://graphite-secure.sports.yahoo.com/v1/query/shangrila/trendingGameIds

    Args:
        count: count query parameter.
        league: league query parameter.
        date_flip_offset: dateFlipOffset query parameter.
        dates: dates query parameter.
        return_parsed: parse the payload through parse_yahoo_shangrila -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.yahoo.yahoo_shangrila import yahoo_trending_game_ids
            yahoo_trending_game_ids()

        See Also:
            * `cfbfastR`_ - R sister package for college football
            * `Yahoo Sports`_ - data origin (unofficial, undocumented endpoints)

        .. _cfbfastR: https://cfbfastR.sportsdataverse.org
        .. _Yahoo Sports: https://sports.yahoo.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "count": count,
        "league": league,
        "dateFlipOffset": date_flip_offset,
        "dates": dates,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://graphite-secure.sports.yahoo.com/v1/query/shangrila/trendingGameIds",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_shangrila(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_editorial_boxscore(
    game_id: str,
    v: Optional[int] = None,
    polling: Optional[int] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Full game box score + play-by-play (normalized stat dictionaries)

    Endpoint: ``GET https://api-secure.sports.yahoo.com/v1/editorial/s/boxscore/{game_id}``
    Example URL: https://api-secure.sports.yahoo.com/v1/editorial/s/boxscore/ncaaf.g.202509200023

    Args:
        game_id: game_id path parameter.
        v: v query parameter.
        polling: polling query parameter.
        return_parsed: parse the payload through parse_yahoo_editorial -> dict of polars DataFrames (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a dict of pandas DataFrames (same keys) instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A dict of polars/pandas DataFrames keyed by the feed's id-keyed collections by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            yahoo_editorial_boxscore(game_id='ncaaf.g.202509200023')
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "v": v,
        "polling": polling,
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://api-secure.sports.yahoo.com/v1/editorial/s/boxscore/{game_id}",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_editorial(raw, return_as_pandas=return_as_pandas)
    return raw


def yahoo_editorial_scoreboard(
    leagues: Optional[str] = None,
    week: Optional[int] = None,
    season: Optional[int] = None,
    conferences: Optional[str] = None,
    count: Optional[int] = None,
    v: Optional[int] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Scoreboard: games + teams + leagues + odds (fat payload)

    Endpoint: ``GET https://api-secure.sports.yahoo.com/v1/editorial/s/scoreboard``
    Example URL: https://api-secure.sports.yahoo.com/v1/editorial/s/scoreboard

    Args:
        leagues: leagues query parameter.
        week: week query parameter.
        season: season query parameter.
        conferences: conferences query parameter.
        count: count query parameter.
        v: v query parameter.
        return_parsed: parse the payload through parse_yahoo_editorial -> dict of polars DataFrames (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a dict of pandas DataFrames (same keys) instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A dict of polars/pandas DataFrames keyed by the feed's id-keyed collections by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            yahoo_editorial_scoreboard()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "leagues": leagues,
        "week": week,
        "season": season,
        "conferences": conferences,
        "count": count,
        "v": v,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api-secure.sports.yahoo.com/v1/editorial/s/scoreboard",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_yahoo_editorial(raw, return_as_pandas=return_as_pandas)
    return raw
