# GENERATED by tools/codegen/generate.py -- DO NOT EDIT.
# Source: tools/codegen/endpoints/pff_api.yaml
"""pff_api wrappers (host https://api.pff.com). Generated."""

from __future__ import annotations

from typing import TYPE_CHECKING, Dict, List, Optional, Union  # noqa: F401

from sportsdataverse.nfl.pff_api_runtime import _get
from sportsdataverse.nfl.pff_parsers import parse_pff_player_detail, parse_pff_report, parse_pff_v2_table

if TYPE_CHECKING:  # pragma: no cover -- annotation-only imports (PEP 563 defers eval)
    import pandas as pd
    import polars as pl

__all__ = [
    "pff_api_ref_leagues",
    "pff_api_ref_games",
    "pff_api_ref_players",
    "pff_api_team_list",
    "pff_api_team_overview",
    "pff_api_team_summary",
    "pff_api_player_seasons",
    "pff_api_player_snaps_summary",
    "pff_api_player_position_pivot",
    "pff_api_player_offense_summary",
    "pff_api_player_offense_blocking",
    "pff_api_player_offense_pass_blocking",
    "pff_api_player_offense_run_blocking",
    "pff_api_player_passing_summary",
    "pff_api_player_passing_concept",
    "pff_api_player_passing_depth",
    "pff_api_player_passing_pressure",
    "pff_api_player_rushing_direction",
    "pff_api_player_rushing_summary",
    "pff_api_player_receiving_depth",
    "pff_api_player_receiving_summary",
    "pff_api_player_defense_summary",
    "pff_api_player_field_goal_summary",
    "pff_api_player_kickoff_summary",
    "pff_api_player_punting_summary",
    "pff_api_player_return_summary",
    "pff_api_player_special_summary",
    "pff_api_facet_offense_summary",
    "pff_api_facet_offense_blocking",
    "pff_api_facet_offense_pass_blocking",
    "pff_api_facet_offense_run_blocking",
    "pff_api_facet_passing_allowed_pressure",
    "pff_api_facet_passing_concept",
    "pff_api_facet_passing_depth",
    "pff_api_facet_passing_detail",
    "pff_api_facet_passing_pressure",
    "pff_api_facet_passing_summary",
    "pff_api_facet_receiving_concept",
    "pff_api_facet_receiving_coverage",
    "pff_api_facet_receiving_depth",
    "pff_api_facet_receiving_scheme",
    "pff_api_facet_receiving_summary",
    "pff_api_facet_rushing_direction",
    "pff_api_facet_rushing_summary",
    "pff_api_facet_defense_coverage",
    "pff_api_facet_defense_coverage_scheme",
    "pff_api_facet_defense_coverage_matchup",
    "pff_api_facet_defense_pass_rush",
    "pff_api_facet_defense_run",
    "pff_api_facet_defense_summary",
    "pff_api_facet_field_goal_summary",
    "pff_api_facet_kickoff_summary",
    "pff_api_facet_punting_summary",
    "pff_api_facet_return_summary",
    "pff_api_facet_special_summary",
    "pff_api_signature_passing_time_in_pocket",
    "pff_api_signature_pass_blocking_efficiency_line",
    "pff_api_signature_defense_outside_pass_rush",
    "pff_api_signature_defense_slot_coverage",
    "pff_api_whoami",
    "pff_api_position_report",
    "pff_api_team_directory",
    "pff_api_team_stats",
    "pff_api_team_roster",
    "pff_api_team_schedule",
    "pff_api_team_leaders",
    "pff_api_team_rushing_direction",
    "pff_api_team_report",
]


def pff_api_ref_leagues(
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """List the leagues you can read, with their seasons and weeks

    Endpoint: ``GET https://api.pff.com/v1/leagues``
    Example URL: https://api.pff.com/v1/leagues

    Args:
        headers: optional headers dict reused across calls; an ``Authorization: Bearer <key>`` here wins over ``api_key=`` (a keyword accepted by every wrapper) and the ``PFF_API_KEY`` / ``SDV_PY_PFF_API_KEY`` environment. With no key anywhere the call raises RuntimeError -- there is no anonymous access.
        return_parsed: parse the payload through parse_pff_report -> polars DataFrame (default True). Pass return_parsed=False for The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``..
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``. when ``return_parsed=False``.

    Raises:
        RuntimeError: No PFF API key (pass ``api_key=`` or set ``PFF_API_KEY``).
        NoDataError: PFF answered 404 (no such route, report or team).
        AssetFetchError: The request was refused or failed (401/403/429/5xx) -- the answer is unknown, never an empty frame.
        ValueError: PFF rejected the parameters (400 ``invalid_parameter`` / 422).

    Example:
        Quick start::

            from sportsdataverse.nfl import pff_api_ref_leagues
            pff_api_ref_leagues()

        See Also:
            * `PFF Developer API`_ - reference + CLI guide
            * `nflverse`_ - NFL data ecosystem

        .. _PFF Developer API: https://developer.pff.com
        .. _nflverse: https://nflverse.nflverse.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {}
    _params.update(_caller_params)
    raw = _get(
        "https://api.pff.com/v1/leagues",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_pff_report(raw, return_as_pandas=return_as_pandas)
    return raw


def pff_api_ref_games(
    league: str,
    season: int,
    week: int,
    franchise_id: Optional[int] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """List game results for a league, season and week

    Endpoint: ``GET https://api.pff.com/v1/games``
    Example URL: https://api.pff.com/v1/games?league=nfl&season=2022&week=1

    Args:
        league: League slug, and the FIRST positional argument of every command that takes one — every command except the facet leaderboards, where --league stays a flag because --game alone is a complete request.
        season: Season, as a four-digit year, taken as a positional argument.
        week: Week, and the THIRD positional argument of games — the one command that takes a single week number rather than a comma-separated list.
        franchise_id: Franchise (team) id.
        headers: optional headers dict reused across calls; an ``Authorization: Bearer <key>`` here wins over ``api_key=`` (a keyword accepted by every wrapper) and the ``PFF_API_KEY`` / ``SDV_PY_PFF_API_KEY`` environment. With no key anywhere the call raises RuntimeError -- there is no anonymous access.
        return_parsed: parse the payload through parse_pff_report -> polars DataFrame (default True). Pass return_parsed=False for The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``..
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``. when ``return_parsed=False``.

    Raises:
        RuntimeError: No PFF API key (pass ``api_key=`` or set ``PFF_API_KEY``).
        NoDataError: PFF answered 404 (no such route, report or team).
        AssetFetchError: The request was refused or failed (401/403/429/5xx) -- the answer is unknown, never an empty frame.
        ValueError: PFF rejected the parameters (400 ``invalid_parameter`` / 422).

    Example:
        Quick start::

            from sportsdataverse.nfl import pff_api_ref_games
            pff_api_ref_games(league='nfl', season=2022, week=1)

        See Also:
            * `PFF Developer API`_ - reference + CLI guide
            * `nflverse`_ - NFL data ecosystem

        .. _PFF Developer API: https://developer.pff.com
        .. _nflverse: https://nflverse.nflverse.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "league": league,
        "season": season,
        "week": week,
        "franchise_id": franchise_id,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.pff.com/v1/games",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_pff_report(raw, return_as_pandas=return_as_pandas)
    return raw


def pff_api_ref_players(
    league: str,
    id: Optional[int] = None,
    name: Optional[str] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Search the player directory by name or id

    Endpoint: ``GET https://api.pff.com/v1/players``
    Example URL: https://api.pff.com/v1/players?league=nfl&name=burrow

    Args:
        league: League slug, and the FIRST positional argument of every command that takes one — every command except the facet leaderboards, where --league stays a flag because --game alone is a complete request.
        id: Exact player-id lookup, ref-players only.
        name: Free-text player-name search, ref-players only.
        headers: optional headers dict reused across calls; an ``Authorization: Bearer <key>`` here wins over ``api_key=`` (a keyword accepted by every wrapper) and the ``PFF_API_KEY`` / ``SDV_PY_PFF_API_KEY`` environment. With no key anywhere the call raises RuntimeError -- there is no anonymous access.
        return_parsed: parse the payload through parse_pff_report -> polars DataFrame (default True). Pass return_parsed=False for The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``..
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``. when ``return_parsed=False``.

    Raises:
        RuntimeError: No PFF API key (pass ``api_key=`` or set ``PFF_API_KEY``).
        NoDataError: PFF answered 404 (no such route, report or team).
        AssetFetchError: The request was refused or failed (401/403/429/5xx) -- the answer is unknown, never an empty frame.
        ValueError: PFF rejected the parameters (400 ``invalid_parameter`` / 422).

    Example:
        Quick start::

            from sportsdataverse.nfl import pff_api_ref_players
            pff_api_ref_players(league='nfl', name='burrow')

        See Also:
            * `PFF Developer API`_ - reference + CLI guide
            * `nflverse`_ - NFL data ecosystem

        .. _PFF Developer API: https://developer.pff.com
        .. _nflverse: https://nflverse.nflverse.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "league": league,
        "id": id,
        "name": name,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.pff.com/v1/players",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_pff_report(raw, return_as_pandas=return_as_pandas)
    return raw


def pff_api_team_list(
    league: str,
    season: Optional[int] = None,
    week: Optional[str] = None,
    franchise_id: Optional[int] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """List a season's teams, franchise groups and schedule

    Endpoint: ``GET https://api.pff.com/v1/teams``
    Example URL: https://api.pff.com/v1/teams?league=nfl&season=2022

    Args:
        league: League slug, and the FIRST positional argument of every command that takes one — every command except the facet leaderboards, where --league stays a flag because --game alone is a complete request.
        season: Season, as a four-digit year.
        week: Week filter.
        franchise_id: Franchise (team) id.
        headers: optional headers dict reused across calls; an ``Authorization: Bearer <key>`` here wins over ``api_key=`` (a keyword accepted by every wrapper) and the ``PFF_API_KEY`` / ``SDV_PY_PFF_API_KEY`` environment. With no key anywhere the call raises RuntimeError -- there is no anonymous access.
        return_parsed: parse the payload through parse_pff_report -> polars DataFrame (default True). Pass return_parsed=False for The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``..
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``. when ``return_parsed=False``.

    Raises:
        RuntimeError: No PFF API key (pass ``api_key=`` or set ``PFF_API_KEY``).
        NoDataError: PFF answered 404 (no such route, report or team).
        AssetFetchError: The request was refused or failed (401/403/429/5xx) -- the answer is unknown, never an empty frame.
        ValueError: PFF rejected the parameters (400 ``invalid_parameter`` / 422).

    Example:
        Quick start::

            from sportsdataverse.nfl import pff_api_team_list
            pff_api_team_list(league='nfl', season=2022)

        See Also:
            * `PFF Developer API`_ - reference + CLI guide
            * `nflverse`_ - NFL data ecosystem

        .. _PFF Developer API: https://developer.pff.com
        .. _nflverse: https://nflverse.nflverse.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "league": league,
        "season": season,
        "week": week,
        "franchise_id": franchise_id,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.pff.com/v1/teams",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_pff_report(raw, return_as_pandas=return_as_pandas)
    return raw


def pff_api_team_overview(
    league: str,
    season: int,
    week: Optional[str] = None,
    franchise_id: Optional[int] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Season-to-date team report, one row per team

    Endpoint: ``GET https://api.pff.com/v1/teams/overview``
    Example URL: https://api.pff.com/v1/teams/overview?league=nfl&season=2022

    Args:
        league: League slug, and the FIRST positional argument of every command that takes one — every command except the facet leaderboards, where --league stays a flag because --game alone is a complete request.
        season: Season, as a four-digit year, taken as a positional argument.
        week: Week filter.
        franchise_id: Franchise (team) id.
        headers: optional headers dict reused across calls; an ``Authorization: Bearer <key>`` here wins over ``api_key=`` (a keyword accepted by every wrapper) and the ``PFF_API_KEY`` / ``SDV_PY_PFF_API_KEY`` environment. With no key anywhere the call raises RuntimeError -- there is no anonymous access.
        return_parsed: parse the payload through parse_pff_report -> polars DataFrame (default True). Pass return_parsed=False for The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``..
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``. when ``return_parsed=False``.

    Raises:
        RuntimeError: No PFF API key (pass ``api_key=`` or set ``PFF_API_KEY``).
        NoDataError: PFF answered 404 (no such route, report or team).
        AssetFetchError: The request was refused or failed (401/403/429/5xx) -- the answer is unknown, never an empty frame.
        ValueError: PFF rejected the parameters (400 ``invalid_parameter`` / 422).

    Example:
        Quick start::

            from sportsdataverse.nfl import pff_api_team_overview
            pff_api_team_overview(league='nfl', season=2022)

        See Also:
            * `PFF Developer API`_ - reference + CLI guide
            * `nflverse`_ - NFL data ecosystem

        .. _PFF Developer API: https://developer.pff.com
        .. _nflverse: https://nflverse.nflverse.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "league": league,
        "season": season,
        "week": week,
        "franchise_id": franchise_id,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.pff.com/v1/teams/overview",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_pff_report(raw, return_as_pandas=return_as_pandas)
    return raw


def pff_api_team_summary(
    league: str,
    season: int,
    franchise_id: int,
    week: Optional[str] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Per-game team report for one franchise, one row per game

    Endpoint: ``GET https://api.pff.com/v1/teams/summary``
    Example URL: https://api.pff.com/v1/teams/summary?league=nfl&season=2022&franchise_id=7

    Args:
        league: League slug, and the FIRST positional argument of every command that takes one — every command except the facet leaderboards, where --league stays a flag because --game alone is a complete request.
        season: Season, as a four-digit year, taken as a positional argument.
        week: Week filter.
        franchise_id: Franchise (team) id, and the THIRD positional argument of team-summary — the report is franchise-scoped, so there is no all-teams form of it.
        headers: optional headers dict reused across calls; an ``Authorization: Bearer <key>`` here wins over ``api_key=`` (a keyword accepted by every wrapper) and the ``PFF_API_KEY`` / ``SDV_PY_PFF_API_KEY`` environment. With no key anywhere the call raises RuntimeError -- there is no anonymous access.
        return_parsed: parse the payload through parse_pff_report -> polars DataFrame (default True). Pass return_parsed=False for The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``..
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``. when ``return_parsed=False``.

    Raises:
        RuntimeError: No PFF API key (pass ``api_key=`` or set ``PFF_API_KEY``).
        NoDataError: PFF answered 404 (no such route, report or team).
        AssetFetchError: The request was refused or failed (401/403/429/5xx) -- the answer is unknown, never an empty frame.
        ValueError: PFF rejected the parameters (400 ``invalid_parameter`` / 422).

    Example:
        Quick start::

            from sportsdataverse.nfl import pff_api_team_summary
            pff_api_team_summary(franchise_id=7, league='nfl', season=2022)

        See Also:
            * `PFF Developer API`_ - reference + CLI guide
            * `nflverse`_ - NFL data ecosystem

        .. _PFF Developer API: https://developer.pff.com
        .. _nflverse: https://nflverse.nflverse.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "league": league,
        "season": season,
        "week": week,
        "franchise_id": franchise_id,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.pff.com/v1/teams/summary",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_pff_report(raw, return_as_pandas=return_as_pandas)
    return raw


def pff_api_player_seasons(
    league: str,
    player_id: int,
    season: Optional[int] = None,
    week: Optional[str] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """List the seasons a player has data for

    Endpoint: ``GET https://api.pff.com/v1/player/seasons``
    Example URL: https://api.pff.com/v1/player/seasons?league=nfl&season=2022&player_id=28022

    Args:
        league: League slug, and the FIRST positional argument of every command that takes one — every command except the facet leaderboards, where --league stays a flag because --game alone is a complete request.
        season: Season, as a four-digit year.
        week: Week filter.
        player_id: Player id, taken as a positional argument.
        headers: optional headers dict reused across calls; an ``Authorization: Bearer <key>`` here wins over ``api_key=`` (a keyword accepted by every wrapper) and the ``PFF_API_KEY`` / ``SDV_PY_PFF_API_KEY`` environment. With no key anywhere the call raises RuntimeError -- there is no anonymous access.
        return_parsed: parse the payload through parse_pff_report -> polars DataFrame (default True). Pass return_parsed=False for The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``..
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``. when ``return_parsed=False``.

    Raises:
        RuntimeError: No PFF API key (pass ``api_key=`` or set ``PFF_API_KEY``).
        NoDataError: PFF answered 404 (no such route, report or team).
        AssetFetchError: The request was refused or failed (401/403/429/5xx) -- the answer is unknown, never an empty frame.
        ValueError: PFF rejected the parameters (400 ``invalid_parameter`` / 422).

    Example:
        Quick start::

            from sportsdataverse.nfl import pff_api_player_seasons
            pff_api_player_seasons(league='nfl', player_id=28022, season=2022)

        See Also:
            * `PFF Developer API`_ - reference + CLI guide
            * `nflverse`_ - NFL data ecosystem

        .. _PFF Developer API: https://developer.pff.com
        .. _nflverse: https://nflverse.nflverse.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "league": league,
        "season": season,
        "week": week,
        "player_id": player_id,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.pff.com/v1/player/seasons",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_pff_report(raw, return_as_pandas=return_as_pandas)
    return raw


def pff_api_player_snaps_summary(
    league: str,
    season: int,
    player_id: int,
    week: Optional[str] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Snap counts for a player, broken out by position

    Endpoint: ``GET https://api.pff.com/v1/player/snaps/summary``
    Example URL: https://api.pff.com/v1/player/snaps/summary?league=nfl&season=2022&player_id=28022

    Args:
        league: League slug, and the FIRST positional argument of every command that takes one — every command except the facet leaderboards, where --league stays a flag because --game alone is a complete request.
        season: Season, as a four-digit year, taken as a positional argument.
        week: Week filter.
        player_id: Player id, taken as a positional argument.
        headers: optional headers dict reused across calls; an ``Authorization: Bearer <key>`` here wins over ``api_key=`` (a keyword accepted by every wrapper) and the ``PFF_API_KEY`` / ``SDV_PY_PFF_API_KEY`` environment. With no key anywhere the call raises RuntimeError -- there is no anonymous access.
        return_parsed: parse the payload through parse_pff_report -> polars DataFrame (default True). Pass return_parsed=False for The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``..
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``. when ``return_parsed=False``.

    Raises:
        RuntimeError: No PFF API key (pass ``api_key=`` or set ``PFF_API_KEY``).
        NoDataError: PFF answered 404 (no such route, report or team).
        AssetFetchError: The request was refused or failed (401/403/429/5xx) -- the answer is unknown, never an empty frame.
        ValueError: PFF rejected the parameters (400 ``invalid_parameter`` / 422).

    Example:
        Quick start::

            from sportsdataverse.nfl import pff_api_player_snaps_summary
            pff_api_player_snaps_summary(league='nfl', player_id=28022, season=2022)

        See Also:
            * `PFF Developer API`_ - reference + CLI guide
            * `nflverse`_ - NFL data ecosystem

        .. _PFF Developer API: https://developer.pff.com
        .. _nflverse: https://nflverse.nflverse.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "league": league,
        "season": season,
        "week": week,
        "player_id": player_id,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.pff.com/v1/player/snaps/summary",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_pff_report(raw, return_as_pandas=return_as_pandas)
    return raw


def pff_api_player_position_pivot(
    league: str,
    season: int,
    player_id: int,
    week: Optional[str] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Player snap counts pivoted by position

    Endpoint: ``GET https://api.pff.com/v1/player/position/pivot``
    Example URL: https://api.pff.com/v1/player/position/pivot?league=nfl&season=2022&player_id=28022

    Args:
        league: League slug, and the FIRST positional argument of every command that takes one — every command except the facet leaderboards, where --league stays a flag because --game alone is a complete request.
        season: Season, as a four-digit year, taken as a positional argument.
        week: Week filter.
        player_id: Player id, taken as a positional argument.
        headers: optional headers dict reused across calls; an ``Authorization: Bearer <key>`` here wins over ``api_key=`` (a keyword accepted by every wrapper) and the ``PFF_API_KEY`` / ``SDV_PY_PFF_API_KEY`` environment. With no key anywhere the call raises RuntimeError -- there is no anonymous access.
        return_parsed: parse the payload through parse_pff_report -> polars DataFrame (default True). Pass return_parsed=False for The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``..
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``. when ``return_parsed=False``.

    Raises:
        RuntimeError: No PFF API key (pass ``api_key=`` or set ``PFF_API_KEY``).
        NoDataError: PFF answered 404 (no such route, report or team).
        AssetFetchError: The request was refused or failed (401/403/429/5xx) -- the answer is unknown, never an empty frame.
        ValueError: PFF rejected the parameters (400 ``invalid_parameter`` / 422).

    Example:
        Quick start::

            from sportsdataverse.nfl import pff_api_player_position_pivot
            pff_api_player_position_pivot(league='nfl', player_id=28022, season=2022)

        See Also:
            * `PFF Developer API`_ - reference + CLI guide
            * `nflverse`_ - NFL data ecosystem

        .. _PFF Developer API: https://developer.pff.com
        .. _nflverse: https://nflverse.nflverse.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "league": league,
        "season": season,
        "week": week,
        "player_id": player_id,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.pff.com/v1/player/position/pivot",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_pff_report(raw, return_as_pandas=return_as_pandas)
    return raw


def pff_api_player_offense_summary(
    league: str,
    player_id: int,
    season: Optional[int] = None,
    week: Optional[str] = None,
    career: Optional[str] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Offense summary for one player

    Endpoint: ``GET https://api.pff.com/v1/player/offense/summary``
    Example URL: https://api.pff.com/v1/player/offense/summary?league=nfl&season=2022&player_id=28022

    Args:
        league: League slug, and the FIRST positional argument of every command that takes one — every command except the facet leaderboards, where --league stays a flag because --game alone is a complete request.
        season: Season, as a four-digit year.
        week: Week filter.
        player_id: Player id, taken as a positional argument.
        career: Career-aggregate toggle, on the player report operations only.
        headers: optional headers dict reused across calls; an ``Authorization: Bearer <key>`` here wins over ``api_key=`` (a keyword accepted by every wrapper) and the ``PFF_API_KEY`` / ``SDV_PY_PFF_API_KEY`` environment. With no key anywhere the call raises RuntimeError -- there is no anonymous access.
        return_parsed: parse the payload through parse_pff_player_detail -> polars DataFrame (default True). Pass return_parsed=False for The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``..
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``. when ``return_parsed=False``.

    Raises:
        RuntimeError: No PFF API key (pass ``api_key=`` or set ``PFF_API_KEY``).
        NoDataError: PFF answered 404 (no such route, report or team).
        AssetFetchError: The request was refused or failed (401/403/429/5xx) -- the answer is unknown, never an empty frame.
        ValueError: PFF rejected the parameters (400 ``invalid_parameter`` / 422).

    Example:
        Quick start::

            from sportsdataverse.nfl import pff_api_player_offense_summary
            pff_api_player_offense_summary(league='nfl', player_id=28022, season=2022)

        See Also:
            * `PFF Developer API`_ - reference + CLI guide
            * `nflverse`_ - NFL data ecosystem

        .. _PFF Developer API: https://developer.pff.com
        .. _nflverse: https://nflverse.nflverse.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "league": league,
        "season": season,
        "week": week,
        "player_id": player_id,
        "career": career,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.pff.com/v1/player/offense/summary",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_pff_player_detail(raw, return_as_pandas=return_as_pandas)
    return raw


def pff_api_player_offense_blocking(
    league: str,
    player_id: int,
    season: Optional[int] = None,
    week: Optional[str] = None,
    career: Optional[str] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Blocking report for one player

    Endpoint: ``GET https://api.pff.com/v1/player/offense/blocking``
    Example URL: https://api.pff.com/v1/player/offense/blocking?league=nfl&season=2022&player_id=28022

    Args:
        league: League slug, and the FIRST positional argument of every command that takes one — every command except the facet leaderboards, where --league stays a flag because --game alone is a complete request.
        season: Season, as a four-digit year.
        week: Week filter.
        player_id: Player id, taken as a positional argument.
        career: Career-aggregate toggle, on the player report operations only.
        headers: optional headers dict reused across calls; an ``Authorization: Bearer <key>`` here wins over ``api_key=`` (a keyword accepted by every wrapper) and the ``PFF_API_KEY`` / ``SDV_PY_PFF_API_KEY`` environment. With no key anywhere the call raises RuntimeError -- there is no anonymous access.
        return_parsed: parse the payload through parse_pff_player_detail -> polars DataFrame (default True). Pass return_parsed=False for The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``..
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``. when ``return_parsed=False``.

    Raises:
        RuntimeError: No PFF API key (pass ``api_key=`` or set ``PFF_API_KEY``).
        NoDataError: PFF answered 404 (no such route, report or team).
        AssetFetchError: The request was refused or failed (401/403/429/5xx) -- the answer is unknown, never an empty frame.
        ValueError: PFF rejected the parameters (400 ``invalid_parameter`` / 422).

    Example:
        Quick start::

            from sportsdataverse.nfl import pff_api_player_offense_blocking
            pff_api_player_offense_blocking(league='nfl', player_id=28022, season=2022)

        See Also:
            * `PFF Developer API`_ - reference + CLI guide
            * `nflverse`_ - NFL data ecosystem

        .. _PFF Developer API: https://developer.pff.com
        .. _nflverse: https://nflverse.nflverse.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "league": league,
        "season": season,
        "week": week,
        "player_id": player_id,
        "career": career,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.pff.com/v1/player/offense/blocking",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_pff_player_detail(raw, return_as_pandas=return_as_pandas)
    return raw


def pff_api_player_offense_pass_blocking(
    league: str,
    player_id: int,
    season: Optional[int] = None,
    week: Optional[str] = None,
    career: Optional[str] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Pass-blocking report for one player

    Endpoint: ``GET https://api.pff.com/v1/player/offense/pass_blocking``
    Example URL: https://api.pff.com/v1/player/offense/pass_blocking?league=nfl&season=2022&player_id=28022

    Args:
        league: League slug, and the FIRST positional argument of every command that takes one — every command except the facet leaderboards, where --league stays a flag because --game alone is a complete request.
        season: Season, as a four-digit year.
        week: Week filter.
        player_id: Player id, taken as a positional argument.
        career: Career-aggregate toggle, on the player report operations only.
        headers: optional headers dict reused across calls; an ``Authorization: Bearer <key>`` here wins over ``api_key=`` (a keyword accepted by every wrapper) and the ``PFF_API_KEY`` / ``SDV_PY_PFF_API_KEY`` environment. With no key anywhere the call raises RuntimeError -- there is no anonymous access.
        return_parsed: parse the payload through parse_pff_player_detail -> polars DataFrame (default True). Pass return_parsed=False for The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``..
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``. when ``return_parsed=False``.

    Raises:
        RuntimeError: No PFF API key (pass ``api_key=`` or set ``PFF_API_KEY``).
        NoDataError: PFF answered 404 (no such route, report or team).
        AssetFetchError: The request was refused or failed (401/403/429/5xx) -- the answer is unknown, never an empty frame.
        ValueError: PFF rejected the parameters (400 ``invalid_parameter`` / 422).

    Example:
        Quick start::

            from sportsdataverse.nfl import pff_api_player_offense_pass_blocking
            pff_api_player_offense_pass_blocking(league='nfl', player_id=28022, season=2022)

        See Also:
            * `PFF Developer API`_ - reference + CLI guide
            * `nflverse`_ - NFL data ecosystem

        .. _PFF Developer API: https://developer.pff.com
        .. _nflverse: https://nflverse.nflverse.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "league": league,
        "season": season,
        "week": week,
        "player_id": player_id,
        "career": career,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.pff.com/v1/player/offense/pass_blocking",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_pff_player_detail(raw, return_as_pandas=return_as_pandas)
    return raw


def pff_api_player_offense_run_blocking(
    league: str,
    player_id: int,
    season: Optional[int] = None,
    week: Optional[str] = None,
    career: Optional[str] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Run-blocking report for one player

    Endpoint: ``GET https://api.pff.com/v1/player/offense/run_blocking``
    Example URL: https://api.pff.com/v1/player/offense/run_blocking?league=nfl&season=2022&player_id=28022

    Args:
        league: League slug, and the FIRST positional argument of every command that takes one — every command except the facet leaderboards, where --league stays a flag because --game alone is a complete request.
        season: Season, as a four-digit year.
        week: Week filter.
        player_id: Player id, taken as a positional argument.
        career: Career-aggregate toggle, on the player report operations only.
        headers: optional headers dict reused across calls; an ``Authorization: Bearer <key>`` here wins over ``api_key=`` (a keyword accepted by every wrapper) and the ``PFF_API_KEY`` / ``SDV_PY_PFF_API_KEY`` environment. With no key anywhere the call raises RuntimeError -- there is no anonymous access.
        return_parsed: parse the payload through parse_pff_player_detail -> polars DataFrame (default True). Pass return_parsed=False for The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``..
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``. when ``return_parsed=False``.

    Raises:
        RuntimeError: No PFF API key (pass ``api_key=`` or set ``PFF_API_KEY``).
        NoDataError: PFF answered 404 (no such route, report or team).
        AssetFetchError: The request was refused or failed (401/403/429/5xx) -- the answer is unknown, never an empty frame.
        ValueError: PFF rejected the parameters (400 ``invalid_parameter`` / 422).

    Example:
        Quick start::

            from sportsdataverse.nfl import pff_api_player_offense_run_blocking
            pff_api_player_offense_run_blocking(league='nfl', player_id=28022, season=2022)

        See Also:
            * `PFF Developer API`_ - reference + CLI guide
            * `nflverse`_ - NFL data ecosystem

        .. _PFF Developer API: https://developer.pff.com
        .. _nflverse: https://nflverse.nflverse.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "league": league,
        "season": season,
        "week": week,
        "player_id": player_id,
        "career": career,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.pff.com/v1/player/offense/run_blocking",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_pff_player_detail(raw, return_as_pandas=return_as_pandas)
    return raw


def pff_api_player_passing_summary(
    league: str,
    player_id: int,
    season: Optional[int] = None,
    week: Optional[str] = None,
    career: Optional[str] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Passing summary for one player

    Endpoint: ``GET https://api.pff.com/v1/player/passing/summary``
    Example URL: https://api.pff.com/v1/player/passing/summary?league=nfl&season=2022&player_id=28022

    Args:
        league: League slug, and the FIRST positional argument of every command that takes one — every command except the facet leaderboards, where --league stays a flag because --game alone is a complete request.
        season: Season, as a four-digit year.
        week: Week filter.
        player_id: Player id, taken as a positional argument.
        career: Career-aggregate toggle, on the player report operations only.
        headers: optional headers dict reused across calls; an ``Authorization: Bearer <key>`` here wins over ``api_key=`` (a keyword accepted by every wrapper) and the ``PFF_API_KEY`` / ``SDV_PY_PFF_API_KEY`` environment. With no key anywhere the call raises RuntimeError -- there is no anonymous access.
        return_parsed: parse the payload through parse_pff_player_detail -> polars DataFrame (default True). Pass return_parsed=False for The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``..
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``. when ``return_parsed=False``.

    Raises:
        RuntimeError: No PFF API key (pass ``api_key=`` or set ``PFF_API_KEY``).
        NoDataError: PFF answered 404 (no such route, report or team).
        AssetFetchError: The request was refused or failed (401/403/429/5xx) -- the answer is unknown, never an empty frame.
        ValueError: PFF rejected the parameters (400 ``invalid_parameter`` / 422).

    Example:
        Quick start::

            from sportsdataverse.nfl import pff_api_player_passing_summary
            pff_api_player_passing_summary(league='nfl', player_id=28022, season=2022)

        See Also:
            * `PFF Developer API`_ - reference + CLI guide
            * `nflverse`_ - NFL data ecosystem

        .. _PFF Developer API: https://developer.pff.com
        .. _nflverse: https://nflverse.nflverse.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "league": league,
        "season": season,
        "week": week,
        "player_id": player_id,
        "career": career,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.pff.com/v1/player/passing/summary",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_pff_player_detail(raw, return_as_pandas=return_as_pandas)
    return raw


def pff_api_player_passing_concept(
    league: str,
    season: int,
    player_id: int,
    week: Optional[str] = None,
    career: Optional[str] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Passing by play concept for one player

    Endpoint: ``GET https://api.pff.com/v1/player/passing/concept``
    Example URL: https://api.pff.com/v1/player/passing/concept?league=nfl&season=2022&player_id=28022

    Args:
        league: League slug, and the FIRST positional argument of every command that takes one — every command except the facet leaderboards, where --league stays a flag because --game alone is a complete request.
        season: Season, as a four-digit year, taken as a positional argument.
        week: Week filter.
        player_id: Player id, taken as a positional argument.
        career: Career-aggregate toggle, on the player report operations only.
        headers: optional headers dict reused across calls; an ``Authorization: Bearer <key>`` here wins over ``api_key=`` (a keyword accepted by every wrapper) and the ``PFF_API_KEY`` / ``SDV_PY_PFF_API_KEY`` environment. With no key anywhere the call raises RuntimeError -- there is no anonymous access.
        return_parsed: parse the payload through parse_pff_report -> polars DataFrame (default True). Pass return_parsed=False for The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``..
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``. when ``return_parsed=False``.

    Raises:
        RuntimeError: No PFF API key (pass ``api_key=`` or set ``PFF_API_KEY``).
        NoDataError: PFF answered 404 (no such route, report or team).
        AssetFetchError: The request was refused or failed (401/403/429/5xx) -- the answer is unknown, never an empty frame.
        ValueError: PFF rejected the parameters (400 ``invalid_parameter`` / 422).

    Example:
        Quick start::

            from sportsdataverse.nfl import pff_api_player_passing_concept
            pff_api_player_passing_concept(league='nfl', player_id=28022, season=2022)

        See Also:
            * `PFF Developer API`_ - reference + CLI guide
            * `nflverse`_ - NFL data ecosystem

        .. _PFF Developer API: https://developer.pff.com
        .. _nflverse: https://nflverse.nflverse.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "league": league,
        "season": season,
        "week": week,
        "player_id": player_id,
        "career": career,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.pff.com/v1/player/passing/concept",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_pff_report(raw, return_as_pandas=return_as_pandas)
    return raw


def pff_api_player_passing_depth(
    league: str,
    season: int,
    player_id: int,
    week: Optional[str] = None,
    career: Optional[str] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Passing by target depth for one player

    Endpoint: ``GET https://api.pff.com/v1/player/passing/depth``
    Example URL: https://api.pff.com/v1/player/passing/depth?league=nfl&season=2022&player_id=28022

    Args:
        league: League slug, and the FIRST positional argument of every command that takes one — every command except the facet leaderboards, where --league stays a flag because --game alone is a complete request.
        season: Season, as a four-digit year, taken as a positional argument.
        week: Week filter.
        player_id: Player id, taken as a positional argument.
        career: Career-aggregate toggle, on the player report operations only.
        headers: optional headers dict reused across calls; an ``Authorization: Bearer <key>`` here wins over ``api_key=`` (a keyword accepted by every wrapper) and the ``PFF_API_KEY`` / ``SDV_PY_PFF_API_KEY`` environment. With no key anywhere the call raises RuntimeError -- there is no anonymous access.
        return_parsed: parse the payload through parse_pff_report -> polars DataFrame (default True). Pass return_parsed=False for The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``..
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``. when ``return_parsed=False``.

    Raises:
        RuntimeError: No PFF API key (pass ``api_key=`` or set ``PFF_API_KEY``).
        NoDataError: PFF answered 404 (no such route, report or team).
        AssetFetchError: The request was refused or failed (401/403/429/5xx) -- the answer is unknown, never an empty frame.
        ValueError: PFF rejected the parameters (400 ``invalid_parameter`` / 422).

    Example:
        Quick start::

            from sportsdataverse.nfl import pff_api_player_passing_depth
            pff_api_player_passing_depth(league='nfl', player_id=28022, season=2022)

        See Also:
            * `PFF Developer API`_ - reference + CLI guide
            * `nflverse`_ - NFL data ecosystem

        .. _PFF Developer API: https://developer.pff.com
        .. _nflverse: https://nflverse.nflverse.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "league": league,
        "season": season,
        "week": week,
        "player_id": player_id,
        "career": career,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.pff.com/v1/player/passing/depth",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_pff_report(raw, return_as_pandas=return_as_pandas)
    return raw


def pff_api_player_passing_pressure(
    league: str,
    season: int,
    player_id: int,
    week: Optional[str] = None,
    career: Optional[str] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Passing under pressure for one player

    Endpoint: ``GET https://api.pff.com/v1/player/passing/pressure``
    Example URL: https://api.pff.com/v1/player/passing/pressure?league=nfl&season=2022&player_id=28022

    Args:
        league: League slug, and the FIRST positional argument of every command that takes one — every command except the facet leaderboards, where --league stays a flag because --game alone is a complete request.
        season: Season, as a four-digit year, taken as a positional argument.
        week: Week filter.
        player_id: Player id, taken as a positional argument.
        career: Career-aggregate toggle, on the player report operations only.
        headers: optional headers dict reused across calls; an ``Authorization: Bearer <key>`` here wins over ``api_key=`` (a keyword accepted by every wrapper) and the ``PFF_API_KEY`` / ``SDV_PY_PFF_API_KEY`` environment. With no key anywhere the call raises RuntimeError -- there is no anonymous access.
        return_parsed: parse the payload through parse_pff_report -> polars DataFrame (default True). Pass return_parsed=False for The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``..
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``. when ``return_parsed=False``.

    Raises:
        RuntimeError: No PFF API key (pass ``api_key=`` or set ``PFF_API_KEY``).
        NoDataError: PFF answered 404 (no such route, report or team).
        AssetFetchError: The request was refused or failed (401/403/429/5xx) -- the answer is unknown, never an empty frame.
        ValueError: PFF rejected the parameters (400 ``invalid_parameter`` / 422).

    Example:
        Quick start::

            from sportsdataverse.nfl import pff_api_player_passing_pressure
            pff_api_player_passing_pressure(league='nfl', player_id=28022, season=2022)

        See Also:
            * `PFF Developer API`_ - reference + CLI guide
            * `nflverse`_ - NFL data ecosystem

        .. _PFF Developer API: https://developer.pff.com
        .. _nflverse: https://nflverse.nflverse.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "league": league,
        "season": season,
        "week": week,
        "player_id": player_id,
        "career": career,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.pff.com/v1/player/passing/pressure",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_pff_report(raw, return_as_pandas=return_as_pandas)
    return raw


def pff_api_player_rushing_direction(
    league: str,
    season: int,
    player_id: int,
    week: Optional[str] = None,
    career: Optional[str] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Rushing by direction for one player

    Endpoint: ``GET https://api.pff.com/v1/player/rushing/direction``
    Example URL: https://api.pff.com/v1/player/rushing/direction?league=nfl&season=2022&player_id=28022

    Args:
        league: League slug, and the FIRST positional argument of every command that takes one — every command except the facet leaderboards, where --league stays a flag because --game alone is a complete request.
        season: Season, as a four-digit year, taken as a positional argument.
        week: Week filter.
        player_id: Player id, taken as a positional argument.
        career: Career-aggregate toggle, on the player report operations only.
        headers: optional headers dict reused across calls; an ``Authorization: Bearer <key>`` here wins over ``api_key=`` (a keyword accepted by every wrapper) and the ``PFF_API_KEY`` / ``SDV_PY_PFF_API_KEY`` environment. With no key anywhere the call raises RuntimeError -- there is no anonymous access.
        return_parsed: parse the payload through parse_pff_report -> polars DataFrame (default True). Pass return_parsed=False for The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``..
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``. when ``return_parsed=False``.

    Raises:
        RuntimeError: No PFF API key (pass ``api_key=`` or set ``PFF_API_KEY``).
        NoDataError: PFF answered 404 (no such route, report or team).
        AssetFetchError: The request was refused or failed (401/403/429/5xx) -- the answer is unknown, never an empty frame.
        ValueError: PFF rejected the parameters (400 ``invalid_parameter`` / 422).

    Example:
        Quick start::

            from sportsdataverse.nfl import pff_api_player_rushing_direction
            pff_api_player_rushing_direction(league='nfl', player_id=28022, season=2022)

        See Also:
            * `PFF Developer API`_ - reference + CLI guide
            * `nflverse`_ - NFL data ecosystem

        .. _PFF Developer API: https://developer.pff.com
        .. _nflverse: https://nflverse.nflverse.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "league": league,
        "season": season,
        "week": week,
        "player_id": player_id,
        "career": career,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.pff.com/v1/player/rushing/direction",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_pff_report(raw, return_as_pandas=return_as_pandas)
    return raw


def pff_api_player_rushing_summary(
    league: str,
    player_id: int,
    season: Optional[int] = None,
    week: Optional[str] = None,
    career: Optional[str] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Rushing summary for one player

    Endpoint: ``GET https://api.pff.com/v1/player/rushing/summary``
    Example URL: https://api.pff.com/v1/player/rushing/summary?league=nfl&season=2022&player_id=28022

    Args:
        league: League slug, and the FIRST positional argument of every command that takes one — every command except the facet leaderboards, where --league stays a flag because --game alone is a complete request.
        season: Season, as a four-digit year.
        week: Week filter.
        player_id: Player id, taken as a positional argument.
        career: Career-aggregate toggle, on the player report operations only.
        headers: optional headers dict reused across calls; an ``Authorization: Bearer <key>`` here wins over ``api_key=`` (a keyword accepted by every wrapper) and the ``PFF_API_KEY`` / ``SDV_PY_PFF_API_KEY`` environment. With no key anywhere the call raises RuntimeError -- there is no anonymous access.
        return_parsed: parse the payload through parse_pff_player_detail -> polars DataFrame (default True). Pass return_parsed=False for The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``..
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``. when ``return_parsed=False``.

    Raises:
        RuntimeError: No PFF API key (pass ``api_key=`` or set ``PFF_API_KEY``).
        NoDataError: PFF answered 404 (no such route, report or team).
        AssetFetchError: The request was refused or failed (401/403/429/5xx) -- the answer is unknown, never an empty frame.
        ValueError: PFF rejected the parameters (400 ``invalid_parameter`` / 422).

    Example:
        Quick start::

            from sportsdataverse.nfl import pff_api_player_rushing_summary
            pff_api_player_rushing_summary(league='nfl', player_id=28022, season=2022)

        See Also:
            * `PFF Developer API`_ - reference + CLI guide
            * `nflverse`_ - NFL data ecosystem

        .. _PFF Developer API: https://developer.pff.com
        .. _nflverse: https://nflverse.nflverse.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "league": league,
        "season": season,
        "week": week,
        "player_id": player_id,
        "career": career,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.pff.com/v1/player/rushing/summary",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_pff_player_detail(raw, return_as_pandas=return_as_pandas)
    return raw


def pff_api_player_receiving_depth(
    league: str,
    season: int,
    player_id: int,
    week: Optional[str] = None,
    career: Optional[str] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Receiving by target depth for one player

    Endpoint: ``GET https://api.pff.com/v1/player/receiving/depth``
    Example URL: https://api.pff.com/v1/player/receiving/depth?league=nfl&season=2022&player_id=28022

    Args:
        league: League slug, and the FIRST positional argument of every command that takes one — every command except the facet leaderboards, where --league stays a flag because --game alone is a complete request.
        season: Season, as a four-digit year, taken as a positional argument.
        week: Week filter.
        player_id: Player id, taken as a positional argument.
        career: Career-aggregate toggle, on the player report operations only.
        headers: optional headers dict reused across calls; an ``Authorization: Bearer <key>`` here wins over ``api_key=`` (a keyword accepted by every wrapper) and the ``PFF_API_KEY`` / ``SDV_PY_PFF_API_KEY`` environment. With no key anywhere the call raises RuntimeError -- there is no anonymous access.
        return_parsed: parse the payload through parse_pff_report -> polars DataFrame (default True). Pass return_parsed=False for The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``..
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``. when ``return_parsed=False``.

    Raises:
        RuntimeError: No PFF API key (pass ``api_key=`` or set ``PFF_API_KEY``).
        NoDataError: PFF answered 404 (no such route, report or team).
        AssetFetchError: The request was refused or failed (401/403/429/5xx) -- the answer is unknown, never an empty frame.
        ValueError: PFF rejected the parameters (400 ``invalid_parameter`` / 422).

    Example:
        Quick start::

            from sportsdataverse.nfl import pff_api_player_receiving_depth
            pff_api_player_receiving_depth(league='nfl', player_id=28022, season=2022)

        See Also:
            * `PFF Developer API`_ - reference + CLI guide
            * `nflverse`_ - NFL data ecosystem

        .. _PFF Developer API: https://developer.pff.com
        .. _nflverse: https://nflverse.nflverse.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "league": league,
        "season": season,
        "week": week,
        "player_id": player_id,
        "career": career,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.pff.com/v1/player/receiving/depth",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_pff_report(raw, return_as_pandas=return_as_pandas)
    return raw


def pff_api_player_receiving_summary(
    league: str,
    player_id: int,
    season: Optional[int] = None,
    week: Optional[str] = None,
    career: Optional[str] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Receiving summary for one player

    Endpoint: ``GET https://api.pff.com/v1/player/receiving/summary``
    Example URL: https://api.pff.com/v1/player/receiving/summary?league=nfl&season=2022&player_id=28022

    Args:
        league: League slug, and the FIRST positional argument of every command that takes one — every command except the facet leaderboards, where --league stays a flag because --game alone is a complete request.
        season: Season, as a four-digit year.
        week: Week filter.
        player_id: Player id, taken as a positional argument.
        career: Career-aggregate toggle, on the player report operations only.
        headers: optional headers dict reused across calls; an ``Authorization: Bearer <key>`` here wins over ``api_key=`` (a keyword accepted by every wrapper) and the ``PFF_API_KEY`` / ``SDV_PY_PFF_API_KEY`` environment. With no key anywhere the call raises RuntimeError -- there is no anonymous access.
        return_parsed: parse the payload through parse_pff_player_detail -> polars DataFrame (default True). Pass return_parsed=False for The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``..
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``. when ``return_parsed=False``.

    Raises:
        RuntimeError: No PFF API key (pass ``api_key=`` or set ``PFF_API_KEY``).
        NoDataError: PFF answered 404 (no such route, report or team).
        AssetFetchError: The request was refused or failed (401/403/429/5xx) -- the answer is unknown, never an empty frame.
        ValueError: PFF rejected the parameters (400 ``invalid_parameter`` / 422).

    Example:
        Quick start::

            from sportsdataverse.nfl import pff_api_player_receiving_summary
            pff_api_player_receiving_summary(league='nfl', player_id=28022, season=2022)

        See Also:
            * `PFF Developer API`_ - reference + CLI guide
            * `nflverse`_ - NFL data ecosystem

        .. _PFF Developer API: https://developer.pff.com
        .. _nflverse: https://nflverse.nflverse.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "league": league,
        "season": season,
        "week": week,
        "player_id": player_id,
        "career": career,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.pff.com/v1/player/receiving/summary",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_pff_player_detail(raw, return_as_pandas=return_as_pandas)
    return raw


def pff_api_player_defense_summary(
    league: str,
    player_id: int,
    season: Optional[int] = None,
    week: Optional[str] = None,
    career: Optional[str] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Defense summary for one player

    Endpoint: ``GET https://api.pff.com/v1/player/defense/summary``
    Example URL: https://api.pff.com/v1/player/defense/summary?league=nfl&season=2022&player_id=28022

    Args:
        league: League slug, and the FIRST positional argument of every command that takes one — every command except the facet leaderboards, where --league stays a flag because --game alone is a complete request.
        season: Season, as a four-digit year.
        week: Week filter.
        player_id: Player id, taken as a positional argument.
        career: Career-aggregate toggle, on the player report operations only.
        headers: optional headers dict reused across calls; an ``Authorization: Bearer <key>`` here wins over ``api_key=`` (a keyword accepted by every wrapper) and the ``PFF_API_KEY`` / ``SDV_PY_PFF_API_KEY`` environment. With no key anywhere the call raises RuntimeError -- there is no anonymous access.
        return_parsed: parse the payload through parse_pff_player_detail -> polars DataFrame (default True). Pass return_parsed=False for The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``..
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``. when ``return_parsed=False``.

    Raises:
        RuntimeError: No PFF API key (pass ``api_key=`` or set ``PFF_API_KEY``).
        NoDataError: PFF answered 404 (no such route, report or team).
        AssetFetchError: The request was refused or failed (401/403/429/5xx) -- the answer is unknown, never an empty frame.
        ValueError: PFF rejected the parameters (400 ``invalid_parameter`` / 422).

    Example:
        Quick start::

            from sportsdataverse.nfl import pff_api_player_defense_summary
            pff_api_player_defense_summary(league='nfl', player_id=28022, season=2022)

        See Also:
            * `PFF Developer API`_ - reference + CLI guide
            * `nflverse`_ - NFL data ecosystem

        .. _PFF Developer API: https://developer.pff.com
        .. _nflverse: https://nflverse.nflverse.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "league": league,
        "season": season,
        "week": week,
        "player_id": player_id,
        "career": career,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.pff.com/v1/player/defense/summary",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_pff_player_detail(raw, return_as_pandas=return_as_pandas)
    return raw


def pff_api_player_field_goal_summary(
    league: str,
    player_id: int,
    season: Optional[int] = None,
    week: Optional[str] = None,
    career: Optional[str] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Field-goal kicking for one player

    Endpoint: ``GET https://api.pff.com/v1/player/field_goal/summary``
    Example URL: https://api.pff.com/v1/player/field_goal/summary?league=nfl&season=2022&player_id=28022

    Args:
        league: League slug, and the FIRST positional argument of every command that takes one — every command except the facet leaderboards, where --league stays a flag because --game alone is a complete request.
        season: Season, as a four-digit year.
        week: Week filter.
        player_id: Player id, taken as a positional argument.
        career: Career-aggregate toggle, on the player report operations only.
        headers: optional headers dict reused across calls; an ``Authorization: Bearer <key>`` here wins over ``api_key=`` (a keyword accepted by every wrapper) and the ``PFF_API_KEY`` / ``SDV_PY_PFF_API_KEY`` environment. With no key anywhere the call raises RuntimeError -- there is no anonymous access.
        return_parsed: parse the payload through parse_pff_player_detail -> polars DataFrame (default True). Pass return_parsed=False for The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``..
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``. when ``return_parsed=False``.

    Raises:
        RuntimeError: No PFF API key (pass ``api_key=`` or set ``PFF_API_KEY``).
        NoDataError: PFF answered 404 (no such route, report or team).
        AssetFetchError: The request was refused or failed (401/403/429/5xx) -- the answer is unknown, never an empty frame.
        ValueError: PFF rejected the parameters (400 ``invalid_parameter`` / 422).

    Example:
        Quick start::

            from sportsdataverse.nfl import pff_api_player_field_goal_summary
            pff_api_player_field_goal_summary(league='nfl', player_id=28022, season=2022)

        See Also:
            * `PFF Developer API`_ - reference + CLI guide
            * `nflverse`_ - NFL data ecosystem

        .. _PFF Developer API: https://developer.pff.com
        .. _nflverse: https://nflverse.nflverse.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "league": league,
        "season": season,
        "week": week,
        "player_id": player_id,
        "career": career,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.pff.com/v1/player/field_goal/summary",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_pff_player_detail(raw, return_as_pandas=return_as_pandas)
    return raw


def pff_api_player_kickoff_summary(
    league: str,
    player_id: int,
    season: Optional[int] = None,
    week: Optional[str] = None,
    career: Optional[str] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Kickoffs for one player

    Endpoint: ``GET https://api.pff.com/v1/player/kickoff/summary``
    Example URL: https://api.pff.com/v1/player/kickoff/summary?league=nfl&season=2022&player_id=28022

    Args:
        league: League slug, and the FIRST positional argument of every command that takes one — every command except the facet leaderboards, where --league stays a flag because --game alone is a complete request.
        season: Season, as a four-digit year.
        week: Week filter.
        player_id: Player id, taken as a positional argument.
        career: Career-aggregate toggle, on the player report operations only.
        headers: optional headers dict reused across calls; an ``Authorization: Bearer <key>`` here wins over ``api_key=`` (a keyword accepted by every wrapper) and the ``PFF_API_KEY`` / ``SDV_PY_PFF_API_KEY`` environment. With no key anywhere the call raises RuntimeError -- there is no anonymous access.
        return_parsed: parse the payload through parse_pff_player_detail -> polars DataFrame (default True). Pass return_parsed=False for The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``..
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``. when ``return_parsed=False``.

    Raises:
        RuntimeError: No PFF API key (pass ``api_key=`` or set ``PFF_API_KEY``).
        NoDataError: PFF answered 404 (no such route, report or team).
        AssetFetchError: The request was refused or failed (401/403/429/5xx) -- the answer is unknown, never an empty frame.
        ValueError: PFF rejected the parameters (400 ``invalid_parameter`` / 422).

    Example:
        Quick start::

            from sportsdataverse.nfl import pff_api_player_kickoff_summary
            pff_api_player_kickoff_summary(league='nfl', player_id=28022, season=2022)

        See Also:
            * `PFF Developer API`_ - reference + CLI guide
            * `nflverse`_ - NFL data ecosystem

        .. _PFF Developer API: https://developer.pff.com
        .. _nflverse: https://nflverse.nflverse.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "league": league,
        "season": season,
        "week": week,
        "player_id": player_id,
        "career": career,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.pff.com/v1/player/kickoff/summary",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_pff_player_detail(raw, return_as_pandas=return_as_pandas)
    return raw


def pff_api_player_punting_summary(
    league: str,
    player_id: int,
    season: Optional[int] = None,
    week: Optional[str] = None,
    career: Optional[str] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Punting for one player

    Endpoint: ``GET https://api.pff.com/v1/player/punting/summary``
    Example URL: https://api.pff.com/v1/player/punting/summary?league=nfl&season=2022&player_id=28022

    Args:
        league: League slug, and the FIRST positional argument of every command that takes one — every command except the facet leaderboards, where --league stays a flag because --game alone is a complete request.
        season: Season, as a four-digit year.
        week: Week filter.
        player_id: Player id, taken as a positional argument.
        career: Career-aggregate toggle, on the player report operations only.
        headers: optional headers dict reused across calls; an ``Authorization: Bearer <key>`` here wins over ``api_key=`` (a keyword accepted by every wrapper) and the ``PFF_API_KEY`` / ``SDV_PY_PFF_API_KEY`` environment. With no key anywhere the call raises RuntimeError -- there is no anonymous access.
        return_parsed: parse the payload through parse_pff_player_detail -> polars DataFrame (default True). Pass return_parsed=False for The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``..
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``. when ``return_parsed=False``.

    Raises:
        RuntimeError: No PFF API key (pass ``api_key=`` or set ``PFF_API_KEY``).
        NoDataError: PFF answered 404 (no such route, report or team).
        AssetFetchError: The request was refused or failed (401/403/429/5xx) -- the answer is unknown, never an empty frame.
        ValueError: PFF rejected the parameters (400 ``invalid_parameter`` / 422).

    Example:
        Quick start::

            from sportsdataverse.nfl import pff_api_player_punting_summary
            pff_api_player_punting_summary(league='nfl', player_id=28022, season=2022)

        See Also:
            * `PFF Developer API`_ - reference + CLI guide
            * `nflverse`_ - NFL data ecosystem

        .. _PFF Developer API: https://developer.pff.com
        .. _nflverse: https://nflverse.nflverse.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "league": league,
        "season": season,
        "week": week,
        "player_id": player_id,
        "career": career,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.pff.com/v1/player/punting/summary",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_pff_player_detail(raw, return_as_pandas=return_as_pandas)
    return raw


def pff_api_player_return_summary(
    league: str,
    player_id: int,
    season: Optional[int] = None,
    week: Optional[str] = None,
    career: Optional[str] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Kick and punt returns for one player

    Endpoint: ``GET https://api.pff.com/v1/player/return/summary``
    Example URL: https://api.pff.com/v1/player/return/summary?league=nfl&season=2022&player_id=28022

    Args:
        league: League slug, and the FIRST positional argument of every command that takes one — every command except the facet leaderboards, where --league stays a flag because --game alone is a complete request.
        season: Season, as a four-digit year.
        week: Week filter.
        player_id: Player id, taken as a positional argument.
        career: Career-aggregate toggle, on the player report operations only.
        headers: optional headers dict reused across calls; an ``Authorization: Bearer <key>`` here wins over ``api_key=`` (a keyword accepted by every wrapper) and the ``PFF_API_KEY`` / ``SDV_PY_PFF_API_KEY`` environment. With no key anywhere the call raises RuntimeError -- there is no anonymous access.
        return_parsed: parse the payload through parse_pff_player_detail -> polars DataFrame (default True). Pass return_parsed=False for The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``..
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``. when ``return_parsed=False``.

    Raises:
        RuntimeError: No PFF API key (pass ``api_key=`` or set ``PFF_API_KEY``).
        NoDataError: PFF answered 404 (no such route, report or team).
        AssetFetchError: The request was refused or failed (401/403/429/5xx) -- the answer is unknown, never an empty frame.
        ValueError: PFF rejected the parameters (400 ``invalid_parameter`` / 422).

    Example:
        Quick start::

            from sportsdataverse.nfl import pff_api_player_return_summary
            pff_api_player_return_summary(league='nfl', player_id=28022, season=2022)

        See Also:
            * `PFF Developer API`_ - reference + CLI guide
            * `nflverse`_ - NFL data ecosystem

        .. _PFF Developer API: https://developer.pff.com
        .. _nflverse: https://nflverse.nflverse.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "league": league,
        "season": season,
        "week": week,
        "player_id": player_id,
        "career": career,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.pff.com/v1/player/return/summary",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_pff_player_detail(raw, return_as_pandas=return_as_pandas)
    return raw


def pff_api_player_special_summary(
    league: str,
    player_id: int,
    season: Optional[int] = None,
    week: Optional[str] = None,
    career: Optional[str] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Special-teams summary for one player

    Endpoint: ``GET https://api.pff.com/v1/player/special/summary``
    Example URL: https://api.pff.com/v1/player/special/summary?league=nfl&season=2022&player_id=28022

    Args:
        league: League slug, and the FIRST positional argument of every command that takes one — every command except the facet leaderboards, where --league stays a flag because --game alone is a complete request.
        season: Season, as a four-digit year.
        week: Week filter.
        player_id: Player id, taken as a positional argument.
        career: Career-aggregate toggle, on the player report operations only.
        headers: optional headers dict reused across calls; an ``Authorization: Bearer <key>`` here wins over ``api_key=`` (a keyword accepted by every wrapper) and the ``PFF_API_KEY`` / ``SDV_PY_PFF_API_KEY`` environment. With no key anywhere the call raises RuntimeError -- there is no anonymous access.
        return_parsed: parse the payload through parse_pff_player_detail -> polars DataFrame (default True). Pass return_parsed=False for The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``..
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``. when ``return_parsed=False``.

    Raises:
        RuntimeError: No PFF API key (pass ``api_key=`` or set ``PFF_API_KEY``).
        NoDataError: PFF answered 404 (no such route, report or team).
        AssetFetchError: The request was refused or failed (401/403/429/5xx) -- the answer is unknown, never an empty frame.
        ValueError: PFF rejected the parameters (400 ``invalid_parameter`` / 422).

    Example:
        Quick start::

            from sportsdataverse.nfl import pff_api_player_special_summary
            pff_api_player_special_summary(league='nfl', player_id=28022, season=2022)

        See Also:
            * `PFF Developer API`_ - reference + CLI guide
            * `nflverse`_ - NFL data ecosystem

        .. _PFF Developer API: https://developer.pff.com
        .. _nflverse: https://nflverse.nflverse.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "league": league,
        "season": season,
        "week": week,
        "player_id": player_id,
        "career": career,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.pff.com/v1/player/special/summary",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_pff_player_detail(raw, return_as_pandas=return_as_pandas)
    return raw


def pff_api_facet_offense_summary(
    league: Optional[str] = None,
    season: Optional[str] = None,
    week: Optional[str] = None,
    franchise_id: Optional[int] = None,
    game_id: Optional[int] = None,
    division: Optional[str] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """League-wide offense summary leaderboard

    Endpoint: ``GET https://api.pff.com/v1/facet/offense/summary``
    Example URL: https://api.pff.com/v1/facet/offense/summary?league=nfl&season=2022

    Args:
        league: League slug for the leaderboard commands — exactly nfl, ncaa, hs, aaf and ufl are recognised; anything else is rejected.
        season: Season for the leaderboard commands.
        week: Week filter.
        franchise_id: Franchise (team) id.
        game_id: Single-game filter, facet family only, forwarded uncoerced.
        division: NCAA division fan-out, facet family only, and **only honoured when league=ncaa** — for any other league it is ignored entirely.
        headers: optional headers dict reused across calls; an ``Authorization: Bearer <key>`` here wins over ``api_key=`` (a keyword accepted by every wrapper) and the ``PFF_API_KEY`` / ``SDV_PY_PFF_API_KEY`` environment. With no key anywhere the call raises RuntimeError -- there is no anonymous access.
        return_parsed: parse the payload through parse_pff_report -> polars DataFrame (default True). Pass return_parsed=False for The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``..
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``. when ``return_parsed=False``.

    Raises:
        RuntimeError: No PFF API key (pass ``api_key=`` or set ``PFF_API_KEY``).
        NoDataError: PFF answered 404 (no such route, report or team).
        AssetFetchError: The request was refused or failed (401/403/429/5xx) -- the answer is unknown, never an empty frame.
        ValueError: PFF rejected the parameters (400 ``invalid_parameter`` / 422).

    Example:
        Quick start::

            from sportsdataverse.nfl import pff_api_facet_offense_summary
            pff_api_facet_offense_summary(league='nfl', season='2022')

        See Also:
            * `PFF Developer API`_ - reference + CLI guide
            * `nflverse`_ - NFL data ecosystem

        .. _PFF Developer API: https://developer.pff.com
        .. _nflverse: https://nflverse.nflverse.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "league": league,
        "season": season,
        "week": week,
        "franchise_id": franchise_id,
        "game_id": game_id,
        "division": division,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.pff.com/v1/facet/offense/summary",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_pff_report(raw, return_as_pandas=return_as_pandas)
    return raw


def pff_api_facet_offense_blocking(
    league: Optional[str] = None,
    season: Optional[str] = None,
    week: Optional[str] = None,
    franchise_id: Optional[int] = None,
    game_id: Optional[int] = None,
    division: Optional[str] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """League-wide blocking leaderboard

    Endpoint: ``GET https://api.pff.com/v1/facet/offense/blocking``
    Example URL: https://api.pff.com/v1/facet/offense/blocking?league=nfl&season=2022

    Args:
        league: League slug for the leaderboard commands — exactly nfl, ncaa, hs, aaf and ufl are recognised; anything else is rejected.
        season: Season for the leaderboard commands.
        week: Week filter.
        franchise_id: Franchise (team) id.
        game_id: Single-game filter, facet family only, forwarded uncoerced.
        division: NCAA division fan-out, facet family only, and **only honoured when league=ncaa** — for any other league it is ignored entirely.
        headers: optional headers dict reused across calls; an ``Authorization: Bearer <key>`` here wins over ``api_key=`` (a keyword accepted by every wrapper) and the ``PFF_API_KEY`` / ``SDV_PY_PFF_API_KEY`` environment. With no key anywhere the call raises RuntimeError -- there is no anonymous access.
        return_parsed: parse the payload through parse_pff_report -> polars DataFrame (default True). Pass return_parsed=False for The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``..
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``. when ``return_parsed=False``.

    Raises:
        RuntimeError: No PFF API key (pass ``api_key=`` or set ``PFF_API_KEY``).
        NoDataError: PFF answered 404 (no such route, report or team).
        AssetFetchError: The request was refused or failed (401/403/429/5xx) -- the answer is unknown, never an empty frame.
        ValueError: PFF rejected the parameters (400 ``invalid_parameter`` / 422).

    Example:
        Quick start::

            from sportsdataverse.nfl import pff_api_facet_offense_blocking
            pff_api_facet_offense_blocking(league='nfl', season='2022')

        See Also:
            * `PFF Developer API`_ - reference + CLI guide
            * `nflverse`_ - NFL data ecosystem

        .. _PFF Developer API: https://developer.pff.com
        .. _nflverse: https://nflverse.nflverse.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "league": league,
        "season": season,
        "week": week,
        "franchise_id": franchise_id,
        "game_id": game_id,
        "division": division,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.pff.com/v1/facet/offense/blocking",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_pff_report(raw, return_as_pandas=return_as_pandas)
    return raw


def pff_api_facet_offense_pass_blocking(
    league: Optional[str] = None,
    season: Optional[str] = None,
    week: Optional[str] = None,
    franchise_id: Optional[int] = None,
    game_id: Optional[int] = None,
    division: Optional[str] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """League-wide pass-blocking leaderboard

    Endpoint: ``GET https://api.pff.com/v1/facet/offense/pass_blocking``
    Example URL: https://api.pff.com/v1/facet/offense/pass_blocking?league=nfl&season=2022

    Args:
        league: League slug for the leaderboard commands — exactly nfl, ncaa, hs, aaf and ufl are recognised; anything else is rejected.
        season: Season for the leaderboard commands.
        week: Week filter.
        franchise_id: Franchise (team) id.
        game_id: Single-game filter, facet family only, forwarded uncoerced.
        division: NCAA division fan-out, facet family only, and **only honoured when league=ncaa** — for any other league it is ignored entirely.
        headers: optional headers dict reused across calls; an ``Authorization: Bearer <key>`` here wins over ``api_key=`` (a keyword accepted by every wrapper) and the ``PFF_API_KEY`` / ``SDV_PY_PFF_API_KEY`` environment. With no key anywhere the call raises RuntimeError -- there is no anonymous access.
        return_parsed: parse the payload through parse_pff_report -> polars DataFrame (default True). Pass return_parsed=False for The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``..
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``. when ``return_parsed=False``.

    Raises:
        RuntimeError: No PFF API key (pass ``api_key=`` or set ``PFF_API_KEY``).
        NoDataError: PFF answered 404 (no such route, report or team).
        AssetFetchError: The request was refused or failed (401/403/429/5xx) -- the answer is unknown, never an empty frame.
        ValueError: PFF rejected the parameters (400 ``invalid_parameter`` / 422).

    Example:
        Quick start::

            from sportsdataverse.nfl import pff_api_facet_offense_pass_blocking
            pff_api_facet_offense_pass_blocking(league='nfl', season='2022')

        See Also:
            * `PFF Developer API`_ - reference + CLI guide
            * `nflverse`_ - NFL data ecosystem

        .. _PFF Developer API: https://developer.pff.com
        .. _nflverse: https://nflverse.nflverse.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "league": league,
        "season": season,
        "week": week,
        "franchise_id": franchise_id,
        "game_id": game_id,
        "division": division,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.pff.com/v1/facet/offense/pass_blocking",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_pff_report(raw, return_as_pandas=return_as_pandas)
    return raw


def pff_api_facet_offense_run_blocking(
    league: Optional[str] = None,
    season: Optional[str] = None,
    week: Optional[str] = None,
    franchise_id: Optional[int] = None,
    game_id: Optional[int] = None,
    division: Optional[str] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """League-wide run-blocking leaderboard

    Endpoint: ``GET https://api.pff.com/v1/facet/offense/run_blocking``
    Example URL: https://api.pff.com/v1/facet/offense/run_blocking?league=nfl&season=2022

    Args:
        league: League slug for the leaderboard commands — exactly nfl, ncaa, hs, aaf and ufl are recognised; anything else is rejected.
        season: Season for the leaderboard commands.
        week: Week filter.
        franchise_id: Franchise (team) id.
        game_id: Single-game filter, facet family only, forwarded uncoerced.
        division: NCAA division fan-out, facet family only, and **only honoured when league=ncaa** — for any other league it is ignored entirely.
        headers: optional headers dict reused across calls; an ``Authorization: Bearer <key>`` here wins over ``api_key=`` (a keyword accepted by every wrapper) and the ``PFF_API_KEY`` / ``SDV_PY_PFF_API_KEY`` environment. With no key anywhere the call raises RuntimeError -- there is no anonymous access.
        return_parsed: parse the payload through parse_pff_report -> polars DataFrame (default True). Pass return_parsed=False for The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``..
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``. when ``return_parsed=False``.

    Raises:
        RuntimeError: No PFF API key (pass ``api_key=`` or set ``PFF_API_KEY``).
        NoDataError: PFF answered 404 (no such route, report or team).
        AssetFetchError: The request was refused or failed (401/403/429/5xx) -- the answer is unknown, never an empty frame.
        ValueError: PFF rejected the parameters (400 ``invalid_parameter`` / 422).

    Example:
        Quick start::

            from sportsdataverse.nfl import pff_api_facet_offense_run_blocking
            pff_api_facet_offense_run_blocking(league='nfl', season='2022')

        See Also:
            * `PFF Developer API`_ - reference + CLI guide
            * `nflverse`_ - NFL data ecosystem

        .. _PFF Developer API: https://developer.pff.com
        .. _nflverse: https://nflverse.nflverse.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "league": league,
        "season": season,
        "week": week,
        "franchise_id": franchise_id,
        "game_id": game_id,
        "division": division,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.pff.com/v1/facet/offense/run_blocking",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_pff_report(raw, return_as_pandas=return_as_pandas)
    return raw


def pff_api_facet_passing_allowed_pressure(
    league: Optional[str] = None,
    season: Optional[str] = None,
    week: Optional[str] = None,
    franchise_id: Optional[int] = None,
    game_id: Optional[int] = None,
    division: Optional[str] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """League-wide pressure-allowed leaderboard

    Endpoint: ``GET https://api.pff.com/v1/facet/passing/allowed_pressure``
    Example URL: https://api.pff.com/v1/facet/passing/allowed_pressure?league=nfl&season=2022

    Args:
        league: League slug for the leaderboard commands — exactly nfl, ncaa, hs, aaf and ufl are recognised; anything else is rejected.
        season: Season for the leaderboard commands.
        week: Week filter.
        franchise_id: Franchise (team) id.
        game_id: Single-game filter, facet family only, forwarded uncoerced.
        division: NCAA division fan-out, facet family only, and **only honoured when league=ncaa** — for any other league it is ignored entirely.
        headers: optional headers dict reused across calls; an ``Authorization: Bearer <key>`` here wins over ``api_key=`` (a keyword accepted by every wrapper) and the ``PFF_API_KEY`` / ``SDV_PY_PFF_API_KEY`` environment. With no key anywhere the call raises RuntimeError -- there is no anonymous access.
        return_parsed: parse the payload through parse_pff_report -> polars DataFrame (default True). Pass return_parsed=False for The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``..
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``. when ``return_parsed=False``.

    Raises:
        RuntimeError: No PFF API key (pass ``api_key=`` or set ``PFF_API_KEY``).
        NoDataError: PFF answered 404 (no such route, report or team).
        AssetFetchError: The request was refused or failed (401/403/429/5xx) -- the answer is unknown, never an empty frame.
        ValueError: PFF rejected the parameters (400 ``invalid_parameter`` / 422).

    Example:
        Quick start::

            from sportsdataverse.nfl import pff_api_facet_passing_allowed_pressure
            pff_api_facet_passing_allowed_pressure(league='nfl', season='2022')

        See Also:
            * `PFF Developer API`_ - reference + CLI guide
            * `nflverse`_ - NFL data ecosystem

        .. _PFF Developer API: https://developer.pff.com
        .. _nflverse: https://nflverse.nflverse.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "league": league,
        "season": season,
        "week": week,
        "franchise_id": franchise_id,
        "game_id": game_id,
        "division": division,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.pff.com/v1/facet/passing/allowed_pressure",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_pff_report(raw, return_as_pandas=return_as_pandas)
    return raw


def pff_api_facet_passing_concept(
    league: Optional[str] = None,
    season: Optional[str] = None,
    week: Optional[str] = None,
    franchise_id: Optional[int] = None,
    game_id: Optional[int] = None,
    division: Optional[str] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """League-wide passing-by-concept leaderboard

    Endpoint: ``GET https://api.pff.com/v1/facet/passing/concept``
    Example URL: https://api.pff.com/v1/facet/passing/concept?league=nfl&season=2022

    Args:
        league: League slug for the leaderboard commands — exactly nfl, ncaa, hs, aaf and ufl are recognised; anything else is rejected.
        season: Season for the leaderboard commands.
        week: Week filter.
        franchise_id: Franchise (team) id.
        game_id: Single-game filter, facet family only, forwarded uncoerced.
        division: NCAA division fan-out, facet family only, and **only honoured when league=ncaa** — for any other league it is ignored entirely.
        headers: optional headers dict reused across calls; an ``Authorization: Bearer <key>`` here wins over ``api_key=`` (a keyword accepted by every wrapper) and the ``PFF_API_KEY`` / ``SDV_PY_PFF_API_KEY`` environment. With no key anywhere the call raises RuntimeError -- there is no anonymous access.
        return_parsed: parse the payload through parse_pff_report -> polars DataFrame (default True). Pass return_parsed=False for The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``..
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``. when ``return_parsed=False``.

    Raises:
        RuntimeError: No PFF API key (pass ``api_key=`` or set ``PFF_API_KEY``).
        NoDataError: PFF answered 404 (no such route, report or team).
        AssetFetchError: The request was refused or failed (401/403/429/5xx) -- the answer is unknown, never an empty frame.
        ValueError: PFF rejected the parameters (400 ``invalid_parameter`` / 422).

    Example:
        Quick start::

            from sportsdataverse.nfl import pff_api_facet_passing_concept
            pff_api_facet_passing_concept(league='nfl', season='2022')

        See Also:
            * `PFF Developer API`_ - reference + CLI guide
            * `nflverse`_ - NFL data ecosystem

        .. _PFF Developer API: https://developer.pff.com
        .. _nflverse: https://nflverse.nflverse.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "league": league,
        "season": season,
        "week": week,
        "franchise_id": franchise_id,
        "game_id": game_id,
        "division": division,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.pff.com/v1/facet/passing/concept",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_pff_report(raw, return_as_pandas=return_as_pandas)
    return raw


def pff_api_facet_passing_depth(
    league: Optional[str] = None,
    season: Optional[str] = None,
    week: Optional[str] = None,
    franchise_id: Optional[int] = None,
    game_id: Optional[int] = None,
    division: Optional[str] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """League-wide passing-by-depth leaderboard

    Endpoint: ``GET https://api.pff.com/v1/facet/passing/depth``
    Example URL: https://api.pff.com/v1/facet/passing/depth?league=nfl&season=2022

    Args:
        league: League slug for the leaderboard commands — exactly nfl, ncaa, hs, aaf and ufl are recognised; anything else is rejected.
        season: Season for the leaderboard commands.
        week: Week filter.
        franchise_id: Franchise (team) id.
        game_id: Single-game filter, facet family only, forwarded uncoerced.
        division: NCAA division fan-out, facet family only, and **only honoured when league=ncaa** — for any other league it is ignored entirely.
        headers: optional headers dict reused across calls; an ``Authorization: Bearer <key>`` here wins over ``api_key=`` (a keyword accepted by every wrapper) and the ``PFF_API_KEY`` / ``SDV_PY_PFF_API_KEY`` environment. With no key anywhere the call raises RuntimeError -- there is no anonymous access.
        return_parsed: parse the payload through parse_pff_report -> polars DataFrame (default True). Pass return_parsed=False for The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``..
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``. when ``return_parsed=False``.

    Raises:
        RuntimeError: No PFF API key (pass ``api_key=`` or set ``PFF_API_KEY``).
        NoDataError: PFF answered 404 (no such route, report or team).
        AssetFetchError: The request was refused or failed (401/403/429/5xx) -- the answer is unknown, never an empty frame.
        ValueError: PFF rejected the parameters (400 ``invalid_parameter`` / 422).

    Example:
        Quick start::

            from sportsdataverse.nfl import pff_api_facet_passing_depth
            pff_api_facet_passing_depth(league='nfl', season='2022')

        See Also:
            * `PFF Developer API`_ - reference + CLI guide
            * `nflverse`_ - NFL data ecosystem

        .. _PFF Developer API: https://developer.pff.com
        .. _nflverse: https://nflverse.nflverse.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "league": league,
        "season": season,
        "week": week,
        "franchise_id": franchise_id,
        "game_id": game_id,
        "division": division,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.pff.com/v1/facet/passing/depth",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_pff_report(raw, return_as_pandas=return_as_pandas)
    return raw


def pff_api_facet_passing_detail(
    league: Optional[str] = None,
    season: Optional[str] = None,
    week: Optional[str] = None,
    franchise_id: Optional[int] = None,
    game_id: Optional[int] = None,
    division: Optional[str] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """League-wide passing detail leaderboard

    Endpoint: ``GET https://api.pff.com/v1/facet/passing/detail``
    Example URL: https://api.pff.com/v1/facet/passing/detail?league=nfl&season=2022

    Args:
        league: League slug for the leaderboard commands — exactly nfl, ncaa, hs, aaf and ufl are recognised; anything else is rejected.
        season: Season for the leaderboard commands.
        week: Week filter.
        franchise_id: Franchise (team) id.
        game_id: Single-game filter, facet family only, forwarded uncoerced.
        division: NCAA division fan-out, facet family only, and **only honoured when league=ncaa** — for any other league it is ignored entirely.
        headers: optional headers dict reused across calls; an ``Authorization: Bearer <key>`` here wins over ``api_key=`` (a keyword accepted by every wrapper) and the ``PFF_API_KEY`` / ``SDV_PY_PFF_API_KEY`` environment. With no key anywhere the call raises RuntimeError -- there is no anonymous access.
        return_parsed: parse the payload through parse_pff_report -> polars DataFrame (default True). Pass return_parsed=False for The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``..
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``. when ``return_parsed=False``.

    Raises:
        RuntimeError: No PFF API key (pass ``api_key=`` or set ``PFF_API_KEY``).
        NoDataError: PFF answered 404 (no such route, report or team).
        AssetFetchError: The request was refused or failed (401/403/429/5xx) -- the answer is unknown, never an empty frame.
        ValueError: PFF rejected the parameters (400 ``invalid_parameter`` / 422).

    Example:
        Quick start::

            from sportsdataverse.nfl import pff_api_facet_passing_detail
            pff_api_facet_passing_detail(league='nfl', season='2022')

        See Also:
            * `PFF Developer API`_ - reference + CLI guide
            * `nflverse`_ - NFL data ecosystem

        .. _PFF Developer API: https://developer.pff.com
        .. _nflverse: https://nflverse.nflverse.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "league": league,
        "season": season,
        "week": week,
        "franchise_id": franchise_id,
        "game_id": game_id,
        "division": division,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.pff.com/v1/facet/passing/detail",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_pff_report(raw, return_as_pandas=return_as_pandas)
    return raw


def pff_api_facet_passing_pressure(
    league: Optional[str] = None,
    season: Optional[str] = None,
    week: Optional[str] = None,
    franchise_id: Optional[int] = None,
    game_id: Optional[int] = None,
    division: Optional[str] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """League-wide passing-under-pressure leaderboard

    Endpoint: ``GET https://api.pff.com/v1/facet/passing/pressure``
    Example URL: https://api.pff.com/v1/facet/passing/pressure?league=nfl&season=2022

    Args:
        league: League slug for the leaderboard commands — exactly nfl, ncaa, hs, aaf and ufl are recognised; anything else is rejected.
        season: Season for the leaderboard commands.
        week: Week filter.
        franchise_id: Franchise (team) id.
        game_id: Single-game filter, facet family only, forwarded uncoerced.
        division: NCAA division fan-out, facet family only, and **only honoured when league=ncaa** — for any other league it is ignored entirely.
        headers: optional headers dict reused across calls; an ``Authorization: Bearer <key>`` here wins over ``api_key=`` (a keyword accepted by every wrapper) and the ``PFF_API_KEY`` / ``SDV_PY_PFF_API_KEY`` environment. With no key anywhere the call raises RuntimeError -- there is no anonymous access.
        return_parsed: parse the payload through parse_pff_report -> polars DataFrame (default True). Pass return_parsed=False for The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``..
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``. when ``return_parsed=False``.

    Raises:
        RuntimeError: No PFF API key (pass ``api_key=`` or set ``PFF_API_KEY``).
        NoDataError: PFF answered 404 (no such route, report or team).
        AssetFetchError: The request was refused or failed (401/403/429/5xx) -- the answer is unknown, never an empty frame.
        ValueError: PFF rejected the parameters (400 ``invalid_parameter`` / 422).

    Example:
        Quick start::

            from sportsdataverse.nfl import pff_api_facet_passing_pressure
            pff_api_facet_passing_pressure(league='nfl', season='2022')

        See Also:
            * `PFF Developer API`_ - reference + CLI guide
            * `nflverse`_ - NFL data ecosystem

        .. _PFF Developer API: https://developer.pff.com
        .. _nflverse: https://nflverse.nflverse.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "league": league,
        "season": season,
        "week": week,
        "franchise_id": franchise_id,
        "game_id": game_id,
        "division": division,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.pff.com/v1/facet/passing/pressure",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_pff_report(raw, return_as_pandas=return_as_pandas)
    return raw


def pff_api_facet_passing_summary(
    league: Optional[str] = None,
    season: Optional[str] = None,
    week: Optional[str] = None,
    franchise_id: Optional[int] = None,
    game_id: Optional[int] = None,
    division: Optional[str] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """League-wide passing summary leaderboard

    Endpoint: ``GET https://api.pff.com/v1/facet/passing/summary``
    Example URL: https://api.pff.com/v1/facet/passing/summary?league=nfl&season=2022

    Args:
        league: League slug for the leaderboard commands — exactly nfl, ncaa, hs, aaf and ufl are recognised; anything else is rejected.
        season: Season for the leaderboard commands.
        week: Week filter.
        franchise_id: Franchise (team) id.
        game_id: Single-game filter, facet family only, forwarded uncoerced.
        division: NCAA division fan-out, facet family only, and **only honoured when league=ncaa** — for any other league it is ignored entirely.
        headers: optional headers dict reused across calls; an ``Authorization: Bearer <key>`` here wins over ``api_key=`` (a keyword accepted by every wrapper) and the ``PFF_API_KEY`` / ``SDV_PY_PFF_API_KEY`` environment. With no key anywhere the call raises RuntimeError -- there is no anonymous access.
        return_parsed: parse the payload through parse_pff_report -> polars DataFrame (default True). Pass return_parsed=False for The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``..
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``. when ``return_parsed=False``.

    Raises:
        RuntimeError: No PFF API key (pass ``api_key=`` or set ``PFF_API_KEY``).
        NoDataError: PFF answered 404 (no such route, report or team).
        AssetFetchError: The request was refused or failed (401/403/429/5xx) -- the answer is unknown, never an empty frame.
        ValueError: PFF rejected the parameters (400 ``invalid_parameter`` / 422).

    Example:
        Quick start::

            from sportsdataverse.nfl import pff_api_facet_passing_summary
            pff_api_facet_passing_summary(league='nfl', season='2022')

        See Also:
            * `PFF Developer API`_ - reference + CLI guide
            * `nflverse`_ - NFL data ecosystem

        .. _PFF Developer API: https://developer.pff.com
        .. _nflverse: https://nflverse.nflverse.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "league": league,
        "season": season,
        "week": week,
        "franchise_id": franchise_id,
        "game_id": game_id,
        "division": division,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.pff.com/v1/facet/passing/summary",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_pff_report(raw, return_as_pandas=return_as_pandas)
    return raw


def pff_api_facet_receiving_concept(
    league: Optional[str] = None,
    season: Optional[str] = None,
    week: Optional[str] = None,
    franchise_id: Optional[int] = None,
    game_id: Optional[int] = None,
    division: Optional[str] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """League-wide receiving-by-concept leaderboard

    Endpoint: ``GET https://api.pff.com/v1/facet/receiving/concept``
    Example URL: https://api.pff.com/v1/facet/receiving/concept?league=nfl&season=2022

    Args:
        league: League slug for the leaderboard commands — exactly nfl, ncaa, hs, aaf and ufl are recognised; anything else is rejected.
        season: Season for the leaderboard commands.
        week: Week filter.
        franchise_id: Franchise (team) id.
        game_id: Single-game filter, facet family only, forwarded uncoerced.
        division: NCAA division fan-out, facet family only, and **only honoured when league=ncaa** — for any other league it is ignored entirely.
        headers: optional headers dict reused across calls; an ``Authorization: Bearer <key>`` here wins over ``api_key=`` (a keyword accepted by every wrapper) and the ``PFF_API_KEY`` / ``SDV_PY_PFF_API_KEY`` environment. With no key anywhere the call raises RuntimeError -- there is no anonymous access.
        return_parsed: parse the payload through parse_pff_report -> polars DataFrame (default True). Pass return_parsed=False for The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``..
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``. when ``return_parsed=False``.

    Raises:
        RuntimeError: No PFF API key (pass ``api_key=`` or set ``PFF_API_KEY``).
        NoDataError: PFF answered 404 (no such route, report or team).
        AssetFetchError: The request was refused or failed (401/403/429/5xx) -- the answer is unknown, never an empty frame.
        ValueError: PFF rejected the parameters (400 ``invalid_parameter`` / 422).

    Example:
        Quick start::

            from sportsdataverse.nfl import pff_api_facet_receiving_concept
            pff_api_facet_receiving_concept(league='nfl', season='2022')

        See Also:
            * `PFF Developer API`_ - reference + CLI guide
            * `nflverse`_ - NFL data ecosystem

        .. _PFF Developer API: https://developer.pff.com
        .. _nflverse: https://nflverse.nflverse.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "league": league,
        "season": season,
        "week": week,
        "franchise_id": franchise_id,
        "game_id": game_id,
        "division": division,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.pff.com/v1/facet/receiving/concept",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_pff_report(raw, return_as_pandas=return_as_pandas)
    return raw


def pff_api_facet_receiving_coverage(
    league: Optional[str] = None,
    season: Optional[str] = None,
    week: Optional[str] = None,
    franchise_id: Optional[int] = None,
    game_id: Optional[int] = None,
    division: Optional[str] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """League-wide receiving-versus-coverage leaderboard

    Endpoint: ``GET https://api.pff.com/v1/facet/receiving/coverage``
    Example URL: https://api.pff.com/v1/facet/receiving/coverage?league=nfl&season=2022

    Args:
        league: League slug for the leaderboard commands — exactly nfl, ncaa, hs, aaf and ufl are recognised; anything else is rejected.
        season: Season for the leaderboard commands.
        week: Week filter.
        franchise_id: Franchise (team) id.
        game_id: Single-game filter, facet family only, forwarded uncoerced.
        division: NCAA division fan-out, facet family only, and **only honoured when league=ncaa** — for any other league it is ignored entirely.
        headers: optional headers dict reused across calls; an ``Authorization: Bearer <key>`` here wins over ``api_key=`` (a keyword accepted by every wrapper) and the ``PFF_API_KEY`` / ``SDV_PY_PFF_API_KEY`` environment. With no key anywhere the call raises RuntimeError -- there is no anonymous access.
        return_parsed: parse the payload through parse_pff_report -> polars DataFrame (default True). Pass return_parsed=False for The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``..
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``. when ``return_parsed=False``.

    Raises:
        RuntimeError: No PFF API key (pass ``api_key=`` or set ``PFF_API_KEY``).
        NoDataError: PFF answered 404 (no such route, report or team).
        AssetFetchError: The request was refused or failed (401/403/429/5xx) -- the answer is unknown, never an empty frame.
        ValueError: PFF rejected the parameters (400 ``invalid_parameter`` / 422).

    Example:
        Quick start::

            from sportsdataverse.nfl import pff_api_facet_receiving_coverage
            pff_api_facet_receiving_coverage(league='nfl', season='2022')

        See Also:
            * `PFF Developer API`_ - reference + CLI guide
            * `nflverse`_ - NFL data ecosystem

        .. _PFF Developer API: https://developer.pff.com
        .. _nflverse: https://nflverse.nflverse.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "league": league,
        "season": season,
        "week": week,
        "franchise_id": franchise_id,
        "game_id": game_id,
        "division": division,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.pff.com/v1/facet/receiving/coverage",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_pff_report(raw, return_as_pandas=return_as_pandas)
    return raw


def pff_api_facet_receiving_depth(
    league: Optional[str] = None,
    season: Optional[str] = None,
    week: Optional[str] = None,
    franchise_id: Optional[int] = None,
    game_id: Optional[int] = None,
    division: Optional[str] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """League-wide receiving-by-depth leaderboard

    Endpoint: ``GET https://api.pff.com/v1/facet/receiving/depth``
    Example URL: https://api.pff.com/v1/facet/receiving/depth?league=nfl&season=2022

    Args:
        league: League slug for the leaderboard commands — exactly nfl, ncaa, hs, aaf and ufl are recognised; anything else is rejected.
        season: Season for the leaderboard commands.
        week: Week filter.
        franchise_id: Franchise (team) id.
        game_id: Single-game filter, facet family only, forwarded uncoerced.
        division: NCAA division fan-out, facet family only, and **only honoured when league=ncaa** — for any other league it is ignored entirely.
        headers: optional headers dict reused across calls; an ``Authorization: Bearer <key>`` here wins over ``api_key=`` (a keyword accepted by every wrapper) and the ``PFF_API_KEY`` / ``SDV_PY_PFF_API_KEY`` environment. With no key anywhere the call raises RuntimeError -- there is no anonymous access.
        return_parsed: parse the payload through parse_pff_report -> polars DataFrame (default True). Pass return_parsed=False for The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``..
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``. when ``return_parsed=False``.

    Raises:
        RuntimeError: No PFF API key (pass ``api_key=`` or set ``PFF_API_KEY``).
        NoDataError: PFF answered 404 (no such route, report or team).
        AssetFetchError: The request was refused or failed (401/403/429/5xx) -- the answer is unknown, never an empty frame.
        ValueError: PFF rejected the parameters (400 ``invalid_parameter`` / 422).

    Example:
        Quick start::

            from sportsdataverse.nfl import pff_api_facet_receiving_depth
            pff_api_facet_receiving_depth(league='nfl', season='2022')

        See Also:
            * `PFF Developer API`_ - reference + CLI guide
            * `nflverse`_ - NFL data ecosystem

        .. _PFF Developer API: https://developer.pff.com
        .. _nflverse: https://nflverse.nflverse.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "league": league,
        "season": season,
        "week": week,
        "franchise_id": franchise_id,
        "game_id": game_id,
        "division": division,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.pff.com/v1/facet/receiving/depth",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_pff_report(raw, return_as_pandas=return_as_pandas)
    return raw


def pff_api_facet_receiving_scheme(
    league: Optional[str] = None,
    season: Optional[str] = None,
    week: Optional[str] = None,
    franchise_id: Optional[int] = None,
    game_id: Optional[int] = None,
    division: Optional[str] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """League-wide receiving-by-scheme leaderboard

    Endpoint: ``GET https://api.pff.com/v1/facet/receiving/scheme``
    Example URL: https://api.pff.com/v1/facet/receiving/scheme?league=nfl&season=2022

    Args:
        league: League slug for the leaderboard commands — exactly nfl, ncaa, hs, aaf and ufl are recognised; anything else is rejected.
        season: Season for the leaderboard commands.
        week: Week filter.
        franchise_id: Franchise (team) id.
        game_id: Single-game filter, facet family only, forwarded uncoerced.
        division: NCAA division fan-out, facet family only, and **only honoured when league=ncaa** — for any other league it is ignored entirely.
        headers: optional headers dict reused across calls; an ``Authorization: Bearer <key>`` here wins over ``api_key=`` (a keyword accepted by every wrapper) and the ``PFF_API_KEY`` / ``SDV_PY_PFF_API_KEY`` environment. With no key anywhere the call raises RuntimeError -- there is no anonymous access.
        return_parsed: parse the payload through parse_pff_report -> polars DataFrame (default True). Pass return_parsed=False for The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``..
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``. when ``return_parsed=False``.

    Raises:
        RuntimeError: No PFF API key (pass ``api_key=`` or set ``PFF_API_KEY``).
        NoDataError: PFF answered 404 (no such route, report or team).
        AssetFetchError: The request was refused or failed (401/403/429/5xx) -- the answer is unknown, never an empty frame.
        ValueError: PFF rejected the parameters (400 ``invalid_parameter`` / 422).

    Example:
        Quick start::

            from sportsdataverse.nfl import pff_api_facet_receiving_scheme
            pff_api_facet_receiving_scheme(league='nfl', season='2022')

        See Also:
            * `PFF Developer API`_ - reference + CLI guide
            * `nflverse`_ - NFL data ecosystem

        .. _PFF Developer API: https://developer.pff.com
        .. _nflverse: https://nflverse.nflverse.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "league": league,
        "season": season,
        "week": week,
        "franchise_id": franchise_id,
        "game_id": game_id,
        "division": division,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.pff.com/v1/facet/receiving/scheme",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_pff_report(raw, return_as_pandas=return_as_pandas)
    return raw


def pff_api_facet_receiving_summary(
    league: Optional[str] = None,
    season: Optional[str] = None,
    week: Optional[str] = None,
    franchise_id: Optional[int] = None,
    game_id: Optional[int] = None,
    division: Optional[str] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """League-wide receiving summary leaderboard

    Endpoint: ``GET https://api.pff.com/v1/facet/receiving/summary``
    Example URL: https://api.pff.com/v1/facet/receiving/summary?league=nfl&season=2022

    Args:
        league: League slug for the leaderboard commands — exactly nfl, ncaa, hs, aaf and ufl are recognised; anything else is rejected.
        season: Season for the leaderboard commands.
        week: Week filter.
        franchise_id: Franchise (team) id.
        game_id: Single-game filter, facet family only, forwarded uncoerced.
        division: NCAA division fan-out, facet family only, and **only honoured when league=ncaa** — for any other league it is ignored entirely.
        headers: optional headers dict reused across calls; an ``Authorization: Bearer <key>`` here wins over ``api_key=`` (a keyword accepted by every wrapper) and the ``PFF_API_KEY`` / ``SDV_PY_PFF_API_KEY`` environment. With no key anywhere the call raises RuntimeError -- there is no anonymous access.
        return_parsed: parse the payload through parse_pff_report -> polars DataFrame (default True). Pass return_parsed=False for The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``..
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``. when ``return_parsed=False``.

    Raises:
        RuntimeError: No PFF API key (pass ``api_key=`` or set ``PFF_API_KEY``).
        NoDataError: PFF answered 404 (no such route, report or team).
        AssetFetchError: The request was refused or failed (401/403/429/5xx) -- the answer is unknown, never an empty frame.
        ValueError: PFF rejected the parameters (400 ``invalid_parameter`` / 422).

    Example:
        Quick start::

            from sportsdataverse.nfl import pff_api_facet_receiving_summary
            pff_api_facet_receiving_summary(league='nfl', season='2022')

        See Also:
            * `PFF Developer API`_ - reference + CLI guide
            * `nflverse`_ - NFL data ecosystem

        .. _PFF Developer API: https://developer.pff.com
        .. _nflverse: https://nflverse.nflverse.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "league": league,
        "season": season,
        "week": week,
        "franchise_id": franchise_id,
        "game_id": game_id,
        "division": division,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.pff.com/v1/facet/receiving/summary",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_pff_report(raw, return_as_pandas=return_as_pandas)
    return raw


def pff_api_facet_rushing_direction(
    league: Optional[str] = None,
    season: Optional[str] = None,
    week: Optional[str] = None,
    franchise_id: Optional[int] = None,
    game_id: Optional[int] = None,
    division: Optional[str] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """League-wide rushing-by-direction leaderboard

    Endpoint: ``GET https://api.pff.com/v1/facet/rushing/direction``
    Example URL: https://api.pff.com/v1/facet/rushing/direction?league=nfl&season=2022

    Args:
        league: League slug for the leaderboard commands — exactly nfl, ncaa, hs, aaf and ufl are recognised; anything else is rejected.
        season: Season for the leaderboard commands.
        week: Week filter.
        franchise_id: Franchise (team) id.
        game_id: Single-game filter, facet family only, forwarded uncoerced.
        division: NCAA division fan-out, facet family only, and **only honoured when league=ncaa** — for any other league it is ignored entirely.
        headers: optional headers dict reused across calls; an ``Authorization: Bearer <key>`` here wins over ``api_key=`` (a keyword accepted by every wrapper) and the ``PFF_API_KEY`` / ``SDV_PY_PFF_API_KEY`` environment. With no key anywhere the call raises RuntimeError -- there is no anonymous access.
        return_parsed: parse the payload through parse_pff_report -> polars DataFrame (default True). Pass return_parsed=False for The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``..
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``. when ``return_parsed=False``.

    Raises:
        RuntimeError: No PFF API key (pass ``api_key=`` or set ``PFF_API_KEY``).
        NoDataError: PFF answered 404 (no such route, report or team).
        AssetFetchError: The request was refused or failed (401/403/429/5xx) -- the answer is unknown, never an empty frame.
        ValueError: PFF rejected the parameters (400 ``invalid_parameter`` / 422).

    Example:
        Quick start::

            from sportsdataverse.nfl import pff_api_facet_rushing_direction
            pff_api_facet_rushing_direction(league='nfl', season='2022')

        See Also:
            * `PFF Developer API`_ - reference + CLI guide
            * `nflverse`_ - NFL data ecosystem

        .. _PFF Developer API: https://developer.pff.com
        .. _nflverse: https://nflverse.nflverse.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "league": league,
        "season": season,
        "week": week,
        "franchise_id": franchise_id,
        "game_id": game_id,
        "division": division,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.pff.com/v1/facet/rushing/direction",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_pff_report(raw, return_as_pandas=return_as_pandas)
    return raw


def pff_api_facet_rushing_summary(
    league: Optional[str] = None,
    season: Optional[str] = None,
    week: Optional[str] = None,
    franchise_id: Optional[int] = None,
    game_id: Optional[int] = None,
    division: Optional[str] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """League-wide rushing summary leaderboard

    Endpoint: ``GET https://api.pff.com/v1/facet/rushing/summary``
    Example URL: https://api.pff.com/v1/facet/rushing/summary?league=nfl&season=2022

    Args:
        league: League slug for the leaderboard commands — exactly nfl, ncaa, hs, aaf and ufl are recognised; anything else is rejected.
        season: Season for the leaderboard commands.
        week: Week filter.
        franchise_id: Franchise (team) id.
        game_id: Single-game filter, facet family only, forwarded uncoerced.
        division: NCAA division fan-out, facet family only, and **only honoured when league=ncaa** — for any other league it is ignored entirely.
        headers: optional headers dict reused across calls; an ``Authorization: Bearer <key>`` here wins over ``api_key=`` (a keyword accepted by every wrapper) and the ``PFF_API_KEY`` / ``SDV_PY_PFF_API_KEY`` environment. With no key anywhere the call raises RuntimeError -- there is no anonymous access.
        return_parsed: parse the payload through parse_pff_report -> polars DataFrame (default True). Pass return_parsed=False for The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``..
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``. when ``return_parsed=False``.

    Raises:
        RuntimeError: No PFF API key (pass ``api_key=`` or set ``PFF_API_KEY``).
        NoDataError: PFF answered 404 (no such route, report or team).
        AssetFetchError: The request was refused or failed (401/403/429/5xx) -- the answer is unknown, never an empty frame.
        ValueError: PFF rejected the parameters (400 ``invalid_parameter`` / 422).

    Example:
        Quick start::

            from sportsdataverse.nfl import pff_api_facet_rushing_summary
            pff_api_facet_rushing_summary(league='nfl', season='2022')

        See Also:
            * `PFF Developer API`_ - reference + CLI guide
            * `nflverse`_ - NFL data ecosystem

        .. _PFF Developer API: https://developer.pff.com
        .. _nflverse: https://nflverse.nflverse.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "league": league,
        "season": season,
        "week": week,
        "franchise_id": franchise_id,
        "game_id": game_id,
        "division": division,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.pff.com/v1/facet/rushing/summary",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_pff_report(raw, return_as_pandas=return_as_pandas)
    return raw


def pff_api_facet_defense_coverage(
    league: Optional[str] = None,
    season: Optional[str] = None,
    week: Optional[str] = None,
    franchise_id: Optional[int] = None,
    game_id: Optional[int] = None,
    division: Optional[str] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """League-wide coverage leaderboard

    Endpoint: ``GET https://api.pff.com/v1/facet/defense/coverage``
    Example URL: https://api.pff.com/v1/facet/defense/coverage?league=nfl&season=2022

    Args:
        league: League slug for the leaderboard commands — exactly nfl, ncaa, hs, aaf and ufl are recognised; anything else is rejected.
        season: Season for the leaderboard commands.
        week: Week filter.
        franchise_id: Franchise (team) id.
        game_id: Single-game filter, facet family only, forwarded uncoerced.
        division: NCAA division fan-out, facet family only, and **only honoured when league=ncaa** — for any other league it is ignored entirely.
        headers: optional headers dict reused across calls; an ``Authorization: Bearer <key>`` here wins over ``api_key=`` (a keyword accepted by every wrapper) and the ``PFF_API_KEY`` / ``SDV_PY_PFF_API_KEY`` environment. With no key anywhere the call raises RuntimeError -- there is no anonymous access.
        return_parsed: parse the payload through parse_pff_report -> polars DataFrame (default True). Pass return_parsed=False for The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``..
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``. when ``return_parsed=False``.

    Raises:
        RuntimeError: No PFF API key (pass ``api_key=`` or set ``PFF_API_KEY``).
        NoDataError: PFF answered 404 (no such route, report or team).
        AssetFetchError: The request was refused or failed (401/403/429/5xx) -- the answer is unknown, never an empty frame.
        ValueError: PFF rejected the parameters (400 ``invalid_parameter`` / 422).

    Example:
        Quick start::

            from sportsdataverse.nfl import pff_api_facet_defense_coverage
            pff_api_facet_defense_coverage(league='nfl', season='2022')

        See Also:
            * `PFF Developer API`_ - reference + CLI guide
            * `nflverse`_ - NFL data ecosystem

        .. _PFF Developer API: https://developer.pff.com
        .. _nflverse: https://nflverse.nflverse.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "league": league,
        "season": season,
        "week": week,
        "franchise_id": franchise_id,
        "game_id": game_id,
        "division": division,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.pff.com/v1/facet/defense/coverage",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_pff_report(raw, return_as_pandas=return_as_pandas)
    return raw


def pff_api_facet_defense_coverage_scheme(
    league: Optional[str] = None,
    season: Optional[str] = None,
    week: Optional[str] = None,
    franchise_id: Optional[int] = None,
    game_id: Optional[int] = None,
    division: Optional[str] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """League-wide coverage-by-scheme leaderboard

    Endpoint: ``GET https://api.pff.com/v1/facet/defense/coverage_scheme``
    Example URL: https://api.pff.com/v1/facet/defense/coverage_scheme?league=nfl&season=2022

    Args:
        league: League slug for the leaderboard commands — exactly nfl, ncaa, hs, aaf and ufl are recognised; anything else is rejected.
        season: Season for the leaderboard commands.
        week: Week filter.
        franchise_id: Franchise (team) id.
        game_id: Single-game filter, facet family only, forwarded uncoerced.
        division: NCAA division fan-out, facet family only, and **only honoured when league=ncaa** — for any other league it is ignored entirely.
        headers: optional headers dict reused across calls; an ``Authorization: Bearer <key>`` here wins over ``api_key=`` (a keyword accepted by every wrapper) and the ``PFF_API_KEY`` / ``SDV_PY_PFF_API_KEY`` environment. With no key anywhere the call raises RuntimeError -- there is no anonymous access.
        return_parsed: parse the payload through parse_pff_report -> polars DataFrame (default True). Pass return_parsed=False for The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``..
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``. when ``return_parsed=False``.

    Raises:
        RuntimeError: No PFF API key (pass ``api_key=`` or set ``PFF_API_KEY``).
        NoDataError: PFF answered 404 (no such route, report or team).
        AssetFetchError: The request was refused or failed (401/403/429/5xx) -- the answer is unknown, never an empty frame.
        ValueError: PFF rejected the parameters (400 ``invalid_parameter`` / 422).

    Example:
        Quick start::

            from sportsdataverse.nfl import pff_api_facet_defense_coverage_scheme
            pff_api_facet_defense_coverage_scheme(league='nfl', season='2022')

        See Also:
            * `PFF Developer API`_ - reference + CLI guide
            * `nflverse`_ - NFL data ecosystem

        .. _PFF Developer API: https://developer.pff.com
        .. _nflverse: https://nflverse.nflverse.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "league": league,
        "season": season,
        "week": week,
        "franchise_id": franchise_id,
        "game_id": game_id,
        "division": division,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.pff.com/v1/facet/defense/coverage_scheme",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_pff_report(raw, return_as_pandas=return_as_pandas)
    return raw


def pff_api_facet_defense_coverage_matchup(
    league: Optional[str] = None,
    season: Optional[str] = None,
    week: Optional[str] = None,
    franchise_id: Optional[int] = None,
    game_id: Optional[int] = None,
    division: Optional[str] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """League-wide coverage matchup leaderboard

    Endpoint: ``GET https://api.pff.com/v1/facet/defense/coverage_matchup``
    Example URL: https://api.pff.com/v1/facet/defense/coverage_matchup?league=nfl&season=2022

    Args:
        league: League slug for the leaderboard commands — exactly nfl, ncaa, hs, aaf and ufl are recognised; anything else is rejected.
        season: Season for the leaderboard commands.
        week: Week filter.
        franchise_id: Franchise (team) id.
        game_id: Single-game filter, facet family only, forwarded uncoerced.
        division: NCAA division fan-out, facet family only, and **only honoured when league=ncaa** — for any other league it is ignored entirely.
        headers: optional headers dict reused across calls; an ``Authorization: Bearer <key>`` here wins over ``api_key=`` (a keyword accepted by every wrapper) and the ``PFF_API_KEY`` / ``SDV_PY_PFF_API_KEY`` environment. With no key anywhere the call raises RuntimeError -- there is no anonymous access.
        return_parsed: parse the payload through parse_pff_report -> polars DataFrame (default True). Pass return_parsed=False for The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``..
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``. when ``return_parsed=False``.

    Raises:
        RuntimeError: No PFF API key (pass ``api_key=`` or set ``PFF_API_KEY``).
        NoDataError: PFF answered 404 (no such route, report or team).
        AssetFetchError: The request was refused or failed (401/403/429/5xx) -- the answer is unknown, never an empty frame.
        ValueError: PFF rejected the parameters (400 ``invalid_parameter`` / 422).

    Example:
        Quick start::

            from sportsdataverse.nfl import pff_api_facet_defense_coverage_matchup
            pff_api_facet_defense_coverage_matchup(league='nfl', season='2022')

        See Also:
            * `PFF Developer API`_ - reference + CLI guide
            * `nflverse`_ - NFL data ecosystem

        .. _PFF Developer API: https://developer.pff.com
        .. _nflverse: https://nflverse.nflverse.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "league": league,
        "season": season,
        "week": week,
        "franchise_id": franchise_id,
        "game_id": game_id,
        "division": division,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.pff.com/v1/facet/defense/coverage_matchup",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_pff_report(raw, return_as_pandas=return_as_pandas)
    return raw


def pff_api_facet_defense_pass_rush(
    league: Optional[str] = None,
    season: Optional[str] = None,
    week: Optional[str] = None,
    franchise_id: Optional[int] = None,
    game_id: Optional[int] = None,
    division: Optional[str] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """League-wide pass-rush leaderboard

    Endpoint: ``GET https://api.pff.com/v1/facet/defense/pass_rush``
    Example URL: https://api.pff.com/v1/facet/defense/pass_rush?league=nfl&season=2022

    Args:
        league: League slug for the leaderboard commands — exactly nfl, ncaa, hs, aaf and ufl are recognised; anything else is rejected.
        season: Season for the leaderboard commands.
        week: Week filter.
        franchise_id: Franchise (team) id.
        game_id: Single-game filter, facet family only, forwarded uncoerced.
        division: NCAA division fan-out, facet family only, and **only honoured when league=ncaa** — for any other league it is ignored entirely.
        headers: optional headers dict reused across calls; an ``Authorization: Bearer <key>`` here wins over ``api_key=`` (a keyword accepted by every wrapper) and the ``PFF_API_KEY`` / ``SDV_PY_PFF_API_KEY`` environment. With no key anywhere the call raises RuntimeError -- there is no anonymous access.
        return_parsed: parse the payload through parse_pff_report -> polars DataFrame (default True). Pass return_parsed=False for The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``..
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``. when ``return_parsed=False``.

    Raises:
        RuntimeError: No PFF API key (pass ``api_key=`` or set ``PFF_API_KEY``).
        NoDataError: PFF answered 404 (no such route, report or team).
        AssetFetchError: The request was refused or failed (401/403/429/5xx) -- the answer is unknown, never an empty frame.
        ValueError: PFF rejected the parameters (400 ``invalid_parameter`` / 422).

    Example:
        Quick start::

            from sportsdataverse.nfl import pff_api_facet_defense_pass_rush
            pff_api_facet_defense_pass_rush(league='nfl', season='2022')

        See Also:
            * `PFF Developer API`_ - reference + CLI guide
            * `nflverse`_ - NFL data ecosystem

        .. _PFF Developer API: https://developer.pff.com
        .. _nflverse: https://nflverse.nflverse.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "league": league,
        "season": season,
        "week": week,
        "franchise_id": franchise_id,
        "game_id": game_id,
        "division": division,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.pff.com/v1/facet/defense/pass_rush",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_pff_report(raw, return_as_pandas=return_as_pandas)
    return raw


def pff_api_facet_defense_run(
    league: Optional[str] = None,
    season: Optional[str] = None,
    week: Optional[str] = None,
    franchise_id: Optional[int] = None,
    game_id: Optional[int] = None,
    division: Optional[str] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """League-wide run-defense leaderboard

    Endpoint: ``GET https://api.pff.com/v1/facet/defense/run``
    Example URL: https://api.pff.com/v1/facet/defense/run?league=nfl&season=2022

    Args:
        league: League slug for the leaderboard commands — exactly nfl, ncaa, hs, aaf and ufl are recognised; anything else is rejected.
        season: Season for the leaderboard commands.
        week: Week filter.
        franchise_id: Franchise (team) id.
        game_id: Single-game filter, facet family only, forwarded uncoerced.
        division: NCAA division fan-out, facet family only, and **only honoured when league=ncaa** — for any other league it is ignored entirely.
        headers: optional headers dict reused across calls; an ``Authorization: Bearer <key>`` here wins over ``api_key=`` (a keyword accepted by every wrapper) and the ``PFF_API_KEY`` / ``SDV_PY_PFF_API_KEY`` environment. With no key anywhere the call raises RuntimeError -- there is no anonymous access.
        return_parsed: parse the payload through parse_pff_report -> polars DataFrame (default True). Pass return_parsed=False for The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``..
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``. when ``return_parsed=False``.

    Raises:
        RuntimeError: No PFF API key (pass ``api_key=`` or set ``PFF_API_KEY``).
        NoDataError: PFF answered 404 (no such route, report or team).
        AssetFetchError: The request was refused or failed (401/403/429/5xx) -- the answer is unknown, never an empty frame.
        ValueError: PFF rejected the parameters (400 ``invalid_parameter`` / 422).

    Example:
        Quick start::

            from sportsdataverse.nfl import pff_api_facet_defense_run
            pff_api_facet_defense_run(league='nfl', season='2022')

        See Also:
            * `PFF Developer API`_ - reference + CLI guide
            * `nflverse`_ - NFL data ecosystem

        .. _PFF Developer API: https://developer.pff.com
        .. _nflverse: https://nflverse.nflverse.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "league": league,
        "season": season,
        "week": week,
        "franchise_id": franchise_id,
        "game_id": game_id,
        "division": division,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.pff.com/v1/facet/defense/run",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_pff_report(raw, return_as_pandas=return_as_pandas)
    return raw


def pff_api_facet_defense_summary(
    league: Optional[str] = None,
    season: Optional[str] = None,
    week: Optional[str] = None,
    franchise_id: Optional[int] = None,
    game_id: Optional[int] = None,
    division: Optional[str] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """League-wide defense summary leaderboard

    Endpoint: ``GET https://api.pff.com/v1/facet/defense/summary``
    Example URL: https://api.pff.com/v1/facet/defense/summary?league=nfl&season=2022

    Args:
        league: League slug for the leaderboard commands — exactly nfl, ncaa, hs, aaf and ufl are recognised; anything else is rejected.
        season: Season for the leaderboard commands.
        week: Week filter.
        franchise_id: Franchise (team) id.
        game_id: Single-game filter, facet family only, forwarded uncoerced.
        division: NCAA division fan-out, facet family only, and **only honoured when league=ncaa** — for any other league it is ignored entirely.
        headers: optional headers dict reused across calls; an ``Authorization: Bearer <key>`` here wins over ``api_key=`` (a keyword accepted by every wrapper) and the ``PFF_API_KEY`` / ``SDV_PY_PFF_API_KEY`` environment. With no key anywhere the call raises RuntimeError -- there is no anonymous access.
        return_parsed: parse the payload through parse_pff_report -> polars DataFrame (default True). Pass return_parsed=False for The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``..
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``. when ``return_parsed=False``.

    Raises:
        RuntimeError: No PFF API key (pass ``api_key=`` or set ``PFF_API_KEY``).
        NoDataError: PFF answered 404 (no such route, report or team).
        AssetFetchError: The request was refused or failed (401/403/429/5xx) -- the answer is unknown, never an empty frame.
        ValueError: PFF rejected the parameters (400 ``invalid_parameter`` / 422).

    Example:
        Quick start::

            from sportsdataverse.nfl import pff_api_facet_defense_summary
            pff_api_facet_defense_summary(league='nfl', season='2022')

        See Also:
            * `PFF Developer API`_ - reference + CLI guide
            * `nflverse`_ - NFL data ecosystem

        .. _PFF Developer API: https://developer.pff.com
        .. _nflverse: https://nflverse.nflverse.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "league": league,
        "season": season,
        "week": week,
        "franchise_id": franchise_id,
        "game_id": game_id,
        "division": division,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.pff.com/v1/facet/defense/summary",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_pff_report(raw, return_as_pandas=return_as_pandas)
    return raw


def pff_api_facet_field_goal_summary(
    league: Optional[str] = None,
    season: Optional[str] = None,
    week: Optional[str] = None,
    franchise_id: Optional[int] = None,
    game_id: Optional[int] = None,
    division: Optional[str] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """League-wide field-goal kicking leaderboard

    Endpoint: ``GET https://api.pff.com/v1/facet/field_goal/summary``
    Example URL: https://api.pff.com/v1/facet/field_goal/summary?league=nfl&season=2022

    Args:
        league: League slug for the leaderboard commands — exactly nfl, ncaa, hs, aaf and ufl are recognised; anything else is rejected.
        season: Season for the leaderboard commands.
        week: Week filter.
        franchise_id: Franchise (team) id.
        game_id: Single-game filter, facet family only, forwarded uncoerced.
        division: NCAA division fan-out, facet family only, and **only honoured when league=ncaa** — for any other league it is ignored entirely.
        headers: optional headers dict reused across calls; an ``Authorization: Bearer <key>`` here wins over ``api_key=`` (a keyword accepted by every wrapper) and the ``PFF_API_KEY`` / ``SDV_PY_PFF_API_KEY`` environment. With no key anywhere the call raises RuntimeError -- there is no anonymous access.
        return_parsed: parse the payload through parse_pff_report -> polars DataFrame (default True). Pass return_parsed=False for The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``..
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``. when ``return_parsed=False``.

    Raises:
        RuntimeError: No PFF API key (pass ``api_key=`` or set ``PFF_API_KEY``).
        NoDataError: PFF answered 404 (no such route, report or team).
        AssetFetchError: The request was refused or failed (401/403/429/5xx) -- the answer is unknown, never an empty frame.
        ValueError: PFF rejected the parameters (400 ``invalid_parameter`` / 422).

    Example:
        Quick start::

            from sportsdataverse.nfl import pff_api_facet_field_goal_summary
            pff_api_facet_field_goal_summary(league='nfl', season='2022')

        See Also:
            * `PFF Developer API`_ - reference + CLI guide
            * `nflverse`_ - NFL data ecosystem

        .. _PFF Developer API: https://developer.pff.com
        .. _nflverse: https://nflverse.nflverse.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "league": league,
        "season": season,
        "week": week,
        "franchise_id": franchise_id,
        "game_id": game_id,
        "division": division,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.pff.com/v1/facet/field_goal/summary",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_pff_report(raw, return_as_pandas=return_as_pandas)
    return raw


def pff_api_facet_kickoff_summary(
    league: Optional[str] = None,
    season: Optional[str] = None,
    week: Optional[str] = None,
    franchise_id: Optional[int] = None,
    game_id: Optional[int] = None,
    division: Optional[str] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """League-wide kickoff leaderboard

    Endpoint: ``GET https://api.pff.com/v1/facet/kickoff/summary``
    Example URL: https://api.pff.com/v1/facet/kickoff/summary?league=nfl&season=2022

    Args:
        league: League slug for the leaderboard commands — exactly nfl, ncaa, hs, aaf and ufl are recognised; anything else is rejected.
        season: Season for the leaderboard commands.
        week: Week filter.
        franchise_id: Franchise (team) id.
        game_id: Single-game filter, facet family only, forwarded uncoerced.
        division: NCAA division fan-out, facet family only, and **only honoured when league=ncaa** — for any other league it is ignored entirely.
        headers: optional headers dict reused across calls; an ``Authorization: Bearer <key>`` here wins over ``api_key=`` (a keyword accepted by every wrapper) and the ``PFF_API_KEY`` / ``SDV_PY_PFF_API_KEY`` environment. With no key anywhere the call raises RuntimeError -- there is no anonymous access.
        return_parsed: parse the payload through parse_pff_report -> polars DataFrame (default True). Pass return_parsed=False for The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``..
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``. when ``return_parsed=False``.

    Raises:
        RuntimeError: No PFF API key (pass ``api_key=`` or set ``PFF_API_KEY``).
        NoDataError: PFF answered 404 (no such route, report or team).
        AssetFetchError: The request was refused or failed (401/403/429/5xx) -- the answer is unknown, never an empty frame.
        ValueError: PFF rejected the parameters (400 ``invalid_parameter`` / 422).

    Example:
        Quick start::

            from sportsdataverse.nfl import pff_api_facet_kickoff_summary
            pff_api_facet_kickoff_summary(league='nfl', season='2022')

        See Also:
            * `PFF Developer API`_ - reference + CLI guide
            * `nflverse`_ - NFL data ecosystem

        .. _PFF Developer API: https://developer.pff.com
        .. _nflverse: https://nflverse.nflverse.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "league": league,
        "season": season,
        "week": week,
        "franchise_id": franchise_id,
        "game_id": game_id,
        "division": division,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.pff.com/v1/facet/kickoff/summary",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_pff_report(raw, return_as_pandas=return_as_pandas)
    return raw


def pff_api_facet_punting_summary(
    league: Optional[str] = None,
    season: Optional[str] = None,
    week: Optional[str] = None,
    franchise_id: Optional[int] = None,
    game_id: Optional[int] = None,
    division: Optional[str] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """League-wide punting leaderboard

    Endpoint: ``GET https://api.pff.com/v1/facet/punting/summary``
    Example URL: https://api.pff.com/v1/facet/punting/summary?league=nfl&season=2022

    Args:
        league: League slug for the leaderboard commands — exactly nfl, ncaa, hs, aaf and ufl are recognised; anything else is rejected.
        season: Season for the leaderboard commands.
        week: Week filter.
        franchise_id: Franchise (team) id.
        game_id: Single-game filter, facet family only, forwarded uncoerced.
        division: NCAA division fan-out, facet family only, and **only honoured when league=ncaa** — for any other league it is ignored entirely.
        headers: optional headers dict reused across calls; an ``Authorization: Bearer <key>`` here wins over ``api_key=`` (a keyword accepted by every wrapper) and the ``PFF_API_KEY`` / ``SDV_PY_PFF_API_KEY`` environment. With no key anywhere the call raises RuntimeError -- there is no anonymous access.
        return_parsed: parse the payload through parse_pff_report -> polars DataFrame (default True). Pass return_parsed=False for The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``..
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``. when ``return_parsed=False``.

    Raises:
        RuntimeError: No PFF API key (pass ``api_key=`` or set ``PFF_API_KEY``).
        NoDataError: PFF answered 404 (no such route, report or team).
        AssetFetchError: The request was refused or failed (401/403/429/5xx) -- the answer is unknown, never an empty frame.
        ValueError: PFF rejected the parameters (400 ``invalid_parameter`` / 422).

    Example:
        Quick start::

            from sportsdataverse.nfl import pff_api_facet_punting_summary
            pff_api_facet_punting_summary(league='nfl', season='2022')

        See Also:
            * `PFF Developer API`_ - reference + CLI guide
            * `nflverse`_ - NFL data ecosystem

        .. _PFF Developer API: https://developer.pff.com
        .. _nflverse: https://nflverse.nflverse.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "league": league,
        "season": season,
        "week": week,
        "franchise_id": franchise_id,
        "game_id": game_id,
        "division": division,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.pff.com/v1/facet/punting/summary",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_pff_report(raw, return_as_pandas=return_as_pandas)
    return raw


def pff_api_facet_return_summary(
    league: Optional[str] = None,
    season: Optional[str] = None,
    week: Optional[str] = None,
    franchise_id: Optional[int] = None,
    game_id: Optional[int] = None,
    division: Optional[str] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """League-wide return leaderboard

    Endpoint: ``GET https://api.pff.com/v1/facet/return/summary``
    Example URL: https://api.pff.com/v1/facet/return/summary?league=nfl&season=2022

    Args:
        league: League slug for the leaderboard commands — exactly nfl, ncaa, hs, aaf and ufl are recognised; anything else is rejected.
        season: Season for the leaderboard commands.
        week: Week filter.
        franchise_id: Franchise (team) id.
        game_id: Single-game filter, facet family only, forwarded uncoerced.
        division: NCAA division fan-out, facet family only, and **only honoured when league=ncaa** — for any other league it is ignored entirely.
        headers: optional headers dict reused across calls; an ``Authorization: Bearer <key>`` here wins over ``api_key=`` (a keyword accepted by every wrapper) and the ``PFF_API_KEY`` / ``SDV_PY_PFF_API_KEY`` environment. With no key anywhere the call raises RuntimeError -- there is no anonymous access.
        return_parsed: parse the payload through parse_pff_report -> polars DataFrame (default True). Pass return_parsed=False for The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``..
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``. when ``return_parsed=False``.

    Raises:
        RuntimeError: No PFF API key (pass ``api_key=`` or set ``PFF_API_KEY``).
        NoDataError: PFF answered 404 (no such route, report or team).
        AssetFetchError: The request was refused or failed (401/403/429/5xx) -- the answer is unknown, never an empty frame.
        ValueError: PFF rejected the parameters (400 ``invalid_parameter`` / 422).

    Example:
        Quick start::

            from sportsdataverse.nfl import pff_api_facet_return_summary
            pff_api_facet_return_summary(league='nfl', season='2022')

        See Also:
            * `PFF Developer API`_ - reference + CLI guide
            * `nflverse`_ - NFL data ecosystem

        .. _PFF Developer API: https://developer.pff.com
        .. _nflverse: https://nflverse.nflverse.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "league": league,
        "season": season,
        "week": week,
        "franchise_id": franchise_id,
        "game_id": game_id,
        "division": division,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.pff.com/v1/facet/return/summary",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_pff_report(raw, return_as_pandas=return_as_pandas)
    return raw


def pff_api_facet_special_summary(
    league: Optional[str] = None,
    season: Optional[str] = None,
    week: Optional[str] = None,
    franchise_id: Optional[int] = None,
    game_id: Optional[int] = None,
    division: Optional[str] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """League-wide special-teams leaderboard

    Endpoint: ``GET https://api.pff.com/v1/facet/special/summary``
    Example URL: https://api.pff.com/v1/facet/special/summary?league=nfl&season=2022

    Args:
        league: League slug for the leaderboard commands — exactly nfl, ncaa, hs, aaf and ufl are recognised; anything else is rejected.
        season: Season for the leaderboard commands.
        week: Week filter.
        franchise_id: Franchise (team) id.
        game_id: Single-game filter, facet family only, forwarded uncoerced.
        division: NCAA division fan-out, facet family only, and **only honoured when league=ncaa** — for any other league it is ignored entirely.
        headers: optional headers dict reused across calls; an ``Authorization: Bearer <key>`` here wins over ``api_key=`` (a keyword accepted by every wrapper) and the ``PFF_API_KEY`` / ``SDV_PY_PFF_API_KEY`` environment. With no key anywhere the call raises RuntimeError -- there is no anonymous access.
        return_parsed: parse the payload through parse_pff_report -> polars DataFrame (default True). Pass return_parsed=False for The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``..
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``. when ``return_parsed=False``.

    Raises:
        RuntimeError: No PFF API key (pass ``api_key=`` or set ``PFF_API_KEY``).
        NoDataError: PFF answered 404 (no such route, report or team).
        AssetFetchError: The request was refused or failed (401/403/429/5xx) -- the answer is unknown, never an empty frame.
        ValueError: PFF rejected the parameters (400 ``invalid_parameter`` / 422).

    Example:
        Quick start::

            from sportsdataverse.nfl import pff_api_facet_special_summary
            pff_api_facet_special_summary(league='nfl', season='2022')

        See Also:
            * `PFF Developer API`_ - reference + CLI guide
            * `nflverse`_ - NFL data ecosystem

        .. _PFF Developer API: https://developer.pff.com
        .. _nflverse: https://nflverse.nflverse.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "league": league,
        "season": season,
        "week": week,
        "franchise_id": franchise_id,
        "game_id": game_id,
        "division": division,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.pff.com/v1/facet/special/summary",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_pff_report(raw, return_as_pandas=return_as_pandas)
    return raw


def pff_api_signature_passing_time_in_pocket(
    league: str,
    season: str,
    week: str,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Signature stat: time in pocket

    Endpoint: ``GET https://api.pff.com/v1/facet/signature/passing/time_in_pocket``
    Example URL: https://api.pff.com/v1/facet/signature/passing/time_in_pocket?league=nfl&season=2022&week=1

    Args:
        league: League slug, and the FIRST positional argument of every command that takes one — every command except the facet leaderboards, where --league stays a flag because --game alone is a complete request.
        season: Season, and the SECOND positional argument of the four signature commands.
        week: Week, and the THIRD positional argument of the four signature commands.
        headers: optional headers dict reused across calls; an ``Authorization: Bearer <key>`` here wins over ``api_key=`` (a keyword accepted by every wrapper) and the ``PFF_API_KEY`` / ``SDV_PY_PFF_API_KEY`` environment. With no key anywhere the call raises RuntimeError -- there is no anonymous access.
        return_parsed: parse the payload through parse_pff_report -> polars DataFrame (default True). Pass return_parsed=False for The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``..
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``. when ``return_parsed=False``.

    Raises:
        RuntimeError: No PFF API key (pass ``api_key=`` or set ``PFF_API_KEY``).
        NoDataError: PFF answered 404 (no such route, report or team).
        AssetFetchError: The request was refused or failed (401/403/429/5xx) -- the answer is unknown, never an empty frame.
        ValueError: PFF rejected the parameters (400 ``invalid_parameter`` / 422).

    Example:
        Quick start::

            from sportsdataverse.nfl import pff_api_signature_passing_time_in_pocket
            pff_api_signature_passing_time_in_pocket(league='nfl', season='2022', week='1')

        See Also:
            * `PFF Developer API`_ - reference + CLI guide
            * `nflverse`_ - NFL data ecosystem

        .. _PFF Developer API: https://developer.pff.com
        .. _nflverse: https://nflverse.nflverse.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "league": league,
        "season": season,
        "week": week,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.pff.com/v1/facet/signature/passing/time_in_pocket",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_pff_report(raw, return_as_pandas=return_as_pandas)
    return raw


def pff_api_signature_pass_blocking_efficiency_line(
    league: str,
    season: str,
    week: str,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Signature stat: pass-blocking efficiency, by line

    Endpoint: ``GET https://api.pff.com/v1/facet/signature/pass-blocking/efficiency/line``
    Example URL: https://api.pff.com/v1/facet/signature/pass-blocking/efficiency/line?league=nfl&season=2022&week=1

    Args:
        league: League slug, and the FIRST positional argument of every command that takes one — every command except the facet leaderboards, where --league stays a flag because --game alone is a complete request.
        season: Season, and the SECOND positional argument of the four signature commands.
        week: Week, and the THIRD positional argument of the four signature commands.
        headers: optional headers dict reused across calls; an ``Authorization: Bearer <key>`` here wins over ``api_key=`` (a keyword accepted by every wrapper) and the ``PFF_API_KEY`` / ``SDV_PY_PFF_API_KEY`` environment. With no key anywhere the call raises RuntimeError -- there is no anonymous access.
        return_parsed: parse the payload through parse_pff_report -> polars DataFrame (default True). Pass return_parsed=False for The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``..
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``. when ``return_parsed=False``.

    Raises:
        RuntimeError: No PFF API key (pass ``api_key=`` or set ``PFF_API_KEY``).
        NoDataError: PFF answered 404 (no such route, report or team).
        AssetFetchError: The request was refused or failed (401/403/429/5xx) -- the answer is unknown, never an empty frame.
        ValueError: PFF rejected the parameters (400 ``invalid_parameter`` / 422).

    Example:
        Quick start::

            from sportsdataverse.nfl import pff_api_signature_pass_blocking_efficiency_line
            pff_api_signature_pass_blocking_efficiency_line(league='nfl', season='2022', week='1')

        See Also:
            * `PFF Developer API`_ - reference + CLI guide
            * `nflverse`_ - NFL data ecosystem

        .. _PFF Developer API: https://developer.pff.com
        .. _nflverse: https://nflverse.nflverse.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "league": league,
        "season": season,
        "week": week,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.pff.com/v1/facet/signature/pass-blocking/efficiency/line",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_pff_report(raw, return_as_pandas=return_as_pandas)
    return raw


def pff_api_signature_defense_outside_pass_rush(
    league: str,
    season: str,
    week: str,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Signature stat: outside pass rush

    Endpoint: ``GET https://api.pff.com/v1/facet/signature/defense/outside_pass_rush``
    Example URL: https://api.pff.com/v1/facet/signature/defense/outside_pass_rush?league=nfl&season=2022&week=1

    Args:
        league: League slug, and the FIRST positional argument of every command that takes one — every command except the facet leaderboards, where --league stays a flag because --game alone is a complete request.
        season: Season, and the SECOND positional argument of the four signature commands.
        week: Week, and the THIRD positional argument of the four signature commands.
        headers: optional headers dict reused across calls; an ``Authorization: Bearer <key>`` here wins over ``api_key=`` (a keyword accepted by every wrapper) and the ``PFF_API_KEY`` / ``SDV_PY_PFF_API_KEY`` environment. With no key anywhere the call raises RuntimeError -- there is no anonymous access.
        return_parsed: parse the payload through parse_pff_report -> polars DataFrame (default True). Pass return_parsed=False for The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``..
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``. when ``return_parsed=False``.

    Raises:
        RuntimeError: No PFF API key (pass ``api_key=`` or set ``PFF_API_KEY``).
        NoDataError: PFF answered 404 (no such route, report or team).
        AssetFetchError: The request was refused or failed (401/403/429/5xx) -- the answer is unknown, never an empty frame.
        ValueError: PFF rejected the parameters (400 ``invalid_parameter`` / 422).

    Example:
        Quick start::

            from sportsdataverse.nfl import pff_api_signature_defense_outside_pass_rush
            pff_api_signature_defense_outside_pass_rush(league='nfl', season='2022', week='1')

        See Also:
            * `PFF Developer API`_ - reference + CLI guide
            * `nflverse`_ - NFL data ecosystem

        .. _PFF Developer API: https://developer.pff.com
        .. _nflverse: https://nflverse.nflverse.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "league": league,
        "season": season,
        "week": week,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.pff.com/v1/facet/signature/defense/outside_pass_rush",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_pff_report(raw, return_as_pandas=return_as_pandas)
    return raw


def pff_api_signature_defense_slot_coverage(
    league: str,
    season: str,
    week: str,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Signature stat: slot coverage

    Endpoint: ``GET https://api.pff.com/v1/facet/signature/defense/slot_coverage``
    Example URL: https://api.pff.com/v1/facet/signature/defense/slot_coverage?league=nfl&season=2022&week=1

    Args:
        league: League slug, and the FIRST positional argument of every command that takes one — every command except the facet leaderboards, where --league stays a flag because --game alone is a complete request.
        season: Season, and the SECOND positional argument of the four signature commands.
        week: Week, and the THIRD positional argument of the four signature commands.
        headers: optional headers dict reused across calls; an ``Authorization: Bearer <key>`` here wins over ``api_key=`` (a keyword accepted by every wrapper) and the ``PFF_API_KEY`` / ``SDV_PY_PFF_API_KEY`` environment. With no key anywhere the call raises RuntimeError -- there is no anonymous access.
        return_parsed: parse the payload through parse_pff_report -> polars DataFrame (default True). Pass return_parsed=False for The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``..
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``. when ``return_parsed=False``.

    Raises:
        RuntimeError: No PFF API key (pass ``api_key=`` or set ``PFF_API_KEY``).
        NoDataError: PFF answered 404 (no such route, report or team).
        AssetFetchError: The request was refused or failed (401/403/429/5xx) -- the answer is unknown, never an empty frame.
        ValueError: PFF rejected the parameters (400 ``invalid_parameter`` / 422).

    Example:
        Quick start::

            from sportsdataverse.nfl import pff_api_signature_defense_slot_coverage
            pff_api_signature_defense_slot_coverage(league='nfl', season='2022', week='1')

        See Also:
            * `PFF Developer API`_ - reference + CLI guide
            * `nflverse`_ - NFL data ecosystem

        .. _PFF Developer API: https://developer.pff.com
        .. _nflverse: https://nflverse.nflverse.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "league": league,
        "season": season,
        "week": week,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.pff.com/v1/facet/signature/defense/slot_coverage",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_pff_report(raw, return_as_pandas=return_as_pandas)
    return raw


def pff_api_whoami(
    headers: Optional[Dict[str, str]] = None,
    **kwargs,
) -> Dict:
    """Show what this API believes about the current credential

    Endpoint: ``GET https://api.pff.com/v1/auth/whoami``
    Example URL: https://api.pff.com/v1/auth/whoami

    Args:
        headers: optional headers dict reused across calls; an ``Authorization: Bearer <key>`` here wins over ``api_key=`` (a keyword accepted by every wrapper) and the ``PFF_API_KEY`` / ``SDV_PY_PFF_API_KEY`` environment. With no key anywhere the call raises RuntimeError -- there is no anonymous access.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        The decoded json body. /v1 routes return the premium stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``..

    Raises:
        RuntimeError: No PFF API key (pass ``api_key=`` or set ``PFF_API_KEY``).
        NoDataError: PFF answered 404 (no such route, report or team).
        AssetFetchError: The request was refused or failed (401/403/429/5xx) -- the answer is unknown, never an empty frame.
        ValueError: PFF rejected the parameters (400 ``invalid_parameter`` / 422).

    Example:
        Quick start::

            from sportsdataverse.nfl import pff_api_whoami
            pff_api_whoami()

        See Also:
            * `PFF Developer API`_ - reference + CLI guide
            * `nflverse`_ - NFL data ecosystem

        .. _PFF Developer API: https://developer.pff.com
        .. _nflverse: https://nflverse.nflverse.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {}
    _params.update(_caller_params)
    raw = _get(
        "https://api.pff.com/v1/auth/whoami",
        params=_params,
        headers=headers,
        **kwargs,
    )
    return raw


def pff_api_position_report(
    league: str,
    report: str,
    season: Optional[int] = None,
    week_group: Optional[str] = None,
    week: Optional[int] = None,
    week_to: Optional[int] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """One of nineteen player reports for the whole league, one row per player

    Endpoint: ``GET https://api.pff.com/v2/{league}/positions/reports/{report}``
    Example URL: https://api.pff.com/v2/nfl/positions/reports/offense?season=2022

    Args:
        league: League slug: nfl or ncaa.
        report: Which report: offense, passing, passing-depth, passing-pressure, receiving, receiving-depth, rushing, blocking, pass-blocking, run-blocking, defense, run-defense, pass-rush, coverage, special-teams, kick-returns, field-goals, punting, kickoffs — the same vocabulary as team-report.
        season: Season, as a four-digit year: 2006 or later, and at most one year past the current season.
        week_group: Which part of the season to cover: REG (regular season), PO (playoffs) or REGPO (both, the default).
        week: Narrow the report to one week of the weekGroup — or, with weekTo, to a span of weeks.
        week_to: The last week of a span that starts at week; requires week.
        headers: optional headers dict reused across calls; an ``Authorization: Bearer <key>`` here wins over ``api_key=`` (a keyword accepted by every wrapper) and the ``PFF_API_KEY`` / ``SDV_PY_PFF_API_KEY`` environment. With no key anywhere the call raises RuntimeError -- there is no anonymous access.
        return_parsed: parse the payload through parse_pff_v2_table -> polars DataFrame (default True). Pass return_parsed=False for The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``..
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``. when ``return_parsed=False``.

    Raises:
        RuntimeError: No PFF API key (pass ``api_key=`` or set ``PFF_API_KEY``).
        NoDataError: PFF answered 404 (no such route, report or team).
        AssetFetchError: The request was refused or failed (401/403/429/5xx) -- the answer is unknown, never an empty frame.
        ValueError: PFF rejected the parameters (400 ``invalid_parameter`` / 422).

    Example:
        Quick start::

            from sportsdataverse.nfl import pff_api_position_report
            pff_api_position_report(league='nfl', report='offense', season=2022)

        See Also:
            * `PFF Developer API`_ - reference + CLI guide
            * `nflverse`_ - NFL data ecosystem

        .. _PFF Developer API: https://developer.pff.com
        .. _nflverse: https://nflverse.nflverse.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "season": season,
        "weekGroup": week_group,
        "week": week,
        "weekTo": week_to,
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://api.pff.com/v2/{league}/positions/reports/{report}",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_pff_v2_table(raw, return_as_pandas=return_as_pandas)
    return raw


def pff_api_team_directory(
    league: str,
    season: Optional[int] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """The league's teams for a season, with ids, slugs, colours and groups

    Endpoint: ``GET https://api.pff.com/v2/{league}/teams``
    Example URL: https://api.pff.com/v2/nfl/teams?season=2022

    Args:
        league: League slug: nfl or ncaa.
        season: Season, as a four-digit year: 2006 or later, and at most one year past the current season.
        headers: optional headers dict reused across calls; an ``Authorization: Bearer <key>`` here wins over ``api_key=`` (a keyword accepted by every wrapper) and the ``PFF_API_KEY`` / ``SDV_PY_PFF_API_KEY`` environment. With no key anywhere the call raises RuntimeError -- there is no anonymous access.
        return_parsed: parse the payload through parse_pff_v2_table -> polars DataFrame (default True). Pass return_parsed=False for The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``..
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``. when ``return_parsed=False``.

    Raises:
        RuntimeError: No PFF API key (pass ``api_key=`` or set ``PFF_API_KEY``).
        NoDataError: PFF answered 404 (no such route, report or team).
        AssetFetchError: The request was refused or failed (401/403/429/5xx) -- the answer is unknown, never an empty frame.
        ValueError: PFF rejected the parameters (400 ``invalid_parameter`` / 422).

    Example:
        Quick start::

            from sportsdataverse.nfl import pff_api_team_directory
            pff_api_team_directory(league='nfl', season=2022)

        See Also:
            * `PFF Developer API`_ - reference + CLI guide
            * `nflverse`_ - NFL data ecosystem

        .. _PFF Developer API: https://developer.pff.com
        .. _nflverse: https://nflverse.nflverse.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "season": season,
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://api.pff.com/v2/{league}/teams",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_pff_v2_table(raw, return_as_pandas=return_as_pandas)
    return raw


def pff_api_team_stats(
    league: str,
    season: Optional[int] = None,
    week_group: Optional[str] = None,
    week_ids: Optional[str] = None,
    category: Optional[str] = None,
    scope: Optional[str] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Team stats table for one category, every value ranked against the scope

    Endpoint: ``GET https://api.pff.com/v2/{league}/teams/stats``
    Example URL: https://api.pff.com/v2/nfl/teams/stats?season=2022

    Args:
        league: League slug: nfl or ncaa.
        season: Season, as a four-digit year: 2006 or later, and at most one year past the current season.
        week_group: Which part of the season to cover: REG (regular season), PO (playoffs) or REGPO (both, the default).
        week_ids: Comma-separated week ids to cover instead of a whole weekGroup — 1,2,3 is the first three regular-season weeks.
        category: Which stat category the table covers.
        scope: Which teams the ranks are computed against — and which rows come back: league (every team, the default), a conference (afc, nfc) or a division (afc-east … nfc-west).
        headers: optional headers dict reused across calls; an ``Authorization: Bearer <key>`` here wins over ``api_key=`` (a keyword accepted by every wrapper) and the ``PFF_API_KEY`` / ``SDV_PY_PFF_API_KEY`` environment. With no key anywhere the call raises RuntimeError -- there is no anonymous access.
        return_parsed: parse the payload through parse_pff_v2_table -> polars DataFrame (default True). Pass return_parsed=False for The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``..
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``. when ``return_parsed=False``.

    Raises:
        RuntimeError: No PFF API key (pass ``api_key=`` or set ``PFF_API_KEY``).
        NoDataError: PFF answered 404 (no such route, report or team).
        AssetFetchError: The request was refused or failed (401/403/429/5xx) -- the answer is unknown, never an empty frame.
        ValueError: PFF rejected the parameters (400 ``invalid_parameter`` / 422).

    Example:
        Quick start::

            from sportsdataverse.nfl import pff_api_team_stats
            pff_api_team_stats(league='nfl', season=2022)

        See Also:
            * `PFF Developer API`_ - reference + CLI guide
            * `nflverse`_ - NFL data ecosystem

        .. _PFF Developer API: https://developer.pff.com
        .. _nflverse: https://nflverse.nflverse.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "season": season,
        "weekGroup": week_group,
        "weekIds": week_ids,
        "category": category,
        "scope": scope,
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://api.pff.com/v2/{league}/teams/stats",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_pff_v2_table(raw, return_as_pandas=return_as_pandas)
    return raw


def pff_api_team_roster(
    league: str,
    team: str,
    season: Optional[int] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """A team's depth-chart roster with grades, ranks and snap counts

    Endpoint: ``GET https://api.pff.com/v2/{league}/teams/{team}/roster``
    Example URL: https://api.pff.com/v2/nfl/teams/cincinnati-bengals/roster?season=2022

    Args:
        league: League slug: nfl or ncaa.
        team: The team, as its slug (los-angeles-rams) or its numeric franchise id (26) — both resolve through the league's team directory for the season, and both produce the same answer.
        season: Season, as a four-digit year: 2006 or later, and at most one year past the current season.
        headers: optional headers dict reused across calls; an ``Authorization: Bearer <key>`` here wins over ``api_key=`` (a keyword accepted by every wrapper) and the ``PFF_API_KEY`` / ``SDV_PY_PFF_API_KEY`` environment. With no key anywhere the call raises RuntimeError -- there is no anonymous access.
        return_parsed: parse the payload through parse_pff_v2_table -> polars DataFrame (default True). Pass return_parsed=False for The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``..
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``. when ``return_parsed=False``.

    Raises:
        RuntimeError: No PFF API key (pass ``api_key=`` or set ``PFF_API_KEY``).
        NoDataError: PFF answered 404 (no such route, report or team).
        AssetFetchError: The request was refused or failed (401/403/429/5xx) -- the answer is unknown, never an empty frame.
        ValueError: PFF rejected the parameters (400 ``invalid_parameter`` / 422).

    Example:
        Quick start::

            from sportsdataverse.nfl import pff_api_team_roster
            pff_api_team_roster(league='nfl', season=2022, team='cincinnati-bengals')

        See Also:
            * `PFF Developer API`_ - reference + CLI guide
            * `nflverse`_ - NFL data ecosystem

        .. _PFF Developer API: https://developer.pff.com
        .. _nflverse: https://nflverse.nflverse.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "season": season,
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://api.pff.com/v2/{league}/teams/{team}/roster",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_pff_v2_table(raw, return_as_pandas=return_as_pandas)
    return raw


def pff_api_team_schedule(
    league: str,
    team: str,
    season: Optional[int] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """A team's season schedule, with results and strength of schedule

    Endpoint: ``GET https://api.pff.com/v2/{league}/teams/{team}/schedule``
    Example URL: https://api.pff.com/v2/nfl/teams/cincinnati-bengals/schedule?season=2022

    Args:
        league: League slug: nfl or ncaa.
        team: The team, as its slug (los-angeles-rams) or its numeric franchise id (26) — both resolve through the league's team directory for the season, and both produce the same answer.
        season: Season, as a four-digit year: 2006 or later, and at most one year past the current season.
        headers: optional headers dict reused across calls; an ``Authorization: Bearer <key>`` here wins over ``api_key=`` (a keyword accepted by every wrapper) and the ``PFF_API_KEY`` / ``SDV_PY_PFF_API_KEY`` environment. With no key anywhere the call raises RuntimeError -- there is no anonymous access.
        return_parsed: parse the payload through parse_pff_v2_table -> polars DataFrame (default True). Pass return_parsed=False for The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``..
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``. when ``return_parsed=False``.

    Raises:
        RuntimeError: No PFF API key (pass ``api_key=`` or set ``PFF_API_KEY``).
        NoDataError: PFF answered 404 (no such route, report or team).
        AssetFetchError: The request was refused or failed (401/403/429/5xx) -- the answer is unknown, never an empty frame.
        ValueError: PFF rejected the parameters (400 ``invalid_parameter`` / 422).

    Example:
        Quick start::

            from sportsdataverse.nfl import pff_api_team_schedule
            pff_api_team_schedule(league='nfl', season=2022, team='cincinnati-bengals')

        See Also:
            * `PFF Developer API`_ - reference + CLI guide
            * `nflverse`_ - NFL data ecosystem

        .. _PFF Developer API: https://developer.pff.com
        .. _nflverse: https://nflverse.nflverse.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "season": season,
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://api.pff.com/v2/{league}/teams/{team}/schedule",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_pff_v2_table(raw, return_as_pandas=return_as_pandas)
    return raw


def pff_api_team_leaders(
    league: str,
    team: str,
    season: Optional[int] = None,
    week_group: Optional[str] = None,
    group: Optional[str] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """A team's leaders for one position group, with rank and percentile

    Endpoint: ``GET https://api.pff.com/v2/{league}/teams/{team}/leaders``
    Example URL: https://api.pff.com/v2/nfl/teams/cincinnati-bengals/leaders?season=2022

    Args:
        league: League slug: nfl or ncaa.
        team: The team, as its slug (los-angeles-rams) or its numeric franchise id (26) — both resolve through the league's team directory for the season, and both produce the same answer.
        season: Season, as a four-digit year: 2006 or later, and at most one year past the current season.
        week_group: Which part of the season to cover: REG (regular season), PO (playoffs) or REGPO (both, the default).
        group: Which position group the leaders come from — receiving (the default), passing, rushing or defense.
        headers: optional headers dict reused across calls; an ``Authorization: Bearer <key>`` here wins over ``api_key=`` (a keyword accepted by every wrapper) and the ``PFF_API_KEY`` / ``SDV_PY_PFF_API_KEY`` environment. With no key anywhere the call raises RuntimeError -- there is no anonymous access.
        return_parsed: parse the payload through parse_pff_v2_table -> polars DataFrame (default True). Pass return_parsed=False for The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``..
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``. when ``return_parsed=False``.

    Raises:
        RuntimeError: No PFF API key (pass ``api_key=`` or set ``PFF_API_KEY``).
        NoDataError: PFF answered 404 (no such route, report or team).
        AssetFetchError: The request was refused or failed (401/403/429/5xx) -- the answer is unknown, never an empty frame.
        ValueError: PFF rejected the parameters (400 ``invalid_parameter`` / 422).

    Example:
        Quick start::

            from sportsdataverse.nfl import pff_api_team_leaders
            pff_api_team_leaders(league='nfl', season=2022, team='cincinnati-bengals')

        See Also:
            * `PFF Developer API`_ - reference + CLI guide
            * `nflverse`_ - NFL data ecosystem

        .. _PFF Developer API: https://developer.pff.com
        .. _nflverse: https://nflverse.nflverse.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "season": season,
        "weekGroup": week_group,
        "group": group,
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://api.pff.com/v2/{league}/teams/{team}/leaders",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_pff_v2_table(raw, return_as_pandas=return_as_pandas)
    return raw


def pff_api_team_rushing_direction(
    league: str,
    team: str,
    season: Optional[int] = None,
    week_group: Optional[str] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """A team's rushing by direction, one row per rusher and gap, plus totals

    Endpoint: ``GET https://api.pff.com/v2/{league}/teams/{team}/reports/rushing-direction``
    Example URL: https://api.pff.com/v2/nfl/teams/cincinnati-bengals/reports/rushing-direction?season=2022

    Args:
        league: League slug: nfl or ncaa.
        team: The team, as its slug (los-angeles-rams) or its numeric franchise id (26) — both resolve through the league's team directory for the season, and both produce the same answer.
        season: Season, as a four-digit year: 2006 or later, and at most one year past the current season.
        week_group: Which part of the season to cover: REG (regular season), PO (playoffs) or REGPO (both, the default).
        headers: optional headers dict reused across calls; an ``Authorization: Bearer <key>`` here wins over ``api_key=`` (a keyword accepted by every wrapper) and the ``PFF_API_KEY`` / ``SDV_PY_PFF_API_KEY`` environment. With no key anywhere the call raises RuntimeError -- there is no anonymous access.
        return_parsed: parse the payload through parse_pff_v2_table -> polars DataFrame (default True). Pass return_parsed=False for The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``..
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``. when ``return_parsed=False``.

    Raises:
        RuntimeError: No PFF API key (pass ``api_key=`` or set ``PFF_API_KEY``).
        NoDataError: PFF answered 404 (no such route, report or team).
        AssetFetchError: The request was refused or failed (401/403/429/5xx) -- the answer is unknown, never an empty frame.
        ValueError: PFF rejected the parameters (400 ``invalid_parameter`` / 422).

    Example:
        Quick start::

            from sportsdataverse.nfl import pff_api_team_rushing_direction
            pff_api_team_rushing_direction(league='nfl', season=2022, team='cincinnati-bengals')

        See Also:
            * `PFF Developer API`_ - reference + CLI guide
            * `nflverse`_ - NFL data ecosystem

        .. _PFF Developer API: https://developer.pff.com
        .. _nflverse: https://nflverse.nflverse.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "season": season,
        "weekGroup": week_group,
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://api.pff.com/v2/{league}/teams/{team}/reports/rushing-direction",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_pff_v2_table(raw, return_as_pandas=return_as_pandas)
    return raw


def pff_api_team_report(
    league: str,
    team: str,
    report: str,
    season: Optional[int] = None,
    week_group: Optional[str] = None,
    week: Optional[int] = None,
    week_to: Optional[int] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """One of nineteen player reports for a team, one row per player

    Endpoint: ``GET https://api.pff.com/v2/{league}/teams/{team}/reports/{report}``
    Example URL: https://api.pff.com/v2/nfl/teams/cincinnati-bengals/reports/offense?season=2022

    Args:
        league: League slug: nfl or ncaa.
        team: The team, as its slug (los-angeles-rams) or its numeric franchise id (26) — both resolve through the league's team directory for the season, and both produce the same answer.
        report: Which report: offense, passing, passing-depth, passing-pressure, receiving, receiving-depth, rushing, blocking, pass-blocking, run-blocking, defense, run-defense, pass-rush, coverage, special-teams, kick-returns, field-goals, punting, kickoffs.
        season: Season, as a four-digit year: 2006 or later, and at most one year past the current season.
        week_group: Which part of the season to cover: REG (regular season), PO (playoffs) or REGPO (both, the default).
        week: Narrow the report to one week of the weekGroup — or, with weekTo, to a span of weeks.
        week_to: The last week of a span that starts at week; requires week.
        headers: optional headers dict reused across calls; an ``Authorization: Bearer <key>`` here wins over ``api_key=`` (a keyword accepted by every wrapper) and the ``PFF_API_KEY`` / ``SDV_PY_PFF_API_KEY`` environment. With no key anywhere the call raises RuntimeError -- there is no anonymous access.
        return_parsed: parse the payload through parse_pff_v2_table -> polars DataFrame (default True). Pass return_parsed=False for The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``..
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; The decoded JSON body. /v1 routes return the Premium Stats envelope (``{report_slug: rows}``); /v2 routes return ``{..meta.., columns, rows}``. when ``return_parsed=False``.

    Raises:
        RuntimeError: No PFF API key (pass ``api_key=`` or set ``PFF_API_KEY``).
        NoDataError: PFF answered 404 (no such route, report or team).
        AssetFetchError: The request was refused or failed (401/403/429/5xx) -- the answer is unknown, never an empty frame.
        ValueError: PFF rejected the parameters (400 ``invalid_parameter`` / 422).

    Example:
        Quick start::

            from sportsdataverse.nfl import pff_api_team_report
            pff_api_team_report(league='nfl', report='offense', season=2022, team='cincinnati-bengals')

        See Also:
            * `PFF Developer API`_ - reference + CLI guide
            * `nflverse`_ - NFL data ecosystem

        .. _PFF Developer API: https://developer.pff.com
        .. _nflverse: https://nflverse.nflverse.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "season": season,
        "weekGroup": week_group,
        "week": week,
        "weekTo": week_to,
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://api.pff.com/v2/{league}/teams/{team}/reports/{report}",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_pff_v2_table(raw, return_as_pandas=return_as_pandas)
    return raw
