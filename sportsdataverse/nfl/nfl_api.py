# GENERATED by tools/codegen/generate.py -- DO NOT EDIT.
# Source: tools/codegen/endpoints/nfl_api.yaml
"""nfl_api wrappers (host https://api.nfl.com). Generated."""

from __future__ import annotations

from typing import TYPE_CHECKING, Dict, List, Optional, Union  # noqa: F401

from sportsdataverse.nfl.nfl_api_runtime import _get, _bool_str
from sportsdataverse.nfl.nfl_api_parsers import (
    parse_nfl_combine_profiles,
    parse_nfl_draft_picks,
    parse_nfl_game_summaries,
    parse_nfl_injuries,
    parse_nfl_rosters,
    parse_nfl_standings,
    parse_nfl_team,
    parse_nfl_teams_history,
    parse_nfl_weekly_game_details,
    parse_nfl_weeks,
    parse_nfl_weeks_by_date,
)

if TYPE_CHECKING:  # pragma: no cover -- annotation-only imports (PEP 563 defers eval)
    import pandas as pd
    import polars as pl

__all__ = [
    "nfl_standings",
    "nfl_rosters",
    "nfl_teams_history",
    "nfl_team",
    "nfl_weeks",
    "nfl_weeks_by_date",
    "nfl_combine_profiles",
    "nfl_draft_picks",
    "nfl_injuries",
    "nfl_game_summaries",
    "nfl_weekly_game_details",
]


def nfl_standings(
    season: Optional[int] = 2024,
    season_type: Optional[str] = "REG",
    week: Optional[int] = 1,
    limit: Optional[int] = 40,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /football/v2/standings — one row per team standing across the returned week(s).

    Endpoint: ``GET https://api.nfl.com/football/v2/standings``
    Example URL: https://api.nfl.com/football/v2/standings?season=2024&seasonType=REG&week=18

    Args:
        season: season query parameter.
        season_type: seasonType query parameter.
        week: week query parameter.
        limit: limit query parameter.
        headers: optional pre-minted auth headers dict (e.g. from nfl_headers_gen()) to reuse across calls; a fresh anonymous token is minted when omitted.
        return_parsed: parse the payload through parse_nfl_standings -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            nfl_standings(season=2024, season_type='REG', week=18)
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "season": season,
        "seasonType": season_type,
        "week": week,
        "limit": limit,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.nfl.com/football/v2/standings",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_nfl_standings(raw, return_as_pandas=return_as_pandas)
    return raw


def nfl_rosters(
    season: Optional[int] = 2024,
    limit: Optional[int] = 40,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /football/v2/rosters — one row per team roster for the season.

    Endpoint: ``GET https://api.nfl.com/football/v2/rosters``
    Example URL: https://api.nfl.com/football/v2/rosters?season=2024

    Args:
        season: season query parameter.
        limit: limit query parameter.
        headers: optional pre-minted auth headers dict (e.g. from nfl_headers_gen()) to reuse across calls; a fresh anonymous token is minted when omitted.
        return_parsed: parse the payload through parse_nfl_rosters -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            nfl_rosters(season=2024)
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "season": season,
        "limit": limit,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.nfl.com/football/v2/rosters",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_nfl_rosters(raw, return_as_pandas=return_as_pandas)
    return raw


def nfl_teams_history(
    season: Optional[int] = 2024,
    limit: Optional[int] = 40,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /football/v2/teams/history — one row per team for a season.

    Endpoint: ``GET https://api.nfl.com/football/v2/teams/history``
    Example URL: https://api.nfl.com/football/v2/teams/history?season=2024

    Args:
        season: season query parameter.
        limit: limit query parameter.
        headers: optional pre-minted auth headers dict (e.g. from nfl_headers_gen()) to reuse across calls; a fresh anonymous token is minted when omitted.
        return_parsed: parse the payload through parse_nfl_teams_history -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            nfl_teams_history(season=2024)
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "season": season,
        "limit": limit,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.nfl.com/football/v2/teams/history",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_nfl_teams_history(raw, return_as_pandas=return_as_pandas)
    return raw


def nfl_team(
    team_id: str,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /football/v2/teams/{team_id} — single-team detail (one row).

    Endpoint: ``GET https://api.nfl.com/football/v2/teams/{team_id}``
    Example URL: https://api.nfl.com/football/v2/teams/10403800-517c-7b8c-65a3-c61b95d86123

    Args:
        team_id: team_id path parameter.
        headers: optional pre-minted auth headers dict (e.g. from nfl_headers_gen()) to reuse across calls; a fresh anonymous token is minted when omitted.
        return_parsed: parse the payload through parse_nfl_team -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            nfl_team(team_id='10403800-517c-7b8c-65a3-c61b95d86123')
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {}
    _params.update(_caller_params)
    raw = _get(
        f"https://api.nfl.com/football/v2/teams/{team_id}",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_nfl_team(raw, return_as_pandas=return_as_pandas)
    return raw


def nfl_weeks(
    season: int = 2024,
    season_type: str = "REG",
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /football/v2/weeks/season/{season}/seasonType/{season_type} — week calendar (one row per week).

    Endpoint: ``GET https://api.nfl.com/football/v2/weeks/season/{season}/seasonType/{season_type}``
    Example URL: https://api.nfl.com/football/v2/weeks/season/2024/seasonType/REG

    Args:
        season: season path parameter.
        season_type: season_type path parameter.
        headers: optional pre-minted auth headers dict (e.g. from nfl_headers_gen()) to reuse across calls; a fresh anonymous token is minted when omitted.
        return_parsed: parse the payload through parse_nfl_weeks -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            nfl_weeks(season=2024, season_type='REG')
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {}
    _params.update(_caller_params)
    raw = _get(
        f"https://api.nfl.com/football/v2/weeks/season/{season}/seasonType/{season_type}",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_nfl_weeks(raw, return_as_pandas=return_as_pandas)
    return raw


def nfl_weeks_by_date(
    date: str,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /football/v2/weeks/date/{YYYY-MM-DD} — the week containing a date (one row).

    Endpoint: ``GET https://api.nfl.com/football/v2/weeks/date/{date}``
    Example URL: https://api.nfl.com/football/v2/weeks/date/2024-09-08

    Args:
        date: date path parameter.
        headers: optional pre-minted auth headers dict (e.g. from nfl_headers_gen()) to reuse across calls; a fresh anonymous token is minted when omitted.
        return_parsed: parse the payload through parse_nfl_weeks_by_date -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            nfl_weeks_by_date(date='2024-09-08')
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {}
    _params.update(_caller_params)
    raw = _get(
        f"https://api.nfl.com/football/v2/weeks/date/{date}",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_nfl_weeks_by_date(raw, return_as_pandas=return_as_pandas)
    return raw


def nfl_combine_profiles(
    year: Optional[int] = 2024,
    limit: Optional[int] = 40,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /football/v2/combine/profiles — one row per combine prospect.

    Endpoint: ``GET https://api.nfl.com/football/v2/combine/profiles``
    Example URL: https://api.nfl.com/football/v2/combine/profiles?year=2024

    Args:
        year: year query parameter.
        limit: limit query parameter.
        headers: optional pre-minted auth headers dict (e.g. from nfl_headers_gen()) to reuse across calls; a fresh anonymous token is minted when omitted.
        return_parsed: parse the payload through parse_nfl_combine_profiles -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            nfl_combine_profiles(year=2024)
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "year": year,
        "limit": limit,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.nfl.com/football/v2/combine/profiles",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_nfl_combine_profiles(raw, return_as_pandas=return_as_pandas)
    return raw


def nfl_draft_picks(
    year: Optional[int] = 2024,
    limit: Optional[int] = 40,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /football/v2/draft/picks/report — one row per draft pick.

    Endpoint: ``GET https://api.nfl.com/football/v2/draft/picks/report``
    Example URL: https://api.nfl.com/football/v2/draft/picks/report?year=2024

    Args:
        year: year query parameter.
        limit: limit query parameter.
        headers: optional pre-minted auth headers dict (e.g. from nfl_headers_gen()) to reuse across calls; a fresh anonymous token is minted when omitted.
        return_parsed: parse the payload through parse_nfl_draft_picks -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            nfl_draft_picks(year=2024)
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "year": year,
        "limit": limit,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.nfl.com/football/v2/draft/picks/report",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_nfl_draft_picks(raw, return_as_pandas=return_as_pandas)
    return raw


def nfl_injuries(
    season: Optional[int] = 2024,
    season_type: Optional[str] = "REG",
    week: Optional[int] = 1,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /football/v2/injuries — one row per injured player.

    Endpoint: ``GET https://api.nfl.com/football/v2/injuries``
    Example URL: https://api.nfl.com/football/v2/injuries?season=2024&seasonType=REG&week=1

    Args:
        season: season query parameter.
        season_type: seasonType query parameter.
        week: week query parameter.
        headers: optional pre-minted auth headers dict (e.g. from nfl_headers_gen()) to reuse across calls; a fresh anonymous token is minted when omitted.
        return_parsed: parse the payload through parse_nfl_injuries -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            nfl_injuries(season=2024, season_type='REG', week=1)
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "season": season,
        "seasonType": season_type,
        "week": week,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.nfl.com/football/v2/injuries",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_nfl_injuries(raw, return_as_pandas=return_as_pandas)
    return raw


def nfl_game_summaries(
    season: Optional[int] = 2024,
    season_type: Optional[str] = "REG",
    week: Optional[int] = 1,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /football/v2/stats/live/game-summaries — one row per game (live state).

    Endpoint: ``GET https://api.nfl.com/football/v2/stats/live/game-summaries``
    Example URL: https://api.nfl.com/football/v2/stats/live/game-summaries?season=2024&seasonType=REG&week=1

    Args:
        season: season query parameter.
        season_type: seasonType query parameter.
        week: week query parameter.
        headers: optional pre-minted auth headers dict (e.g. from nfl_headers_gen()) to reuse across calls; a fresh anonymous token is minted when omitted.
        return_parsed: parse the payload through parse_nfl_game_summaries -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            nfl_game_summaries(season=2024, season_type='REG', week=1)
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "season": season,
        "seasonType": season_type,
        "week": week,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.nfl.com/football/v2/stats/live/game-summaries",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_nfl_game_summaries(raw, return_as_pandas=return_as_pandas)
    return raw


def nfl_weekly_game_details(
    season: Optional[int] = 2024,
    season_type: Optional[str] = "REG",
    week: Optional[int] = 1,
    include_drive_chart: Optional[bool] = True,
    include_replays: Optional[bool] = False,
    include_standings: Optional[bool] = False,
    include_tagged_videos: Optional[bool] = False,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /football/v2/experience/weekly-game-details — one row per game (bare list).

    Endpoint: ``GET https://api.nfl.com/football/v2/experience/weekly-game-details``
    Example URL: https://api.nfl.com/football/v2/experience/weekly-game-details?season=2024&type=REG&week=1

    Args:
        season: season query parameter.
        season_type: type query parameter.
        week: week query parameter.
        include_drive_chart: includeDriveChart query parameter.
        include_replays: includeReplays query parameter.
        include_standings: includeStandings query parameter.
        include_tagged_videos: includeTaggedVideos query parameter.
        headers: optional pre-minted auth headers dict (e.g. from nfl_headers_gen()) to reuse across calls; a fresh anonymous token is minted when omitted.
        return_parsed: parse the payload through parse_nfl_weekly_game_details -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            nfl_weekly_game_details(season=2024, season_type='REG', week=1)
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "season": season,
        "type": season_type,
        "week": week,
        "includeDriveChart": _bool_str(include_drive_chart),
        "includeReplays": _bool_str(include_replays),
        "includeStandings": _bool_str(include_standings),
        "includeTaggedVideos": _bool_str(include_tagged_videos),
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.nfl.com/football/v2/experience/weekly-game-details",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_nfl_weekly_game_details(raw, return_as_pandas=return_as_pandas)
    return raw
