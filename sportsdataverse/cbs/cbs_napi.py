# GENERATED by tools/codegen/generate.py -- DO NOT EDIT.
# Source: tools/codegen/endpoints/cbs_napi.yaml
"""cbs_napi wrappers (host https://api.cbssports.com/napi). Generated."""

from __future__ import annotations

from typing import TYPE_CHECKING, Dict, List, Optional, Union  # noqa: F401

from sportsdataverse._codegen_runtime import _get
from sportsdataverse.cbs.cbs_napi_parsers import parse_cbs_napi, parse_cbs_napi_standings

if TYPE_CHECKING:  # pragma: no cover -- annotation-only imports (PEP 563 defers eval)
    import pandas as pd
    import polars as pl

__all__ = [
    "cbs_bulk",
    "cbs_client_config",
    "cbs_coach_rankings",
    "cbs_coach_team_associations",
    "cbs_division_subdivisions",
    "cbs_endpoint_registry",
    "cbs_event",
    "cbs_event_entrants",
    "cbs_event_leaderboard",
    "cbs_event_seasons",
    "cbs_event_venues",
    "cbs_game",
    "cbs_game_betting_splits",
    "cbs_game_boxscore",
    "cbs_game_content_preview",
    "cbs_game_content_recap",
    "cbs_game_content_story",
    "cbs_game_featured",
    "cbs_game_lineup",
    "cbs_game_odds",
    "cbs_game_odds_hq",
    "cbs_game_outcomes",
    "cbs_game_probable_players",
    "cbs_game_props",
    "cbs_game_rtwp",
    "cbs_game_ruwt_highlights",
    "cbs_game_scoring_boxscores",
    "cbs_game_scoring_drives",
    "cbs_game_scoring_leaders",
    "cbs_game_scoring_player_stats",
    "cbs_game_scoring_plays",
    "cbs_game_scoring_rosters",
    "cbs_game_scoring_scoreboard",
    "cbs_game_scoring_scores",
    "cbs_game_scoring_team_stats",
    "cbs_game_scoring_winprob",
    "cbs_game_scoring_ytd_player_stats",
    "cbs_game_scoring_ytd_team_stats",
    "cbs_game_ticket",
    "cbs_game_weather",
    "cbs_golf_event_markets",
    "cbs_golf_player_markets",
    "cbs_golfer_results",
    "cbs_league",
    "cbs_league_teams",
    "cbs_odds",
    "cbs_player",
    "cbs_player_combine_data",
    "cbs_player_depth_charts",
    "cbs_player_draft_info",
    "cbs_player_encyclopedia",
    "cbs_player_futures",
    "cbs_player_game_stats",
    "cbs_player_hockey_meta",
    "cbs_player_injuries",
    "cbs_player_meta_baseball",
    "cbs_player_meta_golf",
    "cbs_player_outlook",
    "cbs_player_position_rankings",
    "cbs_player_rankings",
    "cbs_player_recruit_associations",
    "cbs_player_standings",
    "cbs_player_stats",
    "cbs_player_team_associations",
    "cbs_player_transactions",
    "cbs_recruit_rankings",
    "cbs_season",
    "cbs_season_teams",
    "cbs_sport",
    "cbs_sport_leagues",
    "cbs_team_futures",
    "cbs_team_metadata",
    "cbs_team_players",
    "cbs_team_polls",
    "cbs_team_rankings",
    "cbs_team_rankings_sportsline",
    "cbs_team_seasons",
    "cbs_team_standings",
    "cbs_team_standings_sportsline",
    "cbs_team_stats",
    "cbs_venue",
    "cbs_venue_metadata",
]


def cbs_bulk(
    player_resource: Optional[str] = None,
    team_resource: Optional[str] = None,
    game_resource: Optional[str] = None,
    venue_resource: Optional[str] = None,
    event_resource: Optional[str] = None,
    featured_game_resource: Optional[str] = None,
    golf_event_markets_resource: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Resolve resources in bulk to save HTTP traffic.

    Endpoint: ``GET https://api.cbssports.com/napi/resource/bulk``
    Example URL: https://api.cbssports.com/napi/resource/bulk

    Args:
        player_resource: CSV list of player IDs to retrieve.
        team_resource: CSV list of team IDs to retrieve.
        game_resource: CSV list of game IDs to retrieve.
        venue_resource: CSV list of venue IDs to retrieve
        event_resource: CSV list of event IDs to retrieve
        featured_game_resource: CSV list of game IDs to retrieve.
        golf_event_markets_resource: CSV list of golf event markets IDs to retrieve
        return_parsed: parse the payload through parse_cbs_napi -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries. An unknown id is NOT an exception -- NAPI answers it with HTTP 200 plus an ``{error|errors|warnings}`` envelope, which parses to a zero-row frame.

    Example:
        Quick start::

            from sportsdataverse.cbs import cbs_bulk
            cbs_bulk()

        See Also:
            * `CBS Sports NAPI registry`_ - the API's own self-documenting endpoint catalog (data origin)
            * `nflfastR`_ - R sister package; NAPI is cross-sport, this is its NFL counterpart

        .. _CBS Sports NAPI registry: https://api.cbssports.com/napi/resource/endpoint/registry
        .. _nflfastR: https://www.nflfastr.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "PlayerResource": player_resource,
        "TeamResource": team_resource,
        "GameResource": game_resource,
        "VenueResource": venue_resource,
        "EventResource": event_resource,
        "FeaturedGameResource": featured_game_resource,
        "GolfEventMarketsResource": golf_event_markets_resource,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.cbssports.com/napi/resource/bulk",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_cbs_napi(raw, return_as_pandas=return_as_pandas)
    return raw


def cbs_client_config(
    client_name: str,
    resources: Optional[str] = None,
    league_id: Optional[str] = None,
    classifier: Optional[str] = None,
    key_name: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Get configuration for how a client should access our APIs, or any additional settings they want supplied to them.

    Endpoint: ``GET https://api.cbssports.com/napi/resource/client/config/{client_name}``
    Example URL: https://api.cbssports.com/napi/resource/client/config/cbs

    Args:
        client_name: Client name as it appears in the database
        resources: Allowed: league, season.
        league_id: View option.  Filter by leagueId.
        classifier: View option.  Filter by a certain classifier.
        key_name: View option.  Filter by a custom key name.
        return_parsed: parse the payload through parse_cbs_napi -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries. An unknown id is NOT an exception -- NAPI answers it with HTTP 200 plus an ``{error|errors|warnings}`` envelope, which parses to a zero-row frame.

    Example:
        Quick start::

            from sportsdataverse.cbs import cbs_client_config
            cbs_client_config(client_name='cbs')

        See Also:
            * `CBS Sports NAPI registry`_ - the API's own self-documenting endpoint catalog (data origin)
            * `nflfastR`_ - R sister package; NAPI is cross-sport, this is its NFL counterpart

        .. _CBS Sports NAPI registry: https://api.cbssports.com/napi/resource/endpoint/registry
        .. _nflfastR: https://www.nflfastr.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "resources": resources,
        "leagueId": league_id,
        "classifier": classifier,
        "keyName": key_name,
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://api.cbssports.com/napi/resource/client/config/{client_name}",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_cbs_napi(raw, return_as_pandas=return_as_pandas)
    return raw


def cbs_coach_rankings(
    coach_id: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Get rankings resource for a coach.

    Endpoint: ``GET https://api.cbssports.com/napi/resource/coach/rankings/{coach_id}``
    Example URL: https://api.cbssports.com/napi/resource/coach/rankings

    Args:
        coach_id: Numerical player ID
        return_parsed: parse the payload through parse_cbs_napi -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries. An unknown id is NOT an exception -- NAPI answers it with HTTP 200 plus an ``{error|errors|warnings}`` envelope, which parses to a zero-row frame.

    Example:
        Quick start::

            from sportsdataverse.cbs import cbs_coach_rankings
            cbs_coach_rankings()

        See Also:
            * `CBS Sports NAPI registry`_ - the API's own self-documenting endpoint catalog (data origin)
            * `nflfastR`_ - R sister package; NAPI is cross-sport, this is its NFL counterpart

        .. _CBS Sports NAPI registry: https://api.cbssports.com/napi/resource/endpoint/registry
        .. _nflfastR: https://www.nflfastr.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {}
    _params.update(_caller_params)
    raw = _get(
        f"https://api.cbssports.com/napi/resource/coach/rankings/{coach_id}",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_cbs_napi(raw, return_as_pandas=return_as_pandas)
    return raw


def cbs_coach_team_associations(
    coach_id: int,
    resources: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Get team associations for a particular coach.

    Endpoint: ``GET https://api.cbssports.com/napi/resource/coach/teamAssociations/{coach_id}``
    Example URL: https://api.cbssports.com/napi/resource/coach/teamAssociations

    Args:
        coach_id: Numerical player ID
        resources: Specify specific sub-resources to resolve. Defaults to none. Allowed: team.
        return_parsed: parse the payload through parse_cbs_napi -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries. An unknown id is NOT an exception -- NAPI answers it with HTTP 200 plus an ``{error|errors|warnings}`` envelope, which parses to a zero-row frame.

    Example:
        Quick start::

            from sportsdataverse.cbs import cbs_coach_team_associations
            cbs_coach_team_associations()

        See Also:
            * `CBS Sports NAPI registry`_ - the API's own self-documenting endpoint catalog (data origin)
            * `nflfastR`_ - R sister package; NAPI is cross-sport, this is its NFL counterpart

        .. _CBS Sports NAPI registry: https://api.cbssports.com/napi/resource/endpoint/registry
        .. _nflfastR: https://www.nflfastr.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "resources": resources,
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://api.cbssports.com/napi/resource/coach/teamAssociations/{coach_id}",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_cbs_napi(raw, return_as_pandas=return_as_pandas)
    return raw


def cbs_division_subdivisions(
    division_id: int,
    sub_division_id: Optional[int] = None,
    name: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Get subdivisions for a division from Atlas.

    Endpoint: ``GET https://api.cbssports.com/napi/resource/division/subdivisions/{division_id}``
    Example URL: https://api.cbssports.com/napi/resource/division/subdivisions

    Args:
        division_id: Numerical division ID
        sub_division_id: View option for rendering only a certain subdivision
        name: View option for a csv of subdivsion names to render
        return_parsed: parse the payload through parse_cbs_napi -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries. An unknown id is NOT an exception -- NAPI answers it with HTTP 200 plus an ``{error|errors|warnings}`` envelope, which parses to a zero-row frame.

    Example:
        Quick start::

            from sportsdataverse.cbs import cbs_division_subdivisions
            cbs_division_subdivisions()

        See Also:
            * `CBS Sports NAPI registry`_ - the API's own self-documenting endpoint catalog (data origin)
            * `nflfastR`_ - R sister package; NAPI is cross-sport, this is its NFL counterpart

        .. _CBS Sports NAPI registry: https://api.cbssports.com/napi/resource/endpoint/registry
        .. _nflfastR: https://www.nflfastr.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "subDivisionId": sub_division_id,
        "name": name,
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://api.cbssports.com/napi/resource/division/subdivisions/{division_id}",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_cbs_napi(raw, return_as_pandas=return_as_pandas)
    return raw


def cbs_endpoint_registry(
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Get the resource endpoint registry

    Endpoint: ``GET https://api.cbssports.com/napi/resource/endpoint/registry``
    Example URL: https://api.cbssports.com/napi/resource/endpoint/registry

    Args:
        return_parsed: parse the payload through parse_cbs_napi -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries. An unknown id is NOT an exception -- NAPI answers it with HTTP 200 plus an ``{error|errors|warnings}`` envelope, which parses to a zero-row frame.

    Example:
        Quick start::

            from sportsdataverse.cbs import cbs_endpoint_registry
            cbs_endpoint_registry()

        See Also:
            * `CBS Sports NAPI registry`_ - the API's own self-documenting endpoint catalog (data origin)
            * `nflfastR`_ - R sister package; NAPI is cross-sport, this is its NFL counterpart

        .. _CBS Sports NAPI registry: https://api.cbssports.com/napi/resource/endpoint/registry
        .. _nflfastR: https://www.nflfastr.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {}
    _params.update(_caller_params)
    raw = _get(
        "https://api.cbssports.com/napi/resource/endpoint/registry",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_cbs_napi(raw, return_as_pandas=return_as_pandas)
    return raw


def cbs_event(
    event_id: int,
    date_format: Optional[str] = None,
    resources: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Get an event resource

    Endpoint: ``GET https://api.cbssports.com/napi/resource/event/{event_id}``
    Example URL: https://api.cbssports.com/napi/resource/event

    Args:
        event_id: Numerical event ID
        date_format: Optional.  Options here: http://momentjs.com/docs/#/displaying/format/
        resources: Specify specific sub-resources to resolve.  Defaults to none. Allowed: entrants, venues, leaderboard, weather, markets.
        return_parsed: parse the payload through parse_cbs_napi -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries. An unknown id is NOT an exception -- NAPI answers it with HTTP 200 plus an ``{error|errors|warnings}`` envelope, which parses to a zero-row frame.

    Example:
        Quick start::

            from sportsdataverse.cbs import cbs_event
            cbs_event()

        See Also:
            * `CBS Sports NAPI registry`_ - the API's own self-documenting endpoint catalog (data origin)
            * `nflfastR`_ - R sister package; NAPI is cross-sport, this is its NFL counterpart

        .. _CBS Sports NAPI registry: https://api.cbssports.com/napi/resource/endpoint/registry
        .. _nflfastR: https://www.nflfastr.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "dateFormat": date_format,
        "resources": resources,
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://api.cbssports.com/napi/resource/event/{event_id}",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_cbs_napi(raw, return_as_pandas=return_as_pandas)
    return raw


def cbs_event_entrants(
    event_id: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Get players entered in a particular event.

    Endpoint: ``GET https://api.cbssports.com/napi/resource/event/entrants/{event_id}``
    Example URL: https://api.cbssports.com/napi/resource/event/entrants

    Args:
        event_id: Numerical event ID
        return_parsed: parse the payload through parse_cbs_napi -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries. An unknown id is NOT an exception -- NAPI answers it with HTTP 200 plus an ``{error|errors|warnings}`` envelope, which parses to a zero-row frame.

    Example:
        Quick start::

            from sportsdataverse.cbs import cbs_event_entrants
            cbs_event_entrants()

        See Also:
            * `CBS Sports NAPI registry`_ - the API's own self-documenting endpoint catalog (data origin)
            * `nflfastR`_ - R sister package; NAPI is cross-sport, this is its NFL counterpart

        .. _CBS Sports NAPI registry: https://api.cbssports.com/napi/resource/endpoint/registry
        .. _nflfastR: https://www.nflfastr.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {}
    _params.update(_caller_params)
    raw = _get(
        f"https://api.cbssports.com/napi/resource/event/entrants/{event_id}",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_cbs_napi(raw, return_as_pandas=return_as_pandas)
    return raw


def cbs_event_leaderboard(
    event_id: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Get a leaderboard data resource for a particular event.

    Endpoint: ``GET https://api.cbssports.com/napi/resource/event/leaderboard/{event_id}``
    Example URL: https://api.cbssports.com/napi/resource/event/leaderboard

    Args:
        event_id: Numerical event ID
        return_parsed: parse the payload through parse_cbs_napi -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries. An unknown id is NOT an exception -- NAPI answers it with HTTP 200 plus an ``{error|errors|warnings}`` envelope, which parses to a zero-row frame.

    Example:
        Quick start::

            from sportsdataverse.cbs import cbs_event_leaderboard
            cbs_event_leaderboard()

        See Also:
            * `CBS Sports NAPI registry`_ - the API's own self-documenting endpoint catalog (data origin)
            * `nflfastR`_ - R sister package; NAPI is cross-sport, this is its NFL counterpart

        .. _CBS Sports NAPI registry: https://api.cbssports.com/napi/resource/endpoint/registry
        .. _nflfastR: https://www.nflfastr.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {}
    _params.update(_caller_params)
    raw = _get(
        f"https://api.cbssports.com/napi/resource/event/leaderboard/{event_id}",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_cbs_napi(raw, return_as_pandas=return_as_pandas)
    return raw


def cbs_event_seasons(
    event_id: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Get seasons associated to a particular event.

    Endpoint: ``GET https://api.cbssports.com/napi/resource/event/seasons/{event_id}``
    Example URL: https://api.cbssports.com/napi/resource/event/seasons

    Args:
        event_id: Numerical event ID
        return_parsed: parse the payload through parse_cbs_napi -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries. An unknown id is NOT an exception -- NAPI answers it with HTTP 200 plus an ``{error|errors|warnings}`` envelope, which parses to a zero-row frame.

    Example:
        Quick start::

            from sportsdataverse.cbs import cbs_event_seasons
            cbs_event_seasons()

        See Also:
            * `CBS Sports NAPI registry`_ - the API's own self-documenting endpoint catalog (data origin)
            * `nflfastR`_ - R sister package; NAPI is cross-sport, this is its NFL counterpart

        .. _CBS Sports NAPI registry: https://api.cbssports.com/napi/resource/endpoint/registry
        .. _nflfastR: https://www.nflfastr.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {}
    _params.update(_caller_params)
    raw = _get(
        f"https://api.cbssports.com/napi/resource/event/seasons/{event_id}",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_cbs_napi(raw, return_as_pandas=return_as_pandas)
    return raw


def cbs_event_venues(
    event_id: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Get venues associated to a particular event.

    Endpoint: ``GET https://api.cbssports.com/napi/resource/event/venues/{event_id}``
    Example URL: https://api.cbssports.com/napi/resource/event/venues

    Args:
        event_id: Numerical event ID
        return_parsed: parse the payload through parse_cbs_napi -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries. An unknown id is NOT an exception -- NAPI answers it with HTTP 200 plus an ``{error|errors|warnings}`` envelope, which parses to a zero-row frame.

    Example:
        Quick start::

            from sportsdataverse.cbs import cbs_event_venues
            cbs_event_venues()

        See Also:
            * `CBS Sports NAPI registry`_ - the API's own self-documenting endpoint catalog (data origin)
            * `nflfastR`_ - R sister package; NAPI is cross-sport, this is its NFL counterpart

        .. _CBS Sports NAPI registry: https://api.cbssports.com/napi/resource/endpoint/registry
        .. _nflfastR: https://www.nflfastr.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {}
    _params.update(_caller_params)
    raw = _get(
        f"https://api.cbssports.com/napi/resource/event/venues/{event_id}",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_cbs_napi(raw, return_as_pandas=return_as_pandas)
    return raw


def cbs_game(
    game_id: int,
    date_format: Optional[str] = None,
    resources: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Get a game resource

    Endpoint: ``GET https://api.cbssports.com/napi/resource/game/{game_id}``
    Example URL: https://api.cbssports.com/napi/resource/game

    Args:
        game_id: Numerical game ID
        date_format: Optional.  Options here: http://momentjs.com/docs/#/displaying/format/
        resources: Specify specific sub-resources to resolve.  Defaults to none. Allowed: homeTeam, awayTeam, league, lineup, odds, players, standings, conference, division, probablePlayers, player, playerTeamAssociations, injuries, transactions, depthCharts, metaData, boxscore, venue, scoringLeaders, scoringPlayerStats, scoringScoreboard, scoringScores, scoringYtdPlayerStats, scoringYtdTeamStats, scoringRosters, scoringPlays, scoringTeamStats, scoringBoxscores, gameOdds, gameOutcomes, ticket, scoringDrives, scoringWinProb, gameHqOdds, weather, featured, gameProps, bettingSplits, gameRTWP.
        return_parsed: parse the payload through parse_cbs_napi -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries. An unknown id is NOT an exception -- NAPI answers it with HTTP 200 plus an ``{error|errors|warnings}`` envelope, which parses to a zero-row frame.

    Example:
        Quick start::

            from sportsdataverse.cbs import cbs_game
            cbs_game()

        See Also:
            * `CBS Sports NAPI registry`_ - the API's own self-documenting endpoint catalog (data origin)
            * `nflfastR`_ - R sister package; NAPI is cross-sport, this is its NFL counterpart

        .. _CBS Sports NAPI registry: https://api.cbssports.com/napi/resource/endpoint/registry
        .. _nflfastR: https://www.nflfastr.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "dateFormat": date_format,
        "resources": resources,
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://api.cbssports.com/napi/resource/game/{game_id}",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_cbs_napi(raw, return_as_pandas=return_as_pandas)
    return raw


def cbs_game_betting_splits(
    game_id: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Get a BettingSplits resource for a particular game.

    Endpoint: ``GET https://api.cbssports.com/napi/resource/game/bettingSplits/{game_id}``
    Example URL: https://api.cbssports.com/napi/resource/game/bettingSplits

    Args:
        game_id: Numerical game ID
        return_parsed: parse the payload through parse_cbs_napi -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries. An unknown id is NOT an exception -- NAPI answers it with HTTP 200 plus an ``{error|errors|warnings}`` envelope, which parses to a zero-row frame.

    Example:
        Quick start::

            from sportsdataverse.cbs import cbs_game_betting_splits
            cbs_game_betting_splits()

        See Also:
            * `CBS Sports NAPI registry`_ - the API's own self-documenting endpoint catalog (data origin)
            * `nflfastR`_ - R sister package; NAPI is cross-sport, this is its NFL counterpart

        .. _CBS Sports NAPI registry: https://api.cbssports.com/napi/resource/endpoint/registry
        .. _nflfastR: https://www.nflfastr.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {}
    _params.update(_caller_params)
    raw = _get(
        f"https://api.cbssports.com/napi/resource/game/bettingSplits/{game_id}",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_cbs_napi(raw, return_as_pandas=return_as_pandas)
    return raw


def cbs_game_boxscore(
    game_id: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Get boxscore resource

    Endpoint: ``GET https://api.cbssports.com/napi/resource/game/boxscore/{game_id}``
    Example URL: https://api.cbssports.com/napi/resource/game/boxscore

    Args:
        game_id: Numerical game ID
        return_parsed: parse the payload through parse_cbs_napi -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries. An unknown id is NOT an exception -- NAPI answers it with HTTP 200 plus an ``{error|errors|warnings}`` envelope, which parses to a zero-row frame.

    Example:
        Quick start::

            from sportsdataverse.cbs import cbs_game_boxscore
            cbs_game_boxscore()

        See Also:
            * `CBS Sports NAPI registry`_ - the API's own self-documenting endpoint catalog (data origin)
            * `nflfastR`_ - R sister package; NAPI is cross-sport, this is its NFL counterpart

        .. _CBS Sports NAPI registry: https://api.cbssports.com/napi/resource/endpoint/registry
        .. _nflfastR: https://www.nflfastr.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {}
    _params.update(_caller_params)
    raw = _get(
        f"https://api.cbssports.com/napi/resource/game/boxscore/{game_id}",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_cbs_napi(raw, return_as_pandas=return_as_pandas)
    return raw


def cbs_game_content_preview(
    game_id: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Get content for game preview

    Endpoint: ``GET https://api.cbssports.com/napi/resource/game/content/preview/{game_id}``
    Example URL: https://api.cbssports.com/napi/resource/game/content/preview

    Args:
        game_id: Numerical game ID
        return_parsed: parse the payload through parse_cbs_napi -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries. An unknown id is NOT an exception -- NAPI answers it with HTTP 200 plus an ``{error|errors|warnings}`` envelope, which parses to a zero-row frame.

    Example:
        Quick start::

            from sportsdataverse.cbs import cbs_game_content_preview
            cbs_game_content_preview()

        See Also:
            * `CBS Sports NAPI registry`_ - the API's own self-documenting endpoint catalog (data origin)
            * `nflfastR`_ - R sister package; NAPI is cross-sport, this is its NFL counterpart

        .. _CBS Sports NAPI registry: https://api.cbssports.com/napi/resource/endpoint/registry
        .. _nflfastR: https://www.nflfastr.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {}
    _params.update(_caller_params)
    raw = _get(
        f"https://api.cbssports.com/napi/resource/game/content/preview/{game_id}",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_cbs_napi(raw, return_as_pandas=return_as_pandas)
    return raw


def cbs_game_content_recap(
    game_id: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Get content for game recap

    Endpoint: ``GET https://api.cbssports.com/napi/resource/game/content/recap/{game_id}``
    Example URL: https://api.cbssports.com/napi/resource/game/content/recap

    Args:
        game_id: Numerical game ID
        return_parsed: parse the payload through parse_cbs_napi -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries. An unknown id is NOT an exception -- NAPI answers it with HTTP 200 plus an ``{error|errors|warnings}`` envelope, which parses to a zero-row frame.

    Example:
        Quick start::

            from sportsdataverse.cbs import cbs_game_content_recap
            cbs_game_content_recap()

        See Also:
            * `CBS Sports NAPI registry`_ - the API's own self-documenting endpoint catalog (data origin)
            * `nflfastR`_ - R sister package; NAPI is cross-sport, this is its NFL counterpart

        .. _CBS Sports NAPI registry: https://api.cbssports.com/napi/resource/endpoint/registry
        .. _nflfastR: https://www.nflfastr.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {}
    _params.update(_caller_params)
    raw = _get(
        f"https://api.cbssports.com/napi/resource/game/content/recap/{game_id}",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_cbs_napi(raw, return_as_pandas=return_as_pandas)
    return raw


def cbs_game_content_story(
    game_id: int,
    game_ids_story_tags: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Get content for game story

    Endpoint: ``GET https://api.cbssports.com/napi/resource/game/content/story/{game_id}``
    Example URL: https://api.cbssports.com/napi/resource/game/content/story

    Args:
        game_id: Numerical game ID
        game_ids_story_tags: The tags used to retrieve stories
        return_parsed: parse the payload through parse_cbs_napi -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries. An unknown id is NOT an exception -- NAPI answers it with HTTP 200 plus an ``{error|errors|warnings}`` envelope, which parses to a zero-row frame.

    Example:
        Quick start::

            from sportsdataverse.cbs import cbs_game_content_story
            cbs_game_content_story()

        See Also:
            * `CBS Sports NAPI registry`_ - the API's own self-documenting endpoint catalog (data origin)
            * `nflfastR`_ - R sister package; NAPI is cross-sport, this is its NFL counterpart

        .. _CBS Sports NAPI registry: https://api.cbssports.com/napi/resource/endpoint/registry
        .. _nflfastR: https://www.nflfastr.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "gameIdsStoryTags": game_ids_story_tags,
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://api.cbssports.com/napi/resource/game/content/story/{game_id}",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_cbs_napi(raw, return_as_pandas=return_as_pandas)
    return raw


def cbs_game_featured(
    game_id: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Get a FeaturedGame resource.

    Endpoint: ``GET https://api.cbssports.com/napi/resource/game/featured/{game_id}``
    Example URL: https://api.cbssports.com/napi/resource/game/featured

    Args:
        game_id: Numerical game ID
        return_parsed: parse the payload through parse_cbs_napi -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries. An unknown id is NOT an exception -- NAPI answers it with HTTP 200 plus an ``{error|errors|warnings}`` envelope, which parses to a zero-row frame.

    Example:
        Quick start::

            from sportsdataverse.cbs import cbs_game_featured
            cbs_game_featured()

        See Also:
            * `CBS Sports NAPI registry`_ - the API's own self-documenting endpoint catalog (data origin)
            * `nflfastR`_ - R sister package; NAPI is cross-sport, this is its NFL counterpart

        .. _CBS Sports NAPI registry: https://api.cbssports.com/napi/resource/endpoint/registry
        .. _nflfastR: https://www.nflfastr.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {}
    _params.update(_caller_params)
    raw = _get(
        f"https://api.cbssports.com/napi/resource/game/featured/{game_id}",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_cbs_napi(raw, return_as_pandas=return_as_pandas)
    return raw


def cbs_game_lineup(
    game_id: int,
    resources: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Get a lineup resource for a particular game.

    Endpoint: ``GET https://api.cbssports.com/napi/resource/game/lineup/{game_id}``
    Example URL: https://api.cbssports.com/napi/resource/game/lineup

    Args:
        game_id: Numerical game ID
        resources: Specify specific sub-resources to resolve.  Defaults to none. Allowed: playerTeamAssociations, injuries, metaData, playerStats.
        return_parsed: parse the payload through parse_cbs_napi -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries. An unknown id is NOT an exception -- NAPI answers it with HTTP 200 plus an ``{error|errors|warnings}`` envelope, which parses to a zero-row frame.

    Example:
        Quick start::

            from sportsdataverse.cbs import cbs_game_lineup
            cbs_game_lineup()

        See Also:
            * `CBS Sports NAPI registry`_ - the API's own self-documenting endpoint catalog (data origin)
            * `nflfastR`_ - R sister package; NAPI is cross-sport, this is its NFL counterpart

        .. _CBS Sports NAPI registry: https://api.cbssports.com/napi/resource/endpoint/registry
        .. _nflfastR: https://www.nflfastr.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "resources": resources,
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://api.cbssports.com/napi/resource/game/lineup/{game_id}",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_cbs_napi(raw, return_as_pandas=return_as_pandas)
    return raw


def cbs_game_odds(
    game_id: int,
    market_ids: Optional[str] = None,
    book_ids: Optional[str] = None,
    state: Optional[str] = None,
    model: Optional[str] = None,
    show_hidden_odds: Optional[int] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Get an odds resource for a particular game.

    Endpoint: ``GET https://api.cbssports.com/napi/resource/game/odds/{game_id}``
    Example URL: https://api.cbssports.com/napi/resource/game/odds

    Args:
        game_id: Numerical game ID
        market_ids: This value can be used to specify markets, using the marketIds.
        book_ids: This value can be used to specify books, using the bookIds.
        state: This value can be used to specify a state.
        model: This value can be used set the model to be used
        show_hidden_odds: If set to 1, show the odds that has been hidden within the market and/or consensus nodes
        return_parsed: parse the payload through parse_cbs_napi -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries. An unknown id is NOT an exception -- NAPI answers it with HTTP 200 plus an ``{error|errors|warnings}`` envelope, which parses to a zero-row frame.

    Example:
        Quick start::

            from sportsdataverse.cbs import cbs_game_odds
            cbs_game_odds()

        See Also:
            * `CBS Sports NAPI registry`_ - the API's own self-documenting endpoint catalog (data origin)
            * `nflfastR`_ - R sister package; NAPI is cross-sport, this is its NFL counterpart

        .. _CBS Sports NAPI registry: https://api.cbssports.com/napi/resource/endpoint/registry
        .. _nflfastR: https://www.nflfastr.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "marketIds": market_ids,
        "bookIds": book_ids,
        "state": state,
        "model": model,
        "showHiddenOdds": show_hidden_odds,
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://api.cbssports.com/napi/resource/game/odds/{game_id}",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_cbs_napi(raw, return_as_pandas=return_as_pandas)
    return raw


def cbs_game_odds_hq(
    game_id: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Get an HQ odds resource for a particular game.

    Endpoint: ``GET https://api.cbssports.com/napi/resource/game/odds/hq/{game_id}``
    Example URL: https://api.cbssports.com/napi/resource/game/odds/hq

    Args:
        game_id: Numerical game ID
        return_parsed: parse the payload through parse_cbs_napi -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries. An unknown id is NOT an exception -- NAPI answers it with HTTP 200 plus an ``{error|errors|warnings}`` envelope, which parses to a zero-row frame.

    Example:
        Quick start::

            from sportsdataverse.cbs import cbs_game_odds_hq
            cbs_game_odds_hq()

        See Also:
            * `CBS Sports NAPI registry`_ - the API's own self-documenting endpoint catalog (data origin)
            * `nflfastR`_ - R sister package; NAPI is cross-sport, this is its NFL counterpart

        .. _CBS Sports NAPI registry: https://api.cbssports.com/napi/resource/endpoint/registry
        .. _nflfastR: https://www.nflfastr.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {}
    _params.update(_caller_params)
    raw = _get(
        f"https://api.cbssports.com/napi/resource/game/odds/hq/{game_id}",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_cbs_napi(raw, return_as_pandas=return_as_pandas)
    return raw


def cbs_game_outcomes(
    game_id: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Get an odds outcome for a particular game.

    Endpoint: ``GET https://api.cbssports.com/napi/resource/game/outcomes/{game_id}``
    Example URL: https://api.cbssports.com/napi/resource/game/outcomes

    Args:
        game_id: Numerical game ID
        return_parsed: parse the payload through parse_cbs_napi -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries. An unknown id is NOT an exception -- NAPI answers it with HTTP 200 plus an ``{error|errors|warnings}`` envelope, which parses to a zero-row frame.

    Example:
        Quick start::

            from sportsdataverse.cbs import cbs_game_outcomes
            cbs_game_outcomes()

        See Also:
            * `CBS Sports NAPI registry`_ - the API's own self-documenting endpoint catalog (data origin)
            * `nflfastR`_ - R sister package; NAPI is cross-sport, this is its NFL counterpart

        .. _CBS Sports NAPI registry: https://api.cbssports.com/napi/resource/endpoint/registry
        .. _nflfastR: https://www.nflfastr.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {}
    _params.update(_caller_params)
    raw = _get(
        f"https://api.cbssports.com/napi/resource/game/outcomes/{game_id}",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_cbs_napi(raw, return_as_pandas=return_as_pandas)
    return raw


def cbs_game_probable_players(
    game_id: int,
    date_format: Optional[str] = None,
    resources: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Get a list of players who are probably playing in a particular game.

    Endpoint: ``GET https://api.cbssports.com/napi/resource/game/probablePlayers/{game_id}``
    Example URL: https://api.cbssports.com/napi/resource/game/probablePlayers

    Args:
        game_id: Numerical game ID
        date_format: Optional.  Options here: http://momentjs.com/docs/#/displaying/format/
        resources: Specify specific sub-resources to resolve.  Defaults to none. Allowed: player, playerTeamAssociations, injuries, transactions, depthCharts, metaData.
        return_parsed: parse the payload through parse_cbs_napi -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries. An unknown id is NOT an exception -- NAPI answers it with HTTP 200 plus an ``{error|errors|warnings}`` envelope, which parses to a zero-row frame.

    Example:
        Quick start::

            from sportsdataverse.cbs import cbs_game_probable_players
            cbs_game_probable_players()

        See Also:
            * `CBS Sports NAPI registry`_ - the API's own self-documenting endpoint catalog (data origin)
            * `nflfastR`_ - R sister package; NAPI is cross-sport, this is its NFL counterpart

        .. _CBS Sports NAPI registry: https://api.cbssports.com/napi/resource/endpoint/registry
        .. _nflfastR: https://www.nflfastr.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "dateFormat": date_format,
        "resources": resources,
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://api.cbssports.com/napi/resource/game/probablePlayers/{game_id}",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_cbs_napi(raw, return_as_pandas=return_as_pandas)
    return raw


def cbs_game_props(
    game_id: int,
    market_ids: Optional[str] = None,
    book_ids: Optional[str] = None,
    prop_bet_types: Optional[str] = None,
    state: Optional[str] = None,
    include_inactive_markets: Optional[int] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Get game props for a game.

    Endpoint: ``GET https://api.cbssports.com/napi/resource/game/props/{game_id}``
    Example URL: https://api.cbssports.com/napi/resource/game/props

    Args:
        game_id: Numerical game ID
        market_ids: This value can be used to specify markets, using the marketIds.
        book_ids: This value can be used to specify books, using the bookIds.
        prop_bet_types: This value can be used to specify prop bet types, using the propBetTypes. Allowed: player, game, team.
        state: This value can be used to specify a state.
        include_inactive_markets: This value can be used to filter out inactive markets.
        return_parsed: parse the payload through parse_cbs_napi -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries. An unknown id is NOT an exception -- NAPI answers it with HTTP 200 plus an ``{error|errors|warnings}`` envelope, which parses to a zero-row frame.

    Example:
        Quick start::

            from sportsdataverse.cbs import cbs_game_props
            cbs_game_props()

        See Also:
            * `CBS Sports NAPI registry`_ - the API's own self-documenting endpoint catalog (data origin)
            * `nflfastR`_ - R sister package; NAPI is cross-sport, this is its NFL counterpart

        .. _CBS Sports NAPI registry: https://api.cbssports.com/napi/resource/endpoint/registry
        .. _nflfastR: https://www.nflfastr.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "marketIds": market_ids,
        "bookIds": book_ids,
        "propBetTypes": prop_bet_types,
        "state": state,
        "includeInactiveMarkets": include_inactive_markets,
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://api.cbssports.com/napi/resource/game/props/{game_id}",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_cbs_napi(raw, return_as_pandas=return_as_pandas)
    return raw


def cbs_game_rtwp(
    game_id: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Get a rtwp resource for a particular game.

    Endpoint: ``GET https://api.cbssports.com/napi/resource/game/rtwp/{game_id}``
    Example URL: https://api.cbssports.com/napi/resource/game/rtwp

    Args:
        game_id: Numerical game ID
        return_parsed: parse the payload through parse_cbs_napi -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries. An unknown id is NOT an exception -- NAPI answers it with HTTP 200 plus an ``{error|errors|warnings}`` envelope, which parses to a zero-row frame.

    Example:
        Quick start::

            from sportsdataverse.cbs import cbs_game_rtwp
            cbs_game_rtwp()

        See Also:
            * `CBS Sports NAPI registry`_ - the API's own self-documenting endpoint catalog (data origin)
            * `nflfastR`_ - R sister package; NAPI is cross-sport, this is its NFL counterpart

        .. _CBS Sports NAPI registry: https://api.cbssports.com/napi/resource/endpoint/registry
        .. _nflfastR: https://www.nflfastr.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {}
    _params.update(_caller_params)
    raw = _get(
        f"https://api.cbssports.com/napi/resource/game/rtwp/{game_id}",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_cbs_napi(raw, return_as_pandas=return_as_pandas)
    return raw


def cbs_game_ruwt_highlights(
    game_id: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Get the RUWT highlights resource for a particular game.

    Endpoint: ``GET https://api.cbssports.com/napi/resource/game/ruwtHighlights/{game_id}``
    Example URL: https://api.cbssports.com/napi/resource/game/ruwtHighlights

    Args:
        game_id: Numerical game ID
        return_parsed: parse the payload through parse_cbs_napi -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries. An unknown id is NOT an exception -- NAPI answers it with HTTP 200 plus an ``{error|errors|warnings}`` envelope, which parses to a zero-row frame.

    Example:
        Quick start::

            from sportsdataverse.cbs import cbs_game_ruwt_highlights
            cbs_game_ruwt_highlights()

        See Also:
            * `CBS Sports NAPI registry`_ - the API's own self-documenting endpoint catalog (data origin)
            * `nflfastR`_ - R sister package; NAPI is cross-sport, this is its NFL counterpart

        .. _CBS Sports NAPI registry: https://api.cbssports.com/napi/resource/endpoint/registry
        .. _nflfastR: https://www.nflfastr.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {}
    _params.update(_caller_params)
    raw = _get(
        f"https://api.cbssports.com/napi/resource/game/ruwtHighlights/{game_id}",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_cbs_napi(raw, return_as_pandas=return_as_pandas)
    return raw


def cbs_game_scoring_boxscores(
    game_id: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Get an scoring box scores resource for a particular game.

    Endpoint: ``GET https://api.cbssports.com/napi/resource/game/scoring/boxscores/{game_id}``
    Example URL: https://api.cbssports.com/napi/resource/game/scoring/boxscores

    Args:
        game_id: Numerical game ID
        return_parsed: parse the payload through parse_cbs_napi -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries. An unknown id is NOT an exception -- NAPI answers it with HTTP 200 plus an ``{error|errors|warnings}`` envelope, which parses to a zero-row frame.

    Example:
        Quick start::

            from sportsdataverse.cbs import cbs_game_scoring_boxscores
            cbs_game_scoring_boxscores()

        See Also:
            * `CBS Sports NAPI registry`_ - the API's own self-documenting endpoint catalog (data origin)
            * `nflfastR`_ - R sister package; NAPI is cross-sport, this is its NFL counterpart

        .. _CBS Sports NAPI registry: https://api.cbssports.com/napi/resource/endpoint/registry
        .. _nflfastR: https://www.nflfastr.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {}
    _params.update(_caller_params)
    raw = _get(
        f"https://api.cbssports.com/napi/resource/game/scoring/boxscores/{game_id}",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_cbs_napi(raw, return_as_pandas=return_as_pandas)
    return raw


def cbs_game_scoring_drives(
    game_id: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Get a drives resource for a particular game.

    Endpoint: ``GET https://api.cbssports.com/napi/resource/game/scoring/drives/{game_id}``
    Example URL: https://api.cbssports.com/napi/resource/game/scoring/drives

    Args:
        game_id: Numerical game ID
        return_parsed: parse the payload through parse_cbs_napi -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries. An unknown id is NOT an exception -- NAPI answers it with HTTP 200 plus an ``{error|errors|warnings}`` envelope, which parses to a zero-row frame.

    Example:
        Quick start::

            from sportsdataverse.cbs import cbs_game_scoring_drives
            cbs_game_scoring_drives()

        See Also:
            * `CBS Sports NAPI registry`_ - the API's own self-documenting endpoint catalog (data origin)
            * `nflfastR`_ - R sister package; NAPI is cross-sport, this is its NFL counterpart

        .. _CBS Sports NAPI registry: https://api.cbssports.com/napi/resource/endpoint/registry
        .. _nflfastR: https://www.nflfastr.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {}
    _params.update(_caller_params)
    raw = _get(
        f"https://api.cbssports.com/napi/resource/game/scoring/drives/{game_id}",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_cbs_napi(raw, return_as_pandas=return_as_pandas)
    return raw


def cbs_game_scoring_leaders(
    game_id: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Get an scoring leaders resource for a particular game.

    Endpoint: ``GET https://api.cbssports.com/napi/resource/game/scoring/leaders/{game_id}``
    Example URL: https://api.cbssports.com/napi/resource/game/scoring/leaders

    Args:
        game_id: Numerical game ID
        return_parsed: parse the payload through parse_cbs_napi -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries. An unknown id is NOT an exception -- NAPI answers it with HTTP 200 plus an ``{error|errors|warnings}`` envelope, which parses to a zero-row frame.

    Example:
        Quick start::

            from sportsdataverse.cbs import cbs_game_scoring_leaders
            cbs_game_scoring_leaders()

        See Also:
            * `CBS Sports NAPI registry`_ - the API's own self-documenting endpoint catalog (data origin)
            * `nflfastR`_ - R sister package; NAPI is cross-sport, this is its NFL counterpart

        .. _CBS Sports NAPI registry: https://api.cbssports.com/napi/resource/endpoint/registry
        .. _nflfastR: https://www.nflfastr.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {}
    _params.update(_caller_params)
    raw = _get(
        f"https://api.cbssports.com/napi/resource/game/scoring/leaders/{game_id}",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_cbs_napi(raw, return_as_pandas=return_as_pandas)
    return raw


def cbs_game_scoring_player_stats(
    game_id: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Get an scoring player stats resource for a particular game.

    Endpoint: ``GET https://api.cbssports.com/napi/resource/game/scoring/playerStats/{game_id}``
    Example URL: https://api.cbssports.com/napi/resource/game/scoring/playerStats

    Args:
        game_id: Numerical game ID
        return_parsed: parse the payload through parse_cbs_napi -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries. An unknown id is NOT an exception -- NAPI answers it with HTTP 200 plus an ``{error|errors|warnings}`` envelope, which parses to a zero-row frame.

    Example:
        Quick start::

            from sportsdataverse.cbs import cbs_game_scoring_player_stats
            cbs_game_scoring_player_stats()

        See Also:
            * `CBS Sports NAPI registry`_ - the API's own self-documenting endpoint catalog (data origin)
            * `nflfastR`_ - R sister package; NAPI is cross-sport, this is its NFL counterpart

        .. _CBS Sports NAPI registry: https://api.cbssports.com/napi/resource/endpoint/registry
        .. _nflfastR: https://www.nflfastr.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {}
    _params.update(_caller_params)
    raw = _get(
        f"https://api.cbssports.com/napi/resource/game/scoring/playerStats/{game_id}",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_cbs_napi(raw, return_as_pandas=return_as_pandas)
    return raw


def cbs_game_scoring_plays(
    game_id: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Get an scoring plays resource for a particular game.

    Endpoint: ``GET https://api.cbssports.com/napi/resource/game/scoring/plays/{game_id}``
    Example URL: https://api.cbssports.com/napi/resource/game/scoring/plays

    Args:
        game_id: Numerical game ID
        return_parsed: parse the payload through parse_cbs_napi -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries. An unknown id is NOT an exception -- NAPI answers it with HTTP 200 plus an ``{error|errors|warnings}`` envelope, which parses to a zero-row frame.

    Example:
        Quick start::

            from sportsdataverse.cbs import cbs_game_scoring_plays
            cbs_game_scoring_plays()

        See Also:
            * `CBS Sports NAPI registry`_ - the API's own self-documenting endpoint catalog (data origin)
            * `nflfastR`_ - R sister package; NAPI is cross-sport, this is its NFL counterpart

        .. _CBS Sports NAPI registry: https://api.cbssports.com/napi/resource/endpoint/registry
        .. _nflfastR: https://www.nflfastr.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {}
    _params.update(_caller_params)
    raw = _get(
        f"https://api.cbssports.com/napi/resource/game/scoring/plays/{game_id}",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_cbs_napi(raw, return_as_pandas=return_as_pandas)
    return raw


def cbs_game_scoring_rosters(
    game_id: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Get an scoring rosters resource for a particular game.

    Endpoint: ``GET https://api.cbssports.com/napi/resource/game/scoring/rosters/{game_id}``
    Example URL: https://api.cbssports.com/napi/resource/game/scoring/rosters

    Args:
        game_id: Numerical game ID
        return_parsed: parse the payload through parse_cbs_napi -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries. An unknown id is NOT an exception -- NAPI answers it with HTTP 200 plus an ``{error|errors|warnings}`` envelope, which parses to a zero-row frame.

    Example:
        Quick start::

            from sportsdataverse.cbs import cbs_game_scoring_rosters
            cbs_game_scoring_rosters()

        See Also:
            * `CBS Sports NAPI registry`_ - the API's own self-documenting endpoint catalog (data origin)
            * `nflfastR`_ - R sister package; NAPI is cross-sport, this is its NFL counterpart

        .. _CBS Sports NAPI registry: https://api.cbssports.com/napi/resource/endpoint/registry
        .. _nflfastR: https://www.nflfastr.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {}
    _params.update(_caller_params)
    raw = _get(
        f"https://api.cbssports.com/napi/resource/game/scoring/rosters/{game_id}",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_cbs_napi(raw, return_as_pandas=return_as_pandas)
    return raw


def cbs_game_scoring_scoreboard(
    game_id: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Get an scoring scoreboard resource for a particular game.

    Endpoint: ``GET https://api.cbssports.com/napi/resource/game/scoring/scoreboard/{game_id}``
    Example URL: https://api.cbssports.com/napi/resource/game/scoring/scoreboard

    Args:
        game_id: Numerical game ID
        return_parsed: parse the payload through parse_cbs_napi -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries. An unknown id is NOT an exception -- NAPI answers it with HTTP 200 plus an ``{error|errors|warnings}`` envelope, which parses to a zero-row frame.

    Example:
        Quick start::

            from sportsdataverse.cbs import cbs_game_scoring_scoreboard
            cbs_game_scoring_scoreboard()

        See Also:
            * `CBS Sports NAPI registry`_ - the API's own self-documenting endpoint catalog (data origin)
            * `nflfastR`_ - R sister package; NAPI is cross-sport, this is its NFL counterpart

        .. _CBS Sports NAPI registry: https://api.cbssports.com/napi/resource/endpoint/registry
        .. _nflfastR: https://www.nflfastr.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {}
    _params.update(_caller_params)
    raw = _get(
        f"https://api.cbssports.com/napi/resource/game/scoring/scoreboard/{game_id}",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_cbs_napi(raw, return_as_pandas=return_as_pandas)
    return raw


def cbs_game_scoring_scores(
    game_id: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Get an scoring scores resource for a particular game.

    Endpoint: ``GET https://api.cbssports.com/napi/resource/game/scoring/scores/{game_id}``
    Example URL: https://api.cbssports.com/napi/resource/game/scoring/scores

    Args:
        game_id: Numerical game ID
        return_parsed: parse the payload through parse_cbs_napi -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries. An unknown id is NOT an exception -- NAPI answers it with HTTP 200 plus an ``{error|errors|warnings}`` envelope, which parses to a zero-row frame.

    Example:
        Quick start::

            from sportsdataverse.cbs import cbs_game_scoring_scores
            cbs_game_scoring_scores()

        See Also:
            * `CBS Sports NAPI registry`_ - the API's own self-documenting endpoint catalog (data origin)
            * `nflfastR`_ - R sister package; NAPI is cross-sport, this is its NFL counterpart

        .. _CBS Sports NAPI registry: https://api.cbssports.com/napi/resource/endpoint/registry
        .. _nflfastR: https://www.nflfastr.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {}
    _params.update(_caller_params)
    raw = _get(
        f"https://api.cbssports.com/napi/resource/game/scoring/scores/{game_id}",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_cbs_napi(raw, return_as_pandas=return_as_pandas)
    return raw


def cbs_game_scoring_team_stats(
    game_id: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Get an scoring team stats resource for a particular game.

    Endpoint: ``GET https://api.cbssports.com/napi/resource/game/scoring/teamStats/{game_id}``
    Example URL: https://api.cbssports.com/napi/resource/game/scoring/teamStats

    Args:
        game_id: Numerical game ID
        return_parsed: parse the payload through parse_cbs_napi -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries. An unknown id is NOT an exception -- NAPI answers it with HTTP 200 plus an ``{error|errors|warnings}`` envelope, which parses to a zero-row frame.

    Example:
        Quick start::

            from sportsdataverse.cbs import cbs_game_scoring_team_stats
            cbs_game_scoring_team_stats()

        See Also:
            * `CBS Sports NAPI registry`_ - the API's own self-documenting endpoint catalog (data origin)
            * `nflfastR`_ - R sister package; NAPI is cross-sport, this is its NFL counterpart

        .. _CBS Sports NAPI registry: https://api.cbssports.com/napi/resource/endpoint/registry
        .. _nflfastR: https://www.nflfastr.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {}
    _params.update(_caller_params)
    raw = _get(
        f"https://api.cbssports.com/napi/resource/game/scoring/teamStats/{game_id}",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_cbs_napi(raw, return_as_pandas=return_as_pandas)
    return raw


def cbs_game_scoring_winprob(
    game_id: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Get an scoring winprob resource for a particular game.

    Endpoint: ``GET https://api.cbssports.com/napi/resource/game/scoring/winprob/{game_id}``
    Example URL: https://api.cbssports.com/napi/resource/game/scoring/winprob

    Args:
        game_id: Numerical game ID
        return_parsed: parse the payload through parse_cbs_napi -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries. An unknown id is NOT an exception -- NAPI answers it with HTTP 200 plus an ``{error|errors|warnings}`` envelope, which parses to a zero-row frame.

    Example:
        Quick start::

            from sportsdataverse.cbs import cbs_game_scoring_winprob
            cbs_game_scoring_winprob()

        See Also:
            * `CBS Sports NAPI registry`_ - the API's own self-documenting endpoint catalog (data origin)
            * `nflfastR`_ - R sister package; NAPI is cross-sport, this is its NFL counterpart

        .. _CBS Sports NAPI registry: https://api.cbssports.com/napi/resource/endpoint/registry
        .. _nflfastR: https://www.nflfastr.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {}
    _params.update(_caller_params)
    raw = _get(
        f"https://api.cbssports.com/napi/resource/game/scoring/winprob/{game_id}",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_cbs_napi(raw, return_as_pandas=return_as_pandas)
    return raw


def cbs_game_scoring_ytd_player_stats(
    game_id: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Get an scoring YTD player stats resource for a particular game.

    Endpoint: ``GET https://api.cbssports.com/napi/resource/game/scoring/ytdPlayerStats/{game_id}``
    Example URL: https://api.cbssports.com/napi/resource/game/scoring/ytdPlayerStats

    Args:
        game_id: Numerical game ID
        return_parsed: parse the payload through parse_cbs_napi -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries. An unknown id is NOT an exception -- NAPI answers it with HTTP 200 plus an ``{error|errors|warnings}`` envelope, which parses to a zero-row frame.

    Example:
        Quick start::

            from sportsdataverse.cbs import cbs_game_scoring_ytd_player_stats
            cbs_game_scoring_ytd_player_stats()

        See Also:
            * `CBS Sports NAPI registry`_ - the API's own self-documenting endpoint catalog (data origin)
            * `nflfastR`_ - R sister package; NAPI is cross-sport, this is its NFL counterpart

        .. _CBS Sports NAPI registry: https://api.cbssports.com/napi/resource/endpoint/registry
        .. _nflfastR: https://www.nflfastr.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {}
    _params.update(_caller_params)
    raw = _get(
        f"https://api.cbssports.com/napi/resource/game/scoring/ytdPlayerStats/{game_id}",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_cbs_napi(raw, return_as_pandas=return_as_pandas)
    return raw


def cbs_game_scoring_ytd_team_stats(
    game_id: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Get an scoring YTD team stats resource for a particular game.

    Endpoint: ``GET https://api.cbssports.com/napi/resource/game/scoring/ytdTeamStats/{game_id}``
    Example URL: https://api.cbssports.com/napi/resource/game/scoring/ytdTeamStats

    Args:
        game_id: Numerical game ID
        return_parsed: parse the payload through parse_cbs_napi -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries. An unknown id is NOT an exception -- NAPI answers it with HTTP 200 plus an ``{error|errors|warnings}`` envelope, which parses to a zero-row frame.

    Example:
        Quick start::

            from sportsdataverse.cbs import cbs_game_scoring_ytd_team_stats
            cbs_game_scoring_ytd_team_stats()

        See Also:
            * `CBS Sports NAPI registry`_ - the API's own self-documenting endpoint catalog (data origin)
            * `nflfastR`_ - R sister package; NAPI is cross-sport, this is its NFL counterpart

        .. _CBS Sports NAPI registry: https://api.cbssports.com/napi/resource/endpoint/registry
        .. _nflfastR: https://www.nflfastr.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {}
    _params.update(_caller_params)
    raw = _get(
        f"https://api.cbssports.com/napi/resource/game/scoring/ytdTeamStats/{game_id}",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_cbs_napi(raw, return_as_pandas=return_as_pandas)
    return raw


def cbs_game_ticket(
    game_id: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Get a ticket resource for a particular game.

    Endpoint: ``GET https://api.cbssports.com/napi/resource/game/ticket/{game_id}``
    Example URL: https://api.cbssports.com/napi/resource/game/ticket

    Args:
        game_id: Numerical game ID
        return_parsed: parse the payload through parse_cbs_napi -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries. An unknown id is NOT an exception -- NAPI answers it with HTTP 200 plus an ``{error|errors|warnings}`` envelope, which parses to a zero-row frame.

    Example:
        Quick start::

            from sportsdataverse.cbs import cbs_game_ticket
            cbs_game_ticket()

        See Also:
            * `CBS Sports NAPI registry`_ - the API's own self-documenting endpoint catalog (data origin)
            * `nflfastR`_ - R sister package; NAPI is cross-sport, this is its NFL counterpart

        .. _CBS Sports NAPI registry: https://api.cbssports.com/napi/resource/endpoint/registry
        .. _nflfastR: https://www.nflfastr.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {}
    _params.update(_caller_params)
    raw = _get(
        f"https://api.cbssports.com/napi/resource/game/ticket/{game_id}",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_cbs_napi(raw, return_as_pandas=return_as_pandas)
    return raw


def cbs_game_weather(
    game_id: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Get a Weather resource for a particular game.

    Endpoint: ``GET https://api.cbssports.com/napi/resource/game/weather/{game_id}``
    Example URL: https://api.cbssports.com/napi/resource/game/weather

    Args:
        game_id: Numerical game ID
        return_parsed: parse the payload through parse_cbs_napi -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries. An unknown id is NOT an exception -- NAPI answers it with HTTP 200 plus an ``{error|errors|warnings}`` envelope, which parses to a zero-row frame.

    Example:
        Quick start::

            from sportsdataverse.cbs import cbs_game_weather
            cbs_game_weather()

        See Also:
            * `CBS Sports NAPI registry`_ - the API's own self-documenting endpoint catalog (data origin)
            * `nflfastR`_ - R sister package; NAPI is cross-sport, this is its NFL counterpart

        .. _CBS Sports NAPI registry: https://api.cbssports.com/napi/resource/endpoint/registry
        .. _nflfastR: https://www.nflfastr.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {}
    _params.update(_caller_params)
    raw = _get(
        f"https://api.cbssports.com/napi/resource/game/weather/{game_id}",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_cbs_napi(raw, return_as_pandas=return_as_pandas)
    return raw


def cbs_golf_event_markets(
    event_id: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Get markets for an event.

    Endpoint: ``GET https://api.cbssports.com/napi/resource/golf/event/markets/{event_id}``
    Example URL: https://api.cbssports.com/napi/resource/golf/event/markets

    Args:
        event_id: Numerical event ID
        return_parsed: parse the payload through parse_cbs_napi -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries. An unknown id is NOT an exception -- NAPI answers it with HTTP 200 plus an ``{error|errors|warnings}`` envelope, which parses to a zero-row frame.

    Example:
        Quick start::

            from sportsdataverse.cbs import cbs_golf_event_markets
            cbs_golf_event_markets()

        See Also:
            * `CBS Sports NAPI registry`_ - the API's own self-documenting endpoint catalog (data origin)
            * `nflfastR`_ - R sister package; NAPI is cross-sport, this is its NFL counterpart

        .. _CBS Sports NAPI registry: https://api.cbssports.com/napi/resource/endpoint/registry
        .. _nflfastR: https://www.nflfastr.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {}
    _params.update(_caller_params)
    raw = _get(
        f"https://api.cbssports.com/napi/resource/golf/event/markets/{event_id}",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_cbs_napi(raw, return_as_pandas=return_as_pandas)
    return raw


def cbs_golf_player_markets(
    player_id: int,
    event_id: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Get markets for a golfer.

    Endpoint: ``GET https://api.cbssports.com/napi/resource/golf/player/markets/{player_id}``
    Example URL: https://api.cbssports.com/napi/resource/golf/player/markets/1751796

    Args:
        player_id: Numerical player ID
        event_id: View option.  Filter by eventId.
        return_parsed: parse the payload through parse_cbs_napi -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries. An unknown id is NOT an exception -- NAPI answers it with HTTP 200 plus an ``{error|errors|warnings}`` envelope, which parses to a zero-row frame.

    Example:
        Quick start::

            from sportsdataverse.cbs import cbs_golf_player_markets
            cbs_golf_player_markets(player_id=1751796)

        See Also:
            * `CBS Sports NAPI registry`_ - the API's own self-documenting endpoint catalog (data origin)
            * `nflfastR`_ - R sister package; NAPI is cross-sport, this is its NFL counterpart

        .. _CBS Sports NAPI registry: https://api.cbssports.com/napi/resource/endpoint/registry
        .. _nflfastR: https://www.nflfastr.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "eventId": event_id,
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://api.cbssports.com/napi/resource/golf/player/markets/{player_id}",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_cbs_napi(raw, return_as_pandas=return_as_pandas)
    return raw


def cbs_golfer_results(
    player_id: int,
    season_year: Optional[int] = None,
    season_id: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Get golfer tournament results resource for a particular player.

    Endpoint: ``GET https://api.cbssports.com/napi/resource/golfer/results/{player_id}``
    Example URL: https://api.cbssports.com/napi/resource/golfer/results/1751796

    Args:
        player_id: Numerical player ID
        season_year: View option.  Filter by seasonType.
        season_id: View option.  Filter by seasonId.
        return_parsed: parse the payload through parse_cbs_napi -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries. An unknown id is NOT an exception -- NAPI answers it with HTTP 200 plus an ``{error|errors|warnings}`` envelope, which parses to a zero-row frame.

    Example:
        Quick start::

            from sportsdataverse.cbs import cbs_golfer_results
            cbs_golfer_results(player_id=1751796)

        See Also:
            * `CBS Sports NAPI registry`_ - the API's own self-documenting endpoint catalog (data origin)
            * `nflfastR`_ - R sister package; NAPI is cross-sport, this is its NFL counterpart

        .. _CBS Sports NAPI registry: https://api.cbssports.com/napi/resource/endpoint/registry
        .. _nflfastR: https://www.nflfastr.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "seasonYear": season_year,
        "seasonId": season_id,
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://api.cbssports.com/napi/resource/golfer/results/{player_id}",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_cbs_napi(raw, return_as_pandas=return_as_pandas)
    return raw


def cbs_league(
    league_id: int,
    resources: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Get a league resource

    Endpoint: ``GET https://api.cbssports.com/napi/resource/league/{league_id}``
    Example URL: https://api.cbssports.com/napi/resource/league/59

    Args:
        league_id: Numerical league ID
        resources: Specify specific sub-resources to resolve.  Defaults to none. Allowed: teams, players, standings, conference, division, polls, teamSeasons.
        return_parsed: parse the payload through parse_cbs_napi -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries. An unknown id is NOT an exception -- NAPI answers it with HTTP 200 plus an ``{error|errors|warnings}`` envelope, which parses to a zero-row frame.

    Example:
        Quick start::

            from sportsdataverse.cbs import cbs_league
            cbs_league(league_id=59)

        See Also:
            * `CBS Sports NAPI registry`_ - the API's own self-documenting endpoint catalog (data origin)
            * `nflfastR`_ - R sister package; NAPI is cross-sport, this is its NFL counterpart

        .. _CBS Sports NAPI registry: https://api.cbssports.com/napi/resource/endpoint/registry
        .. _nflfastR: https://www.nflfastr.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "resources": resources,
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://api.cbssports.com/napi/resource/league/{league_id}",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_cbs_napi(raw, return_as_pandas=return_as_pandas)
    return raw


def cbs_league_teams(
    league_id: int,
    resources: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Get team resources on a league with optional vendor overlay

    Endpoint: ``GET https://api.cbssports.com/napi/resource/league/teams/{league_id}``
    Example URL: https://api.cbssports.com/napi/resource/league/teams/59

    Args:
        league_id: Numerical league Id - gets team from team table not teams for season
        resources: Specify specific sub-resources to resolve.  Defaults to none. Allowed: players, standings, conference, division, playerTeamAssociations, injuries, transactions, depthCharts, polls, teamSeasons, sportsLineStandings.
        return_parsed: parse the payload through parse_cbs_napi -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries. An unknown id is NOT an exception -- NAPI answers it with HTTP 200 plus an ``{error|errors|warnings}`` envelope, which parses to a zero-row frame.

    Example:
        Quick start::

            from sportsdataverse.cbs import cbs_league_teams
            cbs_league_teams(league_id=59)

        See Also:
            * `CBS Sports NAPI registry`_ - the API's own self-documenting endpoint catalog (data origin)
            * `nflfastR`_ - R sister package; NAPI is cross-sport, this is its NFL counterpart

        .. _CBS Sports NAPI registry: https://api.cbssports.com/napi/resource/endpoint/registry
        .. _nflfastR: https://www.nflfastr.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "resources": resources,
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://api.cbssports.com/napi/resource/league/teams/{league_id}",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_cbs_napi(raw, return_as_pandas=return_as_pandas)
    return raw


def cbs_odds(
    game_id: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Get an odds resource for a particular game.

    Endpoint: ``GET https://api.cbssports.com/napi/resource/odds/{game_id}``
    Example URL: https://api.cbssports.com/napi/resource/odds

    Args:
        game_id: Numerical game ID
        return_parsed: parse the payload through parse_cbs_napi -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries. An unknown id is NOT an exception -- NAPI answers it with HTTP 200 plus an ``{error|errors|warnings}`` envelope, which parses to a zero-row frame.

    Example:
        Quick start::

            from sportsdataverse.cbs import cbs_odds
            cbs_odds()

        See Also:
            * `CBS Sports NAPI registry`_ - the API's own self-documenting endpoint catalog (data origin)
            * `nflfastR`_ - R sister package; NAPI is cross-sport, this is its NFL counterpart

        .. _CBS Sports NAPI registry: https://api.cbssports.com/napi/resource/endpoint/registry
        .. _nflfastR: https://www.nflfastr.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {}
    _params.update(_caller_params)
    raw = _get(
        f"https://api.cbssports.com/napi/resource/odds/{game_id}",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_cbs_napi(raw, return_as_pandas=return_as_pandas)
    return raw


def cbs_player(
    player_id: int,
    date_format: Optional[str] = None,
    year: Optional[int] = None,
    resources: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Get a player resource

    Endpoint: ``GET https://api.cbssports.com/napi/resource/player/{player_id}``
    Example URL: https://api.cbssports.com/napi/resource/player/1751796

    Args:
        player_id: Numerical player ID
        date_format: Optional for any date field.  Options here: http://momentjs.com/docs/#/displaying/format/
        year: Optional year in YYYY format (for Transactions only)
        resources: Specify specific sub-resources to resolve.  Defaults to none. Allowed: playerTeamAssociations, injuries, transactions, depthCharts, metaData, playerStats, standings, rankings, playerOutlook, draftInfo, combineData, positionRankings, gameStats, encyclopedia, golferResults, playerGolfMetadata, playerFutures, golferMarkets, recruitTeamAssociations, coachTeamAssociations, recruitRankings, coachRankings.
        return_parsed: parse the payload through parse_cbs_napi -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries. An unknown id is NOT an exception -- NAPI answers it with HTTP 200 plus an ``{error|errors|warnings}`` envelope, which parses to a zero-row frame.

    Example:
        Quick start::

            from sportsdataverse.cbs import cbs_player
            cbs_player(player_id=1751796)

        See Also:
            * `CBS Sports NAPI registry`_ - the API's own self-documenting endpoint catalog (data origin)
            * `nflfastR`_ - R sister package; NAPI is cross-sport, this is its NFL counterpart

        .. _CBS Sports NAPI registry: https://api.cbssports.com/napi/resource/endpoint/registry
        .. _nflfastR: https://www.nflfastr.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "dateFormat": date_format,
        "year": year,
        "resources": resources,
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://api.cbssports.com/napi/resource/player/{player_id}",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_cbs_napi(raw, return_as_pandas=return_as_pandas)
    return raw


def cbs_player_combine_data(
    player_id: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Get draft related info for a particular player.

    Endpoint: ``GET https://api.cbssports.com/napi/resource/player/combineData/{player_id}``
    Example URL: https://api.cbssports.com/napi/resource/player/combineData/1751796

    Args:
        player_id: Numerical player ID
        return_parsed: parse the payload through parse_cbs_napi -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries. An unknown id is NOT an exception -- NAPI answers it with HTTP 200 plus an ``{error|errors|warnings}`` envelope, which parses to a zero-row frame.

    Example:
        Quick start::

            from sportsdataverse.cbs import cbs_player_combine_data
            cbs_player_combine_data(player_id=1751796)

        See Also:
            * `CBS Sports NAPI registry`_ - the API's own self-documenting endpoint catalog (data origin)
            * `nflfastR`_ - R sister package; NAPI is cross-sport, this is its NFL counterpart

        .. _CBS Sports NAPI registry: https://api.cbssports.com/napi/resource/endpoint/registry
        .. _nflfastR: https://www.nflfastr.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {}
    _params.update(_caller_params)
    raw = _get(
        f"https://api.cbssports.com/napi/resource/player/combineData/{player_id}",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_cbs_napi(raw, return_as_pandas=return_as_pandas)
    return raw


def cbs_player_depth_charts(
    player_id: int,
    position: Optional[str] = None,
    pitch_pos: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Get depth charts for a particular player.

    Endpoint: ``GET https://api.cbssports.com/napi/resource/player/depthCharts/{player_id}``
    Example URL: https://api.cbssports.com/napi/resource/player/depthCharts/1751796

    Args:
        player_id: Numerical player ID
        position: A csv of positions to filter with
        pitch_pos: A csv of pitch positions to filter with (baseball only)
        return_parsed: parse the payload through parse_cbs_napi -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries. An unknown id is NOT an exception -- NAPI answers it with HTTP 200 plus an ``{error|errors|warnings}`` envelope, which parses to a zero-row frame.

    Example:
        Quick start::

            from sportsdataverse.cbs import cbs_player_depth_charts
            cbs_player_depth_charts(player_id=1751796)

        See Also:
            * `CBS Sports NAPI registry`_ - the API's own self-documenting endpoint catalog (data origin)
            * `nflfastR`_ - R sister package; NAPI is cross-sport, this is its NFL counterpart

        .. _CBS Sports NAPI registry: https://api.cbssports.com/napi/resource/endpoint/registry
        .. _nflfastR: https://www.nflfastr.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "position": position,
        "pitchPos": pitch_pos,
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://api.cbssports.com/napi/resource/player/depthCharts/{player_id}",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_cbs_napi(raw, return_as_pandas=return_as_pandas)
    return raw


def cbs_player_draft_info(
    player_id: int,
    season_year: Optional[str] = None,
    season_type: Optional[str] = None,
    season_id: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Get draft info for a particular player.

    Endpoint: ``GET https://api.cbssports.com/napi/resource/player/draftInfo/{player_id}``
    Example URL: https://api.cbssports.com/napi/resource/player/draftInfo/1751796

    Args:
        player_id: Numerical player ID
        season_year: View option, filter by seasonYear
        season_type: View option, filter by seasonType Allowed: regular, pre, post.
        season_id: View option, filter by seasonId
        return_parsed: parse the payload through parse_cbs_napi -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries. An unknown id is NOT an exception -- NAPI answers it with HTTP 200 plus an ``{error|errors|warnings}`` envelope, which parses to a zero-row frame.

    Example:
        Quick start::

            from sportsdataverse.cbs import cbs_player_draft_info
            cbs_player_draft_info(player_id=1751796)

        See Also:
            * `CBS Sports NAPI registry`_ - the API's own self-documenting endpoint catalog (data origin)
            * `nflfastR`_ - R sister package; NAPI is cross-sport, this is its NFL counterpart

        .. _CBS Sports NAPI registry: https://api.cbssports.com/napi/resource/endpoint/registry
        .. _nflfastR: https://www.nflfastr.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "seasonYear": season_year,
        "seasonType": season_type,
        "seasonId": season_id,
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://api.cbssports.com/napi/resource/player/draftInfo/{player_id}",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_cbs_napi(raw, return_as_pandas=return_as_pandas)
    return raw


def cbs_player_encyclopedia(
    player_id: int,
    season_year: Optional[int] = None,
    season_id: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Get encyclopedia resource for a particular player.

    Endpoint: ``GET https://api.cbssports.com/napi/resource/player/encyclopedia/{player_id}``
    Example URL: https://api.cbssports.com/napi/resource/player/encyclopedia/1751796

    Args:
        player_id: Numerical player ID
        season_year: View option.  Filter by seasonType.
        season_id: View option.  Filter by seasonId.
        return_parsed: parse the payload through parse_cbs_napi -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries. An unknown id is NOT an exception -- NAPI answers it with HTTP 200 plus an ``{error|errors|warnings}`` envelope, which parses to a zero-row frame.

    Example:
        Quick start::

            from sportsdataverse.cbs import cbs_player_encyclopedia
            cbs_player_encyclopedia(player_id=1751796)

        See Also:
            * `CBS Sports NAPI registry`_ - the API's own self-documenting endpoint catalog (data origin)
            * `nflfastR`_ - R sister package; NAPI is cross-sport, this is its NFL counterpart

        .. _CBS Sports NAPI registry: https://api.cbssports.com/napi/resource/endpoint/registry
        .. _nflfastR: https://www.nflfastr.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "seasonYear": season_year,
        "seasonId": season_id,
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://api.cbssports.com/napi/resource/player/encyclopedia/{player_id}",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_cbs_napi(raw, return_as_pandas=return_as_pandas)
    return raw


def cbs_player_futures(
    player_id: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Get futures for a player.

    Endpoint: ``GET https://api.cbssports.com/napi/resource/player/futures/{player_id}``
    Example URL: https://api.cbssports.com/napi/resource/player/futures/1751796

    Args:
        player_id: Numerical player ID
        return_parsed: parse the payload through parse_cbs_napi -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries. An unknown id is NOT an exception -- NAPI answers it with HTTP 200 plus an ``{error|errors|warnings}`` envelope, which parses to a zero-row frame.

    Example:
        Quick start::

            from sportsdataverse.cbs import cbs_player_futures
            cbs_player_futures(player_id=1751796)

        See Also:
            * `CBS Sports NAPI registry`_ - the API's own self-documenting endpoint catalog (data origin)
            * `nflfastR`_ - R sister package; NAPI is cross-sport, this is its NFL counterpart

        .. _CBS Sports NAPI registry: https://api.cbssports.com/napi/resource/endpoint/registry
        .. _nflfastR: https://www.nflfastr.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {}
    _params.update(_caller_params)
    raw = _get(
        f"https://api.cbssports.com/napi/resource/player/futures/{player_id}",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_cbs_napi(raw, return_as_pandas=return_as_pandas)
    return raw


def cbs_player_game_stats(
    player_id: int,
    game_id: Optional[str] = None,
    season_year: Optional[str] = None,
    season_type: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Get game stats for a particular player.

    Endpoint: ``GET https://api.cbssports.com/napi/resource/player/gameStats/{player_id}``
    Example URL: https://api.cbssports.com/napi/resource/player/gameStats/1751796

    Args:
        player_id: Numerical player ID
        game_id: View option, filter by gameId
        season_year: Season Year in YYYY format
        season_type: Csv list of pre, regular, or post Allowed: pre, regular, post.
        return_parsed: parse the payload through parse_cbs_napi -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries. An unknown id is NOT an exception -- NAPI answers it with HTTP 200 plus an ``{error|errors|warnings}`` envelope, which parses to a zero-row frame.

    Example:
        Quick start::

            from sportsdataverse.cbs import cbs_player_game_stats
            cbs_player_game_stats(player_id=1751796)

        See Also:
            * `CBS Sports NAPI registry`_ - the API's own self-documenting endpoint catalog (data origin)
            * `nflfastR`_ - R sister package; NAPI is cross-sport, this is its NFL counterpart

        .. _CBS Sports NAPI registry: https://api.cbssports.com/napi/resource/endpoint/registry
        .. _nflfastR: https://www.nflfastr.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "gameId": game_id,
        "seasonYear": season_year,
        "seasonType": season_type,
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://api.cbssports.com/napi/resource/player/gameStats/{player_id}",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_cbs_napi(raw, return_as_pandas=return_as_pandas)
    return raw


def cbs_player_hockey_meta(
    player_id: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Get the hockey meta data resource for a particular player.

    Endpoint: ``GET https://api.cbssports.com/napi/resource/player/hockey/meta/{player_id}``
    Example URL: https://api.cbssports.com/napi/resource/player/hockey/meta/1751796

    Args:
        player_id: Numerical player ID
        return_parsed: parse the payload through parse_cbs_napi -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries. An unknown id is NOT an exception -- NAPI answers it with HTTP 200 plus an ``{error|errors|warnings}`` envelope, which parses to a zero-row frame.

    Example:
        Quick start::

            from sportsdataverse.cbs import cbs_player_hockey_meta
            cbs_player_hockey_meta(player_id=1751796)

        See Also:
            * `CBS Sports NAPI registry`_ - the API's own self-documenting endpoint catalog (data origin)
            * `nflfastR`_ - R sister package; NAPI is cross-sport, this is its NFL counterpart

        .. _CBS Sports NAPI registry: https://api.cbssports.com/napi/resource/endpoint/registry
        .. _nflfastR: https://www.nflfastr.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {}
    _params.update(_caller_params)
    raw = _get(
        f"https://api.cbssports.com/napi/resource/player/hockey/meta/{player_id}",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_cbs_napi(raw, return_as_pandas=return_as_pandas)
    return raw


def cbs_player_injuries(
    player_id: int,
    date_format: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Get injuries for a particular player.

    Endpoint: ``GET https://api.cbssports.com/napi/resource/player/injuries/{player_id}``
    Example URL: https://api.cbssports.com/napi/resource/player/injuries/1751796

    Args:
        player_id: Numerical player ID
        date_format: Optional.  Options here: http://momentjs.com/docs/#/displaying/format/
        return_parsed: parse the payload through parse_cbs_napi -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries. An unknown id is NOT an exception -- NAPI answers it with HTTP 200 plus an ``{error|errors|warnings}`` envelope, which parses to a zero-row frame.

    Example:
        Quick start::

            from sportsdataverse.cbs import cbs_player_injuries
            cbs_player_injuries(player_id=1751796)

        See Also:
            * `CBS Sports NAPI registry`_ - the API's own self-documenting endpoint catalog (data origin)
            * `nflfastR`_ - R sister package; NAPI is cross-sport, this is its NFL counterpart

        .. _CBS Sports NAPI registry: https://api.cbssports.com/napi/resource/endpoint/registry
        .. _nflfastR: https://www.nflfastr.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "dateFormat": date_format,
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://api.cbssports.com/napi/resource/player/injuries/{player_id}",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_cbs_napi(raw, return_as_pandas=return_as_pandas)
    return raw


def cbs_player_meta_baseball(
    player_id: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Get meta data associated to a particular baseball player.

    Endpoint: ``GET https://api.cbssports.com/napi/resource/player/meta/baseball/{player_id}``
    Example URL: https://api.cbssports.com/napi/resource/player/meta/baseball/1751796

    Args:
        player_id: Numerical player ID
        return_parsed: parse the payload through parse_cbs_napi -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries. An unknown id is NOT an exception -- NAPI answers it with HTTP 200 plus an ``{error|errors|warnings}`` envelope, which parses to a zero-row frame.

    Example:
        Quick start::

            from sportsdataverse.cbs import cbs_player_meta_baseball
            cbs_player_meta_baseball(player_id=1751796)

        See Also:
            * `CBS Sports NAPI registry`_ - the API's own self-documenting endpoint catalog (data origin)
            * `nflfastR`_ - R sister package; NAPI is cross-sport, this is its NFL counterpart

        .. _CBS Sports NAPI registry: https://api.cbssports.com/napi/resource/endpoint/registry
        .. _nflfastR: https://www.nflfastr.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {}
    _params.update(_caller_params)
    raw = _get(
        f"https://api.cbssports.com/napi/resource/player/meta/baseball/{player_id}",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_cbs_napi(raw, return_as_pandas=return_as_pandas)
    return raw


def cbs_player_meta_golf(
    player_id: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Get a metadata resource for a particular golf player.

    Endpoint: ``GET https://api.cbssports.com/napi/resource/player/meta/golf/{player_id}``
    Example URL: https://api.cbssports.com/napi/resource/player/meta/golf/1751796

    Args:
        player_id: Numerical player ID
        return_parsed: parse the payload through parse_cbs_napi -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries. An unknown id is NOT an exception -- NAPI answers it with HTTP 200 plus an ``{error|errors|warnings}`` envelope, which parses to a zero-row frame.

    Example:
        Quick start::

            from sportsdataverse.cbs import cbs_player_meta_golf
            cbs_player_meta_golf(player_id=1751796)

        See Also:
            * `CBS Sports NAPI registry`_ - the API's own self-documenting endpoint catalog (data origin)
            * `nflfastR`_ - R sister package; NAPI is cross-sport, this is its NFL counterpart

        .. _CBS Sports NAPI registry: https://api.cbssports.com/napi/resource/endpoint/registry
        .. _nflfastR: https://www.nflfastr.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {}
    _params.update(_caller_params)
    raw = _get(
        f"https://api.cbssports.com/napi/resource/player/meta/golf/{player_id}",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_cbs_napi(raw, return_as_pandas=return_as_pandas)
    return raw


def cbs_player_outlook(
    player_id: int,
    date_format: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Get outlook for a player (context is now)

    Endpoint: ``GET https://api.cbssports.com/napi/resource/player/outlook/{player_id}``
    Example URL: https://api.cbssports.com/napi/resource/player/outlook/1751796

    Args:
        player_id: Numerical player ID
        date_format: Optional format for dateCreated field. Available options here: http://momentjs.com/docs/#/displaying/format/
        return_parsed: parse the payload through parse_cbs_napi -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries. An unknown id is NOT an exception -- NAPI answers it with HTTP 200 plus an ``{error|errors|warnings}`` envelope, which parses to a zero-row frame.

    Example:
        Quick start::

            from sportsdataverse.cbs import cbs_player_outlook
            cbs_player_outlook(player_id=1751796)

        See Also:
            * `CBS Sports NAPI registry`_ - the API's own self-documenting endpoint catalog (data origin)
            * `nflfastR`_ - R sister package; NAPI is cross-sport, this is its NFL counterpart

        .. _CBS Sports NAPI registry: https://api.cbssports.com/napi/resource/endpoint/registry
        .. _nflfastR: https://www.nflfastr.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "dateFormat": date_format,
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://api.cbssports.com/napi/resource/player/outlook/{player_id}",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_cbs_napi(raw, return_as_pandas=return_as_pandas)
    return raw


def cbs_player_position_rankings(
    player_id: int,
    position: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Get position rankings for a player

    Endpoint: ``GET https://api.cbssports.com/napi/resource/player/positionRankings/{player_id}``
    Example URL: https://api.cbssports.com/napi/resource/player/positionRankings/1751796

    Args:
        player_id: Numerical player ID
        position: Filter by position
        return_parsed: parse the payload through parse_cbs_napi -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries. An unknown id is NOT an exception -- NAPI answers it with HTTP 200 plus an ``{error|errors|warnings}`` envelope, which parses to a zero-row frame.

    Example:
        Quick start::

            from sportsdataverse.cbs import cbs_player_position_rankings
            cbs_player_position_rankings(player_id=1751796)

        See Also:
            * `CBS Sports NAPI registry`_ - the API's own self-documenting endpoint catalog (data origin)
            * `nflfastR`_ - R sister package; NAPI is cross-sport, this is its NFL counterpart

        .. _CBS Sports NAPI registry: https://api.cbssports.com/napi/resource/endpoint/registry
        .. _nflfastR: https://www.nflfastr.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "position": position,
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://api.cbssports.com/napi/resource/player/positionRankings/{player_id}",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_cbs_napi(raw, return_as_pandas=return_as_pandas)
    return raw


def cbs_player_rankings(
    player_id: int,
    season_year: Optional[int] = None,
    season_type: Optional[str] = None,
    season_id: Optional[str] = None,
    is_current: Optional[str] = None,
    categories: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Get all rankings for a player.

    Endpoint: ``GET https://api.cbssports.com/napi/resource/player/rankings/{player_id}``
    Example URL: https://api.cbssports.com/napi/resource/player/rankings/1751796

    Args:
        player_id: Numerical player ID
        season_year: View option.  Filter by seasonYear.
        season_type: View option.  Filter by seasonType. Allowed: regular, pre, post.
        season_id: View option.  Filter by seasonId.
        is_current: View option.  Only show stats for seasons where isCurrent is true. Allowed: 1.
        categories: View option.  Only return the specified rankings categories.
        return_parsed: parse the payload through parse_cbs_napi -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries. An unknown id is NOT an exception -- NAPI answers it with HTTP 200 plus an ``{error|errors|warnings}`` envelope, which parses to a zero-row frame.

    Example:
        Quick start::

            from sportsdataverse.cbs import cbs_player_rankings
            cbs_player_rankings(player_id=1751796)

        See Also:
            * `CBS Sports NAPI registry`_ - the API's own self-documenting endpoint catalog (data origin)
            * `nflfastR`_ - R sister package; NAPI is cross-sport, this is its NFL counterpart

        .. _CBS Sports NAPI registry: https://api.cbssports.com/napi/resource/endpoint/registry
        .. _nflfastR: https://www.nflfastr.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "seasonYear": season_year,
        "seasonType": season_type,
        "seasonId": season_id,
        "isCurrent": is_current,
        "categories": categories,
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://api.cbssports.com/napi/resource/player/rankings/{player_id}",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_cbs_napi(raw, return_as_pandas=return_as_pandas)
    return raw


def cbs_player_recruit_associations(
    player_id: int,
    resources: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Get recruit associations for a particular player.

    Endpoint: ``GET https://api.cbssports.com/napi/resource/player/recruitAssociations/{player_id}``
    Example URL: https://api.cbssports.com/napi/resource/player/recruitAssociations/1751796

    Args:
        player_id: Numerical player ID
        resources: Specify specific sub-resources to resolve. Defaults to none. Allowed: team.
        return_parsed: parse the payload through parse_cbs_napi -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries. An unknown id is NOT an exception -- NAPI answers it with HTTP 200 plus an ``{error|errors|warnings}`` envelope, which parses to a zero-row frame.

    Example:
        Quick start::

            from sportsdataverse.cbs import cbs_player_recruit_associations
            cbs_player_recruit_associations(player_id=1751796)

        See Also:
            * `CBS Sports NAPI registry`_ - the API's own self-documenting endpoint catalog (data origin)
            * `nflfastR`_ - R sister package; NAPI is cross-sport, this is its NFL counterpart

        .. _CBS Sports NAPI registry: https://api.cbssports.com/napi/resource/endpoint/registry
        .. _nflfastR: https://www.nflfastr.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "resources": resources,
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://api.cbssports.com/napi/resource/player/recruitAssociations/{player_id}",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_cbs_napi(raw, return_as_pandas=return_as_pandas)
    return raw


def cbs_player_standings(
    player_id: int,
    season_year: Optional[int] = None,
    season_type: Optional[str] = None,
    season_id: Optional[str] = None,
    is_current: Optional[str] = None,
    resources: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Get standings for a particular player

    Endpoint: ``GET https://api.cbssports.com/napi/resource/player/standings/{player_id}``
    Example URL: https://api.cbssports.com/napi/resource/player/standings/1751796

    Args:
        player_id: Numerical player ID
        season_year: View option.  Filter by seasonYear.
        season_type: View option.  Filter by seasonType. Allowed: regular, pre, post.
        season_id: View option.  Filter by seasonId.
        is_current: View option.  Only show standings for seasons where isCurrent is true. Allowed: 1.
        resources: Specify specific sub-resources to resolve.  Defaults to none. Allowed: league.
        return_parsed: parse the payload through parse_cbs_napi -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries. An unknown id is NOT an exception -- NAPI answers it with HTTP 200 plus an ``{error|errors|warnings}`` envelope, which parses to a zero-row frame.

    Example:
        Quick start::

            from sportsdataverse.cbs import cbs_player_standings
            cbs_player_standings(player_id=1751796)

        See Also:
            * `CBS Sports NAPI registry`_ - the API's own self-documenting endpoint catalog (data origin)
            * `nflfastR`_ - R sister package; NAPI is cross-sport, this is its NFL counterpart

        .. _CBS Sports NAPI registry: https://api.cbssports.com/napi/resource/endpoint/registry
        .. _nflfastR: https://www.nflfastr.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "seasonYear": season_year,
        "seasonType": season_type,
        "seasonId": season_id,
        "isCurrent": is_current,
        "resources": resources,
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://api.cbssports.com/napi/resource/player/standings/{player_id}",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_cbs_napi(raw, return_as_pandas=return_as_pandas)
    return raw


def cbs_player_stats(
    player_id: int,
    season_year: Optional[int] = None,
    season_type: Optional[str] = None,
    season_id: Optional[str] = None,
    is_current: Optional[str] = None,
    team_id: Optional[int] = None,
    team_abbr: Optional[str] = None,
    is_total: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Get all statistics for a player.

    Endpoint: ``GET https://api.cbssports.com/napi/resource/player/stats/{player_id}``
    Example URL: https://api.cbssports.com/napi/resource/player/stats/1751796

    Args:
        player_id: Numerical player ID
        season_year: View option.  Filter by seasonYear.
        season_type: View option.  Filter by seasonType. Allowed: regular, pre, post.
        season_id: View option.  Filter by seasonId.
        is_current: View option.  Only show stats for seasons where isCurrent is true. Allowed: 1.
        team_id: View option.  Filter by teamId.
        team_abbr: View option.  Filter by a specific team abbreviation.
        is_total: View option.  Filter only the isTotal record for players who played for multiple teams. Allowed: 1.
        return_parsed: parse the payload through parse_cbs_napi -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries. An unknown id is NOT an exception -- NAPI answers it with HTTP 200 plus an ``{error|errors|warnings}`` envelope, which parses to a zero-row frame.

    Example:
        Quick start::

            from sportsdataverse.cbs import cbs_player_stats
            cbs_player_stats(player_id=1751796)

        See Also:
            * `CBS Sports NAPI registry`_ - the API's own self-documenting endpoint catalog (data origin)
            * `nflfastR`_ - R sister package; NAPI is cross-sport, this is its NFL counterpart

        .. _CBS Sports NAPI registry: https://api.cbssports.com/napi/resource/endpoint/registry
        .. _nflfastR: https://www.nflfastr.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "seasonYear": season_year,
        "seasonType": season_type,
        "seasonId": season_id,
        "isCurrent": is_current,
        "teamId": team_id,
        "teamAbbr": team_abbr,
        "isTotal": is_total,
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://api.cbssports.com/napi/resource/player/stats/{player_id}",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_cbs_napi(raw, return_as_pandas=return_as_pandas)
    return raw


def cbs_player_team_associations(
    player_id: int,
    assoc_type: Optional[str] = None,
    roster_status: Optional[str] = None,
    resources: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Get team associations for a particular player.

    Endpoint: ``GET https://api.cbssports.com/napi/resource/player/teamAssociations/{player_id}``
    Example URL: https://api.cbssports.com/napi/resource/player/teamAssociations/1751796

    Args:
        player_id: Numerical player ID
        assoc_type: Filter associations by assoc type Allowed: C, H, S, F.
        roster_status: Filter associations by roster status Allowed: ACT, NWT, MIN, MNR, RET, DEV, CUT, DIS, DL, IR, UFA, UDF, EXE, TRA, SUS, PUP, FA, RFA, KIA, INA.
        resources: Specify specific sub-resources to resolve. Defaults to none. Allowed: team.
        return_parsed: parse the payload through parse_cbs_napi -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries. An unknown id is NOT an exception -- NAPI answers it with HTTP 200 plus an ``{error|errors|warnings}`` envelope, which parses to a zero-row frame.

    Example:
        Quick start::

            from sportsdataverse.cbs import cbs_player_team_associations
            cbs_player_team_associations(player_id=1751796)

        See Also:
            * `CBS Sports NAPI registry`_ - the API's own self-documenting endpoint catalog (data origin)
            * `nflfastR`_ - R sister package; NAPI is cross-sport, this is its NFL counterpart

        .. _CBS Sports NAPI registry: https://api.cbssports.com/napi/resource/endpoint/registry
        .. _nflfastR: https://www.nflfastr.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "assocType": assoc_type,
        "rosterStatus": roster_status,
        "resources": resources,
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://api.cbssports.com/napi/resource/player/teamAssociations/{player_id}",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_cbs_napi(raw, return_as_pandas=return_as_pandas)
    return raw


def cbs_player_transactions(
    player_id: int,
    date_format: Optional[str] = None,
    season_year: Optional[int] = None,
    season_type: Optional[str] = None,
    season_id: Optional[str] = None,
    resources: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Get transactions resource for a particular player.

    Endpoint: ``GET https://api.cbssports.com/napi/resource/player/transactions/{player_id}``
    Example URL: https://api.cbssports.com/napi/resource/player/transactions/1751796

    Args:
        player_id: Numerical player ID
        date_format: Optional.  Options here: http://momentjs.com/docs/#/displaying/format/
        season_year: View option.  Filter by seasonYear.
        season_type: View option.  Filter by seasonType. Allowed: regular, pre, post.
        season_id: View option.  Filter by seasonId.
        resources: Specify specific sub-resources to resolve. Defaults to none. Allowed: targetTeam, currentTeam, fromTeam.
        return_parsed: parse the payload through parse_cbs_napi -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries. An unknown id is NOT an exception -- NAPI answers it with HTTP 200 plus an ``{error|errors|warnings}`` envelope, which parses to a zero-row frame.

    Example:
        Quick start::

            from sportsdataverse.cbs import cbs_player_transactions
            cbs_player_transactions(player_id=1751796)

        See Also:
            * `CBS Sports NAPI registry`_ - the API's own self-documenting endpoint catalog (data origin)
            * `nflfastR`_ - R sister package; NAPI is cross-sport, this is its NFL counterpart

        .. _CBS Sports NAPI registry: https://api.cbssports.com/napi/resource/endpoint/registry
        .. _nflfastR: https://www.nflfastr.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "dateFormat": date_format,
        "seasonYear": season_year,
        "seasonType": season_type,
        "seasonId": season_id,
        "resources": resources,
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://api.cbssports.com/napi/resource/player/transactions/{player_id}",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_cbs_napi(raw, return_as_pandas=return_as_pandas)
    return raw


def cbs_recruit_rankings(
    player_id: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Get rankings resource for a recruit.

    Endpoint: ``GET https://api.cbssports.com/napi/resource/recruit/rankings/{player_id}``
    Example URL: https://api.cbssports.com/napi/resource/recruit/rankings/1751796

    Args:
        player_id: Numerical player ID
        return_parsed: parse the payload through parse_cbs_napi -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries. An unknown id is NOT an exception -- NAPI answers it with HTTP 200 plus an ``{error|errors|warnings}`` envelope, which parses to a zero-row frame.

    Example:
        Quick start::

            from sportsdataverse.cbs import cbs_recruit_rankings
            cbs_recruit_rankings(player_id=1751796)

        See Also:
            * `CBS Sports NAPI registry`_ - the API's own self-documenting endpoint catalog (data origin)
            * `nflfastR`_ - R sister package; NAPI is cross-sport, this is its NFL counterpart

        .. _CBS Sports NAPI registry: https://api.cbssports.com/napi/resource/endpoint/registry
        .. _nflfastR: https://www.nflfastr.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {}
    _params.update(_caller_params)
    raw = _get(
        f"https://api.cbssports.com/napi/resource/recruit/rankings/{player_id}",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_cbs_napi(raw, return_as_pandas=return_as_pandas)
    return raw


def cbs_season(
    season_id: int,
    date_format: Optional[str] = None,
    resources: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Get a season resource from Atlas.

    Endpoint: ``GET https://api.cbssports.com/napi/resource/season/{season_id}``
    Example URL: https://api.cbssports.com/napi/resource/season/59

    Args:
        season_id: Numerical season ID
        date_format: Optional.  Options here: http://momentjs.com/docs/#/displaying/format/
        resources: Specify specific sub-resources to resolve.  Defaults to none. Allowed: sport, league, teams.
        return_parsed: parse the payload through parse_cbs_napi -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries. An unknown id is NOT an exception -- NAPI answers it with HTTP 200 plus an ``{error|errors|warnings}`` envelope, which parses to a zero-row frame.

    Example:
        Quick start::

            from sportsdataverse.cbs import cbs_season
            cbs_season(season_id=59)

        See Also:
            * `CBS Sports NAPI registry`_ - the API's own self-documenting endpoint catalog (data origin)
            * `nflfastR`_ - R sister package; NAPI is cross-sport, this is its NFL counterpart

        .. _CBS Sports NAPI registry: https://api.cbssports.com/napi/resource/endpoint/registry
        .. _nflfastR: https://www.nflfastr.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "dateFormat": date_format,
        "resources": resources,
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://api.cbssports.com/napi/resource/season/{season_id}",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_cbs_napi(raw, return_as_pandas=return_as_pandas)
    return raw


def cbs_season_teams(
    season_id: int,
    resources: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Get team resources associated to a season

    Endpoint: ``GET https://api.cbssports.com/napi/resource/season/teams/{season_id}``
    Example URL: https://api.cbssports.com/napi/resource/season/teams/59

    Args:
        season_id: Optional seasonYear for leagues that change teams each year.
        resources: Specify specific sub-resources to resolve.  Defaults to none. Allowed: players, standings, conference, division, playerTeamAssociations, injuries, transactions, depthCharts, polls, teamSeasons, sportsLineStandings, league.
        return_parsed: parse the payload through parse_cbs_napi -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries. An unknown id is NOT an exception -- NAPI answers it with HTTP 200 plus an ``{error|errors|warnings}`` envelope, which parses to a zero-row frame.

    Example:
        Quick start::

            from sportsdataverse.cbs import cbs_season_teams
            cbs_season_teams(season_id=59)

        See Also:
            * `CBS Sports NAPI registry`_ - the API's own self-documenting endpoint catalog (data origin)
            * `nflfastR`_ - R sister package; NAPI is cross-sport, this is its NFL counterpart

        .. _CBS Sports NAPI registry: https://api.cbssports.com/napi/resource/endpoint/registry
        .. _nflfastR: https://www.nflfastr.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "resources": resources,
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://api.cbssports.com/napi/resource/season/teams/{season_id}",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_cbs_napi(raw, return_as_pandas=return_as_pandas)
    return raw


def cbs_sport(
    sport_id: int,
    resources: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Get a sport resource from Atlas.

    Endpoint: ``GET https://api.cbssports.com/napi/resource/sport/{sport_id}``
    Example URL: https://api.cbssports.com/napi/resource/sport/1

    Args:
        sport_id: Numerical sport ID
        resources: Specify specific sub-resources to resolve.  Defaults to none. Allowed: leagues.
        return_parsed: parse the payload through parse_cbs_napi -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries. An unknown id is NOT an exception -- NAPI answers it with HTTP 200 plus an ``{error|errors|warnings}`` envelope, which parses to a zero-row frame.

    Example:
        Quick start::

            from sportsdataverse.cbs import cbs_sport
            cbs_sport(sport_id=1)

        See Also:
            * `CBS Sports NAPI registry`_ - the API's own self-documenting endpoint catalog (data origin)
            * `nflfastR`_ - R sister package; NAPI is cross-sport, this is its NFL counterpart

        .. _CBS Sports NAPI registry: https://api.cbssports.com/napi/resource/endpoint/registry
        .. _nflfastR: https://www.nflfastr.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "resources": resources,
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://api.cbssports.com/napi/resource/sport/{sport_id}",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_cbs_napi(raw, return_as_pandas=return_as_pandas)
    return raw


def cbs_sport_leagues(
    sport_id: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Get league resources for a sport.

    Endpoint: ``GET https://api.cbssports.com/napi/resource/sport/leagues/{sport_id}``
    Example URL: https://api.cbssports.com/napi/resource/sport/leagues/1

    Args:
        sport_id: Numerical league ID
        return_parsed: parse the payload through parse_cbs_napi -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries. An unknown id is NOT an exception -- NAPI answers it with HTTP 200 plus an ``{error|errors|warnings}`` envelope, which parses to a zero-row frame.

    Example:
        Quick start::

            from sportsdataverse.cbs import cbs_sport_leagues
            cbs_sport_leagues(sport_id=1)

        See Also:
            * `CBS Sports NAPI registry`_ - the API's own self-documenting endpoint catalog (data origin)
            * `nflfastR`_ - R sister package; NAPI is cross-sport, this is its NFL counterpart

        .. _CBS Sports NAPI registry: https://api.cbssports.com/napi/resource/endpoint/registry
        .. _nflfastR: https://www.nflfastr.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {}
    _params.update(_caller_params)
    raw = _get(
        f"https://api.cbssports.com/napi/resource/sport/leagues/{sport_id}",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_cbs_napi(raw, return_as_pandas=return_as_pandas)
    return raw


def cbs_team_futures(
    team_id: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Get futures for a team.

    Endpoint: ``GET https://api.cbssports.com/napi/resource/team/futures/{team_id}``
    Example URL: https://api.cbssports.com/napi/resource/team/futures/404

    Args:
        team_id: Numerical team ID
        return_parsed: parse the payload through parse_cbs_napi -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries. An unknown id is NOT an exception -- NAPI answers it with HTTP 200 plus an ``{error|errors|warnings}`` envelope, which parses to a zero-row frame.

    Example:
        Quick start::

            from sportsdataverse.cbs import cbs_team_futures
            cbs_team_futures(team_id=404)

        See Also:
            * `CBS Sports NAPI registry`_ - the API's own self-documenting endpoint catalog (data origin)
            * `nflfastR`_ - R sister package; NAPI is cross-sport, this is its NFL counterpart

        .. _CBS Sports NAPI registry: https://api.cbssports.com/napi/resource/endpoint/registry
        .. _nflfastR: https://www.nflfastr.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {}
    _params.update(_caller_params)
    raw = _get(
        f"https://api.cbssports.com/napi/resource/team/futures/{team_id}",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_cbs_napi(raw, return_as_pandas=return_as_pandas)
    return raw


def cbs_team_metadata(
    team_id: int,
    resources: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Get a team metadata resource

    Endpoint: ``GET https://api.cbssports.com/napi/resource/team/metadata/{team_id}``
    Example URL: https://api.cbssports.com/napi/resource/team/metadata/404

    Args:
        team_id: Numerical team ID
        resources: Specify specific sub-resources to resolve. Defaults to none. Allowed: team.
        return_parsed: parse the payload through parse_cbs_napi -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries. An unknown id is NOT an exception -- NAPI answers it with HTTP 200 plus an ``{error|errors|warnings}`` envelope, which parses to a zero-row frame.

    Example:
        Quick start::

            from sportsdataverse.cbs import cbs_team_metadata
            cbs_team_metadata(team_id=404)

        See Also:
            * `CBS Sports NAPI registry`_ - the API's own self-documenting endpoint catalog (data origin)
            * `nflfastR`_ - R sister package; NAPI is cross-sport, this is its NFL counterpart

        .. _CBS Sports NAPI registry: https://api.cbssports.com/napi/resource/endpoint/registry
        .. _nflfastR: https://www.nflfastr.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "resources": resources,
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://api.cbssports.com/napi/resource/team/metadata/{team_id}",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_cbs_napi(raw, return_as_pandas=return_as_pandas)
    return raw


def cbs_team_players(
    team_id: int,
    resources: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Get player resources on a team.

    Endpoint: ``GET https://api.cbssports.com/napi/resource/team/players/{team_id}``
    Example URL: https://api.cbssports.com/napi/resource/team/players/404

    Args:
        team_id: Numerical team ID
        resources: Specify specific sub-resources to resolve.  Defaults to none. Allowed: playerTeamAssociations, injuries, transactions, depthCharts.
        return_parsed: parse the payload through parse_cbs_napi -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries. An unknown id is NOT an exception -- NAPI answers it with HTTP 200 plus an ``{error|errors|warnings}`` envelope, which parses to a zero-row frame.

    Example:
        Quick start::

            from sportsdataverse.cbs import cbs_team_players
            cbs_team_players(team_id=404)

        See Also:
            * `CBS Sports NAPI registry`_ - the API's own self-documenting endpoint catalog (data origin)
            * `nflfastR`_ - R sister package; NAPI is cross-sport, this is its NFL counterpart

        .. _CBS Sports NAPI registry: https://api.cbssports.com/napi/resource/endpoint/registry
        .. _nflfastR: https://www.nflfastr.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "resources": resources,
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://api.cbssports.com/napi/resource/team/players/{team_id}",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_cbs_napi(raw, return_as_pandas=return_as_pandas)
    return raw


def cbs_team_polls(
    team_id: int,
    polls: Optional[str] = None,
    season_id: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Retrieve team rankings data from our various polls.

    Endpoint: ``GET https://api.cbssports.com/napi/resource/team/polls/{team_id}``
    Example URL: https://api.cbssports.com/napi/resource/team/polls/404

    Args:
        team_id: Numerical team ID
        polls: View option.  Filter by a certain poll name. Allowed: coaches, ap, fcscoachespoll, statstsnfcspoll, rpi, playoffselectioncommitteepoll, net.
        season_id: View option. Filter by seasonId.
        return_parsed: parse the payload through parse_cbs_napi -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries. An unknown id is NOT an exception -- NAPI answers it with HTTP 200 plus an ``{error|errors|warnings}`` envelope, which parses to a zero-row frame.

    Example:
        Quick start::

            from sportsdataverse.cbs import cbs_team_polls
            cbs_team_polls(team_id=404)

        See Also:
            * `CBS Sports NAPI registry`_ - the API's own self-documenting endpoint catalog (data origin)
            * `nflfastR`_ - R sister package; NAPI is cross-sport, this is its NFL counterpart

        .. _CBS Sports NAPI registry: https://api.cbssports.com/napi/resource/endpoint/registry
        .. _nflfastR: https://www.nflfastr.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "polls": polls,
        "seasonId": season_id,
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://api.cbssports.com/napi/resource/team/polls/{team_id}",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_cbs_napi(raw, return_as_pandas=return_as_pandas)
    return raw


def cbs_team_rankings(
    team_id: int,
    season_year: Optional[int] = None,
    season_type: Optional[str] = None,
    season_id: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Get rankings for a team (by season)

    Endpoint: ``GET https://api.cbssports.com/napi/resource/team/rankings/{team_id}``
    Example URL: https://api.cbssports.com/napi/resource/team/rankings/404

    Args:
        team_id: Numerical team id
        season_year: View option.  Filter by seasonYear.
        season_type: View option.  Filter by seasonType. Allowed: regular, pre, post.
        season_id: View option.  Filter by seasonId.
        return_parsed: parse the payload through parse_cbs_napi -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries. An unknown id is NOT an exception -- NAPI answers it with HTTP 200 plus an ``{error|errors|warnings}`` envelope, which parses to a zero-row frame.

    Example:
        Quick start::

            from sportsdataverse.cbs import cbs_team_rankings
            cbs_team_rankings(team_id=404)

        See Also:
            * `CBS Sports NAPI registry`_ - the API's own self-documenting endpoint catalog (data origin)
            * `nflfastR`_ - R sister package; NAPI is cross-sport, this is its NFL counterpart

        .. _CBS Sports NAPI registry: https://api.cbssports.com/napi/resource/endpoint/registry
        .. _nflfastR: https://www.nflfastr.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "seasonYear": season_year,
        "seasonType": season_type,
        "seasonId": season_id,
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://api.cbssports.com/napi/resource/team/rankings/{team_id}",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_cbs_napi(raw, return_as_pandas=return_as_pandas)
    return raw


def cbs_team_rankings_sportsline(
    team_id: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Get sportsline rankings for a team

    Endpoint: ``GET https://api.cbssports.com/napi/resource/team/rankings/sportsline/{team_id}``
    Example URL: https://api.cbssports.com/napi/resource/team/rankings/sportsline/404

    Args:
        team_id: Numerical team id
        return_parsed: parse the payload through parse_cbs_napi -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries. An unknown id is NOT an exception -- NAPI answers it with HTTP 200 plus an ``{error|errors|warnings}`` envelope, which parses to a zero-row frame.

    Example:
        Quick start::

            from sportsdataverse.cbs import cbs_team_rankings_sportsline
            cbs_team_rankings_sportsline(team_id=404)

        See Also:
            * `CBS Sports NAPI registry`_ - the API's own self-documenting endpoint catalog (data origin)
            * `nflfastR`_ - R sister package; NAPI is cross-sport, this is its NFL counterpart

        .. _CBS Sports NAPI registry: https://api.cbssports.com/napi/resource/endpoint/registry
        .. _nflfastR: https://www.nflfastr.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {}
    _params.update(_caller_params)
    raw = _get(
        f"https://api.cbssports.com/napi/resource/team/rankings/sportsline/{team_id}",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_cbs_napi(raw, return_as_pandas=return_as_pandas)
    return raw


def cbs_team_seasons(
    team_id: int,
    date_format: Optional[str] = None,
    season_year: Optional[int] = None,
    season_type: Optional[str] = None,
    season_id: Optional[str] = None,
    resources: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Get a list of Season resources associated to a Team.

    Endpoint: ``GET https://api.cbssports.com/napi/resource/team/seasons/{team_id}``
    Example URL: https://api.cbssports.com/napi/resource/team/seasons/404

    Args:
        team_id: Numerical team ID
        date_format: Optional.  Options here: http://momentjs.com/docs/#/displaying/format/
        season_year: View option.  Filter by seasonYear.
        season_type: View option.  Filter by seasonType. Allowed: regular, pre, post.
        season_id: View option.  Filter by seasonId.
        resources: Specify specific sub-resources to resolve.  Defaults to none. Allowed: league.
        return_parsed: parse the payload through parse_cbs_napi -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries. An unknown id is NOT an exception -- NAPI answers it with HTTP 200 plus an ``{error|errors|warnings}`` envelope, which parses to a zero-row frame.

    Example:
        Quick start::

            from sportsdataverse.cbs import cbs_team_seasons
            cbs_team_seasons(team_id=404)

        See Also:
            * `CBS Sports NAPI registry`_ - the API's own self-documenting endpoint catalog (data origin)
            * `nflfastR`_ - R sister package; NAPI is cross-sport, this is its NFL counterpart

        .. _CBS Sports NAPI registry: https://api.cbssports.com/napi/resource/endpoint/registry
        .. _nflfastR: https://www.nflfastr.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "dateFormat": date_format,
        "seasonYear": season_year,
        "seasonType": season_type,
        "seasonId": season_id,
        "resources": resources,
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://api.cbssports.com/napi/resource/team/seasons/{team_id}",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_cbs_napi(raw, return_as_pandas=return_as_pandas)
    return raw


def cbs_team_standings(
    team_id: int,
    year: Optional[int] = None,
    season_type: Optional[str] = None,
    season_id: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Get standings for a particular team.

    Endpoint: ``GET https://api.cbssports.com/napi/resource/team/standings/{team_id}``
    Example URL: https://api.cbssports.com/napi/resource/team/standings/404

    Args:
        team_id: Numerical team ID
        year: Optional year in YYYY format
        season_type: View option.  Filter by seasonType. v3 only! Allowed: regular, pre, post.
        season_id: View option.  Filter by seasonId. v3 only!
        return_parsed: parse the payload through parse_cbs_napi_standings -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries. An unknown id is NOT an exception -- NAPI answers it with HTTP 200 plus an ``{error|errors|warnings}`` envelope, which parses to a zero-row frame.

    Example:
        Quick start::

            from sportsdataverse.cbs import cbs_team_standings
            cbs_team_standings(team_id=404)

        See Also:
            * `CBS Sports NAPI registry`_ - the API's own self-documenting endpoint catalog (data origin)
            * `nflfastR`_ - R sister package; NAPI is cross-sport, this is its NFL counterpart

        .. _CBS Sports NAPI registry: https://api.cbssports.com/napi/resource/endpoint/registry
        .. _nflfastR: https://www.nflfastr.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "year": year,
        "seasonType": season_type,
        "seasonId": season_id,
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://api.cbssports.com/napi/resource/team/standings/{team_id}",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_cbs_napi_standings(raw, return_as_pandas=return_as_pandas)
    return raw


def cbs_team_standings_sportsline(
    team_id: int,
    date_format: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Get SportsLine standings for a particular team.

    Endpoint: ``GET https://api.cbssports.com/napi/resource/team/standings/sportsline/{team_id}``
    Example URL: https://api.cbssports.com/napi/resource/team/standings/sportsline/404

    Args:
        team_id: Numerical team ID
        date_format: Optional.  Options here: http://momentjs.com/docs/#/displaying/format/
        return_parsed: parse the payload through parse_cbs_napi -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries. An unknown id is NOT an exception -- NAPI answers it with HTTP 200 plus an ``{error|errors|warnings}`` envelope, which parses to a zero-row frame.

    Example:
        Quick start::

            from sportsdataverse.cbs import cbs_team_standings_sportsline
            cbs_team_standings_sportsline(team_id=404)

        See Also:
            * `CBS Sports NAPI registry`_ - the API's own self-documenting endpoint catalog (data origin)
            * `nflfastR`_ - R sister package; NAPI is cross-sport, this is its NFL counterpart

        .. _CBS Sports NAPI registry: https://api.cbssports.com/napi/resource/endpoint/registry
        .. _nflfastR: https://www.nflfastr.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "dateFormat": date_format,
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://api.cbssports.com/napi/resource/team/standings/sportsline/{team_id}",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_cbs_napi(raw, return_as_pandas=return_as_pandas)
    return raw


def cbs_team_stats(
    team_id: int,
    season_year: Optional[int] = None,
    season_type: Optional[str] = None,
    season_id: Optional[str] = None,
    is_current: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Get all statistics for a team.

    Endpoint: ``GET https://api.cbssports.com/napi/resource/team/stats/{team_id}``
    Example URL: https://api.cbssports.com/napi/resource/team/stats/404

    Args:
        team_id: Numerical team ID
        season_year: View option.  Filter by seasonYear.
        season_type: View option.  Filter by seasonType. Allowed: regular, pre, post.
        season_id: View option.  Filter by seasonId.
        is_current: View option.  Only show stats for seasons where isCurrent is true. Allowed: 1.
        return_parsed: parse the payload through parse_cbs_napi -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries. An unknown id is NOT an exception -- NAPI answers it with HTTP 200 plus an ``{error|errors|warnings}`` envelope, which parses to a zero-row frame.

    Example:
        Quick start::

            from sportsdataverse.cbs import cbs_team_stats
            cbs_team_stats(team_id=404)

        See Also:
            * `CBS Sports NAPI registry`_ - the API's own self-documenting endpoint catalog (data origin)
            * `nflfastR`_ - R sister package; NAPI is cross-sport, this is its NFL counterpart

        .. _CBS Sports NAPI registry: https://api.cbssports.com/napi/resource/endpoint/registry
        .. _nflfastR: https://www.nflfastr.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "seasonYear": season_year,
        "seasonType": season_type,
        "seasonId": season_id,
        "isCurrent": is_current,
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://api.cbssports.com/napi/resource/team/stats/{team_id}",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_cbs_napi(raw, return_as_pandas=return_as_pandas)
    return raw


def cbs_venue(
    venue_id: int,
    resources: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Get a venue resource

    Endpoint: ``GET https://api.cbssports.com/napi/resource/venue/{venue_id}``
    Example URL: https://api.cbssports.com/napi/resource/venue

    Args:
        venue_id: Numerical venue ID
        resources: Specify specific sub-resources to resolve. Defaults to none. Allowed: metaData.
        return_parsed: parse the payload through parse_cbs_napi -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries. An unknown id is NOT an exception -- NAPI answers it with HTTP 200 plus an ``{error|errors|warnings}`` envelope, which parses to a zero-row frame.

    Example:
        Quick start::

            from sportsdataverse.cbs import cbs_venue
            cbs_venue()

        See Also:
            * `CBS Sports NAPI registry`_ - the API's own self-documenting endpoint catalog (data origin)
            * `nflfastR`_ - R sister package; NAPI is cross-sport, this is its NFL counterpart

        .. _CBS Sports NAPI registry: https://api.cbssports.com/napi/resource/endpoint/registry
        .. _nflfastR: https://www.nflfastr.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "resources": resources,
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://api.cbssports.com/napi/resource/venue/{venue_id}",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_cbs_napi(raw, return_as_pandas=return_as_pandas)
    return raw


def cbs_venue_metadata(
    venue_id: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Get a venues metadata resource

    Endpoint: ``GET https://api.cbssports.com/napi/resource/venue/metadata/{venue_id}``
    Example URL: https://api.cbssports.com/napi/resource/venue/metadata

    Args:
        venue_id: Numerical venue ID
        return_parsed: parse the payload through parse_cbs_napi -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries. An unknown id is NOT an exception -- NAPI answers it with HTTP 200 plus an ``{error|errors|warnings}`` envelope, which parses to a zero-row frame.

    Example:
        Quick start::

            from sportsdataverse.cbs import cbs_venue_metadata
            cbs_venue_metadata()

        See Also:
            * `CBS Sports NAPI registry`_ - the API's own self-documenting endpoint catalog (data origin)
            * `nflfastR`_ - R sister package; NAPI is cross-sport, this is its NFL counterpart

        .. _CBS Sports NAPI registry: https://api.cbssports.com/napi/resource/endpoint/registry
        .. _nflfastR: https://www.nflfastr.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {}
    _params.update(_caller_params)
    raw = _get(
        f"https://api.cbssports.com/napi/resource/venue/metadata/{venue_id}",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_cbs_napi(raw, return_as_pandas=return_as_pandas)
    return raw
