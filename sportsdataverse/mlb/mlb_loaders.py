# GENERATED by tools/codegen/generate.py -- DO NOT EDIT.
# Source: tools/codegen/endpoints/releases.yaml
"""sportsdataverse.mlb dataset loaders (sportsdataverse-data releases). Generated."""

from __future__ import annotations

import polars as pl

from sportsdataverse._codegen_runtime import (
    SeasonNotFoundError,  # noqa: F401
    _as_season_list,
    _cast_ids_int64,  # noqa: F401  (used only by loaders declaring id_int64)
    _read_release_parquet,
    cli_warn,
)

__all__ = [
    "load_mlb_re24_matrix",
    "load_mlb_we_table",
    "load_mlb_wpa",
    "load_mlb_expected_stats",
    "load_mlb_expected_hr",
    "load_mlb_batter_projection",
    "load_mlb_oaa",
    "load_mlb_catcher_framing",
    "load_mlb_xera",
    "load_mlb_stuff_plus",
    "load_mlb_command_plus",
    "load_ncaa_baseball_pbp",
    "load_ncaa_baseball_schedule",
]


def load_mlb_re24_matrix(seasons, return_as_pandas: bool = False):
    """Load mlb_game_state (sportsdataverse-data release).

    Source: https://github.com/sportsdataverse/sportsdataverse-data/releases/tag/mlb_game_state

    Args:
        seasons: an int or iterable of seasons (>= 2015).
        return_as_pandas: return a pandas DataFrame instead of polars.

    Returns:
        A polars (or pandas) DataFrame; seasons with no published asset are
        skipped with a warning rather than raising (404-safe).

        |col_name   |type    |
        |:----------|:-------|
        |base_state |String  |
        |outs       |Int64   |
        |re         |Float64 |
        |n          |UInt32  |
        |season     |Int64   |

    Example:
        Quick start::

            load_mlb_re24_matrix(seasons=2024)
    """
    frames, missing = [], []
    for season in _as_season_list(seasons):
        if int(season) < 2015:
            raise SeasonNotFoundError("season cannot be less than 2015")
        df = _read_release_parquet(
            f"https://github.com/sportsdataverse/sportsdataverse-data/releases/download/mlb_game_state/mlb_re24_matrix_{season}.parquet"
        )
        if df is None:
            missing.append(season)
            continue
        frames.append(df)
    if missing:
        cli_warn("load_mlb_re24_matrix: no data for season(s) {missing} (skipped)".format(missing=missing))
    # diagonal: per-season release schemas can drift (columns added/dropped
    # over the years) -- union columns, null-fill gaps.
    out = pl.concat(frames, how="diagonal_relaxed") if frames else pl.DataFrame()
    return out.to_pandas(use_pyarrow_extension_array=True) if return_as_pandas else out


def load_mlb_we_table(seasons, return_as_pandas: bool = False):
    """Load mlb_game_state (sportsdataverse-data release).

    Source: https://github.com/sportsdataverse/sportsdataverse-data/releases/tag/mlb_game_state

    Args:
        seasons: an int or iterable of seasons (>= 2015).
        return_as_pandas: return a pandas DataFrame instead of polars.

    Returns:
        A polars (or pandas) DataFrame; seasons with no published asset are
        skipped with a warning rather than raising (404-safe).

        |col_name          |type    |
        |:-----------------|:-------|
        |inning_capped     |Int64   |
        |half              |String  |
        |base_state        |String  |
        |outs_start        |Int64   |
        |score_diff_bucket |Int64   |
        |home_win_exp      |Float64 |
        |n                 |UInt32  |
        |season            |Int64   |

    Example:
        Quick start::

            load_mlb_we_table(seasons=2024)
    """
    frames, missing = [], []
    for season in _as_season_list(seasons):
        if int(season) < 2015:
            raise SeasonNotFoundError("season cannot be less than 2015")
        df = _read_release_parquet(
            f"https://github.com/sportsdataverse/sportsdataverse-data/releases/download/mlb_game_state/mlb_we_table_{season}.parquet"
        )
        if df is None:
            missing.append(season)
            continue
        frames.append(df)
    if missing:
        cli_warn("load_mlb_we_table: no data for season(s) {missing} (skipped)".format(missing=missing))
    # diagonal: per-season release schemas can drift (columns added/dropped
    # over the years) -- union columns, null-fill gaps.
    out = pl.concat(frames, how="diagonal_relaxed") if frames else pl.DataFrame()
    return out.to_pandas(use_pyarrow_extension_array=True) if return_as_pandas else out


def load_mlb_wpa(seasons, return_as_pandas: bool = False):
    """Load mlb_game_state (sportsdataverse-data release).

    Source: https://github.com/sportsdataverse/sportsdataverse-data/releases/tag/mlb_game_state

    Args:
        seasons: an int or iterable of seasons (>= 2015).
        return_as_pandas: return a pandas DataFrame instead of polars.

    Returns:
        A polars (or pandas) DataFrame; seasons with no published asset are
        skipped with a warning rather than raising (404-safe).

        |col_name     |type    |
        |:------------|:-------|
        |game_id      |String  |
        |at_bat_index |Int64   |
        |wpa          |Float64 |
        |season       |Int64   |

    Example:
        Quick start::

            load_mlb_wpa(seasons=2024)
    """
    frames, missing = [], []
    for season in _as_season_list(seasons):
        if int(season) < 2015:
            raise SeasonNotFoundError("season cannot be less than 2015")
        df = _read_release_parquet(
            f"https://github.com/sportsdataverse/sportsdataverse-data/releases/download/mlb_game_state/mlb_wpa_{season}.parquet"
        )
        if df is None:
            missing.append(season)
            continue
        frames.append(df)
    if missing:
        cli_warn("load_mlb_wpa: no data for season(s) {missing} (skipped)".format(missing=missing))
    # diagonal: per-season release schemas can drift (columns added/dropped
    # over the years) -- union columns, null-fill gaps.
    out = pl.concat(frames, how="diagonal_relaxed") if frames else pl.DataFrame()
    return out.to_pandas(use_pyarrow_extension_array=True) if return_as_pandas else out


def load_mlb_expected_stats(seasons, return_as_pandas: bool = False):
    """Load mlb_hitting_models (sportsdataverse-data release).

    Source: https://github.com/sportsdataverse/sportsdataverse-data/releases/tag/mlb_hitting_models

    Args:
        seasons: an int or iterable of seasons (>= 2015).
        return_as_pandas: return a pandas DataFrame instead of polars.

    Returns:
        A polars (or pandas) DataFrame; seasons with no published asset are
        skipped with a warning rather than raising (404-safe).

        |col_name |type    |
        |:--------|:-------|
        |batter   |Int64   |
        |season   |Int64   |
        |pa       |Int64   |
        |ab       |Int64   |
        |xwoba    |Float64 |
        |xba      |Float64 |
        |xslg     |Float64 |

    Example:
        Quick start::

            load_mlb_expected_stats(seasons=2024)
    """
    frames, missing = [], []
    for season in _as_season_list(seasons):
        if int(season) < 2015:
            raise SeasonNotFoundError("season cannot be less than 2015")
        df = _read_release_parquet(
            f"https://github.com/sportsdataverse/sportsdataverse-data/releases/download/mlb_hitting_models/mlb_expected_stats_{season}.parquet"
        )
        if df is None:
            missing.append(season)
            continue
        frames.append(df)
    if missing:
        cli_warn("load_mlb_expected_stats: no data for season(s) {missing} (skipped)".format(missing=missing))
    # diagonal: per-season release schemas can drift (columns added/dropped
    # over the years) -- union columns, null-fill gaps.
    out = pl.concat(frames, how="diagonal_relaxed") if frames else pl.DataFrame()
    return out.to_pandas(use_pyarrow_extension_array=True) if return_as_pandas else out


def load_mlb_expected_hr(seasons, return_as_pandas: bool = False):
    """Load mlb_hitting_models (sportsdataverse-data release).

    Source: https://github.com/sportsdataverse/sportsdataverse-data/releases/tag/mlb_hitting_models

    Args:
        seasons: an int or iterable of seasons (>= 2015).
        return_as_pandas: return a pandas DataFrame instead of polars.

    Returns:
        A polars (or pandas) DataFrame; seasons with no published asset are
        skipped with a warning rather than raising (404-safe).

        |col_name          |type    |
        |:-----------------|:-------|
        |batter            |Int64   |
        |season            |Int64   |
        |hr                |Int64   |
        |xhr_neutral       |Float64 |
        |xhr_park_adj      |Float64 |
        |hr_above_expected |Float64 |

    Example:
        Quick start::

            load_mlb_expected_hr(seasons=2024)
    """
    frames, missing = [], []
    for season in _as_season_list(seasons):
        if int(season) < 2015:
            raise SeasonNotFoundError("season cannot be less than 2015")
        df = _read_release_parquet(
            f"https://github.com/sportsdataverse/sportsdataverse-data/releases/download/mlb_hitting_models/mlb_expected_hr_{season}.parquet"
        )
        if df is None:
            missing.append(season)
            continue
        frames.append(df)
    if missing:
        cli_warn("load_mlb_expected_hr: no data for season(s) {missing} (skipped)".format(missing=missing))
    # diagonal: per-season release schemas can drift (columns added/dropped
    # over the years) -- union columns, null-fill gaps.
    out = pl.concat(frames, how="diagonal_relaxed") if frames else pl.DataFrame()
    return out.to_pandas(use_pyarrow_extension_array=True) if return_as_pandas else out


def load_mlb_batter_projection(seasons, return_as_pandas: bool = False):
    """Load mlb_hitting_models (sportsdataverse-data release).

    Source: https://github.com/sportsdataverse/sportsdataverse-data/releases/tag/mlb_hitting_models

    Args:
        seasons: an int or iterable of seasons (>= 2016).
        return_as_pandas: return a pandas DataFrame instead of polars.

    Returns:
        A polars (or pandas) DataFrame; seasons with no published asset are
        skipped with a warning rather than raising (404-safe).

        |col_name   |type    |
        |:----------|:-------|
        |batter     |Int64   |
        |age        |Int64   |
        |proj_xwoba |Float64 |
        |proj_pa    |Float64 |

    Example:
        Quick start::

            load_mlb_batter_projection(seasons=2024)
    """
    frames, missing = [], []
    for season in _as_season_list(seasons):
        if int(season) < 2016:
            raise SeasonNotFoundError("season cannot be less than 2016")
        df = _read_release_parquet(
            f"https://github.com/sportsdataverse/sportsdataverse-data/releases/download/mlb_hitting_models/mlb_batter_projection_{season}.parquet"
        )
        if df is None:
            missing.append(season)
            continue
        frames.append(df)
    if missing:
        cli_warn("load_mlb_batter_projection: no data for season(s) {missing} (skipped)".format(missing=missing))
    # diagonal: per-season release schemas can drift (columns added/dropped
    # over the years) -- union columns, null-fill gaps.
    out = pl.concat(frames, how="diagonal_relaxed") if frames else pl.DataFrame()
    return out.to_pandas(use_pyarrow_extension_array=True) if return_as_pandas else out


def load_mlb_oaa(seasons, return_as_pandas: bool = False):
    """Load mlb_fielding_models (sportsdataverse-data release).

    Source: https://github.com/sportsdataverse/sportsdataverse-data/releases/tag/mlb_fielding_models

    Args:
        seasons: an int or iterable of seasons (>= 2015).
        return_as_pandas: return a pandas DataFrame instead of polars.

    Returns:
        A polars (or pandas) DataFrame; seasons with no published asset are
        skipped with a warning rather than raising (404-safe).

        |col_name      |type    |
        |:-------------|:-------|
        |fielder_id    |String  |
        |position      |Int64   |
        |opportunities |UInt32  |
        |oaa           |Float64 |
        |season        |Int64   |

    Example:
        Quick start::

            load_mlb_oaa(seasons=2024)
    """
    frames, missing = [], []
    for season in _as_season_list(seasons):
        if int(season) < 2015:
            raise SeasonNotFoundError("season cannot be less than 2015")
        df = _read_release_parquet(
            f"https://github.com/sportsdataverse/sportsdataverse-data/releases/download/mlb_fielding_models/mlb_oaa_{season}.parquet"
        )
        if df is None:
            missing.append(season)
            continue
        frames.append(df)
    if missing:
        cli_warn("load_mlb_oaa: no data for season(s) {missing} (skipped)".format(missing=missing))
    # diagonal: per-season release schemas can drift (columns added/dropped
    # over the years) -- union columns, null-fill gaps.
    out = pl.concat(frames, how="diagonal_relaxed") if frames else pl.DataFrame()
    return out.to_pandas(use_pyarrow_extension_array=True) if return_as_pandas else out


def load_mlb_catcher_framing(seasons, return_as_pandas: bool = False):
    """Load mlb_fielding_models (sportsdataverse-data release).

    Source: https://github.com/sportsdataverse/sportsdataverse-data/releases/tag/mlb_fielding_models

    Args:
        seasons: an int or iterable of seasons (>= 2015).
        return_as_pandas: return a pandas DataFrame instead of polars.

    Returns:
        A polars (or pandas) DataFrame; seasons with no published asset are
        skipped with a warning rather than raising (404-safe).

        |col_name       |type    |
        |:--------------|:-------|
        |catcher_id     |String  |
        |takes          |UInt32  |
        |framing_runs   |Float64 |
        |strikes_gained |Float64 |
        |season         |Int64   |

    Example:
        Quick start::

            load_mlb_catcher_framing(seasons=2024)
    """
    frames, missing = [], []
    for season in _as_season_list(seasons):
        if int(season) < 2015:
            raise SeasonNotFoundError("season cannot be less than 2015")
        df = _read_release_parquet(
            f"https://github.com/sportsdataverse/sportsdataverse-data/releases/download/mlb_fielding_models/mlb_catcher_framing_{season}.parquet"
        )
        if df is None:
            missing.append(season)
            continue
        frames.append(df)
    if missing:
        cli_warn("load_mlb_catcher_framing: no data for season(s) {missing} (skipped)".format(missing=missing))
    # diagonal: per-season release schemas can drift (columns added/dropped
    # over the years) -- union columns, null-fill gaps.
    out = pl.concat(frames, how="diagonal_relaxed") if frames else pl.DataFrame()
    return out.to_pandas(use_pyarrow_extension_array=True) if return_as_pandas else out


def load_mlb_xera(seasons, return_as_pandas: bool = False):
    """Load mlb_pitching_models (sportsdataverse-data release).

    Source: https://github.com/sportsdataverse/sportsdataverse-data/releases/tag/mlb_pitching_models

    Args:
        seasons: an int or iterable of seasons (>= 2015).
        return_as_pandas: return a pandas DataFrame instead of polars.

    Returns:
        A polars (or pandas) DataFrame; seasons with no published asset are
        skipped with a warning rather than raising (404-safe).

        |col_name |type    |
        |:--------|:-------|
        |pitcher  |Int64   |
        |season   |Int64   |
        |x_woba   |Float64 |
        |x_era    |Float64 |

    Example:
        Quick start::

            load_mlb_xera(seasons=2024)
    """
    frames, missing = [], []
    for season in _as_season_list(seasons):
        if int(season) < 2015:
            raise SeasonNotFoundError("season cannot be less than 2015")
        df = _read_release_parquet(
            f"https://github.com/sportsdataverse/sportsdataverse-data/releases/download/mlb_pitching_models/mlb_xera_{season}.parquet"
        )
        if df is None:
            missing.append(season)
            continue
        frames.append(df)
    if missing:
        cli_warn("load_mlb_xera: no data for season(s) {missing} (skipped)".format(missing=missing))
    # diagonal: per-season release schemas can drift (columns added/dropped
    # over the years) -- union columns, null-fill gaps.
    out = pl.concat(frames, how="diagonal_relaxed") if frames else pl.DataFrame()
    return out.to_pandas(use_pyarrow_extension_array=True) if return_as_pandas else out


def load_mlb_stuff_plus(seasons, return_as_pandas: bool = False):
    """Load mlb_pitching_models (sportsdataverse-data release).

    Source: https://github.com/sportsdataverse/sportsdataverse-data/releases/tag/mlb_pitching_models

    Args:
        seasons: an int or iterable of seasons (>= 2015).
        return_as_pandas: return a pandas DataFrame instead of polars.

    Returns:
        A polars (or pandas) DataFrame; seasons with no published asset are
        skipped with a warning rather than raising (404-safe).

        |col_name     |type    |
        |:------------|:-------|
        |pitcher      |Int64   |
        |pitch_type   |String  |
        |stuff_rv_hat |Float64 |
        |stuff_plus   |Float64 |
        |season       |Int64   |

    Example:
        Quick start::

            load_mlb_stuff_plus(seasons=2024)
    """
    frames, missing = [], []
    for season in _as_season_list(seasons):
        if int(season) < 2015:
            raise SeasonNotFoundError("season cannot be less than 2015")
        df = _read_release_parquet(
            f"https://github.com/sportsdataverse/sportsdataverse-data/releases/download/mlb_pitching_models/mlb_stuff_plus_{season}.parquet"
        )
        if df is None:
            missing.append(season)
            continue
        frames.append(df)
    if missing:
        cli_warn("load_mlb_stuff_plus: no data for season(s) {missing} (skipped)".format(missing=missing))
    # diagonal: per-season release schemas can drift (columns added/dropped
    # over the years) -- union columns, null-fill gaps.
    out = pl.concat(frames, how="diagonal_relaxed") if frames else pl.DataFrame()
    return out.to_pandas(use_pyarrow_extension_array=True) if return_as_pandas else out


def load_mlb_command_plus(seasons, return_as_pandas: bool = False):
    """Load mlb_pitching_models (sportsdataverse-data release).

    Source: https://github.com/sportsdataverse/sportsdataverse-data/releases/tag/mlb_pitching_models

    Args:
        seasons: an int or iterable of seasons (>= 2015).
        return_as_pandas: return a pandas DataFrame instead of polars.

    Returns:
        A polars (or pandas) DataFrame; seasons with no published asset are
        skipped with a warning rather than raising (404-safe).

        |col_name        |type    |
        |:---------------|:-------|
        |pitcher         |Int64   |
        |location_rv_hat |Float64 |
        |command_plus    |Float64 |
        |season          |Int64   |

    Example:
        Quick start::

            load_mlb_command_plus(seasons=2024)
    """
    frames, missing = [], []
    for season in _as_season_list(seasons):
        if int(season) < 2015:
            raise SeasonNotFoundError("season cannot be less than 2015")
        df = _read_release_parquet(
            f"https://github.com/sportsdataverse/sportsdataverse-data/releases/download/mlb_pitching_models/mlb_command_plus_{season}.parquet"
        )
        if df is None:
            missing.append(season)
            continue
        frames.append(df)
    if missing:
        cli_warn("load_mlb_command_plus: no data for season(s) {missing} (skipped)".format(missing=missing))
    # diagonal: per-season release schemas can drift (columns added/dropped
    # over the years) -- union columns, null-fill gaps.
    out = pl.concat(frames, how="diagonal_relaxed") if frames else pl.DataFrame()
    return out.to_pandas(use_pyarrow_extension_array=True) if return_as_pandas else out


def load_ncaa_baseball_pbp(seasons, return_as_pandas: bool = False):
    """Load ncaa_baseball_pbp (sportsdataverse-data release).

    Source: https://github.com/sportsdataverse/sportsdataverse-data/releases/tag/ncaa_baseball_pbp

    Args:
        seasons: an int or iterable of seasons (>= 2017).
        return_as_pandas: return a pandas DataFrame instead of polars.

    Returns:
        A polars (or pandas) DataFrame; seasons with no published asset are
        skipped with a warning rather than raising (404-safe).

        |col_name       |type    |
        |:--------------|:-------|
        |game_date      |String  |
        |location       |String  |
        |attendance     |Boolean |
        |inning         |String  |
        |inning_top_bot |String  |
        |score          |String  |
        |batting        |String  |
        |fielding       |String  |
        |description    |String  |
        |year           |Int32   |
        |game_pbp_url   |String  |
        |game_pbp_id    |Int32   |
        |game_info_url  |String  |
        |contest_id     |Int32   |

    Example:
        Quick start::

            load_ncaa_baseball_pbp(seasons=2023)
    """
    frames, missing = [], []
    for season in _as_season_list(seasons):
        if int(season) < 2017:
            raise SeasonNotFoundError("season cannot be less than 2017")
        df = _read_release_parquet(
            f"https://github.com/sportsdataverse/sportsdataverse-data/releases/download/ncaa_baseball_pbp/ncaa_baseball_pbp_{season}.parquet"
        )
        if df is None:
            missing.append(season)
            continue
        frames.append(df)
    if missing:
        cli_warn("load_ncaa_baseball_pbp: no data for season(s) {missing} (skipped)".format(missing=missing))
    # diagonal: per-season release schemas can drift (columns added/dropped
    # over the years) -- union columns, null-fill gaps.
    out = pl.concat(frames, how="diagonal_relaxed") if frames else pl.DataFrame()
    return out.to_pandas(use_pyarrow_extension_array=True) if return_as_pandas else out


def load_ncaa_baseball_schedule(seasons, return_as_pandas: bool = False):
    """Load ncaa_baseball_schedules (sportsdataverse-data release).

    Source: https://github.com/sportsdataverse/sportsdataverse-data/releases/tag/ncaa_baseball_schedules

    Args:
        seasons: an int or iterable of seasons (>= 2012).
        return_as_pandas: return a pandas DataFrame instead of polars.

    Returns:
        A polars (or pandas) DataFrame; seasons with no published asset are
        skipped with a warning rather than raising (404-safe).

        |col_name                |type   |
        |:-----------------------|:------|
        |year                    |Int32  |
        |season_id               |Int32  |
        |date                    |String |
        |home_team               |String |
        |home_team_id            |Int32  |
        |home_team_score         |Int32  |
        |home_team_conference    |String |
        |home_team_conference_id |Int32  |
        |home_team_slug          |String |
        |home_team_division      |Int32  |
        |away_team               |String |
        |away_team_id            |Int32  |
        |away_team_score         |Int32  |
        |away_team_conference    |String |
        |away_team_conference_id |Int32  |
        |away_team_slug          |String |
        |away_team_division      |Int32  |
        |neutral_site            |String |
        |innings                 |Int32  |
        |slug                    |String |
        |game_info_url           |String |
        |contest_id              |Int32  |

    Example:
        Quick start::

            load_ncaa_baseball_schedule(seasons=2023)
    """
    frames, missing = [], []
    for season in _as_season_list(seasons):
        if int(season) < 2012:
            raise SeasonNotFoundError("season cannot be less than 2012")
        df = _read_release_parquet(
            f"https://github.com/sportsdataverse/sportsdataverse-data/releases/download/ncaa_baseball_schedules/ncaa_baseball_schedule_{season}.parquet"
        )
        if df is None:
            missing.append(season)
            continue
        frames.append(df)
    if missing:
        cli_warn("load_ncaa_baseball_schedule: no data for season(s) {missing} (skipped)".format(missing=missing))
    # diagonal: per-season release schemas can drift (columns added/dropped
    # over the years) -- union columns, null-fill gaps.
    out = pl.concat(frames, how="diagonal_relaxed") if frames else pl.DataFrame()
    return out.to_pandas(use_pyarrow_extension_array=True) if return_as_pandas else out
