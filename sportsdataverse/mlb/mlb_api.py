# GENERATED by tools/codegen/generate.py -- DO NOT EDIT.
# Source: tools/codegen/endpoints/mlb_api.yaml
"""mlb_api wrappers (host https://statsapi.mlb.com). Generated."""

from __future__ import annotations

from typing import Dict, Optional, Union  # noqa: F401

from sportsdataverse._codegen_runtime import _get
from sportsdataverse.mlb.mlb_api_parsers import parse_mlb_api_list, parse_mlb_api_schedule, parse_mlb_api_team_roster

__all__ = [
    "mlb_api_schedule_postseason",
    "mlb_api_pbp",
    "mlb_api_boxscore",
    "mlb_api_linescore",
    "mlb_api_play_by_play",
    "mlb_api_game_context_metrics",
    "mlb_api_win_probability",
    "mlb_api_game_content",
    "mlb_api_team",
    "mlb_api_team_roster",
    "mlb_api_team_alumni",
    "mlb_api_team_affiliates",
    "mlb_api_people",
    "mlb_api_person",
    "mlb_api_person_game_stats",
    "mlb_api_sport_players",
    "mlb_api_sports",
    "mlb_api_leagues",
    "mlb_api_season",
    "mlb_api_venues",
    "mlb_api_venue",
    "mlb_api_meta",
    "mlb_api_awards",
    "mlb_api_award_recipients",
    "mlb_api_draft",
    "mlb_api_umpires",
]


def mlb_api_schedule_postseason(
    season: Optional[Union[int, str]] = None,
    sport_id: Optional[int] = 1,
    hydrate: Optional[str] = None,
    *,
    return_parsed: bool = False,
    return_as_pandas: bool = False,
    **kwargs,
) -> Dict:
    """GET /api/v1/schedule/postseason — postseason-only schedule for a season.

    Endpoint: ``GET https://statsapi.mlb.com/api/v1/schedule/postseason``
    Example URL: https://statsapi.mlb.com/api/v1/schedule/postseason

    Args:
        season: season query parameter.
        sport_id: sportId query parameter.
        hydrate: hydrate query parameter.
        return_parsed: dispatch the raw payload through parse_mlb_api_schedule -> polars DataFrame.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        polars/pandas DataFrame when ``return_parsed=True``, else the raw JSON ``Dict``.

    Example:
        >>> mlb_api_schedule_postseason()
    """
    raw = _get(
        "https://statsapi.mlb.com/api/v1/schedule/postseason",
        params={
            "season": season,
            "sportId": sport_id,
            "hydrate": hydrate,
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_mlb_api_schedule(raw, return_as_pandas=return_as_pandas)
    return raw


def mlb_api_pbp(
    game_pk: int,
    language: Optional[str] = None,
    timecode: Optional[str] = None,
    hydrate: Optional[str] = None,
    fields: Optional[str] = None,
    *,
    return_parsed: bool = False,
    return_as_pandas: bool = False,
    **kwargs,
) -> Dict:
    """GET /api/v1.1/game/{gamePk}/feed/live — live firehose (v1.1).

    Endpoint: ``GET https://statsapi.mlb.com/api/v1.1/game/{game_pk}/feed/live``
    Example URL: https://statsapi.mlb.com/api/v1.1/game/716390/feed/live

    Args:
        game_pk: game_pk path parameter.
        language: language query parameter.
        timecode: language query parameter.
        hydrate: hydrate query parameter.
        fields: fields query parameter.
        return_parsed: dispatch the raw payload through parse_mlb_api_list -> polars DataFrame.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        polars/pandas DataFrame when ``return_parsed=True``, else the raw JSON ``Dict``.

    Example:
        >>> mlb_api_pbp(game_pk=716390)
    """
    raw = _get(
        f"https://statsapi.mlb.com/api/v1.1/game/{game_pk}/feed/live",
        params={
            "language": language,
            "language": timecode,
            "hydrate": hydrate,
            "fields": fields,
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_mlb_api_list(raw, return_as_pandas=return_as_pandas)
    return raw


def mlb_api_boxscore(
    game_pk: int,
    timecode: Optional[str] = None,
    fields: Optional[str] = None,
    *,
    return_parsed: bool = False,
    return_as_pandas: bool = False,
    **kwargs,
) -> Dict:
    """GET /api/v1/game/{gamePk}/boxscore — team + player boxscore for one game.

    Endpoint: ``GET https://statsapi.mlb.com/api/v1/game/{game_pk}/boxscore``
    Example URL: https://statsapi.mlb.com/api/v1/game/716390/boxscore

    Args:
        game_pk: game_pk path parameter.
        timecode: timecode query parameter.
        fields: fields query parameter.
        return_parsed: dispatch the raw payload through parse_mlb_api_list -> polars DataFrame.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        polars/pandas DataFrame when ``return_parsed=True``, else the raw JSON ``Dict``.

    Example:
        >>> mlb_api_boxscore(game_pk=716390)
    """
    raw = _get(
        f"https://statsapi.mlb.com/api/v1/game/{game_pk}/boxscore",
        params={
            "timecode": timecode,
            "fields": fields,
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_mlb_api_list(raw, return_as_pandas=return_as_pandas)
    return raw


def mlb_api_linescore(
    game_pk: int,
    timecode: Optional[str] = None,
    fields: Optional[str] = None,
    *,
    return_parsed: bool = False,
    return_as_pandas: bool = False,
    **kwargs,
) -> Dict:
    """GET /api/v1/game/{gamePk}/linescore — inning-by-inning + current game state.

    Endpoint: ``GET https://statsapi.mlb.com/api/v1/game/{game_pk}/linescore``
    Example URL: https://statsapi.mlb.com/api/v1/game/716390/linescore

    Args:
        game_pk: game_pk path parameter.
        timecode: timecode query parameter.
        fields: fields query parameter.
        return_parsed: dispatch the raw payload through parse_mlb_api_list -> polars DataFrame.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        polars/pandas DataFrame when ``return_parsed=True``, else the raw JSON ``Dict``.

    Example:
        >>> mlb_api_linescore(game_pk=716390)
    """
    raw = _get(
        f"https://statsapi.mlb.com/api/v1/game/{game_pk}/linescore",
        params={
            "timecode": timecode,
            "fields": fields,
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_mlb_api_list(raw, return_as_pandas=return_as_pandas)
    return raw


def mlb_api_play_by_play(
    game_pk: int,
    timecode: Optional[str] = None,
    fields: Optional[str] = None,
    *,
    return_parsed: bool = False,
    return_as_pandas: bool = False,
    **kwargs,
) -> Dict:
    """GET /api/v1/game/{gamePk}/playByPlay — play-by-play with at-bat detail.

    Endpoint: ``GET https://statsapi.mlb.com/api/v1/game/{game_pk}/playByPlay``
    Example URL: https://statsapi.mlb.com/api/v1/game/716390/playByPlay

    Args:
        game_pk: game_pk path parameter.
        timecode: timecode query parameter.
        fields: fields query parameter.
        return_parsed: dispatch the raw payload through parse_mlb_api_list -> polars DataFrame.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        polars/pandas DataFrame when ``return_parsed=True``, else the raw JSON ``Dict``.

    Example:
        >>> mlb_api_play_by_play(game_pk=716390)
    """
    raw = _get(
        f"https://statsapi.mlb.com/api/v1/game/{game_pk}/playByPlay",
        params={
            "timecode": timecode,
            "fields": fields,
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_mlb_api_list(raw, return_as_pandas=return_as_pandas)
    return raw


def mlb_api_game_context_metrics(
    game_pk: int,
    fields: Optional[str] = None,
    *,
    return_parsed: bool = False,
    return_as_pandas: bool = False,
    **kwargs,
) -> Dict:
    """GET /api/v1/game/{gamePk}/contextMetrics — WP, leverage index, in-game context.

    Endpoint: ``GET https://statsapi.mlb.com/api/v1/game/{game_pk}/contextMetrics``
    Example URL: https://statsapi.mlb.com/api/v1/game/716390/contextMetrics

    Args:
        game_pk: game_pk path parameter.
        fields: fields query parameter.
        return_parsed: dispatch the raw payload through parse_mlb_api_list -> polars DataFrame.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        polars/pandas DataFrame when ``return_parsed=True``, else the raw JSON ``Dict``.

    Example:
        >>> mlb_api_game_context_metrics(game_pk=716390)
    """
    raw = _get(
        f"https://statsapi.mlb.com/api/v1/game/{game_pk}/contextMetrics",
        params={
            "fields": fields,
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_mlb_api_list(raw, return_as_pandas=return_as_pandas)
    return raw


def mlb_api_win_probability(
    game_pk: int,
    fields: Optional[str] = None,
    *,
    return_parsed: bool = False,
    return_as_pandas: bool = False,
    **kwargs,
) -> Dict:
    """GET /api/v1/game/{gamePk}/winProbability — per-play WP timeline.

    Endpoint: ``GET https://statsapi.mlb.com/api/v1/game/{game_pk}/winProbability``
    Example URL: https://statsapi.mlb.com/api/v1/game/716390/winProbability

    Args:
        game_pk: game_pk path parameter.
        fields: fields query parameter.
        return_parsed: dispatch the raw payload through parse_mlb_api_list -> polars DataFrame.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        polars/pandas DataFrame when ``return_parsed=True``, else the raw JSON ``Dict``.

    Example:
        >>> mlb_api_win_probability(game_pk=716390)
    """
    raw = _get(
        f"https://statsapi.mlb.com/api/v1/game/{game_pk}/winProbability",
        params={
            "fields": fields,
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_mlb_api_list(raw, return_as_pandas=return_as_pandas)
    return raw


def mlb_api_game_content(
    game_pk: int,
    *,
    return_parsed: bool = False,
    return_as_pandas: bool = False,
    **kwargs,
) -> Dict:
    """GET /api/v1/game/{gamePk}/content — articles, highlights, editorial content.

    Endpoint: ``GET https://statsapi.mlb.com/api/v1/game/{game_pk}/content``
    Example URL: https://statsapi.mlb.com/api/v1/game/716390/content

    Args:
        game_pk: game_pk path parameter.
        return_parsed: dispatch the raw payload through parse_mlb_api_list -> polars DataFrame.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        polars/pandas DataFrame when ``return_parsed=True``, else the raw JSON ``Dict``.

    Example:
        >>> mlb_api_game_content(game_pk=716390)
    """
    raw = _get(
        f"https://statsapi.mlb.com/api/v1/game/{game_pk}/content",
        params={
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_mlb_api_list(raw, return_as_pandas=return_as_pandas)
    return raw


def mlb_api_team(
    team_id: int,
    season: Optional[Union[int, str]] = None,
    sport_id: Optional[int] = 1,
    hydrate: Optional[str] = None,
    fields: Optional[str] = None,
    *,
    return_parsed: bool = False,
    return_as_pandas: bool = False,
    **kwargs,
) -> Dict:
    """GET /api/v1/teams/{teamId} — single team detail.

    Endpoint: ``GET https://statsapi.mlb.com/api/v1/teams/{team_id}``
    Example URL: https://statsapi.mlb.com/api/v1/teams/10

    Args:
        team_id: team_id path parameter.
        season: season query parameter.
        sport_id: sportId query parameter.
        hydrate: hydrate query parameter.
        fields: fields query parameter.
        return_parsed: dispatch the raw payload through parse_mlb_api_list -> polars DataFrame.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        polars/pandas DataFrame when ``return_parsed=True``, else the raw JSON ``Dict``.

    Example:
        >>> mlb_api_team(team_id=10)
    """
    raw = _get(
        f"https://statsapi.mlb.com/api/v1/teams/{team_id}",
        params={
            "season": season,
            "sportId": sport_id,
            "hydrate": hydrate,
            "fields": fields,
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_mlb_api_list(raw, return_as_pandas=return_as_pandas)
    return raw


def mlb_api_team_roster(
    team_id: int,
    season: Optional[Union[int, str]] = None,
    roster_type: Optional[str] = "active",
    date: Optional[str] = None,
    hydrate: Optional[str] = None,
    fields: Optional[str] = None,
    *,
    return_parsed: bool = False,
    return_as_pandas: bool = False,
    **kwargs,
) -> Dict:
    """GET /api/v1/teams/{teamId}/roster — team roster.

    Endpoint: ``GET https://statsapi.mlb.com/api/v1/teams/{team_id}/roster``
    Example URL: https://statsapi.mlb.com/api/v1/teams/10/roster

    Args:
        team_id: team_id path parameter.
        season: season query parameter.
        roster_type: rosterType query parameter.
        date: date query parameter.
        hydrate: hydrate query parameter.
        fields: fields query parameter.
        return_parsed: dispatch the raw payload through parse_mlb_api_team_roster -> polars DataFrame.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        polars/pandas DataFrame when ``return_parsed=True``, else the raw JSON ``Dict``.

    Example:
        >>> mlb_api_team_roster(team_id=10)
    """
    raw = _get(
        f"https://statsapi.mlb.com/api/v1/teams/{team_id}/roster",
        params={
            "season": season,
            "rosterType": roster_type,
            "date": date,
            "hydrate": hydrate,
            "fields": fields,
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_mlb_api_team_roster(raw, return_as_pandas=return_as_pandas)
    return raw


def mlb_api_team_alumni(
    team_id: int,
    season: Optional[Union[int, str]] = None,
    group: Optional[str] = "hitting",
    hydrate: Optional[str] = None,
    *,
    return_parsed: bool = False,
    return_as_pandas: bool = False,
    **kwargs,
) -> Dict:
    """GET /api/v1/teams/{teamId}/alumni — players who played for this team in a season.

    Endpoint: ``GET https://statsapi.mlb.com/api/v1/teams/{team_id}/alumni``
    Example URL: https://statsapi.mlb.com/api/v1/teams/10/alumni

    Args:
        team_id: team_id path parameter.
        season: season query parameter.
        group: group query parameter.
        hydrate: hydrate query parameter.
        return_parsed: dispatch the raw payload through parse_mlb_api_list -> polars DataFrame.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        polars/pandas DataFrame when ``return_parsed=True``, else the raw JSON ``Dict``.

    Example:
        >>> mlb_api_team_alumni(team_id=10)
    """
    raw = _get(
        f"https://statsapi.mlb.com/api/v1/teams/{team_id}/alumni",
        params={
            "season": season,
            "group": group,
            "hydrate": hydrate,
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_mlb_api_list(raw, return_as_pandas=return_as_pandas)
    return raw


def mlb_api_team_affiliates(
    team_ids: Optional[Union[int, List[int]]] = None,
    sport_id: Optional[int] = 1,
    season: Optional[Union[int, str]] = None,
    hydrate: Optional[str] = None,
    *,
    return_parsed: bool = False,
    return_as_pandas: bool = False,
    **kwargs,
) -> Dict:
    """GET /api/v1/teams/affiliates — org affiliates (MLB parent → minor league chain).

    Endpoint: ``GET https://statsapi.mlb.com/api/v1/teams/affiliates``
    Example URL: https://statsapi.mlb.com/api/v1/teams/affiliates

    Args:
        team_ids: teamIds query parameter.
        sport_id: sportId query parameter.
        season: season query parameter.
        hydrate: hydrate query parameter.
        return_parsed: dispatch the raw payload through parse_mlb_api_list -> polars DataFrame.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        polars/pandas DataFrame when ``return_parsed=True``, else the raw JSON ``Dict``.

    Example:
        >>> mlb_api_team_affiliates()
    """
    raw = _get(
        "https://statsapi.mlb.com/api/v1/teams/affiliates",
        params={
            "teamIds": team_ids,
            "sportId": sport_id,
            "season": season,
            "hydrate": hydrate,
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_mlb_api_list(raw, return_as_pandas=return_as_pandas)
    return raw


def mlb_api_people(
    person_ids: Optional[Union[int, List[int]]] = None,
    hydrate: Optional[str] = None,
    fields: Optional[str] = None,
    *,
    return_parsed: bool = False,
    return_as_pandas: bool = False,
    **kwargs,
) -> Dict:
    """GET /api/v1/people?personIds=... — bulk person lookup by MLBAM id.

    Endpoint: ``GET https://statsapi.mlb.com/api/v1/people``
    Example URL: https://statsapi.mlb.com/api/v1/people

    Args:
        person_ids: personIds query parameter.
        hydrate: hydrate query parameter.
        fields: fields query parameter.
        return_parsed: dispatch the raw payload through parse_mlb_api_list -> polars DataFrame.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        polars/pandas DataFrame when ``return_parsed=True``, else the raw JSON ``Dict``.

    Example:
        >>> mlb_api_people()
    """
    raw = _get(
        "https://statsapi.mlb.com/api/v1/people",
        params={
            "personIds": person_ids,
            "hydrate": hydrate,
            "fields": fields,
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_mlb_api_list(raw, return_as_pandas=return_as_pandas)
    return raw


def mlb_api_person(
    person_id: int,
    season: Optional[Union[int, str]] = None,
    hydrate: Optional[str] = None,
    fields: Optional[str] = None,
    *,
    return_parsed: bool = False,
    return_as_pandas: bool = False,
    **kwargs,
) -> Dict:
    """GET /api/v1/people/{personId} — single person detail.

    Endpoint: ``GET https://statsapi.mlb.com/api/v1/people/{person_id}``
    Example URL: https://statsapi.mlb.com/api/v1/people/660271

    Args:
        person_id: person_id path parameter.
        season: season query parameter.
        hydrate: hydrate query parameter.
        fields: fields query parameter.
        return_parsed: dispatch the raw payload through parse_mlb_api_list -> polars DataFrame.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        polars/pandas DataFrame when ``return_parsed=True``, else the raw JSON ``Dict``.

    Example:
        >>> mlb_api_person(person_id=660271)
    """
    raw = _get(
        f"https://statsapi.mlb.com/api/v1/people/{person_id}",
        params={
            "season": season,
            "hydrate": hydrate,
            "fields": fields,
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_mlb_api_list(raw, return_as_pandas=return_as_pandas)
    return raw


def mlb_api_person_game_stats(
    person_id: int,
    game_pk: Union[int, str],
    fields: Optional[str] = None,
    *,
    return_parsed: bool = False,
    return_as_pandas: bool = False,
    **kwargs,
) -> Dict:
    """GET /api/v1/people/{personId}/stats/game/{gamePk} — one player, one game.

    Endpoint: ``GET https://statsapi.mlb.com/api/v1/people/{person_id}/stats/game/{game_pk}``
    Example URL: https://statsapi.mlb.com/api/v1/people/660271/stats/game/716390

    Args:
        person_id: person_id path parameter.
        game_pk: game_pk path parameter.
        fields: fields query parameter.
        return_parsed: dispatch the raw payload through parse_mlb_api_list -> polars DataFrame.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        polars/pandas DataFrame when ``return_parsed=True``, else the raw JSON ``Dict``.

    Example:
        >>> mlb_api_person_game_stats(person_id=660271, game_pk=716390)
    """
    raw = _get(
        f"https://statsapi.mlb.com/api/v1/people/{person_id}/stats/game/{game_pk}",
        params={
            "fields": fields,
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_mlb_api_list(raw, return_as_pandas=return_as_pandas)
    return raw


def mlb_api_sport_players(
    sport_id: Optional[int] = 1,
    season: Optional[Union[int, str]] = None,
    hydrate: Optional[str] = None,
    fields: Optional[str] = None,
    *,
    return_parsed: bool = False,
    return_as_pandas: bool = False,
    **kwargs,
) -> Dict:
    """GET /api/v1/sports/{sportId}/players — every player in a sport for a season.

    Endpoint: ``GET https://statsapi.mlb.com/api/v1/sports/{sport_id}/players``
    Example URL: https://statsapi.mlb.com/api/v1/sports

    Args:
        sport_id: sport_id path parameter.
        season: season query parameter.
        hydrate: hydrate query parameter.
        fields: fields query parameter.
        return_parsed: dispatch the raw payload through parse_mlb_api_list -> polars DataFrame.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        polars/pandas DataFrame when ``return_parsed=True``, else the raw JSON ``Dict``.

    Example:
        >>> mlb_api_sport_players()
    """
    raw = _get(
        f"https://statsapi.mlb.com/api/v1/sports/{sport_id}/players",
        params={
            "season": season,
            "hydrate": hydrate,
            "fields": fields,
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_mlb_api_list(raw, return_as_pandas=return_as_pandas)
    return raw


def mlb_api_sports(
    sport_id: Optional[int] = None,
    *,
    return_parsed: bool = False,
    return_as_pandas: bool = False,
    **kwargs,
) -> Dict:
    """GET /api/v1/sports — list known sports (MLB, MiLB, KBO, NPB, …).

    Endpoint: ``GET https://statsapi.mlb.com/api/v1/sports``
    Example URL: https://statsapi.mlb.com/api/v1/sports

    Args:
        sport_id: sportId query parameter.
        return_parsed: dispatch the raw payload through parse_mlb_api_list -> polars DataFrame.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        polars/pandas DataFrame when ``return_parsed=True``, else the raw JSON ``Dict``.

    Example:
        >>> mlb_api_sports()
    """
    raw = _get(
        "https://statsapi.mlb.com/api/v1/sports",
        params={
            "sportId": sport_id,
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_mlb_api_list(raw, return_as_pandas=return_as_pandas)
    return raw


def mlb_api_leagues(
    sport_id: Optional[int] = 1,
    season: Optional[Union[int, str]] = None,
    league_ids: Optional[Union[int, List[int]]] = None,
    *,
    return_parsed: bool = False,
    return_as_pandas: bool = False,
    **kwargs,
) -> Dict:
    """GET /api/v1/leagues — list leagues.

    Endpoint: ``GET https://statsapi.mlb.com/api/v1/leagues``
    Example URL: https://statsapi.mlb.com/api/v1/leagues

    Args:
        sport_id: sportId query parameter.
        season: season query parameter.
        league_ids: leagueIds query parameter.
        return_parsed: dispatch the raw payload through parse_mlb_api_list -> polars DataFrame.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        polars/pandas DataFrame when ``return_parsed=True``, else the raw JSON ``Dict``.

    Example:
        >>> mlb_api_leagues()
    """
    raw = _get(
        "https://statsapi.mlb.com/api/v1/leagues",
        params={
            "sportId": sport_id,
            "season": season,
            "leagueIds": league_ids,
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_mlb_api_list(raw, return_as_pandas=return_as_pandas)
    return raw


def mlb_api_season(
    season_id: Union[int, str],
    sport_id: Optional[int] = 1,
    *,
    return_parsed: bool = False,
    return_as_pandas: bool = False,
    **kwargs,
) -> Dict:
    """GET /api/v1/seasons/{seasonId} — single season detail.

    Endpoint: ``GET https://statsapi.mlb.com/api/v1/seasons/{season_id}``
    Example URL: https://statsapi.mlb.com/api/v1/seasons/X

    Args:
        season_id: season_id path parameter.
        sport_id: sportId query parameter.
        return_parsed: dispatch the raw payload through parse_mlb_api_list -> polars DataFrame.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        polars/pandas DataFrame when ``return_parsed=True``, else the raw JSON ``Dict``.

    Example:
        >>> mlb_api_season(season_id='X')
    """
    raw = _get(
        f"https://statsapi.mlb.com/api/v1/seasons/{season_id}",
        params={
            "sportId": sport_id,
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_mlb_api_list(raw, return_as_pandas=return_as_pandas)
    return raw


def mlb_api_venues(
    season: Optional[Union[int, str]] = None,
    sport_ids: Optional[Union[int, List[int]]] = None,
    hydrate: Optional[str] = None,
    *,
    return_parsed: bool = False,
    return_as_pandas: bool = False,
    **kwargs,
) -> Dict:
    """GET /api/v1/venues — list venues.

    Endpoint: ``GET https://statsapi.mlb.com/api/v1/venues``
    Example URL: https://statsapi.mlb.com/api/v1/venues

    Args:
        season: season query parameter.
        sport_ids: sportIds query parameter.
        hydrate: hydrate query parameter.
        return_parsed: dispatch the raw payload through parse_mlb_api_list -> polars DataFrame.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        polars/pandas DataFrame when ``return_parsed=True``, else the raw JSON ``Dict``.

    Example:
        >>> mlb_api_venues()
    """
    raw = _get(
        "https://statsapi.mlb.com/api/v1/venues",
        params={
            "season": season,
            "sportIds": sport_ids,
            "hydrate": hydrate,
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_mlb_api_list(raw, return_as_pandas=return_as_pandas)
    return raw


def mlb_api_venue(
    venue_id: int,
    season: Optional[Union[int, str]] = None,
    hydrate: Optional[str] = None,
    *,
    return_parsed: bool = False,
    return_as_pandas: bool = False,
    **kwargs,
) -> Dict:
    """GET /api/v1/venues/{venueId} — single venue detail.

    Endpoint: ``GET https://statsapi.mlb.com/api/v1/venues/{venue_id}``
    Example URL: https://statsapi.mlb.com/api/v1/venues/15

    Args:
        venue_id: venue_id path parameter.
        season: season query parameter.
        hydrate: hydrate query parameter.
        return_parsed: dispatch the raw payload through parse_mlb_api_list -> polars DataFrame.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        polars/pandas DataFrame when ``return_parsed=True``, else the raw JSON ``Dict``.

    Example:
        >>> mlb_api_venue(venue_id=15)
    """
    raw = _get(
        f"https://statsapi.mlb.com/api/v1/venues/{venue_id}",
        params={
            "season": season,
            "hydrate": hydrate,
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_mlb_api_list(raw, return_as_pandas=return_as_pandas)
    return raw


def mlb_api_meta(
    meta_type: str,
    *,
    return_parsed: bool = False,
    return_as_pandas: bool = False,
    **kwargs,
) -> Dict:
    """GET /api/v1/{metaType} — enum lookup (the API's self-describing surface).

    Endpoint: ``GET https://statsapi.mlb.com/api/v1/{meta_type}``
    Example URL: https://statsapi.mlb.com/api/v1/leagueLeaderTypes

    Args:
        meta_type: meta_type path parameter.
        return_parsed: dispatch the raw payload through parse_mlb_api_list -> polars DataFrame.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        polars/pandas DataFrame when ``return_parsed=True``, else the raw JSON ``Dict``.

    Example:
        >>> mlb_api_meta(meta_type='leagueLeaderTypes')
    """
    raw = _get(
        f"https://statsapi.mlb.com/api/v1/{meta_type}",
        params={
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_mlb_api_list(raw, return_as_pandas=return_as_pandas)
    return raw


def mlb_api_awards(
    sport_id: Optional[int] = None,
    *,
    return_parsed: bool = False,
    return_as_pandas: bool = False,
    **kwargs,
) -> Dict:
    """GET /api/v1/awards — list award IDs (call with no params to enumerate).

    Endpoint: ``GET https://statsapi.mlb.com/api/v1/awards``
    Example URL: https://statsapi.mlb.com/api/v1/awards

    Args:
        sport_id: sportId query parameter.
        return_parsed: dispatch the raw payload through parse_mlb_api_list -> polars DataFrame.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        polars/pandas DataFrame when ``return_parsed=True``, else the raw JSON ``Dict``.

    Example:
        >>> mlb_api_awards()
    """
    raw = _get(
        "https://statsapi.mlb.com/api/v1/awards",
        params={
            "sportId": sport_id,
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_mlb_api_list(raw, return_as_pandas=return_as_pandas)
    return raw


def mlb_api_award_recipients(
    award_id: str,
    season: Optional[Union[int, str]] = None,
    sport_id: Optional[int] = 1,
    hydrate: Optional[str] = None,
    *,
    return_parsed: bool = False,
    return_as_pandas: bool = False,
    **kwargs,
) -> Dict:
    """GET /api/v1/awards/{awardId}/recipients — historical winners of one award.

    Endpoint: ``GET https://statsapi.mlb.com/api/v1/awards/{award_id}/recipients``
    Example URL: https://statsapi.mlb.com/api/v1/awards/MLBHOF/recipients

    Args:
        award_id: award_id path parameter.
        season: season query parameter.
        sport_id: sportId query parameter.
        hydrate: hydrate query parameter.
        return_parsed: dispatch the raw payload through parse_mlb_api_list -> polars DataFrame.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        polars/pandas DataFrame when ``return_parsed=True``, else the raw JSON ``Dict``.

    Example:
        >>> mlb_api_award_recipients(award_id='MLBHOF')
    """
    raw = _get(
        f"https://statsapi.mlb.com/api/v1/awards/{award_id}/recipients",
        params={
            "season": season,
            "sportId": sport_id,
            "hydrate": hydrate,
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_mlb_api_list(raw, return_as_pandas=return_as_pandas)
    return raw


def mlb_api_draft(
    year: Union[int, str],
    round_: Optional[Union[int, str]] = None,
    team_id: Optional[int] = None,
    player_id: Optional[int] = None,
    limit: Optional[int] = 100,
    *,
    return_parsed: bool = False,
    return_as_pandas: bool = False,
    **kwargs,
) -> Dict:
    """GET /api/v1/draft/{year} — draft results for a year (optionally one round).

    Endpoint: ``GET https://statsapi.mlb.com/api/v1/draft/{year}``
    Example URL: https://statsapi.mlb.com/api/v1/draft/2024

    Args:
        year: year path parameter.
        round_: round query parameter.
        team_id: teamId query parameter.
        player_id: playerId query parameter.
        limit: limit query parameter.
        return_parsed: dispatch the raw payload through parse_mlb_api_list -> polars DataFrame.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        polars/pandas DataFrame when ``return_parsed=True``, else the raw JSON ``Dict``.

    Example:
        >>> mlb_api_draft(year=2024)
    """
    raw = _get(
        f"https://statsapi.mlb.com/api/v1/draft/{year}",
        params={
            "round": round_,
            "teamId": team_id,
            "playerId": player_id,
            "limit": limit,
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_mlb_api_list(raw, return_as_pandas=return_as_pandas)
    return raw


def mlb_api_umpires(
    *,
    return_parsed: bool = False,
    return_as_pandas: bool = False,
    **kwargs,
) -> Dict:
    """GET /api/v1/jobs/umpires — current umpire crew assignments.

    Endpoint: ``GET https://statsapi.mlb.com/api/v1/jobs/umpires``
    Example URL: https://statsapi.mlb.com/api/v1/jobs/umpires

    Args:
        return_parsed: dispatch the raw payload through parse_mlb_api_list -> polars DataFrame.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        polars/pandas DataFrame when ``return_parsed=True``, else the raw JSON ``Dict``.

    Example:
        >>> mlb_api_umpires()
    """
    raw = _get(
        "https://statsapi.mlb.com/api/v1/jobs/umpires",
        params={
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_mlb_api_list(raw, return_as_pandas=return_as_pandas)
    return raw
