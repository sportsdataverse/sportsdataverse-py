# GENERATED by tools/codegen/generate.py -- DO NOT EDIT.
# Source: tools/codegen/endpoints/mls_api.yaml
"""mls_api wrappers (host https://stats-api.mlssoccer.com). Generated."""

from __future__ import annotations

from typing import TYPE_CHECKING, Dict, List, Optional, Union  # noqa: F401

from sportsdataverse.soccer.mls.mls_api_runtime import _get, bool_str
from sportsdataverse.soccer.mls.mls_api_parsers import (
    parse_mls_api,
    parse_mls_entity,
    parse_mls_match,
    parse_mls_standings,
)

if TYPE_CHECKING:  # pragma: no cover -- annotation-only imports (PEP 563 defers eval)
    import pandas as pd
    import polars as pl

__all__ = [
    "mls_club",
    "mls_competition_seasons",
    "mls_competitions",
    "mls_content_season",
    "mls_content_seasons",
    "mls_match",
    "mls_season_matches",
    "mls_sportapi_club_players",
    "mls_sportapi_clubs_by_sportec_ids",
    "mls_sportapi_match",
    "mls_sportapi_matches_by_sportec_ids",
    "mls_standings",
]


def mls_club(
    club_id: str,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Single club detail (stats-api).

    Endpoint: ``GET https://stats-api.mlssoccer.com/clubs/{club_id}``
    Example URL: https://stats-api.mlssoccer.com/clubs/MLS-CLU-000001

    Args:
        club_id: club_id path parameter.
        return_parsed: parse the payload through parse_mls_entity -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        sportsdataverse.errors.NoESPNDataError: the MLS host returned 404 (unknown id, or an unplayed match on ``/matches/{matchId}``).
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.mls import mls_club
            mls_club(club_id='MLS-CLU-000001')

        See Also:
            * `MLS`_ - the public site these hosts render
            * `American Soccer Analysis`_ - advanced MLS xG / g+ metrics (``asa_*`` wrappers)

        .. _MLS: https://www.mlssoccer.com/
        .. _American Soccer Analysis: https://www.americansocceranalysis.com/
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://stats-api.mlssoccer.com/clubs/{club_id}",
        params=_params,
    )
    if return_parsed:
        return parse_mls_entity(raw, return_as_pandas=return_as_pandas)
    return raw


def mls_competition_seasons(
    competition_id: str,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """List seasons for a competition (stats-api).

    Endpoint: ``GET https://stats-api.mlssoccer.com/competitions/{competition_id}/seasons``
    Example URL: https://stats-api.mlssoccer.com/competitions/MLS-COM-000001/seasons

    Args:
        competition_id: competition_id path parameter.
        return_parsed: parse the payload through parse_mls_api -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        sportsdataverse.errors.NoESPNDataError: the MLS host returned 404 (unknown id, or an unplayed match on ``/matches/{matchId}``).
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.mls import mls_competition_seasons
            mls_competition_seasons(competition_id='MLS-COM-000001')

        See Also:
            * `MLS`_ - the public site these hosts render
            * `American Soccer Analysis`_ - advanced MLS xG / g+ metrics (``asa_*`` wrappers)

        .. _MLS: https://www.mlssoccer.com/
        .. _American Soccer Analysis: https://www.americansocceranalysis.com/
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://stats-api.mlssoccer.com/competitions/{competition_id}/seasons",
        params=_params,
    )
    if return_parsed:
        return parse_mls_api(raw, return_as_pandas=return_as_pandas)
    return raw


def mls_competitions(
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """List competitions (stats-api).

    Endpoint: ``GET https://stats-api.mlssoccer.com/competitions``
    Example URL: https://stats-api.mlssoccer.com/competitions

    Args:
        return_parsed: parse the payload through parse_mls_api -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        sportsdataverse.errors.NoESPNDataError: the MLS host returned 404 (unknown id, or an unplayed match on ``/matches/{matchId}``).
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.mls import mls_competitions
            mls_competitions()

        See Also:
            * `MLS`_ - the public site these hosts render
            * `American Soccer Analysis`_ - advanced MLS xG / g+ metrics (``asa_*`` wrappers)

        .. _MLS: https://www.mlssoccer.com/
        .. _American Soccer Analysis: https://www.americansocceranalysis.com/
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        "https://stats-api.mlssoccer.com/competitions",
        params=_params,
    )
    if return_parsed:
        return parse_mls_api(raw, return_as_pandas=return_as_pandas)
    return raw


def mls_content_season(
    slug: str,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Season content entity by slug (dapi / Contentful).

    Endpoint: ``GET https://dapi.mlssoccer.com/v2/content/en-us/seasons/{slug}``
    Example URL: https://dapi.mlssoccer.com/v2/content/en-us/seasons/mls-regular-season-2026

    Args:
        slug: slug path parameter.
        return_parsed: parse the payload through parse_mls_entity -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        sportsdataverse.errors.NoESPNDataError: the MLS host returned 404 (unknown id, or an unplayed match on ``/matches/{matchId}``).
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.mls import mls_content_season
            mls_content_season(slug='mls-regular-season-2026')

        See Also:
            * `MLS`_ - the public site these hosts render
            * `American Soccer Analysis`_ - advanced MLS xG / g+ metrics (``asa_*`` wrappers)

        .. _MLS: https://www.mlssoccer.com/
        .. _American Soccer Analysis: https://www.americansocceranalysis.com/
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://dapi.mlssoccer.com/v2/content/en-us/seasons/{slug}",
        params=_params,
    )
    if return_parsed:
        return parse_mls_entity(raw, return_as_pandas=return_as_pandas)
    return raw


def mls_content_seasons(
    competition_sportec_id: Optional[str] = None,
    sportec_id: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Query season content entities (dapi / Contentful).

    Endpoint: ``GET https://dapi.mlssoccer.com/v2/content/en-us/seasons``
    Example URL: https://dapi.mlssoccer.com/v2/content/en-us/seasons

    Args:
        competition_sportec_id: fields.competitionSportecId query parameter.
        sportec_id: fields.sportecId query parameter.
        return_parsed: parse the payload through parse_mls_api -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        sportsdataverse.errors.NoESPNDataError: the MLS host returned 404 (unknown id, or an unplayed match on ``/matches/{matchId}``).
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.mls import mls_content_seasons
            mls_content_seasons()

        See Also:
            * `MLS`_ - the public site these hosts render
            * `American Soccer Analysis`_ - advanced MLS xG / g+ metrics (``asa_*`` wrappers)

        .. _MLS: https://www.mlssoccer.com/
        .. _American Soccer Analysis: https://www.americansocceranalysis.com/
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "fields.competitionSportecId": competition_sportec_id,
        "fields.sportecId": sportec_id,
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        "https://dapi.mlssoccer.com/v2/content/en-us/seasons",
        params=_params,
    )
    if return_parsed:
        return parse_mls_api(raw, return_as_pandas=return_as_pandas)
    return raw


def mls_match(
    match_id: str,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Single match detail incl. lineups + referees (stats-api).

    Endpoint: ``GET https://stats-api.mlssoccer.com/matches/{match_id}``
    Example URL: https://stats-api.mlssoccer.com/matches/MLS-MAT-0009H8

    Args:
        match_id: match_id path parameter.
        return_parsed: parse the payload through parse_mls_match -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        sportsdataverse.errors.NoESPNDataError: the MLS host returned 404 (unknown id, or an unplayed match on ``/matches/{matchId}``).
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.mls import mls_match
            mls_match(match_id='MLS-MAT-0009H8')

        See Also:
            * `MLS`_ - the public site these hosts render
            * `American Soccer Analysis`_ - advanced MLS xG / g+ metrics (``asa_*`` wrappers)

        .. _MLS: https://www.mlssoccer.com/
        .. _American Soccer Analysis: https://www.americansocceranalysis.com/
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://stats-api.mlssoccer.com/matches/{match_id}",
        params=_params,
    )
    if return_parsed:
        return parse_mls_match(raw, return_as_pandas=return_as_pandas)
    return raw


def mls_season_matches(
    season_id: str,
    match_date_gte: Optional[str] = None,
    match_date_lte: Optional[str] = None,
    competition_id: Optional[str] = None,
    per_page: Optional[int] = None,
    sort: Optional[str] = None,
    series_name: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """List matches in a season, date-windowed (stats-api).

    Endpoint: ``GET https://stats-api.mlssoccer.com/matches/seasons/{season_id}``
    Example URL: https://stats-api.mlssoccer.com/matches/seasons/MLS-SEA-0001KA

    Args:
        season_id: season_id path parameter.
        match_date_gte: match_date[gte] query parameter.
        match_date_lte: match_date[lte] query parameter.
        competition_id: competition_id query parameter.
        per_page: per_page query parameter.
        sort: sort query parameter.
        series_name: series_name query parameter.
        return_parsed: parse the payload through parse_mls_api -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        sportsdataverse.errors.NoESPNDataError: the MLS host returned 404 (unknown id, or an unplayed match on ``/matches/{matchId}``).
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.mls import mls_season_matches
            mls_season_matches(season_id='MLS-SEA-0001KA')

        See Also:
            * `MLS`_ - the public site these hosts render
            * `American Soccer Analysis`_ - advanced MLS xG / g+ metrics (``asa_*`` wrappers)

        .. _MLS: https://www.mlssoccer.com/
        .. _American Soccer Analysis: https://www.americansocceranalysis.com/
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "match_date[gte]": match_date_gte,
        "match_date[lte]": match_date_lte,
        "competition_id": competition_id,
        "per_page": per_page,
        "sort": sort,
        "series_name": series_name,
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://stats-api.mlssoccer.com/matches/seasons/{season_id}",
        params=_params,
    )
    if return_parsed:
        return parse_mls_api(raw, return_as_pandas=return_as_pandas)
    return raw


def mls_sportapi_club_players(
    club_id: str,
    culture: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Club roster with player Sportec ids (sportapi).

    Endpoint: ``GET https://sportapi.mlssoccer.com/api/players/byClub/{club_id}``
    Example URL: https://sportapi.mlssoccer.com/api/players/byClub/MLS-CLU-000001

    Args:
        club_id: club_id path parameter.
        culture: culture query parameter.
        return_parsed: parse the payload through parse_mls_api -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        sportsdataverse.errors.NoESPNDataError: the MLS host returned 404 (unknown id, or an unplayed match on ``/matches/{matchId}``).
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.mls import mls_sportapi_club_players
            mls_sportapi_club_players(club_id='MLS-CLU-000001')

        See Also:
            * `MLS`_ - the public site these hosts render
            * `American Soccer Analysis`_ - advanced MLS xG / g+ metrics (``asa_*`` wrappers)

        .. _MLS: https://www.mlssoccer.com/
        .. _American Soccer Analysis: https://www.americansocceranalysis.com/
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "culture": culture,
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://sportapi.mlssoccer.com/api/players/byClub/{club_id}",
        params=_params,
    )
    if return_parsed:
        return parse_mls_api(raw, return_as_pandas=return_as_pandas)
    return raw


def mls_sportapi_clubs_by_sportec_ids(
    ids: str,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Batch club detail by Sportec ids (sportapi).

    Endpoint: ``GET https://sportapi.mlssoccer.com/api/clubs/bySportecIds/{ids}``
    Example URL: https://sportapi.mlssoccer.com/api/clubs/bySportecIds/MLS-MAT-0009H8

    Args:
        ids: ids path parameter.
        return_parsed: parse the payload through parse_mls_api -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        sportsdataverse.errors.NoESPNDataError: the MLS host returned 404 (unknown id, or an unplayed match on ``/matches/{matchId}``).
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.mls import mls_sportapi_clubs_by_sportec_ids
            mls_sportapi_clubs_by_sportec_ids(ids='MLS-MAT-0009H8')

        See Also:
            * `MLS`_ - the public site these hosts render
            * `American Soccer Analysis`_ - advanced MLS xG / g+ metrics (``asa_*`` wrappers)

        .. _MLS: https://www.mlssoccer.com/
        .. _American Soccer Analysis: https://www.americansocceranalysis.com/
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://sportapi.mlssoccer.com/api/clubs/bySportecIds/{ids}",
        params=_params,
    )
    if return_parsed:
        return parse_mls_api(raw, return_as_pandas=return_as_pandas)
    return raw


def mls_sportapi_match(
    match_id: str,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Single match detail (sportapi / .NET).

    Endpoint: ``GET https://sportapi.mlssoccer.com/api/matches/{match_id}``
    Example URL: https://sportapi.mlssoccer.com/api/matches/MLS-MAT-0009H8

    Args:
        match_id: match_id path parameter.
        return_parsed: parse the payload through parse_mls_entity -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        sportsdataverse.errors.NoESPNDataError: the MLS host returned 404 (unknown id, or an unplayed match on ``/matches/{matchId}``).
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.mls import mls_sportapi_match
            mls_sportapi_match(match_id='MLS-MAT-0009H8')

        See Also:
            * `MLS`_ - the public site these hosts render
            * `American Soccer Analysis`_ - advanced MLS xG / g+ metrics (``asa_*`` wrappers)

        .. _MLS: https://www.mlssoccer.com/
        .. _American Soccer Analysis: https://www.americansocceranalysis.com/
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://sportapi.mlssoccer.com/api/matches/{match_id}",
        params=_params,
    )
    if return_parsed:
        return parse_mls_entity(raw, return_as_pandas=return_as_pandas)
    return raw


def mls_sportapi_matches_by_sportec_ids(
    ids: str,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Batch match detail by Sportec ids (sportapi).

    Endpoint: ``GET https://sportapi.mlssoccer.com/api/matches/bySportecIds/{ids}``
    Example URL: https://sportapi.mlssoccer.com/api/matches/bySportecIds/MLS-MAT-0009H8

    Args:
        ids: ids path parameter.
        return_parsed: parse the payload through parse_mls_api -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        sportsdataverse.errors.NoESPNDataError: the MLS host returned 404 (unknown id, or an unplayed match on ``/matches/{matchId}``).
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.mls import mls_sportapi_matches_by_sportec_ids
            mls_sportapi_matches_by_sportec_ids(ids='MLS-MAT-0009H8')

        See Also:
            * `MLS`_ - the public site these hosts render
            * `American Soccer Analysis`_ - advanced MLS xG / g+ metrics (``asa_*`` wrappers)

        .. _MLS: https://www.mlssoccer.com/
        .. _American Soccer Analysis: https://www.americansocceranalysis.com/
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://sportapi.mlssoccer.com/api/matches/bySportecIds/{ids}",
        params=_params,
    )
    if return_parsed:
        return parse_mls_api(raw, return_as_pandas=return_as_pandas)
    return raw


def mls_standings(
    competition_id: str,
    season_id: str,
    category: Optional[str] = None,
    standings_type: Optional[str] = None,
    is_live: Optional[bool] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Standings table for a competition season (stats-api).

    Endpoint: ``GET https://stats-api.mlssoccer.com/competitions/{competition_id}/seasons/{season_id}/standings``
    Example URL: https://stats-api.mlssoccer.com/competitions/MLS-COM-000001/seasons/MLS-SEA-0001KA/standings

    Args:
        competition_id: competition_id path parameter.
        season_id: season_id path parameter.
        category: category query parameter.
        standings_type: type query parameter.
        is_live: is_live query parameter.
        return_parsed: parse the payload through parse_mls_standings -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        sportsdataverse.errors.NoESPNDataError: the MLS host returned 404 (unknown id, or an unplayed match on ``/matches/{matchId}``).
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.mls import mls_standings
            mls_standings(competition_id='MLS-COM-000001', season_id='MLS-SEA-0001KA')

        See Also:
            * `MLS`_ - the public site these hosts render
            * `American Soccer Analysis`_ - advanced MLS xG / g+ metrics (``asa_*`` wrappers)

        .. _MLS: https://www.mlssoccer.com/
        .. _American Soccer Analysis: https://www.americansocceranalysis.com/
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "category": category,
        "type": standings_type,
        "is_live": bool_str(is_live),
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://stats-api.mlssoccer.com/competitions/{competition_id}/seasons/{season_id}/standings",
        params=_params,
    )
    if return_parsed:
        return parse_mls_standings(raw, return_as_pandas=return_as_pandas)
    return raw
