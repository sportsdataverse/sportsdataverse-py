# GENERATED by tools/codegen/generate.py -- DO NOT EDIT.
# Source: tools/codegen/endpoints/nwsl_api.yaml
"""nwsl_api wrappers (host https://api-sdp.nwslsoccer.com/v1/nwsl/football). Generated."""

from __future__ import annotations

from typing import TYPE_CHECKING, Dict, List, Optional, Union  # noqa: F401

from sportsdataverse.soccer.nwsl.nwsl_api_runtime import _get
from sportsdataverse.soccer.nwsl.nwsl_api_parsers import (
    parse_nwsl_lineups,
    parse_nwsl_sdp,
    parse_nwsl_standings,
    parse_nwsl_stats,
)

if TYPE_CHECKING:  # pragma: no cover -- annotation-only imports (PEP 563 defers eval)
    import pandas as pd
    import polars as pl

__all__ = [
    "nwsl_competitions",
    "nwsl_match_lineups",
    "nwsl_matchdays",
    "nwsl_player_stats",
    "nwsl_season_matches",
    "nwsl_stages",
    "nwsl_standings",
    "nwsl_team_stats",
    "nwsl_teams",
]


def nwsl_competitions(
    locale: Optional[str] = "en-US",
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """All competitions StatsPerform tracks for NWSL (league + friendlies/cups).

    Endpoint: ``GET https://api-sdp.nwslsoccer.com/v1/nwsl/football/competitions``
    Example URL: https://api-sdp.nwslsoccer.com/v1/nwsl/football/competitions

    Args:
        locale: UI locale, always `en-US`.
        return_parsed: parse the payload through parse_nwsl_sdp -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        sportsdataverse.errors.NoESPNDataError: the SDP host returned 404 (unknown composite id, or an unmapped match sub-resource).
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.nwsl import nwsl_competitions
            nwsl_competitions()

        See Also:
            * `NWSL`_ - the public site this feed renders
            * `American Soccer Analysis`_ - advanced NWSL xG / g+ metrics (``asa_*`` wrappers)

        .. _NWSL: https://www.nwslsoccer.com/
        .. _American Soccer Analysis: https://www.americansocceranalysis.com/
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "locale": locale,
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api-sdp.nwslsoccer.com/v1/nwsl/football/competitions",
        params=_params,
    )
    if return_parsed:
        return parse_nwsl_sdp(raw, return_as_pandas=return_as_pandas)
    return raw


def nwsl_match_lineups(
    season_id: str,
    match_id: str,
    locale: Optional[str] = "en-US",
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Team lineups (starting XI + bench + staff) for a match.

    Endpoint: ``GET https://api-sdp.nwslsoccer.com/v1/nwsl/football/seasons/{season_id}/matches/{match_id}/lineups``
    Example URL: https://api-sdp.nwslsoccer.com/v1/nwsl/football/seasons/nwsl::Football_Season::0b6761e4701749f593690c0f338da74c/matches/nwsl::Football_Match::0b6761e4701749f593690c0f338da74c/lineups

    Args:
        season_id: season_id path parameter.
        match_id: match_id path parameter.
        locale: UI locale, always `en-US`.
        return_parsed: parse the payload through parse_nwsl_lineups -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        sportsdataverse.errors.NoESPNDataError: the SDP host returned 404 (unknown composite id, or an unmapped match sub-resource).
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.nwsl import nwsl_match_lineups
            nwsl_match_lineups(match_id='nwsl::Football_Match::0b6761e4701749f593690c0f338da74c', season_id='nwsl::Football_Season::0b6761e4701749f593690c0f338da74c')

        See Also:
            * `NWSL`_ - the public site this feed renders
            * `American Soccer Analysis`_ - advanced NWSL xG / g+ metrics (``asa_*`` wrappers)

        .. _NWSL: https://www.nwslsoccer.com/
        .. _American Soccer Analysis: https://www.americansocceranalysis.com/
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "locale": locale,
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://api-sdp.nwslsoccer.com/v1/nwsl/football/seasons/{season_id}/matches/{match_id}/lineups",
        params=_params,
    )
    if return_parsed:
        return parse_nwsl_lineups(raw, return_as_pandas=return_as_pandas)
    return raw


def nwsl_matchdays(
    season_id: str,
    locale: Optional[str] = "en-US",
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Match days (rounds) for a season.

    Endpoint: ``GET https://api-sdp.nwslsoccer.com/v1/nwsl/football/seasons/{season_id}/matchdays``
    Example URL: https://api-sdp.nwslsoccer.com/v1/nwsl/football/seasons/nwsl::Football_Season::0b6761e4701749f593690c0f338da74c/matchdays

    Args:
        season_id: season_id path parameter.
        locale: UI locale, always `en-US`.
        return_parsed: parse the payload through parse_nwsl_sdp -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        sportsdataverse.errors.NoESPNDataError: the SDP host returned 404 (unknown composite id, or an unmapped match sub-resource).
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.nwsl import nwsl_matchdays
            nwsl_matchdays(season_id='nwsl::Football_Season::0b6761e4701749f593690c0f338da74c')

        See Also:
            * `NWSL`_ - the public site this feed renders
            * `American Soccer Analysis`_ - advanced NWSL xG / g+ metrics (``asa_*`` wrappers)

        .. _NWSL: https://www.nwslsoccer.com/
        .. _American Soccer Analysis: https://www.americansocceranalysis.com/
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "locale": locale,
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://api-sdp.nwslsoccer.com/v1/nwsl/football/seasons/{season_id}/matchdays",
        params=_params,
    )
    if return_parsed:
        return parse_nwsl_sdp(raw, return_as_pandas=return_as_pandas)
    return raw


def nwsl_player_stats(
    season_id: str,
    locale: Optional[str] = "en-US",
    category: Optional[str] = None,
    role: Optional[str] = None,
    direction: Optional[str] = None,
    page: Optional[int] = None,
    page_num_element: Optional[int] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Player-stats leaderboard for a season (paginated).

    Endpoint: ``GET https://api-sdp.nwslsoccer.com/v1/nwsl/football/seasons/{season_id}/stats/players``
    Example URL: https://api-sdp.nwslsoccer.com/v1/nwsl/football/seasons/nwsl::Football_Season::0b6761e4701749f593690c0f338da74c/stats/players

    Args:
        season_id: season_id path parameter.
        locale: UI locale, always `en-US`.
        category: Stat family: `general` (default), `attack`, `defence`, etc.
        role: Position filter, e.g. `all`.
        direction: `asc` or `desc`.
        page: 1-based page number.
        page_num_element: Page size (e.g. 400).
        return_parsed: parse the payload through parse_nwsl_stats -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        sportsdataverse.errors.NoESPNDataError: the SDP host returned 404 (unknown composite id, or an unmapped match sub-resource).
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.nwsl import nwsl_player_stats
            nwsl_player_stats(season_id='nwsl::Football_Season::0b6761e4701749f593690c0f338da74c')

        See Also:
            * `NWSL`_ - the public site this feed renders
            * `American Soccer Analysis`_ - advanced NWSL xG / g+ metrics (``asa_*`` wrappers)

        .. _NWSL: https://www.nwslsoccer.com/
        .. _American Soccer Analysis: https://www.americansocceranalysis.com/
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "locale": locale,
        "category": category,
        "role": role,
        "direction": direction,
        "page": page,
        "pageNumElement": page_num_element,
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://api-sdp.nwslsoccer.com/v1/nwsl/football/seasons/{season_id}/stats/players",
        params=_params,
    )
    if return_parsed:
        return parse_nwsl_stats(raw, return_as_pandas=return_as_pandas)
    return raw


def nwsl_season_matches(
    season_ids: Optional[str] = None,
    locale: Optional[str] = "en-US",
    start_date: Optional[str] = None,
    end_date: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Matches across one or more seasons within a US-format date window.

    Endpoint: ``GET https://api-sdp.nwslsoccer.com/v1/nwsl/football/seasons/multipleSeasonMatches``
    Example URL: https://api-sdp.nwslsoccer.com/v1/nwsl/football/seasons/multipleSeasonMatches

    Args:
        season_ids: Comma-separated composite Season ids.
        locale: UI locale, always `en-US`.
        start_date: Window start, `MM/DD/YYYY` (US format).
        end_date: Window end, `MM/DD/YYYY` (US format).
        return_parsed: parse the payload through parse_nwsl_sdp -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        sportsdataverse.errors.NoESPNDataError: the SDP host returned 404 (unknown composite id, or an unmapped match sub-resource).
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.nwsl import nwsl_season_matches
            nwsl_season_matches()

        See Also:
            * `NWSL`_ - the public site this feed renders
            * `American Soccer Analysis`_ - advanced NWSL xG / g+ metrics (``asa_*`` wrappers)

        .. _NWSL: https://www.nwslsoccer.com/
        .. _American Soccer Analysis: https://www.americansocceranalysis.com/
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "seasonIds": season_ids,
        "locale": locale,
        "startDate": start_date,
        "endDate": end_date,
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api-sdp.nwslsoccer.com/v1/nwsl/football/seasons/multipleSeasonMatches",
        params=_params,
    )
    if return_parsed:
        return parse_nwsl_sdp(raw, return_as_pandas=return_as_pandas)
    return raw


def nwsl_stages(
    season_id: str,
    locale: Optional[str] = "en-US",
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Competition stages for a season (may be empty for league play).

    Endpoint: ``GET https://api-sdp.nwslsoccer.com/v1/nwsl/football/seasons/{season_id}/stages``
    Example URL: https://api-sdp.nwslsoccer.com/v1/nwsl/football/seasons/nwsl::Football_Season::0b6761e4701749f593690c0f338da74c/stages

    Args:
        season_id: season_id path parameter.
        locale: UI locale, always `en-US`.
        return_parsed: parse the payload through parse_nwsl_sdp -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        sportsdataverse.errors.NoESPNDataError: the SDP host returned 404 (unknown composite id, or an unmapped match sub-resource).
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.nwsl import nwsl_stages
            nwsl_stages(season_id='nwsl::Football_Season::0b6761e4701749f593690c0f338da74c')

        See Also:
            * `NWSL`_ - the public site this feed renders
            * `American Soccer Analysis`_ - advanced NWSL xG / g+ metrics (``asa_*`` wrappers)

        .. _NWSL: https://www.nwslsoccer.com/
        .. _American Soccer Analysis: https://www.americansocceranalysis.com/
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "locale": locale,
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://api-sdp.nwslsoccer.com/v1/nwsl/football/seasons/{season_id}/stages",
        params=_params,
    )
    if return_parsed:
        return parse_nwsl_sdp(raw, return_as_pandas=return_as_pandas)
    return raw


def nwsl_standings(
    season_id: str,
    locale: Optional[str] = "en-US",
    order_by: Optional[str] = None,
    direction: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Overall standings table for a season (table/home/away splits).

    Endpoint: ``GET https://api-sdp.nwslsoccer.com/v1/nwsl/football/seasons/{season_id}/standings/overall``
    Example URL: https://api-sdp.nwslsoccer.com/v1/nwsl/football/seasons/nwsl::Football_Season::0b6761e4701749f593690c0f338da74c/standings/overall

    Args:
        season_id: season_id path parameter.
        locale: UI locale, always `en-US`.
        order_by: Sort field, e.g. `rank`.
        direction: `asc` or `desc`.
        return_parsed: parse the payload through parse_nwsl_standings -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        sportsdataverse.errors.NoESPNDataError: the SDP host returned 404 (unknown composite id, or an unmapped match sub-resource).
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.nwsl import nwsl_standings
            nwsl_standings(season_id='nwsl::Football_Season::0b6761e4701749f593690c0f338da74c')

        See Also:
            * `NWSL`_ - the public site this feed renders
            * `American Soccer Analysis`_ - advanced NWSL xG / g+ metrics (``asa_*`` wrappers)

        .. _NWSL: https://www.nwslsoccer.com/
        .. _American Soccer Analysis: https://www.americansocceranalysis.com/
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "locale": locale,
        "orderBy": order_by,
        "direction": direction,
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://api-sdp.nwslsoccer.com/v1/nwsl/football/seasons/{season_id}/standings/overall",
        params=_params,
    )
    if return_parsed:
        return parse_nwsl_standings(raw, return_as_pandas=return_as_pandas)
    return raw


def nwsl_team_stats(
    season_id: str,
    locale: Optional[str] = "en-US",
    category: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Team-stats leaderboard for a season.

    Endpoint: ``GET https://api-sdp.nwslsoccer.com/v1/nwsl/football/seasons/{season_id}/stats/teams``
    Example URL: https://api-sdp.nwslsoccer.com/v1/nwsl/football/seasons/nwsl::Football_Season::0b6761e4701749f593690c0f338da74c/stats/teams

    Args:
        season_id: season_id path parameter.
        locale: UI locale, always `en-US`.
        category: Stat family: `general` (default), `attack`, `defence`, etc.
        return_parsed: parse the payload through parse_nwsl_stats -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        sportsdataverse.errors.NoESPNDataError: the SDP host returned 404 (unknown composite id, or an unmapped match sub-resource).
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.nwsl import nwsl_team_stats
            nwsl_team_stats(season_id='nwsl::Football_Season::0b6761e4701749f593690c0f338da74c')

        See Also:
            * `NWSL`_ - the public site this feed renders
            * `American Soccer Analysis`_ - advanced NWSL xG / g+ metrics (``asa_*`` wrappers)

        .. _NWSL: https://www.nwslsoccer.com/
        .. _American Soccer Analysis: https://www.americansocceranalysis.com/
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "locale": locale,
        "category": category,
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://api-sdp.nwslsoccer.com/v1/nwsl/football/seasons/{season_id}/stats/teams",
        params=_params,
    )
    if return_parsed:
        return parse_nwsl_stats(raw, return_as_pandas=return_as_pandas)
    return raw


def nwsl_teams(
    season_id: str,
    locale: Optional[str] = "en-US",
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Teams participating in a season.

    Endpoint: ``GET https://api-sdp.nwslsoccer.com/v1/nwsl/football/seasons/{season_id}/teams``
    Example URL: https://api-sdp.nwslsoccer.com/v1/nwsl/football/seasons/nwsl::Football_Season::0b6761e4701749f593690c0f338da74c/teams

    Args:
        season_id: season_id path parameter.
        locale: UI locale, always `en-US`.
        return_parsed: parse the payload through parse_nwsl_sdp -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        sportsdataverse.errors.NoESPNDataError: the SDP host returned 404 (unknown composite id, or an unmapped match sub-resource).
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.nwsl import nwsl_teams
            nwsl_teams(season_id='nwsl::Football_Season::0b6761e4701749f593690c0f338da74c')

        See Also:
            * `NWSL`_ - the public site this feed renders
            * `American Soccer Analysis`_ - advanced NWSL xG / g+ metrics (``asa_*`` wrappers)

        .. _NWSL: https://www.nwslsoccer.com/
        .. _American Soccer Analysis: https://www.americansocceranalysis.com/
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "locale": locale,
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://api-sdp.nwslsoccer.com/v1/nwsl/football/seasons/{season_id}/teams",
        params=_params,
    )
    if return_parsed:
        return parse_nwsl_sdp(raw, return_as_pandas=return_as_pandas)
    return raw
