# GENERATED by tools/codegen/generate.py -- DO NOT EDIT.
# Source: tools/codegen/endpoints/asa.yaml
"""asa wrappers (host https://app.americansocceranalysis.com/api/v1). Generated."""

from __future__ import annotations

from typing import TYPE_CHECKING, Dict, List, Optional, Union  # noqa: F401

from sportsdataverse._codegen_runtime import _get
from sportsdataverse.soccer.asa_parsers import parse_asa, parse_asa_goals_added

if TYPE_CHECKING:  # pragma: no cover -- annotation-only imports (PEP 563 defers eval)
    import pandas as pd
    import polars as pl

__all__ = [
    "asa_games",
    "asa_games_xgoals",
    "asa_goalkeepers_goals_added",
    "asa_goalkeepers_xgoals",
    "asa_managers",
    "asa_players",
    "asa_players_goals_added",
    "asa_players_salaries",
    "asa_players_xgoals",
    "asa_referees",
    "asa_stadia",
    "asa_teams",
    "asa_teams_goals_added",
    "asa_teams_xgoals",
    "asa_teams_xpass",
]


def asa_games(
    league_slug: str,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Games/fixtures with final scores, venue/official/manager FKs.

    Endpoint: ``GET https://app.americansocceranalysis.com/api/v1/{league_slug}/games``
    Example URL: https://app.americansocceranalysis.com/api/v1/mls/games

    Args:
        league_slug: league_slug path parameter.
        return_parsed: parse the payload through parse_asa -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        sportsdataverse.errors.NoESPNDataError: the ASA API returned 404 (route not published for that league).
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.soccer import asa_games
            asa_games(league_slug='mls')

        See Also:
            * `itscalledsoccer`_ - official ASA R/Python client over the same API
            * `American Soccer Analysis`_ - data origin (xG, g+, xPass models)

        .. _itscalledsoccer: https://github.com/American-Soccer-Analysis/itscalledsoccer
        .. _American Soccer Analysis: https://www.americansocceranalysis.com/
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://app.americansocceranalysis.com/api/v1/{league_slug}/games",
        params=_params,
    )
    if return_parsed:
        return parse_asa(raw, return_as_pandas=return_as_pandas)
    return raw


def asa_games_xgoals(
    league_slug: str,
    season_name: Optional[str] = None,
    stage_name: Optional[str] = None,
    minimum_minutes: Optional[str] = None,
    general_position: Optional[str] = None,
    split_by_teams: Optional[str] = None,
    split_by_seasons: Optional[str] = None,
    split_by_games: Optional[str] = None,
    start_date: Optional[str] = None,
    end_date: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Per-game expected-goals + expected points for both sides.

    Endpoint: ``GET https://app.americansocceranalysis.com/api/v1/{league_slug}/games/xgoals``
    Example URL: https://app.americansocceranalysis.com/api/v1/mls/games/xgoals

    Args:
        league_slug: league_slug path parameter.
        season_name: season_name query parameter.
        stage_name: stage_name query parameter.
        minimum_minutes: minimum_minutes query parameter.
        general_position: general_position query parameter.
        split_by_teams: split_by_teams query parameter.
        split_by_seasons: split_by_seasons query parameter.
        split_by_games: split_by_games query parameter.
        start_date: start_date query parameter.
        end_date: end_date query parameter.
        return_parsed: parse the payload through parse_asa -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        sportsdataverse.errors.NoESPNDataError: the ASA API returned 404 (route not published for that league).
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.soccer import asa_games_xgoals
            asa_games_xgoals(league_slug='mls')

        See Also:
            * `itscalledsoccer`_ - official ASA R/Python client over the same API
            * `American Soccer Analysis`_ - data origin (xG, g+, xPass models)

        .. _itscalledsoccer: https://github.com/American-Soccer-Analysis/itscalledsoccer
        .. _American Soccer Analysis: https://www.americansocceranalysis.com/
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "season_name": season_name,
        "stage_name": stage_name,
        "minimum_minutes": minimum_minutes,
        "general_position": general_position,
        "split_by_teams": split_by_teams,
        "split_by_seasons": split_by_seasons,
        "split_by_games": split_by_games,
        "start_date": start_date,
        "end_date": end_date,
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://app.americansocceranalysis.com/api/v1/{league_slug}/games/xgoals",
        params=_params,
    )
    if return_parsed:
        return parse_asa(raw, return_as_pandas=return_as_pandas)
    return raw


def asa_goalkeepers_goals_added(
    league_slug: str,
    season_name: Optional[str] = None,
    stage_name: Optional[str] = None,
    minimum_minutes: Optional[str] = None,
    general_position: Optional[str] = None,
    split_by_teams: Optional[str] = None,
    split_by_seasons: Optional[str] = None,
    split_by_games: Optional[str] = None,
    start_date: Optional[str] = None,
    end_date: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Per-goalkeeper Goals Added with a per-action-type data[] breakdown.

    Endpoint: ``GET https://app.americansocceranalysis.com/api/v1/{league_slug}/goalkeepers/goals-added``
    Example URL: https://app.americansocceranalysis.com/api/v1/mls/goalkeepers/goals-added

    Args:
        league_slug: league_slug path parameter.
        season_name: season_name query parameter.
        stage_name: stage_name query parameter.
        minimum_minutes: minimum_minutes query parameter.
        general_position: general_position query parameter.
        split_by_teams: split_by_teams query parameter.
        split_by_seasons: split_by_seasons query parameter.
        split_by_games: split_by_games query parameter.
        start_date: start_date query parameter.
        end_date: end_date query parameter.
        return_parsed: parse the payload through parse_asa_goals_added -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        sportsdataverse.errors.NoESPNDataError: the ASA API returned 404 (route not published for that league).
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.soccer import asa_goalkeepers_goals_added
            asa_goalkeepers_goals_added(league_slug='mls')

        See Also:
            * `itscalledsoccer`_ - official ASA R/Python client over the same API
            * `American Soccer Analysis`_ - data origin (xG, g+, xPass models)

        .. _itscalledsoccer: https://github.com/American-Soccer-Analysis/itscalledsoccer
        .. _American Soccer Analysis: https://www.americansocceranalysis.com/
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "season_name": season_name,
        "stage_name": stage_name,
        "minimum_minutes": minimum_minutes,
        "general_position": general_position,
        "split_by_teams": split_by_teams,
        "split_by_seasons": split_by_seasons,
        "split_by_games": split_by_games,
        "start_date": start_date,
        "end_date": end_date,
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://app.americansocceranalysis.com/api/v1/{league_slug}/goalkeepers/goals-added",
        params=_params,
    )
    if return_parsed:
        return parse_asa_goals_added(raw, return_as_pandas=return_as_pandas)
    return raw


def asa_goalkeepers_xgoals(
    league_slug: str,
    season_name: Optional[str] = None,
    stage_name: Optional[str] = None,
    minimum_minutes: Optional[str] = None,
    general_position: Optional[str] = None,
    split_by_teams: Optional[str] = None,
    split_by_seasons: Optional[str] = None,
    split_by_games: Optional[str] = None,
    start_date: Optional[str] = None,
    end_date: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Per-goalkeeper shot-stopping vs post-shot expected goals.

    Endpoint: ``GET https://app.americansocceranalysis.com/api/v1/{league_slug}/goalkeepers/xgoals``
    Example URL: https://app.americansocceranalysis.com/api/v1/mls/goalkeepers/xgoals

    Args:
        league_slug: league_slug path parameter.
        season_name: season_name query parameter.
        stage_name: stage_name query parameter.
        minimum_minutes: minimum_minutes query parameter.
        general_position: general_position query parameter.
        split_by_teams: split_by_teams query parameter.
        split_by_seasons: split_by_seasons query parameter.
        split_by_games: split_by_games query parameter.
        start_date: start_date query parameter.
        end_date: end_date query parameter.
        return_parsed: parse the payload through parse_asa -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        sportsdataverse.errors.NoESPNDataError: the ASA API returned 404 (route not published for that league).
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.soccer import asa_goalkeepers_xgoals
            asa_goalkeepers_xgoals(league_slug='mls')

        See Also:
            * `itscalledsoccer`_ - official ASA R/Python client over the same API
            * `American Soccer Analysis`_ - data origin (xG, g+, xPass models)

        .. _itscalledsoccer: https://github.com/American-Soccer-Analysis/itscalledsoccer
        .. _American Soccer Analysis: https://www.americansocceranalysis.com/
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "season_name": season_name,
        "stage_name": stage_name,
        "minimum_minutes": minimum_minutes,
        "general_position": general_position,
        "split_by_teams": split_by_teams,
        "split_by_seasons": split_by_seasons,
        "split_by_games": split_by_games,
        "start_date": start_date,
        "end_date": end_date,
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://app.americansocceranalysis.com/api/v1/{league_slug}/goalkeepers/xgoals",
        params=_params,
    )
    if return_parsed:
        return parse_asa(raw, return_as_pandas=return_as_pandas)
    return raw


def asa_managers(
    league_slug: str,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Managers/head coaches.

    Endpoint: ``GET https://app.americansocceranalysis.com/api/v1/{league_slug}/managers``
    Example URL: https://app.americansocceranalysis.com/api/v1/mls/managers

    Args:
        league_slug: league_slug path parameter.
        return_parsed: parse the payload through parse_asa -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        sportsdataverse.errors.NoESPNDataError: the ASA API returned 404 (route not published for that league).
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.soccer import asa_managers
            asa_managers(league_slug='mls')

        See Also:
            * `itscalledsoccer`_ - official ASA R/Python client over the same API
            * `American Soccer Analysis`_ - data origin (xG, g+, xPass models)

        .. _itscalledsoccer: https://github.com/American-Soccer-Analysis/itscalledsoccer
        .. _American Soccer Analysis: https://www.americansocceranalysis.com/
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://app.americansocceranalysis.com/api/v1/{league_slug}/managers",
        params=_params,
    )
    if return_parsed:
        return parse_asa(raw, return_as_pandas=return_as_pandas)
    return raw


def asa_players(
    league_slug: str,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Players in the league (identity + biometrics + positions).

    Endpoint: ``GET https://app.americansocceranalysis.com/api/v1/{league_slug}/players``
    Example URL: https://app.americansocceranalysis.com/api/v1/mls/players

    Args:
        league_slug: league_slug path parameter.
        return_parsed: parse the payload through parse_asa -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        sportsdataverse.errors.NoESPNDataError: the ASA API returned 404 (route not published for that league).
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.soccer import asa_players
            asa_players(league_slug='mls')

        See Also:
            * `itscalledsoccer`_ - official ASA R/Python client over the same API
            * `American Soccer Analysis`_ - data origin (xG, g+, xPass models)

        .. _itscalledsoccer: https://github.com/American-Soccer-Analysis/itscalledsoccer
        .. _American Soccer Analysis: https://www.americansocceranalysis.com/
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://app.americansocceranalysis.com/api/v1/{league_slug}/players",
        params=_params,
    )
    if return_parsed:
        return parse_asa(raw, return_as_pandas=return_as_pandas)
    return raw


def asa_players_goals_added(
    league_slug: str,
    season_name: Optional[str] = None,
    stage_name: Optional[str] = None,
    minimum_minutes: Optional[str] = None,
    general_position: Optional[str] = None,
    split_by_teams: Optional[str] = None,
    split_by_seasons: Optional[str] = None,
    split_by_games: Optional[str] = None,
    start_date: Optional[str] = None,
    end_date: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Per-player Goals Added (g+) with a per-action-type data[] breakdown.

    Endpoint: ``GET https://app.americansocceranalysis.com/api/v1/{league_slug}/players/goals-added``
    Example URL: https://app.americansocceranalysis.com/api/v1/mls/players/goals-added

    Args:
        league_slug: league_slug path parameter.
        season_name: season_name query parameter.
        stage_name: stage_name query parameter.
        minimum_minutes: minimum_minutes query parameter.
        general_position: general_position query parameter.
        split_by_teams: split_by_teams query parameter.
        split_by_seasons: split_by_seasons query parameter.
        split_by_games: split_by_games query parameter.
        start_date: start_date query parameter.
        end_date: end_date query parameter.
        return_parsed: parse the payload through parse_asa_goals_added -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        sportsdataverse.errors.NoESPNDataError: the ASA API returned 404 (route not published for that league).
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.soccer import asa_players_goals_added
            asa_players_goals_added(league_slug='mls')

        See Also:
            * `itscalledsoccer`_ - official ASA R/Python client over the same API
            * `American Soccer Analysis`_ - data origin (xG, g+, xPass models)

        .. _itscalledsoccer: https://github.com/American-Soccer-Analysis/itscalledsoccer
        .. _American Soccer Analysis: https://www.americansocceranalysis.com/
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "season_name": season_name,
        "stage_name": stage_name,
        "minimum_minutes": minimum_minutes,
        "general_position": general_position,
        "split_by_teams": split_by_teams,
        "split_by_seasons": split_by_seasons,
        "split_by_games": split_by_games,
        "start_date": start_date,
        "end_date": end_date,
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://app.americansocceranalysis.com/api/v1/{league_slug}/players/goals-added",
        params=_params,
    )
    if return_parsed:
        return parse_asa_goals_added(raw, return_as_pandas=return_as_pandas)
    return raw


def asa_players_salaries(
    league_slug: str,
    season_name: Optional[str] = None,
    stage_name: Optional[str] = None,
    minimum_minutes: Optional[str] = None,
    general_position: Optional[str] = None,
    split_by_teams: Optional[str] = None,
    split_by_seasons: Optional[str] = None,
    split_by_games: Optional[str] = None,
    start_date: Optional[str] = None,
    end_date: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Player salaries (MLS only; server caps the response at 10000 rows).

    Endpoint: ``GET https://app.americansocceranalysis.com/api/v1/{league_slug}/players/salaries``
    Example URL: https://app.americansocceranalysis.com/api/v1/mls/players/salaries

    Args:
        league_slug: league_slug path parameter.
        season_name: season_name query parameter.
        stage_name: stage_name query parameter.
        minimum_minutes: minimum_minutes query parameter.
        general_position: general_position query parameter.
        split_by_teams: split_by_teams query parameter.
        split_by_seasons: split_by_seasons query parameter.
        split_by_games: split_by_games query parameter.
        start_date: start_date query parameter.
        end_date: end_date query parameter.
        return_parsed: parse the payload through parse_asa -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        sportsdataverse.errors.NoESPNDataError: the ASA API returned 404 (route not published for that league).
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.soccer import asa_players_salaries
            asa_players_salaries(league_slug='mls')

        See Also:
            * `itscalledsoccer`_ - official ASA R/Python client over the same API
            * `American Soccer Analysis`_ - data origin (xG, g+, xPass models)

        .. _itscalledsoccer: https://github.com/American-Soccer-Analysis/itscalledsoccer
        .. _American Soccer Analysis: https://www.americansocceranalysis.com/
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "season_name": season_name,
        "stage_name": stage_name,
        "minimum_minutes": minimum_minutes,
        "general_position": general_position,
        "split_by_teams": split_by_teams,
        "split_by_seasons": split_by_seasons,
        "split_by_games": split_by_games,
        "start_date": start_date,
        "end_date": end_date,
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://app.americansocceranalysis.com/api/v1/{league_slug}/players/salaries",
        params=_params,
    )
    if return_parsed:
        return parse_asa(raw, return_as_pandas=return_as_pandas)
    return raw


def asa_players_xgoals(
    league_slug: str,
    season_name: Optional[str] = None,
    stage_name: Optional[str] = None,
    minimum_minutes: Optional[str] = None,
    general_position: Optional[str] = None,
    split_by_teams: Optional[str] = None,
    split_by_seasons: Optional[str] = None,
    split_by_games: Optional[str] = None,
    start_date: Optional[str] = None,
    end_date: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Per-player expected-goals + attacking production.

    Endpoint: ``GET https://app.americansocceranalysis.com/api/v1/{league_slug}/players/xgoals``
    Example URL: https://app.americansocceranalysis.com/api/v1/mls/players/xgoals

    Args:
        league_slug: league_slug path parameter.
        season_name: season_name query parameter.
        stage_name: stage_name query parameter.
        minimum_minutes: minimum_minutes query parameter.
        general_position: general_position query parameter.
        split_by_teams: split_by_teams query parameter.
        split_by_seasons: split_by_seasons query parameter.
        split_by_games: split_by_games query parameter.
        start_date: start_date query parameter.
        end_date: end_date query parameter.
        return_parsed: parse the payload through parse_asa -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        sportsdataverse.errors.NoESPNDataError: the ASA API returned 404 (route not published for that league).
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.soccer import asa_players_xgoals
            asa_players_xgoals(league_slug='mls')

        See Also:
            * `itscalledsoccer`_ - official ASA R/Python client over the same API
            * `American Soccer Analysis`_ - data origin (xG, g+, xPass models)

        .. _itscalledsoccer: https://github.com/American-Soccer-Analysis/itscalledsoccer
        .. _American Soccer Analysis: https://www.americansocceranalysis.com/
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "season_name": season_name,
        "stage_name": stage_name,
        "minimum_minutes": minimum_minutes,
        "general_position": general_position,
        "split_by_teams": split_by_teams,
        "split_by_seasons": split_by_seasons,
        "split_by_games": split_by_games,
        "start_date": start_date,
        "end_date": end_date,
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://app.americansocceranalysis.com/api/v1/{league_slug}/players/xgoals",
        params=_params,
    )
    if return_parsed:
        return parse_asa(raw, return_as_pandas=return_as_pandas)
    return raw


def asa_referees(
    league_slug: str,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Match referees.

    Endpoint: ``GET https://app.americansocceranalysis.com/api/v1/{league_slug}/referees``
    Example URL: https://app.americansocceranalysis.com/api/v1/mls/referees

    Args:
        league_slug: league_slug path parameter.
        return_parsed: parse the payload through parse_asa -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        sportsdataverse.errors.NoESPNDataError: the ASA API returned 404 (route not published for that league).
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.soccer import asa_referees
            asa_referees(league_slug='mls')

        See Also:
            * `itscalledsoccer`_ - official ASA R/Python client over the same API
            * `American Soccer Analysis`_ - data origin (xG, g+, xPass models)

        .. _itscalledsoccer: https://github.com/American-Soccer-Analysis/itscalledsoccer
        .. _American Soccer Analysis: https://www.americansocceranalysis.com/
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://app.americansocceranalysis.com/api/v1/{league_slug}/referees",
        params=_params,
    )
    if return_parsed:
        return parse_asa(raw, return_as_pandas=return_as_pandas)
    return raw


def asa_stadia(
    league_slug: str,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Stadia (venue metadata incl. coordinates + pitch dimensions).

    Endpoint: ``GET https://app.americansocceranalysis.com/api/v1/{league_slug}/stadia``
    Example URL: https://app.americansocceranalysis.com/api/v1/mls/stadia

    Args:
        league_slug: league_slug path parameter.
        return_parsed: parse the payload through parse_asa -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        sportsdataverse.errors.NoESPNDataError: the ASA API returned 404 (route not published for that league).
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.soccer import asa_stadia
            asa_stadia(league_slug='mls')

        See Also:
            * `itscalledsoccer`_ - official ASA R/Python client over the same API
            * `American Soccer Analysis`_ - data origin (xG, g+, xPass models)

        .. _itscalledsoccer: https://github.com/American-Soccer-Analysis/itscalledsoccer
        .. _American Soccer Analysis: https://www.americansocceranalysis.com/
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://app.americansocceranalysis.com/api/v1/{league_slug}/stadia",
        params=_params,
    )
    if return_parsed:
        return parse_asa(raw, return_as_pandas=return_as_pandas)
    return raw


def asa_teams(
    league_slug: str,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Teams in the league (full table, no filter params).

    Endpoint: ``GET https://app.americansocceranalysis.com/api/v1/{league_slug}/teams``
    Example URL: https://app.americansocceranalysis.com/api/v1/mls/teams

    Args:
        league_slug: league_slug path parameter.
        return_parsed: parse the payload through parse_asa -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        sportsdataverse.errors.NoESPNDataError: the ASA API returned 404 (route not published for that league).
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.soccer import asa_teams
            asa_teams(league_slug='mls')

        See Also:
            * `itscalledsoccer`_ - official ASA R/Python client over the same API
            * `American Soccer Analysis`_ - data origin (xG, g+, xPass models)

        .. _itscalledsoccer: https://github.com/American-Soccer-Analysis/itscalledsoccer
        .. _American Soccer Analysis: https://www.americansocceranalysis.com/
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://app.americansocceranalysis.com/api/v1/{league_slug}/teams",
        params=_params,
    )
    if return_parsed:
        return parse_asa(raw, return_as_pandas=return_as_pandas)
    return raw


def asa_teams_goals_added(
    league_slug: str,
    season_name: Optional[str] = None,
    stage_name: Optional[str] = None,
    minimum_minutes: Optional[str] = None,
    general_position: Optional[str] = None,
    split_by_teams: Optional[str] = None,
    split_by_seasons: Optional[str] = None,
    split_by_games: Optional[str] = None,
    start_date: Optional[str] = None,
    end_date: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Per-team Goals Added for/against with a per-action-type data[] breakdown.

    Endpoint: ``GET https://app.americansocceranalysis.com/api/v1/{league_slug}/teams/goals-added``
    Example URL: https://app.americansocceranalysis.com/api/v1/mls/teams/goals-added

    Args:
        league_slug: league_slug path parameter.
        season_name: season_name query parameter.
        stage_name: stage_name query parameter.
        minimum_minutes: minimum_minutes query parameter.
        general_position: general_position query parameter.
        split_by_teams: split_by_teams query parameter.
        split_by_seasons: split_by_seasons query parameter.
        split_by_games: split_by_games query parameter.
        start_date: start_date query parameter.
        end_date: end_date query parameter.
        return_parsed: parse the payload through parse_asa_goals_added -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        sportsdataverse.errors.NoESPNDataError: the ASA API returned 404 (route not published for that league).
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.soccer import asa_teams_goals_added
            asa_teams_goals_added(league_slug='mls')

        See Also:
            * `itscalledsoccer`_ - official ASA R/Python client over the same API
            * `American Soccer Analysis`_ - data origin (xG, g+, xPass models)

        .. _itscalledsoccer: https://github.com/American-Soccer-Analysis/itscalledsoccer
        .. _American Soccer Analysis: https://www.americansocceranalysis.com/
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "season_name": season_name,
        "stage_name": stage_name,
        "minimum_minutes": minimum_minutes,
        "general_position": general_position,
        "split_by_teams": split_by_teams,
        "split_by_seasons": split_by_seasons,
        "split_by_games": split_by_games,
        "start_date": start_date,
        "end_date": end_date,
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://app.americansocceranalysis.com/api/v1/{league_slug}/teams/goals-added",
        params=_params,
    )
    if return_parsed:
        return parse_asa_goals_added(raw, return_as_pandas=return_as_pandas)
    return raw


def asa_teams_xgoals(
    league_slug: str,
    season_name: Optional[str] = None,
    stage_name: Optional[str] = None,
    minimum_minutes: Optional[str] = None,
    general_position: Optional[str] = None,
    split_by_teams: Optional[str] = None,
    split_by_seasons: Optional[str] = None,
    split_by_games: Optional[str] = None,
    start_date: Optional[str] = None,
    end_date: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Per-team expected-goals for/against + expected points.

    Endpoint: ``GET https://app.americansocceranalysis.com/api/v1/{league_slug}/teams/xgoals``
    Example URL: https://app.americansocceranalysis.com/api/v1/mls/teams/xgoals

    Args:
        league_slug: league_slug path parameter.
        season_name: season_name query parameter.
        stage_name: stage_name query parameter.
        minimum_minutes: minimum_minutes query parameter.
        general_position: general_position query parameter.
        split_by_teams: split_by_teams query parameter.
        split_by_seasons: split_by_seasons query parameter.
        split_by_games: split_by_games query parameter.
        start_date: start_date query parameter.
        end_date: end_date query parameter.
        return_parsed: parse the payload through parse_asa -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        sportsdataverse.errors.NoESPNDataError: the ASA API returned 404 (route not published for that league).
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.soccer import asa_teams_xgoals
            asa_teams_xgoals(league_slug='mls')

        See Also:
            * `itscalledsoccer`_ - official ASA R/Python client over the same API
            * `American Soccer Analysis`_ - data origin (xG, g+, xPass models)

        .. _itscalledsoccer: https://github.com/American-Soccer-Analysis/itscalledsoccer
        .. _American Soccer Analysis: https://www.americansocceranalysis.com/
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "season_name": season_name,
        "stage_name": stage_name,
        "minimum_minutes": minimum_minutes,
        "general_position": general_position,
        "split_by_teams": split_by_teams,
        "split_by_seasons": split_by_seasons,
        "split_by_games": split_by_games,
        "start_date": start_date,
        "end_date": end_date,
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://app.americansocceranalysis.com/api/v1/{league_slug}/teams/xgoals",
        params=_params,
    )
    if return_parsed:
        return parse_asa(raw, return_as_pandas=return_as_pandas)
    return raw


def asa_teams_xpass(
    league_slug: str,
    season_name: Optional[str] = None,
    stage_name: Optional[str] = None,
    minimum_minutes: Optional[str] = None,
    general_position: Optional[str] = None,
    split_by_teams: Optional[str] = None,
    split_by_seasons: Optional[str] = None,
    split_by_games: Optional[str] = None,
    start_date: Optional[str] = None,
    end_date: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Per-team expected-passing (completion over expected, vertical distance).

    Endpoint: ``GET https://app.americansocceranalysis.com/api/v1/{league_slug}/teams/xpass``
    Example URL: https://app.americansocceranalysis.com/api/v1/mls/teams/xpass

    Args:
        league_slug: league_slug path parameter.
        season_name: season_name query parameter.
        stage_name: stage_name query parameter.
        minimum_minutes: minimum_minutes query parameter.
        general_position: general_position query parameter.
        split_by_teams: split_by_teams query parameter.
        split_by_seasons: split_by_seasons query parameter.
        split_by_games: split_by_games query parameter.
        start_date: start_date query parameter.
        end_date: end_date query parameter.
        return_parsed: parse the payload through parse_asa -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Raises:
        sportsdataverse.errors.NoESPNDataError: the ASA API returned 404 (route not published for that league).
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.soccer import asa_teams_xpass
            asa_teams_xpass(league_slug='mls')

        See Also:
            * `itscalledsoccer`_ - official ASA R/Python client over the same API
            * `American Soccer Analysis`_ - data origin (xG, g+, xPass models)

        .. _itscalledsoccer: https://github.com/American-Soccer-Analysis/itscalledsoccer
        .. _American Soccer Analysis: https://www.americansocceranalysis.com/
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "season_name": season_name,
        "stage_name": stage_name,
        "minimum_minutes": minimum_minutes,
        "general_position": general_position,
        "split_by_teams": split_by_teams,
        "split_by_seasons": split_by_seasons,
        "split_by_games": split_by_games,
        "start_date": start_date,
        "end_date": end_date,
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://app.americansocceranalysis.com/api/v1/{league_slug}/teams/xpass",
        params=_params,
    )
    if return_parsed:
        return parse_asa(raw, return_as_pandas=return_as_pandas)
    return raw
