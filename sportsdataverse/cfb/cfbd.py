# GENERATED by tools/codegen/generate.py -- DO NOT EDIT.
# Source: tools/codegen/endpoints/cfbd.yaml
"""cfbd wrappers (host https://api.collegefootballdata.com). Generated."""

from __future__ import annotations

from typing import TYPE_CHECKING, Dict, List, Optional, Union  # noqa: F401

from sportsdataverse.cfb.cfbd_runtime import _get
from sportsdataverse.cfb.cfbd_parsers import parse_cfbd_records

if TYPE_CHECKING:  # pragma: no cover -- annotation-only imports (PEP 563 defers eval)
    import pandas as pd
    import polars as pl

__all__ = [
    "cfbd_calendar",
    "cfbd_coaches",
    "cfbd_conferences",
    "cfbd_draft_picks",
    "cfbd_draft_positions",
    "cfbd_draft_teams",
    "cfbd_drives",
    "cfbd_game_box_advanced",
    "cfbd_games",
    "cfbd_games_media",
    "cfbd_games_players",
    "cfbd_games_teams",
    "cfbd_games_weather",
    "cfbd_info",
    "cfbd_lines",
    "cfbd_live_plays",
    "cfbd_metrics_fg_ep",
    "cfbd_metrics_wp",
    "cfbd_metrics_wp_pregame",
    "cfbd_player_portal",
    "cfbd_player_returning",
    "cfbd_player_search",
    "cfbd_player_usage",
    "cfbd_plays",
    "cfbd_plays_stats",
    "cfbd_plays_stats_types",
    "cfbd_plays_types",
    "cfbd_ppa_games",
    "cfbd_ppa_players_games",
    "cfbd_ppa_players_season",
    "cfbd_ppa_predicted",
    "cfbd_ppa_teams",
    "cfbd_rankings",
    "cfbd_ratings_elo",
    "cfbd_ratings_fpi",
    "cfbd_ratings_sp",
    "cfbd_ratings_sp_conferences",
    "cfbd_ratings_srs",
    "cfbd_records",
    "cfbd_recruiting_groups",
    "cfbd_recruiting_players",
    "cfbd_recruiting_teams",
    "cfbd_roster",
    "cfbd_scoreboard",
    "cfbd_stats_categories",
    "cfbd_stats_game_advanced",
    "cfbd_stats_player_season",
    "cfbd_stats_season",
    "cfbd_stats_season_advanced",
    "cfbd_talent",
    "cfbd_teams",
    "cfbd_teams_fbs",
    "cfbd_teams_matchup",
    "cfbd_venues",
    "cfbd_wepa_players_kicking",
    "cfbd_wepa_players_passing",
    "cfbd_wepa_players_rushing",
    "cfbd_wepa_team_season",
]


def cfbd_calendar(
    year: int,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /calendar — Retrieves calendar information

    Endpoint: ``GET https://api.collegefootballdata.com/calendar``
    Example URL: https://api.collegefootballdata.com/calendar

    Args:
        year: year query parameter.
        headers: optional pre-minted auth headers dict (e.g. from nfl_headers_gen()) to reuse across calls; a fresh anonymous token is minted when omitted.
        return_parsed: parse the payload through parse_cfbd_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            cfbd_calendar()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "year": year,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.collegefootballdata.com/calendar",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_cfbd_records(raw, return_as_pandas=return_as_pandas)
    return raw


def cfbd_coaches(
    first_name: Optional[str] = None,
    last_name: Optional[str] = None,
    team: Optional[str] = None,
    year: Optional[int] = None,
    min_year: Optional[int] = None,
    max_year: Optional[int] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /coaches — Retrieves historical head coach information and records

    Endpoint: ``GET https://api.collegefootballdata.com/coaches``
    Example URL: https://api.collegefootballdata.com/coaches

    Args:
        first_name: firstName query parameter.
        last_name: lastName query parameter.
        team: team query parameter.
        year: year query parameter.
        min_year: minYear query parameter.
        max_year: maxYear query parameter.
        headers: optional pre-minted auth headers dict (e.g. from nfl_headers_gen()) to reuse across calls; a fresh anonymous token is minted when omitted.
        return_parsed: parse the payload through parse_cfbd_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            cfbd_coaches()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "firstName": first_name,
        "lastName": last_name,
        "team": team,
        "year": year,
        "minYear": min_year,
        "maxYear": max_year,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.collegefootballdata.com/coaches",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_cfbd_records(raw, return_as_pandas=return_as_pandas)
    return raw


def cfbd_conferences(
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /conferences — Retrieves list of conferences

    Endpoint: ``GET https://api.collegefootballdata.com/conferences``
    Example URL: https://api.collegefootballdata.com/conferences

    Args:
        headers: optional pre-minted auth headers dict (e.g. from nfl_headers_gen()) to reuse across calls; a fresh anonymous token is minted when omitted.
        return_parsed: parse the payload through parse_cfbd_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            cfbd_conferences()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {}
    _params.update(_caller_params)
    raw = _get(
        "https://api.collegefootballdata.com/conferences",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_cfbd_records(raw, return_as_pandas=return_as_pandas)
    return raw


def cfbd_draft_picks(
    year: Optional[int] = None,
    team: Optional[str] = None,
    school: Optional[str] = None,
    conference: Optional[str] = None,
    position: Optional[str] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /draft/picks — Retrieve historical NFL draft data

    Endpoint: ``GET https://api.collegefootballdata.com/draft/picks``
    Example URL: https://api.collegefootballdata.com/draft/picks

    Args:
        year: year query parameter.
        team: team query parameter.
        school: school query parameter.
        conference: conference query parameter.
        position: position query parameter.
        headers: optional pre-minted auth headers dict (e.g. from nfl_headers_gen()) to reuse across calls; a fresh anonymous token is minted when omitted.
        return_parsed: parse the payload through parse_cfbd_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            cfbd_draft_picks()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "year": year,
        "team": team,
        "school": school,
        "conference": conference,
        "position": position,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.collegefootballdata.com/draft/picks",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_cfbd_records(raw, return_as_pandas=return_as_pandas)
    return raw


def cfbd_draft_positions(
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /draft/positions — Retrieves list of player position categories for the NFL Draft

    Endpoint: ``GET https://api.collegefootballdata.com/draft/positions``
    Example URL: https://api.collegefootballdata.com/draft/positions

    Args:
        headers: optional pre-minted auth headers dict (e.g. from nfl_headers_gen()) to reuse across calls; a fresh anonymous token is minted when omitted.
        return_parsed: parse the payload through parse_cfbd_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            cfbd_draft_positions()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {}
    _params.update(_caller_params)
    raw = _get(
        "https://api.collegefootballdata.com/draft/positions",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_cfbd_records(raw, return_as_pandas=return_as_pandas)
    return raw


def cfbd_draft_teams(
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /draft/teams — Retrieves list of NFL teams

    Endpoint: ``GET https://api.collegefootballdata.com/draft/teams``
    Example URL: https://api.collegefootballdata.com/draft/teams

    Args:
        headers: optional pre-minted auth headers dict (e.g. from nfl_headers_gen()) to reuse across calls; a fresh anonymous token is minted when omitted.
        return_parsed: parse the payload through parse_cfbd_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            cfbd_draft_teams()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {}
    _params.update(_caller_params)
    raw = _get(
        "https://api.collegefootballdata.com/draft/teams",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_cfbd_records(raw, return_as_pandas=return_as_pandas)
    return raw


def cfbd_drives(
    year: int,
    season_type: Optional[str] = None,
    week: Optional[int] = None,
    team: Optional[str] = None,
    offense: Optional[str] = None,
    defense: Optional[str] = None,
    conference: Optional[str] = None,
    offense_conference: Optional[str] = None,
    defense_conference: Optional[str] = None,
    classification: Optional[str] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /drives — Retrieves historical drive data

    Endpoint: ``GET https://api.collegefootballdata.com/drives``
    Example URL: https://api.collegefootballdata.com/drives

    Args:
        year: year query parameter.
        season_type: seasonType query parameter.
        week: week query parameter.
        team: team query parameter.
        offense: offense query parameter.
        defense: defense query parameter.
        conference: conference query parameter.
        offense_conference: offenseConference query parameter.
        defense_conference: defenseConference query parameter.
        classification: classification query parameter.
        headers: optional pre-minted auth headers dict (e.g. from nfl_headers_gen()) to reuse across calls; a fresh anonymous token is minted when omitted.
        return_parsed: parse the payload through parse_cfbd_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            cfbd_drives()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "year": year,
        "seasonType": season_type,
        "week": week,
        "team": team,
        "offense": offense,
        "defense": defense,
        "conference": conference,
        "offenseConference": offense_conference,
        "defenseConference": defense_conference,
        "classification": classification,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.collegefootballdata.com/drives",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_cfbd_records(raw, return_as_pandas=return_as_pandas)
    return raw


def cfbd_game_box_advanced(
    id: int,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /game/box/advanced — Retrieves an advanced box score for a game

    Endpoint: ``GET https://api.collegefootballdata.com/game/box/advanced``
    Example URL: https://api.collegefootballdata.com/game/box/advanced

    Args:
        id: id query parameter.
        headers: optional pre-minted auth headers dict (e.g. from nfl_headers_gen()) to reuse across calls; a fresh anonymous token is minted when omitted.
        return_parsed: parse the payload through parse_cfbd_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            cfbd_game_box_advanced()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "id": id,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.collegefootballdata.com/game/box/advanced",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_cfbd_records(raw, return_as_pandas=return_as_pandas)
    return raw


def cfbd_games(
    year: Optional[int] = None,
    week: Optional[int] = None,
    season_type: Optional[str] = None,
    classification: Optional[str] = None,
    team: Optional[str] = None,
    home: Optional[str] = None,
    away: Optional[str] = None,
    conference: Optional[str] = None,
    id: Optional[int] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /games — Retrieves historical game data

    Endpoint: ``GET https://api.collegefootballdata.com/games``
    Example URL: https://api.collegefootballdata.com/games

    Args:
        year: year query parameter.
        week: week query parameter.
        season_type: seasonType query parameter.
        classification: classification query parameter.
        team: team query parameter.
        home: home query parameter.
        away: away query parameter.
        conference: conference query parameter.
        id: id query parameter.
        headers: optional pre-minted auth headers dict (e.g. from nfl_headers_gen()) to reuse across calls; a fresh anonymous token is minted when omitted.
        return_parsed: parse the payload through parse_cfbd_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            cfbd_games()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "year": year,
        "week": week,
        "seasonType": season_type,
        "classification": classification,
        "team": team,
        "home": home,
        "away": away,
        "conference": conference,
        "id": id,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.collegefootballdata.com/games",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_cfbd_records(raw, return_as_pandas=return_as_pandas)
    return raw


def cfbd_games_media(
    year: int,
    season_type: Optional[str] = None,
    week: Optional[int] = None,
    team: Optional[str] = None,
    conference: Optional[str] = None,
    media_type: Optional[str] = None,
    classification: Optional[str] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /games/media — Retrieves media information for games

    Endpoint: ``GET https://api.collegefootballdata.com/games/media``
    Example URL: https://api.collegefootballdata.com/games/media

    Args:
        year: year query parameter.
        season_type: seasonType query parameter.
        week: week query parameter.
        team: team query parameter.
        conference: conference query parameter.
        media_type: mediaType query parameter.
        classification: classification query parameter.
        headers: optional pre-minted auth headers dict (e.g. from nfl_headers_gen()) to reuse across calls; a fresh anonymous token is minted when omitted.
        return_parsed: parse the payload through parse_cfbd_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            cfbd_games_media()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "year": year,
        "seasonType": season_type,
        "week": week,
        "team": team,
        "conference": conference,
        "mediaType": media_type,
        "classification": classification,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.collegefootballdata.com/games/media",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_cfbd_records(raw, return_as_pandas=return_as_pandas)
    return raw


def cfbd_games_players(
    year: Optional[int] = None,
    week: Optional[int] = None,
    team: Optional[str] = None,
    conference: Optional[str] = None,
    classification: Optional[str] = None,
    season_type: Optional[str] = None,
    category: Optional[str] = None,
    id: Optional[int] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /games/players — Retrieves player box score statistics

    Endpoint: ``GET https://api.collegefootballdata.com/games/players``
    Example URL: https://api.collegefootballdata.com/games/players

    Args:
        year: year query parameter.
        week: week query parameter.
        team: team query parameter.
        conference: conference query parameter.
        classification: classification query parameter.
        season_type: seasonType query parameter.
        category: category query parameter.
        id: id query parameter.
        headers: optional pre-minted auth headers dict (e.g. from nfl_headers_gen()) to reuse across calls; a fresh anonymous token is minted when omitted.
        return_parsed: parse the payload through parse_cfbd_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            cfbd_games_players()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "year": year,
        "week": week,
        "team": team,
        "conference": conference,
        "classification": classification,
        "seasonType": season_type,
        "category": category,
        "id": id,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.collegefootballdata.com/games/players",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_cfbd_records(raw, return_as_pandas=return_as_pandas)
    return raw


def cfbd_games_teams(
    year: Optional[int] = None,
    week: Optional[int] = None,
    team: Optional[str] = None,
    conference: Optional[str] = None,
    classification: Optional[str] = None,
    season_type: Optional[str] = None,
    id: Optional[int] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /games/teams — Retrieves team box score statistics

    Endpoint: ``GET https://api.collegefootballdata.com/games/teams``
    Example URL: https://api.collegefootballdata.com/games/teams

    Args:
        year: year query parameter.
        week: week query parameter.
        team: team query parameter.
        conference: conference query parameter.
        classification: classification query parameter.
        season_type: seasonType query parameter.
        id: id query parameter.
        headers: optional pre-minted auth headers dict (e.g. from nfl_headers_gen()) to reuse across calls; a fresh anonymous token is minted when omitted.
        return_parsed: parse the payload through parse_cfbd_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            cfbd_games_teams()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "year": year,
        "week": week,
        "team": team,
        "conference": conference,
        "classification": classification,
        "seasonType": season_type,
        "id": id,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.collegefootballdata.com/games/teams",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_cfbd_records(raw, return_as_pandas=return_as_pandas)
    return raw


def cfbd_games_weather(
    year: Optional[int] = None,
    season_type: Optional[str] = None,
    week: Optional[int] = None,
    team: Optional[str] = None,
    conference: Optional[str] = None,
    classification: Optional[str] = None,
    game_id: Optional[int] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /games/weather — Retrieve historical and future weather data (Patreon only)

    Endpoint: ``GET https://api.collegefootballdata.com/games/weather``
    Example URL: https://api.collegefootballdata.com/games/weather

    Args:
        year: year query parameter.
        season_type: seasonType query parameter.
        week: week query parameter.
        team: team query parameter.
        conference: conference query parameter.
        classification: classification query parameter.
        game_id: gameId query parameter.
        headers: optional pre-minted auth headers dict (e.g. from nfl_headers_gen()) to reuse across calls; a fresh anonymous token is minted when omitted.
        return_parsed: parse the payload through parse_cfbd_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            cfbd_games_weather()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "year": year,
        "seasonType": season_type,
        "week": week,
        "team": team,
        "conference": conference,
        "classification": classification,
        "gameId": game_id,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.collegefootballdata.com/games/weather",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_cfbd_records(raw, return_as_pandas=return_as_pandas)
    return raw


def cfbd_info(
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /info — Retrieves information about the user, including their Patreon level and remaining API calls.

    Endpoint: ``GET https://api.collegefootballdata.com/info``
    Example URL: https://api.collegefootballdata.com/info

    Args:
        headers: optional pre-minted auth headers dict (e.g. from nfl_headers_gen()) to reuse across calls; a fresh anonymous token is minted when omitted.
        return_parsed: parse the payload through parse_cfbd_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            cfbd_info()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {}
    _params.update(_caller_params)
    raw = _get(
        "https://api.collegefootballdata.com/info",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_cfbd_records(raw, return_as_pandas=return_as_pandas)
    return raw


def cfbd_lines(
    game_id: Optional[int] = None,
    year: Optional[int] = None,
    season_type: Optional[str] = None,
    week: Optional[int] = None,
    team: Optional[str] = None,
    home: Optional[str] = None,
    away: Optional[str] = None,
    conference: Optional[str] = None,
    provider: Optional[str] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /lines — Retrieves historical betting data

    Endpoint: ``GET https://api.collegefootballdata.com/lines``
    Example URL: https://api.collegefootballdata.com/lines

    Args:
        game_id: gameId query parameter.
        year: year query parameter.
        season_type: seasonType query parameter.
        week: week query parameter.
        team: team query parameter.
        home: home query parameter.
        away: away query parameter.
        conference: conference query parameter.
        provider: provider query parameter.
        headers: optional pre-minted auth headers dict (e.g. from nfl_headers_gen()) to reuse across calls; a fresh anonymous token is minted when omitted.
        return_parsed: parse the payload through parse_cfbd_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            cfbd_lines()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "gameId": game_id,
        "year": year,
        "seasonType": season_type,
        "week": week,
        "team": team,
        "home": home,
        "away": away,
        "conference": conference,
        "provider": provider,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.collegefootballdata.com/lines",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_cfbd_records(raw, return_as_pandas=return_as_pandas)
    return raw


def cfbd_live_plays(
    game_id: int,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /live/plays — Queries live play-by-play data and advanced stats

    Endpoint: ``GET https://api.collegefootballdata.com/live/plays``
    Example URL: https://api.collegefootballdata.com/live/plays

    Args:
        game_id: gameId query parameter.
        headers: optional pre-minted auth headers dict (e.g. from nfl_headers_gen()) to reuse across calls; a fresh anonymous token is minted when omitted.
        return_parsed: parse the payload through parse_cfbd_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            cfbd_live_plays()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "gameId": game_id,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.collegefootballdata.com/live/plays",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_cfbd_records(raw, return_as_pandas=return_as_pandas)
    return raw


def cfbd_metrics_fg_ep(
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /metrics/fg/ep — Queries field goal expected points values

    Endpoint: ``GET https://api.collegefootballdata.com/metrics/fg/ep``
    Example URL: https://api.collegefootballdata.com/metrics/fg/ep

    Args:
        headers: optional pre-minted auth headers dict (e.g. from nfl_headers_gen()) to reuse across calls; a fresh anonymous token is minted when omitted.
        return_parsed: parse the payload through parse_cfbd_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            cfbd_metrics_fg_ep()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {}
    _params.update(_caller_params)
    raw = _get(
        "https://api.collegefootballdata.com/metrics/fg/ep",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_cfbd_records(raw, return_as_pandas=return_as_pandas)
    return raw


def cfbd_metrics_wp(
    game_id: int,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /metrics/wp — Query play win probabilities by game

    Endpoint: ``GET https://api.collegefootballdata.com/metrics/wp``
    Example URL: https://api.collegefootballdata.com/metrics/wp

    Args:
        game_id: gameId query parameter.
        headers: optional pre-minted auth headers dict (e.g. from nfl_headers_gen()) to reuse across calls; a fresh anonymous token is minted when omitted.
        return_parsed: parse the payload through parse_cfbd_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            cfbd_metrics_wp()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "gameId": game_id,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.collegefootballdata.com/metrics/wp",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_cfbd_records(raw, return_as_pandas=return_as_pandas)
    return raw


def cfbd_metrics_wp_pregame(
    year: Optional[int] = None,
    week: Optional[int] = None,
    season_type: Optional[str] = None,
    team: Optional[str] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /metrics/wp/pregame — Queries pregame win probabilities

    Endpoint: ``GET https://api.collegefootballdata.com/metrics/wp/pregame``
    Example URL: https://api.collegefootballdata.com/metrics/wp/pregame

    Args:
        year: year query parameter.
        week: week query parameter.
        season_type: seasonType query parameter.
        team: team query parameter.
        headers: optional pre-minted auth headers dict (e.g. from nfl_headers_gen()) to reuse across calls; a fresh anonymous token is minted when omitted.
        return_parsed: parse the payload through parse_cfbd_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            cfbd_metrics_wp_pregame()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "year": year,
        "week": week,
        "seasonType": season_type,
        "team": team,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.collegefootballdata.com/metrics/wp/pregame",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_cfbd_records(raw, return_as_pandas=return_as_pandas)
    return raw


def cfbd_player_portal(
    year: int,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /player/portal — Retrieves transfer portal data for a given year

    Endpoint: ``GET https://api.collegefootballdata.com/player/portal``
    Example URL: https://api.collegefootballdata.com/player/portal

    Args:
        year: year query parameter.
        headers: optional pre-minted auth headers dict (e.g. from nfl_headers_gen()) to reuse across calls; a fresh anonymous token is minted when omitted.
        return_parsed: parse the payload through parse_cfbd_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            cfbd_player_portal()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "year": year,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.collegefootballdata.com/player/portal",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_cfbd_records(raw, return_as_pandas=return_as_pandas)
    return raw


def cfbd_player_returning(
    year: Optional[int] = None,
    team: Optional[str] = None,
    conference: Optional[str] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /player/returning — Retrieves returning production data. Either a year or team filter must be specified.

    Endpoint: ``GET https://api.collegefootballdata.com/player/returning``
    Example URL: https://api.collegefootballdata.com/player/returning

    Args:
        year: year query parameter.
        team: team query parameter.
        conference: conference query parameter.
        headers: optional pre-minted auth headers dict (e.g. from nfl_headers_gen()) to reuse across calls; a fresh anonymous token is minted when omitted.
        return_parsed: parse the payload through parse_cfbd_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            cfbd_player_returning()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "year": year,
        "team": team,
        "conference": conference,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.collegefootballdata.com/player/returning",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_cfbd_records(raw, return_as_pandas=return_as_pandas)
    return raw


def cfbd_player_search(
    search_term: str,
    year: Optional[int] = None,
    team: Optional[str] = None,
    position: Optional[str] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /player/search — Search for players (lists top 100 results)

    Endpoint: ``GET https://api.collegefootballdata.com/player/search``
    Example URL: https://api.collegefootballdata.com/player/search

    Args:
        search_term: searchTerm query parameter.
        year: year query parameter.
        team: team query parameter.
        position: position query parameter.
        headers: optional pre-minted auth headers dict (e.g. from nfl_headers_gen()) to reuse across calls; a fresh anonymous token is minted when omitted.
        return_parsed: parse the payload through parse_cfbd_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            cfbd_player_search()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "searchTerm": search_term,
        "year": year,
        "team": team,
        "position": position,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.collegefootballdata.com/player/search",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_cfbd_records(raw, return_as_pandas=return_as_pandas)
    return raw


def cfbd_player_usage(
    year: int,
    conference: Optional[str] = None,
    position: Optional[str] = None,
    team: Optional[str] = None,
    player_id: Optional[int] = None,
    exclude_garbage_time: Optional[bool] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /player/usage — Retrieves player usage data for a given season

    Endpoint: ``GET https://api.collegefootballdata.com/player/usage``
    Example URL: https://api.collegefootballdata.com/player/usage

    Args:
        year: year query parameter.
        conference: conference query parameter.
        position: position query parameter.
        team: team query parameter.
        player_id: playerId query parameter.
        exclude_garbage_time: excludeGarbageTime query parameter.
        headers: optional pre-minted auth headers dict (e.g. from nfl_headers_gen()) to reuse across calls; a fresh anonymous token is minted when omitted.
        return_parsed: parse the payload through parse_cfbd_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            cfbd_player_usage()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "year": year,
        "conference": conference,
        "position": position,
        "team": team,
        "playerId": player_id,
        "excludeGarbageTime": exclude_garbage_time,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.collegefootballdata.com/player/usage",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_cfbd_records(raw, return_as_pandas=return_as_pandas)
    return raw


def cfbd_plays(
    year: int,
    week: int,
    team: Optional[str] = None,
    offense: Optional[str] = None,
    defense: Optional[str] = None,
    offense_conference: Optional[str] = None,
    defense_conference: Optional[str] = None,
    conference: Optional[str] = None,
    play_type: Optional[str] = None,
    season_type: Optional[str] = None,
    classification: Optional[str] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /plays — Retrieves historical play data

    Endpoint: ``GET https://api.collegefootballdata.com/plays``
    Example URL: https://api.collegefootballdata.com/plays

    Args:
        year: year query parameter.
        week: week query parameter.
        team: team query parameter.
        offense: offense query parameter.
        defense: defense query parameter.
        offense_conference: offenseConference query parameter.
        defense_conference: defenseConference query parameter.
        conference: conference query parameter.
        play_type: playType query parameter.
        season_type: seasonType query parameter.
        classification: classification query parameter.
        headers: optional pre-minted auth headers dict (e.g. from nfl_headers_gen()) to reuse across calls; a fresh anonymous token is minted when omitted.
        return_parsed: parse the payload through parse_cfbd_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            cfbd_plays()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "year": year,
        "week": week,
        "team": team,
        "offense": offense,
        "defense": defense,
        "offenseConference": offense_conference,
        "defenseConference": defense_conference,
        "conference": conference,
        "playType": play_type,
        "seasonType": season_type,
        "classification": classification,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.collegefootballdata.com/plays",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_cfbd_records(raw, return_as_pandas=return_as_pandas)
    return raw


def cfbd_plays_stats(
    year: Optional[int] = None,
    week: Optional[int] = None,
    team: Optional[str] = None,
    game_id: Optional[int] = None,
    athlete_id: Optional[int] = None,
    stat_type_id: Optional[int] = None,
    season_type: Optional[str] = None,
    conference: Optional[str] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /plays/stats — Retrieve player-play associations (limit 2000)

    Endpoint: ``GET https://api.collegefootballdata.com/plays/stats``
    Example URL: https://api.collegefootballdata.com/plays/stats

    Args:
        year: year query parameter.
        week: week query parameter.
        team: team query parameter.
        game_id: gameId query parameter.
        athlete_id: athleteId query parameter.
        stat_type_id: statTypeId query parameter.
        season_type: seasonType query parameter.
        conference: conference query parameter.
        headers: optional pre-minted auth headers dict (e.g. from nfl_headers_gen()) to reuse across calls; a fresh anonymous token is minted when omitted.
        return_parsed: parse the payload through parse_cfbd_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            cfbd_plays_stats()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "year": year,
        "week": week,
        "team": team,
        "gameId": game_id,
        "athleteId": athlete_id,
        "statTypeId": stat_type_id,
        "seasonType": season_type,
        "conference": conference,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.collegefootballdata.com/plays/stats",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_cfbd_records(raw, return_as_pandas=return_as_pandas)
    return raw


def cfbd_plays_stats_types(
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /plays/stats/types — Retrieves available play stat types

    Endpoint: ``GET https://api.collegefootballdata.com/plays/stats/types``
    Example URL: https://api.collegefootballdata.com/plays/stats/types

    Args:
        headers: optional pre-minted auth headers dict (e.g. from nfl_headers_gen()) to reuse across calls; a fresh anonymous token is minted when omitted.
        return_parsed: parse the payload through parse_cfbd_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            cfbd_plays_stats_types()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {}
    _params.update(_caller_params)
    raw = _get(
        "https://api.collegefootballdata.com/plays/stats/types",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_cfbd_records(raw, return_as_pandas=return_as_pandas)
    return raw


def cfbd_plays_types(
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /plays/types — Retrieves available play types

    Endpoint: ``GET https://api.collegefootballdata.com/plays/types``
    Example URL: https://api.collegefootballdata.com/plays/types

    Args:
        headers: optional pre-minted auth headers dict (e.g. from nfl_headers_gen()) to reuse across calls; a fresh anonymous token is minted when omitted.
        return_parsed: parse the payload through parse_cfbd_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            cfbd_plays_types()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {}
    _params.update(_caller_params)
    raw = _get(
        "https://api.collegefootballdata.com/plays/types",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_cfbd_records(raw, return_as_pandas=return_as_pandas)
    return raw


def cfbd_ppa_games(
    year: int,
    week: Optional[int] = None,
    season_type: Optional[str] = None,
    team: Optional[str] = None,
    conference: Optional[str] = None,
    exclude_garbage_time: Optional[bool] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /ppa/games — Retrieves historical team PPA metrics by game

    Endpoint: ``GET https://api.collegefootballdata.com/ppa/games``
    Example URL: https://api.collegefootballdata.com/ppa/games

    Args:
        year: year query parameter.
        week: week query parameter.
        season_type: seasonType query parameter.
        team: team query parameter.
        conference: conference query parameter.
        exclude_garbage_time: excludeGarbageTime query parameter.
        headers: optional pre-minted auth headers dict (e.g. from nfl_headers_gen()) to reuse across calls; a fresh anonymous token is minted when omitted.
        return_parsed: parse the payload through parse_cfbd_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            cfbd_ppa_games()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "year": year,
        "week": week,
        "seasonType": season_type,
        "team": team,
        "conference": conference,
        "excludeGarbageTime": exclude_garbage_time,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.collegefootballdata.com/ppa/games",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_cfbd_records(raw, return_as_pandas=return_as_pandas)
    return raw


def cfbd_ppa_players_games(
    year: int,
    week: Optional[int] = None,
    season_type: Optional[str] = None,
    team: Optional[str] = None,
    position: Optional[str] = None,
    player_id: Optional[str] = None,
    threshold: Optional[float] = None,
    exclude_garbage_time: Optional[bool] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /ppa/players/games — Queries player PPA statistics by game

    Endpoint: ``GET https://api.collegefootballdata.com/ppa/players/games``
    Example URL: https://api.collegefootballdata.com/ppa/players/games

    Args:
        year: year query parameter.
        week: week query parameter.
        season_type: seasonType query parameter.
        team: team query parameter.
        position: position query parameter.
        player_id: playerId query parameter.
        threshold: threshold query parameter.
        exclude_garbage_time: excludeGarbageTime query parameter.
        headers: optional pre-minted auth headers dict (e.g. from nfl_headers_gen()) to reuse across calls; a fresh anonymous token is minted when omitted.
        return_parsed: parse the payload through parse_cfbd_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            cfbd_ppa_players_games()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "year": year,
        "week": week,
        "seasonType": season_type,
        "team": team,
        "position": position,
        "playerId": player_id,
        "threshold": threshold,
        "excludeGarbageTime": exclude_garbage_time,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.collegefootballdata.com/ppa/players/games",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_cfbd_records(raw, return_as_pandas=return_as_pandas)
    return raw


def cfbd_ppa_players_season(
    year: Optional[int] = None,
    conference: Optional[str] = None,
    team: Optional[str] = None,
    position: Optional[str] = None,
    player_id: Optional[str] = None,
    threshold: Optional[float] = None,
    exclude_garbage_time: Optional[bool] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /ppa/players/season — Queries player PPA statistics by season

    Endpoint: ``GET https://api.collegefootballdata.com/ppa/players/season``
    Example URL: https://api.collegefootballdata.com/ppa/players/season

    Args:
        year: year query parameter.
        conference: conference query parameter.
        team: team query parameter.
        position: position query parameter.
        player_id: playerId query parameter.
        threshold: threshold query parameter.
        exclude_garbage_time: excludeGarbageTime query parameter.
        headers: optional pre-minted auth headers dict (e.g. from nfl_headers_gen()) to reuse across calls; a fresh anonymous token is minted when omitted.
        return_parsed: parse the payload through parse_cfbd_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            cfbd_ppa_players_season()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "year": year,
        "conference": conference,
        "team": team,
        "position": position,
        "playerId": player_id,
        "threshold": threshold,
        "excludeGarbageTime": exclude_garbage_time,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.collegefootballdata.com/ppa/players/season",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_cfbd_records(raw, return_as_pandas=return_as_pandas)
    return raw


def cfbd_ppa_predicted(
    down: int,
    distance: int,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /ppa/predicted — Query Predicted Points values by down and distance

    Endpoint: ``GET https://api.collegefootballdata.com/ppa/predicted``
    Example URL: https://api.collegefootballdata.com/ppa/predicted

    Args:
        down: down query parameter.
        distance: distance query parameter.
        headers: optional pre-minted auth headers dict (e.g. from nfl_headers_gen()) to reuse across calls; a fresh anonymous token is minted when omitted.
        return_parsed: parse the payload through parse_cfbd_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            cfbd_ppa_predicted()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "down": down,
        "distance": distance,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.collegefootballdata.com/ppa/predicted",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_cfbd_records(raw, return_as_pandas=return_as_pandas)
    return raw


def cfbd_ppa_teams(
    year: Optional[int] = None,
    team: Optional[str] = None,
    conference: Optional[str] = None,
    exclude_garbage_time: Optional[bool] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /ppa/teams — Retrieves historical team PPA metrics by season

    Endpoint: ``GET https://api.collegefootballdata.com/ppa/teams``
    Example URL: https://api.collegefootballdata.com/ppa/teams

    Args:
        year: year query parameter.
        team: team query parameter.
        conference: conference query parameter.
        exclude_garbage_time: excludeGarbageTime query parameter.
        headers: optional pre-minted auth headers dict (e.g. from nfl_headers_gen()) to reuse across calls; a fresh anonymous token is minted when omitted.
        return_parsed: parse the payload through parse_cfbd_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            cfbd_ppa_teams()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "year": year,
        "team": team,
        "conference": conference,
        "excludeGarbageTime": exclude_garbage_time,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.collegefootballdata.com/ppa/teams",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_cfbd_records(raw, return_as_pandas=return_as_pandas)
    return raw


def cfbd_rankings(
    year: int,
    season_type: Optional[str] = None,
    week: Optional[float] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /rankings — Retrieves historical poll data

    Endpoint: ``GET https://api.collegefootballdata.com/rankings``
    Example URL: https://api.collegefootballdata.com/rankings

    Args:
        year: year query parameter.
        season_type: seasonType query parameter.
        week: week query parameter.
        headers: optional pre-minted auth headers dict (e.g. from nfl_headers_gen()) to reuse across calls; a fresh anonymous token is minted when omitted.
        return_parsed: parse the payload through parse_cfbd_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            cfbd_rankings()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "year": year,
        "seasonType": season_type,
        "week": week,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.collegefootballdata.com/rankings",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_cfbd_records(raw, return_as_pandas=return_as_pandas)
    return raw


def cfbd_ratings_elo(
    year: Optional[int] = None,
    week: Optional[int] = None,
    season_type: Optional[str] = None,
    team: Optional[str] = None,
    conference: Optional[str] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /ratings/elo — Retrieves historical Elo ratings

    Endpoint: ``GET https://api.collegefootballdata.com/ratings/elo``
    Example URL: https://api.collegefootballdata.com/ratings/elo

    Args:
        year: year query parameter.
        week: week query parameter.
        season_type: seasonType query parameter.
        team: team query parameter.
        conference: conference query parameter.
        headers: optional pre-minted auth headers dict (e.g. from nfl_headers_gen()) to reuse across calls; a fresh anonymous token is minted when omitted.
        return_parsed: parse the payload through parse_cfbd_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            cfbd_ratings_elo()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "year": year,
        "week": week,
        "seasonType": season_type,
        "team": team,
        "conference": conference,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.collegefootballdata.com/ratings/elo",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_cfbd_records(raw, return_as_pandas=return_as_pandas)
    return raw


def cfbd_ratings_fpi(
    year: Optional[int] = None,
    team: Optional[str] = None,
    conference: Optional[str] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /ratings/fpi — Retrieves historical Football Power Index (FPI) ratings

    Endpoint: ``GET https://api.collegefootballdata.com/ratings/fpi``
    Example URL: https://api.collegefootballdata.com/ratings/fpi

    Args:
        year: year query parameter.
        team: team query parameter.
        conference: conference query parameter.
        headers: optional pre-minted auth headers dict (e.g. from nfl_headers_gen()) to reuse across calls; a fresh anonymous token is minted when omitted.
        return_parsed: parse the payload through parse_cfbd_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            cfbd_ratings_fpi()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "year": year,
        "team": team,
        "conference": conference,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.collegefootballdata.com/ratings/fpi",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_cfbd_records(raw, return_as_pandas=return_as_pandas)
    return raw


def cfbd_ratings_sp(
    year: Optional[int] = None,
    team: Optional[str] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /ratings/sp — Retrieves SP+ ratings for a given year or school

    Endpoint: ``GET https://api.collegefootballdata.com/ratings/sp``
    Example URL: https://api.collegefootballdata.com/ratings/sp

    Args:
        year: year query parameter.
        team: team query parameter.
        headers: optional pre-minted auth headers dict (e.g. from nfl_headers_gen()) to reuse across calls; a fresh anonymous token is minted when omitted.
        return_parsed: parse the payload through parse_cfbd_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            cfbd_ratings_sp()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "year": year,
        "team": team,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.collegefootballdata.com/ratings/sp",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_cfbd_records(raw, return_as_pandas=return_as_pandas)
    return raw


def cfbd_ratings_sp_conferences(
    year: Optional[int] = None,
    conference: Optional[str] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /ratings/sp/conferences — Retrieves aggregated historical conference SP+ data

    Endpoint: ``GET https://api.collegefootballdata.com/ratings/sp/conferences``
    Example URL: https://api.collegefootballdata.com/ratings/sp/conferences

    Args:
        year: year query parameter.
        conference: conference query parameter.
        headers: optional pre-minted auth headers dict (e.g. from nfl_headers_gen()) to reuse across calls; a fresh anonymous token is minted when omitted.
        return_parsed: parse the payload through parse_cfbd_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            cfbd_ratings_sp_conferences()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "year": year,
        "conference": conference,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.collegefootballdata.com/ratings/sp/conferences",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_cfbd_records(raw, return_as_pandas=return_as_pandas)
    return raw


def cfbd_ratings_srs(
    year: Optional[int] = None,
    team: Optional[str] = None,
    conference: Optional[str] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /ratings/srs — Retrieves historical SRS for a year or team

    Endpoint: ``GET https://api.collegefootballdata.com/ratings/srs``
    Example URL: https://api.collegefootballdata.com/ratings/srs

    Args:
        year: year query parameter.
        team: team query parameter.
        conference: conference query parameter.
        headers: optional pre-minted auth headers dict (e.g. from nfl_headers_gen()) to reuse across calls; a fresh anonymous token is minted when omitted.
        return_parsed: parse the payload through parse_cfbd_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            cfbd_ratings_srs()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "year": year,
        "team": team,
        "conference": conference,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.collegefootballdata.com/ratings/srs",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_cfbd_records(raw, return_as_pandas=return_as_pandas)
    return raw


def cfbd_records(
    year: Optional[int] = None,
    team: Optional[str] = None,
    conference: Optional[str] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /records — Retrieves historical team records

    Endpoint: ``GET https://api.collegefootballdata.com/records``
    Example URL: https://api.collegefootballdata.com/records

    Args:
        year: year query parameter.
        team: team query parameter.
        conference: conference query parameter.
        headers: optional pre-minted auth headers dict (e.g. from nfl_headers_gen()) to reuse across calls; a fresh anonymous token is minted when omitted.
        return_parsed: parse the payload through parse_cfbd_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            cfbd_records()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "year": year,
        "team": team,
        "conference": conference,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.collegefootballdata.com/records",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_cfbd_records(raw, return_as_pandas=return_as_pandas)
    return raw


def cfbd_recruiting_groups(
    team: Optional[str] = None,
    conference: Optional[str] = None,
    recruit_type: Optional[str] = None,
    start_year: Optional[int] = None,
    end_year: Optional[int] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /recruiting/groups — Retrieves aggregated recruiting statistics by team and position grouping

    Endpoint: ``GET https://api.collegefootballdata.com/recruiting/groups``
    Example URL: https://api.collegefootballdata.com/recruiting/groups

    Args:
        team: team query parameter.
        conference: conference query parameter.
        recruit_type: recruitType query parameter.
        start_year: startYear query parameter.
        end_year: endYear query parameter.
        headers: optional pre-minted auth headers dict (e.g. from nfl_headers_gen()) to reuse across calls; a fresh anonymous token is minted when omitted.
        return_parsed: parse the payload through parse_cfbd_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            cfbd_recruiting_groups()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "team": team,
        "conference": conference,
        "recruitType": recruit_type,
        "startYear": start_year,
        "endYear": end_year,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.collegefootballdata.com/recruiting/groups",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_cfbd_records(raw, return_as_pandas=return_as_pandas)
    return raw


def cfbd_recruiting_players(
    year: Optional[int] = None,
    team: Optional[str] = None,
    position: Optional[str] = None,
    state: Optional[str] = None,
    classification: Optional[str] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /recruiting/players — Retrieves player recruiting rankings

    Endpoint: ``GET https://api.collegefootballdata.com/recruiting/players``
    Example URL: https://api.collegefootballdata.com/recruiting/players

    Args:
        year: year query parameter.
        team: team query parameter.
        position: position query parameter.
        state: state query parameter.
        classification: classification query parameter.
        headers: optional pre-minted auth headers dict (e.g. from nfl_headers_gen()) to reuse across calls; a fresh anonymous token is minted when omitted.
        return_parsed: parse the payload through parse_cfbd_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            cfbd_recruiting_players()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "year": year,
        "team": team,
        "position": position,
        "state": state,
        "classification": classification,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.collegefootballdata.com/recruiting/players",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_cfbd_records(raw, return_as_pandas=return_as_pandas)
    return raw


def cfbd_recruiting_teams(
    year: Optional[int] = None,
    team: Optional[str] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /recruiting/teams — Retrieves team recruiting rankings

    Endpoint: ``GET https://api.collegefootballdata.com/recruiting/teams``
    Example URL: https://api.collegefootballdata.com/recruiting/teams

    Args:
        year: year query parameter.
        team: team query parameter.
        headers: optional pre-minted auth headers dict (e.g. from nfl_headers_gen()) to reuse across calls; a fresh anonymous token is minted when omitted.
        return_parsed: parse the payload through parse_cfbd_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            cfbd_recruiting_teams()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "year": year,
        "team": team,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.collegefootballdata.com/recruiting/teams",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_cfbd_records(raw, return_as_pandas=return_as_pandas)
    return raw


def cfbd_roster(
    team: Optional[str] = None,
    year: Optional[int] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /roster — Retrieves historical roster data

    Endpoint: ``GET https://api.collegefootballdata.com/roster``
    Example URL: https://api.collegefootballdata.com/roster

    Args:
        team: team query parameter.
        year: year query parameter.
        headers: optional pre-minted auth headers dict (e.g. from nfl_headers_gen()) to reuse across calls; a fresh anonymous token is minted when omitted.
        return_parsed: parse the payload through parse_cfbd_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            cfbd_roster()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "team": team,
        "year": year,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.collegefootballdata.com/roster",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_cfbd_records(raw, return_as_pandas=return_as_pandas)
    return raw


def cfbd_scoreboard(
    classification: Optional[str] = None,
    conference: Optional[str] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /scoreboard — Retrieves live scoreboard data

    Endpoint: ``GET https://api.collegefootballdata.com/scoreboard``
    Example URL: https://api.collegefootballdata.com/scoreboard

    Args:
        classification: classification query parameter.
        conference: conference query parameter.
        headers: optional pre-minted auth headers dict (e.g. from nfl_headers_gen()) to reuse across calls; a fresh anonymous token is minted when omitted.
        return_parsed: parse the payload through parse_cfbd_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            cfbd_scoreboard()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "classification": classification,
        "conference": conference,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.collegefootballdata.com/scoreboard",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_cfbd_records(raw, return_as_pandas=return_as_pandas)
    return raw


def cfbd_stats_categories(
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /stats/categories — Gets team statistical categories

    Endpoint: ``GET https://api.collegefootballdata.com/stats/categories``
    Example URL: https://api.collegefootballdata.com/stats/categories

    Args:
        headers: optional pre-minted auth headers dict (e.g. from nfl_headers_gen()) to reuse across calls; a fresh anonymous token is minted when omitted.
        return_parsed: parse the payload through parse_cfbd_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            cfbd_stats_categories()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {}
    _params.update(_caller_params)
    raw = _get(
        "https://api.collegefootballdata.com/stats/categories",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_cfbd_records(raw, return_as_pandas=return_as_pandas)
    return raw


def cfbd_stats_game_advanced(
    year: Optional[int] = None,
    team: Optional[str] = None,
    week: Optional[float] = None,
    opponent: Optional[str] = None,
    exclude_garbage_time: Optional[bool] = None,
    season_type: Optional[str] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /stats/game/advanced — Retrieves advanced statistics aggregated by game

    Endpoint: ``GET https://api.collegefootballdata.com/stats/game/advanced``
    Example URL: https://api.collegefootballdata.com/stats/game/advanced

    Args:
        year: year query parameter.
        team: team query parameter.
        week: week query parameter.
        opponent: opponent query parameter.
        exclude_garbage_time: excludeGarbageTime query parameter.
        season_type: seasonType query parameter.
        headers: optional pre-minted auth headers dict (e.g. from nfl_headers_gen()) to reuse across calls; a fresh anonymous token is minted when omitted.
        return_parsed: parse the payload through parse_cfbd_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            cfbd_stats_game_advanced()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "year": year,
        "team": team,
        "week": week,
        "opponent": opponent,
        "excludeGarbageTime": exclude_garbage_time,
        "seasonType": season_type,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.collegefootballdata.com/stats/game/advanced",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_cfbd_records(raw, return_as_pandas=return_as_pandas)
    return raw


def cfbd_stats_player_season(
    year: int,
    conference: Optional[str] = None,
    team: Optional[str] = None,
    start_week: Optional[int] = None,
    end_week: Optional[int] = None,
    season_type: Optional[str] = None,
    category: Optional[str] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /stats/player/season — Retrieves aggregated player statistics for a given season

    Endpoint: ``GET https://api.collegefootballdata.com/stats/player/season``
    Example URL: https://api.collegefootballdata.com/stats/player/season

    Args:
        year: year query parameter.
        conference: conference query parameter.
        team: team query parameter.
        start_week: startWeek query parameter.
        end_week: endWeek query parameter.
        season_type: seasonType query parameter.
        category: category query parameter.
        headers: optional pre-minted auth headers dict (e.g. from nfl_headers_gen()) to reuse across calls; a fresh anonymous token is minted when omitted.
        return_parsed: parse the payload through parse_cfbd_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            cfbd_stats_player_season()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "year": year,
        "conference": conference,
        "team": team,
        "startWeek": start_week,
        "endWeek": end_week,
        "seasonType": season_type,
        "category": category,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.collegefootballdata.com/stats/player/season",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_cfbd_records(raw, return_as_pandas=return_as_pandas)
    return raw


def cfbd_stats_season(
    year: Optional[int] = None,
    team: Optional[str] = None,
    conference: Optional[str] = None,
    start_week: Optional[int] = None,
    end_week: Optional[int] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /stats/season — Retrieves aggregated team season statistics

    Endpoint: ``GET https://api.collegefootballdata.com/stats/season``
    Example URL: https://api.collegefootballdata.com/stats/season

    Args:
        year: year query parameter.
        team: team query parameter.
        conference: conference query parameter.
        start_week: startWeek query parameter.
        end_week: endWeek query parameter.
        headers: optional pre-minted auth headers dict (e.g. from nfl_headers_gen()) to reuse across calls; a fresh anonymous token is minted when omitted.
        return_parsed: parse the payload through parse_cfbd_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            cfbd_stats_season()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "year": year,
        "team": team,
        "conference": conference,
        "startWeek": start_week,
        "endWeek": end_week,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.collegefootballdata.com/stats/season",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_cfbd_records(raw, return_as_pandas=return_as_pandas)
    return raw


def cfbd_stats_season_advanced(
    year: Optional[int] = None,
    team: Optional[str] = None,
    exclude_garbage_time: Optional[bool] = None,
    start_week: Optional[int] = None,
    end_week: Optional[int] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /stats/season/advanced — Retrieves advanced season statistics for teams

    Endpoint: ``GET https://api.collegefootballdata.com/stats/season/advanced``
    Example URL: https://api.collegefootballdata.com/stats/season/advanced

    Args:
        year: year query parameter.
        team: team query parameter.
        exclude_garbage_time: excludeGarbageTime query parameter.
        start_week: startWeek query parameter.
        end_week: endWeek query parameter.
        headers: optional pre-minted auth headers dict (e.g. from nfl_headers_gen()) to reuse across calls; a fresh anonymous token is minted when omitted.
        return_parsed: parse the payload through parse_cfbd_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            cfbd_stats_season_advanced()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "year": year,
        "team": team,
        "excludeGarbageTime": exclude_garbage_time,
        "startWeek": start_week,
        "endWeek": end_week,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.collegefootballdata.com/stats/season/advanced",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_cfbd_records(raw, return_as_pandas=return_as_pandas)
    return raw


def cfbd_talent(
    year: int,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /talent — Retrieve 247 Team Talent Composite for a given year

    Endpoint: ``GET https://api.collegefootballdata.com/talent``
    Example URL: https://api.collegefootballdata.com/talent

    Args:
        year: year query parameter.
        headers: optional pre-minted auth headers dict (e.g. from nfl_headers_gen()) to reuse across calls; a fresh anonymous token is minted when omitted.
        return_parsed: parse the payload through parse_cfbd_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            cfbd_talent()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "year": year,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.collegefootballdata.com/talent",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_cfbd_records(raw, return_as_pandas=return_as_pandas)
    return raw


def cfbd_teams(
    conference: Optional[str] = None,
    year: Optional[int] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /teams — Retrieves team information

    Endpoint: ``GET https://api.collegefootballdata.com/teams``
    Example URL: https://api.collegefootballdata.com/teams

    Args:
        conference: conference query parameter.
        year: year query parameter.
        headers: optional pre-minted auth headers dict (e.g. from nfl_headers_gen()) to reuse across calls; a fresh anonymous token is minted when omitted.
        return_parsed: parse the payload through parse_cfbd_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            cfbd_teams()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "conference": conference,
        "year": year,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.collegefootballdata.com/teams",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_cfbd_records(raw, return_as_pandas=return_as_pandas)
    return raw


def cfbd_teams_fbs(
    year: Optional[int] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /teams/fbs — Retrieves information on teams playing in the highest division of CFB

    Endpoint: ``GET https://api.collegefootballdata.com/teams/fbs``
    Example URL: https://api.collegefootballdata.com/teams/fbs

    Args:
        year: year query parameter.
        headers: optional pre-minted auth headers dict (e.g. from nfl_headers_gen()) to reuse across calls; a fresh anonymous token is minted when omitted.
        return_parsed: parse the payload through parse_cfbd_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            cfbd_teams_fbs()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "year": year,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.collegefootballdata.com/teams/fbs",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_cfbd_records(raw, return_as_pandas=return_as_pandas)
    return raw


def cfbd_teams_matchup(
    team1: str,
    team2: str,
    min_year: Optional[int] = None,
    max_year: Optional[int] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /teams/matchup — Retrieves historical matchup details for two given teams

    Endpoint: ``GET https://api.collegefootballdata.com/teams/matchup``
    Example URL: https://api.collegefootballdata.com/teams/matchup

    Args:
        team1: team1 query parameter.
        team2: team2 query parameter.
        min_year: minYear query parameter.
        max_year: maxYear query parameter.
        headers: optional pre-minted auth headers dict (e.g. from nfl_headers_gen()) to reuse across calls; a fresh anonymous token is minted when omitted.
        return_parsed: parse the payload through parse_cfbd_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            cfbd_teams_matchup()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "team1": team1,
        "team2": team2,
        "minYear": min_year,
        "maxYear": max_year,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.collegefootballdata.com/teams/matchup",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_cfbd_records(raw, return_as_pandas=return_as_pandas)
    return raw


def cfbd_venues(
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /venues — Retrieve list of venues

    Endpoint: ``GET https://api.collegefootballdata.com/venues``
    Example URL: https://api.collegefootballdata.com/venues

    Args:
        headers: optional pre-minted auth headers dict (e.g. from nfl_headers_gen()) to reuse across calls; a fresh anonymous token is minted when omitted.
        return_parsed: parse the payload through parse_cfbd_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            cfbd_venues()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {}
    _params.update(_caller_params)
    raw = _get(
        "https://api.collegefootballdata.com/venues",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_cfbd_records(raw, return_as_pandas=return_as_pandas)
    return raw


def cfbd_wepa_players_kicking(
    year: Optional[int] = None,
    team: Optional[str] = None,
    conference: Optional[str] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /wepa/players/kicking — Retrieve Points Added Above Replacement (PAAR) ratings for kickers

    Endpoint: ``GET https://api.collegefootballdata.com/wepa/players/kicking``
    Example URL: https://api.collegefootballdata.com/wepa/players/kicking

    Args:
        year: year query parameter.
        team: team query parameter.
        conference: conference query parameter.
        headers: optional pre-minted auth headers dict (e.g. from nfl_headers_gen()) to reuse across calls; a fresh anonymous token is minted when omitted.
        return_parsed: parse the payload through parse_cfbd_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            cfbd_wepa_players_kicking()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "year": year,
        "team": team,
        "conference": conference,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.collegefootballdata.com/wepa/players/kicking",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_cfbd_records(raw, return_as_pandas=return_as_pandas)
    return raw


def cfbd_wepa_players_passing(
    year: Optional[int] = None,
    team: Optional[str] = None,
    conference: Optional[str] = None,
    position: Optional[str] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /wepa/players/passing — Retrieve opponent-adjusted player passing statistics

    Endpoint: ``GET https://api.collegefootballdata.com/wepa/players/passing``
    Example URL: https://api.collegefootballdata.com/wepa/players/passing

    Args:
        year: year query parameter.
        team: team query parameter.
        conference: conference query parameter.
        position: position query parameter.
        headers: optional pre-minted auth headers dict (e.g. from nfl_headers_gen()) to reuse across calls; a fresh anonymous token is minted when omitted.
        return_parsed: parse the payload through parse_cfbd_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            cfbd_wepa_players_passing()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "year": year,
        "team": team,
        "conference": conference,
        "position": position,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.collegefootballdata.com/wepa/players/passing",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_cfbd_records(raw, return_as_pandas=return_as_pandas)
    return raw


def cfbd_wepa_players_rushing(
    year: Optional[int] = None,
    team: Optional[str] = None,
    conference: Optional[str] = None,
    position: Optional[str] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /wepa/players/rushing — Retrieve opponent-adjusted player rushing statistics

    Endpoint: ``GET https://api.collegefootballdata.com/wepa/players/rushing``
    Example URL: https://api.collegefootballdata.com/wepa/players/rushing

    Args:
        year: year query parameter.
        team: team query parameter.
        conference: conference query parameter.
        position: position query parameter.
        headers: optional pre-minted auth headers dict (e.g. from nfl_headers_gen()) to reuse across calls; a fresh anonymous token is minted when omitted.
        return_parsed: parse the payload through parse_cfbd_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            cfbd_wepa_players_rushing()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "year": year,
        "team": team,
        "conference": conference,
        "position": position,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.collegefootballdata.com/wepa/players/rushing",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_cfbd_records(raw, return_as_pandas=return_as_pandas)
    return raw


def cfbd_wepa_team_season(
    year: Optional[int] = None,
    team: Optional[str] = None,
    conference: Optional[str] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /wepa/team/season — Retrieve opponent-adjusted team season statistics

    Endpoint: ``GET https://api.collegefootballdata.com/wepa/team/season``
    Example URL: https://api.collegefootballdata.com/wepa/team/season

    Args:
        year: year query parameter.
        team: team query parameter.
        conference: conference query parameter.
        headers: optional pre-minted auth headers dict (e.g. from nfl_headers_gen()) to reuse across calls; a fresh anonymous token is minted when omitted.
        return_parsed: parse the payload through parse_cfbd_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            cfbd_wepa_team_season()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "year": year,
        "team": team,
        "conference": conference,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://api.collegefootballdata.com/wepa/team/season",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_cfbd_records(raw, return_as_pandas=return_as_pandas)
    return raw
