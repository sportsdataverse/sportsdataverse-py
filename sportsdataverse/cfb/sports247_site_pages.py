# GENERATED by tools/codegen/generate.py -- DO NOT EDIT.
# Source: tools/codegen/endpoints/sports247_site_pages.yaml
"""sports247_site_pages wrappers (host https://247sports.com). Generated."""

from __future__ import annotations

from typing import TYPE_CHECKING, Dict, List, Optional, Union  # noqa: F401

from sportsdataverse.cfb.sports247_site_pages_runtime import _get
from sportsdataverse.cfb.sports247_site_pages_parsers import parse_sports247_site_page

if TYPE_CHECKING:  # pragma: no cover -- annotation-only imports (PEP 563 defers eval)
    import pandas as pd
    import polars as pl

__all__ = [
    "sports247_site_pages_coach",
    "sports247_site_pages_coach_alma_mater",
    "sports247_site_pages_coach_hometown",
    "sports247_site_pages_coach_ranking",
    "sports247_site_pages_coach_rankings",
    "sports247_site_pages_event",
    "sports247_site_pages_institution",
    "sports247_site_pages_institution_list",
    "sports247_site_pages_institution_location",
    "sports247_site_pages_institution_timeline_events",
    "sports247_site_pages_league_draft_picks",
    "sports247_site_pages_league_institutions",
    "sports247_site_pages_page_feeds",
    "sports247_site_pages_player",
    "sports247_site_pages_player_current_institution",
    "sports247_site_pages_player_high_school",
    "sports247_site_pages_player_institution",
    "sports247_site_pages_player_institution_evaluation",
    "sports247_site_pages_player_primary_sport",
    "sports247_site_pages_player_search",
    "sports247_site_pages_playersport",
    "sports247_site_pages_playersport_institution",
    "sports247_site_pages_playersport_rank_history",
    "sports247_site_pages_position_rankings",
    "sports247_site_pages_recruit_interest",
    "sports247_site_pages_recruitment_final_choice",
    "sports247_site_pages_recruitment_institution",
    "sports247_site_pages_recruitment_interests",
    "sports247_site_pages_recruitment_offers",
    "sports247_site_pages_recruitment_player_sport",
    "sports247_site_pages_season_current_expert_predictions",
    "sports247_site_pages_season_recruit_interest_events",
    "sports247_site_pages_season_recruit_interests",
    "sports247_site_pages_season_recruits",
    "sports247_site_pages_season_roster_embed",
]


def sports247_site_pages_coach(
    key: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Coach identity detail.

    Endpoint: ``GET https://247sports.com/Coach/{key}.json``
    Example URL: https://247sports.com/Coach

    Args:
        key: key path parameter.
        return_parsed: parse the payload through parse_sports247_site_page -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            sports247_site_pages_coach()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://247sports.com/Coach/{key}.json",
        params=_params,
    )
    if return_parsed:
        return parse_sports247_site_page(raw, return_as_pandas=return_as_pandas)
    return raw


def sports247_site_pages_coach_alma_mater(
    key: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Coach alma-mater Institution.

    Endpoint: ``GET https://247sports.com/Coach/{key}/AlmaMater.json``
    Example URL: https://247sports.com/Coach/1504/AlmaMater.json

    Args:
        key: key path parameter.
        return_parsed: parse the payload through parse_sports247_site_page -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            sports247_site_pages_coach_alma_mater(key=1504)
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://247sports.com/Coach/{key}/AlmaMater.json",
        params=_params,
    )
    if return_parsed:
        return parse_sports247_site_page(raw, return_as_pandas=return_as_pandas)
    return raw


def sports247_site_pages_coach_hometown(
    key: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Coach hometown Location.

    Endpoint: ``GET https://247sports.com/Coach/{key}/Hometown.json``
    Example URL: https://247sports.com/Coach/1504/Hometown.json

    Args:
        key: key path parameter.
        return_parsed: parse the payload through parse_sports247_site_page -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            sports247_site_pages_coach_hometown(key=1504)
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://247sports.com/Coach/{key}/Hometown.json",
        params=_params,
    )
    if return_parsed:
        return parse_sports247_site_page(raw, return_as_pandas=return_as_pandas)
    return raw


def sports247_site_pages_coach_ranking(
    key: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Single CoachRanking row.

    Endpoint: ``GET https://247sports.com/CoachRanking/{key}.json``
    Example URL: https://247sports.com/CoachRanking

    Args:
        key: key path parameter.
        return_parsed: parse the payload through parse_sports247_site_page -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            sports247_site_pages_coach_ranking()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://247sports.com/CoachRanking/{key}.json",
        params=_params,
    )
    if return_parsed:
        return parse_sports247_site_page(raw, return_as_pandas=return_as_pandas)
    return raw


def sports247_site_pages_coach_rankings(
    key: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Coach's recruiting-ranking history (one row per Ranking snapshot).

    Endpoint: ``GET https://247sports.com/Coach/{key}/CoachRankings.json``
    Example URL: https://247sports.com/Coach/1531/CoachRankings.json

    Args:
        key: key path parameter.
        return_parsed: parse the payload through parse_sports247_site_page -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            sports247_site_pages_coach_rankings(key=1531)
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://247sports.com/Coach/{key}/CoachRankings.json",
        params=_params,
    )
    if return_parsed:
        return parse_sports247_site_page(raw, return_as_pandas=return_as_pandas)
    return raw


def sports247_site_pages_event(
    slug: str,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Recruiting event detail (camp/combine/regional).

    Endpoint: ``GET https://247sports.com/Event/{slug}.json``
    Example URL: https://247sports.com/Event

    Args:
        slug: slug path parameter.
        return_parsed: parse the payload through parse_sports247_site_page -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            sports247_site_pages_event()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://247sports.com/Event/{slug}.json",
        params=_params,
    )
    if return_parsed:
        return parse_sports247_site_page(raw, return_as_pandas=return_as_pandas)
    return raw


def sports247_site_pages_institution(
    key: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Institution (school/team) detail.

    Endpoint: ``GET https://247sports.com/Institution/{key}.json``
    Example URL: https://247sports.com/Institution

    Args:
        key: key path parameter.
        return_parsed: parse the payload through parse_sports247_site_page -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            sports247_site_pages_institution()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://247sports.com/Institution/{key}.json",
        params=_params,
    )
    if return_parsed:
        return parse_sports247_site_page(raw, return_as_pandas=return_as_pandas)
    return raw


def sports247_site_pages_institution_list(
    items: Optional[int] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Institution directory (paginated list).

    Endpoint: ``GET https://247sports.com/Institution.json``
    Example URL: https://247sports.com/Institution.json

    Args:
        items: items query parameter.
        return_parsed: parse the payload through parse_sports247_site_page -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            sports247_site_pages_institution_list()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "items": items,
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        "https://247sports.com/Institution.json",
        params=_params,
    )
    if return_parsed:
        return parse_sports247_site_page(raw, return_as_pandas=return_as_pandas)
    return raw


def sports247_site_pages_institution_location(
    key: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Institution location (city/state/coords/tax).

    Endpoint: ``GET https://247sports.com/Institution/{key}/Location.json``
    Example URL: https://247sports.com/Institution/24099/Location.json

    Args:
        key: key path parameter.
        return_parsed: parse the payload through parse_sports247_site_page -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            sports247_site_pages_institution_location(key=24099)
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://247sports.com/Institution/{key}/Location.json",
        params=_params,
    )
    if return_parsed:
        return parse_sports247_site_page(raw, return_as_pandas=return_as_pandas)
    return raw


def sports247_site_pages_institution_timeline_events(
    school_slug: str,
    key: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Institution recruiting timeline (site-authored event blurbs).

    Endpoint: ``GET https://247sports.com/college/{school_slug}/Institution/{key}/TimelineEvents.json``
    Example URL: https://247sports.com/college/florida/Institution/24099/TimelineEvents.json

    Args:
        school_slug: school_slug path parameter.
        key: key path parameter.
        return_parsed: parse the payload through parse_sports247_site_page -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            sports247_site_pages_institution_timeline_events(key=24099, school_slug='florida')
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://247sports.com/college/{school_slug}/Institution/{key}/TimelineEvents.json",
        params=_params,
    )
    if return_parsed:
        return parse_sports247_site_page(raw, return_as_pandas=return_as_pandas)
    return raw


def sports247_site_pages_league_draft_picks(
    league_slug: str,
    year: Optional[int] = None,
    round: Optional[int] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Pro-draft picks embed for a league/year/round.

    Endpoint: ``GET https://247sports.com/League/{league_slug}/DraftPicks/ConfigureEmbed/.json``
    Example URL: https://247sports.com/League/NFL/DraftPicks/ConfigureEmbed/.json

    Args:
        league_slug: league_slug path parameter.
        year: year query parameter.
        round: round query parameter.
        return_parsed: parse the payload through parse_sports247_site_page -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            sports247_site_pages_league_draft_picks(league_slug='NFL')
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "year": year,
        "round": round,
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://247sports.com/League/{league_slug}/DraftPicks/ConfigureEmbed/.json",
        params=_params,
    )
    if return_parsed:
        return parse_sports247_site_page(raw, return_as_pandas=return_as_pandas)
    return raw


def sports247_site_pages_league_institutions(
    league_id: int,
    items: Optional[int] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Institutions belonging to a league.

    Endpoint: ``GET https://247sports.com/League/{league_id}/Institutions.json``
    Example URL: https://247sports.com/League/6/Institutions.json

    Args:
        league_id: league_id path parameter.
        items: items query parameter.
        return_parsed: parse the payload through parse_sports247_site_page -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            sports247_site_pages_league_institutions(league_id=6)
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "items": items,
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://247sports.com/League/{league_id}/Institutions.json",
        params=_params,
    )
    if return_parsed:
        return parse_sports247_site_page(raw, return_as_pandas=return_as_pandas)
    return raw


def sports247_site_pages_page_feeds(
    page_id: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """News/headline feed items for a site Page.

    Endpoint: ``GET https://247sports.com/Page/{page_id}/Feeds.json``
    Example URL: https://247sports.com/Page/100134/Feeds.json

    Args:
        page_id: page_id path parameter.
        return_parsed: parse the payload through parse_sports247_site_page -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            sports247_site_pages_page_feeds(page_id=100134)
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://247sports.com/Page/{page_id}/Feeds.json",
        params=_params,
    )
    if return_parsed:
        return parse_sports247_site_page(raw, return_as_pandas=return_as_pandas)
    return raw


def sports247_site_pages_player(
    key: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Player detail (identity + primary-sport rating/ranks).

    Endpoint: ``GET https://247sports.com/Player/{key}.json``
    Example URL: https://247sports.com/Player

    Args:
        key: key path parameter.
        return_parsed: parse the payload through parse_sports247_site_page -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            sports247_site_pages_player()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://247sports.com/Player/{key}.json",
        params=_params,
    )
    if return_parsed:
        return parse_sports247_site_page(raw, return_as_pandas=return_as_pandas)
    return raw


def sports247_site_pages_player_current_institution(
    key: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Player's current PlayerInstitution (committed/enrolled school).

    Endpoint: ``GET https://247sports.com/Player/{key}/CurrentPlayerInstitution.json``
    Example URL: https://247sports.com/Player/46083769/CurrentPlayerInstitution.json

    Args:
        key: key path parameter.
        return_parsed: parse the payload through parse_sports247_site_page -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            sports247_site_pages_player_current_institution(key=46083769)
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://247sports.com/Player/{key}/CurrentPlayerInstitution.json",
        params=_params,
    )
    if return_parsed:
        return parse_sports247_site_page(raw, return_as_pandas=return_as_pandas)
    return raw


def sports247_site_pages_player_high_school(
    key: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Player's high-school PlayerInstitution row.

    Endpoint: ``GET https://247sports.com/Player/{key}/PlayerHighSchool.json``
    Example URL: https://247sports.com/Player/46051367/PlayerHighSchool.json

    Args:
        key: key path parameter.
        return_parsed: parse the payload through parse_sports247_site_page -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            sports247_site_pages_player_high_school(key=46051367)
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://247sports.com/Player/{key}/PlayerHighSchool.json",
        params=_params,
    )
    if return_parsed:
        return parse_sports247_site_page(raw, return_as_pandas=return_as_pandas)
    return raw


def sports247_site_pages_player_institution(
    key: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Player-at-institution association detail.

    Endpoint: ``GET https://247sports.com/PlayerInstitution/{key}.json``
    Example URL: https://247sports.com/PlayerInstitution

    Args:
        key: key path parameter.
        return_parsed: parse the payload through parse_sports247_site_page -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            sports247_site_pages_player_institution()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://247sports.com/PlayerInstitution/{key}.json",
        params=_params,
    )
    if return_parsed:
        return parse_sports247_site_page(raw, return_as_pandas=return_as_pandas)
    return raw


def sports247_site_pages_player_institution_evaluation(
    key: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Scout evaluation of a player-institution fit.

    Endpoint: ``GET https://247sports.com/PlayerInstitutionEvaluation/{key}.json``
    Example URL: https://247sports.com/PlayerInstitutionEvaluation

    Args:
        key: key path parameter.
        return_parsed: parse the payload through parse_sports247_site_page -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            sports247_site_pages_player_institution_evaluation()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://247sports.com/PlayerInstitutionEvaluation/{key}.json",
        params=_params,
    )
    if return_parsed:
        return parse_sports247_site_page(raw, return_as_pandas=return_as_pandas)
    return raw


def sports247_site_pages_player_primary_sport(
    key: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Player's primary PlayerSport (rating/class/positions).

    Endpoint: ``GET https://247sports.com/Player/{key}/PrimaryPlayerSport.json``
    Example URL: https://247sports.com/Player/46051367/PrimaryPlayerSport.json

    Args:
        key: key path parameter.
        return_parsed: parse the payload through parse_sports247_site_page -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            sports247_site_pages_player_primary_sport(key=46051367)
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://247sports.com/Player/{key}/PrimaryPlayerSport.json",
        params=_params,
    )
    if return_parsed:
        return parse_sports247_site_page(raw, return_as_pandas=return_as_pandas)
    return raw


def sports247_site_pages_player_search(
    first_name: Optional[str] = None,
    last_name: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Player name search.

    Endpoint: ``GET https://247sports.com/Player.json``
    Example URL: https://247sports.com/Player.json

    Args:
        first_name: FirstName query parameter.
        last_name: LastName query parameter.
        return_parsed: parse the payload through parse_sports247_site_page -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            sports247_site_pages_player_search()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "FirstName": first_name,
        "LastName": last_name,
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        "https://247sports.com/Player.json",
        params=_params,
    )
    if return_parsed:
        return parse_sports247_site_page(raw, return_as_pandas=return_as_pandas)
    return raw


def sports247_site_pages_playersport(
    key: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """PlayerSport detail (note lowercase route segment).

    Endpoint: ``GET https://247sports.com/playersport/{key}.json``
    Example URL: https://247sports.com/playersport

    Args:
        key: key path parameter.
        return_parsed: parse the payload through parse_sports247_site_page -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            sports247_site_pages_playersport()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://247sports.com/playersport/{key}.json",
        params=_params,
    )
    if return_parsed:
        return parse_sports247_site_page(raw, return_as_pandas=return_as_pandas)
    return raw


def sports247_site_pages_playersport_institution(
    key: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """PlayerInstitution linked to a PlayerSport.

    Endpoint: ``GET https://247sports.com/PlayerSport/{key}/PlayerInstitution.json``
    Example URL: https://247sports.com/PlayerSport/279200/PlayerInstitution.json

    Args:
        key: key path parameter.
        return_parsed: parse the payload through parse_sports247_site_page -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            sports247_site_pages_playersport_institution(key=279200)
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://247sports.com/PlayerSport/{key}/PlayerInstitution.json",
        params=_params,
    )
    if return_parsed:
        return parse_sports247_site_page(raw, return_as_pandas=return_as_pandas)
    return raw


def sports247_site_pages_playersport_rank_history(
    key: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Ranking history for a PlayerSport (one row per Ranking snapshot).

    Endpoint: ``GET https://247sports.com/PlayerSport/{key}/RecruitRankHistory.json``
    Example URL: https://247sports.com/PlayerSport/250563/RecruitRankHistory.json

    Args:
        key: key path parameter.
        return_parsed: parse the payload through parse_sports247_site_page -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            sports247_site_pages_playersport_rank_history(key=250563)
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://247sports.com/PlayerSport/{key}/RecruitRankHistory.json",
        params=_params,
    )
    if return_parsed:
        return parse_sports247_site_page(raw, return_as_pandas=return_as_pandas)
    return raw


def sports247_site_pages_position_rankings(
    key: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Player-sport rankings for a position.

    Endpoint: ``GET https://247sports.com/Position/{key}/playersportrankings.json``
    Example URL: https://247sports.com/Position/14/playersportrankings.json

    Args:
        key: key path parameter.
        return_parsed: parse the payload through parse_sports247_site_page -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            sports247_site_pages_position_rankings(key=14)
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://247sports.com/Position/{key}/playersportrankings.json",
        params=_params,
    )
    if return_parsed:
        return parse_sports247_site_page(raw, return_as_pandas=return_as_pandas)
    return raw


def sports247_site_pages_recruit_interest(
    key: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Single recruit-interest (school<->recruit link) detail.

    Endpoint: ``GET https://247sports.com/RecruitInterest/{key}.json``
    Example URL: https://247sports.com/RecruitInterest

    Args:
        key: key path parameter.
        return_parsed: parse the payload through parse_sports247_site_page -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            sports247_site_pages_recruit_interest()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://247sports.com/RecruitInterest/{key}.json",
        params=_params,
    )
    if return_parsed:
        return parse_sports247_site_page(raw, return_as_pandas=return_as_pandas)
    return raw


def sports247_site_pages_recruitment_final_choice(
    key: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Final-choice PlayerSport/commit for a recruitment.

    Endpoint: ``GET https://247sports.com/Recruitment/{key}/FinalChoice.json``
    Example URL: https://247sports.com/Recruitment/114978/FinalChoice.json

    Args:
        key: key path parameter.
        return_parsed: parse the payload through parse_sports247_site_page -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            sports247_site_pages_recruitment_final_choice(key=114978)
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://247sports.com/Recruitment/{key}/FinalChoice.json",
        params=_params,
    )
    if return_parsed:
        return parse_sports247_site_page(raw, return_as_pandas=return_as_pandas)
    return raw


def sports247_site_pages_recruitment_institution(
    key: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Committed institution for a recruitment.

    Endpoint: ``GET https://247sports.com/Recruitment/{key}/Institution.json``
    Example URL: https://247sports.com/Recruitment/114978/Institution.json

    Args:
        key: key path parameter.
        return_parsed: parse the payload through parse_sports247_site_page -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            sports247_site_pages_recruitment_institution(key=114978)
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://247sports.com/Recruitment/{key}/Institution.json",
        params=_params,
    )
    if return_parsed:
        return parse_sports247_site_page(raw, return_as_pandas=return_as_pandas)
    return raw


def sports247_site_pages_recruitment_interests(
    key: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """All institutions the recruit has interest links with.

    Endpoint: ``GET https://247sports.com/Recruitment/{key}/Interests.json``
    Example URL: https://247sports.com/Recruitment/114978/Interests.json

    Args:
        key: key path parameter.
        return_parsed: parse the payload through parse_sports247_site_page -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            sports247_site_pages_recruitment_interests(key=114978)
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://247sports.com/Recruitment/{key}/Interests.json",
        params=_params,
    )
    if return_parsed:
        return parse_sports247_site_page(raw, return_as_pandas=return_as_pandas)
    return raw


def sports247_site_pages_recruitment_offers(
    key: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Institutions that have offered the recruit.

    Endpoint: ``GET https://247sports.com/Recruitment/{key}/Offers.json``
    Example URL: https://247sports.com/Recruitment/114978/Offers.json

    Args:
        key: key path parameter.
        return_parsed: parse the payload through parse_sports247_site_page -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            sports247_site_pages_recruitment_offers(key=114978)
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://247sports.com/Recruitment/{key}/Offers.json",
        params=_params,
    )
    if return_parsed:
        return parse_sports247_site_page(raw, return_as_pandas=return_as_pandas)
    return raw


def sports247_site_pages_recruitment_player_sport(
    key: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """PlayerSport underlying a recruitment.

    Endpoint: ``GET https://247sports.com/Recruitment/{key}/PlayerSport.json``
    Example URL: https://247sports.com/Recruitment/114978/PlayerSport.json

    Args:
        key: key path parameter.
        return_parsed: parse the payload through parse_sports247_site_page -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            sports247_site_pages_recruitment_player_sport(key=114978)
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://247sports.com/Recruitment/{key}/PlayerSport.json",
        params=_params,
    )
    if return_parsed:
        return parse_sports247_site_page(raw, return_as_pandas=return_as_pandas)
    return raw


def sports247_site_pages_season_current_expert_predictions(
    season: str,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Current expert 'crystal ball' predictions for a season.

    Endpoint: ``GET https://247sports.com/Season/{season}/CurrentExpertPredictions.json``
    Example URL: https://247sports.com/Season/2026-Football/CurrentExpertPredictions.json

    Args:
        season: season path parameter.
        return_parsed: parse the payload through parse_sports247_site_page -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            sports247_site_pages_season_current_expert_predictions(season='2026-Football')
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://247sports.com/Season/{season}/CurrentExpertPredictions.json",
        params=_params,
    )
    if return_parsed:
        return parse_sports247_site_page(raw, return_as_pandas=return_as_pandas)
    return raw


def sports247_site_pages_season_recruit_interest_events(
    season: str,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Recruit-interest timeline events for a season (offers/visits/commits).

    Endpoint: ``GET https://247sports.com/Season/{season}/RecruitInterestEvents.json``
    Example URL: https://247sports.com/Season/2026-Football/RecruitInterestEvents.json

    Args:
        season: season path parameter.
        return_parsed: parse the payload through parse_sports247_site_page -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            sports247_site_pages_season_recruit_interest_events(season='2026-Football')
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://247sports.com/Season/{season}/RecruitInterestEvents.json",
        params=_params,
    )
    if return_parsed:
        return parse_sports247_site_page(raw, return_as_pandas=return_as_pandas)
    return raw


def sports247_site_pages_season_recruit_interests(
    season: str,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """All recruit interests for a season (paginated).

    Endpoint: ``GET https://247sports.com/Season/{season}/RecruitInterests.json``
    Example URL: https://247sports.com/Season/2026-Football/RecruitInterests.json

    Args:
        season: season path parameter.
        return_parsed: parse the payload through parse_sports247_site_page -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            sports247_site_pages_season_recruit_interests(season='2026-Football')
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://247sports.com/Season/{season}/RecruitInterests.json",
        params=_params,
    )
    if return_parsed:
        return parse_sports247_site_page(raw, return_as_pandas=return_as_pandas)
    return raw


def sports247_site_pages_season_recruits(
    season: str,
    items: Optional[int] = None,
    page: Optional[int] = None,
    player_full_name: Optional[str] = None,
    institution: Optional[int] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Recruit class rankings for a season (rich per-recruit rows with inlined Player).

    Endpoint: ``GET https://247sports.com/Season/{season}/Recruits.json``
    Example URL: https://247sports.com/Season/2026-Football/Recruits.json

    Args:
        season: season path parameter.
        items: Items query parameter.
        page: Page query parameter.
        player_full_name: Player.FullName query parameter.
        institution: Institution query parameter.
        return_parsed: parse the payload through parse_sports247_site_page -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            sports247_site_pages_season_recruits(season='2026-Football')
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "Items": items,
        "Page": page,
        "Player.FullName": player_full_name,
        "Institution": institution,
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://247sports.com/Season/{season}/Recruits.json",
        params=_params,
    )
    if return_parsed:
        return parse_sports247_site_page(raw, return_as_pandas=return_as_pandas)
    return raw


def sports247_site_pages_season_roster_embed(
    season: str,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Signed-class roster embed (PlayerSport rows). Accuracy can lag.

    Endpoint: ``GET https://247sports.com/Season/{season}/Roster/Embed.json``
    Example URL: https://247sports.com/Season/2020-Football/Roster/Embed.json

    Args:
        season: season path parameter.
        return_parsed: parse the payload through parse_sports247_site_page -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            sports247_site_pages_season_roster_embed(season='2020-Football')
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://247sports.com/Season/{season}/Roster/Embed.json",
        params=_params,
    )
    if return_parsed:
        return parse_sports247_site_page(raw, return_as_pandas=return_as_pandas)
    return raw
