# GENERATED by tools/codegen/generate.py -- DO NOT EDIT.
# Source: tools/codegen/endpoints/on3.yaml
"""on3 wrappers (host https://api.on3.com/public/rdb/v1). Generated."""

from __future__ import annotations

from typing import TYPE_CHECKING, Dict, List, Optional, Union  # noqa: F401

from sportsdataverse.cfb.on3_runtime import _get
from sportsdataverse.cfb.on3_parsers import parse_on3_rdb

if TYPE_CHECKING:  # pragma: no cover -- annotation-only imports (PEP 563 defers eval)
    import pandas as pd
    import polars as pl

__all__ = [
    "on3_coaches_history",
    "on3_coaches_profile",
    "on3_collective_groups",
    "on3_collective_groups_deals",
    "on3_collective_groups_key",
    "on3_commits_latest",
    "on3_commits_organizations_latest_commits",
    "on3_commits_organizations_org_key",
    "on3_draft_organization_rank",
    "on3_draft_pick_organization_rank",
    "on3_drafts",
    "on3_drafts_by_stars",
    "on3_drafts_by_stars_summary",
    "on3_drafts_players",
    "on3_filters_conferences",
    "on3_filters_draft_rounds",
    "on3_filters_positions",
    "on3_filters_sports",
    "on3_filters_status",
    "on3_filters_teams",
    "on3_filters_years",
    "on3_nil_100",
    "on3_nil_100_v2",
    "on3_nil_compliances_state",
    "on3_nil_rankings",
    "on3_organizations_draft_class_by_state",
    "on3_organizations_draft_class_by_year",
    "on3_organizations_draft_count_by_stars",
    "on3_organizations_draft_count_by_year",
    "on3_organizations_draft_ranking_summary",
    "on3_organizations_drafted_players",
    "on3_organizations_drafts_by_stars_summary",
    "on3_organizations_roster",
    "on3_organizations_roster_header",
    "on3_people_combine_measurements",
    "on3_people_latest_valuation",
    "on3_people_measurements",
    "on3_people_measurements_averages",
    "on3_people_person_connections",
    "on3_people_social",
    "on3_people_social_post_summary",
    "on3_people_track_and_field_measurements",
    "on3_people_valuation_growth",
    "on3_person_connections_connection_key",
    "on3_person_primary_recruitment_evaluation",
    "on3_person_recruitment_evaluations",
    "on3_person_sport_profile_recruit",
    "on3_person_sport_rankings",
    "on3_player_all_rankings",
    "on3_player_database_updates",
    "on3_player_images",
    "on3_player_organizations",
    "on3_player_organizations_org_key",
    "on3_player_person_rankings",
    "on3_player_profile",
    "on3_player_team_targets",
    "on3_player_verified",
    "on3_player_videos",
    "on3_player_visit_center",
    "on3_players_industry_comparision",
    "on3_players_industry_comparision_list",
    "on3_predictions_user_key",
    "on3_quotes",
    "on3_quotes_key",
    "on3_recruitment_primary_recruitment_evaluation",
    "on3_recruitment_recruitment_evaluations",
    "on3_recruitments_latest_rpm_picks",
    "on3_recruitments_profile",
    "on3_recruitments_rpm_picks",
    "on3_recruitments_rpm_summary",
    "on3_team_ranking",
    "on3_team_ranking_bluechips_team_rankings",
    "on3_team_ranking_consensus_team_rankings",
    "on3_team_ranking_organizations_summary",
    "on3_team_ranking_team_rankings",
    "on3_transfers_best_available",
    "on3_transfers_latest",
    "on3_videos_video_key",
]


def on3_coaches_history(
    person_key: int,
    page: Optional[int] = None,
    page_size: Optional[int] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /rdb/v1/coaches/{personKey}/history

    Endpoint: ``GET https://api.on3.com/public/rdb/v1/coaches/{person_key}/history``
    Example URL: https://api.on3.com/public/rdb/v1/coaches/89617/history

    Args:
        person_key: person_key path parameter.
        page: page query parameter.
        page_size: pageSize query parameter.
        return_parsed: parse the payload through parse_on3_rdb -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            on3_coaches_history(person_key=89617)
    """
    raw = _get(
        f"https://api.on3.com/public/rdb/v1/coaches/{person_key}/history",
        params={
            "page": page,
            "pageSize": page_size,
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_on3_rdb(raw, return_as_pandas=return_as_pandas)
    return raw


def on3_coaches_profile(
    person_key: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /rdb/v1/coaches/{personKey}/profile

    Endpoint: ``GET https://api.on3.com/public/rdb/v1/coaches/{person_key}/profile``
    Example URL: https://api.on3.com/public/rdb/v1/coaches/89617/profile

    Args:
        person_key: person_key path parameter.
        return_parsed: parse the payload through parse_on3_rdb -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            on3_coaches_profile(person_key=89617)
    """
    raw = _get(
        f"https://api.on3.com/public/rdb/v1/coaches/{person_key}/profile",
        params={
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_on3_rdb(raw, return_as_pandas=return_as_pandas)
    return raw


def on3_collective_groups(
    sport_key: Optional[int] = None,
    organization_key: Optional[int] = None,
    query: Optional[str] = None,
    page: Optional[int] = None,
    page_size: Optional[int] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /rdb/v1/collective-groups

    Endpoint: ``GET https://api.on3.com/public/rdb/v1/collective-groups``
    Example URL: https://api.on3.com/public/rdb/v1/collective-groups

    Args:
        sport_key: sportKey query parameter.
        organization_key: organizationKey query parameter.
        query: query query parameter.
        page: page query parameter.
        page_size: pageSize query parameter.
        return_parsed: parse the payload through parse_on3_rdb -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            on3_collective_groups()
    """
    raw = _get(
        "https://api.on3.com/public/rdb/v1/collective-groups",
        params={
            "sportKey": sport_key,
            "organizationKey": organization_key,
            "query": query,
            "page": page,
            "pageSize": page_size,
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_on3_rdb(raw, return_as_pandas=return_as_pandas)
    return raw


def on3_collective_groups_deals(
    key: int,
    page: Optional[int] = None,
    page_size: Optional[int] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /rdb/v1/collective-groups/{key}/deals

    Endpoint: ``GET https://api.on3.com/public/rdb/v1/collective-groups/{key}/deals``
    Example URL: https://api.on3.com/public/rdb/v1/collective-groups/1/deals

    Args:
        key: key path parameter.
        page: page query parameter.
        page_size: pageSize query parameter.
        return_parsed: parse the payload through parse_on3_rdb -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            on3_collective_groups_deals(key=1)
    """
    raw = _get(
        f"https://api.on3.com/public/rdb/v1/collective-groups/{key}/deals",
        params={
            "page": page,
            "pageSize": page_size,
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_on3_rdb(raw, return_as_pandas=return_as_pandas)
    return raw


def on3_collective_groups_key(
    key: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /rdb/v1/collective-groups/{key}

    Endpoint: ``GET https://api.on3.com/public/rdb/v1/collective-groups/{key}``
    Example URL: https://api.on3.com/public/rdb/v1/collective-groups/1

    Args:
        key: key path parameter.
        return_parsed: parse the payload through parse_on3_rdb -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            on3_collective_groups_key(key=1)
    """
    raw = _get(
        f"https://api.on3.com/public/rdb/v1/collective-groups/{key}",
        params={
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_on3_rdb(raw, return_as_pandas=return_as_pandas)
    return raw


def on3_commits_latest(
    sport_key: Optional[int] = None,
    page: Optional[int] = None,
    page_size: Optional[int] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /rdb/v1/commits/latest

    Endpoint: ``GET https://api.on3.com/public/rdb/v1/commits/latest``
    Example URL: https://api.on3.com/public/rdb/v1/commits/latest

    Args:
        sport_key: sportKey query parameter.
        page: page query parameter.
        page_size: pageSize query parameter.
        return_parsed: parse the payload through parse_on3_rdb -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            on3_commits_latest()
    """
    raw = _get(
        "https://api.on3.com/public/rdb/v1/commits/latest",
        params={
            "sportKey": sport_key,
            "page": page,
            "pageSize": page_size,
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_on3_rdb(raw, return_as_pandas=return_as_pandas)
    return raw


def on3_commits_organizations_latest_commits(
    org_key: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /rdb/v1/commits/organizations/{orgKey}/latest-commits

    Endpoint: ``GET https://api.on3.com/public/rdb/v1/commits/organizations/{org_key}/latest-commits``
    Example URL: https://api.on3.com/public/rdb/v1/commits/organizations/1867/latest-commits

    Args:
        org_key: org_key path parameter.
        return_parsed: parse the payload through parse_on3_rdb -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            on3_commits_organizations_latest_commits(org_key=1867)
    """
    raw = _get(
        f"https://api.on3.com/public/rdb/v1/commits/organizations/{org_key}/latest-commits",
        params={
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_on3_rdb(raw, return_as_pandas=return_as_pandas)
    return raw


def on3_commits_organizations_org_key(
    org_key: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /rdb/v1/commits/organizations/{orgKey}

    Endpoint: ``GET https://api.on3.com/public/rdb/v1/commits/organizations/{org_key}``
    Example URL: https://api.on3.com/public/rdb/v1/commits/organizations/1867

    Args:
        org_key: org_key path parameter.
        return_parsed: parse the payload through parse_on3_rdb -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            on3_commits_organizations_org_key(org_key=1867)
    """
    raw = _get(
        f"https://api.on3.com/public/rdb/v1/commits/organizations/{org_key}",
        params={
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_on3_rdb(raw, return_as_pandas=return_as_pandas)
    return raw


def on3_draft_organization_rank(
    sport_key: Optional[int] = None,
    year: Optional[int] = None,
    page: Optional[int] = None,
    page_size: Optional[int] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /rdb/v1/draft-organization-rank

    Endpoint: ``GET https://api.on3.com/public/rdb/v1/draft-organization-rank``
    Example URL: https://api.on3.com/public/rdb/v1/draft-organization-rank

    Args:
        sport_key: sportKey query parameter.
        year: year query parameter.
        page: page query parameter.
        page_size: pageSize query parameter.
        return_parsed: parse the payload through parse_on3_rdb -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            on3_draft_organization_rank()
    """
    raw = _get(
        "https://api.on3.com/public/rdb/v1/draft-organization-rank",
        params={
            "sportKey": sport_key,
            "year": year,
            "page": page,
            "pageSize": page_size,
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_on3_rdb(raw, return_as_pandas=return_as_pandas)
    return raw


def on3_draft_pick_organization_rank(
    sport_key: Optional[int] = None,
    year: Optional[int] = None,
    page: Optional[int] = None,
    page_size: Optional[int] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /rdb/v1/draft-pick-organization-rank

    Endpoint: ``GET https://api.on3.com/public/rdb/v1/draft-pick-organization-rank``
    Example URL: https://api.on3.com/public/rdb/v1/draft-pick-organization-rank

    Args:
        sport_key: sportKey query parameter.
        year: year query parameter.
        page: page query parameter.
        page_size: pageSize query parameter.
        return_parsed: parse the payload through parse_on3_rdb -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            on3_draft_pick_organization_rank()
    """
    raw = _get(
        "https://api.on3.com/public/rdb/v1/draft-pick-organization-rank",
        params={
            "sportKey": sport_key,
            "year": year,
            "page": page,
            "pageSize": page_size,
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_on3_rdb(raw, return_as_pandas=return_as_pandas)
    return raw


def on3_drafts(
    sport_key: Optional[int] = None,
    round: Optional[int] = None,
    year: Optional[int] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /rdb/v1/drafts

    Endpoint: ``GET https://api.on3.com/public/rdb/v1/drafts``
    Example URL: https://api.on3.com/public/rdb/v1/drafts

    Args:
        sport_key: sportKey query parameter.
        round: round query parameter.
        year: year query parameter.
        return_parsed: parse the payload through parse_on3_rdb -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            on3_drafts()
    """
    raw = _get(
        "https://api.on3.com/public/rdb/v1/drafts",
        params={
            "sportKey": sport_key,
            "round": round,
            "year": year,
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_on3_rdb(raw, return_as_pandas=return_as_pandas)
    return raw


def on3_drafts_by_stars(
    sport_key: Optional[int] = None,
    year: Optional[int] = None,
    year_span: Optional[int] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /rdb/v1/drafts-by-stars

    Endpoint: ``GET https://api.on3.com/public/rdb/v1/drafts-by-stars``
    Example URL: https://api.on3.com/public/rdb/v1/drafts-by-stars

    Args:
        sport_key: sportKey query parameter.
        year: year query parameter.
        year_span: yearSpan query parameter.
        return_parsed: parse the payload through parse_on3_rdb -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            on3_drafts_by_stars()
    """
    raw = _get(
        "https://api.on3.com/public/rdb/v1/drafts-by-stars",
        params={
            "sportKey": sport_key,
            "year": year,
            "yearSpan": year_span,
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_on3_rdb(raw, return_as_pandas=return_as_pandas)
    return raw


def on3_drafts_by_stars_summary(
    sport_key: Optional[int] = None,
    year: Optional[int] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /rdb/v1/drafts-by-stars-summary

    Endpoint: ``GET https://api.on3.com/public/rdb/v1/drafts-by-stars-summary``
    Example URL: https://api.on3.com/public/rdb/v1/drafts-by-stars-summary

    Args:
        sport_key: sportKey query parameter.
        year: year query parameter.
        return_parsed: parse the payload through parse_on3_rdb -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            on3_drafts_by_stars_summary()
    """
    raw = _get(
        "https://api.on3.com/public/rdb/v1/drafts-by-stars-summary",
        params={
            "sportKey": sport_key,
            "year": year,
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_on3_rdb(raw, return_as_pandas=return_as_pandas)
    return raw


def on3_drafts_players(
    org_key: int,
    year: Optional[int] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /rdb/v1/drafts/{orgKey}/players

    Endpoint: ``GET https://api.on3.com/public/rdb/v1/drafts/{org_key}/players``
    Example URL: https://api.on3.com/public/rdb/v1/drafts/1867/players

    Args:
        org_key: org_key path parameter.
        year: year query parameter.
        return_parsed: parse the payload through parse_on3_rdb -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            on3_drafts_players(org_key=1867)
    """
    raw = _get(
        f"https://api.on3.com/public/rdb/v1/drafts/{org_key}/players",
        params={
            "year": year,
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_on3_rdb(raw, return_as_pandas=return_as_pandas)
    return raw


def on3_filters_conferences(
    year: Optional[int] = None,
    sport_key: Optional[int] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /rdb/v1/filters/conferences

    Endpoint: ``GET https://api.on3.com/public/rdb/v1/filters/conferences``
    Example URL: https://api.on3.com/public/rdb/v1/filters/conferences

    Args:
        year: year query parameter.
        sport_key: sportKey query parameter.
        return_parsed: parse the payload through parse_on3_rdb -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            on3_filters_conferences()
    """
    raw = _get(
        "https://api.on3.com/public/rdb/v1/filters/conferences",
        params={
            "year": year,
            "sportKey": sport_key,
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_on3_rdb(raw, return_as_pandas=return_as_pandas)
    return raw


def on3_filters_draft_rounds(
    year: Optional[int] = None,
    sport_key: Optional[int] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /rdb/v1/filters/draft-rounds

    Endpoint: ``GET https://api.on3.com/public/rdb/v1/filters/draft-rounds``
    Example URL: https://api.on3.com/public/rdb/v1/filters/draft-rounds

    Args:
        year: year query parameter.
        sport_key: sportKey query parameter.
        return_parsed: parse the payload through parse_on3_rdb -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            on3_filters_draft_rounds()
    """
    raw = _get(
        "https://api.on3.com/public/rdb/v1/filters/draft-rounds",
        params={
            "year": year,
            "sportKey": sport_key,
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_on3_rdb(raw, return_as_pandas=return_as_pandas)
    return raw


def on3_filters_positions(
    sport_key: Optional[int] = None,
    position_type: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /rdb/v1/filters/positions

    Endpoint: ``GET https://api.on3.com/public/rdb/v1/filters/positions``
    Example URL: https://api.on3.com/public/rdb/v1/filters/positions

    Args:
        sport_key: sportKey query parameter.
        position_type: positionType query parameter.
        return_parsed: parse the payload through parse_on3_rdb -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            on3_filters_positions()
    """
    raw = _get(
        "https://api.on3.com/public/rdb/v1/filters/positions",
        params={
            "sportKey": sport_key,
            "positionType": position_type,
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_on3_rdb(raw, return_as_pandas=return_as_pandas)
    return raw


def on3_filters_sports(
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /rdb/v1/filters/sports

    Endpoint: ``GET https://api.on3.com/public/rdb/v1/filters/sports``
    Example URL: https://api.on3.com/public/rdb/v1/filters/sports

    Args:
        return_parsed: parse the payload through parse_on3_rdb -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            on3_filters_sports()
    """
    raw = _get(
        "https://api.on3.com/public/rdb/v1/filters/sports",
        params={
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_on3_rdb(raw, return_as_pandas=return_as_pandas)
    return raw


def on3_filters_status(
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /rdb/v1/filters/status

    Endpoint: ``GET https://api.on3.com/public/rdb/v1/filters/status``
    Example URL: https://api.on3.com/public/rdb/v1/filters/status

    Args:
        return_parsed: parse the payload through parse_on3_rdb -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            on3_filters_status()
    """
    raw = _get(
        "https://api.on3.com/public/rdb/v1/filters/status",
        params={
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_on3_rdb(raw, return_as_pandas=return_as_pandas)
    return raw


def on3_filters_teams(
    group_by: Optional[bool] = None,
    year: Optional[int] = None,
    sport_key: Optional[int] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /rdb/v1/filters/teams

    Endpoint: ``GET https://api.on3.com/public/rdb/v1/filters/teams``
    Example URL: https://api.on3.com/public/rdb/v1/filters/teams

    Args:
        group_by: groupBy query parameter.
        year: year query parameter.
        sport_key: sportKey query parameter.
        return_parsed: parse the payload through parse_on3_rdb -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            on3_filters_teams()
    """
    raw = _get(
        "https://api.on3.com/public/rdb/v1/filters/teams",
        params={
            "groupBy": group_by,
            "year": year,
            "sportKey": sport_key,
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_on3_rdb(raw, return_as_pandas=return_as_pandas)
    return raw


def on3_filters_years(
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /rdb/v1/filters/years

    Endpoint: ``GET https://api.on3.com/public/rdb/v1/filters/years``
    Example URL: https://api.on3.com/public/rdb/v1/filters/years

    Args:
        return_parsed: parse the payload through parse_on3_rdb -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            on3_filters_years()
    """
    raw = _get(
        "https://api.on3.com/public/rdb/v1/filters/years",
        params={
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_on3_rdb(raw, return_as_pandas=return_as_pandas)
    return raw


def on3_nil_100(
    year: Optional[int] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /rdb/v1/nil-100

    Endpoint: ``GET https://api.on3.com/public/rdb/v1/nil-100``
    Example URL: https://api.on3.com/public/rdb/v1/nil-100

    Args:
        year: year query parameter.
        return_parsed: parse the payload through parse_on3_rdb -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            on3_nil_100()
    """
    raw = _get(
        "https://api.on3.com/public/rdb/v1/nil-100",
        params={
            "year": year,
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_on3_rdb(raw, return_as_pandas=return_as_pandas)
    return raw


def on3_nil_100_v2(
    year: Optional[int] = None,
    org_key: Optional[int] = None,
    limit: Optional[int] = None,
    page: Optional[int] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /rdb/v2/nil-100

    Endpoint: ``GET https://api.on3.com/public/rdb/v2/nil-100``
    Example URL: https://api.on3.com/public/rdb/v2/nil-100

    Args:
        year: year query parameter.
        org_key: orgKey query parameter.
        limit: limit query parameter.
        page: page query parameter.
        return_parsed: parse the payload through parse_on3_rdb -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            on3_nil_100_v2()
    """
    raw = _get(
        "https://api.on3.com/public/rdb/v2/nil-100",
        params={
            "year": year,
            "orgKey": org_key,
            "limit": limit,
            "page": page,
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_on3_rdb(raw, return_as_pandas=return_as_pandas)
    return raw


def on3_nil_compliances_state(
    state_key: Optional[int] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /rdb/v1/nil-compliances/state

    Endpoint: ``GET https://api.on3.com/public/rdb/v1/nil-compliances/state``
    Example URL: https://api.on3.com/public/rdb/v1/nil-compliances/state

    Args:
        state_key: stateKey query parameter.
        return_parsed: parse the payload through parse_on3_rdb -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            on3_nil_compliances_state()
    """
    raw = _get(
        "https://api.on3.com/public/rdb/v1/nil-compliances/state",
        params={
            "stateKey": state_key,
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_on3_rdb(raw, return_as_pandas=return_as_pandas)
    return raw


def on3_nil_rankings(
    sport_key: Optional[int] = None,
    gender: Optional[str] = None,
    year: Optional[int] = None,
    org_type: Optional[str] = None,
    position_abbr: Optional[str] = None,
    state_abbr: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /rdb/v1/nil-rankings

    Endpoint: ``GET https://api.on3.com/public/rdb/v1/nil-rankings``
    Example URL: https://api.on3.com/public/rdb/v1/nil-rankings

    Args:
        sport_key: sportKey query parameter.
        gender: gender query parameter.
        year: year query parameter.
        org_type: orgType query parameter.
        position_abbr: positionAbbr query parameter.
        state_abbr: stateAbbr query parameter.
        return_parsed: parse the payload through parse_on3_rdb -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            on3_nil_rankings()
    """
    raw = _get(
        "https://api.on3.com/public/rdb/v1/nil-rankings",
        params={
            "sportKey": sport_key,
            "gender": gender,
            "year": year,
            "orgType": org_type,
            "positionAbbr": position_abbr,
            "stateAbbr": state_abbr,
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_on3_rdb(raw, return_as_pandas=return_as_pandas)
    return raw


def on3_organizations_draft_class_by_state(
    organization_key: int,
    page: Optional[int] = None,
    page_size: Optional[int] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /rdb/v1/organizations/{organizationKey}/draft-class-by-state

    Endpoint: ``GET https://api.on3.com/public/rdb/v1/organizations/{organization_key}/draft-class-by-state``
    Example URL: https://api.on3.com/public/rdb/v1/organizations/1867/draft-class-by-state

    Args:
        organization_key: organization_key path parameter.
        page: page query parameter.
        page_size: pageSize query parameter.
        return_parsed: parse the payload through parse_on3_rdb -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            on3_organizations_draft_class_by_state(organization_key=1867)
    """
    raw = _get(
        f"https://api.on3.com/public/rdb/v1/organizations/{organization_key}/draft-class-by-state",
        params={
            "page": page,
            "pageSize": page_size,
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_on3_rdb(raw, return_as_pandas=return_as_pandas)
    return raw


def on3_organizations_draft_class_by_year(
    organization_key: int,
    page: Optional[int] = None,
    page_size: Optional[int] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /rdb/v1/organizations/{organizationKey}/draft-class-by-year

    Endpoint: ``GET https://api.on3.com/public/rdb/v1/organizations/{organization_key}/draft-class-by-year``
    Example URL: https://api.on3.com/public/rdb/v1/organizations/1867/draft-class-by-year

    Args:
        organization_key: organization_key path parameter.
        page: page query parameter.
        page_size: pageSize query parameter.
        return_parsed: parse the payload through parse_on3_rdb -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            on3_organizations_draft_class_by_year(organization_key=1867)
    """
    raw = _get(
        f"https://api.on3.com/public/rdb/v1/organizations/{organization_key}/draft-class-by-year",
        params={
            "page": page,
            "pageSize": page_size,
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_on3_rdb(raw, return_as_pandas=return_as_pandas)
    return raw


def on3_organizations_draft_count_by_stars(
    organization_key: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /rdb/v1/organizations/{organizationKey}/draft-count-by-stars

    Endpoint: ``GET https://api.on3.com/public/rdb/v1/organizations/{organization_key}/draft-count-by-stars``
    Example URL: https://api.on3.com/public/rdb/v1/organizations/1867/draft-count-by-stars

    Args:
        organization_key: organization_key path parameter.
        return_parsed: parse the payload through parse_on3_rdb -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            on3_organizations_draft_count_by_stars(organization_key=1867)
    """
    raw = _get(
        f"https://api.on3.com/public/rdb/v1/organizations/{organization_key}/draft-count-by-stars",
        params={
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_on3_rdb(raw, return_as_pandas=return_as_pandas)
    return raw


def on3_organizations_draft_count_by_year(
    organization_key: int,
    page: Optional[int] = None,
    page_size: Optional[int] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /rdb/v1/organizations/{organizationKey}/draft-count-by-year

    Endpoint: ``GET https://api.on3.com/public/rdb/v1/organizations/{organization_key}/draft-count-by-year``
    Example URL: https://api.on3.com/public/rdb/v1/organizations/1867/draft-count-by-year

    Args:
        organization_key: organization_key path parameter.
        page: page query parameter.
        page_size: pageSize query parameter.
        return_parsed: parse the payload through parse_on3_rdb -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            on3_organizations_draft_count_by_year(organization_key=1867)
    """
    raw = _get(
        f"https://api.on3.com/public/rdb/v1/organizations/{organization_key}/draft-count-by-year",
        params={
            "page": page,
            "pageSize": page_size,
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_on3_rdb(raw, return_as_pandas=return_as_pandas)
    return raw


def on3_organizations_draft_ranking_summary(
    organization_key: int,
    year: Optional[int] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /rdb/v1/organizations/{organizationKey}/draft-ranking-summary

    Endpoint: ``GET https://api.on3.com/public/rdb/v1/organizations/{organization_key}/draft-ranking-summary``
    Example URL: https://api.on3.com/public/rdb/v1/organizations/1867/draft-ranking-summary

    Args:
        organization_key: organization_key path parameter.
        year: year query parameter.
        return_parsed: parse the payload through parse_on3_rdb -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            on3_organizations_draft_ranking_summary(organization_key=1867)
    """
    raw = _get(
        f"https://api.on3.com/public/rdb/v1/organizations/{organization_key}/draft-ranking-summary",
        params={
            "year": year,
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_on3_rdb(raw, return_as_pandas=return_as_pandas)
    return raw


def on3_organizations_drafted_players(
    organization_key: int,
    page: Optional[int] = None,
    page_size: Optional[int] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /rdb/v1/organizations/{organizationKey}/drafted-players

    Endpoint: ``GET https://api.on3.com/public/rdb/v1/organizations/{organization_key}/drafted-players``
    Example URL: https://api.on3.com/public/rdb/v1/organizations/1867/drafted-players

    Args:
        organization_key: organization_key path parameter.
        page: page query parameter.
        page_size: pageSize query parameter.
        return_parsed: parse the payload through parse_on3_rdb -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            on3_organizations_drafted_players(organization_key=1867)
    """
    raw = _get(
        f"https://api.on3.com/public/rdb/v1/organizations/{organization_key}/drafted-players",
        params={
            "page": page,
            "pageSize": page_size,
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_on3_rdb(raw, return_as_pandas=return_as_pandas)
    return raw


def on3_organizations_drafts_by_stars_summary(
    organization_key: int,
    year: Optional[int] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /rdb/v1/organizations/{organizationKey}/drafts-by-stars-summary

    Endpoint: ``GET https://api.on3.com/public/rdb/v1/organizations/{organization_key}/drafts-by-stars-summary``
    Example URL: https://api.on3.com/public/rdb/v1/organizations/1867/drafts-by-stars-summary

    Args:
        organization_key: organization_key path parameter.
        year: year query parameter.
        return_parsed: parse the payload through parse_on3_rdb -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            on3_organizations_drafts_by_stars_summary(organization_key=1867)
    """
    raw = _get(
        f"https://api.on3.com/public/rdb/v1/organizations/{organization_key}/drafts-by-stars-summary",
        params={
            "year": year,
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_on3_rdb(raw, return_as_pandas=return_as_pandas)
    return raw


def on3_organizations_roster(
    organization_key: int,
    sport_key: Optional[int] = None,
    year: Optional[int] = None,
    page: Optional[int] = None,
    page_size: Optional[int] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /rdb/v1/organizations/{organizationKey}/roster

    Endpoint: ``GET https://api.on3.com/public/rdb/v1/organizations/{organization_key}/roster``
    Example URL: https://api.on3.com/public/rdb/v1/organizations/1867/roster

    Args:
        organization_key: organization_key path parameter.
        sport_key: sportKey query parameter.
        year: year query parameter.
        page: page query parameter.
        page_size: pageSize query parameter.
        return_parsed: parse the payload through parse_on3_rdb -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            on3_organizations_roster(organization_key=1867)
    """
    raw = _get(
        f"https://api.on3.com/public/rdb/v1/organizations/{organization_key}/roster",
        params={
            "sportKey": sport_key,
            "year": year,
            "page": page,
            "pageSize": page_size,
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_on3_rdb(raw, return_as_pandas=return_as_pandas)
    return raw


def on3_organizations_roster_header(
    organization_key: int,
    sport_key: Optional[int] = None,
    year: Optional[int] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /rdb/v1/organizations/{organizationKey}/roster-header

    Endpoint: ``GET https://api.on3.com/public/rdb/v1/organizations/{organization_key}/roster-header``
    Example URL: https://api.on3.com/public/rdb/v1/organizations/1867/roster-header

    Args:
        organization_key: organization_key path parameter.
        sport_key: sportKey query parameter.
        year: year query parameter.
        return_parsed: parse the payload through parse_on3_rdb -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            on3_organizations_roster_header(organization_key=1867)
    """
    raw = _get(
        f"https://api.on3.com/public/rdb/v1/organizations/{organization_key}/roster-header",
        params={
            "sportKey": sport_key,
            "year": year,
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_on3_rdb(raw, return_as_pandas=return_as_pandas)
    return raw


def on3_people_combine_measurements(
    person_key: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /rdb/v1/people/{personKey}/combine-measurements

    Endpoint: ``GET https://api.on3.com/public/rdb/v1/people/{person_key}/combine-measurements``
    Example URL: https://api.on3.com/public/rdb/v1/people/89617/combine-measurements

    Args:
        person_key: person_key path parameter.
        return_parsed: parse the payload through parse_on3_rdb -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            on3_people_combine_measurements(person_key=89617)
    """
    raw = _get(
        f"https://api.on3.com/public/rdb/v1/people/{person_key}/combine-measurements",
        params={
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_on3_rdb(raw, return_as_pandas=return_as_pandas)
    return raw


def on3_people_latest_valuation(
    person_key: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /rdb/v1/people/{personKey}/latest-valuation

    Endpoint: ``GET https://api.on3.com/public/rdb/v1/people/{person_key}/latest-valuation``
    Example URL: https://api.on3.com/public/rdb/v1/people/89617/latest-valuation

    Args:
        person_key: person_key path parameter.
        return_parsed: parse the payload through parse_on3_rdb -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            on3_people_latest_valuation(person_key=89617)
    """
    raw = _get(
        f"https://api.on3.com/public/rdb/v1/people/{person_key}/latest-valuation",
        params={
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_on3_rdb(raw, return_as_pandas=return_as_pandas)
    return raw


def on3_people_measurements(
    person_key: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /rdb/v1/people/{personKey}/measurements

    Endpoint: ``GET https://api.on3.com/public/rdb/v1/people/{person_key}/measurements``
    Example URL: https://api.on3.com/public/rdb/v1/people/89617/measurements

    Args:
        person_key: person_key path parameter.
        return_parsed: parse the payload through parse_on3_rdb -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            on3_people_measurements(person_key=89617)
    """
    raw = _get(
        f"https://api.on3.com/public/rdb/v1/people/{person_key}/measurements",
        params={
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_on3_rdb(raw, return_as_pandas=return_as_pandas)
    return raw


def on3_people_measurements_averages(
    person_key: int,
    page: Optional[int] = None,
    page_size: Optional[int] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /rdb/v1/people/{personKey}/measurements/averages

    Endpoint: ``GET https://api.on3.com/public/rdb/v1/people/{person_key}/measurements/averages``
    Example URL: https://api.on3.com/public/rdb/v1/people/89617/measurements/averages

    Args:
        person_key: person_key path parameter.
        page: page query parameter.
        page_size: pageSize query parameter.
        return_parsed: parse the payload through parse_on3_rdb -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            on3_people_measurements_averages(person_key=89617)
    """
    raw = _get(
        f"https://api.on3.com/public/rdb/v1/people/{person_key}/measurements/averages",
        params={
            "page": page,
            "pageSize": page_size,
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_on3_rdb(raw, return_as_pandas=return_as_pandas)
    return raw


def on3_people_person_connections(
    person_key: int,
    page: Optional[int] = None,
    page_size: Optional[int] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /rdb/v1/people/{personKey}/person-connections

    Endpoint: ``GET https://api.on3.com/public/rdb/v1/people/{person_key}/person-connections``
    Example URL: https://api.on3.com/public/rdb/v1/people/89617/person-connections

    Args:
        person_key: person_key path parameter.
        page: page query parameter.
        page_size: pageSize query parameter.
        return_parsed: parse the payload through parse_on3_rdb -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            on3_people_person_connections(person_key=89617)
    """
    raw = _get(
        f"https://api.on3.com/public/rdb/v1/people/{person_key}/person-connections",
        params={
            "page": page,
            "pageSize": page_size,
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_on3_rdb(raw, return_as_pandas=return_as_pandas)
    return raw


def on3_people_social(
    person_key: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /rdb/v1/people/{personKey}/social

    Endpoint: ``GET https://api.on3.com/public/rdb/v1/people/{person_key}/social``
    Example URL: https://api.on3.com/public/rdb/v1/people/89617/social

    Args:
        person_key: person_key path parameter.
        return_parsed: parse the payload through parse_on3_rdb -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            on3_people_social(person_key=89617)
    """
    raw = _get(
        f"https://api.on3.com/public/rdb/v1/people/{person_key}/social",
        params={
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_on3_rdb(raw, return_as_pandas=return_as_pandas)
    return raw


def on3_people_social_post_summary(
    person_key: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /rdb/v1/people/{personKey}/social-post-summary

    Endpoint: ``GET https://api.on3.com/public/rdb/v1/people/{person_key}/social-post-summary``
    Example URL: https://api.on3.com/public/rdb/v1/people/89617/social-post-summary

    Args:
        person_key: person_key path parameter.
        return_parsed: parse the payload through parse_on3_rdb -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            on3_people_social_post_summary(person_key=89617)
    """
    raw = _get(
        f"https://api.on3.com/public/rdb/v1/people/{person_key}/social-post-summary",
        params={
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_on3_rdb(raw, return_as_pandas=return_as_pandas)
    return raw


def on3_people_track_and_field_measurements(
    person_key: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /rdb/v1/people/{personKey}/track-and-field-measurements

    Endpoint: ``GET https://api.on3.com/public/rdb/v1/people/{person_key}/track-and-field-measurements``
    Example URL: https://api.on3.com/public/rdb/v1/people/89617/track-and-field-measurements

    Args:
        person_key: person_key path parameter.
        return_parsed: parse the payload through parse_on3_rdb -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            on3_people_track_and_field_measurements(person_key=89617)
    """
    raw = _get(
        f"https://api.on3.com/public/rdb/v1/people/{person_key}/track-and-field-measurements",
        params={
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_on3_rdb(raw, return_as_pandas=return_as_pandas)
    return raw


def on3_people_valuation_growth(
    person_key: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /rdb/v1/people/{personKey}/valuation-growth

    Endpoint: ``GET https://api.on3.com/public/rdb/v1/people/{person_key}/valuation-growth``
    Example URL: https://api.on3.com/public/rdb/v1/people/89617/valuation-growth

    Args:
        person_key: person_key path parameter.
        return_parsed: parse the payload through parse_on3_rdb -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            on3_people_valuation_growth(person_key=89617)
    """
    raw = _get(
        f"https://api.on3.com/public/rdb/v1/people/{person_key}/valuation-growth",
        params={
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_on3_rdb(raw, return_as_pandas=return_as_pandas)
    return raw


def on3_person_connections_connection_key(
    connection_key: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /rdb/v1/person-connections/{connectionKey}

    Endpoint: ``GET https://api.on3.com/public/rdb/v1/person-connections/{connection_key}``
    Example URL: https://api.on3.com/public/rdb/v1/person-connections/89617

    Args:
        connection_key: connection_key path parameter.
        return_parsed: parse the payload through parse_on3_rdb -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            on3_person_connections_connection_key(connection_key=89617)
    """
    raw = _get(
        f"https://api.on3.com/public/rdb/v1/person-connections/{connection_key}",
        params={
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_on3_rdb(raw, return_as_pandas=return_as_pandas)
    return raw


def on3_person_primary_recruitment_evaluation(
    person_key: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /rdb/v1/person/{personKey}/primary-recruitment-evaluation

    Endpoint: ``GET https://api.on3.com/public/rdb/v1/person/{person_key}/primary-recruitment-evaluation``
    Example URL: https://api.on3.com/public/rdb/v1/person/89617/primary-recruitment-evaluation

    Args:
        person_key: person_key path parameter.
        return_parsed: parse the payload through parse_on3_rdb -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            on3_person_primary_recruitment_evaluation(person_key=89617)
    """
    raw = _get(
        f"https://api.on3.com/public/rdb/v1/person/{person_key}/primary-recruitment-evaluation",
        params={
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_on3_rdb(raw, return_as_pandas=return_as_pandas)
    return raw


def on3_person_recruitment_evaluations(
    person_key: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /rdb/v1/person/{personKey}/recruitment-evaluations

    Endpoint: ``GET https://api.on3.com/public/rdb/v1/person/{person_key}/recruitment-evaluations``
    Example URL: https://api.on3.com/public/rdb/v1/person/89617/recruitment-evaluations

    Args:
        person_key: person_key path parameter.
        return_parsed: parse the payload through parse_on3_rdb -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            on3_person_recruitment_evaluations(person_key=89617)
    """
    raw = _get(
        f"https://api.on3.com/public/rdb/v1/person/{person_key}/recruitment-evaluations",
        params={
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_on3_rdb(raw, return_as_pandas=return_as_pandas)
    return raw


def on3_person_sport_profile_recruit(
    ps_key: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /rdb/v1/person-sport/{psKey}/profile-recruit

    Endpoint: ``GET https://api.on3.com/public/rdb/v1/person-sport/{ps_key}/profile-recruit``
    Example URL: https://api.on3.com/public/rdb/v1/person-sport/89617/profile-recruit

    Args:
        ps_key: ps_key path parameter.
        return_parsed: parse the payload through parse_on3_rdb -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            on3_person_sport_profile_recruit(ps_key=89617)
    """
    raw = _get(
        f"https://api.on3.com/public/rdb/v1/person-sport/{ps_key}/profile-recruit",
        params={
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_on3_rdb(raw, return_as_pandas=return_as_pandas)
    return raw


def on3_person_sport_rankings(
    sport_key: Optional[int] = None,
    year: Optional[int] = None,
    page: Optional[int] = None,
    page_size: Optional[int] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /rdb/v1/person-sport-rankings

    Endpoint: ``GET https://api.on3.com/public/rdb/v1/person-sport-rankings``
    Example URL: https://api.on3.com/public/rdb/v1/person-sport-rankings

    Args:
        sport_key: sportKey query parameter.
        year: year query parameter.
        page: page query parameter.
        page_size: pageSize query parameter.
        return_parsed: parse the payload through parse_on3_rdb -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            on3_person_sport_rankings()
    """
    raw = _get(
        "https://api.on3.com/public/rdb/v1/person-sport-rankings",
        params={
            "sportKey": sport_key,
            "year": year,
            "page": page,
            "pageSize": page_size,
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_on3_rdb(raw, return_as_pandas=return_as_pandas)
    return raw


def on3_player_all_rankings(
    person_key: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /rdb/v1/player/{personKey}/all-rankings

    Endpoint: ``GET https://api.on3.com/public/rdb/v1/player/{person_key}/all-rankings``
    Example URL: https://api.on3.com/public/rdb/v1/player/89617/all-rankings

    Args:
        person_key: person_key path parameter.
        return_parsed: parse the payload through parse_on3_rdb -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            on3_player_all_rankings(person_key=89617)
    """
    raw = _get(
        f"https://api.on3.com/public/rdb/v1/player/{person_key}/all-rankings",
        params={
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_on3_rdb(raw, return_as_pandas=return_as_pandas)
    return raw


def on3_player_database_updates(
    person_key: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /rdb/v1/player/{personKey}/database-updates

    Endpoint: ``GET https://api.on3.com/public/rdb/v1/player/{person_key}/database-updates``
    Example URL: https://api.on3.com/public/rdb/v1/player/89617/database-updates

    Args:
        person_key: person_key path parameter.
        return_parsed: parse the payload through parse_on3_rdb -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            on3_player_database_updates(person_key=89617)
    """
    raw = _get(
        f"https://api.on3.com/public/rdb/v1/player/{person_key}/database-updates",
        params={
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_on3_rdb(raw, return_as_pandas=return_as_pandas)
    return raw


def on3_player_images(
    person_key: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /rdb/v1/player/{personKey}/images

    Endpoint: ``GET https://api.on3.com/public/rdb/v1/player/{person_key}/images``
    Example URL: https://api.on3.com/public/rdb/v1/player/89617/images

    Args:
        person_key: person_key path parameter.
        return_parsed: parse the payload through parse_on3_rdb -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            on3_player_images(person_key=89617)
    """
    raw = _get(
        f"https://api.on3.com/public/rdb/v1/player/{person_key}/images",
        params={
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_on3_rdb(raw, return_as_pandas=return_as_pandas)
    return raw


def on3_player_organizations(
    person_key: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /rdb/v1/player/{personKey}/organizations

    Endpoint: ``GET https://api.on3.com/public/rdb/v1/player/{person_key}/organizations``
    Example URL: https://api.on3.com/public/rdb/v1/player/89617/organizations

    Args:
        person_key: person_key path parameter.
        return_parsed: parse the payload through parse_on3_rdb -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            on3_player_organizations(person_key=89617)
    """
    raw = _get(
        f"https://api.on3.com/public/rdb/v1/player/{person_key}/organizations",
        params={
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_on3_rdb(raw, return_as_pandas=return_as_pandas)
    return raw


def on3_player_organizations_org_key(
    player_key: int,
    org_key: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /rdb/v1/player/{playerKey}/organizations/{orgKey}

    Endpoint: ``GET https://api.on3.com/public/rdb/v1/player/{player_key}/organizations/{org_key}``
    Example URL: https://api.on3.com/public/rdb/v1/player/89617/organizations/1867

    Args:
        player_key: player_key path parameter.
        org_key: org_key path parameter.
        return_parsed: parse the payload through parse_on3_rdb -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            on3_player_organizations_org_key(org_key=1867, player_key=89617)
    """
    raw = _get(
        f"https://api.on3.com/public/rdb/v1/player/{player_key}/organizations/{org_key}",
        params={
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_on3_rdb(raw, return_as_pandas=return_as_pandas)
    return raw


def on3_player_person_rankings(
    person_key: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /rdb/v1/player/{personKey}/rankings

    Endpoint: ``GET https://api.on3.com/public/rdb/v1/player/{person_key}/rankings``
    Example URL: https://api.on3.com/public/rdb/v1/player/89617/rankings

    Args:
        person_key: person_key path parameter.
        return_parsed: parse the payload through parse_on3_rdb -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            on3_player_person_rankings(person_key=89617)
    """
    raw = _get(
        f"https://api.on3.com/public/rdb/v1/player/{person_key}/rankings",
        params={
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_on3_rdb(raw, return_as_pandas=return_as_pandas)
    return raw


def on3_player_profile(
    person_key: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /rdb/v1/player/{personKey}/profile

    Endpoint: ``GET https://api.on3.com/public/rdb/v1/player/{person_key}/profile``
    Example URL: https://api.on3.com/public/rdb/v1/player/89617/profile

    Args:
        person_key: person_key path parameter.
        return_parsed: parse the payload through parse_on3_rdb -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            on3_player_profile(person_key=89617)
    """
    raw = _get(
        f"https://api.on3.com/public/rdb/v1/player/{person_key}/profile",
        params={
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_on3_rdb(raw, return_as_pandas=return_as_pandas)
    return raw


def on3_player_team_targets(
    player_key: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /rdb/v1/player/{playerKey}/team-targets

    Endpoint: ``GET https://api.on3.com/public/rdb/v1/player/{player_key}/team-targets``
    Example URL: https://api.on3.com/public/rdb/v1/player/89617/team-targets

    Args:
        player_key: player_key path parameter.
        return_parsed: parse the payload through parse_on3_rdb -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            on3_player_team_targets(player_key=89617)
    """
    raw = _get(
        f"https://api.on3.com/public/rdb/v1/player/{player_key}/team-targets",
        params={
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_on3_rdb(raw, return_as_pandas=return_as_pandas)
    return raw


def on3_player_verified(
    sport_key: Optional[int] = None,
    page: Optional[int] = None,
    page_size: Optional[int] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /rdb/v1/player/verified

    Endpoint: ``GET https://api.on3.com/public/rdb/v1/player/verified``
    Example URL: https://api.on3.com/public/rdb/v1/player/verified

    Args:
        sport_key: sportKey query parameter.
        page: page query parameter.
        page_size: pageSize query parameter.
        return_parsed: parse the payload through parse_on3_rdb -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            on3_player_verified()
    """
    raw = _get(
        "https://api.on3.com/public/rdb/v1/player/verified",
        params={
            "sportKey": sport_key,
            "page": page,
            "pageSize": page_size,
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_on3_rdb(raw, return_as_pandas=return_as_pandas)
    return raw


def on3_player_videos(
    person_key: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /rdb/v1/player/{personKey}/videos

    Endpoint: ``GET https://api.on3.com/public/rdb/v1/player/{person_key}/videos``
    Example URL: https://api.on3.com/public/rdb/v1/player/89617/videos

    Args:
        person_key: person_key path parameter.
        return_parsed: parse the payload through parse_on3_rdb -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            on3_player_videos(person_key=89617)
    """
    raw = _get(
        f"https://api.on3.com/public/rdb/v1/player/{person_key}/videos",
        params={
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_on3_rdb(raw, return_as_pandas=return_as_pandas)
    return raw


def on3_player_visit_center(
    player_key: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /rdb/v1/player/{playerKey}/visit-center

    Endpoint: ``GET https://api.on3.com/public/rdb/v1/player/{player_key}/visit-center``
    Example URL: https://api.on3.com/public/rdb/v1/player/89617/visit-center

    Args:
        player_key: player_key path parameter.
        return_parsed: parse the payload through parse_on3_rdb -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            on3_player_visit_center(player_key=89617)
    """
    raw = _get(
        f"https://api.on3.com/public/rdb/v1/player/{player_key}/visit-center",
        params={
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_on3_rdb(raw, return_as_pandas=return_as_pandas)
    return raw


def on3_players_industry_comparision(
    sport_key: Optional[int] = None,
    year: Optional[int] = None,
    state_abbr: Optional[str] = None,
    position_abbr: Optional[str] = None,
    page: Optional[int] = None,
    sort_by_industry: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /rdb/v1/players/industry-comparision

    Endpoint: ``GET https://api.on3.com/public/rdb/v1/players/industry-comparision``
    Example URL: https://api.on3.com/public/rdb/v1/players/industry-comparision

    Args:
        sport_key: sportKey query parameter.
        year: year query parameter.
        state_abbr: stateAbbr query parameter.
        position_abbr: positionAbbr query parameter.
        page: page query parameter.
        sort_by_industry: sortByIndustry query parameter.
        return_parsed: parse the payload through parse_on3_rdb -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            on3_players_industry_comparision()
    """
    raw = _get(
        "https://api.on3.com/public/rdb/v1/players/industry-comparision",
        params={
            "sportKey": sport_key,
            "year": year,
            "stateAbbr": state_abbr,
            "positionAbbr": position_abbr,
            "page": page,
            "sortByIndustry": sort_by_industry,
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_on3_rdb(raw, return_as_pandas=return_as_pandas)
    return raw


def on3_players_industry_comparision_list(
    sport_key: Optional[int] = None,
    year: Optional[int] = None,
    page: Optional[int] = None,
    page_size: Optional[int] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /rdb/v1/players/industry-comparision-list

    Endpoint: ``GET https://api.on3.com/public/rdb/v1/players/industry-comparision-list``
    Example URL: https://api.on3.com/public/rdb/v1/players/industry-comparision-list

    Args:
        sport_key: sportKey query parameter.
        year: year query parameter.
        page: page query parameter.
        page_size: pageSize query parameter.
        return_parsed: parse the payload through parse_on3_rdb -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            on3_players_industry_comparision_list()
    """
    raw = _get(
        "https://api.on3.com/public/rdb/v1/players/industry-comparision-list",
        params={
            "sportKey": sport_key,
            "year": year,
            "page": page,
            "pageSize": page_size,
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_on3_rdb(raw, return_as_pandas=return_as_pandas)
    return raw


def on3_predictions_user_key(
    user_key: int,
    page: Optional[int] = None,
    page_size: Optional[int] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Expert prediction accuracy + feed (see PredictionAccuracies)

    Endpoint: ``GET https://api.on3.com/public/rdb/v1/predictions/{user_key}``
    Example URL: https://api.on3.com/public/rdb/v1/predictions/89617

    Args:
        user_key: user_key path parameter.
        page: page query parameter.
        page_size: pageSize query parameter.
        return_parsed: parse the payload through parse_on3_rdb -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            on3_predictions_user_key(user_key=89617)
    """
    raw = _get(
        f"https://api.on3.com/public/rdb/v1/predictions/{user_key}",
        params={
            "page": page,
            "pageSize": page_size,
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_on3_rdb(raw, return_as_pandas=return_as_pandas)
    return raw


def on3_quotes(
    page: Optional[int] = None,
    page_size: Optional[int] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /rdb/v1/quotes

    Endpoint: ``GET https://api.on3.com/public/rdb/v1/quotes``
    Example URL: https://api.on3.com/public/rdb/v1/quotes

    Args:
        page: page query parameter.
        page_size: pageSize query parameter.
        return_parsed: parse the payload through parse_on3_rdb -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            on3_quotes()
    """
    raw = _get(
        "https://api.on3.com/public/rdb/v1/quotes",
        params={
            "page": page,
            "pageSize": page_size,
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_on3_rdb(raw, return_as_pandas=return_as_pandas)
    return raw


def on3_quotes_key(
    key: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /rdb/v1/quotes/{key}

    Endpoint: ``GET https://api.on3.com/public/rdb/v1/quotes/{key}``
    Example URL: https://api.on3.com/public/rdb/v1/quotes/1

    Args:
        key: key path parameter.
        return_parsed: parse the payload through parse_on3_rdb -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            on3_quotes_key(key=1)
    """
    raw = _get(
        f"https://api.on3.com/public/rdb/v1/quotes/{key}",
        params={
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_on3_rdb(raw, return_as_pandas=return_as_pandas)
    return raw


def on3_recruitment_primary_recruitment_evaluation(
    recruitment_key: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /rdb/v1/recruitment/{recruitmentKey}/primary-recruitment-evaluation

    Endpoint: ``GET https://api.on3.com/public/rdb/v1/recruitment/{recruitment_key}/primary-recruitment-evaluation``
    Example URL: https://api.on3.com/public/rdb/v1/recruitment/270036/primary-recruitment-evaluation

    Args:
        recruitment_key: recruitment_key path parameter.
        return_parsed: parse the payload through parse_on3_rdb -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            on3_recruitment_primary_recruitment_evaluation(recruitment_key=270036)
    """
    raw = _get(
        f"https://api.on3.com/public/rdb/v1/recruitment/{recruitment_key}/primary-recruitment-evaluation",
        params={
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_on3_rdb(raw, return_as_pandas=return_as_pandas)
    return raw


def on3_recruitment_recruitment_evaluations(
    recruitment_key: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /rdb/v1/recruitment/{recruitmentKey}/recruitment-evaluations

    Endpoint: ``GET https://api.on3.com/public/rdb/v1/recruitment/{recruitment_key}/recruitment-evaluations``
    Example URL: https://api.on3.com/public/rdb/v1/recruitment/270036/recruitment-evaluations

    Args:
        recruitment_key: recruitment_key path parameter.
        return_parsed: parse the payload through parse_on3_rdb -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            on3_recruitment_recruitment_evaluations(recruitment_key=270036)
    """
    raw = _get(
        f"https://api.on3.com/public/rdb/v1/recruitment/{recruitment_key}/recruitment-evaluations",
        params={
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_on3_rdb(raw, return_as_pandas=return_as_pandas)
    return raw


def on3_recruitments_latest_rpm_picks(
    org_key: Optional[int] = None,
    year: Optional[int] = None,
    page: Optional[int] = None,
    page_size: Optional[int] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Latest RPM (prediction) picks feed — paged {list,pagination}

    Endpoint: ``GET https://api.on3.com/public/rdb/v1/recruitments/latest-rpm-picks``
    Example URL: https://api.on3.com/public/rdb/v1/recruitments/latest-rpm-picks

    Args:
        org_key: orgKey query parameter.
        year: year query parameter.
        page: page query parameter.
        page_size: pageSize query parameter.
        return_parsed: parse the payload through parse_on3_rdb -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            on3_recruitments_latest_rpm_picks()
    """
    raw = _get(
        "https://api.on3.com/public/rdb/v1/recruitments/latest-rpm-picks",
        params={
            "orgKey": org_key,
            "year": year,
            "page": page,
            "pageSize": page_size,
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_on3_rdb(raw, return_as_pandas=return_as_pandas)
    return raw


def on3_recruitments_profile(
    rec_key: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /rdb/v1/recruitments/{recKey}/profile

    Endpoint: ``GET https://api.on3.com/public/rdb/v1/recruitments/{rec_key}/profile``
    Example URL: https://api.on3.com/public/rdb/v1/recruitments/270036/profile

    Args:
        rec_key: rec_key path parameter.
        return_parsed: parse the payload through parse_on3_rdb -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            on3_recruitments_profile(rec_key=270036)
    """
    raw = _get(
        f"https://api.on3.com/public/rdb/v1/recruitments/{rec_key}/profile",
        params={
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_on3_rdb(raw, return_as_pandas=return_as_pandas)
    return raw


def on3_recruitments_rpm_picks(
    rec_key: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /rdb/v1/recruitments/{recKey}/rpm-picks

    Endpoint: ``GET https://api.on3.com/public/rdb/v1/recruitments/{rec_key}/rpm-picks``
    Example URL: https://api.on3.com/public/rdb/v1/recruitments/270036/rpm-picks

    Args:
        rec_key: rec_key path parameter.
        return_parsed: parse the payload through parse_on3_rdb -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            on3_recruitments_rpm_picks(rec_key=270036)
    """
    raw = _get(
        f"https://api.on3.com/public/rdb/v1/recruitments/{rec_key}/rpm-picks",
        params={
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_on3_rdb(raw, return_as_pandas=return_as_pandas)
    return raw


def on3_recruitments_rpm_summary(
    rec_key: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /rdb/v1/recruitments/{recKey}/rpm-summary

    Endpoint: ``GET https://api.on3.com/public/rdb/v1/recruitments/{rec_key}/rpm-summary``
    Example URL: https://api.on3.com/public/rdb/v1/recruitments/270036/rpm-summary

    Args:
        rec_key: rec_key path parameter.
        return_parsed: parse the payload through parse_on3_rdb -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            on3_recruitments_rpm_summary(rec_key=270036)
    """
    raw = _get(
        f"https://api.on3.com/public/rdb/v1/recruitments/{rec_key}/rpm-summary",
        params={
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_on3_rdb(raw, return_as_pandas=return_as_pandas)
    return raw


def on3_team_ranking(
    sport_key: Optional[int] = None,
    year: Optional[int] = None,
    page: Optional[int] = None,
    page_size: Optional[int] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /rdb/v1/team-ranking

    Endpoint: ``GET https://api.on3.com/public/rdb/v1/team-ranking``
    Example URL: https://api.on3.com/public/rdb/v1/team-ranking

    Args:
        sport_key: sportKey query parameter.
        year: year query parameter.
        page: page query parameter.
        page_size: pageSize query parameter.
        return_parsed: parse the payload through parse_on3_rdb -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            on3_team_ranking()
    """
    raw = _get(
        "https://api.on3.com/public/rdb/v1/team-ranking",
        params={
            "sportKey": sport_key,
            "year": year,
            "page": page,
            "pageSize": page_size,
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_on3_rdb(raw, return_as_pandas=return_as_pandas)
    return raw


def on3_team_ranking_bluechips_team_rankings(
    sport_slug: str,
    year: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /rdb/v1/team-ranking/{sport}-{year}/bluechips-team-rankings

    Endpoint: ``GET https://api.on3.com/public/rdb/v1/team-ranking/{sport_slug}-{year}/bluechips-team-rankings``
    Example URL: https://api.on3.com/public/rdb/v1/team-ranking/football-2025/bluechips-team-rankings

    Args:
        sport_slug: sport_slug path parameter.
        year: year path parameter.
        return_parsed: parse the payload through parse_on3_rdb -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            on3_team_ranking_bluechips_team_rankings(sport_slug='football', year=2025)
    """
    raw = _get(
        f"https://api.on3.com/public/rdb/v1/team-ranking/{sport_slug}-{year}/bluechips-team-rankings",
        params={
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_on3_rdb(raw, return_as_pandas=return_as_pandas)
    return raw


def on3_team_ranking_consensus_team_rankings(
    sport_slug: str,
    year: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /rdb/v1/team-ranking/{sport}-{year}/consensus-team-rankings

    Endpoint: ``GET https://api.on3.com/public/rdb/v1/team-ranking/{sport_slug}-{year}/consensus-team-rankings``
    Example URL: https://api.on3.com/public/rdb/v1/team-ranking/football-2025/consensus-team-rankings

    Args:
        sport_slug: sport_slug path parameter.
        year: year path parameter.
        return_parsed: parse the payload through parse_on3_rdb -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            on3_team_ranking_consensus_team_rankings(sport_slug='football', year=2025)
    """
    raw = _get(
        f"https://api.on3.com/public/rdb/v1/team-ranking/{sport_slug}-{year}/consensus-team-rankings",
        params={
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_on3_rdb(raw, return_as_pandas=return_as_pandas)
    return raw


def on3_team_ranking_organizations_summary(
    org_key: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /rdb/v1/team-ranking/organizations/{orgKey}/summary

    Endpoint: ``GET https://api.on3.com/public/rdb/v1/team-ranking/organizations/{org_key}/summary``
    Example URL: https://api.on3.com/public/rdb/v1/team-ranking/organizations/1867/summary

    Args:
        org_key: org_key path parameter.
        return_parsed: parse the payload through parse_on3_rdb -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            on3_team_ranking_organizations_summary(org_key=1867)
    """
    raw = _get(
        f"https://api.on3.com/public/rdb/v1/team-ranking/organizations/{org_key}/summary",
        params={
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_on3_rdb(raw, return_as_pandas=return_as_pandas)
    return raw


def on3_team_ranking_team_rankings(
    sport_slug: str,
    year: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /rdb/v1/team-ranking/{sport}-{year}/team-rankings

    Endpoint: ``GET https://api.on3.com/public/rdb/v1/team-ranking/{sport_slug}-{year}/team-rankings``
    Example URL: https://api.on3.com/public/rdb/v1/team-ranking/football-2025/team-rankings

    Args:
        sport_slug: sport_slug path parameter.
        year: year path parameter.
        return_parsed: parse the payload through parse_on3_rdb -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            on3_team_ranking_team_rankings(sport_slug='football', year=2025)
    """
    raw = _get(
        f"https://api.on3.com/public/rdb/v1/team-ranking/{sport_slug}-{year}/team-rankings",
        params={
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_on3_rdb(raw, return_as_pandas=return_as_pandas)
    return raw


def on3_transfers_best_available(
    org_key: Optional[int] = None,
    sport_key: Optional[int] = None,
    year: Optional[int] = None,
    position_abbr: Optional[str] = None,
    status: Optional[str] = None,
    page: Optional[int] = None,
    cutoff: Optional[int] = None,
    order_by: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /rdb/v1/transfers/best-available

    Endpoint: ``GET https://api.on3.com/public/rdb/v1/transfers/best-available``
    Example URL: https://api.on3.com/public/rdb/v1/transfers/best-available

    Args:
        org_key: orgKey query parameter.
        sport_key: sportKey query parameter.
        year: year query parameter.
        position_abbr: positionAbbr query parameter.
        status: status query parameter.
        page: page query parameter.
        cutoff: cutoff query parameter.
        order_by: orderBy query parameter.
        return_parsed: parse the payload through parse_on3_rdb -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            on3_transfers_best_available()
    """
    raw = _get(
        "https://api.on3.com/public/rdb/v1/transfers/best-available",
        params={
            "orgKey": org_key,
            "sportKey": sport_key,
            "year": year,
            "positionAbbr": position_abbr,
            "status": status,
            "page": page,
            "cutoff": cutoff,
            "orderBy": order_by,
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_on3_rdb(raw, return_as_pandas=return_as_pandas)
    return raw


def on3_transfers_latest(
    org_key: Optional[int] = None,
    sport_key: Optional[int] = None,
    year: Optional[int] = None,
    position_abbr: Optional[str] = None,
    status: Optional[str] = None,
    page: Optional[int] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /rdb/v1/transfers/latest

    Endpoint: ``GET https://api.on3.com/public/rdb/v1/transfers/latest``
    Example URL: https://api.on3.com/public/rdb/v1/transfers/latest

    Args:
        org_key: orgKey query parameter.
        sport_key: sportKey query parameter.
        year: year query parameter.
        position_abbr: positionAbbr query parameter.
        status: status query parameter.
        page: page query parameter.
        return_parsed: parse the payload through parse_on3_rdb -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            on3_transfers_latest()
    """
    raw = _get(
        "https://api.on3.com/public/rdb/v1/transfers/latest",
        params={
            "orgKey": org_key,
            "sportKey": sport_key,
            "year": year,
            "positionAbbr": position_abbr,
            "status": status,
            "page": page,
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_on3_rdb(raw, return_as_pandas=return_as_pandas)
    return raw


def on3_videos_video_key(
    video_key: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """GET /rdb/v1/videos/{videoKey}

    Endpoint: ``GET https://api.on3.com/public/rdb/v1/videos/{video_key}``
    Example URL: https://api.on3.com/public/rdb/v1/videos/1

    Args:
        video_key: video_key path parameter.
        return_parsed: parse the payload through parse_on3_rdb -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            on3_videos_video_key(video_key=1)
    """
    raw = _get(
        f"https://api.on3.com/public/rdb/v1/videos/{video_key}",
        params={
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_on3_rdb(raw, return_as_pandas=return_as_pandas)
    return raw
