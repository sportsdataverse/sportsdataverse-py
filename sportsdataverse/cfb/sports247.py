# GENERATED by tools/codegen/generate.py -- DO NOT EDIT.
# Source: tools/codegen/endpoints/sports247.yaml
"""sports247 wrappers (host https://ipa.247sports.com). Generated."""

from __future__ import annotations

from typing import TYPE_CHECKING, Dict, List, Optional, Union  # noqa: F401

from sportsdataverse.cfb.sports247_runtime import _get
from sportsdataverse.cfb.sports247_parsers import parse_sports247_institution_rankings, parse_sports247_teams

if TYPE_CHECKING:  # pragma: no cover -- annotation-only imports (PEP 563 defers eval)
    import pandas as pd
    import polars as pl

__all__ = [
    "sports247_teams",
    "sports247_institution_rankings",
]


def sports247_teams(
    sport_key: Optional[int] = 1,
    year: Optional[int] = None,
    institution_type: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """247Sports RDB college team directory (teamId / institutionKey / conference) for a sport.

    Endpoint: ``GET https://ipa.247sports.com/rdb/v1/teams/``
    Example URL: https://ipa.247sports.com/rdb/v1/teams/

    Args:
        sport_key: sportKey query parameter.
        year: year query parameter.
        institution_type: institutionType query parameter.
        return_parsed: parse the payload through parse_sports247_teams -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            sports247_teams()
    """
    raw = _get(
        "https://ipa.247sports.com/rdb/v1/teams/",
        params={
            "sportKey": sport_key,
            "year": year,
            "institutionType": institution_type,
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_sports247_teams(raw, return_as_pandas=return_as_pandas)
    return raw


def sports247_institution_rankings(
    year: Union[int, str],
    sport_key: int = 1,
    pagesize: Optional[int] = 50,
    page: Optional[int] = None,
    use_composite: Optional[str] = None,
    conference_abbreviation: Optional[str] = None,
    institution_key: Optional[int] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """247Sports team recruiting-class rankings (247 rank/rating + industry composite) for a sport and class year.

    Endpoint: ``GET https://ipa.247sports.com/rdb/v1/rankings/{sport_key}/{year}/institutionrankings/``
    Example URL: https://ipa.247sports.com/rdb/v1/rankings/1/2026/institutionrankings/

    Args:
        year: year path parameter.
        sport_key: sport_key path parameter.
        pagesize: pagesize query parameter.
        page: page query parameter.
        use_composite: useComposite query parameter.
        conference_abbreviation: conferenceAbbreviation query parameter.
        institution_key: institutionKey query parameter.
        return_parsed: parse the payload through parse_sports247_institution_rankings -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            sports247_institution_rankings(year=2026, sport_key=1)
    """
    raw = _get(
        f"https://ipa.247sports.com/rdb/v1/rankings/{sport_key}/{year}/institutionrankings/",
        params={
            "pagesize": pagesize,
            "page": page,
            "useComposite": use_composite,
            "conferenceAbbreviation": conference_abbreviation,
            "institutionKey": institution_key,
            **{_k: _v for _k, _v in kwargs.items() if _v is not None},
        },
    )
    if return_parsed:
        return parse_sports247_institution_rankings(raw, return_as_pandas=return_as_pandas)
    return raw
