# GENERATED by tools/codegen/generate.py -- DO NOT EDIT.
# Source: tools/codegen/endpoints/releases.yaml
"""sportsdataverse.wbb dataset loaders (sportsdataverse-data releases). Generated."""

from __future__ import annotations

import polars as pl

from sportsdataverse._codegen_runtime import (
    SeasonNotFoundError,  # noqa: F401
    _as_season_list,
    _cast_ids_int64,  # noqa: F401  (used only by loaders declaring id_int64)
    _read_release_parquet,
    cli_warn,
)

__all__ = [
    "load_wbb_pbp",
    "load_wbb_player_boxscore",
    "load_wbb_schedule",
    "load_wbb_team_boxscore",
    "load_wbb_ratings",
    "load_wbb_player_value",
    "load_wbb_game_rosters",
    "load_wbb_officials",
    "load_wbb_player_season_stats",
    "load_wbb_rosters",
    "load_wbb_shots",
    "load_wbb_standings",
    "load_wbb_team_season_stats",
    "load_wbb_player_crosswalk",
    "load_wbb_schedule_crosswalk",
    "load_wbb_team_crosswalk",
    "load_wbb_player_core",
    "load_ncaa_wbb_rapm",
    "load_ncaa_wbb_pbp",
    "load_ncaa_wbb_schedule",
    "load_ncaa_wbb_player_box",
    "load_ncaa_wbb_team_box",
    "load_ncaa_wbb_rosters",
    "load_ncaa_wbb_team_rosters",
    "load_ncaa_wbb_team_ids",
    "load_ncaa_wbb_possessions",
    "load_ncaa_wbb_lineups",
    "load_ncaa_wbb_matchup_stints",
    "load_ncaa_wbb_shots",
    "load_ncaa_wbb_rapm_within_team",
]


def load_wbb_pbp(seasons, return_as_pandas: bool = False):
    """Load espn_womens_college_basketball_pbp (sportsdataverse-data release).

    Source: https://github.com/sportsdataverse/sportsdataverse-data/releases/tag/espn_womens_college_basketball_pbp

    Args:
        seasons: an int or iterable of seasons (>= 2002).
        return_as_pandas: return a pandas DataFrame instead of polars.

    Returns:
        A polars (or pandas) DataFrame; seasons with no published asset are
        skipped with a warning rather than raising (404-safe).

        |col_name                        |type                                                   |
        |:-------------------------------|:------------------------------------------------------|
        |game_play_number                |Int32                                                  |
        |id                              |Int64                                                  |
        |sequence_number                 |Int32                                                  |
        |type_id                         |Int32                                                  |
        |type_text                       |String                                                 |
        |text                            |String                                                 |
        |away_score                      |Int32                                                  |
        |home_score                      |Int32                                                  |
        |period_number                   |Int32                                                  |
        |period_display_value            |String                                                 |
        |clock_display_value             |String                                                 |
        |scoring_play                    |Boolean                                                |
        |score_value                     |Int32                                                  |
        |wallclock                       |String                                                 |
        |shooting_play                   |Boolean                                                |
        |coordinate_x_raw                |Float64                                                |
        |coordinate_y_raw                |Float64                                                |
        |points_attempted                |Int32                                                  |
        |short_description               |String                                                 |
        |team_id                         |Int32                                                  |
        |athlete_id_1                    |Int32                                                  |
        |athlete_id_2                    |Int32                                                  |
        |game_id                         |Int32                                                  |
        |season                          |Int32                                                  |
        |season_type                     |Int32                                                  |
        |home_team_id                    |Int32                                                  |
        |home_team_name                  |String                                                 |
        |home_team_mascot                |String                                                 |
        |home_team_abbrev                |String                                                 |
        |home_team_name_alt              |String                                                 |
        |away_team_id                    |Int32                                                  |
        |away_team_name                  |String                                                 |
        |away_team_mascot                |String                                                 |
        |away_team_abbrev                |String                                                 |
        |away_team_name_alt              |String                                                 |
        |game_spread                     |Float64                                                |
        |home_favorite                   |Boolean                                                |
        |game_spread_available           |Boolean                                                |
        |home_team_spread                |Float64                                                |
        |qtr                             |Int32                                                  |
        |time                            |String                                                 |
        |clock_minutes                   |Int32                                                  |
        |clock_seconds                   |Int32                                                  |
        |home_timeout_called             |Boolean                                                |
        |away_timeout_called             |Boolean                                                |
        |half                            |Int32                                                  |
        |game_half                       |Int32                                                  |
        |lag_qtr                         |Int32                                                  |
        |lead_qtr                        |Int32                                                  |
        |lag_half                        |Int32                                                  |
        |lead_half                       |Int32                                                  |
        |start_quarter_seconds_remaining |Int32                                                  |
        |start_half_seconds_remaining    |Int32                                                  |
        |start_game_seconds_remaining    |Int32                                                  |
        |end_quarter_seconds_remaining   |Int32                                                  |
        |end_half_seconds_remaining      |Int32                                                  |
        |end_game_seconds_remaining      |Int32                                                  |
        |period                          |Int32                                                  |
        |coordinate_x                    |Float64                                                |
        |coordinate_y                    |Float64                                                |
        |game_date                       |Date                                                   |
        |game_date_time                  |Datetime(time_unit='us', time_zone='America/New_York') |
        |athlete_name_1                  |String                                                 |
        |athlete_name_2                  |String                                                 |
        |athlete_name_3                  |String                                                 |

    Raises:
        SeasonNotFoundError: if a requested season is below 2002.

    Example:
        Quick start::

            load_wbb_pbp(seasons=2024)
    """
    frames, missing = [], []
    for season in _as_season_list(seasons):
        if int(season) < 2002:
            raise SeasonNotFoundError("season cannot be less than 2002")
        df = _read_release_parquet(
            f"https://github.com/sportsdataverse/sportsdataverse-data/releases/download/espn_womens_college_basketball_pbp/play_by_play_{season}.parquet"
        )
        if df is None:
            missing.append(season)
            continue
        frames.append(df)
    if missing:
        cli_warn("load_wbb_pbp: no data for season(s) {missing} (skipped)".format(missing=missing))
    # diagonal: per-season release schemas can drift (columns added/dropped
    # over the years) -- union columns, null-fill gaps.
    out = pl.concat(frames, how="diagonal_relaxed") if frames else pl.DataFrame()
    return out.to_pandas(use_pyarrow_extension_array=True) if return_as_pandas else out


def load_wbb_player_boxscore(seasons, return_as_pandas: bool = False):
    """Load espn_womens_college_basketball_player_boxscores (sportsdataverse-data release).

    Source: https://github.com/sportsdataverse/sportsdataverse-data/releases/tag/espn_womens_college_basketball_player_boxscores

    Args:
        seasons: an int or iterable of seasons (>= 2002).
        return_as_pandas: return a pandas DataFrame instead of polars.

    Returns:
        A polars (or pandas) DataFrame; seasons with no published asset are
        skipped with a warning rather than raising (404-safe).

        |col_name                          |type                                                   |
        |:---------------------------------|:------------------------------------------------------|
        |game_id                           |Int32                                                  |
        |season                            |Int32                                                  |
        |season_type                       |Int32                                                  |
        |game_date                         |Date                                                   |
        |game_date_time                    |Datetime(time_unit='us', time_zone='America/New_York') |
        |athlete_id                        |Int32                                                  |
        |athlete_display_name              |String                                                 |
        |team_id                           |Int32                                                  |
        |team_name                         |String                                                 |
        |team_location                     |String                                                 |
        |team_short_display_name           |String                                                 |
        |minutes                           |Float64                                                |
        |field_goals_made                  |Int32                                                  |
        |field_goals_attempted             |Int32                                                  |
        |three_point_field_goals_made      |Int32                                                  |
        |three_point_field_goals_attempted |Int32                                                  |
        |free_throws_made                  |Int32                                                  |
        |free_throws_attempted             |Int32                                                  |
        |offensive_rebounds                |Int32                                                  |
        |defensive_rebounds                |Int32                                                  |
        |rebounds                          |Int32                                                  |
        |assists                           |Int32                                                  |
        |steals                            |Int32                                                  |
        |blocks                            |Int32                                                  |
        |turnovers                         |Int32                                                  |
        |fouls                             |Int32                                                  |
        |points                            |Int32                                                  |
        |starter                           |Boolean                                                |
        |ejected                           |Boolean                                                |
        |did_not_play                      |Boolean                                                |
        |active                            |Boolean                                                |
        |athlete_jersey                    |String                                                 |
        |athlete_short_name                |String                                                 |
        |athlete_headshot_href             |String                                                 |
        |athlete_position_name             |String                                                 |
        |athlete_position_abbreviation     |String                                                 |
        |team_display_name                 |String                                                 |
        |team_uid                          |String                                                 |
        |team_slug                         |String                                                 |
        |team_logo                         |String                                                 |
        |team_abbreviation                 |String                                                 |
        |team_color                        |String                                                 |
        |team_alternate_color              |String                                                 |
        |home_away                         |String                                                 |
        |team_winner                       |Boolean                                                |
        |team_score                        |Int32                                                  |
        |opponent_team_id                  |Int32                                                  |
        |opponent_team_name                |String                                                 |
        |opponent_team_location            |String                                                 |
        |opponent_team_display_name        |String                                                 |
        |opponent_team_abbreviation        |String                                                 |
        |opponent_team_logo                |String                                                 |
        |opponent_team_color               |String                                                 |
        |opponent_team_alternate_color     |String                                                 |
        |opponent_team_score               |Int32                                                  |

    Raises:
        SeasonNotFoundError: if a requested season is below 2002.

    Example:
        Quick start::

            load_wbb_player_boxscore(seasons=2024)
    """
    frames, missing = [], []
    for season in _as_season_list(seasons):
        if int(season) < 2002:
            raise SeasonNotFoundError("season cannot be less than 2002")
        df = _read_release_parquet(
            f"https://github.com/sportsdataverse/sportsdataverse-data/releases/download/espn_womens_college_basketball_player_boxscores/player_box_{season}.parquet"
        )
        if df is None:
            missing.append(season)
            continue
        frames.append(df)
    if missing:
        cli_warn("load_wbb_player_boxscore: no data for season(s) {missing} (skipped)".format(missing=missing))
    # diagonal: per-season release schemas can drift (columns added/dropped
    # over the years) -- union columns, null-fill gaps.
    out = pl.concat(frames, how="diagonal_relaxed") if frames else pl.DataFrame()
    return out.to_pandas(use_pyarrow_extension_array=True) if return_as_pandas else out


def load_wbb_schedule(seasons, return_as_pandas: bool = False):
    """Load espn_womens_college_basketball_schedules (sportsdataverse-data release).

    Source: https://github.com/sportsdataverse/sportsdataverse-data/releases/tag/espn_womens_college_basketball_schedules

    Args:
        seasons: an int or iterable of seasons (>= 2002).
        return_as_pandas: return a pandas DataFrame instead of polars.

    Returns:
        A polars (or pandas) DataFrame; seasons with no published asset are
        skipped with a warning rather than raising (404-safe).

        |col_name                  |type                                                   |
        |:-------------------------|:------------------------------------------------------|
        |id                        |Int32                                                  |
        |uid                       |String                                                 |
        |date                      |String                                                 |
        |attendance                |Float64                                                |
        |time_valid                |Boolean                                                |
        |neutral_site              |Boolean                                                |
        |conference_competition    |Boolean                                                |
        |play_by_play_available    |Boolean                                                |
        |recent                    |Boolean                                                |
        |start_date                |String                                                 |
        |broadcast                 |String                                                 |
        |highlights                |String                                                 |
        |notes_type                |String                                                 |
        |notes_headline            |String                                                 |
        |broadcast_market          |String                                                 |
        |broadcast_name            |String                                                 |
        |type_id                   |Int32                                                  |
        |type_abbreviation         |String                                                 |
        |venue_id                  |Int32                                                  |
        |venue_full_name           |String                                                 |
        |venue_address_city        |String                                                 |
        |venue_address_state       |String                                                 |
        |venue_indoor              |Boolean                                                |
        |status_clock              |Float64                                                |
        |status_display_clock      |String                                                 |
        |status_period             |Float64                                                |
        |status_type_id            |Int32                                                  |
        |status_type_name          |String                                                 |
        |status_type_state         |String                                                 |
        |status_type_completed     |Boolean                                                |
        |status_type_description   |String                                                 |
        |status_type_detail        |String                                                 |
        |status_type_short_detail  |String                                                 |
        |format_regulation_periods |Float64                                                |
        |home_id                   |Int32                                                  |
        |home_uid                  |String                                                 |
        |home_location             |String                                                 |
        |home_name                 |String                                                 |
        |home_abbreviation         |String                                                 |
        |home_display_name         |String                                                 |
        |home_short_display_name   |String                                                 |
        |home_color                |String                                                 |
        |home_alternate_color      |String                                                 |
        |home_is_active            |Boolean                                                |
        |home_venue_id             |Int32                                                  |
        |home_logo                 |String                                                 |
        |home_conference_id        |Int32                                                  |
        |home_score                |Int32                                                  |
        |home_winner               |Boolean                                                |
        |home_current_rank         |Float64                                                |
        |home_linescores           |String                                                 |
        |home_records              |String                                                 |
        |away_id                   |Int32                                                  |
        |away_uid                  |String                                                 |
        |away_location             |String                                                 |
        |away_name                 |String                                                 |
        |away_abbreviation         |String                                                 |
        |away_display_name         |String                                                 |
        |away_short_display_name   |String                                                 |
        |away_color                |String                                                 |
        |away_alternate_color      |String                                                 |
        |away_is_active            |Boolean                                                |
        |away_venue_id             |Int32                                                  |
        |away_logo                 |String                                                 |
        |away_conference_id        |Int32                                                  |
        |away_score                |Int32                                                  |
        |away_winner               |Boolean                                                |
        |away_current_rank         |Float64                                                |
        |away_linescores           |String                                                 |
        |away_records              |String                                                 |
        |game_id                   |Int32                                                  |
        |season                    |Int32                                                  |
        |season_type               |Int32                                                  |
        |status_type_alt_detail    |String                                                 |
        |tournament_id             |Int32                                                  |
        |groups_id                 |Int32                                                  |
        |groups_name               |String                                                 |
        |groups_short_name         |String                                                 |
        |groups_is_conference      |Boolean                                                |
        |game_json                 |Boolean                                                |
        |game_json_url             |String                                                 |
        |game_date_time            |Datetime(time_unit='us', time_zone='America/New_York') |
        |game_date                 |Date                                                   |
        |PBP                       |Boolean                                                |
        |team_box                  |Boolean                                                |
        |player_box                |Boolean                                                |

    Raises:
        SeasonNotFoundError: if a requested season is below 2002.

    Example:
        Quick start::

            load_wbb_schedule(seasons=2024)
    """
    frames, missing = [], []
    for season in _as_season_list(seasons):
        if int(season) < 2002:
            raise SeasonNotFoundError("season cannot be less than 2002")
        df = _read_release_parquet(
            f"https://github.com/sportsdataverse/sportsdataverse-data/releases/download/espn_womens_college_basketball_schedules/wbb_schedule_{season}.parquet"
        )
        if df is None:
            missing.append(season)
            continue
        frames.append(df)
    if missing:
        cli_warn("load_wbb_schedule: no data for season(s) {missing} (skipped)".format(missing=missing))
    # diagonal: per-season release schemas can drift (columns added/dropped
    # over the years) -- union columns, null-fill gaps.
    out = pl.concat(frames, how="diagonal_relaxed") if frames else pl.DataFrame()
    return out.to_pandas(use_pyarrow_extension_array=True) if return_as_pandas else out


def load_wbb_team_boxscore(seasons, return_as_pandas: bool = False):
    """Load espn_womens_college_basketball_team_boxscores (sportsdataverse-data release).

    Source: https://github.com/sportsdataverse/sportsdataverse-data/releases/tag/espn_womens_college_basketball_team_boxscores

    Args:
        seasons: an int or iterable of seasons (>= 2002).
        return_as_pandas: return a pandas DataFrame instead of polars.

    Returns:
        A polars (or pandas) DataFrame; seasons with no published asset are
        skipped with a warning rather than raising (404-safe).

        |col_name                          |type                                                   |
        |:---------------------------------|:------------------------------------------------------|
        |game_id                           |Int32                                                  |
        |season                            |Int32                                                  |
        |season_type                       |Int32                                                  |
        |game_date                         |Date                                                   |
        |game_date_time                    |Datetime(time_unit='us', time_zone='America/New_York') |
        |team_id                           |Int32                                                  |
        |team_uid                          |String                                                 |
        |team_slug                         |String                                                 |
        |team_location                     |String                                                 |
        |team_name                         |String                                                 |
        |team_abbreviation                 |String                                                 |
        |team_display_name                 |String                                                 |
        |team_short_display_name           |String                                                 |
        |team_color                        |String                                                 |
        |team_alternate_color              |String                                                 |
        |team_logo                         |String                                                 |
        |team_home_away                    |String                                                 |
        |team_score                        |Int32                                                  |
        |team_winner                       |Boolean                                                |
        |assists                           |Int32                                                  |
        |blocks                            |Int32                                                  |
        |defensive_rebounds                |Int32                                                  |
        |fast_break_points                 |String                                                 |
        |field_goal_pct                    |Float64                                                |
        |field_goals_made                  |Int32                                                  |
        |field_goals_attempted             |Int32                                                  |
        |fouls                             |Int32                                                  |
        |free_throw_pct                    |Float64                                                |
        |free_throws_made                  |Int32                                                  |
        |free_throws_attempted             |Int32                                                  |
        |largest_lead                      |String                                                 |
        |lead_changes                      |String                                                 |
        |lead_percentage                   |String                                                 |
        |offensive_rebounds                |Int32                                                  |
        |points_in_paint                   |String                                                 |
        |steals                            |Int32                                                  |
        |team_turnovers                    |Int32                                                  |
        |technical_fouls                   |Int32                                                  |
        |three_point_field_goal_pct        |Float64                                                |
        |three_point_field_goals_made      |Int32                                                  |
        |three_point_field_goals_attempted |Int32                                                  |
        |total_rebounds                    |Int32                                                  |
        |total_technical_fouls             |Int32                                                  |
        |total_turnovers                   |Int32                                                  |
        |turnover_points                   |String                                                 |
        |turnovers                         |Int32                                                  |
        |opponent_team_id                  |Int32                                                  |
        |opponent_team_uid                 |String                                                 |
        |opponent_team_slug                |String                                                 |
        |opponent_team_location            |String                                                 |
        |opponent_team_name                |String                                                 |
        |opponent_team_abbreviation        |String                                                 |
        |opponent_team_display_name        |String                                                 |
        |opponent_team_short_display_name  |String                                                 |
        |opponent_team_color               |String                                                 |
        |opponent_team_alternate_color     |String                                                 |
        |opponent_team_logo                |String                                                 |
        |opponent_team_score               |Int32                                                  |

    Raises:
        SeasonNotFoundError: if a requested season is below 2002.

    Example:
        Quick start::

            load_wbb_team_boxscore(seasons=2024)
    """
    frames, missing = [], []
    for season in _as_season_list(seasons):
        if int(season) < 2002:
            raise SeasonNotFoundError("season cannot be less than 2002")
        df = _read_release_parquet(
            f"https://github.com/sportsdataverse/sportsdataverse-data/releases/download/espn_womens_college_basketball_team_boxscores/team_box_{season}.parquet"
        )
        if df is None:
            missing.append(season)
            continue
        frames.append(df)
    if missing:
        cli_warn("load_wbb_team_boxscore: no data for season(s) {missing} (skipped)".format(missing=missing))
    # diagonal: per-season release schemas can drift (columns added/dropped
    # over the years) -- union columns, null-fill gaps.
    out = pl.concat(frames, how="diagonal_relaxed") if frames else pl.DataFrame()
    return out.to_pandas(use_pyarrow_extension_array=True) if return_as_pandas else out


def load_wbb_ratings(seasons, return_as_pandas: bool = False):
    """Load wbb_ratings (sportsdataverse-data release).

    Source: https://github.com/sportsdataverse/sportsdataverse-data/releases/tag/wbb_ratings

    Args:
        seasons: an int or iterable of seasons (>= 2008).
        return_as_pandas: return a pandas DataFrame instead of polars.

    Returns:
        A polars (or pandas) DataFrame; seasons with no published asset are
        skipped with a warning rather than raising (404-safe).

        |col_name  |type    |
        |:---------|:-------|
        |season    |Int64   |
        |team_id   |String  |
        |adj_o     |Float64 |
        |adj_d     |Float64 |
        |adj_em    |Float64 |
        |adj_tempo |Float64 |
        |raw_o     |Float64 |
        |raw_d     |Float64 |
        |games     |Int64   |
        |rank      |Int64   |
        |adj_em_z  |Float64 |

    Raises:
        SeasonNotFoundError: if a requested season is below 2008.

    Example:
        Quick start::

            load_wbb_ratings(seasons=2025)
    """
    frames, missing = [], []
    for season in _as_season_list(seasons):
        if int(season) < 2008:
            raise SeasonNotFoundError("season cannot be less than 2008")
        df = _read_release_parquet(
            f"https://github.com/sportsdataverse/sportsdataverse-data/releases/download/wbb_ratings/wbb_ratings_{season}.parquet"
        )
        if df is None:
            missing.append(season)
            continue
        frames.append(df)
    if missing:
        cli_warn("load_wbb_ratings: no data for season(s) {missing} (skipped)".format(missing=missing))
    # diagonal: per-season release schemas can drift (columns added/dropped
    # over the years) -- union columns, null-fill gaps.
    out = pl.concat(frames, how="diagonal_relaxed") if frames else pl.DataFrame()
    return out.to_pandas(use_pyarrow_extension_array=True) if return_as_pandas else out


def load_wbb_player_value(seasons, return_as_pandas: bool = False):
    """Load wbb_player_value (sportsdataverse-data release).

    Source: https://github.com/sportsdataverse/sportsdataverse-data/releases/tag/wbb_player_value

    Args:
        seasons: an int or iterable of seasons (>= 2014).
        return_as_pandas: return a pandas DataFrame instead of polars.

    Returns:
        A polars (or pandas) DataFrame; seasons with no published asset are
        skipped with a warning rather than raising (404-safe).

        |col_name  |type    |
        |:---------|:-------|
        |player_id |String  |
        |player    |String  |
        |season    |Int64   |
        |team_id   |String  |
        |min       |Float64 |
        |box_obpm  |Float64 |
        |box_dbpm  |Float64 |
        |box_bpm   |Float64 |

    Raises:
        SeasonNotFoundError: if a requested season is below 2014.

    Example:
        Quick start::

            load_wbb_player_value(seasons=2025)
    """
    frames, missing = [], []
    for season in _as_season_list(seasons):
        if int(season) < 2014:
            raise SeasonNotFoundError("season cannot be less than 2014")
        df = _read_release_parquet(
            f"https://github.com/sportsdataverse/sportsdataverse-data/releases/download/wbb_player_value/wbb_player_value_{season}.parquet"
        )
        if df is None:
            missing.append(season)
            continue
        frames.append(df)
    if missing:
        cli_warn("load_wbb_player_value: no data for season(s) {missing} (skipped)".format(missing=missing))
    # diagonal: per-season release schemas can drift (columns added/dropped
    # over the years) -- union columns, null-fill gaps.
    out = pl.concat(frames, how="diagonal_relaxed") if frames else pl.DataFrame()
    return out.to_pandas(use_pyarrow_extension_array=True) if return_as_pandas else out


def load_wbb_game_rosters(seasons, return_as_pandas: bool = False):
    """Load espn_womens_college_basketball_game_rosters (sportsdataverse-data release).

    Source: https://github.com/sportsdataverse/sportsdataverse-data/releases/tag/espn_womens_college_basketball_game_rosters

    Args:
        seasons: an int or iterable of seasons (>= 2026).
        return_as_pandas: return a pandas DataFrame instead of polars.

    Returns:
        A polars (or pandas) DataFrame; seasons with no published asset are
        skipped with a warning rather than raising (404-safe).

        |col_name             |type    |
        |:--------------------|:-------|
        |season               |Int32   |
        |game_id              |String  |
        |team_id              |Int32   |
        |team_slug            |String  |
        |team_abbreviation    |String  |
        |team_display_name    |String  |
        |home_away            |String  |
        |athlete_id           |Int32   |
        |athlete_uid          |String  |
        |athlete_guid         |String  |
        |athlete_display_name |String  |
        |athlete_short_name   |String  |
        |athlete_first_name   |String  |
        |athlete_last_name    |String  |
        |athlete_jersey       |String  |
        |athlete_position     |String  |
        |athlete_headshot     |String  |
        |starter              |Boolean |
        |did_not_play         |Boolean |
        |active               |Boolean |
        |ejected              |Boolean |
        |reason               |String  |

    Raises:
        SeasonNotFoundError: if a requested season is below 2026.

    Example:
        Quick start::

            load_wbb_game_rosters(seasons=2026)
    """
    frames, missing = [], []
    for season in _as_season_list(seasons):
        if int(season) < 2026:
            raise SeasonNotFoundError("season cannot be less than 2026")
        df = _read_release_parquet(
            f"https://github.com/sportsdataverse/sportsdataverse-data/releases/download/espn_womens_college_basketball_game_rosters/game_rosters_{season}.parquet"
        )
        if df is None:
            missing.append(season)
            continue
        frames.append(df)
    if missing:
        cli_warn("load_wbb_game_rosters: no data for season(s) {missing} (skipped)".format(missing=missing))
    # diagonal: per-season release schemas can drift (columns added/dropped
    # over the years) -- union columns, null-fill gaps.
    out = pl.concat(frames, how="diagonal_relaxed") if frames else pl.DataFrame()
    return out.to_pandas(use_pyarrow_extension_array=True) if return_as_pandas else out


def load_wbb_officials(seasons, return_as_pandas: bool = False):
    """Load espn_womens_college_basketball_officials (sportsdataverse-data release).

    Source: https://github.com/sportsdataverse/sportsdataverse-data/releases/tag/espn_womens_college_basketball_officials

    Args:
        seasons: an int or iterable of seasons (>= 2026).
        return_as_pandas: return a pandas DataFrame instead of polars.

    Returns:
        A polars (or pandas) DataFrame; seasons with no published asset are
        skipped with a warning rather than raising (404-safe).

        |col_name              |type   |
        |:---------------------|:------|
        |season                |Int32  |
        |game_id               |String |
        |official_id           |Int32  |
        |official_uid          |String |
        |official_full_name    |String |
        |official_display_name |String |
        |official_first_name   |String |
        |official_last_name    |String |
        |official_order        |Int32  |
        |position_name         |String |
        |position_display_name |String |

    Raises:
        SeasonNotFoundError: if a requested season is below 2026.

    Example:
        Quick start::

            load_wbb_officials(seasons=2026)
    """
    frames, missing = [], []
    for season in _as_season_list(seasons):
        if int(season) < 2026:
            raise SeasonNotFoundError("season cannot be less than 2026")
        df = _read_release_parquet(
            f"https://github.com/sportsdataverse/sportsdataverse-data/releases/download/espn_womens_college_basketball_officials/officials_{season}.parquet"
        )
        if df is None:
            missing.append(season)
            continue
        frames.append(df)
    if missing:
        cli_warn("load_wbb_officials: no data for season(s) {missing} (skipped)".format(missing=missing))
    # diagonal: per-season release schemas can drift (columns added/dropped
    # over the years) -- union columns, null-fill gaps.
    out = pl.concat(frames, how="diagonal_relaxed") if frames else pl.DataFrame()
    return out.to_pandas(use_pyarrow_extension_array=True) if return_as_pandas else out


def load_wbb_player_season_stats(seasons, return_as_pandas: bool = False):
    """Load espn_womens_college_basketball_player_season_stats (sportsdataverse-data release).

    Source: https://github.com/sportsdataverse/sportsdataverse-data/releases/tag/espn_womens_college_basketball_player_season_stats

    Args:
        seasons: an int or iterable of seasons (>= 2026).
        return_as_pandas: return a pandas DataFrame instead of polars.

    Returns:
        A polars (or pandas) DataFrame; seasons with no published asset are
        skipped with a warning rather than raising (404-safe).

        |col_name                      |type    |
        |:-----------------------------|:-------|
        |season                        |Int32   |
        |athlete_id                    |Int32   |
        |athlete_display_name          |String  |
        |athlete_first_name            |String  |
        |athlete_last_name             |String  |
        |athlete_position_abbreviation |String  |
        |athlete_jersey                |String  |
        |team_id                       |Int32   |
        |team_display_name             |String  |
        |category                      |String  |
        |stat_label                    |String  |
        |stat_name                     |String  |
        |stat_display_name             |String  |
        |stat_description              |String  |
        |display_value                 |String  |
        |value                         |Float64 |

    Raises:
        SeasonNotFoundError: if a requested season is below 2026.

    Example:
        Quick start::

            load_wbb_player_season_stats(seasons=2026)
    """
    frames, missing = [], []
    for season in _as_season_list(seasons):
        if int(season) < 2026:
            raise SeasonNotFoundError("season cannot be less than 2026")
        df = _read_release_parquet(
            f"https://github.com/sportsdataverse/sportsdataverse-data/releases/download/espn_womens_college_basketball_player_season_stats/player_season_stats_{season}.parquet"
        )
        if df is None:
            missing.append(season)
            continue
        frames.append(df)
    if missing:
        cli_warn("load_wbb_player_season_stats: no data for season(s) {missing} (skipped)".format(missing=missing))
    # diagonal: per-season release schemas can drift (columns added/dropped
    # over the years) -- union columns, null-fill gaps.
    out = pl.concat(frames, how="diagonal_relaxed") if frames else pl.DataFrame()
    return out.to_pandas(use_pyarrow_extension_array=True) if return_as_pandas else out


def load_wbb_rosters(seasons, return_as_pandas: bool = False):
    """Load espn_womens_college_basketball_rosters (sportsdataverse-data release).

    Source: https://github.com/sportsdataverse/sportsdataverse-data/releases/tag/espn_womens_college_basketball_rosters

    Args:
        seasons: an int or iterable of seasons (>= 2026).
        return_as_pandas: return a pandas DataFrame instead of polars.

    Returns:
        A polars (or pandas) DataFrame; seasons with no published asset are
        skipped with a warning rather than raising (404-safe).

        |col_name                 |type   |
        |:------------------------|:------|
        |season                   |Int32  |
        |team_id                  |Int32  |
        |team_slug                |String |
        |team_abbreviation        |String |
        |team_display_name        |String |
        |team_short_display_name  |String |
        |team_color               |String |
        |team_alternate_color     |String |
        |team_logo                |String |
        |athlete_id               |String |
        |uid                      |String |
        |guid                     |String |
        |full_name                |String |
        |display_name             |String |
        |short_name               |String |
        |first_name               |String |
        |last_name                |String |
        |jersey                   |String |
        |position_abbreviation    |String |
        |position_name            |String |
        |position_id              |String |
        |height                   |String |
        |weight                   |String |
        |age                      |String |
        |date_of_birth            |String |
        |birth_place_city         |String |
        |birth_place_state        |String |
        |birth_place_country      |String |
        |experience_years         |String |
        |experience_display_value |String |
        |headshot_href            |String |
        |headshot_alt             |String |
        |link_web                 |String |
        |status_id                |String |
        |status_name              |String |
        |status_type              |String |

    Raises:
        SeasonNotFoundError: if a requested season is below 2026.

    Example:
        Quick start::

            load_wbb_rosters(seasons=2026)
    """
    frames, missing = [], []
    for season in _as_season_list(seasons):
        if int(season) < 2026:
            raise SeasonNotFoundError("season cannot be less than 2026")
        df = _read_release_parquet(
            f"https://github.com/sportsdataverse/sportsdataverse-data/releases/download/espn_womens_college_basketball_rosters/rosters_{season}.parquet"
        )
        if df is None:
            missing.append(season)
            continue
        frames.append(df)
    if missing:
        cli_warn("load_wbb_rosters: no data for season(s) {missing} (skipped)".format(missing=missing))
    # diagonal: per-season release schemas can drift (columns added/dropped
    # over the years) -- union columns, null-fill gaps.
    out = pl.concat(frames, how="diagonal_relaxed") if frames else pl.DataFrame()
    return out.to_pandas(use_pyarrow_extension_array=True) if return_as_pandas else out


def load_wbb_shots(seasons, return_as_pandas: bool = False):
    """Load espn_womens_college_basketball_shots (sportsdataverse-data release).

    Source: https://github.com/sportsdataverse/sportsdataverse-data/releases/tag/espn_womens_college_basketball_shots

    Args:
        seasons: an int or iterable of seasons (>= 2026).
        return_as_pandas: return a pandas DataFrame instead of polars.

    Returns:
        A polars (or pandas) DataFrame; seasons with no published asset are
        skipped with a warning rather than raising (404-safe).

        |col_name            |type    |
        |:-------------------|:-------|
        |game_id             |Int32   |
        |season              |Int32   |
        |period_number       |Int32   |
        |clock_display_value |String  |
        |team_id             |Int32   |
        |athlete_id_1        |Int32   |
        |athlete_id_2        |Int32   |
        |type_id             |Int32   |
        |type_text           |String  |
        |scoring_play        |Boolean |
        |score_value         |Int32   |
        |coordinate_x        |Float64 |
        |coordinate_y        |Float64 |
        |coordinate_x_raw    |Float64 |
        |coordinate_y_raw    |Float64 |
        |athlete_name_1      |String  |
        |athlete_name_2      |String  |
        |team_name           |String  |
        |team_mascot         |String  |
        |team_abbrev         |String  |

    Raises:
        SeasonNotFoundError: if a requested season is below 2026.

    Example:
        Quick start::

            load_wbb_shots(seasons=2026)
    """
    frames, missing = [], []
    for season in _as_season_list(seasons):
        if int(season) < 2026:
            raise SeasonNotFoundError("season cannot be less than 2026")
        df = _read_release_parquet(
            f"https://github.com/sportsdataverse/sportsdataverse-data/releases/download/espn_womens_college_basketball_shots/shots_{season}.parquet"
        )
        if df is None:
            missing.append(season)
            continue
        frames.append(df)
    if missing:
        cli_warn("load_wbb_shots: no data for season(s) {missing} (skipped)".format(missing=missing))
    # diagonal: per-season release schemas can drift (columns added/dropped
    # over the years) -- union columns, null-fill gaps.
    out = pl.concat(frames, how="diagonal_relaxed") if frames else pl.DataFrame()
    return out.to_pandas(use_pyarrow_extension_array=True) if return_as_pandas else out


def load_wbb_standings(seasons, return_as_pandas: bool = False):
    """Load espn_womens_college_basketball_standings (sportsdataverse-data release).

    Source: https://github.com/sportsdataverse/sportsdataverse-data/releases/tag/espn_womens_college_basketball_standings

    Args:
        seasons: an int or iterable of seasons (>= 2026).
        return_as_pandas: return a pandas DataFrame instead of polars.

    Returns:
        A polars (or pandas) DataFrame; seasons with no published asset are
        skipped with a warning rather than raising (404-safe).

        |col_name                |type    |
        |:-----------------------|:-------|
        |season                  |Int32   |
        |group_id                |String  |
        |group_name              |String  |
        |group_abbreviation      |String  |
        |group_short_name        |String  |
        |team_id                 |Int32   |
        |team_uid                |String  |
        |team_slug               |String  |
        |team_location           |String  |
        |team_name               |String  |
        |team_abbreviation       |String  |
        |team_display_name       |String  |
        |team_short_display_name |String  |
        |team_color              |String  |
        |team_alternate_color    |String  |
        |team_logo               |String  |
        |stat_name               |String  |
        |stat_display_name       |String  |
        |stat_short_display_name |String  |
        |stat_description        |String  |
        |stat_abbreviation       |String  |
        |stat_type               |String  |
        |display_value           |String  |
        |value                   |Float64 |

    Raises:
        SeasonNotFoundError: if a requested season is below 2026.

    Example:
        Quick start::

            load_wbb_standings(seasons=2026)
    """
    frames, missing = [], []
    for season in _as_season_list(seasons):
        if int(season) < 2026:
            raise SeasonNotFoundError("season cannot be less than 2026")
        df = _read_release_parquet(
            f"https://github.com/sportsdataverse/sportsdataverse-data/releases/download/espn_womens_college_basketball_standings/standings_{season}.parquet"
        )
        if df is None:
            missing.append(season)
            continue
        frames.append(df)
    if missing:
        cli_warn("load_wbb_standings: no data for season(s) {missing} (skipped)".format(missing=missing))
    # diagonal: per-season release schemas can drift (columns added/dropped
    # over the years) -- union columns, null-fill gaps.
    out = pl.concat(frames, how="diagonal_relaxed") if frames else pl.DataFrame()
    return out.to_pandas(use_pyarrow_extension_array=True) if return_as_pandas else out


def load_wbb_team_season_stats(seasons, return_as_pandas: bool = False):
    """Load espn_womens_college_basketball_team_season_stats (sportsdataverse-data release).

    Source: https://github.com/sportsdataverse/sportsdataverse-data/releases/tag/espn_womens_college_basketball_team_season_stats

    Args:
        seasons: an int or iterable of seasons (>= 2026).
        return_as_pandas: return a pandas DataFrame instead of polars.

    Returns:
        A polars (or pandas) DataFrame; seasons with no published asset are
        skipped with a warning rather than raising (404-safe).

        |col_name                |type    |
        |:-----------------------|:-------|
        |season                  |Int32   |
        |team_id                 |Int32   |
        |team_slug               |String  |
        |team_abbreviation       |String  |
        |team_display_name       |String  |
        |team_short_display_name |String  |
        |team_color              |String  |
        |team_alternate_color    |String  |
        |team_logo               |String  |
        |category                |String  |
        |stat_label              |String  |
        |stat_name               |String  |
        |stat_display_name       |String  |
        |stat_description        |String  |
        |display_value           |String  |
        |value                   |Float64 |

    Raises:
        SeasonNotFoundError: if a requested season is below 2026.

    Example:
        Quick start::

            load_wbb_team_season_stats(seasons=2026)
    """
    frames, missing = [], []
    for season in _as_season_list(seasons):
        if int(season) < 2026:
            raise SeasonNotFoundError("season cannot be less than 2026")
        df = _read_release_parquet(
            f"https://github.com/sportsdataverse/sportsdataverse-data/releases/download/espn_womens_college_basketball_team_season_stats/team_season_stats_{season}.parquet"
        )
        if df is None:
            missing.append(season)
            continue
        frames.append(df)
    if missing:
        cli_warn("load_wbb_team_season_stats: no data for season(s) {missing} (skipped)".format(missing=missing))
    # diagonal: per-season release schemas can drift (columns added/dropped
    # over the years) -- union columns, null-fill gaps.
    out = pl.concat(frames, how="diagonal_relaxed") if frames else pl.DataFrame()
    return out.to_pandas(use_pyarrow_extension_array=True) if return_as_pandas else out


def load_wbb_player_crosswalk(seasons, return_as_pandas: bool = False):
    """Load wbb_crosswalk (sportsdataverse-data release).

    Source: https://github.com/sportsdataverse/sportsdataverse-data/releases/tag/wbb_crosswalk

    Args:
        seasons: an int or iterable of seasons (>= 2014).
        return_as_pandas: return a pandas DataFrame instead of polars.

    Returns:
        A polars (or pandas) DataFrame; seasons with no published asset are
        skipped with a warning rather than raising (404-safe).

        |col_name           |type    |
        |:------------------|:-------|
        |season             |Int32   |
        |espn_team_id       |Int32   |
        |team_abbreviation  |String  |
        |player_name        |String  |
        |espn_athlete_id    |String  |
        |espn_full_name     |String  |
        |espn_jersey        |String  |
        |espn_position      |String  |
        |fox_athlete_id     |String  |
        |fox_player         |String  |
        |fox_jersey         |String  |
        |fox_position_group |String  |
        |yahoo_player_id    |String  |
        |yahoo_player_name  |String  |
        |match_method       |String  |
        |match_confidence   |Float64 |
        |match_keys         |String  |

    Raises:
        SeasonNotFoundError: if a requested season is below 2014.

    Example:
        Quick start::

            load_wbb_player_crosswalk(seasons=2026)
    """
    frames, missing = [], []
    for season in _as_season_list(seasons):
        if int(season) < 2014:
            raise SeasonNotFoundError("season cannot be less than 2014")
        df = _read_release_parquet(
            f"https://github.com/sportsdataverse/sportsdataverse-data/releases/download/wbb_crosswalk/wbb_player_crosswalk_{season}.parquet"
        )
        if df is None:
            missing.append(season)
            continue
        frames.append(df)
    if missing:
        cli_warn("load_wbb_player_crosswalk: no data for season(s) {missing} (skipped)".format(missing=missing))
    # diagonal: per-season release schemas can drift (columns added/dropped
    # over the years) -- union columns, null-fill gaps.
    out = pl.concat(frames, how="diagonal_relaxed") if frames else pl.DataFrame()
    return out.to_pandas(use_pyarrow_extension_array=True) if return_as_pandas else out


def load_wbb_schedule_crosswalk(seasons, return_as_pandas: bool = False):
    """Load wbb_crosswalk (sportsdataverse-data release).

    Source: https://github.com/sportsdataverse/sportsdataverse-data/releases/tag/wbb_crosswalk

    Args:
        seasons: an int or iterable of seasons (>= 2026).
        return_as_pandas: return a pandas DataFrame instead of polars.

    Returns:
        A polars (or pandas) DataFrame; seasons with no published asset are
        skipped with a warning rather than raising (404-safe).

        |col_name          |type    |
        |:-----------------|:-------|
        |season            |Int32   |
        |game_date         |Date    |
        |home_espn_team_id |Int32   |
        |away_espn_team_id |Int32   |
        |espn_game_id      |String  |
        |bart_muid         |String  |
        |bart_team1        |String  |
        |bart_team2        |String  |
        |bart_winner       |String  |
        |fox_game_id       |String  |
        |yahoo_game_id     |String  |
        |match_method      |String  |
        |match_confidence  |Float64 |

    Raises:
        SeasonNotFoundError: if a requested season is below 2026.

    Example:
        Quick start::

            load_wbb_schedule_crosswalk(seasons=2026)
    """
    frames, missing = [], []
    for season in _as_season_list(seasons):
        if int(season) < 2026:
            raise SeasonNotFoundError("season cannot be less than 2026")
        df = _read_release_parquet(
            f"https://github.com/sportsdataverse/sportsdataverse-data/releases/download/wbb_crosswalk/wbb_schedule_crosswalk_{season}.parquet"
        )
        if df is None:
            missing.append(season)
            continue
        frames.append(df)
    if missing:
        cli_warn("load_wbb_schedule_crosswalk: no data for season(s) {missing} (skipped)".format(missing=missing))
    # diagonal: per-season release schemas can drift (columns added/dropped
    # over the years) -- union columns, null-fill gaps.
    out = pl.concat(frames, how="diagonal_relaxed") if frames else pl.DataFrame()
    return out.to_pandas(use_pyarrow_extension_array=True) if return_as_pandas else out


def load_wbb_team_crosswalk(seasons, return_as_pandas: bool = False):
    """Load wbb_crosswalk (sportsdataverse-data release).

    Source: https://github.com/sportsdataverse/sportsdataverse-data/releases/tag/wbb_crosswalk

    Args:
        seasons: an int or iterable of seasons (>= 2014).
        return_as_pandas: return a pandas DataFrame instead of polars.

    Returns:
        A polars (or pandas) DataFrame; seasons with no published asset are
        skipped with a warning rather than raising (404-safe).

        |col_name              |type    |
        |:---------------------|:-------|
        |season                |Int32   |
        |espn_team_id          |Int32   |
        |espn_abbreviation     |String  |
        |espn_display_name     |String  |
        |espn_short_name       |String  |
        |espn_location         |String  |
        |espn_mascot           |String  |
        |espn_conference       |String  |
        |fox_team_id           |String  |
        |fox_team_name         |String  |
        |fox_section           |String  |
        |bart_team             |String  |
        |bart_conf             |String  |
        |yahoo_team_id         |String  |
        |yahoo_team_name       |String  |
        |fox_match_confidence  |Float64 |
        |bart_match_confidence |Float64 |
        |match_method          |String  |

    Raises:
        SeasonNotFoundError: if a requested season is below 2014.

    Example:
        Quick start::

            load_wbb_team_crosswalk(seasons=2026)
    """
    frames, missing = [], []
    for season in _as_season_list(seasons):
        if int(season) < 2014:
            raise SeasonNotFoundError("season cannot be less than 2014")
        df = _read_release_parquet(
            f"https://github.com/sportsdataverse/sportsdataverse-data/releases/download/wbb_crosswalk/wbb_team_crosswalk_{season}.parquet"
        )
        if df is None:
            missing.append(season)
            continue
        frames.append(df)
    if missing:
        cli_warn("load_wbb_team_crosswalk: no data for season(s) {missing} (skipped)".format(missing=missing))
    # diagonal: per-season release schemas can drift (columns added/dropped
    # over the years) -- union columns, null-fill gaps.
    out = pl.concat(frames, how="diagonal_relaxed") if frames else pl.DataFrame()
    return out.to_pandas(use_pyarrow_extension_array=True) if return_as_pandas else out


def load_wbb_player_core(seasons, return_as_pandas: bool = False):
    """Load espn_womens_college_basketball_player_core (sportsdataverse-data release).

    Source: https://github.com/sportsdataverse/sportsdataverse-data/releases/tag/espn_womens_college_basketball_player_core

    Args:
        seasons: an int or iterable of seasons (>= 2004).
        return_as_pandas: return a pandas DataFrame instead of polars.

    Returns:
        A polars (or pandas) DataFrame; seasons with no published asset are
        skipped with a warning rather than raising (404-safe).

        |col_name              |type    |
        |:---------------------|:-------|
        |season                |Int32   |
        |athlete_id            |Int64   |
        |guid                  |String  |
        |uid                   |String  |
        |slug                  |String  |
        |type                  |String  |
        |first_name            |String  |
        |last_name             |String  |
        |full_name             |String  |
        |display_name          |String  |
        |short_name            |String  |
        |height                |Float64 |
        |display_height        |String  |
        |weight                |Float64 |
        |display_weight        |String  |
        |age                   |Int32   |
        |date_of_birth         |String  |
        |birth_city            |String  |
        |birth_state           |String  |
        |birth_country         |String  |
        |jersey                |String  |
        |position_id           |Int32   |
        |position_name         |String  |
        |position_abbreviation |String  |
        |position_display_name |String  |
        |college_id            |Int32   |
        |current_team_id       |Int32   |
        |headshot_href         |String  |
        |experience_years      |Int32   |
        |status_id             |Int32   |
        |status_name           |String  |
        |status_type           |String  |
        |draft_year            |Int32   |
        |draft_round           |Int32   |
        |draft_selection       |Int32   |
        |active                |Boolean |

    Raises:
        SeasonNotFoundError: if a requested season is below 2004.

    Example:
        Quick start::

            load_wbb_player_core(seasons=2025)
    """
    frames, missing = [], []
    for season in _as_season_list(seasons):
        if int(season) < 2004:
            raise SeasonNotFoundError("season cannot be less than 2004")
        df = _read_release_parquet(
            f"https://github.com/sportsdataverse/sportsdataverse-data/releases/download/espn_womens_college_basketball_player_core/player_core_{season}.parquet"
        )
        if df is None:
            missing.append(season)
            continue
        frames.append(df)
    if missing:
        cli_warn("load_wbb_player_core: no data for season(s) {missing} (skipped)".format(missing=missing))
    # diagonal: per-season release schemas can drift (columns added/dropped
    # over the years) -- union columns, null-fill gaps.
    out = pl.concat(frames, how="diagonal_relaxed") if frames else pl.DataFrame()
    return out.to_pandas(use_pyarrow_extension_array=True) if return_as_pandas else out


def load_ncaa_wbb_rapm(seasons, return_as_pandas: bool = False):
    """Load ncaa_wbb_rapm (sportsdataverse-data release).

    Source: https://github.com/sportsdataverse/sportsdataverse-data/releases/tag/ncaa_wbb_rapm

    Args:
        seasons: an int or iterable of seasons (>= 2011).
        return_as_pandas: return a pandas DataFrame instead of polars.

    Returns:
        A polars (or pandas) DataFrame; seasons with no published asset are
        skipped with a warning rather than raising (404-safe).

        |col_name  |type    |
        |:---------|:-------|
        |season    |Int32   |
        |player_id |String  |
        |person_id |String  |
        |player    |String  |
        |team      |String  |
        |orapm     |Float64 |
        |drapm     |Float64 |
        |rapm_net  |Float64 |
        |off_poss  |Int64   |
        |def_poss  |Int64   |
        |estimand  |String  |

    Raises:
        SeasonNotFoundError: if a requested season is below 2011.

    Example:
        Quick start::

            load_ncaa_wbb_rapm(seasons=2024)
    """
    frames, missing = [], []
    for season in _as_season_list(seasons):
        if int(season) < 2011:
            raise SeasonNotFoundError("season cannot be less than 2011")
        df = _read_release_parquet(
            f"https://github.com/sportsdataverse/sportsdataverse-data/releases/download/ncaa_wbb_rapm/ncaa_wbb_rapm_{season}.parquet"
        )
        if df is None:
            missing.append(season)
            continue
        frames.append(df)
    if missing:
        cli_warn("load_ncaa_wbb_rapm: no data for season(s) {missing} (skipped)".format(missing=missing))
    # diagonal: per-season release schemas can drift (columns added/dropped
    # over the years) -- union columns, null-fill gaps.
    out = pl.concat(frames, how="diagonal_relaxed") if frames else pl.DataFrame()
    return out.to_pandas(use_pyarrow_extension_array=True) if return_as_pandas else out


def load_ncaa_wbb_pbp(seasons, return_as_pandas: bool = False):
    """Load ncaa_wbb_pbp (sportsdataverse-data release).

    Source: https://github.com/sportsdataverse/sportsdataverse-data/releases/tag/ncaa_wbb_pbp

    Args:
        seasons: an int or iterable of seasons (>= 2010).
        return_as_pandas: return a pandas DataFrame instead of polars.

    Returns:
        A polars (or pandas) DataFrame; seasons with no published asset are
        skipped with a warning rather than raising (404-safe).

        |col_name                |type    |
        |:-----------------------|:-------|
        |game_date               |String  |
        |home                    |String  |
        |away                    |String  |
        |period                  |Int64   |
        |clock                   |String  |
        |game_time               |String  |
        |game_seconds            |Int64   |
        |home_score              |Int64   |
        |away_score              |Int64   |
        |event_team              |String  |
        |event_description       |String  |
        |player_1                |String  |
        |player_2                |String  |
        |event_type              |String  |
        |event_result            |String  |
        |shot_value              |Int64   |
        |event_length            |Int64   |
        |poss_num                |Int64   |
        |poss_team               |String  |
        |poss_length             |Int64   |
        |is_transition           |Boolean |
        |home_1                  |String  |
        |home_2                  |String  |
        |home_3                  |String  |
        |home_4                  |String  |
        |home_5                  |String  |
        |away_1                  |String  |
        |away_2                  |String  |
        |away_3                  |String  |
        |away_4                  |String  |
        |away_5                  |String  |
        |status                  |String  |
        |is_garbage_time         |Boolean |
        |sub_deviate             |Int64   |
        |contest_id              |String  |
        |home_ncaa_team_id       |String  |
        |home_espn_team_id       |String  |
        |away_ncaa_team_id       |String  |
        |away_espn_team_id       |String  |
        |event_team_ncaa_team_id |String  |
        |event_team_espn_team_id |String  |
        |poss_team_ncaa_team_id  |String  |
        |poss_team_espn_team_id  |String  |
        |player_1_id             |String  |
        |player_1_clean_name     |String  |
        |player_2_id             |String  |
        |player_2_clean_name     |String  |
        |home_1_player_id        |String  |
        |home_1_clean_name       |String  |
        |home_2_player_id        |String  |
        |home_2_clean_name       |String  |
        |home_3_player_id        |String  |
        |home_3_clean_name       |String  |
        |home_4_player_id        |String  |
        |home_4_clean_name       |String  |
        |home_5_player_id        |String  |
        |home_5_clean_name       |String  |
        |away_1_player_id        |String  |
        |away_1_clean_name       |String  |
        |away_2_player_id        |String  |
        |away_2_clean_name       |String  |
        |away_3_player_id        |String  |
        |away_3_clean_name       |String  |
        |away_4_player_id        |String  |
        |away_4_clean_name       |String  |
        |away_5_player_id        |String  |
        |away_5_clean_name       |String  |
        |espn_game_id            |String  |
        |is_fastbreak            |Boolean |
        |is_from_turnover        |Boolean |
        |is_paint                |Boolean |
        |is_second_chance        |Boolean |
        |assist_player           |String  |
        |ft_number               |Int64   |
        |ft_attempts             |Int64   |
        |foul_class              |String  |
        |is_shooting_foul        |Boolean |
        |is_looseball_foul       |Boolean |
        |is_one_and_one          |Boolean |
        |is_flagrant             |Boolean |
        |foul_tech_class         |String  |
        |ft_awarded              |Int64   |
        |turnover_type           |String  |
        |is_team_turnover        |Boolean |
        |timeout_type            |String  |
        |challenge_outcome       |String  |
        |season                  |Int64   |

    Raises:
        SeasonNotFoundError: if a requested season is below 2010.

    Example:
        Quick start::

            load_ncaa_wbb_pbp(seasons=2024)
    """
    frames, missing = [], []
    for season in _as_season_list(seasons):
        if int(season) < 2010:
            raise SeasonNotFoundError("season cannot be less than 2010")
        df = _read_release_parquet(
            f"https://github.com/sportsdataverse/sportsdataverse-data/releases/download/ncaa_wbb_pbp/ncaa_wbb_pbp_{season}.parquet"
        )
        if df is None:
            missing.append(season)
            continue
        frames.append(df)
    if missing:
        cli_warn("load_ncaa_wbb_pbp: no data for season(s) {missing} (skipped)".format(missing=missing))
    # diagonal: per-season release schemas can drift (columns added/dropped
    # over the years) -- union columns, null-fill gaps.
    out = pl.concat(frames, how="diagonal_relaxed") if frames else pl.DataFrame()
    return out.to_pandas(use_pyarrow_extension_array=True) if return_as_pandas else out


def load_ncaa_wbb_schedule(seasons, return_as_pandas: bool = False):
    """Load ncaa_wbb_schedule (sportsdataverse-data release).

    Source: https://github.com/sportsdataverse/sportsdataverse-data/releases/tag/ncaa_wbb_schedule

    Args:
        seasons: an int or iterable of seasons (>= 2010).
        return_as_pandas: return a pandas DataFrame instead of polars.

    Returns:
        A polars (or pandas) DataFrame; seasons with no published asset are
        skipped with a warning rather than raising (404-safe).

        |col_name   |type   |
        |:----------|:------|
        |contest_id |String |
        |game_date  |String |
        |home       |String |
        |away       |String |
        |home_score |Int64  |
        |away_score |Int64  |
        |season     |Int64  |

    Raises:
        SeasonNotFoundError: if a requested season is below 2010.

    Example:
        Quick start::

            load_ncaa_wbb_schedule(seasons=2024)
    """
    frames, missing = [], []
    for season in _as_season_list(seasons):
        if int(season) < 2010:
            raise SeasonNotFoundError("season cannot be less than 2010")
        df = _read_release_parquet(
            f"https://github.com/sportsdataverse/sportsdataverse-data/releases/download/ncaa_wbb_schedule/ncaa_wbb_schedule_{season}.parquet"
        )
        if df is None:
            missing.append(season)
            continue
        frames.append(df)
    if missing:
        cli_warn("load_ncaa_wbb_schedule: no data for season(s) {missing} (skipped)".format(missing=missing))
    # diagonal: per-season release schemas can drift (columns added/dropped
    # over the years) -- union columns, null-fill gaps.
    out = pl.concat(frames, how="diagonal_relaxed") if frames else pl.DataFrame()
    return out.to_pandas(use_pyarrow_extension_array=True) if return_as_pandas else out


def load_ncaa_wbb_player_box(seasons, return_as_pandas: bool = False):
    """Load ncaa_wbb_player_box (sportsdataverse-data release).

    Source: https://github.com/sportsdataverse/sportsdataverse-data/releases/tag/ncaa_wbb_player_box

    Args:
        seasons: an int or iterable of seasons (>= 2010).
        return_as_pandas: return a pandas DataFrame instead of polars.

    Returns:
        A polars (or pandas) DataFrame; seasons with no published asset are
        skipped with a warning rather than raising (404-safe).

        |col_name          |type    |
        |:-----------------|:-------|
        |game_date         |String  |
        |home              |String  |
        |away              |String  |
        |team              |String  |
        |player            |String  |
        |mins              |Float64 |
        |o_poss            |Float64 |
        |pts               |Float64 |
        |orb               |Float64 |
        |drb               |Float64 |
        |ast               |Float64 |
        |stl               |Float64 |
        |blk               |Float64 |
        |tov               |Float64 |
        |pf                |Float64 |
        |ts_pct            |Float64 |
        |efg_pct           |Float64 |
        |fgm               |Float64 |
        |fga               |Float64 |
        |fg_pct            |Float64 |
        |tpm               |Float64 |
        |tpa               |Float64 |
        |tp_pct            |Float64 |
        |ftm               |Float64 |
        |fta               |Float64 |
        |ft_pct            |Float64 |
        |rimm              |Float64 |
        |rima              |Float64 |
        |rim_pct           |Float64 |
        |midm              |Float64 |
        |mida              |Float64 |
        |mid_pct           |Float64 |
        |pbackm            |Float64 |
        |pbacka            |Float64 |
        |pback_pct         |Float64 |
        |blk_rim           |Float64 |
        |blk_mid           |Float64 |
        |blk_three         |Float64 |
        |pct_fga_trans     |Float64 |
        |pct_tpa_trans     |Float64 |
        |pct_rima_trans    |Float64 |
        |pct_fgm_trans     |Float64 |
        |pct_tpm_trans     |Float64 |
        |pct_rimm_trans    |Float64 |
        |pct_fgm_ast       |Float64 |
        |pct_tpm_ast       |Float64 |
        |pct_rimm_ast      |Float64 |
        |pts_trans         |Float64 |
        |orb_trans         |Float64 |
        |drb_trans         |Float64 |
        |ast_trans         |Float64 |
        |stl_trans         |Float64 |
        |blk_trans         |Float64 |
        |tov_trans         |Float64 |
        |ts_pct_trans      |Float64 |
        |efg_pct_trans     |Float64 |
        |fgm_trans         |Float64 |
        |fga_trans         |Float64 |
        |fg_pct_trans      |Float64 |
        |tpm_trans         |Float64 |
        |tpa_trans         |Float64 |
        |tp_pct_trans      |Float64 |
        |ftm_trans         |Float64 |
        |fta_trans         |Float64 |
        |ft_pct_trans      |Float64 |
        |rimm_trans        |Float64 |
        |rima_trans        |Float64 |
        |rim_pct_trans     |Float64 |
        |midm_trans        |Float64 |
        |mida_trans        |Float64 |
        |mid_pct_trans     |Float64 |
        |pts_half          |Float64 |
        |orb_half          |Float64 |
        |drb_half          |Float64 |
        |ast_half          |Float64 |
        |stl_half          |Float64 |
        |blk_half          |Float64 |
        |tov_half          |Float64 |
        |ts_pct_half       |Float64 |
        |efg_pct_half      |Float64 |
        |fgm_half          |Float64 |
        |fga_half          |Float64 |
        |fg_pct_half       |Float64 |
        |tpm_half          |Float64 |
        |tpa_half          |Float64 |
        |tp_pct_half       |Float64 |
        |ftm_half          |Float64 |
        |fta_half          |Float64 |
        |ft_pct_half       |Float64 |
        |rimm_half         |Float64 |
        |rima_half         |Float64 |
        |rim_pct_half      |Float64 |
        |midm_half         |Float64 |
        |mida_half         |Float64 |
        |mid_pct_half      |Float64 |
        |pts_ast           |Float64 |
        |fgm_ast           |Float64 |
        |tpm_ast           |Float64 |
        |rimm_ast          |Float64 |
        |midm_ast          |Float64 |
        |pts_unast         |Float64 |
        |efg_pct_unast     |Float64 |
        |fgm_unast         |Float64 |
        |fga_unast         |Float64 |
        |fg_pct_unast      |Float64 |
        |tpm_unast         |Float64 |
        |tpa_unast         |Float64 |
        |tp_pct_unast      |Float64 |
        |rimm_unast        |Float64 |
        |rima_unast        |Float64 |
        |rim_pct_unast     |Float64 |
        |midm_unast        |Float64 |
        |mida_unast        |Float64 |
        |mid_pct_unast     |Float64 |
        |contest_id        |String  |
        |home_ncaa_team_id |String  |
        |home_espn_team_id |String  |
        |away_ncaa_team_id |String  |
        |away_espn_team_id |String  |
        |team_ncaa_team_id |String  |
        |team_espn_team_id |String  |
        |player_id         |String  |
        |clean_name        |String  |
        |espn_game_id      |String  |
        |season            |Int64   |

    Raises:
        SeasonNotFoundError: if a requested season is below 2010.

    Example:
        Quick start::

            load_ncaa_wbb_player_box(seasons=2024)
    """
    frames, missing = [], []
    for season in _as_season_list(seasons):
        if int(season) < 2010:
            raise SeasonNotFoundError("season cannot be less than 2010")
        df = _read_release_parquet(
            f"https://github.com/sportsdataverse/sportsdataverse-data/releases/download/ncaa_wbb_player_box/ncaa_wbb_player_box_{season}.parquet"
        )
        if df is None:
            missing.append(season)
            continue
        frames.append(df)
    if missing:
        cli_warn("load_ncaa_wbb_player_box: no data for season(s) {missing} (skipped)".format(missing=missing))
    # diagonal: per-season release schemas can drift (columns added/dropped
    # over the years) -- union columns, null-fill gaps.
    out = pl.concat(frames, how="diagonal_relaxed") if frames else pl.DataFrame()
    return out.to_pandas(use_pyarrow_extension_array=True) if return_as_pandas else out


def load_ncaa_wbb_team_box(seasons, return_as_pandas: bool = False):
    """Load ncaa_wbb_team_box (sportsdataverse-data release).

    Source: https://github.com/sportsdataverse/sportsdataverse-data/releases/tag/ncaa_wbb_team_box

    Args:
        seasons: an int or iterable of seasons (>= 2010).
        return_as_pandas: return a pandas DataFrame instead of polars.

    Returns:
        A polars (or pandas) DataFrame; seasons with no published asset are
        skipped with a warning rather than raising (404-safe).

        |col_name          |type    |
        |:-----------------|:-------|
        |home              |String  |
        |away              |String  |
        |team              |String  |
        |mins              |Float64 |
        |o_mins            |Float64 |
        |d_mins            |Float64 |
        |o_poss            |Float64 |
        |d_poss            |Float64 |
        |ortg              |Float64 |
        |drtg              |Float64 |
        |netrtg            |Float64 |
        |pts               |Float64 |
        |d_pts             |Float64 |
        |fga               |Float64 |
        |d_fga             |Float64 |
        |fgm               |Float64 |
        |d_fgm             |Float64 |
        |tpa               |Float64 |
        |d_tpa             |Float64 |
        |tpm               |Float64 |
        |d_tpm             |Float64 |
        |fta               |Float64 |
        |d_fta             |Float64 |
        |ftm               |Float64 |
        |d_ftm             |Float64 |
        |rima              |Float64 |
        |d_rima            |Float64 |
        |rimm              |Float64 |
        |d_rimm            |Float64 |
        |orb               |Float64 |
        |d_orb             |Float64 |
        |drb               |Float64 |
        |d_drb             |Float64 |
        |blk               |Float64 |
        |d_blk             |Float64 |
        |to                |Float64 |
        |d_to              |Float64 |
        |ast               |Float64 |
        |d_ast             |Float64 |
        |e_poss            |Float64 |
        |fg_pct            |Float64 |
        |d_fg_pct          |Float64 |
        |tpp               |Float64 |
        |d_tpp             |Float64 |
        |ftp               |Float64 |
        |d_ftp             |Float64 |
        |efg_pct           |Float64 |
        |d_efg_pct         |Float64 |
        |ts_pct            |Float64 |
        |d_ts_pct          |Float64 |
        |rim_pct           |Float64 |
        |d_rim_pct         |Float64 |
        |mid_pct           |Float64 |
        |d_mid_pct         |Float64 |
        |tp_rate           |Float64 |
        |d_tp_rate         |Float64 |
        |rim_rate          |Float64 |
        |d_rim_rate        |Float64 |
        |mid_rate          |Float64 |
        |d_mid_rate        |Float64 |
        |ft_rate           |Float64 |
        |d_ft_rate         |Float64 |
        |ast_rate          |Float64 |
        |d_ast_rate        |Float64 |
        |to_rate           |Float64 |
        |d_to_rate         |Float64 |
        |blk_rate          |Float64 |
        |o_blk_rate        |Float64 |
        |orb_pct           |Float64 |
        |drb_pct           |Float64 |
        |time_per_poss     |Float64 |
        |d_time_per_poss   |Float64 |
        |contest_id        |String  |
        |home_ncaa_team_id |String  |
        |home_espn_team_id |String  |
        |away_ncaa_team_id |String  |
        |away_espn_team_id |String  |
        |team_ncaa_team_id |String  |
        |team_espn_team_id |String  |
        |espn_game_id      |String  |
        |season            |Int64   |

    Raises:
        SeasonNotFoundError: if a requested season is below 2010.

    Example:
        Quick start::

            load_ncaa_wbb_team_box(seasons=2024)
    """
    frames, missing = [], []
    for season in _as_season_list(seasons):
        if int(season) < 2010:
            raise SeasonNotFoundError("season cannot be less than 2010")
        df = _read_release_parquet(
            f"https://github.com/sportsdataverse/sportsdataverse-data/releases/download/ncaa_wbb_team_box/ncaa_wbb_team_box_{season}.parquet"
        )
        if df is None:
            missing.append(season)
            continue
        frames.append(df)
    if missing:
        cli_warn("load_ncaa_wbb_team_box: no data for season(s) {missing} (skipped)".format(missing=missing))
    # diagonal: per-season release schemas can drift (columns added/dropped
    # over the years) -- union columns, null-fill gaps.
    out = pl.concat(frames, how="diagonal_relaxed") if frames else pl.DataFrame()
    return out.to_pandas(use_pyarrow_extension_array=True) if return_as_pandas else out


def load_ncaa_wbb_rosters(seasons, return_as_pandas: bool = False):
    """Load ncaa_wbb_rosters (sportsdataverse-data release).

    Source: https://github.com/sportsdataverse/sportsdataverse-data/releases/tag/ncaa_wbb_rosters

    Args:
        seasons: an int or iterable of seasons (>= 2010).
        return_as_pandas: return a pandas DataFrame instead of polars.

    Returns:
        A polars (or pandas) DataFrame; seasons with no published asset are
        skipped with a warning rather than raising (404-safe).

        |col_name |type   |
        |:--------|:------|
        |season   |Int64  |
        |team     |String |
        |player   |String |
        |games    |Int64  |

    Raises:
        SeasonNotFoundError: if a requested season is below 2010.

    Example:
        Quick start::

            load_ncaa_wbb_rosters(seasons=2024)
    """
    frames, missing = [], []
    for season in _as_season_list(seasons):
        if int(season) < 2010:
            raise SeasonNotFoundError("season cannot be less than 2010")
        df = _read_release_parquet(
            f"https://github.com/sportsdataverse/sportsdataverse-data/releases/download/ncaa_wbb_rosters/ncaa_wbb_rosters_{season}.parquet"
        )
        if df is None:
            missing.append(season)
            continue
        frames.append(df)
    if missing:
        cli_warn("load_ncaa_wbb_rosters: no data for season(s) {missing} (skipped)".format(missing=missing))
    # diagonal: per-season release schemas can drift (columns added/dropped
    # over the years) -- union columns, null-fill gaps.
    out = pl.concat(frames, how="diagonal_relaxed") if frames else pl.DataFrame()
    return out.to_pandas(use_pyarrow_extension_array=True) if return_as_pandas else out


def load_ncaa_wbb_team_rosters(seasons, return_as_pandas: bool = False):
    """Load ncaa_wbb_team_rosters (sportsdataverse-data release).

    Source: https://github.com/sportsdataverse/sportsdataverse-data/releases/tag/ncaa_wbb_team_rosters

    Args:
        seasons: an int or iterable of seasons (>= 2010).
        return_as_pandas: return a pandas DataFrame instead of polars.

    Returns:
        A polars (or pandas) DataFrame; seasons with no published asset are
        skipped with a warning rather than raising (404-safe).

        |col_name    |type   |
        |:-----------|:------|
        |season      |Int64  |
        |team_id     |String |
        |team        |String |
        |player_id   |String |
        |player      |String |
        |clean_name  |String |
        |name        |String |
        |jersey      |String |
        |class       |String |
        |position    |String |
        |height      |String |
        |ht_inches   |Int64  |
        |hometown    |String |
        |high_school |String |
        |gp          |String |
        |gs          |String |

    Raises:
        SeasonNotFoundError: if a requested season is below 2010.

    Example:
        Quick start::

            load_ncaa_wbb_team_rosters(seasons=2024)
    """
    frames, missing = [], []
    for season in _as_season_list(seasons):
        if int(season) < 2010:
            raise SeasonNotFoundError("season cannot be less than 2010")
        df = _read_release_parquet(
            f"https://github.com/sportsdataverse/sportsdataverse-data/releases/download/ncaa_wbb_team_rosters/ncaa_wbb_team_rosters_{season}.parquet"
        )
        if df is None:
            missing.append(season)
            continue
        frames.append(df)
    if missing:
        cli_warn("load_ncaa_wbb_team_rosters: no data for season(s) {missing} (skipped)".format(missing=missing))
    # diagonal: per-season release schemas can drift (columns added/dropped
    # over the years) -- union columns, null-fill gaps.
    out = pl.concat(frames, how="diagonal_relaxed") if frames else pl.DataFrame()
    return out.to_pandas(use_pyarrow_extension_array=True) if return_as_pandas else out


def load_ncaa_wbb_team_ids(seasons, return_as_pandas: bool = False):
    """Load ncaa_wbb_team_ids (sportsdataverse-data release).

    Source: https://github.com/sportsdataverse/sportsdataverse-data/releases/tag/ncaa_wbb_team_ids

    Args:
        seasons: an int or iterable of seasons (>= 2010).
        return_as_pandas: return a pandas DataFrame instead of polars.

    Returns:
        A polars (or pandas) DataFrame; seasons with no published asset are
        skipped with a warning rather than raising (404-safe).

        |col_name   |type   |
        |:----------|:------|
        |team       |String |
        |conference |String |
        |id         |String |
        |season     |Int64  |

    Raises:
        SeasonNotFoundError: if a requested season is below 2010.

    Example:
        Quick start::

            load_ncaa_wbb_team_ids(seasons=2024)
    """
    frames, missing = [], []
    for season in _as_season_list(seasons):
        if int(season) < 2010:
            raise SeasonNotFoundError("season cannot be less than 2010")
        df = _read_release_parquet(
            f"https://github.com/sportsdataverse/sportsdataverse-data/releases/download/ncaa_wbb_team_ids/ncaa_wbb_team_ids_{season}.parquet"
        )
        if df is None:
            missing.append(season)
            continue
        frames.append(df)
    if missing:
        cli_warn("load_ncaa_wbb_team_ids: no data for season(s) {missing} (skipped)".format(missing=missing))
    # diagonal: per-season release schemas can drift (columns added/dropped
    # over the years) -- union columns, null-fill gaps.
    out = pl.concat(frames, how="diagonal_relaxed") if frames else pl.DataFrame()
    return out.to_pandas(use_pyarrow_extension_array=True) if return_as_pandas else out


def load_ncaa_wbb_possessions(seasons, return_as_pandas: bool = False):
    """Load ncaa_wbb_possessions (sportsdataverse-data release).

    Source: https://github.com/sportsdataverse/sportsdataverse-data/releases/tag/ncaa_wbb_possessions

    Args:
        seasons: an int or iterable of seasons (>= 2010).
        return_as_pandas: return a pandas DataFrame instead of polars.

    Returns:
        A polars (or pandas) DataFrame; seasons with no published asset are
        skipped with a warning rather than raising (404-safe).

        |col_name               |type   |
        |:----------------------|:------|
        |game_date              |String |
        |home                   |String |
        |away                   |String |
        |period                 |Int64  |
        |poss_num               |Int64  |
        |poss_team              |String |
        |home_1                 |String |
        |home_2                 |String |
        |home_3                 |String |
        |home_4                 |String |
        |home_5                 |String |
        |away_1                 |String |
        |away_2                 |String |
        |away_3                 |String |
        |away_4                 |String |
        |away_5                 |String |
        |home_score             |Int64  |
        |away_score             |Int64  |
        |pts                    |Int64  |
        |is_assisted            |Int64  |
        |is_transition          |Int64  |
        |is_garbage_time        |Int64  |
        |start_event_type       |String |
        |first_shot_time        |Int64  |
        |first_shot_type        |String |
        |last_event_time        |Int64  |
        |last_event_type        |String |
        |contest_id             |String |
        |home_ncaa_team_id      |String |
        |home_espn_team_id      |String |
        |away_ncaa_team_id      |String |
        |away_espn_team_id      |String |
        |poss_team_ncaa_team_id |String |
        |poss_team_espn_team_id |String |
        |home_1_player_id       |String |
        |home_1_clean_name      |String |
        |home_2_player_id       |String |
        |home_2_clean_name      |String |
        |home_3_player_id       |String |
        |home_3_clean_name      |String |
        |home_4_player_id       |String |
        |home_4_clean_name      |String |
        |home_5_player_id       |String |
        |home_5_clean_name      |String |
        |away_1_player_id       |String |
        |away_1_clean_name      |String |
        |away_2_player_id       |String |
        |away_2_clean_name      |String |
        |away_3_player_id       |String |
        |away_3_clean_name      |String |
        |away_4_player_id       |String |
        |away_4_clean_name      |String |
        |away_5_player_id       |String |
        |away_5_clean_name      |String |
        |espn_game_id           |String |
        |season                 |Int64  |

    Raises:
        SeasonNotFoundError: if a requested season is below 2010.

    Example:
        Quick start::

            load_ncaa_wbb_possessions(seasons=2024)
    """
    frames, missing = [], []
    for season in _as_season_list(seasons):
        if int(season) < 2010:
            raise SeasonNotFoundError("season cannot be less than 2010")
        df = _read_release_parquet(
            f"https://github.com/sportsdataverse/sportsdataverse-data/releases/download/ncaa_wbb_possessions/ncaa_wbb_possessions_{season}.parquet"
        )
        if df is None:
            missing.append(season)
            continue
        frames.append(df)
    if missing:
        cli_warn("load_ncaa_wbb_possessions: no data for season(s) {missing} (skipped)".format(missing=missing))
    # diagonal: per-season release schemas can drift (columns added/dropped
    # over the years) -- union columns, null-fill gaps.
    out = pl.concat(frames, how="diagonal_relaxed") if frames else pl.DataFrame()
    return out.to_pandas(use_pyarrow_extension_array=True) if return_as_pandas else out


def load_ncaa_wbb_lineups(seasons, return_as_pandas: bool = False):
    """Load ncaa_wbb_lineups (sportsdataverse-data release).

    Source: https://github.com/sportsdataverse/sportsdataverse-data/releases/tag/ncaa_wbb_lineups

    Args:
        seasons: an int or iterable of seasons (>= 2010).
        return_as_pandas: return a pandas DataFrame instead of polars.

    Returns:
        A polars (or pandas) DataFrame; seasons with no published asset are
        skipped with a warning rather than raising (404-safe).

        |col_name           |type    |
        |:------------------|:-------|
        |lineup_key         |String  |
        |date               |String  |
        |location_type      |String  |
        |team               |String  |
        |team_year          |Int64   |
        |opponent           |String  |
        |lineup_id          |String  |
        |start_min          |Float64 |
        |end_min            |Float64 |
        |duration_mins      |Float64 |
        |player_1           |String  |
        |player_2           |String  |
        |player_3           |String  |
        |player_4           |String  |
        |player_5           |String  |
        |players_in         |String  |
        |players_out        |String  |
        |start_scored       |Int64   |
        |start_allowed      |Int64   |
        |end_scored         |Int64   |
        |end_allowed        |Int64   |
        |start_diff         |Int64   |
        |end_diff           |Int64   |
        |player_count_error |Null    |
        |poss               |Int64   |
        |pts                |Int64   |
        |plus_minus         |Int64   |
        |fga                |Int64   |
        |fgm                |Int64   |
        |rima               |Int64   |
        |rimm               |Int64   |
        |rim_ast            |Int64   |
        |mida               |Int64   |
        |midm               |Int64   |
        |mid_ast            |Int64   |
        |fg2a               |Int64   |
        |fg2m               |Int64   |
        |tpa                |Int64   |
        |tpm                |Int64   |
        |tp_ast             |Int64   |
        |fta                |Int64   |
        |ftm                |Int64   |
        |orb                |Int64   |
        |drb                |Int64   |
        |to                 |Int64   |
        |stl                |Int64   |
        |blk                |Int64   |
        |ast                |Int64   |
        |foul               |Int64   |
        |opp_poss           |Int64   |
        |opp_pts            |Int64   |
        |opp_plus_minus     |Int64   |
        |opp_fga            |Int64   |
        |opp_fgm            |Int64   |
        |opp_rima           |Int64   |
        |opp_rimm           |Int64   |
        |opp_rim_ast        |Int64   |
        |opp_mida           |Int64   |
        |opp_midm           |Int64   |
        |opp_mid_ast        |Int64   |
        |opp_fg2a           |Int64   |
        |opp_fg2m           |Int64   |
        |opp_tpa            |Int64   |
        |opp_tpm            |Int64   |
        |opp_tp_ast         |Int64   |
        |opp_fta            |Int64   |
        |opp_ftm            |Int64   |
        |opp_orb            |Int64   |
        |opp_drb            |Int64   |
        |opp_to             |Int64   |
        |opp_stl            |Int64   |
        |opp_blk            |Int64   |
        |opp_ast            |Int64   |
        |opp_foul           |Int64   |
        |stint_num          |Int64   |
        |contest_id         |String  |
        |season             |Int64   |

    Raises:
        SeasonNotFoundError: if a requested season is below 2010.

    Example:
        Quick start::

            load_ncaa_wbb_lineups(seasons=2024)
    """
    frames, missing = [], []
    for season in _as_season_list(seasons):
        if int(season) < 2010:
            raise SeasonNotFoundError("season cannot be less than 2010")
        df = _read_release_parquet(
            f"https://github.com/sportsdataverse/sportsdataverse-data/releases/download/ncaa_wbb_lineups/ncaa_wbb_lineups_{season}.parquet"
        )
        if df is None:
            missing.append(season)
            continue
        frames.append(df)
    if missing:
        cli_warn("load_ncaa_wbb_lineups: no data for season(s) {missing} (skipped)".format(missing=missing))
    # diagonal: per-season release schemas can drift (columns added/dropped
    # over the years) -- union columns, null-fill gaps.
    out = pl.concat(frames, how="diagonal_relaxed") if frames else pl.DataFrame()
    return out.to_pandas(use_pyarrow_extension_array=True) if return_as_pandas else out


def load_ncaa_wbb_matchup_stints(seasons, return_as_pandas: bool = False):
    """Load ncaa_wbb_matchup_stints (sportsdataverse-data release).

    Source: https://github.com/sportsdataverse/sportsdataverse-data/releases/tag/ncaa_wbb_matchup_stints

    Args:
        seasons: an int or iterable of seasons (>= 2010).
        return_as_pandas: return a pandas DataFrame instead of polars.

    Returns:
        A polars (or pandas) DataFrame; seasons with no published asset are
        skipped with a warning rather than raising (404-safe).

        |col_name         |type   |
        |:----------------|:------|
        |contest_id       |String |
        |season           |Int64  |
        |game_date        |String |
        |home             |String |
        |away             |String |
        |game_stint_num   |Int64  |
        |period           |Int64  |
        |start_seconds    |Int64  |
        |end_seconds      |Int64  |
        |duration_seconds |Int64  |
        |matchup_key      |String |
        |home_lineup_key  |String |
        |away_lineup_key  |String |
        |home_lineup      |String |
        |away_lineup      |String |
        |end_home_score   |Int64  |
        |end_away_score   |Int64  |
        |n_events         |Int64  |
        |n_possessions    |Int64  |
        |start_home_score |Int64  |
        |start_away_score |Int64  |
        |home_pts         |Int64  |
        |away_pts         |Int64  |
        |home_1           |String |
        |home_2           |String |
        |home_3           |String |
        |home_4           |String |
        |home_5           |String |
        |away_1           |String |
        |away_2           |String |
        |away_3           |String |
        |away_4           |String |
        |away_5           |String |

    Raises:
        SeasonNotFoundError: if a requested season is below 2010.

    Example:
        Quick start::

            load_ncaa_wbb_matchup_stints(seasons=2024)
    """
    frames, missing = [], []
    for season in _as_season_list(seasons):
        if int(season) < 2010:
            raise SeasonNotFoundError("season cannot be less than 2010")
        df = _read_release_parquet(
            f"https://github.com/sportsdataverse/sportsdataverse-data/releases/download/ncaa_wbb_matchup_stints/ncaa_wbb_matchup_stints_{season}.parquet"
        )
        if df is None:
            missing.append(season)
            continue
        frames.append(df)
    if missing:
        cli_warn("load_ncaa_wbb_matchup_stints: no data for season(s) {missing} (skipped)".format(missing=missing))
    # diagonal: per-season release schemas can drift (columns added/dropped
    # over the years) -- union columns, null-fill gaps.
    out = pl.concat(frames, how="diagonal_relaxed") if frames else pl.DataFrame()
    return out.to_pandas(use_pyarrow_extension_array=True) if return_as_pandas else out


def load_ncaa_wbb_shots(seasons, return_as_pandas: bool = False):
    """Load ncaa_wbb_shots (sportsdataverse-data release).

    Source: https://github.com/sportsdataverse/sportsdataverse-data/releases/tag/ncaa_wbb_shots

    Args:
        seasons: an int or iterable of seasons (>= 2019).
        return_as_pandas: return a pandas DataFrame instead of polars.

    Returns:
        A polars (or pandas) DataFrame; seasons with no published asset are
        skipped with a warning rather than raising (404-safe).

        |col_name           |type    |
        |:------------------|:-------|
        |season             |Int64   |
        |team_id            |String  |
        |shooter_id         |String  |
        |shot_x             |Float64 |
        |shot_y             |Float64 |
        |dist_ft            |Float64 |
        |shot_zone          |String  |
        |shot_type          |String  |
        |made               |Boolean |
        |point_value        |Int64   |
        |period             |Null    |
        |sec_left           |Null    |
        |source             |String  |
        |contest_id         |String  |
        |ncaa_team_id       |String  |
        |espn_team_id       |String  |
        |shooter_player_id  |String  |
        |shooter_clean_name |String  |
        |espn_game_id       |String  |

    Raises:
        SeasonNotFoundError: if a requested season is below 2019.

    Example:
        Quick start::

            load_ncaa_wbb_shots(seasons=2024)
    """
    frames, missing = [], []
    for season in _as_season_list(seasons):
        if int(season) < 2019:
            raise SeasonNotFoundError("season cannot be less than 2019")
        df = _read_release_parquet(
            f"https://github.com/sportsdataverse/sportsdataverse-data/releases/download/ncaa_wbb_shots/ncaa_wbb_shots_{season}.parquet"
        )
        if df is None:
            missing.append(season)
            continue
        frames.append(df)
    if missing:
        cli_warn("load_ncaa_wbb_shots: no data for season(s) {missing} (skipped)".format(missing=missing))
    # diagonal: per-season release schemas can drift (columns added/dropped
    # over the years) -- union columns, null-fill gaps.
    out = pl.concat(frames, how="diagonal_relaxed") if frames else pl.DataFrame()
    return out.to_pandas(use_pyarrow_extension_array=True) if return_as_pandas else out


def load_ncaa_wbb_rapm_within_team(seasons, return_as_pandas: bool = False):
    """Load ncaa_wbb_rapm_within_team (sportsdataverse-data release).

    Source: https://github.com/sportsdataverse/sportsdataverse-data/releases/tag/ncaa_wbb_rapm_within_team

    Args:
        seasons: an int or iterable of seasons (>= 2010).
        return_as_pandas: return a pandas DataFrame instead of polars.

    Returns:
        A polars (or pandas) DataFrame; seasons with no published asset are
        skipped with a warning rather than raising (404-safe).

        |col_name      |type    |
        |:-------------|:-------|
        |team          |String  |
        |player_code   |String  |
        |rapm_off      |Float64 |
        |rapm_def      |Float64 |
        |team_off_poss |Float64 |
        |num_players   |Int64   |
        |rapm_net      |Float64 |
        |season        |Int32   |
        |player_id     |String  |
        |team_id       |String  |
        |person_id     |String  |

    Raises:
        SeasonNotFoundError: if a requested season is below 2010.

    Example:
        Quick start::

            load_ncaa_wbb_rapm_within_team(seasons=2024)
    """
    frames, missing = [], []
    for season in _as_season_list(seasons):
        if int(season) < 2010:
            raise SeasonNotFoundError("season cannot be less than 2010")
        df = _read_release_parquet(
            f"https://github.com/sportsdataverse/sportsdataverse-data/releases/download/ncaa_wbb_rapm_within_team/ncaa_wbb_rapm_within_team_{season}.parquet"
        )
        if df is None:
            missing.append(season)
            continue
        frames.append(df)
    if missing:
        cli_warn("load_ncaa_wbb_rapm_within_team: no data for season(s) {missing} (skipped)".format(missing=missing))
    # diagonal: per-season release schemas can drift (columns added/dropped
    # over the years) -- union columns, null-fill gaps.
    out = pl.concat(frames, how="diagonal_relaxed") if frames else pl.DataFrame()
    return out.to_pandas(use_pyarrow_extension_array=True) if return_as_pandas else out
