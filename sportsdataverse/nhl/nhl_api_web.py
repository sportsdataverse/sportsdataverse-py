# GENERATED by tools/codegen/generate.py -- DO NOT EDIT.
# Source: tools/codegen/endpoints/nhl_api_web.yaml
"""nhl_api_web wrappers (host https://api-web.nhle.com). Generated."""

from __future__ import annotations

from typing import TYPE_CHECKING, Dict, List, Optional, Union  # noqa: F401

from sportsdataverse._codegen_runtime import _get, format_nhl_season
from sportsdataverse.nhl.nhl_api_web_parsers import (
    parse_nhl_web_boxscore,
    parse_nhl_web_club_schedule,
    parse_nhl_web_club_stats,
    parse_nhl_web_draft_picks,
    parse_nhl_web_draft_rankings,
    parse_nhl_web_landing,
    parse_nhl_web_leaders,
    parse_nhl_web_pbp,
    parse_nhl_web_player_game_log,
    parse_nhl_web_player_landing,
    parse_nhl_web_player_spotlight,
    parse_nhl_web_playoff_series,
    parse_nhl_web_right_rail,
    parse_nhl_web_roster,
    parse_nhl_web_schedule,
    parse_nhl_web_score,
    parse_nhl_web_standings,
    parse_nhl_web_standings_season,
)

if TYPE_CHECKING:  # pragma: no cover -- annotation-only imports (PEP 563 defers eval)
    import pandas as pd
    import polars as pl

__all__ = [
    "nhl_web_pbp",
    "nhl_boxscore",
    "nhl_landing",
    "nhl_right_rail",
    "nhl_web_schedule",
    "nhl_score",
    "nhl_schedule_calendar",
    "nhl_playoff_series",
    "nhl_standings",
    "nhl_standings_season",
    "nhl_club_schedule_season",
    "nhl_club_schedule_month",
    "nhl_club_schedule_week",
    "nhl_club_stats",
    "nhl_club_stats_season",
    "nhl_roster",
    "nhl_roster_season",
    "nhl_player_landing",
    "nhl_player_game_log",
    "nhl_player_spotlight",
    "nhl_skater_leaders",
    "nhl_goalie_leaders",
    "nhl_draft_picks",
    "nhl_draft_rankings",
    "nhl_draft_picks_now",
    "nhl_draft_rankings_now",
    "nhl_draft_tracker_picks_now",
]


def nhl_web_pbp(
    game_id: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Pull the play-by-play feed for one NHL game.

    Endpoint: ``GET https://api-web.nhle.com/v1/gamecenter/{game_id}/play-by-play``
    Example URL: https://api-web.nhle.com/v1/gamecenter/2024020001/play-by-play

    Args:
        game_id: game_id path parameter.
        return_parsed: parse the payload through parse_nhl_web_pbp -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            nhl_web_pbp(game_id=2024020001)
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {}
    _params.update(_caller_params)
    raw = _get(
        f"https://api-web.nhle.com/v1/gamecenter/{game_id}/play-by-play",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_nhl_web_pbp(raw, return_as_pandas=return_as_pandas)
    return raw


def nhl_boxscore(
    game_id: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Pull the boxscore for one NHL game.

    Endpoint: ``GET https://api-web.nhle.com/v1/gamecenter/{game_id}/boxscore``
    Example URL: https://api-web.nhle.com/v1/gamecenter/2024020001/boxscore

    Args:
        game_id: game_id path parameter.
        return_parsed: parse the payload through parse_nhl_web_boxscore -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            nhl_boxscore(game_id=2024020001)
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {}
    _params.update(_caller_params)
    raw = _get(
        f"https://api-web.nhle.com/v1/gamecenter/{game_id}/boxscore",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_nhl_web_boxscore(raw, return_as_pandas=return_as_pandas)
    return raw


def nhl_landing(
    game_id: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Pull the gamecenter landing payload for one NHL game.

    Endpoint: ``GET https://api-web.nhle.com/v1/gamecenter/{game_id}/landing``
    Example URL: https://api-web.nhle.com/v1/gamecenter/2024020001/landing

    Args:
        game_id: game_id path parameter.
        return_parsed: parse the payload through parse_nhl_web_landing -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            nhl_landing(game_id=2024020001)
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {}
    _params.update(_caller_params)
    raw = _get(
        f"https://api-web.nhle.com/v1/gamecenter/{game_id}/landing",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_nhl_web_landing(raw, return_as_pandas=return_as_pandas)
    return raw


def nhl_right_rail(
    game_id: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Pull the gamecenter right-rail payload (in-game widgets).

    Endpoint: ``GET https://api-web.nhle.com/v1/gamecenter/{game_id}/right-rail``
    Example URL: https://api-web.nhle.com/v1/gamecenter/2024020001/right-rail

    Args:
        game_id: game_id path parameter.
        return_parsed: parse the payload through parse_nhl_web_right_rail -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            nhl_right_rail(game_id=2024020001)
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {}
    _params.update(_caller_params)
    raw = _get(
        f"https://api-web.nhle.com/v1/gamecenter/{game_id}/right-rail",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_nhl_web_right_rail(raw, return_as_pandas=return_as_pandas)
    return raw


def nhl_web_schedule(
    date: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Pull the week-of NHL schedule rooted at ``date``.

    Endpoint: ``GET https://api-web.nhle.com/v1/schedule/{date}``
    Example URL: https://api-web.nhle.com/v1/schedule

    Args:
        date: date path parameter.
        return_parsed: parse the payload through parse_nhl_web_schedule -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            nhl_web_schedule()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {}
    _params.update(_caller_params)
    __url = (
        "https://api-web.nhle.com/v1/schedule/now" if date is None else f"https://api-web.nhle.com/v1/schedule/{date}"
    )
    raw = _get(
        __url,
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_nhl_web_schedule(raw, return_as_pandas=return_as_pandas)
    return raw


def nhl_score(
    date: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Pull the single-day scoreboard for ``date``.

    Endpoint: ``GET https://api-web.nhle.com/v1/score/{date}``
    Example URL: https://api-web.nhle.com/v1/score

    Args:
        date: date path parameter.
        return_parsed: parse the payload through parse_nhl_web_score -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            nhl_score()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {}
    _params.update(_caller_params)
    __url = "https://api-web.nhle.com/v1/score/now" if date is None else f"https://api-web.nhle.com/v1/score/{date}"
    raw = _get(
        __url,
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_nhl_web_score(raw, return_as_pandas=return_as_pandas)
    return raw


def nhl_schedule_calendar(
    date: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Pull the calendar of game-days for the season.

    Endpoint: ``GET https://api-web.nhle.com/v1/schedule-calendar/{date}``
    Example URL: https://api-web.nhle.com/v1/schedule-calendar

    Args:
        date: date path parameter.
        return_parsed: parse the payload through parse_nhl_web_schedule -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            nhl_schedule_calendar()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {}
    _params.update(_caller_params)
    __url = (
        "https://api-web.nhle.com/v1/schedule-calendar/now"
        if date is None
        else f"https://api-web.nhle.com/v1/schedule-calendar/{date}"
    )
    raw = _get(
        __url,
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_nhl_web_schedule(raw, return_as_pandas=return_as_pandas)
    return raw


def nhl_playoff_series(
    season: Union[int, str],
    series_letter: str,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Pull a single playoff series payload.

    Endpoint: ``GET https://api-web.nhle.com/v1/schedule/playoff-series/{season}/{series_letter}``
    Example URL: https://api-web.nhle.com/v1/schedule/playoff-series/2025/a

    Args:
        season: season path parameter.
        series_letter: series_letter path parameter.
        return_parsed: parse the payload through parse_nhl_web_playoff_series -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            nhl_playoff_series(season=2025, series_letter='a')
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {}
    _params.update(_caller_params)
    season = format_nhl_season(season)
    __url = f"https://api-web.nhle.com/v1/schedule/playoff-series/{season}/{series_letter}"
    raw = _get(
        __url,
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_nhl_web_playoff_series(raw, return_as_pandas=return_as_pandas)
    return raw


def nhl_standings(
    date: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Pull the NHL standings.

    Endpoint: ``GET https://api-web.nhle.com/v1/standings/{date}``
    Example URL: https://api-web.nhle.com/v1/standings

    Args:
        date: date path parameter.
        return_parsed: parse the payload through parse_nhl_web_standings -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            nhl_standings()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {}
    _params.update(_caller_params)
    __url = (
        "https://api-web.nhle.com/v1/standings/now" if date is None else f"https://api-web.nhle.com/v1/standings/{date}"
    )
    raw = _get(
        __url,
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_nhl_web_standings(raw, return_as_pandas=return_as_pandas)
    return raw


def nhl_standings_season(
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Pull the per-season standings cutover dates.

    Endpoint: ``GET https://api-web.nhle.com/v1/standings-season``
    Example URL: https://api-web.nhle.com/v1/standings-season

    Args:
        return_parsed: parse the payload through parse_nhl_web_standings_season -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            nhl_standings_season()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {}
    _params.update(_caller_params)
    raw = _get(
        "https://api-web.nhle.com/v1/standings-season",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_nhl_web_standings_season(raw, return_as_pandas=return_as_pandas)
    return raw


def nhl_club_schedule_season(
    team: str,
    season: Optional[Union[int, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Pull a team's full-season schedule.

    Endpoint: ``GET https://api-web.nhle.com/v1/club-schedule-season/{team}/{season}``
    Example URL: https://api-web.nhle.com/v1/club-schedule-season/TOR

    Args:
        team: team path parameter.
        season: season path parameter.
        return_parsed: parse the payload through parse_nhl_web_club_schedule -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            nhl_club_schedule_season(team='TOR')
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {}
    _params.update(_caller_params)
    season = format_nhl_season(season)
    __url = (
        f"https://api-web.nhle.com/v1/club-schedule-season/{team}/now"
        if season is None
        else f"https://api-web.nhle.com/v1/club-schedule-season/{team}/{season}"
    )
    raw = _get(
        __url,
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_nhl_web_club_schedule(raw, return_as_pandas=return_as_pandas)
    return raw


def nhl_club_schedule_month(
    team: str,
    month: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Pull a team's schedule for one month.

    Endpoint: ``GET https://api-web.nhle.com/v1/club-schedule/{team}/month/{month}``
    Example URL: https://api-web.nhle.com/v1/club-schedule/TOR/month

    Args:
        team: team path parameter.
        month: month path parameter.
        return_parsed: parse the payload through parse_nhl_web_club_schedule -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            nhl_club_schedule_month(team='TOR')
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {}
    _params.update(_caller_params)
    __url = (
        f"https://api-web.nhle.com/v1/club-schedule/{team}/month/now"
        if month is None
        else f"https://api-web.nhle.com/v1/club-schedule/{team}/month/{month}"
    )
    raw = _get(
        __url,
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_nhl_web_club_schedule(raw, return_as_pandas=return_as_pandas)
    return raw


def nhl_club_schedule_week(
    team: str,
    date: Optional[str] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Pull a team's schedule for one week.

    Endpoint: ``GET https://api-web.nhle.com/v1/club-schedule/{team}/week/{date}``
    Example URL: https://api-web.nhle.com/v1/club-schedule/TOR/week

    Args:
        team: team path parameter.
        date: date path parameter.
        return_parsed: parse the payload through parse_nhl_web_club_schedule -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            nhl_club_schedule_week(team='TOR')
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {}
    _params.update(_caller_params)
    __url = (
        f"https://api-web.nhle.com/v1/club-schedule/{team}/week/now"
        if date is None
        else f"https://api-web.nhle.com/v1/club-schedule/{team}/week/{date}"
    )
    raw = _get(
        __url,
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_nhl_web_club_schedule(raw, return_as_pandas=return_as_pandas)
    return raw


def nhl_club_stats(
    team: str,
    season: Optional[Union[int, str]] = None,
    game_type: int = 2,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Pull a team's season stat block.

    Endpoint: ``GET https://api-web.nhle.com/v1/club-stats/{team}/{season}/{game_type}``
    Example URL: https://api-web.nhle.com/v1/club-stats/TOR

    Args:
        team: team path parameter.
        season: season path parameter.
        game_type: game_type path parameter.
        return_parsed: parse the payload through parse_nhl_web_club_stats -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            nhl_club_stats(team='TOR')
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {}
    _params.update(_caller_params)
    season = format_nhl_season(season)
    __url = (
        f"https://api-web.nhle.com/v1/club-stats/{team}/now"
        if season is None
        else f"https://api-web.nhle.com/v1/club-stats/{team}/{season}/{game_type}"
    )
    raw = _get(
        __url,
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_nhl_web_club_stats(raw, return_as_pandas=return_as_pandas)
    return raw


def nhl_club_stats_season(
    team: str,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Pull the seasons a team has stats for.

    Endpoint: ``GET https://api-web.nhle.com/v1/club-stats-season/{team}``
    Example URL: https://api-web.nhle.com/v1/club-stats-season/TOR

    Args:
        team: team path parameter.
        return_parsed: parse the payload through parse_nhl_web_club_stats -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            nhl_club_stats_season(team='TOR')
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {}
    _params.update(_caller_params)
    raw = _get(
        f"https://api-web.nhle.com/v1/club-stats-season/{team}",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_nhl_web_club_stats(raw, return_as_pandas=return_as_pandas)
    return raw


def nhl_roster(
    team: str,
    season: Optional[Union[int, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Pull a team's roster.

    Endpoint: ``GET https://api-web.nhle.com/v1/roster/{team}/{season}``
    Example URL: https://api-web.nhle.com/v1/roster/TOR

    Args:
        team: team path parameter.
        season: season path parameter.
        return_parsed: parse the payload through parse_nhl_web_roster -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            nhl_roster(team='TOR')
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {}
    _params.update(_caller_params)
    season = format_nhl_season(season)
    __url = (
        f"https://api-web.nhle.com/v1/roster/{team}/current"
        if season is None
        else f"https://api-web.nhle.com/v1/roster/{team}/{season}"
    )
    raw = _get(
        __url,
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_nhl_web_roster(raw, return_as_pandas=return_as_pandas)
    return raw


def nhl_roster_season(
    team: str,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Pull every season a team has had on file.

    Endpoint: ``GET https://api-web.nhle.com/v1/roster-season/{team}``
    Example URL: https://api-web.nhle.com/v1/roster-season/TOR

    Args:
        team: team path parameter.
        return_parsed: parse the payload through parse_nhl_web_roster -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            nhl_roster_season(team='TOR')
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {}
    _params.update(_caller_params)
    raw = _get(
        f"https://api-web.nhle.com/v1/roster-season/{team}",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_nhl_web_roster(raw, return_as_pandas=return_as_pandas)
    return raw


def nhl_player_landing(
    player_id: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Pull the player profile / overview.

    Endpoint: ``GET https://api-web.nhle.com/v1/player/{player_id}/landing``
    Example URL: https://api-web.nhle.com/v1/player/8480801/landing

    Args:
        player_id: player_id path parameter.
        return_parsed: parse the payload through parse_nhl_web_player_landing -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            nhl_player_landing(player_id=8480801)
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {}
    _params.update(_caller_params)
    raw = _get(
        f"https://api-web.nhle.com/v1/player/{player_id}/landing",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_nhl_web_player_landing(raw, return_as_pandas=return_as_pandas)
    return raw


def nhl_player_game_log(
    player_id: int,
    season: Optional[Union[int, str]] = None,
    game_type: int = 2,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Pull a player's game-by-game log.

    Endpoint: ``GET https://api-web.nhle.com/v1/player/{player_id}/game-log/{season}/{game_type}``
    Example URL: https://api-web.nhle.com/v1/player/8480801/game-log

    Args:
        player_id: player_id path parameter.
        season: season path parameter.
        game_type: game_type path parameter.
        return_parsed: parse the payload through parse_nhl_web_player_game_log -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            nhl_player_game_log(player_id=8480801)
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {}
    _params.update(_caller_params)
    season = format_nhl_season(season)
    __url = (
        f"https://api-web.nhle.com/v1/player/{player_id}/game-log/now"
        if season is None
        else f"https://api-web.nhle.com/v1/player/{player_id}/game-log/{season}/{game_type}"
    )
    raw = _get(
        __url,
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_nhl_web_player_game_log(raw, return_as_pandas=return_as_pandas)
    return raw


def nhl_player_spotlight(
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Pull the league's currently featured players.

    Endpoint: ``GET https://api-web.nhle.com/v1/player-spotlight``
    Example URL: https://api-web.nhle.com/v1/player-spotlight

    Args:
        return_parsed: parse the payload through parse_nhl_web_player_spotlight -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            nhl_player_spotlight()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {}
    _params.update(_caller_params)
    raw = _get(
        "https://api-web.nhle.com/v1/player-spotlight",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_nhl_web_player_spotlight(raw, return_as_pandas=return_as_pandas)
    return raw


def nhl_skater_leaders(
    season: Optional[Union[int, str]] = None,
    game_type: int = 2,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Pull skater stat leaders.

    Endpoint: ``GET https://api-web.nhle.com/v1/skater-stats-leaders/{season}/{game_type}``
    Example URL: https://api-web.nhle.com/v1/skater-stats-leaders

    Args:
        season: season path parameter.
        game_type: game_type path parameter.
        return_parsed: parse the payload through parse_nhl_web_leaders -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            nhl_skater_leaders()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {}
    _params.update(_caller_params)
    season = format_nhl_season(season)
    __url = (
        "https://api-web.nhle.com/v1/skater-stats-leaders/current"
        if season is None
        else f"https://api-web.nhle.com/v1/skater-stats-leaders/{season}/{game_type}"
    )
    raw = _get(
        __url,
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_nhl_web_leaders(raw, return_as_pandas=return_as_pandas)
    return raw


def nhl_goalie_leaders(
    season: Optional[Union[int, str]] = None,
    game_type: int = 2,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Pull goalie stat leaders.

    Endpoint: ``GET https://api-web.nhle.com/v1/goalie-stats-leaders/{season}/{game_type}``
    Example URL: https://api-web.nhle.com/v1/goalie-stats-leaders

    Args:
        season: season path parameter.
        game_type: game_type path parameter.
        return_parsed: parse the payload through parse_nhl_web_leaders -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            nhl_goalie_leaders()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {}
    _params.update(_caller_params)
    season = format_nhl_season(season)
    __url = (
        "https://api-web.nhle.com/v1/goalie-stats-leaders/current"
        if season is None
        else f"https://api-web.nhle.com/v1/goalie-stats-leaders/{season}/{game_type}"
    )
    raw = _get(
        __url,
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_nhl_web_leaders(raw, return_as_pandas=return_as_pandas)
    return raw


def nhl_draft_picks(
    year: Union[int, str],
    round_: Union[int, str] = "all",
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Pull NHL draft picks for a year (and optionally one round).

    Endpoint: ``GET https://api-web.nhle.com/v1/draft/picks/{year}/{round_}``
    Example URL: https://api-web.nhle.com/v1/draft/picks/2024

    Args:
        year: year path parameter.
        round_: round_ path parameter.
        return_parsed: parse the payload through parse_nhl_web_draft_picks -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            nhl_draft_picks(year=2024)
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {}
    _params.update(_caller_params)
    raw = _get(
        f"https://api-web.nhle.com/v1/draft/picks/{year}/{round_}",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_nhl_web_draft_picks(raw, return_as_pandas=return_as_pandas)
    return raw


def nhl_draft_rankings(
    year: Union[int, str],
    category: int = 1,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Pull NHL Central Scouting rankings for a draft year.

    Endpoint: ``GET https://api-web.nhle.com/v1/draft/rankings/{year}/{category}``
    Example URL: https://api-web.nhle.com/v1/draft/rankings/2024

    Args:
        year: year path parameter.
        category: category path parameter.
        return_parsed: parse the payload through parse_nhl_web_draft_rankings -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            nhl_draft_rankings(year=2024)
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {}
    _params.update(_caller_params)
    raw = _get(
        f"https://api-web.nhle.com/v1/draft/rankings/{year}/{category}",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_nhl_web_draft_rankings(raw, return_as_pandas=return_as_pandas)
    return raw


def nhl_draft_picks_now(
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Pull the current / most recent draft pick set.

    Endpoint: ``GET https://api-web.nhle.com/v1/draft/picks/now``
    Example URL: https://api-web.nhle.com/v1/draft/picks/now

    Args:
        return_parsed: parse the payload through parse_nhl_web_draft_picks -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            nhl_draft_picks_now()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {}
    _params.update(_caller_params)
    raw = _get(
        "https://api-web.nhle.com/v1/draft/picks/now",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_nhl_web_draft_picks(raw, return_as_pandas=return_as_pandas)
    return raw


def nhl_draft_rankings_now(
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Pull the current Central Scouting rankings.

    Endpoint: ``GET https://api-web.nhle.com/v1/draft/rankings/now``
    Example URL: https://api-web.nhle.com/v1/draft/rankings/now

    Args:
        return_parsed: parse the payload through parse_nhl_web_draft_rankings -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            nhl_draft_rankings_now()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {}
    _params.update(_caller_params)
    raw = _get(
        "https://api-web.nhle.com/v1/draft/rankings/now",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_nhl_web_draft_rankings(raw, return_as_pandas=return_as_pandas)
    return raw


def nhl_draft_tracker_picks_now(
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Pull the live draft-tracker pick list (during the draft itself).

    Endpoint: ``GET https://api-web.nhle.com/v1/draft-tracker/picks/now``
    Example URL: https://api-web.nhle.com/v1/draft-tracker/picks/now

    Args:
        return_parsed: parse the payload through parse_nhl_web_draft_picks -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            nhl_draft_tracker_picks_now()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {}
    _params.update(_caller_params)
    raw = _get(
        "https://api-web.nhle.com/v1/draft-tracker/picks/now",
        params=_params,
        **kwargs,
    )
    if return_parsed:
        return parse_nhl_web_draft_picks(raw, return_as_pandas=return_as_pandas)
    return raw
