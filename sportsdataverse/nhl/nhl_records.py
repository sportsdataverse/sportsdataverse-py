# GENERATED by tools/codegen/generate.py -- DO NOT EDIT.
# Source: tools/codegen/endpoints/nhl_records.yaml
"""nhl_records wrappers (host https://records.nhl.com/site/api). Generated."""

from __future__ import annotations

from typing import TYPE_CHECKING, Dict, List, Optional, Union  # noqa: F401

from sportsdataverse._codegen_runtime import _get
from sportsdataverse.nhl.nhl_records_parsers import parse_nhl_records

if TYPE_CHECKING:  # pragma: no cover -- annotation-only imports (PEP 563 defers eval)
    import pandas as pd
    import polars as pl

__all__ = [
    "nhl_records_awards",
    "nhl_records_awards_by_franchise",
    "nhl_records_awards_trophy_season",
    "nhl_records_coaches",
    "nhl_records_coach",
    "nhl_records_coach_career",
    "nhl_records_coach_career_with_playoffs",
    "nhl_records_coach_franchise",
    "nhl_records_coach_stanley_cup",
    "nhl_records_franchises",
    "nhl_records_franchise_detail",
    "nhl_records_franchise_team_totals",
    "nhl_records_franchise_season_results",
    "nhl_records_franchise_playoff_appearances",
    "nhl_records_franchise_totals",
    "nhl_records_all_time_record_vs_franchise",
    "nhl_records_skater_career_stats",
    "nhl_records_skater_career_leaders",
    "nhl_records_consecutive_100pt_seasons",
    "nhl_records_goalie_career_stats",
    "nhl_records_goalie_career_stats_with_playoffs",
    "nhl_records_goalie_season_stats",
    "nhl_records_goalie_win_streak",
    "nhl_records_goalie_shutout_streak",
    "nhl_records_goalie_win_plateaus",
    "nhl_records_goalie_playoff_streak",
    "nhl_records_goalie_undefeated_streak",
    "nhl_records_draft",
    "nhl_records_draft_by_team",
    "nhl_records_draft_prospect",
    "nhl_records_draft_lottery_odds",
    "nhl_records_expansion_draft_picks",
    "nhl_records_allstar_skater_career",
    "nhl_records_allstar_goalie_career",
    "nhl_records_allstar_coach_career",
    "nhl_records_allstar_skater_game",
    "nhl_records_allstar_goalie_game",
    "nhl_records_attendance",
    "nhl_records_hof_players",
    "nhl_records_hof_players_by_office",
    "nhl_records_gm_career",
    "nhl_records_gm_franchise",
    "nhl_records_home_team_record",
    "nhl_records_away_team_record",
]


def nhl_records_awards(
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """List all NHL award / trophy records.

    Endpoint: ``GET https://records.nhl.com/site/api/award-details``
    Example URL: https://records.nhl.com/site/api/award-details

    Args:
        return_parsed: parse the payload through parse_nhl_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            nhl_records_awards()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        "https://records.nhl.com/site/api/award-details",
        params=_params,
    )
    if return_parsed:
        return parse_nhl_records(raw, return_as_pandas=return_as_pandas)
    return raw


def nhl_records_awards_by_franchise(
    franchise_id: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """List award records for a single franchise.

    Endpoint: ``GET https://records.nhl.com/site/api/award-details/{franchise_id}``
    Example URL: https://records.nhl.com/site/api/award-details/1

    Args:
        franchise_id: franchise_id path parameter.
        return_parsed: parse the payload through parse_nhl_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            nhl_records_awards_by_franchise(franchise_id=1)
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://records.nhl.com/site/api/award-details/{franchise_id}",
        params=_params,
    )
    if return_parsed:
        return parse_nhl_records(raw, return_as_pandas=return_as_pandas)
    return raw


def nhl_records_awards_trophy_season(
    trophy_id: int,
    season_id: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Retrieve the trophy winner for a specific season.

    Endpoint: ``GET https://records.nhl.com/site/api/award-details/trophy/{trophy_id}/season/{season_id}``
    Example URL: https://records.nhl.com/site/api/award-details/trophy/1/season/X

    Args:
        trophy_id: trophy_id path parameter.
        season_id: season_id path parameter.
        return_parsed: parse the payload through parse_nhl_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            nhl_records_awards_trophy_season(trophy_id=1, season_id='X')
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://records.nhl.com/site/api/award-details/trophy/{trophy_id}/season/{season_id}",
        params=_params,
    )
    if return_parsed:
        return parse_nhl_records(raw, return_as_pandas=return_as_pandas)
    return raw


def nhl_records_coaches(
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """List NHL head coaches.

    Endpoint: ``GET https://records.nhl.com/site/api/coach``
    Example URL: https://records.nhl.com/site/api/coach

    Args:
        return_parsed: parse the payload through parse_nhl_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            nhl_records_coaches()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        "https://records.nhl.com/site/api/coach",
        params=_params,
    )
    if return_parsed:
        return parse_nhl_records(raw, return_as_pandas=return_as_pandas)
    return raw


def nhl_records_coach(
    coach_id: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Retrieve one coach by their numeric ID.

    Endpoint: ``GET https://records.nhl.com/site/api/coach/{coach_id}``
    Example URL: https://records.nhl.com/site/api/coach/X

    Args:
        coach_id: coach_id path parameter.
        return_parsed: parse the payload through parse_nhl_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            nhl_records_coach(coach_id='X')
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://records.nhl.com/site/api/coach/{coach_id}",
        params=_params,
    )
    if return_parsed:
        return parse_nhl_records(raw, return_as_pandas=return_as_pandas)
    return raw


def nhl_records_coach_career(
    coach_id: Optional[int] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Coach career-records (regular season).

    Endpoint: ``GET https://records.nhl.com/site/api/coach-career-records/{coach_id}``
    Example URL: https://records.nhl.com/site/api/coach-career-records

    Args:
        coach_id: coach_id path parameter.
        return_parsed: parse the payload through parse_nhl_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            nhl_records_coach_career()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    __url = (
        "https://records.nhl.com/site/api/coach-career-records"
        if coach_id is None
        else f"https://records.nhl.com/site/api/coach-career-records/{coach_id}"
    )
    raw = _get(
        __url,
        params=_params,
    )
    if return_parsed:
        return parse_nhl_records(raw, return_as_pandas=return_as_pandas)
    return raw


def nhl_records_coach_career_with_playoffs(
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Coach career records inclusive of regular season + playoffs.

    Endpoint: ``GET https://records.nhl.com/site/api/coach-career-records-regular-plus-playoffs``
    Example URL: https://records.nhl.com/site/api/coach-career-records-regular-plus-playoffs

    Args:
        return_parsed: parse the payload through parse_nhl_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            nhl_records_coach_career_with_playoffs()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        "https://records.nhl.com/site/api/coach-career-records-regular-plus-playoffs",
        params=_params,
    )
    if return_parsed:
        return parse_nhl_records(raw, return_as_pandas=return_as_pandas)
    return raw


def nhl_records_coach_franchise(
    coach_id: Optional[int] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Coach records scoped to individual franchise stints.

    Endpoint: ``GET https://records.nhl.com/site/api/coach-franchise-records/{coach_id}``
    Example URL: https://records.nhl.com/site/api/coach-franchise-records

    Args:
        coach_id: coach_id path parameter.
        return_parsed: parse the payload through parse_nhl_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            nhl_records_coach_franchise()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    __url = (
        "https://records.nhl.com/site/api/coach-franchise-records"
        if coach_id is None
        else f"https://records.nhl.com/site/api/coach-franchise-records/{coach_id}"
    )
    raw = _get(
        __url,
        params=_params,
    )
    if return_parsed:
        return parse_nhl_records(raw, return_as_pandas=return_as_pandas)
    return raw


def nhl_records_coach_stanley_cup(
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Coach Stanley Cup Final win streak and consecutive-cup records.

    Endpoint: ``GET https://records.nhl.com/site/api/coach-stanley-cup-streak``
    Example URL: https://records.nhl.com/site/api/coach-stanley-cup-streak

    Args:
        return_parsed: parse the payload through parse_nhl_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            nhl_records_coach_stanley_cup()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        "https://records.nhl.com/site/api/coach-stanley-cup-streak",
        params=_params,
    )
    if return_parsed:
        return parse_nhl_records(raw, return_as_pandas=return_as_pandas)
    return raw


def nhl_records_franchises(
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """List all NHL franchises (historical and active).

    Endpoint: ``GET https://records.nhl.com/site/api/franchise``
    Example URL: https://records.nhl.com/site/api/franchise

    Args:
        return_parsed: parse the payload through parse_nhl_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            nhl_records_franchises()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        "https://records.nhl.com/site/api/franchise",
        params=_params,
    )
    if return_parsed:
        return parse_nhl_records(raw, return_as_pandas=return_as_pandas)
    return raw


def nhl_records_franchise_detail(
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Franchise detail records (extended metadata per franchise).

    Endpoint: ``GET https://records.nhl.com/site/api/franchise-detail``
    Example URL: https://records.nhl.com/site/api/franchise-detail

    Args:
        return_parsed: parse the payload through parse_nhl_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            nhl_records_franchise_detail()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        "https://records.nhl.com/site/api/franchise-detail",
        params=_params,
    )
    if return_parsed:
        return parse_nhl_records(raw, return_as_pandas=return_as_pandas)
    return raw


def nhl_records_franchise_team_totals(
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """All-time team totals per franchise (regular season).

    Endpoint: ``GET https://records.nhl.com/site/api/franchise-team-totals``
    Example URL: https://records.nhl.com/site/api/franchise-team-totals

    Args:
        return_parsed: parse the payload through parse_nhl_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            nhl_records_franchise_team_totals()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        "https://records.nhl.com/site/api/franchise-team-totals",
        params=_params,
    )
    if return_parsed:
        return parse_nhl_records(raw, return_as_pandas=return_as_pandas)
    return raw


def nhl_records_franchise_season_results(
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Season-by-season results for each franchise.

    Endpoint: ``GET https://records.nhl.com/site/api/franchise-season-results``
    Example URL: https://records.nhl.com/site/api/franchise-season-results

    Args:
        return_parsed: parse the payload through parse_nhl_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            nhl_records_franchise_season_results()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        "https://records.nhl.com/site/api/franchise-season-results",
        params=_params,
    )
    if return_parsed:
        return parse_nhl_records(raw, return_as_pandas=return_as_pandas)
    return raw


def nhl_records_franchise_playoff_appearances(
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Franchise playoff appearance counts and streak information.

    Endpoint: ``GET https://records.nhl.com/site/api/franchise-playoff-appearances``
    Example URL: https://records.nhl.com/site/api/franchise-playoff-appearances

    Args:
        return_parsed: parse the payload through parse_nhl_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            nhl_records_franchise_playoff_appearances()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        "https://records.nhl.com/site/api/franchise-playoff-appearances",
        params=_params,
    )
    if return_parsed:
        return parse_nhl_records(raw, return_as_pandas=return_as_pandas)
    return raw


def nhl_records_franchise_totals(
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """League-wide franchise totals (all-time aggregate per franchise).

    Endpoint: ``GET https://records.nhl.com/site/api/franchise-totals``
    Example URL: https://records.nhl.com/site/api/franchise-totals

    Args:
        return_parsed: parse the payload through parse_nhl_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            nhl_records_franchise_totals()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        "https://records.nhl.com/site/api/franchise-totals",
        params=_params,
    )
    if return_parsed:
        return parse_nhl_records(raw, return_as_pandas=return_as_pandas)
    return raw


def nhl_records_all_time_record_vs_franchise(
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """All-time head-to-head records between every franchise pairing.

    Endpoint: ``GET https://records.nhl.com/site/api/all-time-record-vs-franchise``
    Example URL: https://records.nhl.com/site/api/all-time-record-vs-franchise

    Args:
        return_parsed: parse the payload through parse_nhl_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            nhl_records_all_time_record_vs_franchise()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        "https://records.nhl.com/site/api/all-time-record-vs-franchise",
        params=_params,
    )
    if return_parsed:
        return parse_nhl_records(raw, return_as_pandas=return_as_pandas)
    return raw


def nhl_records_skater_career_stats(
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Skater career statistics (all-time, regular season).

    Endpoint: ``GET https://records.nhl.com/site/api/skater-career-statistics``
    Example URL: https://records.nhl.com/site/api/skater-career-statistics

    Args:
        return_parsed: parse the payload through parse_nhl_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            nhl_records_skater_career_stats()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        "https://records.nhl.com/site/api/skater-career-statistics",
        params=_params,
    )
    if return_parsed:
        return parse_nhl_records(raw, return_as_pandas=return_as_pandas)
    return raw


def nhl_records_skater_career_leaders(
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """All-time skater career leaderboards.

    Endpoint: ``GET https://records.nhl.com/site/api/skater-career-leaders``
    Example URL: https://records.nhl.com/site/api/skater-career-leaders

    Args:
        return_parsed: parse the payload through parse_nhl_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            nhl_records_skater_career_leaders()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        "https://records.nhl.com/site/api/skater-career-leaders",
        params=_params,
    )
    if return_parsed:
        return parse_nhl_records(raw, return_as_pandas=return_as_pandas)
    return raw


def nhl_records_consecutive_100pt_seasons(
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Skaters with the most consecutive 100-point seasons.

    Endpoint: ``GET https://records.nhl.com/site/api/consecutive-100-point-seasons``
    Example URL: https://records.nhl.com/site/api/consecutive-100-point-seasons

    Args:
        return_parsed: parse the payload through parse_nhl_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            nhl_records_consecutive_100pt_seasons()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        "https://records.nhl.com/site/api/consecutive-100-point-seasons",
        params=_params,
    )
    if return_parsed:
        return parse_nhl_records(raw, return_as_pandas=return_as_pandas)
    return raw


def nhl_records_goalie_career_stats(
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Goaltender career statistics (regular season).

    Endpoint: ``GET https://records.nhl.com/site/api/goalie-career-stats``
    Example URL: https://records.nhl.com/site/api/goalie-career-stats

    Args:
        return_parsed: parse the payload through parse_nhl_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            nhl_records_goalie_career_stats()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        "https://records.nhl.com/site/api/goalie-career-stats",
        params=_params,
    )
    if return_parsed:
        return parse_nhl_records(raw, return_as_pandas=return_as_pandas)
    return raw


def nhl_records_goalie_career_stats_with_playoffs(
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Goaltender career stats inclusive of regular season and playoffs.

    Endpoint: ``GET https://records.nhl.com/site/api/goalie_career_stats_incl_playoffs``
    Example URL: https://records.nhl.com/site/api/goalie_career_stats_incl_playoffs

    Args:
        return_parsed: parse the payload through parse_nhl_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            nhl_records_goalie_career_stats_with_playoffs()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        "https://records.nhl.com/site/api/goalie_career_stats_incl_playoffs",
        params=_params,
    )
    if return_parsed:
        return parse_nhl_records(raw, return_as_pandas=return_as_pandas)
    return raw


def nhl_records_goalie_season_stats(
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Goaltender single-season statistics.

    Endpoint: ``GET https://records.nhl.com/site/api/goalie-season-stats``
    Example URL: https://records.nhl.com/site/api/goalie-season-stats

    Args:
        return_parsed: parse the payload through parse_nhl_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            nhl_records_goalie_season_stats()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        "https://records.nhl.com/site/api/goalie-season-stats",
        params=_params,
    )
    if return_parsed:
        return parse_nhl_records(raw, return_as_pandas=return_as_pandas)
    return raw


def nhl_records_goalie_win_streak(
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Goaltenders with the longest consecutive-win streaks.

    Endpoint: ``GET https://records.nhl.com/site/api/goalie-win-streak``
    Example URL: https://records.nhl.com/site/api/goalie-win-streak

    Args:
        return_parsed: parse the payload through parse_nhl_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            nhl_records_goalie_win_streak()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        "https://records.nhl.com/site/api/goalie-win-streak",
        params=_params,
    )
    if return_parsed:
        return parse_nhl_records(raw, return_as_pandas=return_as_pandas)
    return raw


def nhl_records_goalie_shutout_streak(
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Goaltenders with the longest consecutive-shutout streaks.

    Endpoint: ``GET https://records.nhl.com/site/api/goalie-shutout-streak``
    Example URL: https://records.nhl.com/site/api/goalie-shutout-streak

    Args:
        return_parsed: parse the payload through parse_nhl_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            nhl_records_goalie_shutout_streak()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        "https://records.nhl.com/site/api/goalie-shutout-streak",
        params=_params,
    )
    if return_parsed:
        return parse_nhl_records(raw, return_as_pandas=return_as_pandas)
    return raw


def nhl_records_goalie_win_plateaus(
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Goaltenders who reached each win plateau (100, 200, 300 …).

    Endpoint: ``GET https://records.nhl.com/site/api/goalie-win-plateaus``
    Example URL: https://records.nhl.com/site/api/goalie-win-plateaus

    Args:
        return_parsed: parse the payload through parse_nhl_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            nhl_records_goalie_win_plateaus()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        "https://records.nhl.com/site/api/goalie-win-plateaus",
        params=_params,
    )
    if return_parsed:
        return parse_nhl_records(raw, return_as_pandas=return_as_pandas)
    return raw


def nhl_records_goalie_playoff_streak(
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Goaltender consecutive playoff-win streaks.

    Endpoint: ``GET https://records.nhl.com/site/api/goalie-playoff-streak``
    Example URL: https://records.nhl.com/site/api/goalie-playoff-streak

    Args:
        return_parsed: parse the payload through parse_nhl_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            nhl_records_goalie_playoff_streak()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        "https://records.nhl.com/site/api/goalie-playoff-streak",
        params=_params,
    )
    if return_parsed:
        return parse_nhl_records(raw, return_as_pandas=return_as_pandas)
    return raw


def nhl_records_goalie_undefeated_streak(
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Goaltender longest undefeated streaks (wins + ties).

    Endpoint: ``GET https://records.nhl.com/site/api/goalie-undefeated-streak``
    Example URL: https://records.nhl.com/site/api/goalie-undefeated-streak

    Args:
        return_parsed: parse the payload through parse_nhl_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            nhl_records_goalie_undefeated_streak()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        "https://records.nhl.com/site/api/goalie-undefeated-streak",
        params=_params,
    )
    if return_parsed:
        return parse_nhl_records(raw, return_as_pandas=return_as_pandas)
    return raw


def nhl_records_draft(
    draft_id: Optional[int] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Retrieve NHL Entry Draft picks.

    Endpoint: ``GET https://records.nhl.com/site/api/draft/{draft_id}``
    Example URL: https://records.nhl.com/site/api/draft

    Args:
        draft_id: draft_id path parameter.
        return_parsed: parse the payload through parse_nhl_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            nhl_records_draft()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    __url = (
        "https://records.nhl.com/site/api/draft"
        if draft_id is None
        else f"https://records.nhl.com/site/api/draft/{draft_id}"
    )
    raw = _get(
        __url,
        params=_params,
    )
    if return_parsed:
        return parse_nhl_records(raw, return_as_pandas=return_as_pandas)
    return raw


def nhl_records_draft_by_team(
    team_id: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """All draft picks made by a single team.

    Endpoint: ``GET https://records.nhl.com/site/api/draft/byTeam/{team_id}``
    Example URL: https://records.nhl.com/site/api/draft/byTeam/10

    Args:
        team_id: team_id path parameter.
        return_parsed: parse the payload through parse_nhl_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            nhl_records_draft_by_team(team_id=10)
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://records.nhl.com/site/api/draft/byTeam/{team_id}",
        params=_params,
    )
    if return_parsed:
        return parse_nhl_records(raw, return_as_pandas=return_as_pandas)
    return raw


def nhl_records_draft_prospect(
    prospect_id: Optional[int] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Draft prospect records.

    Endpoint: ``GET https://records.nhl.com/site/api/draft-prospect/{prospect_id}``
    Example URL: https://records.nhl.com/site/api/draft-prospect

    Args:
        prospect_id: prospect_id path parameter.
        return_parsed: parse the payload through parse_nhl_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            nhl_records_draft_prospect()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    __url = (
        "https://records.nhl.com/site/api/draft-prospect"
        if prospect_id is None
        else f"https://records.nhl.com/site/api/draft-prospect/{prospect_id}"
    )
    raw = _get(
        __url,
        params=_params,
    )
    if return_parsed:
        return parse_nhl_records(raw, return_as_pandas=return_as_pandas)
    return raw


def nhl_records_draft_lottery_odds(
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Draft lottery odds (current year or filtered by season).

    Endpoint: ``GET https://records.nhl.com/site/api/draft-lottery-odds``
    Example URL: https://records.nhl.com/site/api/draft-lottery-odds

    Args:
        return_parsed: parse the payload through parse_nhl_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            nhl_records_draft_lottery_odds()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        "https://records.nhl.com/site/api/draft-lottery-odds",
        params=_params,
    )
    if return_parsed:
        return parse_nhl_records(raw, return_as_pandas=return_as_pandas)
    return raw


def nhl_records_expansion_draft_picks(
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Expansion draft picks (e.g. Vegas 2017, Seattle 2021).

    Endpoint: ``GET https://records.nhl.com/site/api/expansion-draft-picks``
    Example URL: https://records.nhl.com/site/api/expansion-draft-picks

    Args:
        return_parsed: parse the payload through parse_nhl_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            nhl_records_expansion_draft_picks()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        "https://records.nhl.com/site/api/expansion-draft-picks",
        params=_params,
    )
    if return_parsed:
        return parse_nhl_records(raw, return_as_pandas=return_as_pandas)
    return raw


def nhl_records_allstar_skater_career(
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """All-Star Game career statistics for skaters.

    Endpoint: ``GET https://records.nhl.com/site/api/all-star-skater-career-stats``
    Example URL: https://records.nhl.com/site/api/all-star-skater-career-stats

    Args:
        return_parsed: parse the payload through parse_nhl_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            nhl_records_allstar_skater_career()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        "https://records.nhl.com/site/api/all-star-skater-career-stats",
        params=_params,
    )
    if return_parsed:
        return parse_nhl_records(raw, return_as_pandas=return_as_pandas)
    return raw


def nhl_records_allstar_goalie_career(
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """All-Star Game career statistics for goaltenders.

    Endpoint: ``GET https://records.nhl.com/site/api/all-star-goaltender-career-stats``
    Example URL: https://records.nhl.com/site/api/all-star-goaltender-career-stats

    Args:
        return_parsed: parse the payload through parse_nhl_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            nhl_records_allstar_goalie_career()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        "https://records.nhl.com/site/api/all-star-goaltender-career-stats",
        params=_params,
    )
    if return_parsed:
        return parse_nhl_records(raw, return_as_pandas=return_as_pandas)
    return raw


def nhl_records_allstar_coach_career(
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """All-Star Game career records for coaches.

    Endpoint: ``GET https://records.nhl.com/site/api/all-star-coach-career-stats``
    Example URL: https://records.nhl.com/site/api/all-star-coach-career-stats

    Args:
        return_parsed: parse the payload through parse_nhl_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            nhl_records_allstar_coach_career()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        "https://records.nhl.com/site/api/all-star-coach-career-stats",
        params=_params,
    )
    if return_parsed:
        return parse_nhl_records(raw, return_as_pandas=return_as_pandas)
    return raw


def nhl_records_allstar_skater_game(
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """All-Star Game single-game scoring records for skaters.

    Endpoint: ``GET https://records.nhl.com/site/api/all-star-skater-game-stats``
    Example URL: https://records.nhl.com/site/api/all-star-skater-game-stats

    Args:
        return_parsed: parse the payload through parse_nhl_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            nhl_records_allstar_skater_game()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        "https://records.nhl.com/site/api/all-star-skater-game-stats",
        params=_params,
    )
    if return_parsed:
        return parse_nhl_records(raw, return_as_pandas=return_as_pandas)
    return raw


def nhl_records_allstar_goalie_game(
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """All-Star Game single-game stats for goaltenders.

    Endpoint: ``GET https://records.nhl.com/site/api/all-star-goaltender-game-stats``
    Example URL: https://records.nhl.com/site/api/all-star-goaltender-game-stats

    Args:
        return_parsed: parse the payload through parse_nhl_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            nhl_records_allstar_goalie_game()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        "https://records.nhl.com/site/api/all-star-goaltender-game-stats",
        params=_params,
    )
    if return_parsed:
        return parse_nhl_records(raw, return_as_pandas=return_as_pandas)
    return raw


def nhl_records_attendance(
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """NHL arena attendance records.

    Endpoint: ``GET https://records.nhl.com/site/api/attendance``
    Example URL: https://records.nhl.com/site/api/attendance

    Args:
        return_parsed: parse the payload through parse_nhl_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            nhl_records_attendance()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        "https://records.nhl.com/site/api/attendance",
        params=_params,
    )
    if return_parsed:
        return parse_nhl_records(raw, return_as_pandas=return_as_pandas)
    return raw


def nhl_records_hof_players(
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Hockey Hall of Fame player inductees.

    Endpoint: ``GET https://records.nhl.com/site/api/hof/players``
    Example URL: https://records.nhl.com/site/api/hof/players

    Args:
        return_parsed: parse the payload through parse_nhl_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            nhl_records_hof_players()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        "https://records.nhl.com/site/api/hof/players",
        params=_params,
    )
    if return_parsed:
        return parse_nhl_records(raw, return_as_pandas=return_as_pandas)
    return raw


def nhl_records_hof_players_by_office(
    office_id: int,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """Hall of Fame players for a specific induction office/category.

    Endpoint: ``GET https://records.nhl.com/site/api/hof/players/{office_id}``
    Example URL: https://records.nhl.com/site/api/hof/players/X

    Args:
        office_id: office_id path parameter.
        return_parsed: parse the payload through parse_nhl_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            nhl_records_hof_players_by_office(office_id='X')
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        f"https://records.nhl.com/site/api/hof/players/{office_id}",
        params=_params,
    )
    if return_parsed:
        return parse_nhl_records(raw, return_as_pandas=return_as_pandas)
    return raw


def nhl_records_gm_career(
    gm_id: Optional[int] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """General Manager career records.

    Endpoint: ``GET https://records.nhl.com/site/api/general-manager/{gm_id}``
    Example URL: https://records.nhl.com/site/api/general-manager

    Args:
        gm_id: gm_id path parameter.
        return_parsed: parse the payload through parse_nhl_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            nhl_records_gm_career()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    __url = (
        "https://records.nhl.com/site/api/general-manager-career-records"
        if gm_id is None
        else f"https://records.nhl.com/site/api/general-manager/{gm_id}"
    )
    raw = _get(
        __url,
        params=_params,
    )
    if return_parsed:
        return parse_nhl_records(raw, return_as_pandas=return_as_pandas)
    return raw


def nhl_records_gm_franchise(
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """General Manager records scoped to franchise stints.

    Endpoint: ``GET https://records.nhl.com/site/api/general-manager-franchise-records``
    Example URL: https://records.nhl.com/site/api/general-manager-franchise-records

    Args:
        return_parsed: parse the payload through parse_nhl_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            nhl_records_gm_franchise()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        "https://records.nhl.com/site/api/general-manager-franchise-records",
        params=_params,
    )
    if return_parsed:
        return parse_nhl_records(raw, return_as_pandas=return_as_pandas)
    return raw


def nhl_records_home_team_record(
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """League-wide home-team win/loss record by season.

    Endpoint: ``GET https://records.nhl.com/site/api/home-team-record``
    Example URL: https://records.nhl.com/site/api/home-team-record

    Args:
        return_parsed: parse the payload through parse_nhl_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            nhl_records_home_team_record()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        "https://records.nhl.com/site/api/home-team-record",
        params=_params,
    )
    if return_parsed:
        return parse_nhl_records(raw, return_as_pandas=return_as_pandas)
    return raw


def nhl_records_away_team_record(
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, Dict]:
    """League-wide away-team win/loss record by season.

    Endpoint: ``GET https://records.nhl.com/site/api/away-team-record``
    Example URL: https://records.nhl.com/site/api/away-team-record

    Args:
        return_parsed: parse the payload through parse_nhl_records -> polars DataFrame (default True). Pass return_parsed=False for the raw JSON Dict.
        return_as_pandas: with return_parsed, return a pandas DataFrame instead of polars.

    Returns:
        A polars/pandas DataFrame by default; the raw JSON ``Dict`` when ``return_parsed=False``.

    Example:
        Quick start::

            nhl_records_away_team_record()
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        **{_k: _v for _k, _v in kwargs.items() if _v is not None},
    }
    _params.update(_caller_params)
    raw = _get(
        "https://records.nhl.com/site/api/away-team-record",
        params=_params,
    )
    if return_parsed:
        return parse_nhl_records(raw, return_as_pandas=return_as_pandas)
    return raw
