# GENERATED by tools/codegen/generate.py -- DO NOT EDIT.
# Source: tools/codegen/endpoints/releases.yaml
"""sportsdataverse.nhl dataset loaders (sportsdataverse-data releases). Generated."""

from __future__ import annotations

import polars as pl

from sportsdataverse._codegen_runtime import (
    SeasonNotFoundError,  # noqa: F401
    _as_season_list,
    _cast_ids_int64,  # noqa: F401  (used only by loaders declaring id_int64)
    _read_release_parquet,
    cli_warn,
)
from sportsdataverse._deprecation import warn_deprecated  # noqa: F401  (used only by deprecated_for shims)

__all__ = [
    "load_nhl_pbp",
    "load_nhl_player_boxscore",
    "load_nhl_schedule",
    "load_nhl_team_boxscore",
    "load_nhl_game_info",
    "load_nhl_game_rosters",
    "load_nhl_goalie_boxscores",
    "load_nhl_linescore",
    "load_nhl_officials",
    "load_nhl_pbp_full",
    "load_nhl_pbp_lite",
    "load_nhl_penalties",
    "load_nhl_player_boxscores",
    "load_nhl_rosters",
    "load_nhl_schedules",
    "load_nhl_scoring",
    "load_nhl_scratches",
    "load_nhl_shifts",
    "load_nhl_shootout",
    "load_nhl_shots_by_period",
    "load_nhl_skater_boxscores",
    "load_nhl_team_boxscores",
    "load_nhl_three_stars",
]


def load_nhl_pbp(seasons, return_as_pandas: bool = False):
    """Load nhl_pbp_full (sportsdataverse-data release).

    Source: https://github.com/sportsdataverse/sportsdataverse-data/releases/tag/nhl_pbp_full

    Args:
        seasons: an int or iterable of seasons (>= 2010).
        return_as_pandas: return a pandas DataFrame instead of polars.

    Returns:
        A polars (or pandas) DataFrame; seasons with no published asset are
        skipped with a warning rather than raising (404-safe).

        |col_name                 |type    |
        |:------------------------|:-------|
        |event_type               |String  |
        |event                    |String  |
        |secondary_type           |String  |
        |event_team_abbr          |String  |
        |event_team_type          |String  |
        |description              |String  |
        |period                   |Int64   |
        |period_type              |String  |
        |period_time              |String  |
        |period_seconds           |Int64   |
        |period_seconds_remaining |Int64   |
        |period_time_remaining    |String  |
        |game_seconds             |Int64   |
        |game_seconds_remaining   |Int64   |
        |home_score               |Int64   |
        |away_score               |Int64   |
        |event_player_1_name      |String  |
        |event_player_1_type      |String  |
        |event_player_1_id        |Int64   |
        |event_player_2_name      |String  |
        |event_player_2_type      |String  |
        |event_player_2_id        |Int64   |
        |event_player_3_name      |String  |
        |event_player_3_type      |String  |
        |event_player_3_id        |Int64   |
        |event_goalie_name        |String  |
        |event_goalie_id          |Int64   |
        |penalty_severity         |String  |
        |penalty_minutes          |Int64   |
        |strength_state           |String  |
        |strength_code            |String  |
        |strength                 |String  |
        |empty_net                |Boolean |
        |extra_attacker           |Boolean |
        |x                        |Int64   |
        |y                        |Int64   |
        |x_fixed                  |Int64   |
        |y_fixed                  |Int64   |
        |shot_distance            |Float64 |
        |shot_angle               |Float64 |
        |home_skaters             |Int64   |
        |away_skaters             |Int64   |
        |home_on_1                |String  |
        |home_on_2                |String  |
        |home_on_3                |String  |
        |home_on_4                |String  |
        |home_on_5                |String  |
        |home_on_6                |String  |
        |home_on_7                |String  |
        |away_on_1                |String  |
        |away_on_2                |String  |
        |away_on_3                |String  |
        |away_on_4                |String  |
        |away_on_5                |String  |
        |away_on_6                |String  |
        |away_on_7                |String  |
        |home_goalie              |String  |
        |away_goalie              |String  |
        |num_on                   |Int64   |
        |players_on               |String  |
        |num_off                  |Int64   |
        |players_off              |String  |
        |game_id                  |Int64   |
        |season                   |String  |
        |season_type              |String  |
        |home_abbr                |String  |
        |away_abbr                |String  |
        |event_idx                |Int64   |
        |event_id                 |Int64   |
        |pptReplayUrl             |String  |
        |away_goalie_in           |Int64   |
        |home_goalie_in           |Int64   |
        |reason                   |String  |
        |secondaryReason          |String  |
        |ids_on                   |String  |
        |ids_off                  |String  |
        |home_on_1_id             |Int64   |
        |away_on_1_id             |Int64   |
        |home_on_2_id             |Int64   |
        |away_on_2_id             |Int64   |
        |home_on_3_id             |Int64   |
        |away_on_3_id             |Int64   |
        |home_on_4_id             |Int64   |
        |away_on_4_id             |Int64   |
        |home_on_5_id             |Int64   |
        |away_on_5_id             |Int64   |
        |home_on_6_id             |Int64   |
        |away_on_6_id             |Int64   |
        |home_on_7_id             |Int64   |
        |away_on_7_id             |Int64   |
        |home_goalie_id           |Int64   |
        |away_goalie_id           |Int64   |
        |xg                       |Float64 |
        |game_date                |String  |

    Raises:
        SeasonNotFoundError: if a requested season is below 2010.

    Example:
        Quick start::

            load_nhl_pbp(seasons=2024)
    """
    frames, missing = [], []
    for season in _as_season_list(seasons):
        if int(season) < 2010:
            raise SeasonNotFoundError("season cannot be less than 2010")
        df = _read_release_parquet(
            f"https://github.com/sportsdataverse/sportsdataverse-data/releases/download/nhl_pbp_full/play_by_play_{season}.parquet"
        )
        if df is None:
            missing.append(season)
            continue
        frames.append(df)
    if missing:
        cli_warn("load_nhl_pbp: no data for season(s) {missing} (skipped)".format(missing=missing))
    # diagonal: per-season release schemas can drift (columns added/dropped
    # over the years) -- union columns, null-fill gaps.
    out = pl.concat(frames, how="diagonal_relaxed") if frames else pl.DataFrame()
    return out.to_pandas(use_pyarrow_extension_array=True) if return_as_pandas else out


def load_nhl_player_boxscore(seasons, return_as_pandas: bool = False):
    """Load nhl_player_boxscores (sportsdataverse-data release).

    Source: https://github.com/sportsdataverse/sportsdataverse-data/releases/tag/nhl_player_boxscores

    Args:
        seasons: an int or iterable of seasons (>= 2010).
        return_as_pandas: return a pandas DataFrame instead of polars.

    Returns:
        A polars (or pandas) DataFrame; seasons with no published asset are
        skipped with a warning rather than raising (404-safe).

        |col_name                    |type    |
        |:---------------------------|:-------|
        |home_away                   |String  |
        |team_id                     |Int64   |
        |team_abbrev                 |String  |
        |player_id                   |Int64   |
        |player_name                 |String  |
        |sweater_number              |Int64   |
        |position                    |String  |
        |goals                       |Int64   |
        |assists                     |Int64   |
        |points                      |Int64   |
        |plus_minus                  |Int64   |
        |pim                         |Int64   |
        |hits                        |Int64   |
        |power_play_goals            |Int64   |
        |shots_on_goal               |Int64   |
        |faceoff_winning_pctg        |Float64 |
        |toi                         |String  |
        |blocked_shots               |Int64   |
        |shifts                      |Int64   |
        |giveaways                   |Int64   |
        |takeaways                   |Int64   |
        |game_id                     |Int64   |
        |season                      |Int64   |
        |game_date                   |String  |
        |even_strength_shots_against |String  |
        |power_play_shots_against    |String  |
        |shorthanded_shots_against   |String  |
        |save_shots_against          |String  |
        |save_pctg                   |Float64 |
        |even_strength_goals_against |Int64   |
        |power_play_goals_against    |Int64   |
        |shorthanded_goals_against   |Int64   |
        |goals_against               |Int64   |
        |starter                     |Boolean |
        |decision                    |String  |
        |shots_against               |Int64   |
        |saves                       |Int64   |

    Raises:
        SeasonNotFoundError: if a requested season is below 2010.

    Example:
        Quick start::

            load_nhl_player_boxscore(seasons=2024)
    """
    frames, missing = [], []
    for season in _as_season_list(seasons):
        if int(season) < 2010:
            raise SeasonNotFoundError("season cannot be less than 2010")
        df = _read_release_parquet(
            f"https://github.com/sportsdataverse/sportsdataverse-data/releases/download/nhl_player_boxscores/player_box_{season}.parquet"
        )
        if df is None:
            missing.append(season)
            continue
        frames.append(df)
    if missing:
        cli_warn("load_nhl_player_boxscore: no data for season(s) {missing} (skipped)".format(missing=missing))
    # diagonal: per-season release schemas can drift (columns added/dropped
    # over the years) -- union columns, null-fill gaps.
    out = pl.concat(frames, how="diagonal_relaxed") if frames else pl.DataFrame()
    return out.to_pandas(use_pyarrow_extension_array=True) if return_as_pandas else out


def load_nhl_schedule(seasons, return_as_pandas: bool = False):
    """Load nhl_schedules (sportsdataverse-data release).

    Source: https://github.com/sportsdataverse/sportsdataverse-data/releases/tag/nhl_schedules

    Args:
        seasons: an int or iterable of seasons (>= 2010).
        return_as_pandas: return a pandas DataFrame instead of polars.

    Returns:
        A polars (or pandas) DataFrame; seasons with no published asset are
        skipped with a warning rather than raising (404-safe).

        |col_name           |type    |
        |:------------------|:-------|
        |game_id            |Int32   |
        |season_full        |String  |
        |game_type          |String  |
        |game_date          |String  |
        |game_time          |String  |
        |home_team_abbr     |String  |
        |away_team_abbr     |String  |
        |home_team_name     |String  |
        |away_team_name     |String  |
        |home_score         |Int32   |
        |away_score         |Int32   |
        |game_state         |String  |
        |venue              |String  |
        |series_letter      |String  |
        |playoff_round      |Int32   |
        |series_game_number |Int32   |
        |season             |Int32   |
        |game_json          |Boolean |
        |game_json_url      |String  |
        |PBP                |Boolean |
        |team_box           |Boolean |
        |player_box         |Boolean |
        |skater_box         |Boolean |
        |goalie_box         |Boolean |
        |game_info          |Boolean |
        |game_rosters       |Boolean |
        |scoring            |Boolean |
        |penalties          |Boolean |
        |scratches          |Boolean |
        |linescore          |Boolean |
        |three_stars        |Boolean |
        |shifts             |Boolean |
        |officials          |Boolean |
        |shots_by_period    |Boolean |
        |shootout           |Boolean |

    Raises:
        SeasonNotFoundError: if a requested season is below 2010.

    Example:
        Quick start::

            load_nhl_schedule(seasons=2024)
    """
    frames, missing = [], []
    for season in _as_season_list(seasons):
        if int(season) < 2010:
            raise SeasonNotFoundError("season cannot be less than 2010")
        df = _read_release_parquet(
            f"https://github.com/sportsdataverse/sportsdataverse-data/releases/download/nhl_schedules/nhl_schedule_{season}.parquet"
        )
        if df is None:
            missing.append(season)
            continue
        frames.append(df)
    if missing:
        cli_warn("load_nhl_schedule: no data for season(s) {missing} (skipped)".format(missing=missing))
    # diagonal: per-season release schemas can drift (columns added/dropped
    # over the years) -- union columns, null-fill gaps.
    out = pl.concat(frames, how="diagonal_relaxed") if frames else pl.DataFrame()
    return out.to_pandas(use_pyarrow_extension_array=True) if return_as_pandas else out


def load_nhl_team_boxscore(seasons, return_as_pandas: bool = False):
    """Load nhl_team_boxscores (sportsdataverse-data release).

    Source: https://github.com/sportsdataverse/sportsdataverse-data/releases/tag/nhl_team_boxscores

    Args:
        seasons: an int or iterable of seasons (>= 2010).
        return_as_pandas: return a pandas DataFrame instead of polars.

    Returns:
        A polars (or pandas) DataFrame; seasons with no published asset are
        skipped with a warning rather than raising (404-safe).

        |col_name         |type    |
        |:----------------|:-------|
        |home_away        |String  |
        |team_id          |Int64   |
        |team_abbrev      |String  |
        |team_name        |String  |
        |goals            |Int64   |
        |shots_on_goal    |Int64   |
        |pim              |Int64   |
        |hits             |Int64   |
        |blocked_shots    |Int64   |
        |giveaways        |Int64   |
        |takeaways        |Int64   |
        |power_play_goals |Int64   |
        |faceoff_win_pctg |Float64 |
        |saves            |Int64   |
        |save_pctg        |Float64 |
        |goals_against    |Int64   |
        |game_id          |Int64   |
        |season           |Int64   |
        |game_date        |String  |

    Raises:
        SeasonNotFoundError: if a requested season is below 2010.

    Example:
        Quick start::

            load_nhl_team_boxscore(seasons=2024)
    """
    frames, missing = [], []
    for season in _as_season_list(seasons):
        if int(season) < 2010:
            raise SeasonNotFoundError("season cannot be less than 2010")
        df = _read_release_parquet(
            f"https://github.com/sportsdataverse/sportsdataverse-data/releases/download/nhl_team_boxscores/team_box_{season}.parquet"
        )
        if df is None:
            missing.append(season)
            continue
        frames.append(df)
    if missing:
        cli_warn("load_nhl_team_boxscore: no data for season(s) {missing} (skipped)".format(missing=missing))
    # diagonal: per-season release schemas can drift (columns added/dropped
    # over the years) -- union columns, null-fill gaps.
    out = pl.concat(frames, how="diagonal_relaxed") if frames else pl.DataFrame()
    return out.to_pandas(use_pyarrow_extension_array=True) if return_as_pandas else out


def load_nhl_game_info(seasons, return_as_pandas: bool = False):
    """Load nhl_game_info (sportsdataverse-data release).

    Source: https://github.com/sportsdataverse/sportsdataverse-data/releases/tag/nhl_game_info

    Args:
        seasons: an int or iterable of seasons (>= 2024).
        return_as_pandas: return a pandas DataFrame instead of polars.

    Returns:
        A polars (or pandas) DataFrame; seasons with no published asset are
        skipped with a warning rather than raising (404-safe).

        |col_name       |type   |
        |:--------------|:------|
        |game_id        |Int64  |
        |season         |Int64  |
        |game_type      |String |
        |game_date      |String |
        |venue          |String |
        |home_team_abbr |String |
        |away_team_abbr |String |
        |home_score     |Int64  |
        |away_score     |Int64  |
        |game_state     |String |

    Raises:
        SeasonNotFoundError: if a requested season is below 2024.

    Example:
        Quick start::

            load_nhl_game_info(seasons=2024)
    """
    frames, missing = [], []
    for season in _as_season_list(seasons):
        if int(season) < 2024:
            raise SeasonNotFoundError("season cannot be less than 2024")
        df = _read_release_parquet(
            f"https://github.com/sportsdataverse/sportsdataverse-data/releases/download/nhl_game_info/game_info_{season}.parquet"
        )
        if df is None:
            missing.append(season)
            continue
        frames.append(df)
    if missing:
        cli_warn("load_nhl_game_info: no data for season(s) {missing} (skipped)".format(missing=missing))
    # diagonal: per-season release schemas can drift (columns added/dropped
    # over the years) -- union columns, null-fill gaps.
    out = pl.concat(frames, how="diagonal_relaxed") if frames else pl.DataFrame()
    return out.to_pandas(use_pyarrow_extension_array=True) if return_as_pandas else out


def load_nhl_game_rosters(seasons, return_as_pandas: bool = False):
    """Load nhl_game_rosters (sportsdataverse-data release).

    Source: https://github.com/sportsdataverse/sportsdataverse-data/releases/tag/nhl_game_rosters

    Args:
        seasons: an int or iterable of seasons (>= 2024).
        return_as_pandas: return a pandas DataFrame instead of polars.

    Returns:
        A polars (or pandas) DataFrame; seasons with no published asset are
        skipped with a warning rather than raising (404-safe).

        |col_name       |type   |
        |:--------------|:------|
        |player_id      |Int64  |
        |full_name      |String |
        |first_name     |String |
        |last_name      |String |
        |team_abbr      |String |
        |team_id        |Int64  |
        |position_code  |String |
        |sweater_number |Int64  |
        |game_id        |Int64  |
        |season         |Int64  |
        |game_date      |String |

    Raises:
        SeasonNotFoundError: if a requested season is below 2024.

    Example:
        Quick start::

            load_nhl_game_rosters(seasons=2024)
    """
    frames, missing = [], []
    for season in _as_season_list(seasons):
        if int(season) < 2024:
            raise SeasonNotFoundError("season cannot be less than 2024")
        df = _read_release_parquet(
            f"https://github.com/sportsdataverse/sportsdataverse-data/releases/download/nhl_game_rosters/game_rosters_{season}.parquet"
        )
        if df is None:
            missing.append(season)
            continue
        frames.append(df)
    if missing:
        cli_warn("load_nhl_game_rosters: no data for season(s) {missing} (skipped)".format(missing=missing))
    # diagonal: per-season release schemas can drift (columns added/dropped
    # over the years) -- union columns, null-fill gaps.
    out = pl.concat(frames, how="diagonal_relaxed") if frames else pl.DataFrame()
    return out.to_pandas(use_pyarrow_extension_array=True) if return_as_pandas else out


def load_nhl_goalie_boxscores(seasons, return_as_pandas: bool = False):
    """Load nhl_goalie_boxscores (sportsdataverse-data release).

    Source: https://github.com/sportsdataverse/sportsdataverse-data/releases/tag/nhl_goalie_boxscores

    Args:
        seasons: an int or iterable of seasons (>= 2024).
        return_as_pandas: return a pandas DataFrame instead of polars.

    Returns:
        A polars (or pandas) DataFrame; seasons with no published asset are
        skipped with a warning rather than raising (404-safe).

        |col_name                    |type    |
        |:---------------------------|:-------|
        |home_away                   |String  |
        |team_id                     |Int64   |
        |team_abbrev                 |String  |
        |player_id                   |Int64   |
        |player_name                 |String  |
        |sweater_number              |Int64   |
        |even_strength_shots_against |String  |
        |power_play_shots_against    |String  |
        |shorthanded_shots_against   |String  |
        |save_shots_against          |String  |
        |save_pctg                   |Float64 |
        |even_strength_goals_against |Int64   |
        |power_play_goals_against    |Int64   |
        |shorthanded_goals_against   |Int64   |
        |pim                         |Int64   |
        |goals_against               |Int64   |
        |toi                         |String  |
        |starter                     |Boolean |
        |decision                    |String  |
        |shots_against               |Int64   |
        |saves                       |Int64   |
        |game_id                     |Int64   |
        |season                      |Int64   |
        |game_date                   |String  |

    Raises:
        SeasonNotFoundError: if a requested season is below 2024.

    Example:
        Quick start::

            load_nhl_goalie_boxscores(seasons=2024)
    """
    frames, missing = [], []
    for season in _as_season_list(seasons):
        if int(season) < 2024:
            raise SeasonNotFoundError("season cannot be less than 2024")
        df = _read_release_parquet(
            f"https://github.com/sportsdataverse/sportsdataverse-data/releases/download/nhl_goalie_boxscores/goalie_box_{season}.parquet"
        )
        if df is None:
            missing.append(season)
            continue
        frames.append(df)
    if missing:
        cli_warn("load_nhl_goalie_boxscores: no data for season(s) {missing} (skipped)".format(missing=missing))
    # diagonal: per-season release schemas can drift (columns added/dropped
    # over the years) -- union columns, null-fill gaps.
    out = pl.concat(frames, how="diagonal_relaxed") if frames else pl.DataFrame()
    return out.to_pandas(use_pyarrow_extension_array=True) if return_as_pandas else out


def load_nhl_linescore(seasons, return_as_pandas: bool = False):
    """Load nhl_linescore (sportsdataverse-data release).

    Source: https://github.com/sportsdataverse/sportsdataverse-data/releases/tag/nhl_linescore

    Args:
        seasons: an int or iterable of seasons (>= 2024).
        return_as_pandas: return a pandas DataFrame instead of polars.

    Returns:
        A polars (or pandas) DataFrame; seasons with no published asset are
        skipped with a warning rather than raising (404-safe).

        |col_name       |type    |
        |:--------------|:-------|
        |game_id        |Int64   |
        |home_team_id   |Int64   |
        |home_team_abbr |String  |
        |home_goals     |Int64   |
        |home_shots     |Int64   |
        |away_team_id   |Int64   |
        |away_team_abbr |String  |
        |away_goals     |Int64   |
        |away_shots     |Int64   |
        |has_shootout   |Boolean |

    Raises:
        SeasonNotFoundError: if a requested season is below 2024.

    Example:
        Quick start::

            load_nhl_linescore(seasons=2024)
    """
    frames, missing = [], []
    for season in _as_season_list(seasons):
        if int(season) < 2024:
            raise SeasonNotFoundError("season cannot be less than 2024")
        df = _read_release_parquet(
            f"https://github.com/sportsdataverse/sportsdataverse-data/releases/download/nhl_linescore/linescore_{season}.parquet"
        )
        if df is None:
            missing.append(season)
            continue
        frames.append(df)
    if missing:
        cli_warn("load_nhl_linescore: no data for season(s) {missing} (skipped)".format(missing=missing))
    # diagonal: per-season release schemas can drift (columns added/dropped
    # over the years) -- union columns, null-fill gaps.
    out = pl.concat(frames, how="diagonal_relaxed") if frames else pl.DataFrame()
    return out.to_pandas(use_pyarrow_extension_array=True) if return_as_pandas else out


def load_nhl_officials(seasons, return_as_pandas: bool = False):
    """Load nhl_officials (sportsdataverse-data release).

    Source: https://github.com/sportsdataverse/sportsdataverse-data/releases/tag/nhl_officials

    Args:
        seasons: an int or iterable of seasons (>= 2025).
        return_as_pandas: return a pandas DataFrame instead of polars.

    Returns:
        A polars (or pandas) DataFrame; seasons with no published asset are
        skipped with a warning rather than raising (404-safe).

        |col_name  |type   |
        |:---------|:------|
        |role      |String |
        |name      |String |
        |game_id   |Int64  |
        |season    |Int64  |
        |game_date |String |

    Raises:
        SeasonNotFoundError: if a requested season is below 2025.

    Example:
        Quick start::

            load_nhl_officials(seasons=2025)
    """
    frames, missing = [], []
    for season in _as_season_list(seasons):
        if int(season) < 2025:
            raise SeasonNotFoundError("season cannot be less than 2025")
        df = _read_release_parquet(
            f"https://github.com/sportsdataverse/sportsdataverse-data/releases/download/nhl_officials/officials_{season}.parquet"
        )
        if df is None:
            missing.append(season)
            continue
        frames.append(df)
    if missing:
        cli_warn("load_nhl_officials: no data for season(s) {missing} (skipped)".format(missing=missing))
    # diagonal: per-season release schemas can drift (columns added/dropped
    # over the years) -- union columns, null-fill gaps.
    out = pl.concat(frames, how="diagonal_relaxed") if frames else pl.DataFrame()
    return out.to_pandas(use_pyarrow_extension_array=True) if return_as_pandas else out


def load_nhl_pbp_full(seasons, return_as_pandas: bool = False):
    """Load nhl_pbp_full (sportsdataverse-data release).

    Source: https://github.com/sportsdataverse/sportsdataverse-data/releases/tag/nhl_pbp_full

    Args:
        seasons: an int or iterable of seasons (>= 2010).
        return_as_pandas: return a pandas DataFrame instead of polars.

    Returns:
        A polars (or pandas) DataFrame; seasons with no published asset are
        skipped with a warning rather than raising (404-safe).

        |col_name                 |type    |
        |:------------------------|:-------|
        |event_type               |String  |
        |event                    |String  |
        |secondary_type           |String  |
        |event_team_abbr          |String  |
        |event_team_type          |String  |
        |description              |String  |
        |period                   |Int64   |
        |period_type              |String  |
        |period_time              |String  |
        |period_seconds           |Int64   |
        |period_seconds_remaining |Int64   |
        |period_time_remaining    |String  |
        |game_seconds             |Int64   |
        |game_seconds_remaining   |Int64   |
        |home_score               |Int64   |
        |away_score               |Int64   |
        |event_player_1_name      |String  |
        |event_player_1_type      |String  |
        |event_player_1_id        |Int64   |
        |event_player_2_name      |String  |
        |event_player_2_type      |String  |
        |event_player_2_id        |Int64   |
        |event_player_3_name      |String  |
        |event_player_3_type      |String  |
        |event_player_3_id        |Int64   |
        |event_goalie_name        |String  |
        |event_goalie_id          |Int64   |
        |penalty_severity         |String  |
        |penalty_minutes          |Int64   |
        |strength_state           |String  |
        |strength_code            |String  |
        |strength                 |String  |
        |empty_net                |Boolean |
        |extra_attacker           |Boolean |
        |x                        |Int64   |
        |y                        |Int64   |
        |x_fixed                  |Int64   |
        |y_fixed                  |Int64   |
        |shot_distance            |Float64 |
        |shot_angle               |Float64 |
        |home_skaters             |Int64   |
        |away_skaters             |Int64   |
        |home_on_1                |String  |
        |home_on_2                |String  |
        |home_on_3                |String  |
        |home_on_4                |String  |
        |home_on_5                |String  |
        |home_on_6                |String  |
        |home_on_7                |String  |
        |away_on_1                |String  |
        |away_on_2                |String  |
        |away_on_3                |String  |
        |away_on_4                |String  |
        |away_on_5                |String  |
        |away_on_6                |String  |
        |away_on_7                |String  |
        |home_goalie              |String  |
        |away_goalie              |String  |
        |num_on                   |Int64   |
        |players_on               |String  |
        |num_off                  |Int64   |
        |players_off              |String  |
        |game_id                  |Int64   |
        |season                   |String  |
        |season_type              |String  |
        |home_abbr                |String  |
        |away_abbr                |String  |
        |event_idx                |Int64   |
        |event_id                 |Int64   |
        |pptReplayUrl             |String  |
        |away_goalie_in           |Int64   |
        |home_goalie_in           |Int64   |
        |reason                   |String  |
        |secondaryReason          |String  |
        |ids_on                   |String  |
        |ids_off                  |String  |
        |home_on_1_id             |Int64   |
        |away_on_1_id             |Int64   |
        |home_on_2_id             |Int64   |
        |away_on_2_id             |Int64   |
        |home_on_3_id             |Int64   |
        |away_on_3_id             |Int64   |
        |home_on_4_id             |Int64   |
        |away_on_4_id             |Int64   |
        |home_on_5_id             |Int64   |
        |away_on_5_id             |Int64   |
        |home_on_6_id             |Int64   |
        |away_on_6_id             |Int64   |
        |home_on_7_id             |Int64   |
        |away_on_7_id             |Int64   |
        |home_goalie_id           |Int64   |
        |away_goalie_id           |Int64   |
        |xg                       |Float64 |
        |game_date                |String  |

    Raises:
        SeasonNotFoundError: if a requested season is below 2010.

    Example:
        Quick start::

            load_nhl_pbp_full(seasons=2010)
    """
    frames, missing = [], []
    for season in _as_season_list(seasons):
        if int(season) < 2010:
            raise SeasonNotFoundError("season cannot be less than 2010")
        df = _read_release_parquet(
            f"https://github.com/sportsdataverse/sportsdataverse-data/releases/download/nhl_pbp_full/play_by_play_{season}.parquet"
        )
        if df is None:
            missing.append(season)
            continue
        frames.append(df)
    if missing:
        cli_warn("load_nhl_pbp_full: no data for season(s) {missing} (skipped)".format(missing=missing))
    # diagonal: per-season release schemas can drift (columns added/dropped
    # over the years) -- union columns, null-fill gaps.
    out = pl.concat(frames, how="diagonal_relaxed") if frames else pl.DataFrame()
    return out.to_pandas(use_pyarrow_extension_array=True) if return_as_pandas else out


def load_nhl_pbp_lite(seasons, return_as_pandas: bool = False):
    """Load nhl_pbp_lite (sportsdataverse-data release).

    Source: https://github.com/sportsdataverse/sportsdataverse-data/releases/tag/nhl_pbp_lite

    Args:
        seasons: an int or iterable of seasons (>= 2010).
        return_as_pandas: return a pandas DataFrame instead of polars.

    Returns:
        A polars (or pandas) DataFrame; seasons with no published asset are
        skipped with a warning rather than raising (404-safe).

        |col_name                 |type    |
        |:------------------------|:-------|
        |event_type               |String  |
        |event                    |String  |
        |secondary_type           |String  |
        |event_team_abbr          |String  |
        |event_team_type          |String  |
        |description              |String  |
        |period                   |Int32   |
        |period_type              |String  |
        |period_time              |String  |
        |period_seconds           |Int32   |
        |period_seconds_remaining |Int32   |
        |period_time_remaining    |String  |
        |game_seconds             |Int32   |
        |game_seconds_remaining   |Int32   |
        |home_score               |Int32   |
        |away_score               |Int32   |
        |event_player_1_name      |String  |
        |event_player_1_type      |String  |
        |event_player_1_id        |Int32   |
        |event_player_2_name      |String  |
        |event_player_2_type      |String  |
        |event_player_2_id        |Int32   |
        |event_player_3_name      |String  |
        |event_player_3_type      |String  |
        |event_player_3_id        |Int32   |
        |event_goalie_name        |String  |
        |event_goalie_id          |Int32   |
        |penalty_severity         |String  |
        |penalty_minutes          |Int32   |
        |strength_state           |String  |
        |strength_code            |String  |
        |strength                 |String  |
        |empty_net                |Boolean |
        |extra_attacker           |Boolean |
        |x                        |Int32   |
        |y                        |Int32   |
        |x_fixed                  |Int32   |
        |y_fixed                  |Int32   |
        |shot_distance            |Float64 |
        |shot_angle               |Float64 |
        |home_skaters             |Int32   |
        |away_skaters             |Int32   |
        |home_on_1                |String  |
        |home_on_2                |String  |
        |home_on_3                |String  |
        |home_on_4                |String  |
        |home_on_5                |String  |
        |home_on_6                |String  |
        |home_on_7                |String  |
        |away_on_1                |String  |
        |away_on_2                |String  |
        |away_on_3                |String  |
        |away_on_4                |String  |
        |away_on_5                |String  |
        |away_on_6                |String  |
        |away_on_7                |String  |
        |home_goalie              |String  |
        |away_goalie              |String  |
        |num_on                   |Int32   |
        |players_on               |String  |
        |num_off                  |Int32   |
        |players_off              |String  |
        |game_id                  |Int32   |
        |season                   |Int32   |
        |season_type              |String  |
        |home_abbr                |String  |
        |away_abbr                |String  |
        |event_idx                |Int32   |
        |event_id                 |Int32   |
        |pptReplayUrl             |String  |
        |away_goalie_in           |Int32   |
        |home_goalie_in           |Int32   |
        |reason                   |String  |
        |secondaryReason          |String  |
        |ids_on                   |String  |
        |ids_off                  |String  |
        |home_on_1_id             |Int32   |
        |away_on_1_id             |Int32   |
        |home_on_2_id             |Int32   |
        |away_on_2_id             |Int32   |
        |home_on_3_id             |Int32   |
        |away_on_3_id             |Int32   |
        |home_on_4_id             |Int32   |
        |away_on_4_id             |Int32   |
        |home_on_5_id             |Int32   |
        |away_on_5_id             |Int32   |
        |home_on_6_id             |Int32   |
        |away_on_6_id             |Int32   |
        |home_on_7_id             |Int32   |
        |away_on_7_id             |Int32   |
        |home_goalie_id           |Int32   |
        |away_goalie_id           |Int32   |
        |xg                       |Float64 |

    Raises:
        SeasonNotFoundError: if a requested season is below 2010.

    Example:
        Quick start::

            load_nhl_pbp_lite(seasons=2010)
    """
    frames, missing = [], []
    for season in _as_season_list(seasons):
        if int(season) < 2010:
            raise SeasonNotFoundError("season cannot be less than 2010")
        df = _read_release_parquet(
            f"https://github.com/sportsdataverse/sportsdataverse-data/releases/download/nhl_pbp_lite/play_by_play_{season}_lite.parquet"
        )
        if df is None:
            missing.append(season)
            continue
        frames.append(df)
    if missing:
        cli_warn("load_nhl_pbp_lite: no data for season(s) {missing} (skipped)".format(missing=missing))
    # diagonal: per-season release schemas can drift (columns added/dropped
    # over the years) -- union columns, null-fill gaps.
    out = pl.concat(frames, how="diagonal_relaxed") if frames else pl.DataFrame()
    return out.to_pandas(use_pyarrow_extension_array=True) if return_as_pandas else out


def load_nhl_penalties(seasons, return_as_pandas: bool = False):
    """Load nhl_penalties (sportsdataverse-data release).

    Source: https://github.com/sportsdataverse/sportsdataverse-data/releases/tag/nhl_penalties

    Args:
        seasons: an int or iterable of seasons (>= 2024).
        return_as_pandas: return a pandas DataFrame instead of polars.

    Returns:
        A polars (or pandas) DataFrame; seasons with no published asset are
        skipped with a warning rather than raising (404-safe).

        |col_name                            |type   |
        |:-----------------------------------|:------|
        |timeInPeriod                        |String |
        |type                                |String |
        |duration                            |Int64  |
        |committedByPlayer.firstName.default |String |
        |committedByPlayer.firstName.cs      |String |
        |committedByPlayer.firstName.de      |String |
        |committedByPlayer.firstName.es      |String |
        |committedByPlayer.firstName.fi      |String |
        |committedByPlayer.firstName.sk      |String |
        |committedByPlayer.firstName.sv      |String |
        |committedByPlayer.firstName.fr      |String |
        |committedByPlayer.lastName.default  |String |
        |committedByPlayer.lastName.cs       |String |
        |committedByPlayer.lastName.de       |String |
        |committedByPlayer.lastName.es       |String |
        |committedByPlayer.lastName.fi       |String |
        |committedByPlayer.lastName.sk       |String |
        |committedByPlayer.lastName.sv       |String |
        |committedByPlayer.sweaterNumber     |Int64  |
        |teamAbbrev.default                  |String |
        |drawnBy.firstName.default           |String |
        |drawnBy.firstName.cs                |String |
        |drawnBy.firstName.de                |String |
        |drawnBy.firstName.es                |String |
        |drawnBy.firstName.fi                |String |
        |drawnBy.firstName.sk                |String |
        |drawnBy.firstName.sv                |String |
        |drawnBy.firstName.fr                |String |
        |drawnBy.lastName.default            |String |
        |drawnBy.lastName.cs                 |String |
        |drawnBy.lastName.de                 |String |
        |drawnBy.lastName.es                 |String |
        |drawnBy.lastName.fi                 |String |
        |drawnBy.lastName.sk                 |String |
        |drawnBy.lastName.sv                 |String |
        |drawnBy.sweaterNumber               |Int64  |
        |descKey                             |String |
        |game_id                             |Int64  |
        |period_number                       |Int64  |
        |period_type                         |String |
        |servedBy.default                    |String |
        |servedBy.cs                         |String |
        |servedBy.fi                         |String |
        |servedBy.sk                         |String |
        |servedBy.sv                         |String |

    Raises:
        SeasonNotFoundError: if a requested season is below 2024.

    Example:
        Quick start::

            load_nhl_penalties(seasons=2024)
    """
    frames, missing = [], []
    for season in _as_season_list(seasons):
        if int(season) < 2024:
            raise SeasonNotFoundError("season cannot be less than 2024")
        df = _read_release_parquet(
            f"https://github.com/sportsdataverse/sportsdataverse-data/releases/download/nhl_penalties/penalties_{season}.parquet"
        )
        if df is None:
            missing.append(season)
            continue
        frames.append(df)
    if missing:
        cli_warn("load_nhl_penalties: no data for season(s) {missing} (skipped)".format(missing=missing))
    # diagonal: per-season release schemas can drift (columns added/dropped
    # over the years) -- union columns, null-fill gaps.
    out = pl.concat(frames, how="diagonal_relaxed") if frames else pl.DataFrame()
    return out.to_pandas(use_pyarrow_extension_array=True) if return_as_pandas else out


def load_nhl_player_boxscores(seasons, return_as_pandas: bool = False):
    """Load nhl_player_boxscores (sportsdataverse-data release).

    Source: https://github.com/sportsdataverse/sportsdataverse-data/releases/tag/nhl_player_boxscores

    Args:
        seasons: an int or iterable of seasons (>= 2010).
        return_as_pandas: return a pandas DataFrame instead of polars.

    Returns:
        A polars (or pandas) DataFrame; seasons with no published asset are
        skipped with a warning rather than raising (404-safe).

        |col_name                    |type    |
        |:---------------------------|:-------|
        |home_away                   |String  |
        |team_id                     |Int64   |
        |team_abbrev                 |String  |
        |player_id                   |Int64   |
        |player_name                 |String  |
        |sweater_number              |Int64   |
        |position                    |String  |
        |goals                       |Int64   |
        |assists                     |Int64   |
        |points                      |Int64   |
        |plus_minus                  |Int64   |
        |pim                         |Int64   |
        |hits                        |Int64   |
        |power_play_goals            |Int64   |
        |shots_on_goal               |Int64   |
        |faceoff_winning_pctg        |Float64 |
        |toi                         |String  |
        |blocked_shots               |Int64   |
        |shifts                      |Int64   |
        |giveaways                   |Int64   |
        |takeaways                   |Int64   |
        |game_id                     |Int64   |
        |season                      |Int64   |
        |game_date                   |String  |
        |even_strength_shots_against |String  |
        |power_play_shots_against    |String  |
        |shorthanded_shots_against   |String  |
        |save_shots_against          |String  |
        |save_pctg                   |Float64 |
        |even_strength_goals_against |Int64   |
        |power_play_goals_against    |Int64   |
        |shorthanded_goals_against   |Int64   |
        |goals_against               |Int64   |
        |starter                     |Boolean |
        |decision                    |String  |
        |shots_against               |Int64   |
        |saves                       |Int64   |

    Raises:
        SeasonNotFoundError: if a requested season is below 2010.

    Example:
        Quick start::

            load_nhl_player_boxscores(seasons=2010)
    """
    frames, missing = [], []
    for season in _as_season_list(seasons):
        if int(season) < 2010:
            raise SeasonNotFoundError("season cannot be less than 2010")
        df = _read_release_parquet(
            f"https://github.com/sportsdataverse/sportsdataverse-data/releases/download/nhl_player_boxscores/player_box_{season}.parquet"
        )
        if df is None:
            missing.append(season)
            continue
        frames.append(df)
    if missing:
        cli_warn("load_nhl_player_boxscores: no data for season(s) {missing} (skipped)".format(missing=missing))
    # diagonal: per-season release schemas can drift (columns added/dropped
    # over the years) -- union columns, null-fill gaps.
    out = pl.concat(frames, how="diagonal_relaxed") if frames else pl.DataFrame()
    return out.to_pandas(use_pyarrow_extension_array=True) if return_as_pandas else out


def load_nhl_rosters(seasons, return_as_pandas: bool = False):
    """Load nhl_rosters (sportsdataverse-data release).

    Source: https://github.com/sportsdataverse/sportsdataverse-data/releases/tag/nhl_rosters

    Args:
        seasons: an int or iterable of seasons (>= 2010).
        return_as_pandas: return a pandas DataFrame instead of polars.

    Returns:
        A polars (or pandas) DataFrame; seasons with no published asset are
        skipped with a warning rather than raising (404-safe).

        |col_name       |type   |
        |:--------------|:------|
        |player_id      |Int32  |
        |full_name      |String |
        |first_name     |String |
        |last_name      |String |
        |team_abbr      |String |
        |team_id        |Int32  |
        |position_code  |String |
        |sweater_number |Int32  |
        |season         |Int32  |
        |game_date      |String |

    Raises:
        SeasonNotFoundError: if a requested season is below 2010.

    Example:
        Quick start::

            load_nhl_rosters(seasons=2010)
    """
    frames, missing = [], []
    for season in _as_season_list(seasons):
        if int(season) < 2010:
            raise SeasonNotFoundError("season cannot be less than 2010")
        df = _read_release_parquet(
            f"https://github.com/sportsdataverse/sportsdataverse-data/releases/download/nhl_rosters/rosters_{season}.parquet"
        )
        if df is None:
            missing.append(season)
            continue
        frames.append(df)
    if missing:
        cli_warn("load_nhl_rosters: no data for season(s) {missing} (skipped)".format(missing=missing))
    # diagonal: per-season release schemas can drift (columns added/dropped
    # over the years) -- union columns, null-fill gaps.
    out = pl.concat(frames, how="diagonal_relaxed") if frames else pl.DataFrame()
    return out.to_pandas(use_pyarrow_extension_array=True) if return_as_pandas else out


def load_nhl_schedules(seasons, return_as_pandas: bool = False):
    """Load nhl_schedules (sportsdataverse-data release).

    Source: https://github.com/sportsdataverse/sportsdataverse-data/releases/tag/nhl_schedules

    Args:
        seasons: an int or iterable of seasons (>= 2010).
        return_as_pandas: return a pandas DataFrame instead of polars.

    Returns:
        A polars (or pandas) DataFrame; seasons with no published asset are
        skipped with a warning rather than raising (404-safe).

        |col_name           |type    |
        |:------------------|:-------|
        |game_id            |Int32   |
        |season_full        |String  |
        |game_type          |String  |
        |game_date          |String  |
        |game_time          |String  |
        |home_team_abbr     |String  |
        |away_team_abbr     |String  |
        |home_team_name     |String  |
        |away_team_name     |String  |
        |home_score         |Int32   |
        |away_score         |Int32   |
        |game_state         |String  |
        |venue              |String  |
        |series_letter      |String  |
        |playoff_round      |Int32   |
        |series_game_number |Int32   |
        |season             |Int32   |
        |game_json          |Boolean |
        |game_json_url      |String  |
        |PBP                |Boolean |
        |team_box           |Boolean |
        |player_box         |Boolean |
        |skater_box         |Boolean |
        |goalie_box         |Boolean |
        |game_info          |Boolean |
        |game_rosters       |Boolean |
        |scoring            |Boolean |
        |penalties          |Boolean |
        |scratches          |Boolean |
        |linescore          |Boolean |
        |three_stars        |Boolean |
        |shifts             |Boolean |
        |officials          |Boolean |
        |shots_by_period    |Boolean |
        |shootout           |Boolean |

    Raises:
        SeasonNotFoundError: if a requested season is below 2010.

    Example:
        Quick start::

            load_nhl_schedules(seasons=2010)
    """
    frames, missing = [], []
    for season in _as_season_list(seasons):
        if int(season) < 2010:
            raise SeasonNotFoundError("season cannot be less than 2010")
        df = _read_release_parquet(
            f"https://github.com/sportsdataverse/sportsdataverse-data/releases/download/nhl_schedules/nhl_schedule_{season}.parquet"
        )
        if df is None:
            missing.append(season)
            continue
        frames.append(df)
    if missing:
        cli_warn("load_nhl_schedules: no data for season(s) {missing} (skipped)".format(missing=missing))
    # diagonal: per-season release schemas can drift (columns added/dropped
    # over the years) -- union columns, null-fill gaps.
    out = pl.concat(frames, how="diagonal_relaxed") if frames else pl.DataFrame()
    return out.to_pandas(use_pyarrow_extension_array=True) if return_as_pandas else out


def load_nhl_scoring(seasons, return_as_pandas: bool = False):
    """Load nhl_scoring (sportsdataverse-data release).

    Source: https://github.com/sportsdataverse/sportsdataverse-data/releases/tag/nhl_scoring

    Args:
        seasons: an int or iterable of seasons (>= 2024).
        return_as_pandas: return a pandas DataFrame instead of polars.

    Returns:
        A polars (or pandas) DataFrame; seasons with no published asset are
        skipped with a warning rather than raising (404-safe).

        |col_name                  |type    |
        |:-------------------------|:-------|
        |situationCode             |String  |
        |eventId                   |Int64   |
        |strength                  |String  |
        |playerId                  |Int64   |
        |firstName.default         |String  |
        |firstName.cs              |String  |
        |firstName.de              |String  |
        |firstName.es              |String  |
        |firstName.fi              |String  |
        |firstName.sk              |String  |
        |firstName.sv              |String  |
        |firstName.fr              |String  |
        |lastName.default          |String  |
        |lastName.cs               |String  |
        |lastName.fi               |String  |
        |lastName.sk               |String  |
        |lastName.de               |String  |
        |lastName.es               |String  |
        |lastName.sv               |String  |
        |name.default              |String  |
        |name.cs                   |String  |
        |name.de                   |String  |
        |name.fi                   |String  |
        |name.sk                   |String  |
        |name.sv                   |String  |
        |teamAbbrev.default        |String  |
        |headshot                  |String  |
        |highlightClipSharingUrl   |String  |
        |highlightClipSharingUrlFr |String  |
        |highlightClip             |Int64   |
        |highlightClipFr           |Int64   |
        |discreteClip              |Int64   |
        |discreteClipFr            |Int64   |
        |goalsToDate               |Int64   |
        |awayScore                 |Int64   |
        |homeScore                 |Int64   |
        |leadingTeamAbbrev.default |String  |
        |timeInPeriod              |String  |
        |shotType                  |String  |
        |goalModifier              |String  |
        |assists                   |String  |
        |pptReplayUrl              |String  |
        |homeTeamDefendingSide     |String  |
        |isHome                    |Boolean |
        |game_id                   |Int64   |
        |period_number             |Int64   |
        |period_type               |String  |
        |goalInGame                |Int64   |

    Raises:
        SeasonNotFoundError: if a requested season is below 2024.

    Example:
        Quick start::

            load_nhl_scoring(seasons=2024)
    """
    frames, missing = [], []
    for season in _as_season_list(seasons):
        if int(season) < 2024:
            raise SeasonNotFoundError("season cannot be less than 2024")
        df = _read_release_parquet(
            f"https://github.com/sportsdataverse/sportsdataverse-data/releases/download/nhl_scoring/scoring_{season}.parquet"
        )
        if df is None:
            missing.append(season)
            continue
        frames.append(df)
    if missing:
        cli_warn("load_nhl_scoring: no data for season(s) {missing} (skipped)".format(missing=missing))
    # diagonal: per-season release schemas can drift (columns added/dropped
    # over the years) -- union columns, null-fill gaps.
    out = pl.concat(frames, how="diagonal_relaxed") if frames else pl.DataFrame()
    return out.to_pandas(use_pyarrow_extension_array=True) if return_as_pandas else out


def load_nhl_scratches(seasons, return_as_pandas: bool = False):
    """Load nhl_scratches (sportsdataverse-data release).

    Source: https://github.com/sportsdataverse/sportsdataverse-data/releases/tag/nhl_scratches

    Args:
        seasons: an int or iterable of seasons (>= 2024).
        return_as_pandas: return a pandas DataFrame instead of polars.

    Returns:
        A polars (or pandas) DataFrame; seasons with no published asset are
        skipped with a warning rather than raising (404-safe).

        |col_name  |type   |
        |:---------|:------|
        |id        |Int64  |
        |firstName |String |
        |lastName  |String |
        |game_id   |Int64  |

    Raises:
        SeasonNotFoundError: if a requested season is below 2024.

    Example:
        Quick start::

            load_nhl_scratches(seasons=2024)
    """
    frames, missing = [], []
    for season in _as_season_list(seasons):
        if int(season) < 2024:
            raise SeasonNotFoundError("season cannot be less than 2024")
        df = _read_release_parquet(
            f"https://github.com/sportsdataverse/sportsdataverse-data/releases/download/nhl_scratches/scratches_{season}.parquet"
        )
        if df is None:
            missing.append(season)
            continue
        frames.append(df)
    if missing:
        cli_warn("load_nhl_scratches: no data for season(s) {missing} (skipped)".format(missing=missing))
    # diagonal: per-season release schemas can drift (columns added/dropped
    # over the years) -- union columns, null-fill gaps.
    out = pl.concat(frames, how="diagonal_relaxed") if frames else pl.DataFrame()
    return out.to_pandas(use_pyarrow_extension_array=True) if return_as_pandas else out


def load_nhl_shifts(seasons, return_as_pandas: bool = False):
    """Load nhl_shifts (sportsdataverse-data release).

    Source: https://github.com/sportsdataverse/sportsdataverse-data/releases/tag/nhl_shifts

    Args:
        seasons: an int or iterable of seasons (>= 2025).
        return_as_pandas: return a pandas DataFrame instead of polars.

    Returns:
        A polars (or pandas) DataFrame; seasons with no published asset are
        skipped with a warning rather than raising (404-safe).

        |col_name               |type   |
        |:----------------------|:------|
        |event_team             |String |
        |period                 |Int64  |
        |period_time            |String |
        |period_seconds         |Int64  |
        |game_seconds           |Int64  |
        |num_on                 |Int64  |
        |players_on             |String |
        |ids_on                 |String |
        |num_off                |Int64  |
        |players_off            |String |
        |ids_off                |String |
        |event                  |String |
        |event_type             |String |
        |game_seconds_remaining |Int64  |
        |game_id                |Int64  |
        |season                 |Int64  |
        |game_date              |String |

    Raises:
        SeasonNotFoundError: if a requested season is below 2025.

    Example:
        Quick start::

            load_nhl_shifts(seasons=2025)
    """
    frames, missing = [], []
    for season in _as_season_list(seasons):
        if int(season) < 2025:
            raise SeasonNotFoundError("season cannot be less than 2025")
        df = _read_release_parquet(
            f"https://github.com/sportsdataverse/sportsdataverse-data/releases/download/nhl_shifts/shifts_{season}.parquet"
        )
        if df is None:
            missing.append(season)
            continue
        frames.append(df)
    if missing:
        cli_warn("load_nhl_shifts: no data for season(s) {missing} (skipped)".format(missing=missing))
    # diagonal: per-season release schemas can drift (columns added/dropped
    # over the years) -- union columns, null-fill gaps.
    out = pl.concat(frames, how="diagonal_relaxed") if frames else pl.DataFrame()
    return out.to_pandas(use_pyarrow_extension_array=True) if return_as_pandas else out


def load_nhl_shootout(seasons, return_as_pandas: bool = False):
    """Load nhl_shootout (sportsdataverse-data release).

    Source: https://github.com/sportsdataverse/sportsdataverse-data/releases/tag/nhl_shootout

    Args:
        seasons: an int or iterable of seasons (>= 2025).
        return_as_pandas: return a pandas DataFrame instead of polars.

    Returns:
        A polars (or pandas) DataFrame; seasons with no published asset are
        skipped with a warning rather than raising (404-safe).

        |col_name                  |type    |
        |:-------------------------|:-------|
        |home                      |Int64   |
        |away                      |Int64   |
        |game_id                   |Int64   |
        |season                    |Int64   |
        |game_date                 |String  |
        |sequence                  |Int64   |
        |playerId                  |Int64   |
        |teamAbbrev.default        |String  |
        |firstName.default         |String  |
        |firstName.cs              |String  |
        |firstName.de              |String  |
        |firstName.es              |String  |
        |firstName.fi              |String  |
        |firstName.sk              |String  |
        |firstName.sv              |String  |
        |lastName.default          |String  |
        |lastName.cs               |String  |
        |lastName.fi               |String  |
        |lastName.sk               |String  |
        |lastName.sv               |String  |
        |shotType                  |String  |
        |result                    |String  |
        |headshot                  |String  |
        |gameWinner                |Boolean |
        |homeScore                 |Int64   |
        |awayScore                 |Int64   |
        |discreteClip              |Int64   |
        |discreteClipFr            |Int64   |
        |highlightClipSharingUrl   |String  |
        |highlightClipSharingUrlFr |String  |
        |highlightClip             |Int64   |
        |highlightClipFr           |Int64   |

    Raises:
        SeasonNotFoundError: if a requested season is below 2025.

    Example:
        Quick start::

            load_nhl_shootout(seasons=2025)
    """
    frames, missing = [], []
    for season in _as_season_list(seasons):
        if int(season) < 2025:
            raise SeasonNotFoundError("season cannot be less than 2025")
        df = _read_release_parquet(
            f"https://github.com/sportsdataverse/sportsdataverse-data/releases/download/nhl_shootout/shootout_summary_{season}.parquet"
        )
        if df is None:
            missing.append(season)
            continue
        frames.append(df)
    if missing:
        cli_warn("load_nhl_shootout: no data for season(s) {missing} (skipped)".format(missing=missing))
    # diagonal: per-season release schemas can drift (columns added/dropped
    # over the years) -- union columns, null-fill gaps.
    out = pl.concat(frames, how="diagonal_relaxed") if frames else pl.DataFrame()
    return out.to_pandas(use_pyarrow_extension_array=True) if return_as_pandas else out


def load_nhl_shots_by_period(seasons, return_as_pandas: bool = False):
    """Load nhl_shots_by_period (sportsdataverse-data release).

    Source: https://github.com/sportsdataverse/sportsdataverse-data/releases/tag/nhl_shots_by_period

    Args:
        seasons: an int or iterable of seasons (>= 2025).
        return_as_pandas: return a pandas DataFrame instead of polars.

    Returns:
        A polars (or pandas) DataFrame; seasons with no published asset are
        skipped with a warning rather than raising (404-safe).

        |col_name               |type   |
        |:----------------------|:------|
        |period                 |Int64  |
        |period_type            |String |
        |max_regulation_periods |Int64  |
        |ot_periods             |Int64  |
        |away                   |Int64  |
        |home                   |Int64  |
        |game_id                |Int64  |
        |season                 |Int64  |
        |game_date              |String |

    Raises:
        SeasonNotFoundError: if a requested season is below 2025.

    Example:
        Quick start::

            load_nhl_shots_by_period(seasons=2025)
    """
    frames, missing = [], []
    for season in _as_season_list(seasons):
        if int(season) < 2025:
            raise SeasonNotFoundError("season cannot be less than 2025")
        df = _read_release_parquet(
            f"https://github.com/sportsdataverse/sportsdataverse-data/releases/download/nhl_shots_by_period/shots_by_period_{season}.parquet"
        )
        if df is None:
            missing.append(season)
            continue
        frames.append(df)
    if missing:
        cli_warn("load_nhl_shots_by_period: no data for season(s) {missing} (skipped)".format(missing=missing))
    # diagonal: per-season release schemas can drift (columns added/dropped
    # over the years) -- union columns, null-fill gaps.
    out = pl.concat(frames, how="diagonal_relaxed") if frames else pl.DataFrame()
    return out.to_pandas(use_pyarrow_extension_array=True) if return_as_pandas else out


def load_nhl_skater_boxscores(seasons, return_as_pandas: bool = False):
    """Load nhl_skater_boxscores (sportsdataverse-data release).

    Source: https://github.com/sportsdataverse/sportsdataverse-data/releases/tag/nhl_skater_boxscores

    Args:
        seasons: an int or iterable of seasons (>= 2024).
        return_as_pandas: return a pandas DataFrame instead of polars.

    Returns:
        A polars (or pandas) DataFrame; seasons with no published asset are
        skipped with a warning rather than raising (404-safe).

        |col_name             |type    |
        |:--------------------|:-------|
        |home_away            |String  |
        |team_id              |Int64   |
        |team_abbrev          |String  |
        |player_id            |Int64   |
        |player_name          |String  |
        |sweater_number       |Int64   |
        |position             |String  |
        |goals                |Int64   |
        |assists              |Int64   |
        |points               |Int64   |
        |plus_minus           |Int64   |
        |pim                  |Int64   |
        |hits                 |Int64   |
        |power_play_goals     |Int64   |
        |shots_on_goal        |Int64   |
        |faceoff_winning_pctg |Float64 |
        |toi                  |String  |
        |blocked_shots        |Int64   |
        |shifts               |Int64   |
        |giveaways            |Int64   |
        |takeaways            |Int64   |
        |game_id              |Int64   |
        |season               |Int64   |
        |game_date            |String  |

    Raises:
        SeasonNotFoundError: if a requested season is below 2024.

    Example:
        Quick start::

            load_nhl_skater_boxscores(seasons=2024)
    """
    frames, missing = [], []
    for season in _as_season_list(seasons):
        if int(season) < 2024:
            raise SeasonNotFoundError("season cannot be less than 2024")
        df = _read_release_parquet(
            f"https://github.com/sportsdataverse/sportsdataverse-data/releases/download/nhl_skater_boxscores/skater_box_{season}.parquet"
        )
        if df is None:
            missing.append(season)
            continue
        frames.append(df)
    if missing:
        cli_warn("load_nhl_skater_boxscores: no data for season(s) {missing} (skipped)".format(missing=missing))
    # diagonal: per-season release schemas can drift (columns added/dropped
    # over the years) -- union columns, null-fill gaps.
    out = pl.concat(frames, how="diagonal_relaxed") if frames else pl.DataFrame()
    return out.to_pandas(use_pyarrow_extension_array=True) if return_as_pandas else out


def load_nhl_team_boxscores(seasons, return_as_pandas: bool = False):
    """Load nhl_team_boxscores (sportsdataverse-data release).

    Source: https://github.com/sportsdataverse/sportsdataverse-data/releases/tag/nhl_team_boxscores

    Args:
        seasons: an int or iterable of seasons (>= 2010).
        return_as_pandas: return a pandas DataFrame instead of polars.

    Returns:
        A polars (or pandas) DataFrame; seasons with no published asset are
        skipped with a warning rather than raising (404-safe).

        |col_name         |type    |
        |:----------------|:-------|
        |home_away        |String  |
        |team_id          |Int64   |
        |team_abbrev      |String  |
        |team_name        |String  |
        |goals            |Int64   |
        |shots_on_goal    |Int64   |
        |pim              |Int64   |
        |hits             |Int64   |
        |blocked_shots    |Int64   |
        |giveaways        |Int64   |
        |takeaways        |Int64   |
        |power_play_goals |Int64   |
        |faceoff_win_pctg |Float64 |
        |saves            |Int64   |
        |save_pctg        |Float64 |
        |goals_against    |Int64   |
        |game_id          |Int64   |
        |season           |Int64   |
        |game_date        |String  |

    Raises:
        SeasonNotFoundError: if a requested season is below 2010.

    Example:
        Quick start::

            load_nhl_team_boxscores(seasons=2010)
    """
    frames, missing = [], []
    for season in _as_season_list(seasons):
        if int(season) < 2010:
            raise SeasonNotFoundError("season cannot be less than 2010")
        df = _read_release_parquet(
            f"https://github.com/sportsdataverse/sportsdataverse-data/releases/download/nhl_team_boxscores/team_box_{season}.parquet"
        )
        if df is None:
            missing.append(season)
            continue
        frames.append(df)
    if missing:
        cli_warn("load_nhl_team_boxscores: no data for season(s) {missing} (skipped)".format(missing=missing))
    # diagonal: per-season release schemas can drift (columns added/dropped
    # over the years) -- union columns, null-fill gaps.
    out = pl.concat(frames, how="diagonal_relaxed") if frames else pl.DataFrame()
    return out.to_pandas(use_pyarrow_extension_array=True) if return_as_pandas else out


def load_nhl_three_stars(seasons, return_as_pandas: bool = False):
    """Load nhl_three_stars (sportsdataverse-data release).

    Source: https://github.com/sportsdataverse/sportsdataverse-data/releases/tag/nhl_three_stars

    Args:
        seasons: an int or iterable of seasons (>= 2024).
        return_as_pandas: return a pandas DataFrame instead of polars.

    Returns:
        A polars (or pandas) DataFrame; seasons with no published asset are
        skipped with a warning rather than raising (404-safe).

        |col_name            |type    |
        |:-------------------|:-------|
        |star                |Int64   |
        |playerId            |Int64   |
        |teamAbbrev          |String  |
        |headshot            |String  |
        |name.default        |String  |
        |name.cs             |String  |
        |name.de             |String  |
        |name.fi             |String  |
        |name.sk             |String  |
        |name.sv             |String  |
        |sweaterNo           |Int64   |
        |position            |String  |
        |goals               |Int64   |
        |assists             |Int64   |
        |points              |Int64   |
        |game_id             |Int64   |
        |winner_id           |Int64   |
        |winner_name         |String  |
        |loser_id            |Int64   |
        |loser_name          |String  |
        |goalsAgainstAverage |Float64 |
        |savePctg            |Float64 |

    Raises:
        SeasonNotFoundError: if a requested season is below 2024.

    Example:
        Quick start::

            load_nhl_three_stars(seasons=2024)
    """
    frames, missing = [], []
    for season in _as_season_list(seasons):
        if int(season) < 2024:
            raise SeasonNotFoundError("season cannot be less than 2024")
        df = _read_release_parquet(
            f"https://github.com/sportsdataverse/sportsdataverse-data/releases/download/nhl_three_stars/three_stars_{season}.parquet"
        )
        if df is None:
            missing.append(season)
            continue
        frames.append(df)
    if missing:
        cli_warn("load_nhl_three_stars: no data for season(s) {missing} (skipped)".format(missing=missing))
    # diagonal: per-season release schemas can drift (columns added/dropped
    # over the years) -- union columns, null-fill gaps.
    out = pl.concat(frames, how="diagonal_relaxed") if frames else pl.DataFrame()
    return out.to_pandas(use_pyarrow_extension_array=True) if return_as_pandas else out
