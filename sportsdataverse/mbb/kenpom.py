# GENERATED by tools/codegen/generate.py -- DO NOT EDIT.
# Source: tools/codegen/endpoints/kenpom.yaml
"""kenpom wrappers (host https://kenpom.com). Generated."""

from __future__ import annotations

from typing import TYPE_CHECKING, Dict, List, Optional, Union  # noqa: F401

from sportsdataverse.mbb.kenpom_runtime import _get
from sportsdataverse.mbb.kenpom_runtime import parse_kenpom_page

if TYPE_CHECKING:  # pragma: no cover -- annotation-only imports (PEP 563 defers eval)
    import pandas as pd
    import polars as pl

__all__ = [
    "kenpom_ratings",
    "kenpom_efficiency",
    "kenpom_four_factors",
    "kenpom_point_distribution",
    "kenpom_height",
    "kenpom_foul_trouble",
    "kenpom_team_stats",
    "kenpom_player_stats",
    "kenpom_kpoy",
    "kenpom_team",
    "kenpom_team_players_expanded",
    "kenpom_game_plan",
    "kenpom_opponent_tracker",
    "kenpom_player_career",
    "kenpom_box",
    "kenpom_win_probability",
    "kenpom_fan_match",
    "kenpom_team_history",
    "kenpom_coach_history",
    "kenpom_program_ratings",
    "kenpom_archive_ratings",
    "kenpom_conference",
    "kenpom_conference_stats",
    "kenpom_conference_history",
    "kenpom_trends",
    "kenpom_home_court_advantage",
    "kenpom_arenas",
    "kenpom_officials",
    "kenpom_referee",
    "kenpom_game_attributes",
]


def kenpom_ratings(
    year: int,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, str]:
    """GET /index.php - Pomeroy season ratings (AdjEM/AdjO/AdjD/AdjT plus SOS, one row per team). Port of hoopR kp_pomeroy_ratings().

    Endpoint: ``GET https://kenpom.com/index.php``
    Example URL: https://kenpom.com/index.php?y=2025

    Args:
        year: Season as a 4-digit ENDING year (2025 = the 2024-25 season). Data begins at 2002.
        headers: optional pre-built request headers to reuse across calls; the authenticated kenpom.com session is resolved and cached when omitted.
        return_parsed: parse the payload through parse_kenpom_page -> dict of polars DataFrames (default True). Pass return_parsed=False for the raw page HTML (``str``).
        return_as_pandas: with return_parsed, return a dict of pandas DataFrames (same keys) instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A ``dict`` of polars DataFrames, one per HTML table on the page, keyed by the table's HTML id (a KenPom page often carries several -- ``team.php`` alone holds the schedule, roster, depth chart and lineup tables) by default; the raw page HTML (``str``) when ``return_parsed=False``.

    Raises:
        RuntimeError: No KenPom credentials could be resolved (pass ``email=``/``password=``, or set ``KENPOM_EMAIL``/``KENPOM_PW``), or kenpom.com rejected them.
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.mbb import kenpom_ratings
            kenpom_ratings(year=2025)

        See Also:
            * `hoopR`_ - R sister package; this is the port of its kp_* family
            * `KenPom`_ - data origin (subscription required)

        .. _hoopR: https://hoopR.sportsdataverse.org
        .. _KenPom: https://kenpom.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "y": year,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://kenpom.com/index.php",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_kenpom_page(raw, return_as_pandas=return_as_pandas)
    return raw


def kenpom_efficiency(
    year: int,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, str]:
    """GET /summary.php - efficiency and tempo summary (adjusted and raw O/D/T, average possession length). Port of hoopR kp_efficiency().

    Endpoint: ``GET https://kenpom.com/summary.php``
    Example URL: https://kenpom.com/summary.php?y=2025

    Args:
        year: Season as a 4-digit ENDING year. Columns are narrower before 2010.
        headers: optional pre-built request headers to reuse across calls; the authenticated kenpom.com session is resolved and cached when omitted.
        return_parsed: parse the payload through parse_kenpom_page -> dict of polars DataFrames (default True). Pass return_parsed=False for the raw page HTML (``str``).
        return_as_pandas: with return_parsed, return a dict of pandas DataFrames (same keys) instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A ``dict`` of polars DataFrames, one per HTML table on the page, keyed by the table's HTML id (a KenPom page often carries several -- ``team.php`` alone holds the schedule, roster, depth chart and lineup tables) by default; the raw page HTML (``str``) when ``return_parsed=False``.

    Raises:
        RuntimeError: No KenPom credentials could be resolved (pass ``email=``/``password=``, or set ``KENPOM_EMAIL``/``KENPOM_PW``), or kenpom.com rejected them.
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.mbb import kenpom_efficiency
            kenpom_efficiency(year=2025)

        See Also:
            * `hoopR`_ - R sister package; this is the port of its kp_* family
            * `KenPom`_ - data origin (subscription required)

        .. _hoopR: https://hoopR.sportsdataverse.org
        .. _KenPom: https://kenpom.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "y": year,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://kenpom.com/summary.php",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_kenpom_page(raw, return_as_pandas=return_as_pandas)
    return raw


def kenpom_four_factors(
    year: int,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, str]:
    """GET /stats.php - four-factors rankings (eFG%, TO%, OR%, FTRate on offense and defense). Port of hoopR kp_fourfactors().

    Endpoint: ``GET https://kenpom.com/stats.php``
    Example URL: https://kenpom.com/stats.php?y=2025

    Args:
        year: Season as a 4-digit ENDING year.
        headers: optional pre-built request headers to reuse across calls; the authenticated kenpom.com session is resolved and cached when omitted.
        return_parsed: parse the payload through parse_kenpom_page -> dict of polars DataFrames (default True). Pass return_parsed=False for the raw page HTML (``str``).
        return_as_pandas: with return_parsed, return a dict of pandas DataFrames (same keys) instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A ``dict`` of polars DataFrames, one per HTML table on the page, keyed by the table's HTML id (a KenPom page often carries several -- ``team.php`` alone holds the schedule, roster, depth chart and lineup tables) by default; the raw page HTML (``str``) when ``return_parsed=False``.

    Raises:
        RuntimeError: No KenPom credentials could be resolved (pass ``email=``/``password=``, or set ``KENPOM_EMAIL``/``KENPOM_PW``), or kenpom.com rejected them.
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.mbb import kenpom_four_factors
            kenpom_four_factors(year=2025)

        See Also:
            * `hoopR`_ - R sister package; this is the port of its kp_* family
            * `KenPom`_ - data origin (subscription required)

        .. _hoopR: https://hoopR.sportsdataverse.org
        .. _KenPom: https://kenpom.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "y": year,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://kenpom.com/stats.php",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_kenpom_page(raw, return_as_pandas=return_as_pandas)
    return raw


def kenpom_point_distribution(
    year: int,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, str]:
    """GET /pointdist.php - share of points scored from 2s, 3s and free throws, offense and defense. Port of hoopR kp_pointdist().

    Endpoint: ``GET https://kenpom.com/pointdist.php``
    Example URL: https://kenpom.com/pointdist.php?y=2025

    Args:
        year: Season as a 4-digit ENDING year.
        headers: optional pre-built request headers to reuse across calls; the authenticated kenpom.com session is resolved and cached when omitted.
        return_parsed: parse the payload through parse_kenpom_page -> dict of polars DataFrames (default True). Pass return_parsed=False for the raw page HTML (``str``).
        return_as_pandas: with return_parsed, return a dict of pandas DataFrames (same keys) instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A ``dict`` of polars DataFrames, one per HTML table on the page, keyed by the table's HTML id (a KenPom page often carries several -- ``team.php`` alone holds the schedule, roster, depth chart and lineup tables) by default; the raw page HTML (``str``) when ``return_parsed=False``.

    Raises:
        RuntimeError: No KenPom credentials could be resolved (pass ``email=``/``password=``, or set ``KENPOM_EMAIL``/``KENPOM_PW``), or kenpom.com rejected them.
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.mbb import kenpom_point_distribution
            kenpom_point_distribution(year=2025)

        See Also:
            * `hoopR`_ - R sister package; this is the port of its kp_* family
            * `KenPom`_ - data origin (subscription required)

        .. _hoopR: https://hoopR.sportsdataverse.org
        .. _KenPom: https://kenpom.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "y": year,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://kenpom.com/pointdist.php",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_kenpom_page(raw, return_as_pandas=return_as_pandas)
    return raw


def kenpom_height(
    year: int,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, str]:
    """GET /height.php - team height, effective height, experience, bench minutes and continuity. Port of hoopR kp_height().

    Endpoint: ``GET https://kenpom.com/height.php``
    Example URL: https://kenpom.com/height.php?y=2025

    Args:
        year: Season as a 4-digit ENDING year. Columns are narrower before 2008.
        headers: optional pre-built request headers to reuse across calls; the authenticated kenpom.com session is resolved and cached when omitted.
        return_parsed: parse the payload through parse_kenpom_page -> dict of polars DataFrames (default True). Pass return_parsed=False for the raw page HTML (``str``).
        return_as_pandas: with return_parsed, return a dict of pandas DataFrames (same keys) instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A ``dict`` of polars DataFrames, one per HTML table on the page, keyed by the table's HTML id (a KenPom page often carries several -- ``team.php`` alone holds the schedule, roster, depth chart and lineup tables) by default; the raw page HTML (``str``) when ``return_parsed=False``.

    Raises:
        RuntimeError: No KenPom credentials could be resolved (pass ``email=``/``password=``, or set ``KENPOM_EMAIL``/``KENPOM_PW``), or kenpom.com rejected them.
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.mbb import kenpom_height
            kenpom_height(year=2025)

        See Also:
            * `hoopR`_ - R sister package; this is the port of its kp_* family
            * `KenPom`_ - data origin (subscription required)

        .. _hoopR: https://hoopR.sportsdataverse.org
        .. _KenPom: https://kenpom.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "y": year,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://kenpom.com/height.php",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_kenpom_page(raw, return_as_pandas=return_as_pandas)
    return raw


def kenpom_foul_trouble(
    year: int,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, str]:
    """GET /foul_trouble.php - team foul-trouble splits (minutes and efficiency with starters in foul trouble). Port of hoopR kp_foul_trouble().

    Endpoint: ``GET https://kenpom.com/foul_trouble.php``
    Example URL: https://kenpom.com/foul_trouble.php?y=2025

    Args:
        year: Season as a 4-digit ENDING year.
        headers: optional pre-built request headers to reuse across calls; the authenticated kenpom.com session is resolved and cached when omitted.
        return_parsed: parse the payload through parse_kenpom_page -> dict of polars DataFrames (default True). Pass return_parsed=False for the raw page HTML (``str``).
        return_as_pandas: with return_parsed, return a dict of pandas DataFrames (same keys) instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A ``dict`` of polars DataFrames, one per HTML table on the page, keyed by the table's HTML id (a KenPom page often carries several -- ``team.php`` alone holds the schedule, roster, depth chart and lineup tables) by default; the raw page HTML (``str``) when ``return_parsed=False``.

    Raises:
        RuntimeError: No KenPom credentials could be resolved (pass ``email=``/``password=``, or set ``KENPOM_EMAIL``/``KENPOM_PW``), or kenpom.com rejected them.
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.mbb import kenpom_foul_trouble
            kenpom_foul_trouble(year=2025)

        See Also:
            * `hoopR`_ - R sister package; this is the port of its kp_* family
            * `KenPom`_ - data origin (subscription required)

        .. _hoopR: https://hoopR.sportsdataverse.org
        .. _KenPom: https://kenpom.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "y": year,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://kenpom.com/foul_trouble.php",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_kenpom_page(raw, return_as_pandas=return_as_pandas)
    return raw


def kenpom_team_stats(
    year: int,
    side: Optional[str] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, str]:
    """GET /teamstats.php - team shooting and style splits; side='o' for offense, 'd' for defense. Port of hoopR kp_teamstats().

    Endpoint: ``GET https://kenpom.com/teamstats.php``
    Example URL: https://kenpom.com/teamstats.php?y=2025&od=o

    Args:
        year: Season as a 4-digit ENDING year.
        side: Side of the ball: 'o' (offense, hoopR's default) or 'd' (defense).
        headers: optional pre-built request headers to reuse across calls; the authenticated kenpom.com session is resolved and cached when omitted.
        return_parsed: parse the payload through parse_kenpom_page -> dict of polars DataFrames (default True). Pass return_parsed=False for the raw page HTML (``str``).
        return_as_pandas: with return_parsed, return a dict of pandas DataFrames (same keys) instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A ``dict`` of polars DataFrames, one per HTML table on the page, keyed by the table's HTML id (a KenPom page often carries several -- ``team.php`` alone holds the schedule, roster, depth chart and lineup tables) by default; the raw page HTML (``str``) when ``return_parsed=False``.

    Raises:
        RuntimeError: No KenPom credentials could be resolved (pass ``email=``/``password=``, or set ``KENPOM_EMAIL``/``KENPOM_PW``), or kenpom.com rejected them.
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.mbb import kenpom_team_stats
            kenpom_team_stats(year=2025, side='o')

        See Also:
            * `hoopR`_ - R sister package; this is the port of its kp_* family
            * `KenPom`_ - data origin (subscription required)

        .. _hoopR: https://hoopR.sportsdataverse.org
        .. _KenPom: https://kenpom.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "y": year,
        "od": side,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://kenpom.com/teamstats.php",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_kenpom_page(raw, return_as_pandas=return_as_pandas)
    return raw


def kenpom_player_stats(
    year: int,
    metric: str,
    conf: Optional[str] = None,
    conf_only: Optional[str] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, str]:
    """GET /playerstats.php - national player leaderboard for one metric. Port of hoopR kp_playerstats().

    Endpoint: ``GET https://kenpom.com/playerstats.php``
    Example URL: https://kenpom.com/playerstats.php?y=2025&s=eFG

    Args:
        year: Season as a 4-digit ENDING year. Data begins at 2004.
        metric: Metric slug as KenPom spells it on the wire - one of ORtg, PctMin, eFG, PctPoss, PctShots, ORPct, DRPct, TORate, ARate, PctBlocks, FTRate, PctStls, TS, FCper40, FDper40, FG2Pct, FG3Pct, FTPct. (hoopR's kp_playerstats() takes the display labels - ORtg, Min, eFG, Poss, Shots, OR, DR, TO, ARate, Blk, FTRate, Stl, TS, FC40, FD40, 2P, 3P, FT - and maps them to these.)
        conf: Conference filter (KenPom abbreviation, e.g. 'ACC', 'B10'); omit for all of Division I.
        conf_only: Conference-games-only toggle: 'c' restricts the leaderboard to conference play.
        headers: optional pre-built request headers to reuse across calls; the authenticated kenpom.com session is resolved and cached when omitted.
        return_parsed: parse the payload through parse_kenpom_page -> dict of polars DataFrames (default True). Pass return_parsed=False for the raw page HTML (``str``).
        return_as_pandas: with return_parsed, return a dict of pandas DataFrames (same keys) instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A ``dict`` of polars DataFrames, one per HTML table on the page, keyed by the table's HTML id (a KenPom page often carries several -- ``team.php`` alone holds the schedule, roster, depth chart and lineup tables) by default; the raw page HTML (``str``) when ``return_parsed=False``.

    Raises:
        RuntimeError: No KenPom credentials could be resolved (pass ``email=``/``password=``, or set ``KENPOM_EMAIL``/``KENPOM_PW``), or kenpom.com rejected them.
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.mbb import kenpom_player_stats
            kenpom_player_stats(year=2025, metric='eFG')

        See Also:
            * `hoopR`_ - R sister package; this is the port of its kp_* family
            * `KenPom`_ - data origin (subscription required)

        .. _hoopR: https://hoopR.sportsdataverse.org
        .. _KenPom: https://kenpom.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "y": year,
        "s": metric,
        "f": conf,
        "c": conf_only,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://kenpom.com/playerstats.php",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_kenpom_page(raw, return_as_pandas=return_as_pandas)
    return raw


def kenpom_kpoy(
    year: int,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, str]:
    """GET /kpoy.php - KenPom Player of the Year standings and the game-MVP table. Port of hoopR kp_kpoy().

    Endpoint: ``GET https://kenpom.com/kpoy.php``
    Example URL: https://kenpom.com/kpoy.php?y=2025

    Args:
        year: Season as a 4-digit ENDING year.
        headers: optional pre-built request headers to reuse across calls; the authenticated kenpom.com session is resolved and cached when omitted.
        return_parsed: parse the payload through parse_kenpom_page -> dict of polars DataFrames (default True). Pass return_parsed=False for the raw page HTML (``str``).
        return_as_pandas: with return_parsed, return a dict of pandas DataFrames (same keys) instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A ``dict`` of polars DataFrames, one per HTML table on the page, keyed by the table's HTML id (a KenPom page often carries several -- ``team.php`` alone holds the schedule, roster, depth chart and lineup tables) by default; the raw page HTML (``str``) when ``return_parsed=False``.

    Raises:
        RuntimeError: No KenPom credentials could be resolved (pass ``email=``/``password=``, or set ``KENPOM_EMAIL``/``KENPOM_PW``), or kenpom.com rejected them.
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.mbb import kenpom_kpoy
            kenpom_kpoy(year=2025)

        See Also:
            * `hoopR`_ - R sister package; this is the port of its kp_* family
            * `KenPom`_ - data origin (subscription required)

        .. _hoopR: https://hoopR.sportsdataverse.org
        .. _KenPom: https://kenpom.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "y": year,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://kenpom.com/kpoy.php",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_kenpom_page(raw, return_as_pandas=return_as_pandas)
    return raw


def kenpom_team(
    team: str,
    year: int,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, str]:
    """GET /team.php - a team's full season page. Returns EVERY table on it, so one call covers hoopR's kp_team_schedule(), kp_team_players(), kp_team_depth_chart() and kp_team_lineups(), which each fetch this same page separately.

    Endpoint: ``GET https://kenpom.com/team.php``
    Example URL: https://kenpom.com/team.php?team=Duke&y=2025

    Args:
        team: KenPom team name, spelled as the site does (e.g. 'Duke', 'Michigan St.').
        year: Season as a 4-digit ENDING year. Lineup tables begin at 2011.
        headers: optional pre-built request headers to reuse across calls; the authenticated kenpom.com session is resolved and cached when omitted.
        return_parsed: parse the payload through parse_kenpom_page -> dict of polars DataFrames (default True). Pass return_parsed=False for the raw page HTML (``str``).
        return_as_pandas: with return_parsed, return a dict of pandas DataFrames (same keys) instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A ``dict`` of polars DataFrames, one per HTML table on the page, keyed by the table's HTML id (a KenPom page often carries several -- ``team.php`` alone holds the schedule, roster, depth chart and lineup tables) by default; the raw page HTML (``str``) when ``return_parsed=False``.

    Raises:
        RuntimeError: No KenPom credentials could be resolved (pass ``email=``/``password=``, or set ``KENPOM_EMAIL``/``KENPOM_PW``), or kenpom.com rejected them.
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.mbb import kenpom_team
            kenpom_team(team='Duke', year=2025)

        See Also:
            * `hoopR`_ - R sister package; this is the port of its kp_* family
            * `KenPom`_ - data origin (subscription required)

        .. _hoopR: https://hoopR.sportsdataverse.org
        .. _KenPom: https://kenpom.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "team": team,
        "y": year,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://kenpom.com/team.php",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_kenpom_page(raw, return_as_pandas=return_as_pandas)
    return raw


def kenpom_team_players_expanded(
    team: str,
    year: int,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, str]:
    """GET /player-expanded.php - a team's expanded per-player table plus the minutes matrix. Covers hoopR's kp_team_player_stats() and kp_minutes_matrix() in one fetch.

    Endpoint: ``GET https://kenpom.com/player-expanded.php``
    Example URL: https://kenpom.com/player-expanded.php?team=Duke&y=2025

    Args:
        team: KenPom team name, spelled as the site does.
        year: Season as a 4-digit ENDING year. Starts ('S') are available from 2014.
        headers: optional pre-built request headers to reuse across calls; the authenticated kenpom.com session is resolved and cached when omitted.
        return_parsed: parse the payload through parse_kenpom_page -> dict of polars DataFrames (default True). Pass return_parsed=False for the raw page HTML (``str``).
        return_as_pandas: with return_parsed, return a dict of pandas DataFrames (same keys) instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A ``dict`` of polars DataFrames, one per HTML table on the page, keyed by the table's HTML id (a KenPom page often carries several -- ``team.php`` alone holds the schedule, roster, depth chart and lineup tables) by default; the raw page HTML (``str``) when ``return_parsed=False``.

    Raises:
        RuntimeError: No KenPom credentials could be resolved (pass ``email=``/``password=``, or set ``KENPOM_EMAIL``/``KENPOM_PW``), or kenpom.com rejected them.
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.mbb import kenpom_team_players_expanded
            kenpom_team_players_expanded(team='Duke', year=2025)

        See Also:
            * `hoopR`_ - R sister package; this is the port of its kp_* family
            * `KenPom`_ - data origin (subscription required)

        .. _hoopR: https://hoopR.sportsdataverse.org
        .. _KenPom: https://kenpom.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "team": team,
        "y": year,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://kenpom.com/player-expanded.php",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_kenpom_page(raw, return_as_pandas=return_as_pandas)
    return raw


def kenpom_game_plan(
    team: str,
    year: int,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, str]:
    """GET /gameplan.php - a team's game-plan page (per-game four factors and personnel splits). Port of hoopR kp_gameplan().

    Endpoint: ``GET https://kenpom.com/gameplan.php``
    Example URL: https://kenpom.com/gameplan.php?team=Duke&y=2025

    Args:
        team: KenPom team name, spelled as the site does.
        year: Season as a 4-digit ENDING year.
        headers: optional pre-built request headers to reuse across calls; the authenticated kenpom.com session is resolved and cached when omitted.
        return_parsed: parse the payload through parse_kenpom_page -> dict of polars DataFrames (default True). Pass return_parsed=False for the raw page HTML (``str``).
        return_as_pandas: with return_parsed, return a dict of pandas DataFrames (same keys) instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A ``dict`` of polars DataFrames, one per HTML table on the page, keyed by the table's HTML id (a KenPom page often carries several -- ``team.php`` alone holds the schedule, roster, depth chart and lineup tables) by default; the raw page HTML (``str``) when ``return_parsed=False``.

    Raises:
        RuntimeError: No KenPom credentials could be resolved (pass ``email=``/``password=``, or set ``KENPOM_EMAIL``/``KENPOM_PW``), or kenpom.com rejected them.
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.mbb import kenpom_game_plan
            kenpom_game_plan(team='Duke', year=2025)

        See Also:
            * `hoopR`_ - R sister package; this is the port of its kp_* family
            * `KenPom`_ - data origin (subscription required)

        .. _hoopR: https://hoopR.sportsdataverse.org
        .. _KenPom: https://kenpom.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "team": team,
        "y": year,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://kenpom.com/gameplan.php",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_kenpom_page(raw, return_as_pandas=return_as_pandas)
    return raw


def kenpom_opponent_tracker(
    team: str,
    year: int,
    side: Optional[str] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, str]:
    """GET /opptracker.php - opponent tracker; side='o' for offense, 'd' for defense. Port of hoopR kp_opptracker().

    Endpoint: ``GET https://kenpom.com/opptracker.php``
    Example URL: https://kenpom.com/opptracker.php?team=Duke&y=2025&t=o

    Args:
        team: KenPom team name, spelled as the site does.
        year: Season as a 4-digit ENDING year. Columns are narrower before 2010.
        side: Side of the ball: 'o' (offense) or 'd' (defense).
        headers: optional pre-built request headers to reuse across calls; the authenticated kenpom.com session is resolved and cached when omitted.
        return_parsed: parse the payload through parse_kenpom_page -> dict of polars DataFrames (default True). Pass return_parsed=False for the raw page HTML (``str``).
        return_as_pandas: with return_parsed, return a dict of pandas DataFrames (same keys) instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A ``dict`` of polars DataFrames, one per HTML table on the page, keyed by the table's HTML id (a KenPom page often carries several -- ``team.php`` alone holds the schedule, roster, depth chart and lineup tables) by default; the raw page HTML (``str``) when ``return_parsed=False``.

    Raises:
        RuntimeError: No KenPom credentials could be resolved (pass ``email=``/``password=``, or set ``KENPOM_EMAIL``/``KENPOM_PW``), or kenpom.com rejected them.
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.mbb import kenpom_opponent_tracker
            kenpom_opponent_tracker(team='Duke', year=2025, side='o')

        See Also:
            * `hoopR`_ - R sister package; this is the port of its kp_* family
            * `KenPom`_ - data origin (subscription required)

        .. _hoopR: https://hoopR.sportsdataverse.org
        .. _KenPom: https://kenpom.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "team": team,
        "y": year,
        "t": side,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://kenpom.com/opptracker.php",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_kenpom_page(raw, return_as_pandas=return_as_pandas)
    return raw


def kenpom_player_career(
    player_id: Union[int, str],
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, str]:
    """GET /player.php - one player's career page (season-by-season stats and game log). Port of hoopR kp_player_career().

    Endpoint: ``GET https://kenpom.com/player.php``
    Example URL: https://kenpom.com/player.php?p=51234

    Args:
        player_id: KenPom player id - the `p=` value on a player-page URL.
        headers: optional pre-built request headers to reuse across calls; the authenticated kenpom.com session is resolved and cached when omitted.
        return_parsed: parse the payload through parse_kenpom_page -> dict of polars DataFrames (default True). Pass return_parsed=False for the raw page HTML (``str``).
        return_as_pandas: with return_parsed, return a dict of pandas DataFrames (same keys) instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A ``dict`` of polars DataFrames, one per HTML table on the page, keyed by the table's HTML id (a KenPom page often carries several -- ``team.php`` alone holds the schedule, roster, depth chart and lineup tables) by default; the raw page HTML (``str``) when ``return_parsed=False``.

    Raises:
        RuntimeError: No KenPom credentials could be resolved (pass ``email=``/``password=``, or set ``KENPOM_EMAIL``/``KENPOM_PW``), or kenpom.com rejected them.
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.mbb import kenpom_player_career
            kenpom_player_career(player_id=51234)

        See Also:
            * `hoopR`_ - R sister package; this is the port of its kp_* family
            * `KenPom`_ - data origin (subscription required)

        .. _hoopR: https://hoopR.sportsdataverse.org
        .. _KenPom: https://kenpom.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "p": player_id,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://kenpom.com/player.php",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_kenpom_page(raw, return_as_pandas=return_as_pandas)
    return raw


def kenpom_box(
    game_id: Union[int, str],
    year: int,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, str]:
    """GET /box.php - box-score detail for one game (per-team four factors, player lines, scoring runs). Port of hoopR kp_box().

    Endpoint: ``GET https://kenpom.com/box.php``
    Example URL: https://kenpom.com/box.php?g=20250401&y=2025

    Args:
        game_id: KenPom game id - the `g=` value on a FanMatch game link.
        year: Season (4-digit ENDING year) the game belongs to.
        headers: optional pre-built request headers to reuse across calls; the authenticated kenpom.com session is resolved and cached when omitted.
        return_parsed: parse the payload through parse_kenpom_page -> dict of polars DataFrames (default True). Pass return_parsed=False for the raw page HTML (``str``).
        return_as_pandas: with return_parsed, return a dict of pandas DataFrames (same keys) instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A ``dict`` of polars DataFrames, one per HTML table on the page, keyed by the table's HTML id (a KenPom page often carries several -- ``team.php`` alone holds the schedule, roster, depth chart and lineup tables) by default; the raw page HTML (``str``) when ``return_parsed=False``.

    Raises:
        RuntimeError: No KenPom credentials could be resolved (pass ``email=``/``password=``, or set ``KENPOM_EMAIL``/``KENPOM_PW``), or kenpom.com rejected them.
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.mbb import kenpom_box
            kenpom_box(game_id=20250401, year=2025)

        See Also:
            * `hoopR`_ - R sister package; this is the port of its kp_* family
            * `KenPom`_ - data origin (subscription required)

        .. _hoopR: https://hoopR.sportsdataverse.org
        .. _KenPom: https://kenpom.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "g": game_id,
        "y": year,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://kenpom.com/box.php",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_kenpom_page(raw, return_as_pandas=return_as_pandas)
    return raw


def kenpom_win_probability(
    game_id: Union[int, str],
    year: int,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, str]:
    """GET /winprob.php - in-game win-probability table for one game. Port of hoopR kp_winprob().

    Endpoint: ``GET https://kenpom.com/winprob.php``
    Example URL: https://kenpom.com/winprob.php?g=20250401&y=2025

    Args:
        game_id: KenPom game id.
        year: Season (4-digit ENDING year) the game belongs to.
        headers: optional pre-built request headers to reuse across calls; the authenticated kenpom.com session is resolved and cached when omitted.
        return_parsed: parse the payload through parse_kenpom_page -> dict of polars DataFrames (default True). Pass return_parsed=False for the raw page HTML (``str``).
        return_as_pandas: with return_parsed, return a dict of pandas DataFrames (same keys) instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A ``dict`` of polars DataFrames, one per HTML table on the page, keyed by the table's HTML id (a KenPom page often carries several -- ``team.php`` alone holds the schedule, roster, depth chart and lineup tables) by default; the raw page HTML (``str``) when ``return_parsed=False``.

    Raises:
        RuntimeError: No KenPom credentials could be resolved (pass ``email=``/``password=``, or set ``KENPOM_EMAIL``/``KENPOM_PW``), or kenpom.com rejected them.
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.mbb import kenpom_win_probability
            kenpom_win_probability(game_id=20250401, year=2025)

        See Also:
            * `hoopR`_ - R sister package; this is the port of its kp_* family
            * `KenPom`_ - data origin (subscription required)

        .. _hoopR: https://hoopR.sportsdataverse.org
        .. _KenPom: https://kenpom.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "g": game_id,
        "y": year,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://kenpom.com/winprob.php",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_kenpom_page(raw, return_as_pandas=return_as_pandas)
    return raw


def kenpom_fan_match(
    date: str,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, str]:
    """GET /fanmatch.php - the FanMatch slate for one date (predictions, thrill score, results). Port of hoopR kp_fanmatch().

    Endpoint: ``GET https://kenpom.com/fanmatch.php``
    Example URL: https://kenpom.com/fanmatch.php?d=2025-02-01

    Args:
        date: Slate date as YYYY-MM-DD.
        headers: optional pre-built request headers to reuse across calls; the authenticated kenpom.com session is resolved and cached when omitted.
        return_parsed: parse the payload through parse_kenpom_page -> dict of polars DataFrames (default True). Pass return_parsed=False for the raw page HTML (``str``).
        return_as_pandas: with return_parsed, return a dict of pandas DataFrames (same keys) instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A ``dict`` of polars DataFrames, one per HTML table on the page, keyed by the table's HTML id (a KenPom page often carries several -- ``team.php`` alone holds the schedule, roster, depth chart and lineup tables) by default; the raw page HTML (``str``) when ``return_parsed=False``.

    Raises:
        RuntimeError: No KenPom credentials could be resolved (pass ``email=``/``password=``, or set ``KENPOM_EMAIL``/``KENPOM_PW``), or kenpom.com rejected them.
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.mbb import kenpom_fan_match
            kenpom_fan_match(date='2025-02-01')

        See Also:
            * `hoopR`_ - R sister package; this is the port of its kp_* family
            * `KenPom`_ - data origin (subscription required)

        .. _hoopR: https://hoopR.sportsdataverse.org
        .. _KenPom: https://kenpom.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "d": date,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://kenpom.com/fanmatch.php",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_kenpom_page(raw, return_as_pandas=return_as_pandas)
    return raw


def kenpom_team_history(
    team: str,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, str]:
    """GET /history.php?t= - a program's season-by-season history. Port of hoopR kp_team_history().

    Endpoint: ``GET https://kenpom.com/history.php``
    Example URL: https://kenpom.com/history.php?t=Duke

    Args:
        team: KenPom team name, spelled as the site does.
        headers: optional pre-built request headers to reuse across calls; the authenticated kenpom.com session is resolved and cached when omitted.
        return_parsed: parse the payload through parse_kenpom_page -> dict of polars DataFrames (default True). Pass return_parsed=False for the raw page HTML (``str``).
        return_as_pandas: with return_parsed, return a dict of pandas DataFrames (same keys) instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A ``dict`` of polars DataFrames, one per HTML table on the page, keyed by the table's HTML id (a KenPom page often carries several -- ``team.php`` alone holds the schedule, roster, depth chart and lineup tables) by default; the raw page HTML (``str``) when ``return_parsed=False``.

    Raises:
        RuntimeError: No KenPom credentials could be resolved (pass ``email=``/``password=``, or set ``KENPOM_EMAIL``/``KENPOM_PW``), or kenpom.com rejected them.
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.mbb import kenpom_team_history
            kenpom_team_history(team='Duke')

        See Also:
            * `hoopR`_ - R sister package; this is the port of its kp_* family
            * `KenPom`_ - data origin (subscription required)

        .. _hoopR: https://hoopR.sportsdataverse.org
        .. _KenPom: https://kenpom.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "t": team,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://kenpom.com/history.php",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_kenpom_page(raw, return_as_pandas=return_as_pandas)
    return raw


def kenpom_coach_history(
    coach: str,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, str]:
    """GET /history.php?c= - a coach's season-by-season history. Port of hoopR kp_coach_history().

    Endpoint: ``GET https://kenpom.com/history.php``
    Example URL: https://kenpom.com/history.php?c=Jon+Scheyer

    Args:
        coach: Coach name as KenPom spells it (e.g. 'Jon Scheyer').
        headers: optional pre-built request headers to reuse across calls; the authenticated kenpom.com session is resolved and cached when omitted.
        return_parsed: parse the payload through parse_kenpom_page -> dict of polars DataFrames (default True). Pass return_parsed=False for the raw page HTML (``str``).
        return_as_pandas: with return_parsed, return a dict of pandas DataFrames (same keys) instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A ``dict`` of polars DataFrames, one per HTML table on the page, keyed by the table's HTML id (a KenPom page often carries several -- ``team.php`` alone holds the schedule, roster, depth chart and lineup tables) by default; the raw page HTML (``str``) when ``return_parsed=False``.

    Raises:
        RuntimeError: No KenPom credentials could be resolved (pass ``email=``/``password=``, or set ``KENPOM_EMAIL``/``KENPOM_PW``), or kenpom.com rejected them.
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.mbb import kenpom_coach_history
            kenpom_coach_history(coach='Jon Scheyer')

        See Also:
            * `hoopR`_ - R sister package; this is the port of its kp_* family
            * `KenPom`_ - data origin (subscription required)

        .. _hoopR: https://hoopR.sportsdataverse.org
        .. _KenPom: https://kenpom.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "c": coach,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://kenpom.com/history.php",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_kenpom_page(raw, return_as_pandas=return_as_pandas)
    return raw


def kenpom_program_ratings(
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, str]:
    """GET /programs.php - program-level ratings across the full KenPom era. Port of hoopR kp_program_ratings().

    Endpoint: ``GET https://kenpom.com/programs.php``
    Example URL: https://kenpom.com/programs.php

    Args:
        headers: optional pre-built request headers to reuse across calls; the authenticated kenpom.com session is resolved and cached when omitted.
        return_parsed: parse the payload through parse_kenpom_page -> dict of polars DataFrames (default True). Pass return_parsed=False for the raw page HTML (``str``).
        return_as_pandas: with return_parsed, return a dict of pandas DataFrames (same keys) instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A ``dict`` of polars DataFrames, one per HTML table on the page, keyed by the table's HTML id (a KenPom page often carries several -- ``team.php`` alone holds the schedule, roster, depth chart and lineup tables) by default; the raw page HTML (``str``) when ``return_parsed=False``.

    Raises:
        RuntimeError: No KenPom credentials could be resolved (pass ``email=``/``password=``, or set ``KENPOM_EMAIL``/``KENPOM_PW``), or kenpom.com rejected them.
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.mbb import kenpom_program_ratings
            kenpom_program_ratings()

        See Also:
            * `hoopR`_ - R sister package; this is the port of its kp_* family
            * `KenPom`_ - data origin (subscription required)

        .. _hoopR: https://hoopR.sportsdataverse.org
        .. _KenPom: https://kenpom.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {}
    _params.update(_caller_params)
    raw = _get(
        "https://kenpom.com/programs.php",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_kenpom_page(raw, return_as_pandas=return_as_pandas)
    return raw


def kenpom_archive_ratings(
    date: str,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, str]:
    """GET /archive.php - the Pomeroy ratings as they stood on a past date. Port of hoopR kp_pomeroy_archive_ratings().

    Endpoint: ``GET https://kenpom.com/archive.php``
    Example URL: https://kenpom.com/archive.php?d=2025-02-01

    Args:
        date: Snapshot date as YYYY-MM-DD.
        headers: optional pre-built request headers to reuse across calls; the authenticated kenpom.com session is resolved and cached when omitted.
        return_parsed: parse the payload through parse_kenpom_page -> dict of polars DataFrames (default True). Pass return_parsed=False for the raw page HTML (``str``).
        return_as_pandas: with return_parsed, return a dict of pandas DataFrames (same keys) instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A ``dict`` of polars DataFrames, one per HTML table on the page, keyed by the table's HTML id (a KenPom page often carries several -- ``team.php`` alone holds the schedule, roster, depth chart and lineup tables) by default; the raw page HTML (``str``) when ``return_parsed=False``.

    Raises:
        RuntimeError: No KenPom credentials could be resolved (pass ``email=``/``password=``, or set ``KENPOM_EMAIL``/``KENPOM_PW``), or kenpom.com rejected them.
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.mbb import kenpom_archive_ratings
            kenpom_archive_ratings(date='2025-02-01')

        See Also:
            * `hoopR`_ - R sister package; this is the port of its kp_* family
            * `KenPom`_ - data origin (subscription required)

        .. _hoopR: https://hoopR.sportsdataverse.org
        .. _KenPom: https://kenpom.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "d": date,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://kenpom.com/archive.php",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_kenpom_page(raw, return_as_pandas=return_as_pandas)
    return raw


def kenpom_conference(
    conf: str,
    year: int,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, str]:
    """GET /conf.php - one conference's season page (standings, efficiency, per-team splits). Port of hoopR kp_conf().

    Endpoint: ``GET https://kenpom.com/conf.php``
    Example URL: https://kenpom.com/conf.php?c=ACC&y=2025

    Args:
        conf: KenPom conference abbreviation (e.g. 'ACC', 'B10', 'SEC').
        year: Season as a 4-digit ENDING year.
        headers: optional pre-built request headers to reuse across calls; the authenticated kenpom.com session is resolved and cached when omitted.
        return_parsed: parse the payload through parse_kenpom_page -> dict of polars DataFrames (default True). Pass return_parsed=False for the raw page HTML (``str``).
        return_as_pandas: with return_parsed, return a dict of pandas DataFrames (same keys) instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A ``dict`` of polars DataFrames, one per HTML table on the page, keyed by the table's HTML id (a KenPom page often carries several -- ``team.php`` alone holds the schedule, roster, depth chart and lineup tables) by default; the raw page HTML (``str``) when ``return_parsed=False``.

    Raises:
        RuntimeError: No KenPom credentials could be resolved (pass ``email=``/``password=``, or set ``KENPOM_EMAIL``/``KENPOM_PW``), or kenpom.com rejected them.
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.mbb import kenpom_conference
            kenpom_conference(conf='ACC', year=2025)

        See Also:
            * `hoopR`_ - R sister package; this is the port of its kp_* family
            * `KenPom`_ - data origin (subscription required)

        .. _hoopR: https://hoopR.sportsdataverse.org
        .. _KenPom: https://kenpom.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "c": conf,
        "y": year,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://kenpom.com/conf.php",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_kenpom_page(raw, return_as_pandas=return_as_pandas)
    return raw


def kenpom_conference_stats(
    year: int,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, str]:
    """GET /confstats.php - league-wide conference comparison for one season. Port of hoopR kp_confstats().

    Endpoint: ``GET https://kenpom.com/confstats.php``
    Example URL: https://kenpom.com/confstats.php?y=2025

    Args:
        year: Season as a 4-digit ENDING year.
        headers: optional pre-built request headers to reuse across calls; the authenticated kenpom.com session is resolved and cached when omitted.
        return_parsed: parse the payload through parse_kenpom_page -> dict of polars DataFrames (default True). Pass return_parsed=False for the raw page HTML (``str``).
        return_as_pandas: with return_parsed, return a dict of pandas DataFrames (same keys) instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A ``dict`` of polars DataFrames, one per HTML table on the page, keyed by the table's HTML id (a KenPom page often carries several -- ``team.php`` alone holds the schedule, roster, depth chart and lineup tables) by default; the raw page HTML (``str``) when ``return_parsed=False``.

    Raises:
        RuntimeError: No KenPom credentials could be resolved (pass ``email=``/``password=``, or set ``KENPOM_EMAIL``/``KENPOM_PW``), or kenpom.com rejected them.
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.mbb import kenpom_conference_stats
            kenpom_conference_stats(year=2025)

        See Also:
            * `hoopR`_ - R sister package; this is the port of its kp_* family
            * `KenPom`_ - data origin (subscription required)

        .. _hoopR: https://hoopR.sportsdataverse.org
        .. _KenPom: https://kenpom.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "y": year,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://kenpom.com/confstats.php",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_kenpom_page(raw, return_as_pandas=return_as_pandas)
    return raw


def kenpom_conference_history(
    conf: str,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, str]:
    """GET /confhistory.php - one conference's season-by-season history. Port of hoopR kp_confhistory().

    Endpoint: ``GET https://kenpom.com/confhistory.php``
    Example URL: https://kenpom.com/confhistory.php?c=ACC

    Args:
        conf: KenPom conference abbreviation.
        headers: optional pre-built request headers to reuse across calls; the authenticated kenpom.com session is resolved and cached when omitted.
        return_parsed: parse the payload through parse_kenpom_page -> dict of polars DataFrames (default True). Pass return_parsed=False for the raw page HTML (``str``).
        return_as_pandas: with return_parsed, return a dict of pandas DataFrames (same keys) instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A ``dict`` of polars DataFrames, one per HTML table on the page, keyed by the table's HTML id (a KenPom page often carries several -- ``team.php`` alone holds the schedule, roster, depth chart and lineup tables) by default; the raw page HTML (``str``) when ``return_parsed=False``.

    Raises:
        RuntimeError: No KenPom credentials could be resolved (pass ``email=``/``password=``, or set ``KENPOM_EMAIL``/``KENPOM_PW``), or kenpom.com rejected them.
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.mbb import kenpom_conference_history
            kenpom_conference_history(conf='ACC')

        See Also:
            * `hoopR`_ - R sister package; this is the port of its kp_* family
            * `KenPom`_ - data origin (subscription required)

        .. _hoopR: https://hoopR.sportsdataverse.org
        .. _KenPom: https://kenpom.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "c": conf,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://kenpom.com/confhistory.php",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_kenpom_page(raw, return_as_pandas=return_as_pandas)
    return raw


def kenpom_trends(
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, str]:
    """GET /trends.php - national Division I trends by season (tempo, efficiency, shooting, fouls). Port of hoopR kp_trends().

    Endpoint: ``GET https://kenpom.com/trends.php``
    Example URL: https://kenpom.com/trends.php

    Args:
        headers: optional pre-built request headers to reuse across calls; the authenticated kenpom.com session is resolved and cached when omitted.
        return_parsed: parse the payload through parse_kenpom_page -> dict of polars DataFrames (default True). Pass return_parsed=False for the raw page HTML (``str``).
        return_as_pandas: with return_parsed, return a dict of pandas DataFrames (same keys) instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A ``dict`` of polars DataFrames, one per HTML table on the page, keyed by the table's HTML id (a KenPom page often carries several -- ``team.php`` alone holds the schedule, roster, depth chart and lineup tables) by default; the raw page HTML (``str``) when ``return_parsed=False``.

    Raises:
        RuntimeError: No KenPom credentials could be resolved (pass ``email=``/``password=``, or set ``KENPOM_EMAIL``/``KENPOM_PW``), or kenpom.com rejected them.
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.mbb import kenpom_trends
            kenpom_trends()

        See Also:
            * `hoopR`_ - R sister package; this is the port of its kp_* family
            * `KenPom`_ - data origin (subscription required)

        .. _hoopR: https://hoopR.sportsdataverse.org
        .. _KenPom: https://kenpom.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {}
    _params.update(_caller_params)
    raw = _get(
        "https://kenpom.com/trends.php",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_kenpom_page(raw, return_as_pandas=return_as_pandas)
    return raw


def kenpom_home_court_advantage(
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, str]:
    """GET /hca.php - per-team home-court advantage estimates. Port of hoopR kp_hca().

    Endpoint: ``GET https://kenpom.com/hca.php``
    Example URL: https://kenpom.com/hca.php

    Args:
        headers: optional pre-built request headers to reuse across calls; the authenticated kenpom.com session is resolved and cached when omitted.
        return_parsed: parse the payload through parse_kenpom_page -> dict of polars DataFrames (default True). Pass return_parsed=False for the raw page HTML (``str``).
        return_as_pandas: with return_parsed, return a dict of pandas DataFrames (same keys) instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A ``dict`` of polars DataFrames, one per HTML table on the page, keyed by the table's HTML id (a KenPom page often carries several -- ``team.php`` alone holds the schedule, roster, depth chart and lineup tables) by default; the raw page HTML (``str``) when ``return_parsed=False``.

    Raises:
        RuntimeError: No KenPom credentials could be resolved (pass ``email=``/``password=``, or set ``KENPOM_EMAIL``/``KENPOM_PW``), or kenpom.com rejected them.
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.mbb import kenpom_home_court_advantage
            kenpom_home_court_advantage()

        See Also:
            * `hoopR`_ - R sister package; this is the port of its kp_* family
            * `KenPom`_ - data origin (subscription required)

        .. _hoopR: https://hoopR.sportsdataverse.org
        .. _KenPom: https://kenpom.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {}
    _params.update(_caller_params)
    raw = _get(
        "https://kenpom.com/hca.php",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_kenpom_page(raw, return_as_pandas=return_as_pandas)
    return raw


def kenpom_arenas(
    year: int,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, str]:
    """GET /arenas.php - arena reference (name, capacity, average attendance) by team. Port of hoopR kp_arenas().

    Endpoint: ``GET https://kenpom.com/arenas.php``
    Example URL: https://kenpom.com/arenas.php?y=2025

    Args:
        year: Season as a 4-digit ENDING year.
        headers: optional pre-built request headers to reuse across calls; the authenticated kenpom.com session is resolved and cached when omitted.
        return_parsed: parse the payload through parse_kenpom_page -> dict of polars DataFrames (default True). Pass return_parsed=False for the raw page HTML (``str``).
        return_as_pandas: with return_parsed, return a dict of pandas DataFrames (same keys) instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A ``dict`` of polars DataFrames, one per HTML table on the page, keyed by the table's HTML id (a KenPom page often carries several -- ``team.php`` alone holds the schedule, roster, depth chart and lineup tables) by default; the raw page HTML (``str``) when ``return_parsed=False``.

    Raises:
        RuntimeError: No KenPom credentials could be resolved (pass ``email=``/``password=``, or set ``KENPOM_EMAIL``/``KENPOM_PW``), or kenpom.com rejected them.
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.mbb import kenpom_arenas
            kenpom_arenas(year=2025)

        See Also:
            * `hoopR`_ - R sister package; this is the port of its kp_* family
            * `KenPom`_ - data origin (subscription required)

        .. _hoopR: https://hoopR.sportsdataverse.org
        .. _KenPom: https://kenpom.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "y": year,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://kenpom.com/arenas.php",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_kenpom_page(raw, return_as_pandas=return_as_pandas)
    return raw


def kenpom_officials(
    year: int,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, str]:
    """GET /officials.php - referee ratings for one season. Port of hoopR kp_officials().

    Endpoint: ``GET https://kenpom.com/officials.php``
    Example URL: https://kenpom.com/officials.php?y=2025

    Args:
        year: Season as a 4-digit ENDING year.
        headers: optional pre-built request headers to reuse across calls; the authenticated kenpom.com session is resolved and cached when omitted.
        return_parsed: parse the payload through parse_kenpom_page -> dict of polars DataFrames (default True). Pass return_parsed=False for the raw page HTML (``str``).
        return_as_pandas: with return_parsed, return a dict of pandas DataFrames (same keys) instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A ``dict`` of polars DataFrames, one per HTML table on the page, keyed by the table's HTML id (a KenPom page often carries several -- ``team.php`` alone holds the schedule, roster, depth chart and lineup tables) by default; the raw page HTML (``str``) when ``return_parsed=False``.

    Raises:
        RuntimeError: No KenPom credentials could be resolved (pass ``email=``/``password=``, or set ``KENPOM_EMAIL``/``KENPOM_PW``), or kenpom.com rejected them.
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.mbb import kenpom_officials
            kenpom_officials(year=2025)

        See Also:
            * `hoopR`_ - R sister package; this is the port of its kp_* family
            * `KenPom`_ - data origin (subscription required)

        .. _hoopR: https://hoopR.sportsdataverse.org
        .. _KenPom: https://kenpom.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "y": year,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://kenpom.com/officials.php",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_kenpom_page(raw, return_as_pandas=return_as_pandas)
    return raw


def kenpom_referee(
    referee: str,
    year: int,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, str]:
    """GET /referee.php - one referee's game log and splits for a season. Port of hoopR kp_referee().

    Endpoint: ``GET https://kenpom.com/referee.php``
    Example URL: https://kenpom.com/referee.php?r=Ron+Groover&y=2025

    Args:
        referee: Referee name as KenPom spells it (take it from the officials table).
        year: Season as a 4-digit ENDING year.
        headers: optional pre-built request headers to reuse across calls; the authenticated kenpom.com session is resolved and cached when omitted.
        return_parsed: parse the payload through parse_kenpom_page -> dict of polars DataFrames (default True). Pass return_parsed=False for the raw page HTML (``str``).
        return_as_pandas: with return_parsed, return a dict of pandas DataFrames (same keys) instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A ``dict`` of polars DataFrames, one per HTML table on the page, keyed by the table's HTML id (a KenPom page often carries several -- ``team.php`` alone holds the schedule, roster, depth chart and lineup tables) by default; the raw page HTML (``str``) when ``return_parsed=False``.

    Raises:
        RuntimeError: No KenPom credentials could be resolved (pass ``email=``/``password=``, or set ``KENPOM_EMAIL``/``KENPOM_PW``), or kenpom.com rejected them.
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.mbb import kenpom_referee
            kenpom_referee(referee='Ron Groover', year=2025)

        See Also:
            * `hoopR`_ - R sister package; this is the port of its kp_* family
            * `KenPom`_ - data origin (subscription required)

        .. _hoopR: https://hoopR.sportsdataverse.org
        .. _KenPom: https://kenpom.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "r": referee,
        "y": year,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://kenpom.com/referee.php",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_kenpom_page(raw, return_as_pandas=return_as_pandas)
    return raw


def kenpom_game_attributes(
    year: int,
    attribute: Optional[str] = None,
    headers: Optional[Dict[str, str]] = None,
    *,
    return_parsed: bool = True,
    return_as_pandas: bool = False,
    **kwargs,
) -> Union[pl.DataFrame, pd.DataFrame, str]:
    """GET /game_attrs.php - season game leaderboards by attribute (thrill score, comebacks, upsets, ...). Port of hoopR kp_game_attrs().

    Endpoint: ``GET https://kenpom.com/game_attrs.php``
    Example URL: https://kenpom.com/game_attrs.php?y=2025&s=ThrillScore

    Args:
        year: Season as a 4-digit ENDING year.
        attribute: Attribute slug, e.g. ThrillScore, Comeback, FanMatch, Upsets, Busts, MinutesPlayed, PossessionLength, LeadChanges.
        headers: optional pre-built request headers to reuse across calls; the authenticated kenpom.com session is resolved and cached when omitted.
        return_parsed: parse the payload through parse_kenpom_page -> dict of polars DataFrames (default True). Pass return_parsed=False for the raw page HTML (``str``).
        return_as_pandas: with return_parsed, return a dict of pandas DataFrames (same keys) instead of polars.
        **kwargs: Forwarded to the underlying HTTP getter.

    Returns:
        A ``dict`` of polars DataFrames, one per HTML table on the page, keyed by the table's HTML id (a KenPom page often carries several -- ``team.php`` alone holds the schedule, roster, depth chart and lineup tables) by default; the raw page HTML (``str``) when ``return_parsed=False``.

    Raises:
        RuntimeError: No KenPom credentials could be resolved (pass ``email=``/``password=``, or set ``KENPOM_EMAIL``/``KENPOM_PW``), or kenpom.com rejected them.
        requests.exceptions.RequestException: Connection-level failure after ``dl_utils.download`` exhausts its retries.

    Example:
        Quick start::

            from sportsdataverse.mbb import kenpom_game_attributes
            kenpom_game_attributes(year=2025, attribute='ThrillScore')

        See Also:
            * `hoopR`_ - R sister package; this is the port of its kp_* family
            * `KenPom`_ - data origin (subscription required)

        .. _hoopR: https://hoopR.sportsdataverse.org
        .. _KenPom: https://kenpom.com
    """
    _caller_params = kwargs.pop("params", None) or {}
    _params = {
        "y": year,
        "s": attribute,
    }
    _params.update(_caller_params)
    raw = _get(
        "https://kenpom.com/game_attrs.php",
        params=_params,
        headers=headers,
        **kwargs,
    )
    if return_parsed:
        return parse_kenpom_page(raw, return_as_pandas=return_as_pandas)
    return raw
