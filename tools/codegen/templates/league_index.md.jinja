---
title: {{ prefix|upper }}
sidebar_label: {{ prefix|upper }}
description: "sdv-py {{ prefix|upper }}: endpoint references, dataset loaders and parsers for {{ prefix|upper }} in the SportsDataverse Python package."
---
# {{ prefix|upper }} (`sportsdataverse.{{ prefix }}`)

| Reference | Functions | Base URL |
|---|---:|---|
{% for a in api_rows %}| [{{ a.label }}](reference/{{ a.slug }}) | {{ a.count }} | `{{ a.base }}` |
{% endfor %}
{% if has_loaders %}| [Dataset loaders](reference/loaders) | {{ loader_count }} | {{ loader_base }} |
{% endif %}{% if has_additional %}| [Additional functions](reference/additional) | {{ additional_count }} | hand-written wrappers, loaders & helpers |
{% endif %}
{% if notebooks %}

## Examples

Worked examples — executed notebooks rendered as pages (refreshed weekly against the live APIs):

{% for nb in notebooks %}- [{{ nb.label }}]({{ nb.url }})
{% endfor %}
{% endif %}
{% if r_parity %}

## Python ↔ R parity

Each `sportsdataverse` function and its equivalent in the sister R package, [`{{ r_pkg }}`](https://github.com/sportsdataverse). Same-named where possible; the R column links the package's pkgdown reference.

| `sportsdataverse.{{ prefix }}` (Python) | `{{ r_pkg }}` (R) |
|---|---|
{% for row in r_parity %}| [`{{ row.py }}`]({{ row.py_url }}) | [`{{ row.r }}`]({{ row.r_url }}) |
{% endfor %}
{% endif %}
